# 2026-05-27

## 주요 작업

### socra-ai-workflow-guide — v0.1 초기 배포

프로덕트 디자이너를 위한 AI Workflow 운영 문서 구축 가이드 웹사이트 첫 배포.

```
# 프로덕트 디자이너를 위한 AI Workflow / 운영 문서 구축 가이드
이 문서의 목적, 접근 원칙, 문서 레이어 구조, 단계별 실행 플랜을 담은 가이드
```

**Claude Code 작업:**
- `ai-workflow-guide.html` / `.css` / `.js` 신규 작성 (Step 0~8, 실행 플랜, 반응형 사이드바)
- `index.html` 홈 페이지 생성
- `product-context.md` 작성 — 핵심 UX 원칙
- GitHub 저장소 생성 (`JumiJeong-design/socra-ai-workflow-guide`)
- v0.1 태그 → GitHub Pages 배포

---

### Step 0 문구 개선 + 코드 정리

```
Step 0의 "Figma에서" 표현을 "디자인 탐색 단계에서"로 바꾸고
```

**Claude Code 작업:**
- 도구 중립적 표현으로 수정
- 인라인 스타일 33개 → CSS 클래스 추출
- `changelog.html` 신규 추가 (버전 히스토리 페이지)

---

### 디자인 리뷰 피드백 반영 + AI 에디터 설정 섹션 추가

**Claude Code 작업:**
- HTML 중복 class 버그 수정, 버전 날짜 오타(`2025.05` → `2026.05`) 수정
- 코드 블록 복사 버튼, section-num 뱃지, `aria-current` 추가
- AI 에디터 연결 설정 섹션 신규 추가 (Claude Code / Cursor / Windsurf / Copilot)

---

### doc-comment 컴포넌트 추가 → 목적 재논의

```
아 그리고 혹시 내부에 댓글 달수도 있어?
아~ 너한테 말하라고? 난 다른 디자이너들이 보고 댓글달길 바란건데
```

- `doc-comment` 컴포넌트 추가했으나 Claude Code용 메모 → 다른 디자이너 협업 댓글은 별도 방식 필요 (미결)

---

### 버전 관리 구조 개선 + v0.2 → v0.3 배포

```
버전 별로 관리해서 v0.2로 쌓고 싶은데
변경된 내용을 확인할 수 있는 화면이 있으면 좋겠고
```

**Claude Code 작업:**
- `sidebar.html` 분리 — 3개 HTML의 중복 버전 선택기 단일화
- 모바일 safe area 전면 대응 (`env(safe-area-inset-*)`, `100dvh`)
- v0.3: 다크 모드, 제품 유형 자가진단, 5가지 확장 섹션 추가

---

## 커밋 (socra-ai-workflow-guide)

| 커밋 | 설명 |
|------|------|
| `60dd2ab` | feat: v0.1 — AI Workflow 가이드 초기 배포 |
| `c75c644` | refactor: 코드 정리 + changelog 추가 |
| `03fdda7` | fix: 디자인 리뷰 피드백 반영 4종 |
| `ac9d4be` | feat: AI 에디터 연결 설정 섹션 추가 |
| `89e1558` | v0.2 bump + 버전 선택기 Merge 충돌 해결 |
| `0e5588f` | v0.3: 다크 모드 + 자가진단 + 확장 섹션 |
| `810ecc4` | fix: 홈 카드 레이아웃 수정 |
