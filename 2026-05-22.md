# 2026-05-22

## 주요 작업

### Notion 업무 로그 자동화

> Notion 업무 로그 자동 저장 구현해줘

- `scripts/sync-devlog-to-notion.mjs` 생성 — devlog/*.md → Notion DB 자동 동기화
- Notion Integration `Socra_Rog Auto` 생성 + 토큰 연결
- DAX 로그 DB Integration 공유 후 연동 완료

---

### components.css 리팩토링

> 나머지 6개 화면 컴포넌트 인스턴스로 재조립

- 7개 screens/*.html의 중복 CSS → `components.css`로 추출
- storybook이 유일한 컴포넌트 CSS 원본, 화면은 링크 방식으로 통일

---

### CSS 토큰 3계층 + 하드코딩 전부 제거

> 하드코딩을 왜 하는거야? 안해야지

- `colors_and_type.css` Primitive/Semantic/Component 3계층 구조화
- 전체 파일 hex/px 하드코딩 0개로 완전 제거
- WCAG 2.1 AA: 모든 화면 aria-label, :focus-visible 적용

---

### Figma Variables 3계층 구축 + 싱크 플로우 확정

> 피그마 싱크해줘

- Primitives(25개) → Theme(36개) → Spacing+Radii(16개) 완성
- CSS ↔ Figma Variables 완전 정렬 확인
- "피그마 싱크해줘" 커맨드: Plugin API로 Variables 읽어 CSS 업데이트 확정

---

### Figma HTML/CSS 기반 편집 가능 화면 싱크 (Codex)

> html 원본으로 하든 html css 파일 읽어서 반영하든 해라

- 새 페이지 `HTML Sync - Component Synced KR` 생성
- 8개 화면 × 3 variant (Web 1440 Light/Dark, Mobile 380 Light) 반영
- 기존 Figma 컴포넌트 인스턴스 사용 원칙 확정

---

### i18n 자동 검사 + 배포 게이트

- `scripts/check-i18n.js` — 누락 키 있으면 Vercel 빌드 차단 (exit 1)
- QA 피드백 반영: 타이포그래피 토큰 개선, greeting 카피 변경

---

## 커밋 (socrachat)

| 커밋 | 설명 |
|------|------|
| `24dbdf8` | refactor: eliminate all hardcoded values; add a11y WCAG 2.1 AA pass |
| `f5ae9fe` | feat: translate pre-filled composer textarea on language switch |
| `bbde767` | feat: automated i18n coverage checker + deploy gate |
| `9208220` | fix: vercel.json — set outputDirectory to root for static site |
| `4dcb5b8` | chore: remove MONO theme, rename DESIGN.md to DESIGN-AP.md |
| `fad503f` | fix: add brand AI variable aliases |
| `ef6d223` | refactor: model icon avatars follow theme color |
| `e50a09d` | fix: restore brand AI vars for charts/bars/persona avatars |
| `df262a6` | fix: add missing components.css link to home.html |
