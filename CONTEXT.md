# 현재 상태 스냅샷

> 새 세션이 현재 기준과 위험 게이트만 빠르게 잡도록 돕는 파일.
> 상세 이력은 `logs/YYYY/MM/YYYY-MM-DD.md`와 각 repo git history를 본다.
> Last updated: 2026-08-26
>
> 이 파일은 **누적 이력이 아니라 스냅샷**이다. 날짜별 경과는 `logs/`가 정본이므로 여기에 날짜 섹션을 쌓지 않고, 현재 상태 한 벌만 덮어쓴다(2026-07-14 정리 — 6/12~6/23 날짜 섹션 6개가 누적돼 worklog와 중복되고 갱신이 밀리던 것을 접음. 2026-08-10 정리 — `Last updated` 줄에 세션 요약이 괄호로 6천 자까지 쌓여 날짜만 남김. **이 줄에는 날짜만 쓴다** — 세션 요약은 worklog로, 상태 변화는 아래 본문 섹션 덮어쓰기로 간다. 2026-08-11 전면 갱신 — 해소된 취소선 항목과 배포 초기 서사를 걷어내고 현행만 남김).

---

## 시작 루틴

1. `scripts/check-context-freshness.sh`를 실행한다.
2. 최신 worklog 1-2개와 필요한 public viewer plan을 확인한다.
3. 작업 대상 repo에서 `git status -sb`와 최근 커밋을 확인한다.
4. 피그마나 코드 write 전에 해당 repo의 최신 규칙 문서를 먼저 읽는다.
5. 문서 구조 변경은 `docs/00-document-role-map.md` 기준으로 진행한다.

`CONTEXT.md`가 최신 worklog보다 오래됐다는 경고가 나오면 이 파일은 stale snapshot으로 취급한다.

---

## 하드 룰

| 규칙 | 올바른 행동 |
| --- | --- |
| 계획은 실행 승인과 다르다 | plan/worklog/backlog 항목만 보고 피그마나 코드에 write하지 않는다. |
| 피그마 시각 변경은 승인 게이트를 거친다 | read-only 감사 → 변경안/영향 범위/rollback 범위 보고 → 주미님 명시 승인 뒤 write. |
| CONTEXT만 믿지 않는다 | 최신 worklog, public viewer, git 상태, 필요 시 Figma 실제 상태를 교차 확인한다. |
| N군데 동시 수정은 설계 냄새다 | 같은 데이터를 여러 곳에 추가해야 하면 구조 개선이나 단일 출처를 먼저 검토한다. |
| worklog 수정은 public viewer까지가 완료다 | 원본 MD, `site/worklog.html`, commit/push, 공개 URL, 월 단위 검증까지 확인한다. |
| 넓은 요청은 먼저 범위를 나눈다 | 분석/계획/수정/배포/승격을 한 번에 섞지 않고, write가 필요한 지점을 분리한다. |

---

## repo 역할

| repo | 역할 | 현재 기준 |
| --- | --- | --- |
| `jumi-worklog` | 날짜별 raw worklog, 세션 스냅샷, 공통 스킬 | 원본 기록. 중복 초안은 커밋하지 않는다. |
| `riiid/prism` / `socraAI_product design` | Prism package, 디자인 시스템 계약, token, Storybook, release | 최종 반영 대상은 `https://github.com/riiid/prism`. 로컬 `socraAI_product design` 원격 상태는 작업 전 재확인한다. |
| `socra-ai-workflow-wiki` | 반복 가능한 AI workflow 지식과 public viewer | 운영 교훈만 승격. 제품/컴포넌트 계약을 중복하지 않는다. |

---

## 현재 상태 (2026-08-26 기준)

### 운영 모드

- **Claude Code 단일 에이전트** 체제다(2026-07-13~, Codex 병행 중단). 브랜치 전략은 main-first: 짧은 feature 브랜치 → PR → main. 장수 병렬 트랙(`plan/parallel-codex`류)은 새로 만들지 않는다.
- 같은 세션에서 병렬이 필요하면 서브에이전트 + `git worktree` 격리를 쓰되 팬아웃은 3~4개 소규모가 기본이다. **감사·리서치류는 인라인이 기본**(7/10·7/13 대규모 팬아웃이 두 번 다 세션리밋에 걸렸다).
- **완료 보고는 3단계로 한다(8/19 신설).** 에이전트는 "완료"를 단독으로 쓰지 않고 `1차` / `공유 가능` / `픽스` 단계명 + 남은 것을 문장으로 남긴다. `공유 가능`은 자가 검사 7항목을 첨부했을 때만, `픽스`는 디자이너만 선언한다. 기준 정본 `docs/20-completion-criteria.md`, 진입 규칙 `AGENTS.md` 「완료 기준」. 배경 — AI의 완료(기획이 그려짐)와 디자이너의 완료(품질·UX 검토 끝)가 달라 남은 일이 기록 없이 사람 머릿속에만 쌓이던 것.
- 2-에이전트 병렬 규칙은 휴면 상태로 보존돼 있다(정본: `riiid/prism`의 `AGENTS.md`). `socra-ai-workflow-wiki`의 핸드오프/컴포넌트 플레이북과 `AGENTS.md`도 2026-07-16에 같은 (휴면) 패턴으로 맞췄다.

### 제품 파일 DS 정합 (`🌍Socra AI Product_PageUI_MVP`)

- **손코딩이 대부분 걷혔다(08-16~18).** 로그인 모달 19장 → `Modal Shell` · 손코딩 시트 16장 → `Sheet/Shell` · `Chat History Panel` 4장과 `QuickStart/Group` 18개 → DS 인스턴스. `History Item` 64개는 패널 교체로 함께 소멸했다.
- **경계는 「껍데기=DS · 본문=제품」이고, 08-19 감사로 기준이 한 겹 더 갈렸다.** 본문 소유는 "본문이냐"가 아니라 **무엇이 붙어 있냐**로 가른다 — 부품·문구가 전부 DS면 DS, 제품 더미 콘텐츠나 외부 라이브러리가 붙었으면 제품이다. 제품 쪽 `Sheet Body` 9개는 전용 섹션(`9266:164437`)에 수납돼 있고 전부 DS `Sheet/Shell`의 본문 슬롯 안에서만 쓰인다(총 12 인스턴스, 떠도는 것 0). 컴포넌트가 된 이유는 재사용이 아니라 `본문`이 **INSTANCE_SWAP**이라서다.
- **08-18 「본문=제품」의 근거 두 개가 08-19 실측으로 뒤집혔다.** ①"재현하면 흘린다"는 2955px 전문 시트에만 해당하고 `모델 선택 리스트`(71노드)·`로그인 게이트`(26노드)는 **로컬 부품 의존 0** ②`strings`(KR/JP/EN 74개)는 제품 로컬이 아니라 **DS 소유**라 승격해도 문구 바인딩이 안 끊긴다. 결론: **`로그인 게이트`는 DS 승격 권장**(DS에 `Modal Shell`+`Modal Content /` 6종 선례가 있어 지금은 로그인 본문이 모달=DS·시트=제품으로 갈려 있다), **`모델 선택 리스트`는 비권장**(5행이 제품 더미 + `ticker / tsla`가 외부 `[VW] Design System` 부품).
- **선택지 제목 문구 정합(08-19).** `모델 선택 리스트` 제목 `옮긴다` → `'옮긴다'`, 설명은 정렬만 말하던 것을 `이 선택을 지지한 AI를 지지율 순으로 보여드려요`로. 제목은 **선택지 텍스트(동적 데이터)**라 인용 부호는 문구 변수가 아니라 **렌더 규칙**이고 글리프가 로케일마다 다르다(JP `「」` · KR `''` · EN `""`). 마스터 1곳 수정으로 화면 2곳이 따라왔고 348/458/853 전부 불변.
- **DS 신설·변경**: `Modal Shell`(Back 축) · `Modal Content` 5종 · `Deep/Footnote` · `QuickStart/Group` · `Sheet/Shell` **hug 전환 + `header` on/off 축** · `layout/safe-area/bottom = 34` · **`Thinking/Progress`(`stage` 6값, 진행바를 축으로)** · **`Deep/VoteCard` `State=answered`**(지나간 카드 — 표면 `neutral/100`·버튼 없음) · `Deep/VoteCard` result 라벨 **「다음 고민」** · **마스터에 남아 있던 클램프 해제**(`textTruncation=ENDING·maxLines=7`, result는 `maxLines=3`까지 — 안 고쳤으면 새 인스턴스가 계속 7줄로 태어났다).
- **토큰 바인딩 현황**: 두 페이지 하드코딩 텍스트 색 **0건**. 간격·패딩·radius **1,974개** 바인딩(값 일치분만이라 시각 변화 0). 남은 미바인딩은 스페이서 해킹(`gap 103`·`454`)과 기기 목업(iOS 상태바·키보드)이라 대상이 아니다.
- **로그인·가입 에러 표현이 전 구간 컴포넌트다(08-18).** 귀속을 셋으로 갈랐다 — 필드=`Text Field error` · 인증코드=`OTP Input error`(**메시지 행 신설**) · 폼 전체=**`Form Error` 신설**. 모바일·웹 손그림 0. 로그인 실패는 §3.1(계정 존재 비노출)대로 **두 필드를 모두 error**로 둬 "어느 쪽인지 특정 안 함"을 시각에서도 지킨다.
- **에러 색·크기 확정(08-18)**: `semantic/text-error` Light를 **red/600**(#cf483e, 4.52:1)으로. red/500은 3.71로 본문 AA 미달이라 테두리·아이콘 전용이다. 에러 문구 크기는 **15**(`body/md-r`)로 통일.
- **입력 표면 규칙(08-18)**: 폼 = 흰 바탕 + 회색 입력(`neutral/50`) / 목록·허브 = 회색 바탕 + 흰 행. 비밀번호 변경 2화면이 목록 규칙을 입고 있어 입력칸이 카드로 읽히던 것을 폼 규칙으로 정정 — Text Field 35개가 한 규칙이 됐다.
- **Deep 사고과정 = 시안 1로 확정, DS 마스터 반영·게시·수락까지 끝났다(08-19).** 마커 24 슬롯(서클 18 `neutral/200` + `neutral/50` 외곽선 1.5 — rail을 끊어 띄운다) + 관통 rail `neutral/150` + 도착 슬롯 카드 `neutral/100`. **파랑은 현재 단계·진행바·헤더 `{k}명`에만.** 헤더에 `LeadIcon`(`lucide:list-checks` 16) 추가, `StepText`는 HUG + **maxWidth 245**(아이콘이 들어와 269에서 재계산). 도착 줄은 §3.4 원문 `{AI 이름}의 의견이 도착했어요`(마침표 없음)이고 **이름은 모델명·에이전트명이 섞인다.** **펼침은 지난 이력만 — 남은 단계를 미리 노출하지 않는다.** Chat·Quick 펼침도 같은 마커, rail만 Deep 전용. 마스터 `4625:2316` 실측 Deep 펼침 361×304, 제품 파일 전파 96/96·`visible` override 27개 생존. 근거판 `9280:110814`, 기록 plan-27 + `design-system/components/thinking-toggle.md`.
- **홈 PC 폭 기준이 채팅과 같아졌다(08-19).** 채팅에만 있던 3폭 기준을 홈에도 세웠다 — `home / Tablet 1024`(`9500:3557`, zone 76·콘텐츠 948·좌우 74) · `home / Desktop 1440 · rail 접힘`(`9500:3886`, zone 76·콘텐츠 1364·좌우 282). **홈 히어로도 뷰포트가 아니라 콘텐츠 영역 기준 중앙**이라 rail을 접으면 1440에서 중심이 858 → 758로 100px 움직이는데, 이건 감수한다 — 홈만 뷰포트 중앙에 걸면 전송 직후 홈 컴포저 → 채팅 컴포저에서 138px이 튄다. 원본 `web / home`도 폭 변수 바인딩 + 패널 세로 FILL로 정리(렌더 해시 동일 = 시각 변화 0), `web / history` 4장은 이미 정합이라 수정 0. 칩 행 Edge Band는 **웹/태블릿 불필요**(800 컬럼 여유 414~479px) · 모바일 393만 유효(여유 42·72·7). DS Guidelines L06 예시에 Tablet 카드 추가.
- **홈 칩 행 가로 스크롤 규격이 폭 무관 식으로 확정됐다(08-19).** 8/12의 "마지막 칩이 걸쳐 보이는 게 스크롤 신호"는 실측에서 작동하지 않았다 — 잘린 양이 카피 길이에 종속돼 0~75px로 흩어지고, JP 커리어·대인관계는 **스크롤이 없는데도 잘려 보였다**(가짜 신호). 참여자 아바타의 같은 규칙은 원이 다 같은 크기라 통하는 것이라 칩에 못 쓴다. 확정 규격 = **클립 폭 `컨테이너 − R`** · 콘텐츠 `padding-left` `R` · 넘침 기준 `컨테이너 − 2R`(`R`=레일 16) · 클립 높이 = 칩 높이 + 위 12/아래 14(**폭과 무관**, 그림자 blur 12·offset (0,2)에서 나온 값) · 페이드 `EdgeBand` `left|right` **40 고정**(클램프 없음). 폭별 360→344/328 · 393→377/361 · 800→784/768. **오른쪽 16은 콘텐츠 패딩이 아니라 뷰포트 인셋**이라 스크롤 여부와 무관하게 항상 비고, 끝까지 밀면 나오는 게 여백이 아니라 칩의 남은 부분이다. **360에서는 KR 대인관계(354)도 넘친다**(393에서 여유 7px). DS `Edge Band`에 `Edge=left|right` 축 추가(variant 8, 컴포넌트 key 불변), `QuickStart/Group` 마스터는 `chips` 절대배치 + 가로 `STRETCH`. 기록 plan-10 「칩 행 스와이프 가이드」 + 시안 보드 `9469:127503`.
- **피드백 사유 시트가 두 세트로 갖춰졌다(08-24).** 기획서 §4.10은 사유를 대상별로 가른다 — 세트 A(리포트·Chat 종합·결정) / 세트 B(개별 AI 답변 품질). **피그마에 둘 다 있었고**, 세트 B에 없던 진입 직후 화면 2장을 신설했다(모바일 `9799:60415` · 웹 `9788:47302`). 같은 이름 `피드백` 6장은 사본이 아니라 **6개 상태**여서 이름을 상태별로 갈랐다(`피드백 · 종합 사유 — 미선택` 등). **미선택 상태에서 보내기가 enabled인 것도 기획서와 일치**한다(사유는 선택 사항). `Survey/Option`의 `기타 (직접 입력)`↔`자유롭게 의견을 남겨주세요`는 카피 차이가 아니라 `State=other`↔`input-focus`다.
- **본문 컴포넌트는 상태로 가르고, 카피 세트는 인스턴스 override다(08-24 확정).** 라벨 몇 줄 때문에 컴포넌트를 새로 만들면 세트가 늘 때마다 배로 불어난다(세트 B가 나머지 상태를 가지면 라벨 5줄 차이로 컴포넌트 10개). 당일 만들었던 카피용 본문 2개(제품 로컬 `9799:60372` · DS `5149:2470`)를 **같은 날 걷었다** — 화면은 공유 본문 + 라벨 override로 되돌렸고 시각 변화 0. **DS 삭제 반영에는 재게시가 필요하다.** 기존 건 `9252:35550`(세트 B 포커스)은 쓰이는 화면을 건드려서 plan-33으로 넘겼다.
- **대기 투표 카드의 이름을 정했다 — 화면 노출용 「모두의 고민」(JP `みんなのお悩み`) · 각주 지시어 「이 고민카드」(붙여쓰기)(08-24 확정).** 각주 `리포트가 완성되면 이 화면은 사라져요`가 무엇을 가리키는지 모호했는데, 사라지는 건 화면이 아니라 카드 한 장이고 더 근본은 카드가 「남의 각색 사연」임을 화면에서 한 번도 말하지 않은 것이었다. **내부 용어는 기획서대로 `사연`·`투표 카드`·`대기 중 투표`를 쓰고, 새로 만든 건 화면 노출용 이름 하나뿐이다.** 후보였던 「로딩 질문」(자리·시점을 이름으로) · 「스낵 질문」(JP `スナック`=술집) · 「랜덤 고민」(§4.7 노출 규칙은 랜덤이 아니라 도메인 매칭) 셋 다 폐기. 제품 파일 `strings`에 `deep/wait-loading-title`·`deep/wait-footnote` 신설(KR/JP/EN) + 5곳 바인딩, DS 마스터 2건 교체·게시·수락 완료. 기록 `docs/plans/34-deep-wait-copy-term-2026-08.md`.
- **각주 노출 규칙 = 마지막 카드 하단 붙박이(08-24 확정).** 정해진 적이 없어 모바일은 빈 카드 1장에만·PC는 3장 전부에 있었다. 대기 영역은 고정 블록이 아니라 투표한 카드가 쌓이는 **누적 스트림**이라 맨 아래 카드를 따라다니게 뒀다 — 모바일 17장 적용, 검증 불량 0. **각주가 첫 화면을 넘어가는 건 결함이 아니다** — 카드가 넘치는 프레임은 선택지·건너뛰기를 보려면 어차피 스크롤하고 그 끝에 각주가 있다. 제외 12장(완성 알림 5 = 시제 충돌 · 360폭 길이 스터디 7 = 재는 값이 달라짐). 그리고 **각주는 기획서가 준 게 아니다** — 본문 §4.7에 조항이 없고 기획안 와프(`1532:19224`)에서 왔다(`rules.md` §33 적용 대상).
- **실패 화면 문구가 기획서 v1.0.7 §5.1로 전수 정합됐다(08-24).** `Answer/Error` 인스턴스 **15곳 실측 → 9곳 교체**. 케이스별 정본은 셋뿐이다 — 리포트 계열 `리포트를 만들지 못했어요. / 다시 시도해 주세요` · 3회 소진 `반복적인 오류로 {답변|리포트}를 받지 못했어요. / 페이지를 새로 고침 하거나 나중에 다시 시도 해주세요` · 그 외 `답변을 받지 못했어요..`(마스터 기본값). **원인별 카피를 쓰지 않는다**(§5.1 "안내 문구는 원인과 무관하게 동일") — `여러 AI의 답변을… 더 많은 답변을 받고싶다면` 같은 케이스 전용 문구를 걷었다. **3회 소진은 버튼을 지우지 않고 `State=exhausted`로 남긴다** — v1.0.6이 Quick 리포트만 예외로 버튼 제거를 시켰던 것을 v1.0.7이 폐기했고, 그 잔재였던 `quick_리포트 3회 실패`(손코드 프레임)도 `Answer/Error` 인스턴스로 교체됐다. 운영 노트 `7818:17631`도 v1.0.7 기준으로 재작성(구 예외를 폐기로 명기). plan-32 트랙 종결 · 기록 plan-10 2026-08-24 절.
- **에러 화면은 면책 문구를 띄우지 않는다(08-24 주미님 확정).** 에러 계열 15장의 AnswerFooter 면책("AI는 실수를 할 수 있습니다…")이 `opacity 0`인 것은 결함이 아니라 **의도**다 — "애초에 답이 안 나왔는데 면책 문구가 뜨는 게 이상해서 가려둔 것". socra 마크만 남는다. **감사에서 드리프트로 세지 말 것**(정상 화면 49장은 `opacity 1`이라 한쪽으로 깨끗이 갈린다 = 규칙의 표식).
- **남은 것**: `socra` 마크 흰색 41건은 DS 마스터 소관이고, 온컬러 흰색(소셜 ✕)은 `neutral/white`가 다크에서 뒤집혀 **모드 불변 토큰**이 필요하다.

### `riiid/prism`

- **리포트 본문과 평가 행 사이 12px은 `Report/Feedback`이 자기 `paddingTop`으로 낸다 — 담는 쪽은 gap을 주지 않는다(08-26, `rules.md` 45번).** 마스터가 `size/12`에 바인딩해 갖고 있어 컨테이너에도 12를 주면 **더해져 24가 된다.** 어느 쪽을 열어도 「12로 맞춰 놨다」로 읽혀 눈으로는 안 잡히고, 인스턴스의 `paddingTop`은 마스터 값을 물려받아 전수가 동일하게 보인다 — **판정은 `컨테이너 itemSpacing + 인스턴스 paddingTop`으로 한다.** 제품 화면 23개가 이 상태였고 8/26에 컨테이너를 0으로 통일(전수 30개 `{0: 30}` 수렴). **코드도 같은 이중 계산이었다**(gap 12 + `pt-4` 16 = 28) — `report-deep.footer-gap` 토큰을 **제거**하고 `ReportFeedback`을 `pt-3`으로 내렸다(PR #251). 토큰을 0으로 안 두고 없앤 건 `size` 램프에 0이 없어서다.
  - **상단 구분선은 없다.** 피그마 마스터에 `strokeTopWeight: 1`이 남아 있지만 stroke paint가 없어 안 그려지는 잔재다. 평가 행이 리포트 카드 **밖 형제**라 선을 그으면 배경 위에 뜬다. 코드 `border-t`도 8/26에 걷었다.
  - **폭이 328·360·361·393·560으로 갈린 건 드리프트가 아니다** — 30개 중 29개가 `FILL`이고 부모 안쪽 폭과 불일치 0건이다. 마스터의 328 고정은 세트 안 기본 크기라 소비처에 영향이 없다. **`FILL` 여부를 안 재고 폭 숫자만 보면 오진한다.**
- **`EdgeBand` 램프 — `Edge=top`만 짙은 끝이 «평탄»하다(08-25 개정). 블러는 두 번 다 탈락했다.** 비-`top` 3방향은 중간 스톱을 가운데(0.5)가 아니라 **옅은 쪽 0.75**(CSS 25%)에 둔다(PR #219). `top`은 여기에 더해 **h160 + 짙은 끝 37.5%(= 헤더 60px)까지 `fade-90` 평탄**이다(PR #247, 마스터 `4432:2061`·`4676:2167`). **같은 증상을 세 번 고쳤다** — 50% → 25%(제목 자리 알파 0.68→0.76) → top 평탄(0.90). 앞의 둘이 원인을 스톱 «위치»로 봤는데 실제로는 **평탄 구간 부재**였다: 평탄 구간이 없으면 헤더가 얹힌 60px이 어떤 스톱 배치에서도 페이드 한복판을 못 벗어난다. **두께를 줄여 쓰면 평탄 구간도 같은 비율로 줄어든다**(스톱이 %라서). 피그마 세트(`4432:2063`) 8 variant + 코드 반영·게시 완료. **블러는 넣지 않는다** — 8/11 progressive, 8/24 균일로 두 번 시도해 두 번 다 텍스트에서 졌다(균일은 띠 끝에 흐림/선명 경계를 만들어 본문 행을 반으로 자른다). **하단 페이드는 `EdgeBand`가 아니다** — `ChatHistoryPanel` 하단은 자체 `FadeGradient`(2스톱 0.1→0.5)이고 유리 푸터 위에 얹히는 자리라 통일하지 않는다.
- **첨부 실패 칩은 '프리뷰=데이터'로 확정됐다(08-14).** 이미지 실패도 `src`가 오면 썸네일을 그리고, 프리뷰를 만들 수 없는 실패(사유 `열 수 없음`)만 회색 기본 면 폴백이다 — 컴포넌트는 실패 사유를 모른다. 대비는 스크림이 아니라 **아이콘 색**이 맡는다(사진 위 ⚠는 흰 선 `attachment.error-icon-on-media`, 폴백은 `color-danger`). **스크림이 탈락한 이유는 대비가 아니라 `error-retry`가 `processing` 칩과 구분되지 않기 때문**이고, 흰 판이 탈락한 이유는 판이 사진 가운데를 가리기 때문이다. DS 마스터(`4189:1909`·`4189:1919`)·게시·제품 인스턴스 9건까지 반영됐고 보드2 `열 수 없음` 칩만 override로 회색 고정. 코드는 PR #157 브랜치의 `baf8853`.
  - **08-18 정합 4건.** ↻도 흰 선(위 종결 항목) · 실화면 3장의 아이콘 오배정(`열 수 없음`인데 ↻ → ⚠) · 상태 아이콘 stroke 1.5 → **1.8**(코드 + 피그마 PDF 변형을 `stroke/width/icon` 재바인딩) · processing 스피너 32 → **24**(컴포넌트 토큰 `attachment.processing-spinner`, `Spinner` 램프엔 24가 없어 크기만 덮음) · hover 스크림도 `src` 조건으로 일반화. **Chromatic 재게시만 남았다** — DS 재게시는 불필요(이미 게시된 세트의 in-place 수정이라 제품 인스턴스가 바로 따라온다, 08-18 양쪽 실측).
  - 근거는 8/11 개발 답변("실패하면 프리뷰를 못 만든다")이 **8/12에 절반 정정**된 것이다 — 손상 파일만 불가고 업로드 실패·용량 초과는 프리뷰가 만들어진다. 기획서 §3.4는 **에러 칩의 썸네일 여부를 규정하지 않는다**(디자인 결정 사항). 대신 전송 잠금의 목적이 "실패한 파일을 모른 채 보내는 일을 막기 위함"이라 식별은 캐논 요구다.
- **최신 배포는 `@riiid/prism@0.20.0`(2026-08-24 밤).** 대기 중 changeset **0건**이다. 그날 하루에 `0.17.0`~`0.20.0`이 연속으로 나갔고, 마지막 둘은 `0.19.1`(계약 md 노드 인용 정정 · 코드 변경 0)과 `0.20.0`(`DeepCTA` `agentName`·`suffix` 공개 prop + `--text-heading-0`·`--text-26` 등 공개 토큰 4종)이다. **`changeset` 누락은 CI가 안 잡는다** — 필수 3종(validate·check·storybook)은 changeset 유무를 안 보므로, 패키지 `src/`를 건드렸으면 머지 전에 손으로 확인한다(8/19 `#171`, 8/24 오전 `DEFAULT_EMPTY_TITLE`, 8/24 저녁 `#220` — 같은 자리에서 세 번 났다).
- **8/13부터 막혀 있던 PR 스택이 전부 main에 들어갔다(08-18)** — #157(edge-scrim 87커밋) · #158(ActionBar 복사됨) · #165(P1 감사 후속, #159 대체) · #164(전체 비교 CI 정정) · #166(glow 폐기). `feat/edge-scrim-exploration`은 아직 살아 있고 미커밋이 쌓여 있다.
- **스쿼시 스택 함정** — 앞 PR이 스쿼시로 머지되면 뒤 PR이 *이미 main에 든* 커밋을 그대로 물고 있어 통째로 가짜 충돌(DIRTY)이 된다. 고유 커밋만 main 위로 옮겨 담으면 풀린다(08-18 실측: 101→7파일 · 106→12파일). 그리고 **`--delete-branch`로 머지하면 그 브랜치를 base로 쓰던 PR이 자동 close되고, 닫힌 PR은 base를 못 바꾼다** — 스택에서는 마지막에만 쓴다.
- **컴포저 포커스 글로우는 `ChatInputBar` 내장이다(08-12 승격, `glow` 기본 `true`).** `:focus-within`에서 `color.gradient.brand` 링 + 정적 halo(0.13)를 켠다. 링은 Deep이 쓰는 `GradientGlow`를 `shimmer={false}`로 재사용하고, `deepActive`면 링을 생략한다(두 겹 방지 — 그때는 halo만). **홈 전용이 아니라 컴포저 공통**이라 채팅 컴포저도 포커스에서 글로우를 갖는다(피그마 원본이 `Bottom Bar` `6756:83751`). 끄려면 `glow={false}`.
- **홈은 모바일·웹 모두 정합이 끝났다(08-12).** 정본 = `Screen / home (mo 393)` `8605:2972`(로컬 컴포넌트 — 히스토리 11장·QA 사본이 자동 추종) · `web / home` `7860:3270`. **모바일과 웹의 차이는 칩 위치 하나**(모바일=컴포저 위 / 웹=아래, 기획서 §4.1)이고 서브카피 자리도 그래서 다르다. 남은 건 기획 대기 2건뿐 — 칩 9종 변환 문장(급하지 않음)·딥링크 파라미터↔홈 요소 매핑.
  - **짧은 뷰포트(393×664)는 스크롤을 받는다(08-13).** 무스크롤 최소 높이가 **709**(히어로 231 + 칩 248 + 간격 20 + 바 210)라 664에 45가 넘친다. 간격만 긁으면 685가 바닥이고 그 밑으로 내리는 항은 로고 60박스뿐이다. **`Bottom Bar`의 fade 50을 깎아 맞추려 하지 말 것** — 그건 스페이서고 `Dock` 210/202는 08-12 승인 수치다(마스터 8 variant를 고쳤다가 원복했다).
  - **그래서 홈 마스터는 '떠 있는 크롬'이다(08-13 전환).** `Header Nav Bar`가 절대배치 오버레이이고 히어로가 상단 60을 `padding-top`으로 돌려받는다(안 돌려주면 로고가 햄버거와 **정지 상태로** 겹친다 — 스크롤 문제가 아니다). 852에서 시각 변화 0을 마스터+인스턴스 14장 전수로 확인. **코드는 아직 안 따라왔다** — `HomePage.stories.tsx`는 옛 모델이고 참고 형태는 `ChatPage.stories.tsx`의 `MobileChat`.
- **코치마크 3종은 v1.0.6 기준으로 제외다(08-12 결정).** `❌ Test(디자이너 고민했던)`로 이동. **유래 = v1.0.3 §4.11 온보딩**(0715 Weekly에서 삭제) — "기획서에 없던 제안"이 아니다. 되살리면 §4.11 수용 기준 2번이 스킵/닫기 UI를 강제한다.
- **로딩 인디케이터 선택 규칙이 생겼다(08-13, `rules.md` 39번).** 4단계 사다리 = 콘텐츠 대체 > 결정형 진행 > 스켈레톤 > 스피너. **스피너는 기본값이 아니라 폴백**(유일하게 아무 정보도 안 주는 인디케이터). 현행 6곳 판정 = 맞음 3 / 틀림 3. 이에 따라 `Empty State`에 `action`·`media` 불리언(둘 다 기본 false)을 신설해 3층 구성이 가능해졌고, `Chat History Panel`에 `type=loading`(스켈레톤 행)을 신설해 히스토리 목록 로딩·실패 화면을 컴포넌트로 옮겼다. **룰 35(Deep 진행바 폐지)를 이 규칙으로 되살리지 않는다** — 차단 문구가 39번 안에 있다.
- 배포 경로는 **GitHub Actions + npm Trusted Publishing(OIDC)**이다. latest = Version PR을 `main`에 머지 / canary = `canary/**` 브랜치 push + changeset. 정본은 `docs/release.md`.
  - 패키지는 `--access restricted`라 **미인증 `npm view`가 E404를 내는 게 정상**이다 — 발행 실패로 오진하지 않는다. 확인은 워크플로 로그로 한다.
  - **빌드 산출물을 저장소에 커밋하지 않는다**(산출물 브랜치 `prism-dist`는 07-31 은퇴).
- **Tailwind `text-*` 크기 유틸리티가 08-11부터 `--font-size-*` 토큰값으로 렌더된다(#123).** 그전엔 `@theme` 미등재로 Tailwind 내장 기본값이 나가고 있었다(`lg` 18≠17, `2xl` 24≠22, markdown h1 36≠28, `text-13`류는 규칙 미생성 — 패키지 42곳). `build.ts` `publicThemeTokens`에 별칭 16종 등재로 일괄 교정. **새 크기 토큰을 추가하면 `--text-*` 별칭도 같이 등재해야 유틸리티가 생긴다.** 정본 plan-23 「text-* 전수 실측」 절.
- **리포트 히어로 결론 헤드라인은 전용 역할이다(08-24).** `heading/0`(모바일 **24**) ↔ `heading/web-0`(웹 **26**) — 범용 `heading/1`(22)은 제품 157회라 값을 올리면 카드·섹션 제목까지 따라 커져서 위 단계를 새로 팠다. 코드는 `text.heading-0`(mo `{font.size.3xl}` / pc `{font.size.26}`) 하나로 받고 `report-deep.title-text`·`report-quick.title-text`가 참조한다. **26은 t셔츠 램프에 없는 off-scale이고 웹 한 자리에서만 쓴다.** mo/web 짝이 24↔26으로 좁은 건 리포트가 웹에서도 488~520px 컬럼이라 디스플레이급으로 벌릴 자리가 아니어서다 — 28·32는 렌더해서 탈락시켰다. **반응형 역할 토큰은 레이아웃이 같이 반응할 때만 붙인다**(`ReportQuick`이 `max-w-[361px]`로 고정돼 있던 걸 근거로 크기를 정했다가 뒤집었다 — 그 고정폭 자체가 Figma와 어긋난 드리프트였다).
- **화면 가장자리 페이드 = `EdgeBand`(08-11 신설, PR #143 대기).** 스크롤 콘텐츠가 위/아래 끝에서 표면색으로 잦아든다. 색은 `color.bg.surface-fade-{10,50,90}`(→ `--color-surface-fade-*`), 피그마 `Edge Band`(`4432:2063`) + 변수 `semantic/surface/fade-*`(게시 완료). 등장 판정은 **소비처가 `scrollTop > 0`으로** 넘긴다. **progressive blur는 탈락**(부분 블러가 한글에서 렌더 결함으로 읽히고, 색의 무게를 안 없앤다) — 코드는 탈락 기록으로 남아 있으니 되살리지 말 것. 모바일 하단은 `BottomBar`가 같은 램프를 이미 깔아 쓰지 않는다.
  - 이름이 `scrim`이 아닌 이유: `color.overlay.scrim`은 **모달 뒤 검정 dim**이다. 가장자리는 dim이 아니라 페이드다.
  - **띠 높이 = 덮을 크롬 + 페이드 50(08-13 규칙화).** 화면 상단은 헤더 60 + 50 = **110**(마스터 기본값), `ChatHistoryPanel`처럼 위에 크롬이 없는 목록 상단은 **50**. 페이드 구간 50은 `BottomBar` fade와 같은 값이라 위아래가 대칭이다. 피그마 채팅 컷만 164로 튀어 있던 걸 110으로 정합했다. **"본문 행이 잘려 읽히니 키운다"는 기준으로 되돌리지 말 것** — 이 컴포넌트의 일은 "덜 보이게"가 아니라 "덜 중요해 보이게"라 읽히는 것 자체는 결함이 아니다(08-13에 한 번 그렇게 적었다가 되돌렸다).
  - **08-12에 `surface: muted | raised` 축이 붙었다(코드 선행). 피그마 세트에도 08-13 실측 기준 축이 있다(variant 4개)** — 계약 문서의 "아직 피그마에 없다"는 줄은 낡았다(피어 미커밋 hunk 안이라 미수정). 페이드는 덮는 색이 아니라 **바탕색으로 사라지는 것**이라 표면이 다르면 기준색도 달라야 한다 — `surface-fade-*`(#F2F4F8 기준)를 흰 패널에 쓰면 페이드 끝에 회색 끼가 남는다. 흰 표면용은 `color.bg.surface-raised-fade-{10,50,90}`.
- **히스토리 패널은 목록 경계를 위아래로 갖는다(08-12 신설).** 아래 = 푸터 위 fade 50(10→50%, 피그마 마스터 `FadeGradient` + 코드 `chat-history-panel.fade-height`/`scrim.10|50`), 위 = `EdgeBand edge="top" surface="raised"`를 `scrollTop > 0`일 때만. **유리 푸터는 재질이지 경계가 아니다** — 그것만 있으면 행이 또렷한 채로 유리 밑에 들어갔다 반대편에서 다시 또렷해진다. 상단 띠는 피그마 마스터에 넣지 않는다(정적 프레임이 스크롤을 표현 못 하고, 넣으면 첫 행이 항상 덮인다).
  - **그라디언트도 변수로 다크를 가를 수 있다** — 막히는 건 스톱에 알파를 남기는 것이지 바인딩이 아니다. **알파를 품은 토큰**(`semantic/surface/elevated-fade-10|50` `4663:2`·`4663:3`)을 걸면 게시 경계까지 넘어간다(제품 파일 7장 실측). 알파 없는 색 변수를 걸면 1로 리셋돼 페이드가 판때기가 된다.
- **첨부 라이트박스 이미지는 안전영역 안에서 중앙 정렬한다(08-11, PR 대기).** "최대 뷰포트 90%"만 걸면 세로 사진이 하단 고정 캡션을 15.8px 침범한다 — 카운터·캡션이 있을 때만 위아래를 비우고(`attachment-lightbox.safe-top` 60 · `safe-bottom` 83) 그 안에서 중앙 정렬한다. 캡션 기준을 이미지 아래로 되돌리는 대안은 폐기(하단 고정을 세로에서만 뒤집게 됨). 피그마 마스터 4종 반영은 남아 있다.
- **첨부 실패 칩은 썸네일을 보여주지 않는다(08-11 확정).** 첨부가 실패하면 프리뷰 자체를 만들 수 없다(개발 확인) — 이미지형 `error`/`error-retry`는 `src`를 받아도 그리지 않고, 사진 없는 기본 면(`bg-surface-muted` = 피그마 `neutral/50`) 위에 빨강 보더와 아이콘만 남는다. 스크림 토큰 `attachment.scrim.error`는 제거. 피그마 마스터(`4189:1909`·`4189:1919`)·게시·제품 인스턴스 9건까지 반영 완료, 코드는 PR 대기(`feat/attachment-failed-preview`). `error-retry`는 MVP에서 안 나올 수 있지만(디바이스→웹 업로드는 네트워크 요청 없음) 주미님 판단으로 마스터·규칙은 보존한다.
  - **함정**: `importComponentByKeyAsync(key)`는 로컬 마스터가 아니라 **게시 스냅샷 프록시**를 돌려준다 — 마스터 실측에 쓰면 이미 고친 것도 "미반영"으로 오진한다.
- **History Item 시각은 상시 표시가 캐논이다(08-11 B/C 모순 해소).** plan-23=B안·계약=C안(웹 숨김)이 같은 날짜로 반대 기록돼 있던 것을 §4.9 문자+승인 시안+실물 실측으로 상시 표시로 판정 — 계약 재작성, `HeroSidebar`·`WebSidebar`의 `showTime={false}` 잔재 제거. loading 행은 스피너+··· **비활성 노출**(`semantic/disabled/fg`, §4.9 "삭제 버튼을 비활성"), engaged ···의 primitive `neutral/400` 직결 드리프트를 `semantic/icon-default`로 복귀(활성 #53555C/비활성 #84878F 단계). **DS 게시 완료·전파 실측(08-11 밤)** — engaged ··· 색은 자동 전파됐고, loading 행만 시안 제작 때 박힌 슬롯 숨김 override가 마스터를 덮고 있어 저장→`resetOverrides()`→재적용으로 복구했다. 다크는 활성 #A0A3A7 / 비활성 #86888C로 단계가 좁아 **hover 툴팁을 채택**(`ChatHistoryPanel`이 loading 행을 기존 `Tooltip`으로 감싸고 `copy.loadingTooltip` 3 locale · 아이콘 배지는 기각 유지). 코드 3커밋은 **PR #151 OPEN** — 빌드 게이트 전부 통과, **[주미님 손] Chromatic 베이스라인 1건 승인 필요**(신규 `GeneratingRow` 스토리) 후 머지 — 08-12 재실행에서 `check`·`validate`·`storybook`·`UI Review`(no changes) 전부 pass 확정. 원본 SHA는 `feat/edge-scrim-exploration`에 잔존(피어 공유 브랜치라 미정리 — 그 브랜치 PR 시 rebase로 거른다).
- **Chromatic은 월 스냅샷 쿼터 소진 상태다(08-10).** 머지분이 전부 UI Review pending — 주미님 "당장은 눈으로 QA하니 비블로커". 쿼터 해제 시 main 전체 비교 1회, 육안 우선순위는 `ModelProfile`을 쓰는 5곳(MessageBubble·ProfileGroup·SheetProfile·Comment·ReportParticipants). CI 자동 업로드는 없고 필요할 때 `pnpm visual` 수동 실행이 기준. **로컬 `pnpm visual`은 게시+최소 스냅샷이다(08-11)** — `preview.tsx` 전역 `disableSnapshot`(`VITE_STORYBOOK_FULL_SNAPSHOTS=true`로만 해제) + `--only-changed`라 Theme 스토리만 찍힌다. 전수 픽셀 비교는 CI `workflow_dispatch`가 정본.
- 문서 형식 게이트는 `.githooks/pre-commit`(→ `scripts/validate-design-system.sh`)으로 커밋 전 자동 실행된다. 새 클론은 `git config core.hooksPath .githooks` 필요.
- 뱃지·칩 기준선: **눌러서 상태가 바뀌면 `Chip`, 그냥 읽는 라벨이면 `BadgeLabel`**(정본 `docs/plans/20` §5-1~5-3). 패키지엔 `Badge`·`BadgeLabel` 포팅 완료, `Button/DeepCTA`는 Figma만.
- `chart/*` semantic은 **종결됐다(08-09)** — 코드 `color.band.*`+`color.badge.{band}.*`, 피그마 `chart/band·badge` 모두 main/DS에 있다. 다만 `apps/storybook/src/stories/chartPalette.ts`의 5색은 **폐기된 시리즈 팔레트**라 교체 대상이다.
- **반응형 레이아웃 기준(2026-08-06~).** DS `➡️ Guidelines` 페이지(`4054:1848`)가 정본, 코드 계약은 `packages/prism/token-contract.md` "레이아웃 `layout.*`" 절, 근거는 `design-system/foundation/layout.md`.
  - **보드는 08-12에 `layout.md`와 재정합됐다 — LAYOUT 01~10 + 예시 15슬롯 재캡처.** 그전까지 보드는 08-09판이라 폐기된 Side Panel 산식을 들고 있었다. 문서에만 있던 절 6개(버블 상한·떠 있는 것은 크롬뿐·패널 열림 셸 재배치·페이드 소유권·safe-bottom·토스트 예외)를 LAYOUT 07~10 + L05 각주로 옮겼다. **예시 이미지 슬롯↔원본 매핑 정본 = `design-system/sync/guideline-example-map.md`**(08-12에 main 회수 + L06 슬롯 ID 3건 오류 수정).
  - 브레이크포인트 Mobile <768 / Tablet 768–1279 / Desktop ≥1280. 768은 토큰 `{mo,pc}` 분기와 같고, **1280은 토큰이 아니다**(`@media` 조건은 `var()`를 못 받는다).
  - **컬럼은 한 겹이 아니다** — 800(평문 답변) 안에서 리포트 560·투표/폼 520이 자기 상한을 갖는다. 토큰 `--layout-column-{text,card,decision}` + 유틸리티 `max-w-{text,card,decision}`.
  - 웹 셸: 사이드바 260/rail 60 + gutter 8 → **zone = width + gutter×2**(276/76). max-width는 뷰포트가 아니라 **콘텐츠 영역** 기준.
  - 모바일 바텀시트의 웹 대체는 시트 성격으로 가른다(LAYOUT 05): 짧은 액션 **Popover 320**(08-11 첨부 240→320) / 결정 차단 Dialog 400·660 / 대조하며 읽기 Side Panel 400·520.
  - **Side Panel을 열면 폭과 무관하게 사이드바를 rail로 접는다(08-11 정정, 08-12 보드 반영).** 패널도 zone(요약 416 · 펼침 536)이라 사이드바를 편 채로는 요약에서도 컬럼 508로 하한 520을 깬다. Desktop 1280 최소치: 요약 `1280−76−416=788`→708, 펼침 `1280−76−536=668`→588. 패널 열림 컬럼은 토큰이 아니라 잔여폭 `min(800, Main−80)`이다(1440에서 748). **"요약 400은 사이드바를 편 채로 둔다"는 초판은 폐기됐다.**
  - **Modal 400의 근거 화면이 바뀌었다(08-12).** 로그인 '다른 방법'이 v1.0.6에서 폐기돼 실측 근거가 `web / 이메일 로그인 (1440 · 모달)`의 `Login Modal` 400×472로 교체됐다. **폭 기준 400 자체는 불변.**
  - **대체는 형태만 옮기는 게 아니라 정보량도 같이 옮긴다(08-11).** 첨부 시트는 행마다 아이콘+라벨+제약(형식·장수·용량)을 주는데 PC 팝오버는 라벨만 있었다. 폭 320은 그 제약 문구(실측 249)를 한 줄로 담는 최소치다. 결과적으로 Desktop 앵커드 오버레이는 첨부·모델 리스트 둘 다 320.
  - **유저 버블은 두 메커니즘 분업이다** — 상자 max/min은 `layout` 변수(Mobile/Web 모드), 텍스트 줄바꿈 캡은 `_TextBubble`의 `breakpoint=mo|web` variant. **하나로 합치려다 되돌린 이력이 있으니 다시 합치지 말 것.**
- **바텀시트 상단은 두 층이다(08-12 확정, 게시 완료).** `Sheet/Header`(`4475:2102`)는 **크롬만** — 뒤로가기(BOOLEAN) · 화면 이름(짧은 명사) · 종료. 축은 `title = on(76) | off(60)`. 헤드라인 문장+설명은 `Sheet/Intro`(`4544:2276`)가 본문 첫 블록으로 맡는다. **좌측 44 슬롯은 뒤로가기가 없어도 자리를 지킨다**(가운데 이름이 흔들리지 않게). 배정 = 로그인·피드백 `title=off`+Intro / 첨부 `title=on` "첨부" / 전문 시트 계열 `title=on` **"AI 프로필"**(뒤로가기는 리스트 경유만). 종료·뒤로가기 모두 면 없음·`icon-muted`, 잉크가 좌우 16선(패딩 0 — 아이콘 박스≠잉크라 실측값). 시트 좌우 거터 16. 근거·결정 이력 `design-system/components/sheet-header.md`, PR #148. **코드 포팅 완료(PR #153, 머지 대기)** — `SheetHeader`·`SheetIntro` 신설, `SheetShell`은 `header` 슬롯(**BREAKING**: `backLabel`·`onBack`·`onClose` 제거). 계약 `component-contracts/sheet-header.md`·`sheet-intro.md`.
- **모바일 히스토리는 전체화면이 아니라 드로어다(08-12).** Drawer 337(= 화면 393 − 슬리버 56)·x0 풀하이트·우측 radius 16, 뒤에 home 화면 + `semantic/overlay/scrim`. **열림은 오버레이가 아니라 푸시다(08-12 확정)** — home을 337만큼 우측으로 밀어(`배경 — home` x=337) 슬리버 56에 home의 **왼쪽 끝**이 남게 한다(진입화면이 옆에 서 있다는 신호 + 탭해서 닫는 어포던스). 밀려난 화면 scrim은 유지. 오버레이면 오른쪽 끝이 잘려 글자 조각으로 읽히고, 웹 사이드바 260이 이미 푸시라 브레이크포인트 일관성도 여기 붙는다. **상태바·홈 인디케이터 더미는 두지 않는다**(08-12 plan-28 — OS/브라우저가 그리는 영역, 8/11 초안의 "StatusBar 최상위"를 대체). **판단 기준 = 내용물의 성격**: 히스토리가 스위처(새 대화 + 목록 + 계정 진입점)인 동안은 드로어, 검색·다중선택·폴더 같은 관리 기능이 들어와 브라우즈 목적지로 커지면 full 재검토. 레퍼런스 갈림도 같은 축이다(Claude·ChatGPT·X=스위처형 드로어 / Gemini·Grok=검색 얹힌 목적지형 full) — 다수결로 정하지 않는다. 제품 파일 `➡️ home/history` 모바일 **13장**(문서 목록 12 + 뒤늦게 발견한 `8372:107738`) 전환 완료, Usage 보드(`7876:21231`) 노트는 폰트 `Pretendard` 미설치로 문구 미갱신, **컴포넌트 마스터·DS 파일 무변경**. 스토리북은 컴포넌트 QA 컨테이너 337 + `Pages/Home`의 `Mobile (393) — History Drawer`. 정본 `design-system/components/chat-history-panel.md`. **배경은 인스턴스가 아니라 home 클론이라 home을 고쳐도 자동 추종이 안 된다** — home 재디자인 후 13장 일괄 재동기화가 대기 중이다(주미님 합의, 절차·노드 표는 plan-10 「배경 클론 유지보수」).
- **상태바·홈 인디케이터 더미는 제품 화면에 두지 않는다(08-12, 정본 plan-28).** 개발 요청 — OS·브라우저가 그리는 영역이라 구현 대상이 아닌데 시안이 전부 iPhone 16 기준이라 전 화면에 들어가 있었다. 제품 파일 정식 4페이지 **화면 145개·333건 처리**(삭제 아님 — `Bottom Bar`는 `HomeIndicator=off` variant 스왑 111, 나머지는 `visible=false` 222), 잔존 가시 0 확인. 제외 = `❌` 페이지와 **폭 2000px↑ 기획서 보드**.
  - **chrome은 flow 안에 있어서 걷으면 본문이 위로 59·컴포저가 아래로 34 움직인다.** 절대배치 오버레이는 안 따라와 **14건을 실측 보정**했다(헤더 앵커 토스트 y127→68 · 컴포저 앵커 +34 · 코치마크 +34 · 흐름 앵커 −59 · 바닥 flush +34). 앞으로 chrome 있는 시안을 손댈 때 같은 후처리가 필요하다.
  - **패키지에도 같은 더미가 있었다 → `env()`로 교체.** `--bottom-bar-safe-area`가 34px 상수(=아이폰 홈 인디케이터)라 노치 없는 기기·데스크톱에 빈 띠가 남았다. `bottom-bar.safe-area` = `env(safe-area-inset-bottom, 0px)`, `bottom-bar.height.chat` = `calc(202px + env(...))`, `SheetShell`은 `h-[env(...)]`. **컴포넌트 토큰에 CSS 환경 함수를 허용한 첫 사례**로 `token-contract.md`에 절 신설(같은 성격 아니면 확대 금지). changeset `minor`(노치 외 환경에서 하단 34 감소 = 시각 변화). 커밋 `d7267e5` — `origin/feat/edge-scrim-exploration` 도달, **main 미도달**(PR 미개설). 공유 워킹트리라 **hunk 필터(`git apply --cached`)로 옆 세션 작업과 갈라 커밋**했고, `toast.md`·`chat-history-panel.md`는 hunk 자체가 섞여 제외했다.
  - **아트보드 기준 393×852는 유지한다.** 문제는 852가 아니라 그 안의 chrome이었고, 걷어내면 852가 곧 "뷰포트 꽉 채운 화면(100dvh)"으로 읽혀 개발이 그대로 매핑한다. 브라우저 높이는 URL바로 계속 변해 진짜 높이 하나를 고를 수도 없다.
- **딤은 30 단일이다(08-12 재확정), 코드도 0.3/0.3 정합 완료.** `semantic/overlay/scrim` 라이트·다크 모두 `black-alpha/30`. 08-11에 2단(시트 30 / 모달 40)으로 나눴다가 **형제 노드 기준으로 다시 세니 "시트 50개"가 모달 32개를 포함한 오분류**여서 철회했고 `overlay/scrim-soft`는 삭제했다. 제품 딤 50개가 이 토큰에 묶여 있다. 코드는 08-12에 두 번 내려왔다 — `f23f86b`(0.48/0.64→0.4/0.6, 당시 피그마 기준) → 30 단일 재확정 후 **`d16d65c`(0.3/0.3, 주미님 확인)**. 둘 다 `feat/edge-scrim-exploration`, **main 미도달**(드로어 PR에 실림). `scrim-media`(라이트박스 60%)는 불변. 소비처는 `ModalDialog`·`BottomSheet` backdrop. blur는 미채택(드래그 표면 재합성·Safari 중첩). 라이트박스 60% 8개는 컴포넌트 내부라 별건.
  - **시안에서 딤을 그리는 주체는 `scrim (시트 위 영역)` 레이어다(08-12 정리).** 그전에는 시트 컷 7장에서 scrim이 죽어 있고 래퍼 프레임 fill이 대신 그리고 있어, scrim을 고쳐도 화면이 안 바뀌고 흰 판이 깔린 컷에서는 딤이 통째로 사라졌다(피드백 컷 2장의 흰 띠). scrim으로 일원화하고 프레임 fill을 비웠다 — **7컷 렌더 픽셀 diff 0.** 함께: 스크롤된 컷의 `뒤 화면 (목업 판)`은 `시트 top + radius 24`까지 내려야 상단 라운드가 드러난다(판이 100에서 끊기면 흰 위에 흰). 기록은 plan-16, 커밋 `284b6fe`(로컬 전용, 미푸시).
- **제품 타이포 램프에 Bold가 없다.** 강조 굵기는 전부 SemiBold이고 Bold는 `foundation/*`(DS 문서용)과 `chat/h1`뿐이다. 15px 강조는 `body/md-sb`.

- **리포트 뷰가 08-12에 전건 컴포넌트화됐다.** 패키지 `ReportDeep`·`ReportOption`·비공개 `ReportSurface` 신설(PR #154 대기), DS에 `Report/Surface`(`4571:2`)·`Report/Option`(`4572:2`) 마스터 신설, 제품 파일 리포트 카드 12장의 면과 선택지 행 42개를 전부 인스턴스로 교체(손그림 0). 면 값(radius·패딩·간격·그림자)이 마스터 한 곳에 모여 화면마다 갈리지 않는다. `ScoreBar`(Quick)와 `ReportOption`(Deep)을 나눈 기준은 **%를 눈에 보이게 쓰는지**다.
- **패널 헤더가 모드로 갈린다.** `layout` 컬렉션에 `sheet/header-pad-y`(Mobile 16 / Web 8) 신설 → `Sheet/Header`의 `title=on`에 바인딩. 웹 21장에 `layout=Web` 명시. 웹 패널 헤더 60(축 30, 본문 헤더와 일치) / 모바일 시트 76. **`layout` 모드를 실제로 타는 첫 값이다** — `bubble/max`·`margin/screen`은 정의만 돼 있고 화면은 값을 직접 박아 쓴다.
- **피그마 섹션 이름 = 스토리북 버킷 이름이다(08-12 확정).** DS `✅ Components`의 `스토리북 계약` 하위 섹션과 `Components/*` 버킷을 1:1로 맞췄다 — Base UI 16 · History 7 · Chat 26 · Overlay 12 · Navigation 2 · Settings 3 · Decision 16, 그리고 `Story book 미계약`에는 진짜 미계약 4개(`Deep/AgentBubble`·`Deep/VoteCard`·`OTP Input`·`Thinking/Toggle`)만. 섹션은 정리함이 아니라 **상태 선언**이라 낡으면 거짓말이 된다 — 실제로 3건이 어긋나 있었다(`Edge Band`는 계약·코드·스토리가 다 있는데 미계약 섹션 잔류 / Sheet 6개·`Attachment/Lightbox`는 스토리가 `Components/Chat/*`인데 피그마만 Overlay). **Overlay 버킷 기준 = 차단 동작(focus-trap·scrim·portal) 소유 여부** — `SheetShell`은 셸만 주고 모달 동작은 소비자가 감싸서 제외, 완결 모달 `BottomSheet`는 잔류. 어긋난 둘 중 정본은 **이미 소비되고 있는 쪽**(스토리 ID는 남이 참조하는 주소)이라 스토리북 기준으로 피그마를 옮겼다. 정본 `design-system/storybook/storybook-map.md`. **검증 스크립트는 피그마를 못 읽어 이 대조는 자동화가 안 된다.** 커밋 `f6281b7`·`a3e4623`(`feat/edge-scrim-exploration`, main 미도달) + `f52cffe`(PR #153).
- **`OTPInput`은 계약 md만 있고 코드가 없다(08-12 분류).** 맵 「스토리 없는 계약」 표가 원래 "피그마가 뺐지만 코드는 유지"(내림) 한 갈래만 상정하고 있었는데 OTP는 정반대(미포팅)라, 두 갈래를 갈라 적었다. 코드가 들어오면 「이름 규칙」 표로 올린다.
- **설정 페이지(`🌱 settings` `8235:180169`)는 08-12에 v1.0.6 전수 검토를 마쳤다(정본 plan-29).** 처리 = 소셜 컷 2장의 `비밀번호 변경` 제거(§3.1 위반 — 소셜은 비밀번호가 없다) · 구컷 8노드 삭제(회원 탈퇴 2·언어·비로그인 + 캡션 4, 구 탈퇴 컷은 fill 변수 미바인딩이라 다크에서 깨지던 것) · **언어 나열을 `日本語 · 한국어 · English`로 전환**(기획서 `한·영·일`이 아니라 와프+JP 타깃 기준 — 주미님 결정, 다음 세션이 "역행"으로 오진하기 쉬운 자리) · **계정 메뉴·로그아웃 확인 모달 9컷 신설**(라이트만). 화면 프레임 30 → 35.
  - **로그인 진입 경로는 08-11에 "설정에서 뺀" 뒤 받는 쪽이 비어 있었다** — 세 페이지 전수 스캔에서 `내 계정`·`로그아웃` 0건, 수용기준 2·6 미충족 상태였다. 지금은 모바일=바텀시트 / 웹·태블릿=`Context Menu` 팝오버(`shadow/lg-blur`)로 채웠다. 모달 문구는 **타이틀=화면정의서 원문 · 서브카피=와프**로 룰 35를 한 컴포넌트 안에서 갈라 적용했다.
  - **미해결**: 다크판 9컷(주미님 "나중에") · DS `History` `login=on`의 `Free` 플랜 배지(§3.2 무료 런칭이라 근거 없음 — 지금은 인스턴스 override로만 숨김) · 와프에만 있는 4건(본인 인증 앞단·완료 화면 2종·소셜 인증 컷·설정 전용 에러 컷) · 스펙 밖 요소 존치(다크 모드 토글·오픈소스 라이선스·앱 버전·프로필 카드). **plan-29 문서는 untracked 미커밋.**

- **재열람 적재(이미 만든 리포트를 다시 여는 대기)는 08-19에 전건 종결됐다.** 생성 대기와 다른 자리라는 걸 기획서 §4.9·§5.1로 갈랐고(재연결 복구 = "재생성이 아니다"), 인디케이터는 `rules.md` 39번 사다리 3단계(스켈레톤), 범위는 `SOCRA-TUTOR-022`("데이터를 제외한 UI는 유지")를 히스토리 목록과 같게 적용했다. **리포트는 목록처럼 높이를 약속하지 않는다** — 선택지 2~5개가 가변이라 고정 부분(아이콘 48·제목·요약·분포 바)까지만 그리고 목록 자리는 비운다. **글래스 면도 그리지 않는다**(적재 중엔 리포트가 올지 모르는 상태라 면이 곧 약속으로 읽힘 — `ReportSurface`는 비공개 유지). 시안 = 제품 파일 `✅ chat/report` 섹션 `9383:113289`(컷 3장), 코드 = `Explorations/재열람 적재`(패키지 export·계약 무변경). **`ReportDeep`/`ReportQuick`에 status 축은 넣지 않는다**(8/6 「로딩 자리표시는 화면 조립 몫」 유지).
  - **실패 화면의 무게는 「빠져나갈 길이 있느냐」로 가른다.** 목록 로드 실패는 그 화면에서 재시도 말고 나갈 길이 없어 `Empty State` 3층이 맞고, 대화 하나를 못 읽은 건 목록 복귀·새 질문이 살아 있어 **콜아웃 한 줄**이다. `RetryButton`은 두 자리 다 안 쓴다(그 계약은 "생성 실패 + 남은 횟수 1/3" 전용).
  - **DS `Callout`에 `Type=error, Layout=action` 신설·게시(`4940:2349`).** `Type=info` 클론 + error 톤 3개 재바인딩, 아이콘은 glyph가 달라 `Type=error, Layout=inline`의 인스턴스를 옮겨 심었다. `success`·`warning`의 action 조합은 여전히 코드에만 있다.

### 열려 있는 결정 (진행을 막고 있는 것)

- ~~**PR #246 머지 대기(08-25)**~~ — **종결(08-26 13:37).** squash 머지로 `2f67cd1d`가 `origin/main`에 도달했다. 기획서 **v1.0.8** 정합 트랙이 통째로 들어갔다 — 미러 등재 · 용어 교체 26곳 · 신규 화면 20여 장 · DS `Modal Content / 약관 동의` 신설 · 단계 인디케이터 22장. 두 세션이 나눠 진행하고 브랜치를 하나로 합쳤다. 워크트리·브랜치도 정리했다. **`plan-32` 열린 항목 0이며 트랙 자체가 닫혔다.**

- ~~**「이용=동의」 문구 21곳(08-25)**~~ → **종결(08-26).** 세어 보니 **26곳**이었고 출처가 개별 카피가 아니라 **마스터 6곳**(DS `Modal Shell` 2 + 제품 로컬 `Sheet Body` 4)의 Footnote 고정이었다. v1.0.8 §3.3의 명시 동의 기준으로 전부 숨겼고(텍스트는 보존 — 필요하면 인스턴스에서 켠다) 마지막 1곳은 라이브러리 수락으로 풀렸다. 부수로 게이트 시트 빈 공간 45px도 잡았다 — `Sheet/Shell`이 FIXED라 마스터만 줄고 화면은 안 줄어 있었다. PR #252.

- **타임스탬프 상대시간 JP 문구 · 예시 연도(08-25 재확인)** — 기획서 §3.4가 날짜만 JP 예시이고 상대시간은 한국어다. 「다른 해」 예시가 `2026年9月1日`이라 현재 연도와 같아 예시가 자기 뜻을 못 보여준다. **v1.0.6·v1.0.7·v1.0.8 세 버전째 그대로** — PM 요청 상태이고 화면(`8527:184741`)은 미정으로 열어 뒀다.

- **심볼 방향 — 도트 문법 + 쏘냥이 ?/! (08-24 밤, 8/31 미팅 대기)** — 브랜딩 2차 아이데이션을 `Socra-Branding`(`8tH9GNtdd2dLiJtKFWFsMG`) `아이디에이션_주미` 섹션에 채웠다: 덱 14장(도트 제안 · 판정 기준 3 · 추천 다이아몬드) + 탐색 보드 5장 + 쏘냥이 픽셀(꼬리 = ? 고민 / ? 째깍 로딩 / ! 결정, 등 점 = 부호의 점). **선결 = 판정 기준 통일**("답을 주는 존재"로 볼지 "사용자가 결정"으로 볼지 — 두 덱 전제 충돌). 쏘냥이를 모더레이터 안건으로 올릴지는 주미님 결정 대기(6/18 C안 재개봉 건이라 명시 안건 필요). 상세는 8/24 worklog 「브랜딩 2차」 절.

- **`fix/ds-offline-modal-surface` 미머지(08-24)** — 분기는 정리해 원격에 올렸다(머지 `b68fa44`). 내용 충돌이 아니라 같은 작업이 두 번 커밋된 상태였다. **다만 브랜치가 아직 미머지라 DeepCTA 트랙 전량이 `origin/main`에 없다.** 그리고 공유 워킹트리는 `behind 4`에 `retry-button.md` 2개가 미커밋으로 떠 있다 — 내용은 들어올 것과 바이트 동일하니 `git checkout -- <두 파일>` 후 pull하면 된다.

- **Deep 대기 「사고 과정 sticky」 5안 중 택1 대기(08-24)** — 완성 알림 콜아웃 안을 접고 **접힌 진행 줄 상시 노출 + 완성 시 그 줄이 리포트 진입으로 전환**으로 방향을 틀었다. 시안 8장 + 프로토타입이 제품 파일 `test — 사고과정 sticky`(`9892:2`)에 있다(원안 풀폭 띠 / `v1` 스피너 캡슐 / `v2` 아바타 캡슐 / `v3` 헤어라인). **추천은 `v2`.** 확정되면 셋이 따라온다 — ① `2-07`·`2-08` 콜아웃 안 폐기 ② 기획 §「완성 시 즉시 종료 → 상시 버튼」 협의(기획서 원문은 수정하지 않고 협의 안건) ③ 첫 진입 각주 `리포트가 완성되면 이 화면은 사라져요` 카피 변경. 칩 인터랙션은 **팝오버가 아니라 스크롤 이동**으로 이미 확정됐다(정본 복제 금지 · 복귀는 기존 `Scroll FAB`).

- **`Callout` action variant 본문 정렬 버그(08-24)** — 제목은 아이콘 옆(28), 본문은 패딩(12)에서 시작해 왼쪽 축이 둘이다. DS 마스터(`1198:1533`) 3종과 `Callout.tsx`(`description`이 `flex-1` 컬럼 밖 형제) 공통이라 **화면이 아니라 컴포넌트 일감**이다. 착수 시 함께 잴 것: 계약 문서의 action 표면이 중립(`bg-surface-muted`)인데 피그마 마스터는 톤 표면으로 이미 갈라져 있다.

- **§4.7 「결과 3초 자동 전환」 삭제를 기획에 반영해야 한다(08-19)** — Deep 대기 투표를 **무클램프 스트림(v3)**으로 채택하면서 카드 넘김이 없어졌고, 넘길 카드가 없으니 자동 전환 조항의 전제가 사라졌다. 디자인·스토리북은 이미 자동 전환 없이 서 있고 기획서(v1.0.7)만 옛 문구다. 「건너뛰기는 카드 등장 3초 후 노출」은 **유지**라는 것도 함께 적어야 한다. 같이 열린 것: **건너뛴 카드의 상태** — 스트림에 남지만 투표를 안 했으니 `answered`가 아니다(나중에 올려서 투표 가능하게 둘지 = §4.7 노출 기록 규칙과 함께 판단).

- **`Sheet Body / 로그인 게이트` DS 승격 승인 대기(08-19)** — 감사에서 권장으로 냈고 write는 안 했다. 범위는 DS에 `Sheet Content / 로그인 게이트` 신설 → 게시 → 제품 인스턴스 2개 스왑 → 297 검증. override가 0이라 스왑 손실이 없다. 함께 열린 것: **인용 부호 로케일 글리프 규칙을 어디에 계약으로 적을지**(토큰이 아니라 렌더 규칙이라 `design-system/foundation/` 쪽이 후보).

- ~~피그마 `✅ Components` 섹션 이동 13건 승인 대기(08-19)~~ — **같은 날 집행 완료.** 13개를 옮기고 재스캔으로 대조했다(바뀐 항목 정확히 13 · 사라진 항목 0 · 밖에서 움직인 항목 0). 롤백 좌표는 `storybook-map.md` 「제외 항목」. **남은 것 하나** — `Row/Item`·`ContextMenu/Item`은 버킷만 옮긴 임시 배치이고, 실은 `Menu Item`(`3352:1374`) 하나로 **합칠 건**이다(`bottom-sheet.md`가 "행 컴포넌트를 셸별로 따로 만들지 않는다"고 못 박아뒀다). 인스턴스가 딸려 움직여 별건으로 남겼다.

- ~~**Deep 사고과정 시안 1 최종 확정(08-19)**~~ → **종결.** 시안 1 채택 · 예정 단계 노출 기각(기획에 없는 추가 사양 + 세로 +80) · 헤더 아이콘 `list-checks` 채택 · 도착 줄은 모델명/에이전트명 **양자택일이 아니라 혼재**(§3.4 `{AI 이름}`). DS 마스터 반영·게시·수락 완료. **남은 것 하나 — 재게시**: Deep 펼침 `Progress`(`4935:2482`) 바인딩 수정분이 마스터에만 있어, 게시 전까지 인스턴스 9개가 문구 숨김 + 진행바 노출 상태다.

- ~~Callout 세트 2축 분리의 게시·수락(08-18)~~ — **같은 날 종결.** `Type`{4톤} × `Layout`{inline,action}로 갈랐고(`Type5` → `Type=info, Layout=action`) 게시·수락까지 3단계 실측으로 확인했다. 제품 파일 SA-1a 3장이 새 축을 보고하고 텍스트 오버라이드도 보존됐다. **이름만 바꾸면 인스턴스가 안전하게 따라온다**는 실증 — componentKey가 유지되기 때문이다.

- ~~첨부 실패 ↻ 아이콘을 흰 선으로 통일할지(08-14)~~ — **08-18 종결.** 물어보러 마스터를 다시 재니 **주미님이 이미 흰 선으로 고쳐 두셨다**(`4189:1919`). 미결로 남아 있던 건 결정이 아니라 코드·문서였고 `errorIconOnMedia = Boolean(src)`로 정합했다. 보드4 `Q1-b` 비교 행은 이력이지 현행이 아니다.
- **첨부 실패 칩 다크(08-14 주미님 "지금은 다크 안 할 거야"로 보류)** — 마스터의 흰 ⚠가 `neutral/white`(Light `#FFF` / Dark `#000`)에 묶여 **다크에서 검정 ⚠**가 된다(모드 지정 렌더로 실측). 사진은 테마를 안 따라가므로 모드 불변이어야 하고 고칠 땐 `alpha/white-alpha/100`(`638:874`). 같은 뒤집힘이 processing 스피너 Arc·× 버튼에도 있다. **코드 토큰은 이미 모드 불변이라 다크에서만 코드와 피그마가 갈린다.**
- **첨부 개발 재질의 3건(08-14)** — ① 실제 업로드 시점(첨부 순간 vs 전송 버튼 — `error-retry` 노출 여부) ② 실패 사유를 사유별로 내려주나 ③ **`5MB 초과`에 로컬 프리뷰를 줄 수 있나**. ③은 스토리 두 곳이 대표 예시로 쓰는 갈래인데 개발 미확인이다 — "불가"면 예시를 `업로드 실패`로 바꿔야 한다. 보드4 Q4·Q5와 함께 물을 것.

- ~~게이트 서브카피~~ — **08-13 존치 + KR 확정으로 종결**(같은 날 폐지→존치로 두 번 뒤집힘, 최종은 존치). 노출 11곳 · **이메일 단계 모달 39곳은 숨김이 정상**(게이트 껍데기 복제 잔재). JP·EN만 미검토 초안. **다음 세션: 이 문구를 다시 폐지하러 가지 말 것** — 룰 35는 기획서 vs 와프 우선순위이지 디자이너 직접 결정을 이기지 않는다.
- **`설정 / 비밀번호 변경` 2필드 👁 적용 여부(08-12)** — §3.1이 "비밀번호 정책 동일"이라 붙는 게 맞아 보이나 설정 페이지는 plan-29 세션 소유라 손대지 않았다. 설정 트랙 몫.
- **색 규칙 잔여 2건** — 규칙 자체는 08-03 확정(구간 3색: 다수·압도 `blue/500` / 접전 `orange/500` / 일부 `purple/400` / 소수 라임, 판정 기준 v1.0.3). 남은 것: ① 라임 소수 바가 트랙 대비 1.19로 3:1 미달 — 색으로 못 풀어 stroke 같은 비색 수단 결정 필요 ② 소수 3개 이상이면 스택바 범례가 전부 라임이라 구분 불가.
- **리포트 구분선 간격 28/40** — 면 안쪽 28, 면 바깥 40으로 갈려 있다. 같은 `TextDivider`가 같은 일을 하는 자리라 한 값으로 모으는 게 맞아 보이지만 디자인 결정이라 대기.
- **리포트 글로우 처분** — `Report/Surface`에 `글로우` BOOLEAN으로 올렸고 전 카드 꺼짐. 켤 거면 모바일(361/328) 지오메트리와 그라데이션 토큰화를 같이 봐야 한다(원본이 하드코딩).
- **히어로 레전드 유무**(07-28 이후 휴면) — 비교 스토리 제작 완료, 채택만 남음. 흡수 전환 4컷이 "레전드 포함" 기준이라 뒤집히면 재작업.
- **`Chip` 축 확장 — Figma 결정 2건 선행**(08-03 이후 휴면) — radius 사각 variant 부재 / 눌린 색 패키지 파랑 vs Figma 검정 / md 텍스트 16 vs 17. 정본을 정해야 코드를 고칠 수 있다.
- **processing 중 전송 처리**(기획 확정 대기) — 오프라인 배너 철회로 "전송만 잠근다"의 근거가 소멸. 컴포저 processing 비교 시안·스토리북 processing 전이 데모가 이것 대기. 단 **"에러 칩이 남아 있으면 전송 불가"는 §3.4 명문이라 유효** — 둘을 섞지 않는다.
- **`--layout-overlay-popover`(240) 처분**(08-11) — 첨부가 320으로 옮겨가며 이 토큰의 소비처가 0이 됐다. 공개 manifest에 실려 있어 값 변경·삭제 둘 다 브레이킹이라 **값은 그대로 두고 비고만 "`Popover` 마스터 기본 폭"으로 정정**했다. 남은 판단 = Figma `Popover` 마스터(`4276:1991`)와 `Popover.tsx` 기본값을 320으로 볼지.
- **§5.3 주미님 확인 4건**(게이트/토스트 문구·세션 승계 고지·비로그인 복구 불가 문구·전송 실패 opacity 20%) + **PM 노트 전달**(`7833:97335`, S9-3 정의 한 줄 수정 포함).
- **비로그인 게이트 닫은 뒤(SA-1a) 잔여 2건**(08-13 갱신 — ⚠ "정본 plan-25"는 유령 참조: 그 파일은 레포에 없다. 정본 = 제품 파일 `📌 결정 노트 — 비로그인 게이트` + `chat-input-bar.md` 08-13 절) — ① **PM 확인**: 소진 후 화면 표시가 기획서 미규정(§3.2는 모달만 정한다) ② **KR 문구 확정 → `strings` 등재**(SA-1a 4문구 + locked placeholder — 현행 화면 문구를 그대로 확정하면 등재는 기계 작업). ~~③ locked 신설~~(08-13 완료 · 게시 대기) ~~④ Mode Toggle 비활성~~(이미 완료·게시됨 — 08-13 실측).
- **룰 39 후속 미결 3건(08-13)** — ~~① 로딩 문구 복원~~ **종결**: 튜터팀 위키 `SOCRA-TUTOR-022`(`PROD QA완료`)가 "데이터를 제외한 UI는 유지하고, **데이터 영역에 스켈레톤을 추가한다**"를 규정하고 "현재 상세 로딩 정의가 없다"고도 적어 뒀다 — 문구는 미규정이고 스켈레톤은 기획 요구였다. 남은 것 = ② `Empty State`의 `no-results`·`error` 슬롯도 `media` 불리언으로 통일할지(지금은 `no-chat`만 불리언이라 `Type=error` 쓰는 화면마다 손으로 꺼야 한다) ③ `Skeleton` 세트/컴포넌트 처분(소비처 0 + 계약 제외 · 폐기 vs 오토레이아웃 수리 · 소비처 0인 지금이 제일 싸다) ④ `AttachmentFile` 진행률을 `meta` 텍스트로 줄지. **일러스트 슬롯은 주미님 확정 = 필요**(불리언으로 되살림).
- **모더레이터 얼굴 잔여 3건(08-13)** — ① `Deep/VoteCard` `State=result`에도 얼굴이 기본 on(뺄지 판단 · `Face` 불리언만 끄면 되므로 비용 0) ② empty → 첫 질문 전환에서 얼굴이 카드 센터→좌상단으로 **1회** 이동(그 뒤 질문 교체 구간은 고정) — 정적 피그마로 판정 불가라 스토리북 설계 필요 ③ `Deep/VoteCard` 계약 문서 부재(DS `Story book 미계약` 섹션 · 오늘 `Face`·`Face swap` 속성이 붙었으니 계약 작성 시 함께).
- 기획서(`docs/20-socra-product-spec-2026-07.md`) [Open Issues] — 타임아웃·RAG 도메인·계정/인증·APPI는 PM/법무 대기라 디자인 착수 불가.
- **로그인·아바타 비블로킹 4건**(08-11, 정본 plan-26) — ① 접힘 레일 헤더 접기 토글(반투명 원형 r24) vs 푸터 아이콘(사각 r12) 형태 불일치 ② `User Profile` radius 크기별 불일치(sm/md/lg=16, xl=28 — 40px에 16이면 원이 아니다) ③ 푸터 `PlanLabel 'Free'` 처분(08-09 합의대로 Footer 재작업 때) ④ 패키지 `UserProfileType` `"empty"` 제거(기본값이기도 함 — 브레이킹 후보).
- ~~**`Navigation Bar` 마스터에 `StatusBar=on/off` 축을 만들지**~~(08-12 종결, plan-28) — **축을 만들지 않고 마스터에서 제거**했다(119→60). 제품 인스턴스 101개가 전부 `visible=false`라 옵션 자체가 불필요했다. 짧은 뷰포트 QA 프레임(393×664)도 2장 생성 완료. **폐기한 안 = variant 축 신설**(단일 COMPONENT를 variant로 쪼개는 비용 대비 실사용 0).
- ~~**개발 전달 — 뷰포트 메타 2줄 + `theme-color` 값**~~(08-12 종결, plan-28) — 값 확정(라이트 `#F2F4F8` / 다크 `#0E0F12`)하고 `packages/prism/README.md` 「뷰포트 메타 (모바일 웹 필수)」 절로 승격했다. **메타가 없어도 에러가 안 나고 조용히 반만 동작**하는 종류라 전달 성공 여부에 기대지 않고 설치자가 지나가는 문서에 박았다. **폐기한 대안 = 라우트별 동적 `theme-color`.** 남은 것은 로그인 라이트만 `#FFFFFF`(primitive 직바인딩)인 드리프트 확인.
- **하단 인셋·컴포저 상한 실기기 확인**(08-12) — 16↔34 전환도 `dvh` 반응도 **피그마·스토리북 데스크톱 어디서도 안 보인다.** 확인용으로 `Foundations/Viewport & Safe Area` 프로브 스토리를 만들어 뒀다 — 아이폰 사파리로 열면 `env()`·`dvh`/`svh`/`lvh`·해석된 토큰 값이 숫자로 뜨고, 키보드 입력란으로 `interactive-widget` 적용 여부까지 본다. **읽기만 하면 된다.**
- 열린 PR 처분 — **#35**(plan-19 일정 v4, CONFLICTING — rebase/close 미정) · **#13**(codex Chip locale, draft — close 유력).

**종결 — 되돌리기 금지(폐기 기록):**

- **socra 마크는 DS 로컬 세트가 정본(08-13 확정, 주미님 "통일하고 DS에서 관리")** — `Answer/Footer`가 물던 **외부 브랜드 라이브러리의 원격 socra를 폐기**하고 DS 로컬 `socra`(`4048:1726`)로 통일했다. 원격을 직접 물면 내부를 못 고쳐 색을 변수에 못 묶고, 실제로 마크가 라이트·다크 양쪽에서 검정 하드코딩이었다. 통일 후 얼굴 인스턴스 143개 단일 참조 · 구세트 0 · 다크 `203,206,210` / 라이트 `48,50,59`로 모드 전환 확인. **브랜드가 바뀌면 DS 로컬 socra만 교체한다 — 원격 직참조로 되돌리지 말 것.** (컴포넌트 설명의 옛 문구 "브랜드 확정 시 원본 라이브러리에서 교체한다"는 08-13 갱신됨)

- **오프라인 배너·전송 비활성 승격 철회(08-10)** — 기획서 §5.3에 없는 표면이 자체 승인으로 들어와 있었다. "전송 시도 → 토스트 안내 + 입력 보존"이 정본. 시안·정합표·`error/offline-banner` 변수까지 정리 완료. **승격을 재개하지 말 것.**
- 색 "강조 여부 2단"안 폐기(08-03) — `docs/plans/16` §8만 읽으면 반대로 간다. 폐기 표시가 문서 정정 잔업.
- 전송 실패 전용 프레임 삭제 — S9 케이스로 일원화(08-09).
- Quick|Deep 세그먼트 토글 탐색 폐기(08-11) — v1.0.6 'Deep On/Off 스위치 하나' 명문. 현행 Deep 칩이 정답.
- 웹 빠르게 시작하기 = 키워드 칩(08-11, §4.1 명문·주미님 확정) — 탭+문장 필 판은 참고용 보존.
- 로그인 first-screen = LINE·Google·이메일 3수단, Apple은 앱 단계(08-11, §3.1) — 6/23 결정 노트(JP=LINE·Apple·Google)는 대체됨.
- **`Popover`·`Menu`는 `className` 폭 지정을 지원하지 않는다(08-11 확정, #130)** — `GlassSurface` 인라인 width가 이기는 구조를 API 모순으로 보지 않고 계약으로 확정했다. 폭은 `style` 또는 변수로 준다. **className 지원으로 되돌리지 말 것.**
- 6자리 코드 입력 = `OTP Input` 신설로 종결(08-11) — `Text Field` 6개 조합안 폐기. DS `4407:2099` 게시 완료, 계약 `otp-input.md`.
- 설정 화면 스펙 외 항목 제거(08-11, **§4.11**·§3.2) — 구독 섹션(MVP 무료)·알림(웹 MVP 미포함)·로그아웃(=[내 계정] 메뉴 소관) 제거. 다크 모드 토글은 재량 유지. **오픈소스 라이선스 행은 존치 확정**(OSS 고지는 라이브러리 라이선스 의무라 스펙 외라도 뺄 성격이 아니다 — 다크 모드의 '재량'과 근거가 다르다). 8/11 밤 모바일·웹 3탭(라이트/다크)·태블릿 **전 9컷 적용** + **사업자 정보 푸터**(수용기준 7) 신설. ※ 절 번호는 **§4.11**이다 — v1.0.5·v1.0.6 모두 4.11이고 `§4.12`는 구판이다.
- 입력창 글로우 = E안 확정·반영 완료(08-11, #136·#137) — `gradient/brand` 링 1px + 정적 halo 0.13, 포커스 트리거, Deep On이면 링 생략. B(2색 선형)·C 계열(흰색 교차 링)·E-1(halo 없음) 폐기.
- `ProfileGroup` hover = B 보더 강조 확정·반영 완료(08-11, #140) — A 배경 필(focus와 같은 그림)·C 리프트(과함) 폐기.
- **기본 아바타 = 이니셜 확정(08-11, 정본 `user-profile.md`)** — 실루엣(LINE·카카오 관행) 왕복 검토 후 기각. 근거: 접힘 레일은 이름 없이 아바타만 떠서 실루엣이 '미설정/익명'으로 역신호 · AI 아바타 많은 화면에서 글자 아바타만이 사람 사용자 언어 · 스펙(SB) 원문이 이미 이니셜. 이니셜 규칙 = 1글자·라틴 대문자·CJK 첫 글자 그대로(로마자 변환 금지)·이모지 시작은 첫 글자/숫자까지 스킵·중립 단색 배경. **실루엣으로 되돌리지 말 것.**
- **`User Profile` `Type=empty` 삭제(08-11, 12→8 variants)** — 표시 이름은 세션과 함께 항상 도착하므로 '처음부터 이니셜, 사진은 도착 시 교체(미취득·실패도 이니셜)' 규칙이면 빈 아바타가 성립하지 않는다. 사용처 0 확인 후 삭제·게시 완료. 패키지 `UserProfileType`의 `"empty"`는 브레이킹이라 별도 PR로 남김.
- **로그인 = 오버레이 모달, first-screen 전면 로그인은 스펙 외(08-11, §3.1)** — 진입점은 게이트·세션 목록 하단 2곳뿐. 정본 모달은 `chat/chart`의 SA-1. first-screen 5판은 `❌ 참고용` 섹션으로 분리(삭제 아님).
- **사고과정 말투 = 존칭·대화체 확정(08-12, 기획서 §3.4 / 정본 `rules.md` 34번)** — 혼잣말·반말 / 유저 호명 / 1인칭 주어 전부 폐기. 근거였던 반대 규정 노트(`6990:133987`, "추론 말투는 혼잣말")는 **대상 소멸**(8페이지 2만 텍스트 전수 스캔 0건)이라 룰 문서가 유일한 기록이다. **와프·구 시안에서 혼잣말체를 봐도 되살리지 말 것.**
- **Deep 사고과정 진행바 = 넣는다(08-14 재정정 · 정본 `rules.md` 35·39번).** 8/12에 "와프에 없으니 빼자"로 갔다가 **주미님 확인으로 되돌렸다** — 기획서 §3.4가 맞고 Deep은 **5단계 + 진행바**가 정본이다. 산식 = analyzing 0 → gathering 10 → responding `10 + 도착÷전체×80`(최대 90) → synthesizing 97 → reporting 100, **% 숫자는 표시하지 않는다.** `단계문구=false`면 단계 문구와 진행바가 **함께** 사라진다. (이 줄은 8/19까지 8/12판 "넣지 않음"으로 남아 있었다 — 되살리지 말 것.)

- **Deep 대기 투표가 걷히는 방식 — 차주 인터랙션 디자이너 논의(8/19).** 시도 3번이 다 반려됐다(즉시 제거="샥", 페이드="얼레벌레", push=판정 대기). 갈림길은 A(모션을 다듬는다) B(배치를 되돌린다 — 대기 투표를 시트/오버레이로 빼면 사라짐 문제 자체가 소멸). 자료는 제품 파일 `인터랙션 고민` 섹션 + Chromatic. **확정된 넷은 다시 열지 않는다** — 채팅방에 안 남음 / 건너뛰기도 결과 노출 / 완성 인지는 Callout / 이동 먼저·정리 나중

### 알려진 캐비엇

- ⚠ **읽으라고 지정된 문서를 건너뛰면 이미 존재하는 방어를 못 쓴다(08-26).** 착수 지시의 `design-system/rules.md`를 안 읽고 시작해 **규칙 42(「variant 전환도 override를 흘린다」)의 게이트를 안 걸었다.** `setProperties`로 `State`를 4번 전환하면서 전환 전 전수 수집·대조 대신 렌더를 눈으로 본 것으로 갈음했다. 사후 대조에서 샌 것은 0이었지만, 그 컴포넌트의 `State` 축이 기존 텍스트를 유지한 채 슬롯을 **더하는** 구조라 안 샌 것이지 확인해서 안 샌 게 아니다. 규칙 42가 「가장 많이 반복된 함정」인 이유가 이것 — **매번 새지 않으니 건너뛰게 된다.** 같은 날 밟은 배치 겹침도 규칙 10이 이미 적고 있던 자리다.
- ⚠ **squash 머지된 브랜치는 `git branch -d`가 거부한다(08-26).** 원본 커밋이 main의 조상이 아니게 되어 git이 미머지로 본다. `-D`로 강제 삭제하기 전에 **SHA 도달성이 아니라 내용 기준으로** main 반영을 확인한다(파일 존재 · 열린 체크박스 수 등). 도달성만 보면 멀쩡한 작업을 유실로 오판한다.
- ⚠ **`[gone]`은 「push만 하면 되는 상태」가 아니다(08-26).** `fix/report-feedback-spacing-v2`가 PR #251 머지로 원격에서 자동 삭제됐는데 로컬 브랜치에 커밋이 계속 쌓였다 — 옆 세션 것 하나, 내 것 하나. **둘 다 origin/main에도 없고 원격 브랜치도 없어 로컬에만 존재했다.** `git status -sb`가 `ahead`가 아니라 `[gone]`을 찍으면 **밀어 넣을 곳이 사라진 것**이므로, 그 브랜치에 그냥 push하면 위 08-25 캐비엇대로 죽은 브랜치가 되살아난다. main 기반으로 워크트리를 새로 파고 내 커밋만 cherry-pick해 올린다. **커밋 직전에 브랜치 이름뿐 아니라 원격 추적 상태까지 읽는다.**
- ⚠ **머지하고 main에서 확인해도 끝이 아니다 — 세션 닫기 전에 다시 쟰다(08-26).** PR #254·#255를 머지하고 `git show origin/main:<path>`로 내용까지 읽어 확인한 직후, 옆 세션의 PR #256(`rescue/orphaned-session-work` — 종료된 세션의 미커밋 파일 수습)이 머지되며 **내 추가분 두 건을 통째로 되돌렸다**(`rules.md` 46번 절, `plan-10` 소절 + 닫은 체크박스 2건). 그 브랜치가 담은 파일이 내 커밋 **이전** 시점 내용이었다 — 08-25 「낡은 base에서 뜼 패치」와 같은 원인인데 가해자가 남이라 내 절차로는 못 막는다. **부분 생존이 더 위험하다:** 같은 PR이 안 건드린 `AGENTS.md` 색인 행만 남아, 색인은 46번을 가리키는데 본문에 46번이 없는 상태가 됐다 — 한 파일만 보면 정상으로 읽힌다. **여러 파일에 걸친 변경은 파일마다 고유 문자열로 전부 다시 센다.** 복구는 현재 main 위에 원래 커밋을 cherry-pick하고, 파일 끝 추가끼리 충돌하면 양쪽 다 남긴다(남의 것 먼저). 검증은 `git diff --numstat`의 삭제 줄이 의도한 교체분과 정확히 일치하는지로 한다.

- ⚠ **머지된 브랜치를 다시 push하면 되살아나 main과 갈라진다(08-25).** 레포는 merge commit 금지·squash만·**머지 시 브랜치 자동삭제** 설정이다. `fix/ds-offline-modal-surface`가 #220 머지로 삭제됐는데 누군가 다시 push해 되살렸고, 그 뒤 main을 한 번도 안 가져와 **30커밋** 벌어졌다. PR #247을 열자 CONFLICTING이 떴고 충돌 10건이 전부 거기서 나왔다. **머지된 브랜치는 재사용하지 않는다** — 새 브랜치를 판다.
- ⚠ **`git branch -a --contains`가 0인 것은 유실의 근거가 못 된다(08-25).** squash + 자동삭제 환경에서는 정상 머지된 커밋도 전부 0으로 나온다. 옆 세션이 이걸 근거로 「8/11~8/19 **119커밋**이 떠 있다」고 보고했는데 표본 6건이 전부 main에 도착해 있었다. **유실 판정은 커밋 계보가 아니라 파일 내용 대조로 한다.**
- ⚠ **`package.json`을 워킹트리에서 읽으면 옛 스크립트 이름을 본다(08-25).** 「main 문서의 `build:token`이 오타」라고 판단해 PR #248로 고쳤는데 **반대였고 #250으로 되돌렸다.** 워킹트리가 8/24 이전 base라 거기엔 옛 이름 `token:build`가 있었고, `740e01a8`(빌드 명령 통합)이 `build:*` 계열로 정리하면서 문서도 같이 고쳐 둔 상태였다. 확인은 `git show origin/main:packages/prism/package.json` 한 줄이면 됐다. **문서 21곳 일괄 수정 직전에 옆 세션이 잡았다.**
- ⚠ **`git checkout --theirs`는 파일 단위라 자동 병합분까지 덮는다(08-25).** 충돌 헝크만 상대 쪽을 취하려다 그 파일의 내 변경을 통째로 날릴 뻔했다. `git checkout --merge`로 복원하고 마커를 직접 편집한다. **복원하면 마커가 `HEAD`/`origin/main`이 아니라 `ours`/`theirs`로 바뀐다** — 정규식으로 처리할 때 걸린다.

- ⚠ **슬롯에 얹힌 자식은 override도 상속도 아니다(08-25).** 위 `overriddenFields` 캐비엇의 다른 얼굴이다. 용어 교체 전파를 「override 4곳 + 마스터 상속 19곳」으로 갈랐는데 게시 후 1곳이 안 왔다 — `Report/Surface`의 콘텐츠 슬롯에 통째로 얹힌 텍스트라 **마스터에 그 문구가 아예 없었다.** `charsOverridden: false`는 「텍스트 override가 아니다」일 뿐 「마스터에서 내려온다」가 아니다. **게시·수락으로 안 풀린다.**

- ⚠ **`swapComponent`도 override를 못 이긴다(08-25).** 규칙 31·42가 막는 것과 같은 성질인데 스왑 쪽도 같다. 모달 본문을 통째로 갈았는데 원본 override 넷(제목·단계·가이드·버튼)이 새 본문의 같은 자리에 얹혀 **구조는 새것인데 글자만 옛것**이 됐다. 스왑 뒤에는 텍스트를 전수로 대조한다.

- ⚠ **조건으로 훑어 일괄 적용하면 옆 세션 신설분이 딸려 온다(08-25).** 이름 패턴으로 프레임을 전수 훑어 단계 표기를 켰더니 그 사이 다른 세션이 만든 3장까지 켜졌다. 알려서 무사했지만 못 알아챘으면 그대로 넘어갔다. **대상은 노드 id로 고정한다.** 피그마 파일도 워킹트리처럼 공유 자원이다.

- ⚠ **빈자리 찾기는 폭으로도 y로도 거르면 안 된다(08-25).** 두 세션이 반대쪽으로 하나씩 밟았다 — 한쪽은 y 구간만 봐서 겹침 10건, 다른 쪽은 `width < 500`으로 걸러 같은 y의 web 1440을 놓쳤다. **「내 것 제외한 모든 최상위 노드의 하단 최댓값」을 구해 그 아래로 내린다.** 배치 후 전수 겹침 재검사는 규칙 10이 이미 요구하고 있다.

- ⚠ **피그마·계약을 고쳐도 코드 토큰은 안 따라온다(08-24).** DeepCTA 면을 `semantic/surface/elevated`로 바꾸고 계약에도 적었는데 `color.cta.deep.bg`는 `neutral.100` 그대로였다. 면 토큰은 **피그마·계약·`semantic.tokens.json` 셋을 같이** 본다. 같은 자리에서 「계약에 규칙만 적고 구현을 빼먹는」 것도 났다 — "높이는 2줄에서 멈춘다"를 적어놓고 코드엔 말줄임이 없었다.
- ⚠ **CSS 변수는 부재가 에러를 안 낸다(08-24).** 스토리가 `--prism-color-blue-600`을 참조했는데 그런 변수가 없어 강조가 조용히 회색으로 상속됐다. 강조가 안 먹으면 변수 이름부터 확인한다. 덧붙여 코드 primitive `blue.600`은 다크값이 없고(`#007FD9` 단일) 피그마 `blue/600`은 모드가 갈린다 — **primitive를 직접 박으면 다크에서 안 읽힌다.**
- ⚠ **jsdom은 CSS를 안 그린다(08-24).** `:hover`·`:disabled` 상태 규칙이 통째로 죽어도 테스트가 조용히 통과한다. 값 검증이 불가능하니 **훅 클래스 존재라도 잠근다.** 시각 판정 자체는 스토리북에서 한다.
- ⚠ **새로 찍은 인스턴스도 곧바로 옛 세대가 된다(08-24).** 제품 파일 데모 5개를 만든 지 10분 만에 마스터를 또 고치자 프록시가 갈리고 그 5개만 옛 사본에 묶였다. **마스터를 고치는 중이면 데모/예시는 마지막에 찍는다.** 이미 찍었으면 기다리지 말고 같은 프록시에서 다시 찍어 갈아끼운다.
- ⚠ **`overriddenFields`에 없다고 override가 아니라고 단정하지 않는다(08-24).** `Report/Quick` 바깥 간격이 프록시 새 세대에도 안 내려와 「수락 대기」로 보고했는데, 마스터 값을 되써도 안 움직였다. **같은 프록시에서 새 인스턴스를 찍어 대조**해야 갈린다(새 것 44 / 기존 28). 그 배열이 해당 필드를 표현할 수 있는지부터 확인할 것.

- ⚠ **`skipInvisibleInstanceChildren=true`면 숨긴 자식이 `children`에서도 사라진다(08-24).** 인스턴스 자식을 `visible=false`로 끈 뒤 조회하면 노드가 없는 것처럼 보여 「클론이 내용을 잃었다」로 오진하기 쉽다. 스크립트 첫 줄에 플래그를 명시하고, 「소멸」로 보이면 플래그부터 의심한다. 덧붙여 **인스턴스 자식의 `visible`을 바꾸면 그 시점의 자식 참조가 무효화된다** — 이름을 먼저 모아두고 조작한다.
- ⚠ **「게시 ≠ 수락」이 항상 성립하진 않는다(08-24).** 내비 마스터에 중첩 인스턴스 + 불리언을 추가한 구조 변경이 사용 파일에 **별도 수락 없이** 반영됐다. 규칙으로 외우지 말고 매번 `getPublishStatusAsync()` + `getMainComponentAsync()`로 잰다.
- ⚠ **화면 단위 전수 감사에서 `nav.parent`를 화면으로 보면 헛돈다(08-24).** 내비가 `Chat Header`·`Frame 2147237055` 같은 래퍼 안에 있다. 화면은 **폭 320~430 + 높이 ≤900 + `clipsContent`**로 잡고, 넘침은 `absoluteTransform` 기준으로 잰다. 좌표만으로 판정하면 정상 화면을 오용으로 잡는다(`SA-1a` 오탐).

- ⚠ **정적 프레임 스냅샷에 스크롤 스트림의 합격 기준을 들이대지 않는다(08-24).** 852 프레임은 첫 화면일 뿐이고 스트림은 1811px까지 자란다. "첫 화면에 보여야 한다"로 각주 위치를 재니 어떤 자리도 통과를 못 했다 — 사용자가 스스로 스크롤해 여는 상태다. **다만 진짜 조건은 따로 있다**: 스트림 하단 패딩이 입력바 높이만큼 없으면 스크롤 끝에서 마지막 요소가 입력바 뒤에 갇힌다(현재 대기 화면 50px 겹침).
- ⚠ **비교 컷은 같은 빌드·같은 스크롤 위치에서 뽑는다(08-24).** `EdgeBand` 스톱 이동안을 "거의 차이 없음"으로 떨어뜨렸다가, 같은 스크롤로 다시 재니 확연히 달랐다. 밴드·페이드류는 **어느 글자가 램프의 어느 알파에 놓이는가**가 전부라 스크롤이 어긋나면 비교가 성립하지 않는다. 이 오판정 하나로 blur를 넣었다 걷는 한 바퀴를 돌았다.
- ⚠ **지원 규격 밖 치수로 재면 없는 결함이 만들어진다(08-24).** `ChatHistoryPanel`을 460px로 줄여 강제로 넘치게 만들고 "유리 푸터 아래로 행이 샌다"를 결함으로 보고했는데, 460은 존재하지 않는 치수였다(웹 1008 / 모바일 드로어 759×337). 실제 규격에선 깨끗하다. **넘침이 필요하면 크기를 줄이지 말고 데이터를 늘리거나 실재하는 좁은 규격 스토리를 쓴다. 결함 보고에는 측정 조건을 같이 적는다.**
- ⚠ **로컬 검증 서버 포트를 재사용하지 않는다(08-24).** 6099에 옆 세션이 띄운 다른 워킹트리의 옛 빌드가 살아 있었다. `http.server`는 `Address already in use`로 죽는데 curl은 200을 주니 내 빌드로 착각한다. **`lsof -a -p <pid> -d cwd`로 서버 루트를 확인하고 시작한다.**
- ⚠ **`mask-image`와 `backdrop-filter`를 같은 요소에 두면 블러가 죽는다(08-24).** 마스크가 backdrop root를 만들어 필터가 읽을 배경이 사라진다. 두 레이어로 갈라도 마스크 얹은 쪽이 같은 문제를 만든다. 그래서 CSS 단독으로 "잦아드는 backdrop blur"는 못 만들고, 세기가 다른 띠를 여러 겹 쌓는 수밖에 없다.
- ⚠ **피그마 background blur는 레이어 알파를 안 따라간다(08-24).** "그라데이션 fill + background blur = 알아서 progressive"가 아니다. 띠 구간 전체가 같은 세기로 흐려지고 끝에서 딱 끊긴다. 포팅 방식을 정하기 전에 프로브로 잰다.
- ⚠ **오래 열어 둔 PR은 머지 전에 실제 기여분을 다시 잰다(08-24).** 웹 헤더 393 상한을 소비처 오버라이드로 막아 둔 PR #200이, 그사이 `#218`이 `HeaderNavBar`에 `width` 축을 신설해 머지되면서 **코드 no-op**이 됐다. `git merge origin/main` 후 `git diff origin/main HEAD --stat`으로 확인한다.
- ⚠ **DS 파일 write는 현재 값을 대조하고 쓴다(08-24).** 각주 마스터를 바꾸려는 순간 주미님이 같은 자리를 직접 고치고 계셨다 — 스크립트가 `expect` 대조로 SKIP을 내서 덮지 않았다. 차이는 띄어쓰기 하나였고, 덮었으면 제품 파일과 DS가 조용히 어긋났다. read→write 사이에 값은 바뀐다.
- ⚠ **브랜치 전략을 정하기 전에 `git fetch`부터(08-24).** 로컬 `main`이 낡아 `git log main..HEAD`에 이미 머지된 커밋들이 보였고, "내 작업이 미머지 커밋 위에 얹혀 있으니 리베이스가 필요하다"로 오판할 뻔했다. fetch 후 보니 베이스는 이미 `origin/main`에 있었고 대상 파일 diff가 0이라 cherry-pick 한 번으로 끝났다.
- ⚠ **컴포넌트 세트 안의 variant를 단독으로 `getPublishStatusAsync`하면 `UNPUBLISHED`가 나온다(08-24).** 게시 단위는 세트다. 변이 하나만 물어보고 "미게시"로 판정하지 말 것. 수락 여부는 `importComponentByKeyAsync` 결과 노드 id와 인스턴스의 `getMainComponentAsync()` id를 대조해 가른다(같으면 수락 완료).
- ⚠ **오늘 날짜 worklog는 「쓰기 전」이 아니라 「착수 전」에 읽는다(08-24).** 세션이 여럿이면 **오늘의 결정도 이미 내려져 있다.** 앞 세션이 카피용 본문 컴포넌트 2개를 만들었다가 주미님 지적으로 당일 걷었는데, 그 기록을 안 읽고 몇 시간 뒤 같은 것을 다시 만들어 게시까지 받고 문서 2건에 결론으로 적었다가 철회했다(`9816:133016`·`5154:2475` 삭제, PR #211). 게시를 헛으로 두 번 만들었다.
- ⚠ **컴포넌트 속성으로 올라간 문구는 텍스트 노드에 바인딩이 없다(08-24).** `boundVariables.characters`가 비어 있어도 소실이 아니다 — `instance.componentProperties[key].boundVariables.value`를 본다. 게이트 모달 Title/Description이 `auth/gate-title`·`auth/gate-sub`에 멀쩡히 묶여 있는데 "교체가 끊었다"로 오판했다.
- ⚠ **공유 워킹트리에서 rebase하면 피어의 미추적 파일을 인질로 잡는다(08-24).** 로컬에선 미추적인데 그사이 main에 tracked로 들어온 파일이 있으면 체크아웃이 막힌다. 지우고 되돌리는 사이 옆 세션이 그 파일을 쓰고 있을 수 있다. **머지 전 리베이스는 `/tmp` worktree에서 한다.**
- ⚠ **같은 plan 문서를 두 세션이 같은 시간대에 쓰면 늦게 쓴 쪽이 이긴다(08-24).** git 이전에 **파일 내용 층**에서 먼저 깨진다 — plan-32에 넣은 절이 「열린 항목 0」 상태줄 갱신 저장에 통째로 덮여 사라졌고(복원 후 한 번 더), 반대로 내 plan-10 편집분은 옆 세션 커밋에 실려 들어갔다. **「트랙 종결」·「열린 항목 0」을 선언하며 문서를 재작성할 때는 그 시점 파일이 남의 미커밋 기록을 지우고 있지 않은지 먼저 본다.** 큰 절을 새로 쓸 땐 저장 직후 grep으로 자기 절이 살아 있는지 확인하는 게 싸다.
- ⚠ **한국어 명사를 일괄 치환하면 조사가 안 따라온다(08-24).** 정본 문구에서 `리포트`→`답변`을 `replace(/리포트/g)`로 파생했더니 `리포트를`이 **`답변를`**이 되어 4개 화면에 들어갔다. 전수 재독으로 잡았다. **을/를·이/가·은/는이 붙는 명사 치환은 조사까지 짝으로 바꾸고, 치환 결과 문자열을 눈으로 읽고 넣는다.**
- ⚠ **카피 세트마다 본문 컴포넌트를 만들지 않는다(08-24 확정).** 상태 축만큼 곱해진다 — 피드백 사유는 리포트 세트만 6개 상태라 세트별로 가르면 12개가 된다. 마스터는 한 세트를 기본값으로 두고 다른 세트는 인스턴스 텍스트 override로 얹는다. 비용은 마스터 구조를 바꿀 때 override 쓰는 화면의 라벨을 전수로 다시 보는 것.
- ⚠ **깨진 글자가 두부(□)가 아니라 랜덤 한글 음절이면 폰트가 아니라 인코딩이다(08-24).** 헤드리스 검증 harness에 `<meta charset="utf-8">`이 없었고 `python3 -m http.server`가 `.html`에 charset을 안 붙여, 한글·일본어가 전부 깨졌다. 이걸 "JP 폰트 없음 → 동적 서브셋"으로 두 번 연속 오진했다. **모양부터 보고 원인을 가른다.**
- ⚠ **원격이 `[gone]`인 브랜치 위의 커밋은 유실 직전이다(08-24).** 머지 후 원격 브랜치가 자동 삭제되면 그 위에 쌓은 커밋은 어디에도 없다. 워크로그 스킬 Step 2.5(`merge-base --is-ancestor origin/main`)로 잡는다. **squash 머지 오탐과 구분할 것** — 같은 검사에 걸려도 `git diff origin/main <sha> -- <path>`가 비어 있으면 내용은 main에 있다.
- ⚠ **깨끗해 보이는 원본에도 옵션 단위 override가 박혀 있다(08-24).** 프레임을 복제해 본문 슬롯을 갈았는데 한 항목만 옛 라벨이 남았다. 원본이 그 자리에 `componentProperties` override를 갖고 있었다. 본문 교체 후 라벨 전수 확인이 필요하다.
- ⚠ **`Skeleton` 바 색이 채팅 배경과 같다 — 배경 위 스켈레톤은 전부 투명하다(08-19).** `bg-surface-muted`와 `semantic/surface/base`가 둘 다 #F2F4F8이다. 카드 안(흰 면)에서는 보이니 "카드 안만 되네"로 오진한다. 피그마는 `neutral/100`(#EAEDF2)을 쓴다. 안 보이면 DOM 존재부터 확인하고, 있으면 자기 색과 부모 배경색을 같이 잰다. 수정은 미착수 — 모든 `Skeleton` 사용처에 걸린다.
- ⚠ **컨테이너 거터 + 컴포넌트 인셋이 이중으로 먹는다(08-19).** 스토리 래퍼가 `px-4`를 주는데 `ReportDeep`도 `surface-inset` 16을 가져 면이 361이 아니라 329로 그려졌다. **스켈레톤과 진짜 리포트가 똑같이 틀려서 눈으로는 안 잡혔다** — "둘이 같다"는 "둘이 맞다"가 아니다. 실제 폭은 피그마 인스턴스 실측이 정본(393→361 · 360→328 · 560→528).
- ⚠ **`claude_ai_Figma` 서버에는 static `Pretendard`가 없다(08-19).** 텍스트 쓰기가 "font family does not exist"로 튕기면 폰트 설치 문제가 아니라 서버 차이다 — `figma`(데스크톱) 서버로 바꾸면 통과. 목록에는 있는데 안 로드되는 경우는 갈래가 다르다(세그먼트 전량 로드).
- **게시 ≠ 수락은 variant 신설에도 그대로 적용된다.** DS에서 게시해도 제품 파일이 라이브러리 업데이트를 받기 전에는 `setProperties`가 "Unable to find a variant"로 막힌다. `importComponentByKeyAsync`는 성공하므로 그것만으로 판정하면 안 된다.
- **헤드리스 검증에서 한글 `textContent`로 DOM 요소를 찾으면 NFC/NFD로 조용히 실패한다.** 화면엔 멀쩡히 보여 "렌더 안 됨"으로 오진한다. 구조(클래스)로 찾는다. 곁다리 둘 — Storybook 에러 UI 문구는 정상일 때도 DOM에 있어 크래시 판정이 오탐이고, `--virtual-time-budget`에서 rAF 폴링은 스토리 마운트 전에 소진된다(`setInterval(50ms)` 사용).
- ⚠ **1440에서만 맞는 값은 복제하는 순간 틀린 값이 된다(08-19).** `web / home` 사이드바 패널이 세로 FIXED 1008인데 1024−16과 우연히 같아 정상으로 보였다. 768(Tablet)로 리사이즈하니 1008이 남아 counter-axis CENTER로 넘쳐 메뉴 y −112·아바타 y 812로 **둘 다 화면 밖**에 나가 rail이 백지로 렌더됐다. 에러도 리턴값 이상도 없고 스크린샷으로만 드러난다. 셸 프레임을 복제하기 전에 자식이 FILL인지 FIXED인지 본다.
- ⚠ **인스턴스 자식은 크기 override가 조용히 무시된다 — 값이 유한하면 컴포넌트 축으로 올린다(08-19).** 사고 과정 진행바를 컷마다 손으로 밀었지만 안 먹었고(일부는 `fill`이 FIXED라 sizing 변경조차 거부), 결국 DS에 `Thinking/Progress`(`stage` 6값)를 만들어 축으로 해결했다. 비율을 굳이 인스턴스에서 주려면 **부모 패딩**이 유일한 레버다. 반대로 분포 그래프처럼 **연속값은 축으로 못 담는다** — 컴포넌트화하면 오히려 비율을 못 바꾸게 되므로 손조립 + 계약 문서화가 맞다.
- ⚠ **오토레이아웃이 아닌 컴포넌트 세트에 변형을 붙이면 기존 변형 위에 정확히 포개진다(08-19).** 리턴값(이름·개수)은 정상이라 스크린샷으로만 드러나고, 세트 프레임도 자동으로 안 커져 새 열이 프레임 밖에 남는다. 격자 좌표와 프레임 크기를 직접 잡는다. 또 **variant를 바꾸면 하위 인스턴스가 교체돼 텍스트 override가 따라오지 않는다**(`default→answered`에서 선택지 문구가 기본값으로 되돌아갔다).
- ⚠ **피그마 섹션 ID는 세션 중에도 바뀐다(08-19).** 시안 섹션을 문서 3곳에 인용해 커밋한 뒤 주미님이 섹션을 다시 만들어(`9371:23718` → `9383:113289`) 옛 ID가 사라졌다. **자식 노드 ID는 그대로**라 `use_figma`는 동작하는데 `get_screenshot`만 "node not found"로 튕겨 도구 문제로 오인하기 쉽다. 부모가 없으면 자식에서 조상 체인을 거슬러 현재 부모를 찾는다 — 인용은 컨테이너보다 자식이 오래 산다.
- ⚠ **코드에 조합이 있다고 피그마에 그 variant가 있는 게 아니다(08-19).** `Callout`은 코드가 tone × variant를 다 열어두지만 피그마는 그린 것만 있다(`Layout=action`이 `Type=info` 한 장뿐이었다). 계약 `callout.md`가 "필요해지면 피그마부터 그린다"고 예고해 둔 자리 — **계약의 '없음' 문장을 먼저 읽으면 손조립을 안 한다.**
- ⚠ **`use_figma`의 clone은 `appendChild` 시점에 폰트를 요구한다(08-19).** 텍스트가 든 노드를 클론해 붙이면 `unloaded font "Pretendard Regular"`로 죽는다. 클론 **대상 안의** 폰트를 스크립트 맨 앞에서 로드한다.
- ⚠ **Tailwind `className`으로 `h-*`/`w-*`를 덮으면 진다(08-19).** `Skeleton` 기본이 `h-4 w-full`이라 클래스를 얹어도 CSS 출력 순서가 이기는 쪽을 정한다(나열 순서가 아니다). 커스텀 치수는 `style={{ height, width }}`로 준다 — 패키지가 이미 그렇게 쓴다(`ChatHistoryPanel.tsx:365`).
- ⚠ **「업데이트 수락」은 기존 인스턴스에만 필요하다(08-19).** DS에 새 variant를 게시한 직후 제품 파일에서 `importComponentByKeyAsync` → `createInstance`가 한 번에 됐다. 수락은 *이미 그 마스터를 물고 있는* 인스턴스를 새 사본으로 갈아끼우는 별개 동작이라, 새로 심는 작업까지 겁먹을 필요는 없다.
- ⚠ **그런데 `constraints`는 수락해도 기존 인스턴스에 안 내려간다(08-19).** 마스터에 `STRETCH`를 넣고 게시·수락까지 했는데 인스턴스는 계속 옛 값이었다. 직접 쓰기는 `cannot be overridden in an instance`, 자식 `resize()`는 조용히 무시, `resetOverrides()`도 안 고친다. **복구법은 인스턴스 재생성뿐**(문구 캡처 → 새 인스턴스 → 문구 복원 → 옛 것 제거). 판정은 **새 인스턴스를 하나 만들어 대조** — 그게 새 값을 받으면 라이브러리는 정상이고 캐시 문제다. **함정의 함정: 폭이 마스터와 같은 인스턴스에서는 안 드러난다**(61개 중 열 폭 800인 9개만 깨졌다). 검산은 값이 아니라 식으로(`chips.width === group.width − rail`).
- ⚠ **원격 프록시 노드는 게시 뒤에도 옛 이름·옛 기하로 파일에 남는다(08-19).** 제품 파일의 마스터 프록시만 보면 "수락 안 됐다"로 오진하는데 **실제 인스턴스는 이미 갱신돼 있었다.** 수락 판정은 프록시가 아니라 인스턴스 기하로 한다. `remote=true`·`parent=null`이면 프록시고 쓰기가 막히니(`Cannot write to internal and read-only node`), 진짜 마스터는 컴포넌트 key로 DS 파일에서 찾는다.
- ⚠ **클론한 텍스트의 글리프 누락은 파일 결함이 아니라 렌더 캐시다(08-19).** 클론 컷에서 일본어 한자만 안 보이고 가나는 멀쩡했다. `getStyledTextSegments`로 정상 형제와 대조하니 문자열·폰트가 완전히 동일 — 폰트 재로드 후 `characters` 재입력으로 복구된다. **화면 말고 데이터부터 대조한다.**
- ⚠ **클립 박스를 콘텐츠에 딱 맞추면 그림자가 네 면 다 잘린다(08-19).** 칩 행 `chips`가 361×40으로 칩 높이에 붙어 있어 그림자(blur 12·offset (0,2))가 전부 잘렸다. 인스턴스가 `clipsContent=false`인 걸 "드리프트"로 보고했는데 **그게 그림자를 살려두고 있던 것**이었다 — 플래그만 켰으면 홈 전 화면이 함께 잘렸다. 플래그 불일치를 고치기 전에 그 플래그가 켜졌을 때 무슨 일이 나는지부터 본다.


- ⚠ **릴리스 PR 감사는 PR diff만 보면 반쪽이다(08-19).** `#170`(0.12.1)은 자기가 소비하는 changeset만 보여줬고 그건 정확했는데, **changeset 없이 들어온 공개 토큰 2건**(`skeleton.bar.radius` 4→8 = `Skeleton` 전 소비처 · `chat-history-panel.skeleton-bar-height` 20→27)은 PR 어디에도 안 나왔다. changeset은 **버전 범프와 CHANGELOG 문구만** 정하고 발행 코드는 그 시점 main 전체라, "배포는 되는데 기록에만 없는" 변경이 생긴다. 감사할 때 `git diff <직전 릴리스 커밋> origin/main -- packages/*/src`를 같이 뜬다. 이번엔 `#171`로 changeset을 보태고 발행했다.
- ⚠ **같은 파일 끝을 쓰는 PR이 여럿이면 충돌 해소는 1회로 안 끝난다(08-19).** #163·#160을 각각 풀어 둘 다 `MERGEABLE`로 만들었는데 #163을 머지하는 순간 #160이 다시 `CONFLICTING`이 됐다. 스택도 참조도 없지만 둘 다 `plan-10` **파일 끝**에 새 절을 덧붙이는 구조라 앞이 들어가면 뒤가 어긋난다. 해소 시점이 아니라 **머지 시점** 기준으로 재고, 여러 개면 하나 넣을 때마다 나머지를 다시 본다. 또 `gh pr list`·`mergeable` 어디에도 **draft 여부가 안 나온다** — `--json isDraft`를 같이 봐야 한다(#169가 그래서 머지에서 튕겼다).
- ⚠ **"레포에 없다"를 근거로 삼는 감사는 워킹트리가 아니라 `origin/main`에 대고 한다(08-19).** 계약 버킷 감사를 `feat/edge-scrim-exploration`(main보다 **28커밋 뒤**) 기준으로 돌려 `Sheet/Header`·`Sheet/Intro`를 "미포팅", `Callout`을 "잘못 올라옴"으로 오판해 보고했다. 실제로는 PR #153이 8/18 머지됐고 Callout도 같은 날 의도적으로 복귀시킨 것이었다. **`git worktree add --detach /tmp/... origin/main`으로 정본 사본을 떠서 잰다.** 브랜치가 **앞선** 경우도 반대 방향 오판을 만드므로(예: `modal-shell.md`는 브랜치에만 있다) 판정은 양쪽을 다 재고 어느 쪽 근거인지 밝혀 적는다.
- ⚠ **워크플로 팬아웃은 검증 단계를 뒤에 몰면 세션리밋에 통째로 노출된다(08-19).** 감사 5개는 완주했는데 뒤에 붙은 **검증 19개가 전멸**했다 — 파이프라인이 앞단부터 채워져 리밋은 항상 뒷단을 친다. 되돌리기 어려운 판정이 걸린 단계는 앞에 두거나 팬아웃 폭을 줄여 앞뒤가 같이 끝나게 짠다. (기존 "감사·리서치류는 인라인이 기본" 규칙과 같은 방향의 3번째 사례)
- ⚠ **`remote === true`는 "우리 DS 것"이 아니라 "어떤 라이브러리 것"이다(08-19).** `모델 선택 리스트` 안 `ticker / tsla`는 **[VW] Design System** 소유라 우리 DS 페이지 전수 스캔(`✅ Components` 689 · `✅ Icons` 2085)에 안 나온다. 같은 이유로 **`importComponentByKeyAsync` 성공은 소유의 증거가 아니다** — DS 파일에서 자기 컴포넌트 키를 import해도 `remote=true` 사본이 잡힌다. 소유 판정은 페이지 스캔으로, import는 "가져다 쓸 수 있나"로만 쓴다.
- ⚠ **인스턴스 `overrides.length === 0`은 "정상"이 아니라 "전부 마스터와 같다"일 뿐이다(08-19).** 제목이 이상해 보여 결함으로 보고했는데 override 0을 정상 근거로 인용했고, 실제로는 **선택지 텍스트(동적 데이터)의 예시값**이었다. 동적 데이터가 들어갈 자리는 마스터에 박힌 값이 곧 예시라 결함과 구분이 안 된다 — 자리의 의미를 먼저 확인한다.
- ⚠ **계약 문서가 인용한 피그마 근거도 실측 대상이다(08-18).** `callout.md`가 "action 표면 = Figma `semantic/surface/base` + `semantic/border-default`"라고 적고 있었는데 **두 변수는 그 노드에 바인딩돼 있지 않았다.** 실측하니 그 variant는 `Type=info`와 완전히 같은 변수를 쓰고 있었다. 원인은 variant 이름이 톤을 안 말하는 자동 생성값(`Type5`)이라 생긴 오독이고, 같은 이름이 `plan-26` 감사에서도 한 번 사람을 헛짚게 했다.
- ⚠ **`appendChild`도 폰트 로드가 필요하다(08-18).** 텍스트를 품은 노드를 재부모화할 때 `unloaded font "Pretendard Medium"`으로 죽는다 — `characters` 편집만 폰트가 필요한 게 아니다. 서브트리 TEXT를 `getStyledTextSegments(['fontName'])`로 전수 수집해 로드하면 통과한다. 스크립트는 원자적이라 실패해도 파일은 안 망가진다.
- ⚠ **의미 기반 토큰은 값 일치로 일괄 바인딩하면 안 된다(08-18).** `paddingBottom === 34`를 한 번에 묶었더니 `Toggle`이 딸려 들어갔다 — 토글 하단 여백이 우연히 34였을 뿐 홈 인디케이터와 무관하다. `size/*` 같은 **스케일** 토큰은 값으로 묶어도 되지만 `layout/safe-area/bottom` 같은 **의미** 토큰은 쓰임을 봐야 한다.
- ⚠ **교체 작업의 검증은 스크린샷이 아니라 치수다(08-18).** 모달·시트를 인스턴스로 갈아끼울 때 함정 3건이 전부 "높이가 원본과 같은가"로만 드러났다 — ①INSTANCE_SWAP은 로컬 컴포넌트일 때 key가 아니라 **id**로만 먹고, key를 주면 `setProperties` 전체가 원자적으로 실패해 다른 속성까지 안 들어간다 ②`componentProperties`만 복사하면 **레이어 가시성 override**가 안 따라온다 ③DS에서 새로 만든 텍스트는 `lineHeight`가 auto라 원본 160%와 6px 어긋난다.
- ⚠ **제품 파일 페이지 id를 스크립트에 박지 말 것(08-18, 3회 반복).** `🍏 login` 소멸 → `chat/chart` → `chat/report` 개명 → `Login/Setting` 신설로 화면 88개가 이동했다. 마지막엔 변환 대상이 **0건으로 조용히 지나갈 뻔했다.** 대상은 이름 패턴으로 재탐색하고 페이지는 부모를 거슬러 찾는다.
- ⚠ **게시 ≠ 수락(08-18).** 라이브러리를 게시해도 사용하는 파일은 자동으로 안 바뀐다. 변수는 자동으로 내려오지만 **컴포넌트 구조 변경은 Assets → 라이브러리 → 업데이트 수락**이 따로 필요하다. 확정 진단은 키로 `importComponentByKeyAsync` 해서 인스턴스의 현재 마스터와 대조 — 같은 키인데 사본이 둘이면 수락 대기다. 단 **옛 세대 프록시를 문 인스턴스는 수락이 안 먹어** 백업 후 `swapComponent`가 유일한 길이다.
- ⚠ **집계할 때 조상에 INSTANCE가 있는지 먼저 거른다(08-18, 하루 3회).** ①변형별 `findOne`이 없는 갈림을 만들어냈다 ②5일 된 감사표 수치를 그대로 믿었다 ③`Settings/Row` **인스턴스 안쪽** 프레임 58개를 손복제로 셌다. 감사표는 착수 시점에 다시 재는 게 전제다 — 8개 항목 중 수치가 맞은 건 하나뿐이었다.
- ⚠ **`resetOverrides()`는 이름과 채움까지 되돌린다(08-18).** 원복하려고 불렀더니 노드 이름이 마스터 이름으로 풀려 다음 스크립트가 대상을 0개로 셌고, 마스터에 없던 fill·stroke 비표시 override도 함께 날아갔다. `swapComponent`도 옛 노드의 페인트 `visible:false`를 새 노드에 얹는다 — **리턴값은 정상인데 화면만 틀린 부류라 스크린샷으로만 잡힌다.**
- ⚠ **마스터에서 노드를 교체하면 그 노드의 값 override만 풀린다(08-18).** 표시 on/off·변형·형제 override는 생존한다. 백업할 것은 "그 노드의 값"이고, 이름·인덱스·기본 visible을 원래대로 맞춰 넣으면 나머지는 붙는다. 리스크를 말하기 전에 **마스터 기본값부터 읽을 것** — 기본이 꺼짐이면 최악은 "튀어나옴"이 아니라 "안 보임"이다.
- ⚠ **공유 워킹트리에서는 검증 실패가 남의 중간 상태일 수 있다(08-18).** `validate-design-system.sh`가 계약 하나가 표에 없다며 실패했는데, 피어가 계약 파일을 만들고 표에 등록하기 전 찰나였고 잠시 뒤 그대로 통과했다. **내 변경 탓으로 단정하고 고치러 들어가면 남의 in-flight 작업을 건드린다.** 커밋도 hunk를 갈라서 한다 — 분리 불가능하면(내 절이 남의 블록 뒤에 이어 붙어 한 hunk가 되면) 아예 손대지 않는 게 낫다.

- ⚠ **행동하기 전에 오늘 worklog를 읽는다 — CONTEXT는 스냅샷이라 늦는다**(08-18 사고). 어제 컨텍스트로 만든 지도를 믿고 릴리스 PR을 머지했다가 **폐기 확정된 기능을 npm에 내보냈다.** 그 순서 제약은 그날 worklog에 이미 적혀 있었다. 이 파일이 stale이면 `check-context-freshness.sh`가 경고한다 — 경고가 뜨면 worklog와 repo 상태를 먼저 읽는다.
- ⚠ **PR 체크의 `UI Tests: N unchanged`는 시각 근거가 아니다.** 일상 빌드는 스냅샷이 꺼져 있어 "차이 없음"이 아니라 "비교 안 함"이다. 시각 판정은 `workflow_dispatch` 전체 비교로만 한다(`forceRebuild`가 08-18 #164로 들어가 이제 실제로 찍힌다). 같은 초록을 08-18에 세 세션이 각각 오독했다.
- ⚠ **문서를 인용하기 전에 코드를 연다**(08-18). 마이그레이션 노트에 "기본값 `false`"라고 썼는데 발행값은 `true`였다 — 출처는 승격 전에 쓰인 계약 문장이 안 고쳐진 채 남은 것이었고, 같은 CHANGELOG 안에 두 값이 공존하고 있었다.

- **"게시했다"와 "소비 파일이 수락했다"는 다르다(08-14).** 라이브러리를 게시해도 제품 파일은 옛 스냅샷을 계속 그릴 수 있다. **게시 실패로 오진하기 전에 `importComponentByKeyAsync(key)`로 게시본을 직접 불러와 대조할 것.** 판정은 세 단계 — ①마스터가 살아 있나(DS 파일) ②게시본에 들어갔나(키로 import) ③소비 파일이 수락했나(인스턴스 `mainComponent.parent.id`).
- **인스턴스 override는 같은 값 재대입으로 안 생긴다(08-14).** 마스터 변경을 따라가지 않게 고정하려면 **다른 값을 한 번 넣었다가 되돌려야** `overrides`에 잡힌다.
- **커밋 전에 "이 브랜치가 어느 PR의 head인가"를 확인할 것(08-14).** `feat/edge-scrim-exploration`에 커밋했다가 고아가 될 뻔했다 — PR #157의 head는 `merge/edge-scrim-main-2026-08-13`이다. 그리고 **경로 지정 커밋은 공유 파일에서 남의 작업을 끌고 온다** — 다른 브랜치로 옮기면 그 몫이 CI를 깨뜨리므로 걷어내야 자립한다.

- **게시 완료와 소비 파일 반영은 다른 사건이다(08-12).** DS에서 `getPublishStatusAsync`가 `CURRENT`여도 제품 파일 인스턴스는 새 속성을 못 본다 — 소비 파일 쪽 **라이브러리 업데이트 수락**이 따로 있다. **판정은 세트가 아니라 인스턴스의 `componentProperties`로 한다.** 급하면 `importComponentByKeyAsync(mainComponent.key)`로 게시본을 끌어와 `swapComponent` — 같은 키라 override(변수 바인딩·텍스트·크기)가 살아남지만, 먼저 1개로 시험하고 나머지에 돌린다.
  - **업데이트를 받은 뒤에도 옛 마스터 프록시는 갱신되지 않는다(08-13).** 인스턴스 143개가 전부 새 세트로 넘어간 뒤에도 옛 프록시 노드(`7751:120479`·`7751:120478`)는 옛 세트 ID와 옛 레이어명을 계속 리포트했다. 프록시로 판정하면 영원히 "아직 안 됐다"가 나온다 — **인스턴스의 `getMainComponentAsync()` 집계로만 판정한다.**
  - **`importComponentByKeyAsync` 실패는 "미게시/삭제" 근거가 아니다(08-13).** 멀쩡히 published된 `Answer/Footer` 키가 "not found"로 튕겼다. 생존 판정은 `search_design_system`(`componentKey`·`updatedAt`이 나온다).
  - **참조가 두 갈래로 갈리면 호스트 컴포넌트별로 묶어본다(08-13).** 호스트별로 딱 정렬되면 인스턴스 수락 문제가 아니라 **호스트 마스터가 옛 것을 참조**하는 것이고, 소비 파일에서 업데이트를 눌러도 숫자가 안 변한다. 고칠 자리는 원본 라이브러리다.
  - **`INSTANCE_SWAP` 기본값은 componentKey가 아니라 노드 id를 받는다(08-13).** key를 넣으면 `Property value is incompatible with component property type`. `preferredValues`의 `type`도 `COMPONENT`/`COMPONENT_SET`만 받고 `LIBRARY`는 거부된다.
- **컴포넌트 세트가 에러 상태면 속성 API가 통째로 실패한다(08-12).** `variantGroupProperties`·`componentPropertyDefinitions` 조회가 `Component set has existing errors`로 튕기고 축·속성 추가도 막힌다. `Text Field`(`52:49`)의 원인은 **같은 좌표에 겹친 중복 variant 1개**라 캔버스로는 안 보였다 — variant 이름 목록을 뽑아 중복을 세면 잡힌다. 문법을 의심하기 전에 세트 무결성부터 본다.
- **문서에 적힌 차단 사유를 액면가로 받지 않는다(08-12).** 캡션이 "lucide:eye 추가 후 반영(잔여)"라고 적어 뒀지만 `lucide:eye`·`eye-off`·`eye-closed`는 이미 DS에 있었고 진짜 차단은 위 세트 오류였다. **"왜 못 했는지"가 적혀 있으면 그것부터 실측한다** — 아니면 없는 작업을 하러 간다.
- **클론한 노드에 `characters`를 쓰면 원본의 strings 바인딩이 조용히 풀린다(08-12).** 기존 컷을 복제해 문구만 덮으면 하드코딩이 되어 JP·EN 모드에서 한국어가 그대로 뜬다. 복제로 화면을 만들면 **바인딩 재연결이 기본 절차**다. `swapComponent` 직후의 인스턴스 subtree를 `findOne`으로 훑는 것도 스테일 자식 참조로 터지므로 얕은 `children.find()`를 쓴다.- **`pnpm visual`의 "Chromatic passed"는 시각 검증이 아니다(08-12).** 스토리북 프리뷰 기본값이 `VITE_STORYBOOK_FULL_SNAPSHOTS !== "true"`면 스냅샷을 꺼서, 옵트인한 `Theme` 스토리 2장만 찍힌다. 홈 3연속 변경(625·626·627)이 전부 passed였지만 셋 다 비교 대상이 아니었다. **전체 비교는 머지 전 워크플로 `workflow_dispatch`로 따로 돌린다.**
- **공유 워킹트리에서 hunk 분리가 안 되면 blob으로 가른다(08-13).** 옆 세션의 미커밋 변경 사이에 내 편집이 끼면 `git add -p`로 못 가른다. **HEAD에 내 편집만 재현한 파일을 만들어 `git hash-object -w` + `git update-index --cacheinfo`로 인덱스에 직접 넣으면** 워킹트리를 건드리지 않고 갈린다. 단 **staged 단독으로 typecheck/test가 도는지 실측할 것** — 내 코드가 피어 코드를 참조하고 있으면 그 순간 깨진다(잠시 스왑해 검증 후 원복). 08-13에 3회 사용.
- **구조 리턴값이 정상인데 화면이 깨지는 부류가 있다(08-13, 하루에 3건).** ① `layoutMode: NONE` 세트에서 `variant.clone()`은 원본 x/y를 물고 와 **원본 위에 정확히 포개진다** ② `createInstance()`로 만든 새 인스턴스는 **마스터 기본값**으로 태어나 옆 인스턴스의 override(면·테두리)를 안 물고 온다 ③ 세트가 `GRID`면 행 트랙이 콘텐츠를 hug해 자식을 숨기는 것만으로 마스터 높이가 바뀐다(255는 고정 상수가 아니라 플레이스홀더가 만든 값이었다). 셋 다 이름·개수·크기 리턴값은 완벽했고 **스크린샷으로만 드러났다.** 피그마 write 뒤 스크린샷은 선택이 아니라 검증 단계다.
- **기획서와 와프가 갈리면 요소의 존재는 와프, 문구는 기획서다(08-12 결정, `rules.md` 35번).** 와프의 카피는 스냅샷 시점에 묶여 쉽게 낡고, 기획서 문장은 그려지지 않은 것까지 적는다.
  - **적용 전에 그 와프 보드가 몇 판인지 본다(08-12).** `설정/계정` 보드(`955:15598`)는 배지가 **`v.1.0.5`**였다(v1.0.6 배지 0건). 해당 절이 v1.0.6 변경 목록에 없어 내용은 유효했지만, 보드 단위로 판이 달라 "와프=최신"은 성립하지 않는다.
  - **한 컴포넌트 안에서 둘이 동시에 돈다** — 로그아웃 모달은 타이틀=화면정의서 원문, 서브카피=와프(기획서에 없는 요소)다. "어느 쪽이 이기냐"로 읽으면 한쪽을 통째로 버리게 된다.
- **override가 박힌 인스턴스를 템플릿으로 복제하면 컴포넌트 속성이 죽어 있다(08-12).** label TEXT에 characters·fill 직접 override가 있으면 `setProperties`가 **에러 없이 통과하는데 화면은 안 바뀐다.** 반환값만 보면 성공으로 읽히므로 렌더로 확인해야 한다. 새 인스턴스는 마스터에서 만든다. `Settings/Row`의 leading 아이콘 슬롯은 이름이 `lucide:*`가 아니라 **`icon`**이라(스왑된 것만 `lucide:*`로 보인다) 이름 접두사로 찾으면 trailing chevron을 잡는다 — 슬롯은 부모 관계로 판별한다.
- **오버레이를 삽입하면 오토레이아웃 플로우에 편입된다(08-12, 2회 재발).** HORIZONTAL이면 화면 밖으로 나란히 밀리고, VERTICAL이면 뒤 콘텐츠가 눌린다(`Chat History Panel` 852 → 1). 삽입 직후 `layoutPositioning = 'ABSOLUTE'`가 기본 절차다. `clone()` → `appendChild`도 **텍스트를 안 건드려도 폰트 미로드로 튕기므로**(reparent 계열 공통) 폰트를 먼저 로드한다 — MCP가 로컬 폰트를 못 보는 건과는 다른 원인이다.
- **인스턴스 자식 크기 override는 조용히 무시된다 — 비율은 패딩으로 준다(08-12, `rules.md` 36번).** `resize()`가 에러 없이 통과하는데 값이 안 바뀐다(위치는 반대로 즉시 에러). 부모를 오토레이아웃 + 자식 FILL로 두고 `paddingRight = 폭 × (1−비율)`로 주면 인스턴스마다 연속값이 나온다. DS `Chart/ProgressBar`의 "인스턴스 사용 불가" 표기는 **영구 제약이 아니라** track이 오토레이아웃이 아니라서 생긴 것.
- **전파 판정을 소비처 1회 읽기로 하지 않는다(08-12).** "in-place 수정은 게시 없이 내려간다"는 관측이 한 번 나왔다가 몇 분 뒤 재현 실패했다 — **같은 세션에서 방금 임포트한 마스터는 라이브를 물고 와 "이미 내려왔다"처럼 보이고**, 플러그인 안에서 구별할 수 없다. 판정은 게시 다이얼로그(사람)가 한다.
- ⚠ **인스턴스 override로 덮은 정합은 마스터의 구멍을 숨긴다(08-19).** `Thinking/Toggle` Deep **펼침**의 `Progress`가 `단계문구`에 안 묶여 있었는데, 제품 파일 인스턴스에 직접 `visible=false` override를 걸어 둔 탓에 8/14 실측이 "진행바 숨김 유지"로 통과했다. 라이브러리 수락 한 번에 인스턴스 9개로 되살아났다. **상태 바인딩은 인스턴스가 아니라 마스터에서 확인한다**(`componentPropertyReferences`가 `{}`인지).
- **Figma override는 부모 경로가 아니라 대상 노드 id로 매칭된다(08-19 실측).** 도착 줄 3개를 새 `ArrivedSlot > card` 아래로 옮겨 부모가 바뀌었는데도 `visible` override 27개가 전부 살아남았다. 구조 개편 시 **노드를 지웠다 새로 만들지만 않으면** 인스턴스 상태는 유지된다.
- ⚠ **1:1 렌더로 색·선을 판정하지 말 것(08-19).** 2px rail과 1.5px 외곽선은 등배 스크린샷에서 사실상 안 보여, 값이 정확한데도 "채움이 안 먹었다"로 오진했다. `node.screenshot({scale:3})`으로 확인한다.
- ⚠ **오토레이아웃이 리플로우를 한 박자 놓친다(08-19).** 같은 스크립트로 4 variant를 고쳤는데 하나만 자식 x좌표가 갱신되지 않아 겹쳐 보였다(속성값은 정상). `appendChild` → `insertChild(0)`로 리플로우를 강제하고, 다중 variant 편집 뒤에는 좌표를 검산한다.
- **불리언 속성의 기본값은 "가장 오래 머무는 상태"로 둔다(08-12, `rules.md` 37번).** `Thinking/Toggle`의 `단계문구` 기본값이 종료 상태(`false`)라 ① 세트 프리뷰가 컴포넌트를 설명하지 못했고 ② 새로 꽂는 인스턴스가 전부 종료 상태로 태어났다. **바인딩된 레이어는 마스터에서 `visible`을 켜도 안 보인다 — 속성 값이 이긴다.** "케이스가 없다"가 실은 "기본값이 꺼져 있다"인 경우를 의심할 것.
- **위계는 변하는 쪽이 가져간다(08-12).** 고정 라벨은 한 번 학습되면 읽히지 않는다. `Thinking/Toggle`은 라벨 `neutral/400` / 변하는 단계 문구 `neutral/500`, 이력은 완료 400 / 현재 500이다.
- **계약 파일(`component-contracts/*.md`)은 포팅 전에 만들 수 없다(08-12).** 문서 게이트가 storybook-map 등재 + 실제 story 존재를 요구한다. 포팅 전 계약은 `design-system/components/*.md` 안 초안 절에 두고 포팅 PR에서 옮긴다.
- **결정이 plan 본문에만 있으면 다음 세션이 되살린다(08-12).** 사고과정 말투가 plan-27에만 있었고 근거 노트는 이미 삭제된 상태였다. **완료 조건에 "이 결정이 다음 사람에게 어떻게 전달되나"를 넣는다** — plan은 작업 기록, 다음 사람이 읽는 건 룰이다.
- **머지 전에 "이 PR을 뒤집는 미커밋 작업이 있나"를 본다(08-12 실사고).** #151을 CI·Chromatic만 확인하고 머지했는데, 공유 워킹트리에 **미커밋으로** 그 결정을 정정하는 changeset이 있었다(기획서 미러가 낡아 생긴 오판을 캐논 대조로 잡은 것). 낡은 근거로 만든 PR이 main에 들어갔다. 공유 워킹트리에서는 **미추적 파일도 읽어야 현재 판단을 안다.**
- **공유 브랜치가 CI를 통과하는지 먼저 확인한다(08-12).** `feat/edge-scrim-exploration`은 `index.ts` export와 테스트만 커밋되고 대상 파일·구현이 미커밋이라 typecheck 8건·test 2건이 깨져 있었다. `git add` 범위가 넓어 남의 파일을 반쯤 쓸어간 결과다. **PR을 열 수 있는 상태인지는 커밋 목록이 아니라 CI로 판정한다.**
- **`env()`·`dvh` 기반 규칙은 뷰포트 메타 두 줄에 걸려 있다(08-12).** `viewport-fit=cover`가 없으면 `env(safe-area-inset-*)`는 iOS에서 **항상 0**이고, `interactive-widget=resizes-content`가 없으면 iOS가 키보드에 대해 시각 뷰포트만 줄여 `dvh`가 안 변한다. 규칙을 세우기 전에 **그 규칙이 반응하는 신호가 실제로 변하는지** 확인한다.
- **넘침을 결함으로 판정하기 전에 `visible`을 본다(08-12).** `deep` 계열 15장에서 Bottom Bar가 프레임을 135 넘어간 걸 보고 구조를 바꿨는데 그 15개는 전부 숨은 노드였다(원복). `findAll`은 숨은 노드도 돌려주고 `skipInvisibleInstanceChildren`은 인스턴스 내부만 건너뛴다. 덧붙여 **넘침량 ≠ 잘리는 픽셀** — 패딩 있는 프레임은 마지막 자식의 하단으로 재야 한다(51로 보고했으나 실제 11).
- **새 variant 축은 override 있는 인스턴스에 안 내려간다 — 죽은 축 재활용이 더 싸다(08-12).** Bottom Bar 도킹 축이 필요했는데, 새 축은 기본값으로 박혀 어차피 인스턴스 128개를 다시 지정해야 했다. 실사용 0이던 `HomeIndicator` 축을 `Dock=screen/inline`으로 **이름만 재정의하니 바인딩이 보존**됐다(`off`→`inline` 정확 매핑).
- **복제본의 절대배치 자식은 원본 좌표를 들고 온다(08-12).** 852 화면을 `clone()` 후 664로 리사이즈했더니 `InputTopFade`가 옛 y에 남아 "잘린 것처럼" 보였다. 프레임 리사이즈는 절대배치를 안 옮긴다.
- **jsdom은 `env()`를 못 읽어 인라인 `style`을 통째로 버린다(08-12).** `style={{ height: "env(safe-area-inset-bottom,0px)" }}`가 DOM에서 사라져 테스트가 떨어진다(브라우저는 정상). Tailwind 임의값 클래스로 두면 클래스 문자열이 남아 검증도 되고, 패키지가 소스 `.tsx`를 배포하므로 소비자 Tailwind가 유틸리티를 생성한다.
- **오버레이 앵커를 문서에 적을 땐 기준 노드까지 적는다(08-12).** `toast.md`의 "컴포저 상단 −16"이 재현 안 돼 헤맸는데, 기준이 `Bottom Bar`가 아니라 그 안의 **`Chat Input Bar`(보이는 카드) 상단**이었다(6px 차).
- **화면 프레임이 오토레이아웃이면 삽입한 배경·스크림이 화면 밖으로 밀린다(08-12).** 제품 파일 history 화면 12장이 전부 가로 오토레이아웃이라 `insertChild`한 노드가 플로우에 편입돼 x=393·786에 나란히 놓였다. 스크린샷 전엔 "왜 배경만 보이지"로 읽힌다 — 삽입 후 `layoutPositioning = "ABSOLUTE"`를 반드시 세운다.
- **생성 파일은 부분 스테이징으로 가른다(08-12).** 워킹트리를 공유하면 `theme.css` 한 파일에 내 토큰 변경과 옆 세션 재생성분이 함께 얹힌다. 통째로 커밋하면 남의 작업을 쓸어가고 빼면 소스와 산출물이 어긋난다. `git diff`에서 내 hunk만 필터해 `git apply --cached`로 스테이징하면 워킹트리를 건드리지 않고 갈린다(옆 세션 렌더도 안 깨짐).
- **variant를 `clone()`으로 늘리면 `componentPropertyReferences`가 소실된다**(08-11, `Settings/Row` `Type=select`). 속성 축은 패널에 멀쩡히 보이고 값도 바뀌는데 렌더가 안 따라와 인스턴스 버그로 오진하기 쉽다. 클론 직후 원본과 refs를 대조하고 임시 인스턴스로 토글 검증할 것 — 게시 후 발견하면 재게시까지 필요하다.

- Product 파일 홈 화면(`5027:3909`)의 컴포저는 DS 라이브러리 마스터가 아니라 **Product 파일 내 로컬 카피**(`5338:50726`)를 참조한다(포크 상태 — DS 마스터를 고쳐도 전파 안 됨). 라이브러리 publish 후 재연결은 defer.
- 죽은 로컬 브랜치 ~20개(`codex/*`·`canary/*`)가 남아 있다. 정리 시 머지 여부를 개별 확인한다.
- **공유 체크아웃의 미커밋분은 `GIT_INDEX_FILE` 스냅샷으로 백업한다(08-12).** `git stash`는 워킹트리를 되돌려 옆 세션 작업을 깨므로 쓰면 안 된다. `GIT_INDEX_FILE=/tmp/x git read-tree HEAD && git add -A && git write-tree` → `git commit-tree -p HEAD` → 백업 브랜치 push. 워킹트리·인덱스 무변경이고 로컬 전용 커밋도 부모 사슬로 함께 보존된다(실사용: `backup/edge-scrim-wip-2026-08-12`, 31파일).
- **같은 폴더에서 두 세션이 동시에 작업하는 일이 반복된다(07-31·08-03·08-10~11).** 대응: 편집 중인 파일은 먼저 별도 브랜치에 스냅샷해 두고, 브랜치 전환·머지는 **전부 `git worktree`로 분리**한다. 공유 체크아웃에서 브랜치를 갈아타면 남의 미커밋 작업이 사라진다.
- **"PR 냈다"를 완료로 쓰지 않는다(08-03).** 완료 처리한 세 건이 전부 OPEN이었고 하나는 CI가 깨져 있었다. 로컬 `pnpm typecheck`/`pnpm test`는 CI 게이트가 아니다 — CI가 도는 건 **`pnpm check`**다.
- **이월 문서는 반나절 만에 낡는다(08-11).** 이월 항목 착수 전 해당 plan 상태 줄과 최근 머지를 먼저 본다 — 피어 세션이 밤사이 전건 완료한 항목을 다시 착수할 뻔했다.
- **버전 관리 밖 작업이 실제로 쌓인다.** 소비처가 없는 탐색물은 PR에 섞지 말고 wip 브랜치로 뺀다(예: `wip/verdict-engine-second-target`).
- **라이브러리 업데이트가 와도 기존 인스턴스는 옛 값을 들고 있다(2026-08-06).** 게시·수락이 끝났는데 버블 폭이 안 바뀌었다 — 인스턴스의 `boundVariables`에서 maxWidth/minWidth가 빠져 있었고 모드·variant 어느 쪽도 안 먹었다. **부모 인스턴스에 `resetOverrides()`** 해야 복구되며, 텍스트 override가 함께 날아가므로 **저장→리셋→복원** 절차가 필요하다. 또 업데이트 후 중첩 인스턴스 **이름이 바뀐다**(`BubbleBox`→`_TextBubble`) — 이름 대신 `componentProperties` 보유 여부로 찾는다.
- **라이브러리 publish는 플러그인 API에 없다.** DS에 컴포넌트를 만들어도 주미님이 Figma에서 직접 게시해야 제품 파일이 본다. 신설 → 게시 대기 → 인스턴스 교체가 기본 리듬이다.
- **게시된 variant를 지우면 다른 세션 인스턴스가 뜬다(07-28).** 축 개편 전 소비자 확인.
- **plan-20 감사표 수치를 근거로 쓰기 전에 다시 센다(2026-08-03).** Tier 1 재사용 잔여는 **5종**이다 — `MessageBubble`·`Button/ActionBar`는 Deep 경로에선 아직 인라인이고, `HeaderNavBar`도 3곳 중 2곳만 치환됐다(`DeepWaitVote.stories.tsx:357`). 같은 날 "6종→4종→3종"으로 두 번 줄였는데 두 번 다 낙관적이었다.
- **한 페이지에서 못 찾은 걸 "파일에 없다"로 쓰지 않는다(2026-08-03).** `ModelChip`·`ChipGroup`이 ✅ Components 검색에 0건이라 "Figma에 없다"고 단정했는데 **틀렸다** — `🧪 Opinion Viz (ideation)` 페이지라 안 잡혔을 뿐이다. 남은 건 제작이 아니라 **ideation → ✅ Components 승격**이다.
- **병렬 팬아웃은 Figma MCP 의존 작업으로 채우지 않는다(2026-08-03).** 조사 4건을 서브에이전트로 갈랐다가 전부 MCP 재연결에 걸려 3시간 반 동안 진전 0. 나눌 땐 **외부 의존이 없는 쪽부터** 가른다.
- **게시 스냅샷은 DS 현행이 아니다(2026-08-10).** `importComponentSetByKeyAsync`로 본 variant 목록은 **마지막 게시 시점**이다. **세트에 변형이 없으면 "왜 없는지"를 계약 문서에서 먼저 확인한다.**
- **원격 컴포넌트는 노드 ID가 아니라 component key로 인용한다(2026-08-10).** 원격 DS 컴포넌트의 제품 파일 프록시 ID는 어느 쪽에서도 안 열려 "소멸"로 오진하기 쉽다. `design-system/rules.md` 30번.
- **인스턴스 감사는 중첩 포함이 기본이다(2026-08-10).** 최상위만 훑으면 중첩 안의 고아를 놓친다.
- **SECTION 자식의 `x`/`y`는 섹션 기준 상대좌표다(2026-08-11).** `sec.x`를 더해 옮기면 자식 전체가 섹션 밖 수만 유닛으로 밀리는데, **자식끼리의 상대 배치는 그대로라 개별 프레임 스크린샷은 멀쩡하다.** 신호는 섹션 스크린샷의 `original_width`가 `sec.width`와 다른 것. 검증은 `자식.x + width ≤ sec.width`로 하고 절대bbox와 섞어 계산하지 않는다(오프셋이 상쇄돼 "이상 없음"이 나온다).
- **드리프트가 규칙적이면 상위 합성 마스터를 의심한다(2026-08-10).** 값이 variant별로 깔끔히 갈리면 인스턴스 오버라이드가 아니라 마스터 내부값이다 — DS 24노드 수정으로 제품 230개가 자동 정합된 사례. `design-system/rules.md` 32번.
- **`swapComponent`는 텍스트 override를 흘린다(2026-08-09·08-10 두 번).** 스왑 전후 텍스트 대조 게이트(NFC normalize) 없이는 파일명·placeholder가 마스터 기본값으로 조용히 덮인다. `design-system/rules.md` 31번.
- **변수 바인딩된 paint의 opacity(<1)는 게시·인스턴스 경계에서 소실된다(2026-08-10).** 틴트는 alpha가 아니라 그 값을 가진 컬러 토큰을 바인딩한다. 게시 포함 여부 판정은 DS에서 `getPublishStatusAsync`(CURRENT/CHANGED) 실측으로 한다.
- **제품 파일에 붙은 기획서 보드 이미지는 구버전 스냅샷이다(2026-08-11).** 카피·규칙 정합의 근거는 항상 노션 버전 폴더 최신본 원문이다.
- **invisible 인스턴스 자식은 기본 스캔에서 빠진다(2026-08-11).** 전수 감사·수정 스크립트는 `figma.skipInvisibleInstanceChildren = false`를 명시하고 시작한다.
- **컴포넌트 TEXT 속성이 걸린 라벨은 `characters` 쓰면 마스터 기본값으로 리셋된다(2026-08-11).** 인스턴스 복제 후 라벨을 바꿀 땐 `setProperties({'label#…': …})`로 속성 단위로 쓴다. 같은 이유로 라이브러리 업데이트 수락 전 인스턴스는 `setProperties`가 새 variant를 못 봐서 실패한다 — variant key `importComponentByKeyAsync` + `swapComponent`로 우회(텍스트 NFC 대조 게이트 필수).
- **게시 다이얼로그는 해제 선택을 기억한다(2026-08-11, 추정).** 로컬 상태가 정상인데 CHANGED가 반복되면 파일이 아니라 게시 선택 목록을 본다.
- **"위반 N건" 감사 count는 판정 전 숫자다(2026-08-11).** stroke "위반 43건"의 실제 수정 대상은 9노드였다 — 계약·코드·규칙 3자 대조 없이 count로 달려들면 계약을 깬다.
- **공유 워킹트리에서 `git add -A` 금지(2026-08-11 실제 사고).** Claude 세션 둘이 같은 폴더에서 일하다 옆 세션 작업 7파일이 남의 PR로 갔고, 저녁엔 역방향으로 옆 세션이 내 브랜치에 커밋했다. 경로를 명시해 커밋하고, 두 번째 세션은 시작할 때 `git worktree`로 분리한다. 복구 레시피는 `docs/agent-parallel-rules.md`(휴면 아님 — 08-11 정정).
- **헤드리스 브라우저로 판정 못 하는 게 있다(2026-08-11).** Playwright WebKit은 backdrop-filter를 아예 못 그리고(`CSS.supports`는 true), 헤드리스 Chromium의 rAF는 vsync에 고정돼 페인트 비용이 안 잡힌다. 기능 판정 전에 **대조군**으로 도구 한계부터 확인한다.
- **컴포넌트 root가 `relative`면 className의 `absolute`는 안 먹는다(2026-08-11).** Tailwind 출력 순서상 `.relative`가 이긴다. 오버레이는 래퍼 div로 띄운다 — `Pages/Chat` Mobile의 BottomBar가 이 이유로 실제로는 안 떠 있었다.
- **인스턴스 variant를 바꾸면 슬롯 콘텐츠가 초기화된다(2026-08-11).** 라이트박스 `count` 전환에 `이미지` 슬롯 사진이 마스터 자리표시로 되돌아갔다. 슬롯 작업은 전환 → 재조회 → 내용 재확인 순으로.
- **렌더 판정은 스크린샷 육안이 아니라 픽셀로 한다(2026-08-11).** 밝아 보인다고 "스크림 미적용"으로 보고했다가 픽셀 (97,98,99) = 흰 배경 + `black-alpha/60`으로 정상임을 확인해 정정했다.
- **카운트 비교 전에 가시성 필터부터.** `visible:false` 잔재와 타 제품 보드가 섞이면 수치가 오염된다.
- **`upload_assets`의 `nodeId` 배치는 무동작이다(08-09 발견, 08-12에 15/15 재현).** 응답은 `success: true` + imageHash를 정상으로 주는데 노드에는 아무것도 안 붙는다. 업로드는 **imageHash 확보용**으로만 쓰고 배치는 항상 `use_figma`로 `fills=[{type:"IMAGE", scaleMode:"FILL", imageHash}]` 수동 바인딩한다.
- **매핑 문서의 노드 ID를 검증 없이 쓰지 않는다(2026-08-12).** `guideline-example-map.md`의 L06 슬롯 3개가 실제로는 다이어그램 노드를 가리키고 있어 도식 배경을 스크린샷으로 덮었다. Guidelines 예시 슬롯은 **이름이 반드시 `ex-*`**다. 더 일반적으로 — **파괴적 쓰기 전에 원본 값을 읽어두는 건 검증이 아니라 백업이다.** 안 읽고 덮으면 대칭 노드 추정으로만 복구된다(`4266:11` 색이 그래서 재구성값으로 남았다).
- **`resize()`는 세로 사이징을 FIXED로 되돌리고, 같은 스크립트에서 읽은 크기는 재레이아웃 전 값이다(2026-08-11).** 폭만 바꾸려 `resize(w, h)`를 부르면 HUG였던 높이가 굳고, 리플로우 직후 `node.height`로 좌표를 계산하면 옛 값이 나온다(211 vs 실제 167 → 44px 어긋남). 리플로우 뒤 좌표는 **다음 호출에서 `get_metadata`로 확정 높이**를 받아 잡는다.
- **규칙 보드의 값을 고쳤으면 그 값이 렌더되는지까지 본다(2026-08-11).** DS `LAYOUT 05` 대체 규칙 표는 셀 32개가 높이 10px + `clipsContent`라 텍스트가 한 줄도 안 보이는 상태로 방치돼 있었다(패딩 합 28 < 높이 10이 신호). 표 데이터를 고쳐도 아무도 못 읽는다.
- **내부가 HUG면 프레임 폭을 줄여도 안 접히고 넘친다(2026-08-11).** 토스트를 536→344로 줄였더니 노드는 344인데 렌더는 570이었다(536은 override가 아니라 hug 결과였다). 폭 감사는 `node.width`만 보면 통과한다 — **`absoluteRenderBounds.width`와 대조**해야 오버플로가 잡힌다(그림자 bleed 약 40px은 정상). 접히게 하려면 `FILL` + TEXT `autoResize=HEIGHT`.
- **`SECTION` 안에 화면이 있으면 "최상위 프레임" 탐색이 섹션에서 멈춘다(2026-08-11).** 토스트 전수에서 flow 섹션 안의 393 화면 4건을 "보드"로 분류해 빼는 바람에 "모바일은 전부 상단"이라는 **결론이 통째로 뒤집혔다**. 화면에 절대 배치되는 오버레이는 최상위가 아니라 **`parent`가 화면인지**로 판정한다. 같은 맥락으로 동명 프레임(`deep` 13장·`Chat` 3장)이 흔해 **이름으로 화면을 세면 "중복"으로 오판**한다.
- **격리 브랜치로 옮길 땐 `git diff`가 아니라 `git diff main`으로 검증한다(2026-08-11, CI가 잡음).** 공유 워킹트리에서 `index.ts`가 "내 변경만"인 걸 `git diff`로 확인하고 통째로 복사했는데, 옆 세션이 **이미 커밋한** `EdgeBand` export가 딸려와 CI가 `Cannot find module './components/EdgeBand.tsx'`로 깨졌다. 커밋된 남의 변경은 작업 브랜치 diff에 안 잡힌다 — **기준은 옮겨갈 base(main)다.** 같은 이유로 **로컬 통과가 CI 통과가 아니다**: 로컬은 옆 세션 파일이 워킹트리에 있어서 통과했다. 격리 worktree에서 같은 명령을 돌려 확인한다(`pnpm`은 심링크 node_modules를 거부하므로 `node_modules/.bin/*` 직접 호출).
- **`git worktree add`를 HEAD에서 따면 남의 브랜치 위에 얹힌다(2026-08-11).** 격리했는데도 base가 옆 세션 PR 브랜치라 내 브랜치에 남의 커밋 9개가 딸려 있었다. **worktree는 `main`에서 따고**, `git rev-list --count main..HEAD`로 내 커밋 수만 남았는지 확인한다. 생성물(토큰 `theme.css`·manifest)은 patch로 옮기지 말고 격리 쪽에서 **재생성**해야 남의 변경이 안 섞인다.
- **피그마 모바일 시안의 y값에는 iOS StatusBar(59)가 들어 있다(2026-08-11).** 이 제품은 모바일 **웹**이 본선이라 실제 렌더엔 StatusBar가 없다 — 시안 절대 y를 코드 계약에 옮기면 틀린다. 앵커 기준(예: "헤더 하단 +8")으로 쓴다.
- **슬롯에 들어가는 컴포넌트는 높이를 FIXED로 두지 않는다(2026-08-12).** 슬롯이 옛 높이를 붙들어 마스터를 바꿔도 안 따라오고, 중첩 인스턴스라 제품 파일에서 손댈 수도 없다(자식 지오메트리 override 불가). HUG로 두고 재게시해야 풀린다.
- **마스터 안에 배치된 인스턴스의 padding override가 마스터 수정을 이긴다(2026-08-12).** 행 마스터를 거터 16으로 고쳐도 `Bottom Sheet` 세트 안 행 5곳이 20을 override로 들고 있어 제품에 안 내려갔다.
- **아이콘 박스와 잉크는 다르다(2026-08-12).** lucide 글리프가 뷰박스 안에 자체 여백을 갖는다(x 6 · chevron 8). 면을 걷어내면 정렬 기준이 박스에서 잉크로 바뀌므로 패딩은 계산이 아니라 실측으로 잡는다.
- **계약 파일은 스토리가 없으면 만들 수 없다(2026-08-11).** `validate-design-system.sh`가 `packages/prism/component-contracts/*.md` ⇄ `storybook-map.md` ⇄ 실제 story 파일 3자를 강제한다. 피그마에만 있고 코드 포팅 전인 컴포넌트의 기록 자리는 `design-system/components/*.md`이고, 거기엔 `이 문서는 피그마 근거 기록입니다.` 배너 + `- 노드: 123:456` + `## Variant` + `## 상태`가 필수다.
- **raw 색을 토큰에 묶는 것 자체가 다크 프레임에선 시각 변경이다(2026-08-11).** raw 검정 40% 딤을 `overlay/scrim`에 바인딩하니 모드가 명시된 다크 프레임만 60%가 됐다(토큰이 제대로 동작한 결과). "바인딩은 픽셀 무변화"라고 미리 단정하지 말고 모드 걸린 프레임을 따로 센다.
- **제품 파일 `strings`는 로컬 컬렉션인데 열거 API가 빈 배열을 준다(2026-08-11).** `getLocalVariableCollectionsAsync()`=[]·`getLocalVariablesAsync('STRING')`=0·`variableIds`=빈 배열인데 실제로는 변수가 존재하고 바인딩돼 있다. 이 때문에 "원격 DS라 게시 선행"으로 한 차례 오판했다. 판별은 바인딩 역추적(`boundVariables.characters.id`) 또는 `createVariable` 1개 프로브(만들고 확인 후 `remove`). 리프 변수(`login/email`)가 있으면 같은 이름 그룹(`login/email/*`)은 못 만든다 → 네임스페이스 분리(`login/mail/*`).
- **새 variant 축은 override 있는 인스턴스에 안 내려간다(2026-08-11).** 세트에 축을 추가하면(`History` `layout=wide|rail`) **어느 축이든 variant override를 가진 인스턴스는 새 축이 기본값으로 박힌다** — 마스터가 rail을 써도 태블릿 13개는 wide로 남았다. "기본값이라 시각 변화 0"은 마스터 쪽 얘기일 뿐이니, 게시 후 대상 인스턴스에 `setProperties`로 명시하고 렌더를 실측한다.
- **프레임 배경이 하드코딩 흰색이면 다크가 반만 먹는다(2026-08-11).** 텍스트·필드는 토큰이라 뒤집히는데 배경만 흰색으로 남아 "다크가 깨졌다"로 보인다. 다크 판을 뜨기 전에 프레임 fill이 `semantic/surface/base`에 바인딩됐는지 먼저 본다.
- **`upload_assets`의 `success`는 "커밋됨"이지 "적용됨"이 아니다(2026-08-11).** `nodeId`를 줬는데도 대상 fill이 안 바뀌었다 — 반환된 `imageHash`로 `fills`를 직접 갈아끼워야 했다. 업로드 후 노드 fill을 다시 읽어 확인한다.

---

- **삭제가 기본 동작인 마이그레이션 스크립트를 쓰지 않는다(08-12 사고).** 피그마 행 교체에서 "지정한 것만 옮기고 옛 노드는 삭제"로 짰다가, 구조가 한 겹 다른 카드에서 코멘트 블록 10개를 지웠다. 못 옮긴 자식이 하나라도 남으면 **건너뛰고 보고**해야 한다. 파일럿은 구조가 다른 케이스를 포함해야 하고, 배치의 부분 실패는 패치 대상이 아니라 가정을 재검토하라는 신호다. 공유 파일 부분 유실은 **복원보다 복제**가 먼저다(복원하면 그날 작업이 통째로 날아간다).
- **슬롯 조작 3제약(08-12 실측).** ① 슬롯에 `appendChild`한 뒤에는 같은 스크립트에서 그 슬롯 자식을 **읽지도 못한다**(핸들 무효) — 이관은 맨 끝에 몰고 후속 편집은 다음 호출로. ② **절대배치 자식은 슬롯이 받지 않고 조용히 남는다** — 원본을 지우면 함께 사라진다. ③ 면을 인스턴스로 바꾸면 내용이 인스턴스 자식이 되므로 `skipInvisibleInstanceChildren = true`면 `findAll`이 안쪽을 못 본다.
- **글래스 카드는 배경을 맞춰야 픽셀 대조가 성립한다.** 면이 70% 반투명이라 뒤 배경이 30% 비친다. 사본을 피그마 페이지 위에 놓고 재면 94% 불일치로 나오고, 실제 화면과 같은 캔버스색 위에 올리면 96.4% 동일로 뒤집힌다.
- **부분 스테이징은 역적용으로 한다 — `-U0` 정방향 선별은 줄번호가 밀린다(08-12).** 공유 워킹트리에서 내 hunk만 커밋할 때, 앞 hunk를 빼면 뒤 hunk의 `+` 줄번호가 그만큼 어긋나고 `-U0`은 컨텍스트가 없어 git이 위치를 되찾지도 못한다. **전부 `git add` → 피어 hunk만 `git apply --cached --reverse --unidiff-zero`**가 안전하다(인덱스가 새 파일과 정확히 일치하므로 줄번호가 항상 맞는다).
- **pre-commit 훅은 워킹트리를 본다 — 커밋될 트리는 따로 검증한다(08-12).** 부분 스테이징한 커밋이 단독으로 성립하는지 보려면 `git write-tree` → `git commit-tree` → `git worktree add --detach <커밋>`으로 인덱스 트리만 떼어 게이트를 돌린다. HEAD도 워킹트리도 안 건드린다. 이걸로 **HEAD 자체가 이미 깨져 있던 것**을 발견했다(`prompt-pill.md` 계약은 커밋됐는데 맵 행이 미커밋).
- **worktree 점유는 "누가 작업 중"과 다르다(08-12).** `git worktree add`가 `already used by worktree at ...`로 막혀도 그 세션이 살아 있다는 뜻은 아니다. **uncommitted 0 · origin과 0/0 · 최근 파일 mtime 없음**이면 잠든 worktree이고 그대로 써도 된다. 점유만 보고 기다리면 헛대기가 된다.
- **`appendChild`의 폰트 요구는 MCP 서버를 바꿔도 안 사라진다(08-12, 위 폰트 캐비엇 보충).** 텍스트를 품은 서브트리를 옮기면 `unloaded font`가 뜨는데, 이건 서버가 로컬 폰트를 못 보는 건과 **다른 문제**다(정상 Plugin API 계약). `findAllWithCriteria(['TEXT'])` → `getStyledTextSegments(['fontName'])`로 실제 폰트를 전수 수집해 로드한다. **로드는 호출마다 다시 해야 한다** — `use_figma` 컨텍스트가 매번 초기화라 "로드만 하는 스크립트"를 따로 돌리면 다음 호출에서 또 막힌다.

- ⚠ **마스터를 고쳐도 detach 프레임은 안 따라온다(08-25).** 홈 히어로 정정 다섯 값이 인스턴스 18곳에는 내려갔는데 detach 12곳은 옛 규격 그대로였고, 주미님이 세 장을 짚어주셔서 알았다. **전파를 인스턴스 수로 세면 detach가 통째로 빠진다.** 감사는 컴포넌트 이름이 아니라 **화면 카피**로 훑어야 detach까지 걸리고, 두 기준(구조 이름 · 카피)이 같은 수를 낼 때 커버리지를 믿는다.

## 다음 후보 작업

### [인계] 2026-08-13 판단 트랙 종료 — 다음 에이전트가 이어받을 것

**⚠ 최우선 — 주미님 손이 필요하다 (2026-08-13 오후 기준)**

**Chromatic 베이스라인 승인 4건이 단일 병목이다.** PR **#147 · #157 · #158 · #159** 전부 `UI Tests`만 대기라 `UNSTABLE`, 머지 불가. `check`·`validate`·`storybook`·`UI Review`는 전부 pass다. 승인이 안 나면 오늘 작업 8커밋이 전부 브랜치에 묶인 채로 남는다.

**#157 → #158 → #159는 직렬 스택**이다(같은 파일을 여러 건이 건드려 충돌을 피하려고 쌓았다). 앞이 머지되면 base가 차례로 내려간다.

### 🔚 마감 절차 — **그날 마지막으로 끝내는 세션이 한다** (2026-08-13 주미님 지시)

중간 세션이 머지·정리를 하면 뒤에 붙는 세션이 또 브랜치를 쌓거나 정리한 걸 되돌린다. **자기 작업이 끝났다고 바로 머지하지 말고, 자기가 마지막인지부터 확인한다.** 아래는 순서 그대로 실행한다.

> **08-19 전면 갱신 — 이전 판은 08-13 기준이라 1~3번이 이미 끝난 일이었다.** 8/18 밤에 #147·#157·#158이 머지되고 #159는
> #158을 `--delete-branch`로 머지하는 순간 base가 사라져 자동 close된 뒤 **#165로 대체 머지**됐다. #155도 머지됐다.
> 아래는 08-19 실측 기준이다. **이 절차를 근거로 쓰기 전에 PR 상태를 한 번 다시 잰다** — 오늘 사고의 원인이 낡은 컨텍스트였다.

1. **Chromatic 빌드 695·698 판정** — 사람만 할 수 있는 유일한 블로커다. #164 머지 후 main `workflow_dispatch` 두 번이 **실제로 스냅샷 321장을 찍었다**(`FULL_SNAPSHOTS: true`·`forceRebuild: true`). 워크플로가 `exitOnceUploaded`라 CI는 결과를 안 기다리니 사람이 안 보면 영영 안 닫힌다. https://www.chromatic.com/build?appId=6a1e5942ec7ecc06429b1040&number=698
2. **열린 PR 처분 — 순서가 아니라 상태로 고른다.** #161 `MERGEABLE/CLEAN`(바로 가능) · #168 `MERGEABLE/UNSTABLE`(`UI Review`만 PENDING인데 main에 branch protection이 없어 **머지 블로커가 아니다**) · **#160·#163은 `CONFLICTING/DIRTY`**라 rebase가 선행이다. 08-18 로그의 「#161 → #160 → #163」 순서는 그 시점 기준이라 낡았다.
3. **미푸시분 push가 브랜치 처분보다 먼저다** — `feat/edge-scrim-exploration`이 원격 같은 이름 브랜치 대비 **16커밋 미푸시**이고, 그중 `docs/plans/30-*.md`는 `origin/main`·원격 브랜치 **양쪽 다 0건**이라 로컬 디스크에만 있다. 지금 브랜치를 지우면 복구처가 없다. ⚠️ 08-18 로그가 구출 대상으로 적은 `0e8aee6`은 **amend로 밀려난 유령 해시**다(실제는 `a95d6ff`) — 그 해시로 cherry-pick하면 실패하거나 옛 판을 집는다.
4. **`feat/edge-scrim-exploration` 브랜치 삭제** — **살려두면 또 쌓인다**(8/11에 #143이 squash 머지된 뒤 안 닫아서 87커밋·충돌 20건·결함 3건이 생겼다). ⚠️ 공유 워킹트리가 이 브랜치를 체크아웃 중이고 **미커밋 36건**이 올라가 있다(08-19 실측, 8/13의 52건에서 감소). 피어 세션 커밋 여부 확인 → 워킹트리를 다른 브랜치로 이동 → 삭제.
5. **worktree 정리 — 숫자가 낡았다. 12·14개가 아니라 6개다**(08-19 실측, prunable 표시 0건). `worktree-model-profile-ring-badge`만 머지돼 바로 제거 가능, `/tmp/prism-glow`는 #168 머지 후, 2026-08-05 워크플로 worktree 3개는 **전부 미머지라 처분 판단이 필요**하다(그냥 지우면 그 브랜치 작업이 사라진다).

**3번을 건너뛰고 4·5번을 하지 않는다.** push 없이 브랜치를 지우면 되돌릴 수 없다.

**P0 (남은 것)**
1. **`Thinking/Toggle` 패키지 포팅 — ⛔ 여전히 보류다.** plan-27에 8/12 주미님 지시로 착수 금지가 걸려 있다("포팅은 아직, 내가 디자인 좀 더 다듬게"). **이 목록의 이전 판이 P0로 올려놨지만 그 블로커를 못 본 것이었다** — 다듬기 완료 신호가 와야 풀린다. 착수 시 인계 사항은 plan-27 §「⛔ 패키지 포팅 보류」에 4줄로 적혀 있다.
2. ~~브랜치 커밋 origin/main 미도달~~ → **PR #157로 해소(8/13)**. 87커밋을 합치면서 브랜치 결함 3건도 같이 고쳤다(아래 캐비엇).
3. ~~`copied` 패키지 포팅~~ → **PR #158로 완료(8/13)**.

**P1**
4·5. ~~웹 상태 프레임 3장 + hover 툴팁~~ → **PR #159로 완료(8/13)**. **"웹 260"의 260은 프레임 폭이 아니었다** — 이 파일의 웹 상태는 전부 1440×1024 안에 260 패널로 그린다. 260 단독은 선례가 없다.
6. ~~`Toast` 닫기 32 → 44~~ → **PR #159로 완료(8/13) · 픽셀 변화 0**. "9건"은 인스턴스 수였고 크기 override가 0건이라 마스터 1건이었다.
7. **`AttachmentChip` 처리 중을 결정형 진행으로 — 남은 유일한 P1.** 칩 마스터에 진행 표현이 아예 없어 **피그마 신규 제작이 선행**이다. 기존 값 재조합이 아니라 없는 걸 만드는 작업이라 앞의 것들과 무게가 다르다.

**P2 (착수 전 주미님 판단)** — 시트 껍데기 통합 · 글로우 처분 · `Skeleton` 계약 신설 · 스펙 밖 요소 존치(다크 토글·OSS·앱 정보) · 와프엔 있고 우리엔 없는 4건

**P3 정리** — `Chart/ProgressBar` 제약 해제 · 접힌 헤더 말줄임 토큰화 · `ToastViewport` 3종 · LAYOUT 03 예시 · `Stream 800` 복구색 · Side Panel 400 데모
**P4 구조 전환(별도 트랙)** — 홈 코드 옛 모델 · 채팅 134장 헤더 · `Bottom Bar` Dark축→모드 · 브레이크포인트 경계

### ⚠ 다시 열지 말 것 (2026-08-13 실측으로 닫힘)

이월 목록의 메모가 **틀린 경우가 반복됐다.** 아래는 실측으로 종결했으니 재개하지 않는다.

- **구분선 28/40** — 드리프트 아님. 중첩 깊이 체계(40 최상위 > 28 면 안 > 20/12 카드 안)
- **데스크톱 패널 닫기 32** — 실물에 없음. `Sheet/Header`는 두 variant 다 44×44
- **소집·도착 숫자** — 대부분 의도된 시나리오 더미. 프레임 이름이 의도를 적어 둔다(`(3명 소집 · 3명 도착)`)
- **모달 어조** — 파괴성 기준으로 이미 규칙적. 고친 건 탈퇴 모달 **버튼 문법**이었다
- **사고과정 문구 16** — JP 실측이 축소 근거를 반증(폭 +12~52% · 15는 줄 수 증가)
- **토스트·시트 핸들 겹침** — 대안 3종 만들어 비교 후 현행 유지. 다시 열려면 조작이 막힌 실사례가 있어야 한다
- **`Attachment/Lightbox` 11px** — 오진. 참고 보드의 **축소 프리뷰**였고 마스터는 36×36 일정
- **`Skeleton` 소비처 0** — 틀림. `ChatHistoryPanel.tsx:366`이 쓰고 공개 export 중이다
- **`--layout-overlay-popover` 미사용** — 틀림. `Popover.tsx`가 기본 폭으로 쓴다(값 변경은 브레이킹)
- **게이트 서브카피** — 폐지→존치로 두 번 뒤집힘. **최종은 존치**이고 이메일 단계 모달 39곳은 숨김이 정상

- **웹 실패 프레임 `RetryButton` 숨김** — "화면이 깨졌다"가 아니다. 버튼은 `Empty State Type=error`가 자체로 갖고 있고 숨겨진 건 폐기 잔재다(8/13 오후 스크린샷 확인)
- **토스트 닫기 "터치 타깃 미달 9건"** — 9번 고칠 일이 아니었다. 크기 override가 0건이라 마스터 1건으로 끝났다

**공통 교훈: 이월 메모의 전제부터 실측한다.** 8/13 판단 9건 중 절반 이상이 "고칠 게 아니었다". **오후 트랙에서도 같은 일이 두 번 더 반복됐다** — 실패 프레임 오진, "9건" 오독.

### ⚠ 8/13 오후에 드러난 브랜치 결함 3건 (PR #157에서 수정)

`feat/edge-scrim-exploration`이 8/12부터 **CI가 통째로 깨진 상태**였는데 아무도 몰랐다.

- **`component.tokens.json`이 유효하지 않은 JSON**(`b694db8`) — 토큰을 엉뚱한 객체 **안쪽에** 끼워 넣었다. `theme.css`에 정상 생성분이 남아 있어 렌더는 멀쩡했다. **생성물이 소스보다 오래 산다.**
- **`index.ts`가 트리에 없는 컴포넌트 3종을 export**(`07597e5`) — 공유 워킹트리에서 옆 세션 편집만 딸려 커밋됐다. `docs/agent-parallel-rules.md`가 경고한 그 사고의 재발.
- **`PromptPill.tsx` `text-[15px]`가 lint 위반** — 개별 게이트만 돌리고 `pnpm check`를 안 돌려서 PR 올린 뒤 CI에서 잡혔다.

**교훈: 개별 게이트 통과 ≠ `pnpm check` 통과.** 한 명령으로 도는 게이트가 있으면 그걸 돌린다.

### 작업 규율

- **워킹트리에 피어 세션 미커밋분 20여 건이 상시 존재한다.** 경로 지정 커밋(`git commit <path>`)이 기본값이고, 넓은 `git add`는 남의 작업을 쓸어 담는다. 오늘도 그 자리에서 `toast.md`·`report-deep.md`·`edge-band.md`·`ChatHistoryPanel.tsx` 편집을 포기하거나 워킹트리에 남겼다.
- **DS 변경은 기본값 = 현행 렌더로 설계한다.** 8/13 신설 7종이 전부 픽셀 변화 0이라 게시가 안전했다. 오후의 토스트 44도 같은 방식이다 — 상자가 아니라 히트영역만 넓혀 픽셀 0.
- **공유 워킹트리를 건드리지 말고 `git worktree`로 격리한다.** 8/13 오후 87커밋 머지·포팅 2건을 전부 `/tmp` 격리 worktree에서 했고, 공유 트리의 피어 미커밋 42건은 손대지 않았다. 다만 worktree가 12개까지 늘었으니 PR 머지 후 일괄 정리가 필요하다.
- **장수 브랜치는 squash 머지 직후 닫는다.** #143이 squash로 머지된 뒤 `feat/edge-scrim-exploration`이 살아남아 87커밋을 더 쌓았고, 충돌 20건과 위 결함 3건을 만들었다. AGENTS.md의 main-first가 이걸 막으려는 규칙이다.


아래는 후보/백로그다. 실제 실행 전 범위와 승인 상태를 다시 확인한다. 상세 to-do는 public viewer의 계획 탭을 정본으로 본다.

- **[내일] DS `Bottom Bar`의 `Dark` variant 축 → 피그마 모드 전환.** 피그마는 다크를 variant로 고르고 코드는 모드로 뒤집는 불일치. 영향 실측 = 인스턴스 4개뿐(`chat/chart` 103개 전부 `Dark=false`, `home/history` 4개만 `true`). 마스터를 `semantic/surface/fade-*`로 재바인딩 → 축 제거 → 홈 다크 컷 3장 모드 명시. 죽은 `Toolbar` 축(옵션 `off` 하나)도 같이. 되돌리기가 번거로워 컷이 적은 지금이 싸다.
- **[대기] `SheetShell` 코드 정합 + `layout.md` Side Panel 절 정정** — 패널 헤더가 코드에선 56에 타이틀이 없고(피그마 60 · leading+Title+CloseBtn), `rounded-t-3xl`이 panel에도 걸려 있다. `layout.md`는 패널을 아직 "떠 있는 크롬"·zone 536으로 적고 있다. 셋 다 `feat/edge-scrim-exploration`이 편집 중인 파일이라 **그 브랜치 머지 후** 한 묶음으로.
- **[인계] 홈 트랙 잔여 4건(08-12 밤 종료 시점)** — 홈 자체는 모바일·웹 다 닫혔고, 남은 건 아래뿐이다. 정본 기록은 plan-23(웹 홈)·plan-18(글로우)·plan-10(코치마크)이고 커밋은 `050babe`·`1b1ff20`(둘 다 `feat/edge-scrim-exploration`, **origin/main 미도달**).
  - ① **홈 스토리 3건 시각 비교 미실시** — `pnpm visual`이 Theme 외 스냅샷을 꺼서 625·626·627이 전부 무의미했다(위 캐비엇). 머지 전 워크플로 전체 비교에서 확인.
  - ② **포커스 글로우 범위 육안 판단** — 승격으로 채팅 컴포저도 포커스 글로우를 갖는다. 과하면 `glow={false}`. 주미님 실물 확인 대기.
  - ③ **halo opacity `0.13` 토큰화** — `chat-input-bar.glow-halo-opacity`. 토큰 파일을 옆 세션이 잡고 있어(미커밋) `token:build`가 그쪽 작업분을 끌고 오므로 풀린 뒤에.
  - ④ **기획 요청 2건** — 칩 9종 변환 문장(주미님 "당장 안 급함") / 딥링크 파라미터↔홈 요소 매핑(§4.1 "개발 착수 전 확정"). **딥링크 헤드라인 2줄 컷은 ④에 딸린 것**이라 별건으로 세지 않는다.
- **[룰 39 코드 후속]** ~~① `ChatHistoryPanel` `status` 축~~(08-13 완료 `9d6cd8c` — `ready|loading|error`, `Skeleton` 첫 실사용) · ~~② `Button` loading~~(08-13 완료 `599d071` — **스피너가 아니라 피그마 3점 `Loading dots`였다.** 룰 39가 이 자리를 '스피너로 내린다'고 적은 것을 정정했다). 남은 것 = ③ `AttachmentChip` 처리 중을 결정형 진행으로(업로드는 바이트를 안다 · 칩 마스터에 진행 표현이 없어 **피그마 선행**). 피그마 잔여 = **웹 260 상태 프레임 3장**(로딩·실패·비로그인)과 `생성 중 행 hover 툴팁` 프레임을 웹으로 이동(PC 전용인데 393 모바일에 그려져 있다). 드리프트 2건 = 코드 빈 상태의 180×131 플레이스홀더(피그마는 `media` 불리언 기본 false라 **코드가 한 세대 뒤**) · 푸터 위 fade가 `isEmpty` 기준이라 `loading`에서 안 뜸(옆 세션 미커밋 영역).
- **[머지 확인] plan-22 문서 커밋 10개 브랜치 4곳 산재(08-09)** — 공유 체크아웃 브랜치 이동 탓. main에는 `e65ae61`만 반영, 나머지는 각 feat/docs 브랜치 PR 머지로 합류. 첨부 에러 정책 충돌 시 `9311cbf`(기획서 정합)가 정답.
- **DS 게시 잔여** — `RetryButton` 세트만 두 번 연속 게시에서 빠져 CHANGED(위생분·비블로킹) — 다음 게시 때 다이얼로그 체크 확인. FileCard stroke 3건도 다음 재게시 포함. (`History Item` `State=loading`·`OTP Input`은 08-11 게시·제품 반영 완료.)
- **socraAI 문서 3건 미커밋(08-11 심야)** — `docs/plans/26-login-signup-screen-spec-sync-2026-08.md`(신설) · `design-system/components/user-profile.md` · `chat-history-panel.md`. 현재 브랜치 `feat/edge-scrim-exploration`의 origin이 `[gone]`이라 커밋처를 정리한 뒤 반영한다.
- **홈 스토리 재정합 2차** — placeholder·액센트 정본 확정(위 결정) 후 `HomePage`·`WebShellFlow`를 새 `web/home` 기준으로 재대조(#117은 8/10 기준).
- **홈/히스토리 후속** — 기획 요청 발신 2건(칩 9종 변환 문장·유도 질문 데이터셋 / 딥링크 파라미터↔홈 요소 매핑) · Coachmark 컴포넌트화(딤+스포트라이트+툴팁) 판단.
- **첨부 라이트박스 마무리** — 피그마 마스터 4종에 안전영역 반영(flow·마스터 담당 세션 몫) · `be2fdd1` 머지(`feat/attachment-lightbox-safe-area` 푸시됨) · 세로 원본 사진 확보 시 슬롯 paint 교체.
- **로그인·설정 트랙(08-11 밤 기준 · 설정 정본 = plan-27)** — 설정은 §4.11 정합 완료(모바일·웹 3탭 라이트/다크·태블릿 9컷 + 사업자 정보 푸터), **언어 선택 5컷 · 비로그인 4컷 · 인증 플로우 6컷**(비밀번호 변경 2 · 탈퇴 인증 1 · 탈퇴 확인 모달 3) 신설. ⚠️ 8/11 이전 기록은 "탈퇴/언어/비로그인 4시트 완료"로 적혀 있었으나 **파일에는 없었다** — 이번 세션에 실제로 제작했다. **설정 트랙은 8/11 심야에 전건 종결**: ① 신규 문구 6종 `strings` 등재 + 51곳 바인딩(KR/JP/EN 실측 완료 · 푸터는 등기 원문 유지·라벨만 번역) — `strings`는 원격 DS가 아니라 **제품 파일 로컬 컬렉션**이었다 ② `Settings/Row` `Type=select` 신설·게시 2회·제품 5컷 교체·패키지 포팅까지(PR #149) ③ 오픈소스 라이선스 행 존치 확정. 남은 것은 **PR #149 머지**와 👁 비밀번호 보기 토글(`lucide:eye` 키 확보, 아이콘 추가만)뿐이다.
- **첨부 잔무** — 개수 토스트 카피(디자인 초안)·툴팁 2.5s 체감 확인·PR #63 머지 상태 재확인.
- **토스트 배치 트랙(08-11)** — **PR #147 OPEN**, 빌드 게이트 전부 통과(`check`·`validate`·`storybook`). **[주미님 손] Chromatic 베이스라인 1건 승인 필요**(신규 `Placement` 스토리) — 승인 전까지 `mergeState=UNSTABLE`. 규칙·제품 파일 13건 정합·`ToastViewport` 포팅은 완료. 남은 것: ① `ToastViewport`가 안 다루는 스택 규칙·진입/퇴장 모션·`duration` 자동 소멸 ② 패널 열림 화면 `EdgeBand` 부재(스트림 748 + 패널 자체 밴드, 시각 변경이라 승인 대기) ③ Side Panel **400 요약 실물 데모 부재**(옛 `7449:11658` 소멸 — "패널 열리면 폭 무관 rail 접힘" 확인용 재작성 필요).
- **[주미님 손] empty state 일러스트** — 검토 보드 `4347:2073`에 일러스트를 넣어 전체 UI 정합 판단. 채택 시 마스터 반영은 별도 승인.
- **[주미님 확인]** `Message Bubble Content=Image` 2건(DS 추가 vs Text+Image 스왑 vs 유지) · 고아 치환 제외 2건(`투자` 세트·변칙 Bottom Bar)과 하단 바 Deep 칩 노출(plan-20) · History/Footer `PlanLabel` "Free" 처분(비로그인 variant 재작업 때).
- **오프스케일 그림자 29건** — auth-card 8%·modal 16%·dialog 15%·Toast 복합. 램프 확장 vs 동결 결정 대기.
- **prism 미커밋 잔여 2건**(19-mvp plan 수정·AttachmentLightbox.stories.tsx, 다른 세션 산출물로 보임) — 커밋 여부 판단.
- **형제 칩 포팅** — `SourceChip`(2변형, 쉬움) / `Attachment/Preview` 팝오버(피그마만, 코드 없음) / `ModelChip`·`ChipGroup`(ideation 승격 선행).
- **Tier 1 잔여 5종** — `ChatInputBar` · `ModelProfile`(데이터 계약 선행) · `Spinner`(반쪽) · `MessageBubble`·`Button/ActionBar`(Deep 경로 인라인) · `HeaderNavBar` 1곳.
- **문서 정정 4건** — `plan-16` §8 폐기 표시 / `plan-21` 대비표 접전 `cyan`→`orange` / `plan-20` Tier 1 수 / `design-system/tokens/color.md` 낡은 헥스 표.
- **눈으로 볼 것 2건** — `DeepCTA` conic 그라데이션 테두리 브라우저 렌더 / 헤더 치환 후 Deep 화면.
- 기획서 대조 B-1 잔여 — Chat 응답 화면 3종 · 피드백 UI(좋아요/싫어요+사유) · 모드명 JP 보조 라벨 · 시각화 그래프 형태 좁히기.
- **기획 대기(차주) — 착수 금지** — 등급 판정 로직 승격·범례 % 노출 / ReportCard shell 실측 / 브레이크포인트 구간 경계 / 헤드라인 18px(off-scale).
- **외부/인프라 블로킹** — 제품 파일 REST 토큰 만료(403) 재발급(팝오버 전수 감사가 막혀 있음) / Chromatic 월 스냅샷 쿼터.
- 미검증 스킬 5개(`bump-version`·`record-trap`·`save-ideation`·`prep-meeting`·`ux-review-gate`) — 따로 돌리지 말고 실제로 그 일을 할 때 결과를 확인한다.

---

## 피그마 기준 정보

| 항목 | 값 |
| --- | --- |
| Socra Design System file key | `DcYgJjGAfObOIM4IyrQjgj` |
| Components page | `0:1` |
| Foundation page | `70:218` |
| Icons page | `74:10109` |
| Page design test | `76:10172` |
| Image reference | `76:10169` |

---

## 공통 스킬

| 스킬 | 트리거 |
| --- | --- |
| `write-worklog` | `워크로그 써줘`, `오늘 정리해줘`, `/write-worklog` |
| `session-snapshot` | `지금까지 뭐했어?`, `중간 정리`, `/session-snapshot` |
| `sync-entry` | `동기화 확인해줘`, `뷰어랑 맞아?`, `/sync-entry` |
| `handoff-check` | `handoff 확인해줘`, `클로드 코드에서 이어받을 수 있어?`, `/handoff-check` |
| `prep-meeting` | `미팅 준비해줘`, `이번주 요약해줘`, `/prep-meeting` |
| `bump-version` | `버전 올려줘`, `배포할게`, `/bump-version` |
| `record-trap` | `이거 기억해줘`, `규칙 추가해줘`, `/record-trap` |

---

## 계정

| 서비스 | 계정 |
| --- | --- |
| Claude Code | candoit.j@gmail.com |
| Figma | jumi.jeong@socra.ai |
| GitHub | JumiJeong-design |
