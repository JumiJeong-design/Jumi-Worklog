# 2026-05-30

## 미해결 항목

- [ ] Figma 디자인 전체 리디자인 — 아이콘 및 컴포넌트 주미님이 직접 수정 예정, 이후 코드 sync 재진행
- [ ] 코드 기반 컴포넌트 라이브러리 기술 스택 확정 (React/Next.js, CSS 방식 결정 후 Code Connect 진행)
- [ ] FIGMA_TOKEN 환경변수 등록 → REST API 전환 (네트워크 정책 변경 적용은 새 세션 필요)
- [ ] 브랜드 컬러 확정 (리디자인 전제조건)
- [ ] Latin + JP 폰트 확정 (Noto Sans JP 재검토 필요)
- [ ] Foundation에 Error states / Motion / Breakpoints 추가 (주미님)

---

## 함정 모음

- **Figma MCP Plugin API lazy loading** — `get_metadata`가 5페이지 중 현재 Figma 데스크탑 앱에 메모리 로드된 2개(Components + Icons)만 반환. Plugin API 특성상 메모리에 없는 페이지는 접근 불가. 해결책: REST API(`api.figma.com/v1/files/:key`) 사용 — 서버에서 파일 전체를 직접 읽어 페이지 수 제한 없음.

- **CCR 네트워크 정책 설정 위치** — Claude Code 앱 설정(코드 표시/테마)이나 사용자 지정(스킬/커넥터) 페이지가 아님. 올바른 경로: 세션 목록 화면 → 세션 hover → cloud icon → Environment gear 아이콘 → Network 탭 → Custom → 도메인 추가. 이 경로 외에 설정 화면 없음. 변경 사항은 새 세션에 적용.

- **ops-plan 전술 vs 전략 혼동** — 첫 초안이 도구 자동화(SessionStart hook, 스킬 라이브러리) 수준에 머물렀음. 글로벌 서비스 계획에는 검증 루프, Japan-First 설계 원칙, 품질 게이트, 팀 스케일 전환 시나리오가 반드시 포함되어야 함. "어떻게 만들 것인가"만 있고 "무엇을 만드는 게 맞는가"가 없으면 반쪽짜리.

---

## 회고 및 인사이트

### AI 도구 활용

- **컨텍스트 자동화는 협업 퀄리티를 바꾼다** — CONTEXT.md가 SessionStart에 자동 로드되면, 새 세션도 이전 세션의 결정 맥락을 즉시 파악. "왜 이렇게 결정했나"가 기록되지 않으면 매번 재설명 비용이 발생함.
- **MCP 도구의 한계를 알아야 우회책이 보인다** — "왜 2개만 보여?" → Plugin API 특성 → REST API 필요 → FIGMA_TOKEN → 네트워크 정책 순서로 추적해야 해결됨. 도구 한계를 모르면 "Figma 구조가 잘못된 것"으로 오인하게 됨.

### 설계 결정

- **검증 루프 없는 빌드 트랙은 반쪽** — ops-plan v2의 핵심 변화: "무엇을 어떻게 만들 것인가"에서 "무엇이 맞는지 빠르게 확인하면서 만들 것인가"로. PMF는 빌드만으로 찾을 수 없음. Build track + Learn track이 동시에 돌아야 함.
- **Japan-First는 로컬라이제이션이 아니라 설계 기준** — 일본을 나중에 조정하는 대상이 아니라 초기 설계의 기준으로 삼아야 현지화 비용이 최소화됨. Noto Sans JP(KR 아님), 정보 밀도, 신뢰 신호, 에러 스테이트 등 초기부터 반영.
- **토큰 네이밍은 디자인-코드 간 계약** — `color-gray-700`은 브랜드 변경 시 이름이 거짓말이 됨. `color-text-secondary` 같은 semantic 이름만이 유효한 계약으로 작동. 3레이어: Primitive → Semantic → Component.

---

## 2026-05-30

### 1. jumi-worklog 레포 자동화 완성

**Claude Code 작업:**
- `CONTEXT.md` 신규 생성 — 세션 시작 시 자동 로드용 현재 상태 스냅샷 (레포 구조, 미해결 항목, 진행 상황, 다음 작업)
- `AGENTS.md` 업데이트 — AI 도구별 진입점 테이블 추가 (Claude Code / Figma MCP / Notion MCP / Slack MCP)
- `README.md` 내용 보강 — 레포 역할 및 구조 설명
- `skills/write-worklog/SKILL.md` 업데이트 — Step 4.5 CONTEXT.md 자동 갱신 절차 추가

---

### 2. socra-ai-product-design 레포 전체 정비

**Claude Code 작업:**
- `AGENTS.md` 업데이트 — 세션 시작 절차 + AI 도구별 진입점 추가
- 중복 write-worklog 스킬 삭제 (정본은 jumi-worklog/skills/)
- `docs/agent-rules.md` 업데이트 — Figma 파일 URL 등록, Code Connect 파이프라인 가이드 추가
- `design-system/components.md` 전면 싱크 — Figma 실제 상태 기준 35개 컴포넌트 현황 업데이트
- Model Profile 아이콘 완료 상태 반영 (5모델 × 3사이즈, 이미지 fill 방식)

---

### 3. Figma 5페이지 구조 확인 + MCP lazy loading 원인 조사

```
Figma Foundation 페이지는 이미 있는데? 다시봐봐
```
```
이상하네 레퍼런스 이미지들도 있는데? 페이지 구분되어있어 총 페이지 5개야
```
```
MCP의 get_metadata 도구가 5개 페이지 중 2개(Components + Icons)만 반환하는 버그는 왜 발생하는건데? 왜 못읽는건데? 이유를 알려줘야 내가 피그마를 개선하든 어케하지
```

**Claude Code 작업:**
- Figma 5페이지 구조 전체 확인: Components / Foundation / Icons / Pages / Image reference
- Plugin API lazy loading 원인 분석: 데스크탑 앱 메모리에 로드된 페이지만 반환하는 구조적 한계
- REST API(`api.figma.com/v1/files/:key`)가 페이지 수 제한 없이 전체 반환함을 확인
- `docs/agent-rules.md` 업데이트 — 5페이지 구조 + MCP lazy loading 주의사항 추가
- `CONTEXT.md` 업데이트 — Figma 5페이지 구조 테이블 반영

---

### 4. Figma REST API 전환 준비 — FIGMA_TOKEN 설정

```
REST API (제한 없음) ← Figma MCP가 이걸 쓰도록 하려면 뭘 해야하는데?
```
```
ㅇㅇ해볼의향있음 알려줘
```

**Claude Code 작업:**
- Figma PAT 수신 및 `~/.figma_token`에 저장 (chmod 600) — **git에 절대 커밋 금지**
- CCR 네트워크 정책에 `api.figma.com` 추가 방법 안내
- 주미님이 직접 code.claude.com → 환경 설정 → Network → Custom → `api.figma.com` 추가 완료
- 네트워크 정책 변경은 새 세션에 적용됨 → 다음 세션에서 REST API 전체 페이지 조회 및 Variables audit 예정

---

### 5. ops-plan.md 전면 재작성 — 글로벌 서비스 아키텍처

```
최선이야? 글로벌 서비스를 만들어야해
```
```
전면 재작성해
```

**Claude Code 작업:**
- v1 초안(도구 자동화 중심) → v2 전면 재작성 (글로벌 서비스 아키텍처 관점)
- 추가된 핵심 섹션:
  - 두 개의 트랙: Build track + Learn track 동시 운영 원칙
  - 일본 시장 설계 원칙 (Japan-First): Noto Sans JP, APPI 컴플라이언스, 신뢰 신호, 에러 스테이트
  - 제품 검증 루프: 가설 → 지표 → 검증 → 반영 사이클
  - 디자인 시스템 글로벌 기준: 토큰 계약 원칙, 3레이어 아키텍처 (Primitive → Semantic → Component)
  - 품질 게이트: Figma lint / ESLint / axe-core / Lighthouse CI / Storybook 시각 회귀
  - 팀 스케일 전환 시나리오: 1인 → 소규모팀 → 글로벌팀

---

### 6. 문서 4종 병렬 업데이트

**Claude Code 작업:**
- `docs/product-context.md` — 현재 검증 중인 가설 테이블(4개 가설, 모두 🔄 검증 전) + 화면 맵 추가
- `design-system/decisions.md` — 서술형 → 구조화 테이블 재포맷 (날짜 / 주제 / 배경 / 결정 / 대안 검토 / 결과·회고)
- `docs/model-registry.md` — 신규 생성: 5개 활성 모델(Claude/GPT/Gemini/Grok/Perplexity) 현황, 모델 교체 체크리스트, 모델 특성 비교표
- `CONTEXT.md` (jumi-worklog) — 검증 가설 테이블, 주요 결정 + 이유 테이블, AI 모델 현황 섹션, Figma 5페이지 구조 테이블 전면 업데이트

---

## 커밋 (jumi-worklog)

| 커밋 | 설명 |
|------|------|
| `c84dc2a` | feat: CONTEXT.md 추가 — 세션 시작 시 자동 로드용 현재 상태 스냅샷 |
| `2c48445` | feat: AGENTS.md에 AI 도구별 진입점 테이블 추가 |
| `fdfabd1` | docs: README.md 내용 보강 — 레포 역할 및 구조 설명 추가 |
| `21d5d1f` | chore: CONTEXT.md 업데이트 — 전체 레포 정비 완료 반영 |
| `452754d` | feat: write-worklog 스킬에 CONTEXT.md 자동 갱신 Step 추가 |
| `47a3705` | chore: CONTEXT.md 업데이트 — 자동화 루프 완성 |
| `f12f71d` | chore: CONTEXT.md 업데이트 — Figma 싱크 완료 |
| `e2005c8` | context: Figma Foundation page confirmed — 5 pages total, MCP limitation noted |
| `9ce7331` | context: update Figma page structure — all 5 pages confirmed |
| `a5f6625` | context: Foundation page includes Markdown design spec |
| `f107050` | docs: add comprehensive AI-native operations architecture plan |
| `11dc466` | docs: rewrite ops-plan — global-service-grade architecture |
| `744fc3a` | context: major update — add validation hypotheses, decision reasons, model status |

---

## 커밋 (socra-ai-product-design)

| 커밋 | 설명 |
|------|------|
| `ed16227` | feat: AGENTS.md에 세션 시작 절차 및 AI 도구별 진입점 추가 |
| `4bb5639` | fix: 유미님 → 주미님 오타 수정 |
| `086e694` | chore: 중복 write-worklog 스킬 삭제 |
| `641d835` | docs: Figma 파일 링크 섹션 + Code Connect 파이프라인 가이드 추가 |
| `c31916d` | docs: components.md Figma 실제 상태로 전면 싱크 (35개 컴포넌트) |
| `5bf97cd` | docs: Model Profile 아이콘 완료 상태로 업데이트 |
| `a144a0d` | docs: Figma 파일 URL 등록 + 아이콘 상태 완료로 업데이트 |
| `9582750` | docs: fix Figma page structure — Foundation page confirmed (5 pages total) |
| `b5d1a4e` | docs: update Figma page structure with all 5 pages confirmed |
| `5d648df` | docs: add Markdown design to Foundation page contents |
| `4b05eb9` | docs: add validation hypothesis table and screen map to product-context |
| `fd6fc1b` | docs: restructure decisions.md with structured table format |
| `64751b3` | docs: create model-registry.md with current model status and change checklist |
