# 2026-06-15 계획

## 기준

- 6/13-6/14 별도 일자 기록은 만들지 않고, 이어받은 맥락은 6/15 계획으로 합친다.
- Storybook 확장 작업은 `storybook-docs.html` 공개 문서와 원본 `docs/10-socra-storybook-collaboration-context.md`를 먼저 확인한다.
- Storybook은 Foundation -> Components -> Screens 순서로 올린다.
- 화면 단위 Storybook은 피그마 `Pages` 또는 `Page design test` source 확인 뒤에만 만든다.
- 피그마 source 없이 demo, Storybook, package에서 화면이나 컴포넌트를 임의 생성하지 않는다.

## 오늘 작업 큐

### 6/13·6/14에서 이관한 일감

- [x] **MD 템플릿 표준화** — `component-doc-template.md` 초안 작성, 공개 docs 사본까지 반영
- [x] **Story 작성 규칙 정리** — `04-storybook-guidelines.md`에 6월 우선 Story 세트 보강
- [x] **컴포넌트별 기존 문서 보정 확인** — `components/*.md` 12개가 공통 섹션 구조를 따르는지 확인
- [x] **QA / handoff 참고 문서 연결** — `README.md`에 template, handoff, QA 문서 연결 보강
- [x] **Storybook IA / inventory를 실제 `riiid/prism` Storybook 구조와 연결하는 구현 TODO 분리**
- [ ] **Week 1 완료 기준 점검** — IA 위치, P0 컴포넌트 목록, 6/15 확인 질문 정리

### 6/15 정리 완료

- [x] 공개 worklog에서 6/13, 6/14 plan을 제거하고 6/15 plan으로 이관한다.
- [x] Storybook IA / inventory를 실제 `riiid/prism` Storybook 구조와 연결하는 TODO를 분리한다.
- [x] `GlassEffect.stories.tsx` 출처를 확인한다.
- [x] `GlassEffect.stories.tsx` code-first glass reference 값을 문서로 고정한다.
- [x] `AgentProfile` / `AgentHeader`는 지금 만들지 않고, 커스텀 agent/persona 식별이 필요할 때 후보로 다시 본다.
- [x] `ActionBar` / `Chat History Panel`을 다음 배치 후보로 추가한다.
- [x] 잘못 만든 demo `/poc/chat` route를 rollback한다.
- [x] POC / 화면 단위 기준을 정정한다: Storybook은 Foundation -> Components -> Screens QA 표면이며, 화면은 피그마 source 확인 뒤 올린다.
- [x] Chromatic Button docs 링크를 Storybook 시각 QA 근거로 기록한다.
- [x] `storybook-docs` 원본 계획을 현재 `riiid/prism` repo 구조에 매핑한다.

### 작업 중 생긴 일감

- [ ] 실제 POC 기능 컴포넌트 후속은 피그마 화면/source를 먼저 확인한 뒤 Storybook screen story 또는 demo 보조 검증으로 연결한다.
- [ ] 다음 sync audit 범위 시작 — hardcoded fill/stroke → text style → effect style → radius/spacing/padding → icon strokeWeight
- [ ] `CHROMATIC_PROJECT_TOKEN`이 있는 환경에서 `pnpm visual` 재실행 후 Chromatic 링크 기록
- [ ] EN/JP mobile/web `Chat History Panel`의 긴 텍스트 `clipping + ellipsis` 처리 디자이너 QA
- [ ] 회사 내 일본 운영 서비스 사례 팔로업 — 일본 운영 서비스 사례를 확인하고 UX/UI·BX 참고 포인트 정리
- [ ] 글래스모피즘 추가 적용 영역 (사용자 직접 작업)
  - [ ] Modal / Bottom Sheet
  - [ ] Settings 카드
  - [ ] Web 헤더
  - [ ] Notification / Toast

### 대기 / 나중

- [ ] 일본시장 리서치 — UX/UI·BX 리서치, 피그마 기반 탐색/리서치
- [ ] 디자인 브릿지 테스트 — `sodam-ai/remotebridge` 기반
- [ ] 헤르메스 테스트 — 스코프 정의 필요

## Storybook 원본 기준

원본 진입점:

- `docs/10-socra-storybook-collaboration-context.md`
- `docs/70-socra-storybook-docs/01-socra-storybook-expansion-plan.md`
- `docs/70-socra-storybook-docs/02-june-execution-plan.md`
- `docs/70-socra-storybook-docs/03-june-task-breakdown.md`
- `docs/70-socra-storybook-docs/04-storybook-guidelines.md`

추천 IA:

- Foundations
- Primitives
- Chat
- Product / Agent
- Product / Compare
- Product / Source
- Product / Chart
- Patterns
- Screens
- QA
- Experiments

6월에는 `Experiments`를 구현 목표로 보지 않고, 실험 아이디어 보관 영역으로만 둔다.

## 현재 Prism repo 상태

- 현재 Storybook 실제 구조는 `apps/storybook/src/stories/*.stories.tsx`의 `Components/*` 13개와 `QA/Theme` 1개다.
- 현재 `design-system/storybook/storybook-map.md`는 피그마 기준으로 패키지에 반영된 컴포넌트만 올리는 계약 인덱스다.
- `Components/*` title rename은 Storybook ID와 리뷰 링크를 바꾸므로 별도 승인 후 진행한다.
- Product IA 전환은 먼저 원본 계획과 현재 repo 구조를 매핑한 뒤 단계별로 처리한다.

## Storybook QA 링크

- Button docs Chromatic: https://6a1e5942ec7ecc06429b1040-iycryfufxc.chromatic.com/?path=/docs/components-button--docs

## Storybook IA 매핑 초안

| 원본 IA | 현재 Prism 상태 | 판단 |
| --- | --- | --- |
| Foundations | Storybook 별도 title 없음 | 토큰/파운데이션 문서 기준 먼저 확인 |
| Primitives | `Components/Button`, `Checkbox`, `Chip`, `Radio`, `Toggle` 등 있음 | title rename은 Storybook ID 변경이므로 별도 승인 필요 |
| Chat | `Components/ChatInputBar`, `Components/HistoryItem` 있음 | Chat IA 이동 전 현재 링크/ID 영향 확인 |
| Product / Agent | `Components/ModelProfile`만 있음. `AgentProfile`, `AgentHeader` 없음 | 즉시 구현하지 않고 agent/persona 요구 생기면 후보 |
| Product / Compare | 없음 | 피그마 source + 패키지 계약 전까지 보류 |
| Product / Source | 없음 | 피그마 source + 데이터 계약 확인 전 보류 |
| Product / Chart | 없음 | MVP 필요 확정 전 보류 |
| Patterns | 없음 | 단일 컴포넌트 확정 뒤 가능 |
| Screens | 없음 | 피그마 `Pages` / `Page design test` source 확인 전 생성 금지 |
| QA | `QA/Theme` 있음 | Locale / Viewport / ErrorStates는 후속 확장 후보 |

현재 원본 `storybook-docs`는 미래 IA / 제품 UX 계획이고, 현재 Prism Storybook은 승인된 패키지 컴포넌트 QA 표면이다. 따라서 `Components/*`를 Product IA로 바로 옮기지 않고, 피그마 source와 패키지 계약이 확인된 항목만 단계적으로 승격한다.

## 후보 판단

### `ActionBar` / `Chat History Panel`

- `ActionBar`: 피그마 컴포넌트 `349:715` 근거가 있고, 메시지 하단 액션 영역으로 P0 Chat/Compare 화면에서 쓸 가능성이 높다.
- `Chat History Panel`: 피그마 컴포넌트 set `67:93` 근거가 있고, History/Chat 재방문 흐름에서 쓸 가능성이 높다.
- 둘 다 `packages/prism/component-contracts/README.md`의 다음 배치 후보에 추가했다.
- 아직 패키지 export, 컴포넌트 계약 초안, Storybook story는 만들지 않는다.

### `AgentProfile` / `AgentHeader`

- 현재 Prism inventory, 패키지 계약, 소스, Storybook에는 없다.
- `ModelProfile`은 Claude/GPT/Gemini/Grok/Perplexity처럼 기반 모델을 식별하는 아이콘 컴포넌트다.
- `AgentProfile`은 커스텀 agent/persona를 제품에서 구분해야 할 때 필요한 후보로 본다.
- `AgentHeader`는 실제 화면에서 agent identity header가 필요해질 때 별도 후보로 판단한다.

### `GlassEffect.stories.tsx`

- 현재 worktree에는 `apps/storybook/src/stories/GlassEffect.stories.tsx`가 없다.
- 원본은 Conductor checkpointer snapshot `448a840:apps/storybook/src/stories/GlassEffect.stories.tsx`에서 확인했다.
- 이 glass 효과는 예외적으로 code-first 시각 레퍼런스로 둔다.
- 피그마 `Socra Design system test`의 glass 관련 컴포넌트/효과가 이 코드를 따라가도록 별도 동기화 후보로 관리한다.
- 현재 작업에서는 패키지/Storybook에 바로 복원하지 않는다.

code-first glass reference:

| 항목 | subtle glass | strong glass |
| --- | --- | --- |
| 예상 사용처 | Header, navigation, lightweight overlay surface | Input bar, sheet, modal-adjacent surface |
| surface | `rgba(255, 255, 255, 0.40)` | `rgba(255, 255, 255, 0.72)` |
| border | `rgba(255, 255, 255, 0.48)` | `rgba(255, 255, 255, 0.55)` |
| backdrop | `blur(10px) saturate(1.35)` | `blur(16px) saturate(1.55)` |
| shadow | `inset 0 1px 0 rgba(255,255,255,0.36), 0 8px 24px rgba(0,0,0,0.24)` | `inset 0 1px 0 rgba(255,255,255,0.44), 0 10px 28px rgba(0,0,0,0.28)` |
| radius in story | `24px` | `28px` |

## Rollback 기록

- 잘못 만든 demo route: `apps/demo/src/app/poc/chat/page.tsx`
- 제거한 homepage link: `apps/demo/src/app/page.tsx`의 `/poc/chat` 링크
- 사유: 피그마 화면/source 확인 없이 임의 화면을 만든 것은 운영 기준에 맞지 않는다.
- 다음 기준: 피그마 `Page design test` 또는 확정된 화면/source를 먼저 read-only로 확인하고, 그 화면 기준으로만 Storybook screen story 또는 demo route를 만든다.

## 다음 확인

- [x] Chromatic Button docs 링크를 기록한다.
- [x] 원본 `storybook-docs` IA와 현재 repo Storybook 구조의 gap을 표로 정리한다.
- [ ] 화면 단위 Storybook 작업 전 피그마 `Pages` / `Page design test` source를 확인한다.
