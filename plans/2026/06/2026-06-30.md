# 2026-06-30 계획

전날(6/29)에서 이관. 하루 마무리에 **한 것만 `[x]`**, 안 할 건 취소선 + 이관 표기.

## 사이드바 collapsed (피그마 → 코드 정합)

- [ ] **Figma `type=collapsed` variant QA** — `Chat History Panel`(2280:5353)에 추가한 레일(3136:1363) 간격·폭(56)·아이콘 정렬·variant 간 간격 확정. (디자이너 판단 영역)
- [ ] (QA 후) **`ChatHistoryPanel`에 collapsed(레일) 뷰 지원 추가** — 공개 API 변경이라 신중. `Explorations/Home Title Word Cycle` Web 토글을 "완전 숨김" → "아이콘 레일"로 정합. 피그마 `type=collapsed` 기준.
- [ ] (선택) 검색(Cmd+K) 옵션 검토 — 채팅 누적 시 최근채팅 접근 보조 수단.

## 홈 히어로 탐색물 (확정 아님)

- [ ] 단어순환 스파이크(`Home Title Word Cycle`) 채택 여부 결정 — 채택 시 Figma 의도 박제 → 토큰화 승격 순서.
- [ ] 홈 배경 탐색물(Aurora/Prism/Dither/Ripple 등) 로컬 누적분 묶어 정리 방향.

## 참고 (재논의 방지)

- magicui 3종(border-beam / text-3d-flip / highlighter) · 최근채팅 팝오버 · hover-peek = 모두 **미채택 결론**(메모리 `project_border_beam_eval`, `project_sidebar_collapse_ux`). 재논의 시 메모리 먼저 확인.
