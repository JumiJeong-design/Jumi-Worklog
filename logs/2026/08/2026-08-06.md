# 2026-08-06

디자인시스템과 제품 화면을 맞춘 날. 컴포넌트 10종을 패키지로 올리고, 제품 파일 토큰을 전수로 묶고, 로컬 컴포넌트를 DS 인스턴스로 바꿨다. 후반은 차트 컴포넌트를 만들다가 **피그마 제약 세 가지를 알아내는 데** 시간을 썼고, 결국 차트 두 개는 "컴포넌트로 쓰지 않는다"가 결론이 됐다.

요청:

```text
지금 화면이랑 DS랑 안맞는 부분있으면 적용해서 git에 올리고 스토리북에도 반영하고싶은데
DeepCTA는 지금보니까 저 라인을 회전하고싶어 반짝이게
Mode Toggle은 디자인을 바꿔야겠다 지금보니까 예전디자인이야
에이전트 프로필이미지도 여러개로 늘어날텐데 저게 영역이 컴포넌트여야지 나중에 바꿔치기가 쉬울것같아서
터치 고려하면 뭐가낫나? 40은 좀 작고 60은 큰데.. 50만들면 안되나
차트 컴포넌트 먼저
```

## 한 일 — 컴포넌트 10종 패키지 포팅

`ActionBar` · `ReportFeedback` · `ScoreBar` · `ReportQuick` · `SurveyOption` · `VoteOption` · `RetryButton` · `CommentHeader`/`CommentItem` · `ProfileGroup`.

`ActionBar`는 값이 틀린 게 아니라 **위치가 문제**였다. 피그마 `Button/ActionBar`는 확정 컴포넌트인데 패키지에 대응이 없어 스토리 두 곳에 손코딩돼 있었고, 그래서 피그마가 바뀌어도 내려올 경로가 없었다. 실측하니 순서(`copy·share·thumbs` ↔ `thumbs·thumbs·copy·share`)·share 노출·아이콘 색 셋 다 어긋나 있었다.

`ReportFeedback`은 주미님이 "리포트 피드백은 타이틀 문구까지 해서 별도"라고 정해 `ActionBar`와 갈랐다. 선택 표시 규칙과 토큰은 공유한다.

## 한 일 — 공개 API 변경 6건

| 컴포넌트 | 변경 |
| --- | --- |
| `DeepCTA` | `state` 제거, `shimmer` 추가 — 라인 위를 광 빔이 도는 Border Glow |
| `ChatInputBar` | `guideText` 제거(피그마에서 문구 행이 사라짐), `size`(`compact`/`wide`) 추가 |
| `ModelProfile` | 크기 축 5→6종(`2xl` 48 신설), `agent` 모델, `avatar` 교체 슬롯 |
| `ModeToggle` | 세그먼티드 → 단일 칩. 공개 API는 그대로 |
| `MessageBubble` | 사용자 말풍선 면 `surface-inset`으로 정정 |
| `AttachmentChip` | 파일형 재시도 버튼 제거 |

`DeepCTA`는 처음에 링 전체를 돌렸는데 주미님이 "라인이 반짝여야지"라고 해서 다시 만들었다. 그라데이션은 정지하고 광 빔만 도는 구조로 바꿨다.

## 한 일 — 토큰 정합

**브랜드 그라데이션 통일.** 흰 스톱 4개짜리 `gradient.deep-cta`와 흰색 없는 `mode-toggle-border`가 갈려 있었다. 주미님이 "모드 토글로 통일" 결정 → `color.gradient.brand` 하나로 합쳤다. 근거는 **광 빔이 흰색인데 링에도 흰 구간이 있으면 빔이 거기서 사라져 도는 게 안 읽힌다**는 것.

**구분선 밝기.** `border.divider`가 `neutral/200`이라 오버레이 목록 선이 어두웠다. 피그마를 보니 **변수는 `neutral/200`인데 실제 `Bottom Sheet`의 `Divider`는 `neutral/100`**을 쓰고 있었다. 화면이 정본이라 코드를 `neutral/100`으로 내렸다.

**구간색 단일 출처화.** `opinionBand.ts`가 램프 실값을 `var()+hex fallback`으로 12개 들고 있었다(사유: blue·orange 램프가 공개 변수가 아니라서). 확인하니 **필요한 값이 전부 공개 토큰으로 있었다** — `--prism-color-band-*`와 `--badge-label-{tone}-*`. 12개 값이 정확히 일치해 렌더 변화 없이 교체했고 hex 사본이 0이 됐다.

## 한 일 — 제품 파일 정합

**토큰 미바인딩 503 → 18.** 실화면 안 미바인딩은 3건뿐이고 남은 18은 sticky note·섹션 크롬·로고색이다. 램프 밖 회색 72건은 최근접 스냅, FRAME 면 129건은 semantic surface에 묶었다. 스크림 16건은 다크에서도 검정이어야 해서 mode-stable한 `alpha/black-alpha`를 썼다.

**로컬 컴포넌트 → DS 인스턴스 110곳.** `profile group` 19 · `socra` 28 · 시트 핸들 13 · `agent list` 4 · `footer` 4 · `Chart/LegendRow` 61.

**고아 마스터 정리.** `footer`와 빈 섹션을 지웠다. `socra`(207곳)·`agent list`(50곳)는 `Key page_test`가 쓰고 있어 남겼다 — 여기서 규칙 하나를 얻었다. **"정본 페이지 참조 0 = 삭제 가능"이 아니다.** `Key page_test`는 감사·write 범위에서 빼는 페이지지만 마스터 삭제만은 이 페이지까지 세야 한다.

## 한 일 — 피그마 컴포넌트 신설

`Agent/Face`(교체 슬롯) · `Agent/Row` · `Report/Participants` · `Sheet/Handle` · `Chart/StackBar` · `Chart/LegendRow` · `ModelProfile 2xl`.

**에이전트 얼굴을 교체 슬롯으로 뺀 게 주미님 지적이었다.** 처음엔 5개 사이즈에 아트웍을 각각 구워 넣었는데 "얼굴이 여러 개로 늘어날 텐데 저게 컴포넌트여야 나중에 바꿔치기가 쉽다"고 해서 다시 만들었다. `Agent/Face`를 독립 컴포넌트로 빼고 `Model Profile` 세트에 **INSTANCE_SWAP 속성**을 달았다. 코드에도 `avatar` 슬롯을 열었다.

**아바타 크기는 48로 갔다.** 참여자 줄이 50인데 램프에 50이 없었다. 주미님이 "40은 작고 60은 큰데 50 만들면 안 되나"라고 물어서, `size` 토큰에 이미 있는 48을 권했다 — Material 터치 최소 규격이라 이름에 근거가 붙고 50은 근거가 없다. 그러면서 **터치는 아바타 크기로 푸는 게 아니라는 것**도 계약에 적었다. WCAG는 보이는 크기가 아니라 타겟 영역을 재므로 감싸는 요소 패딩으로 44를 확보하면 된다.

## 알아낸 것 — 피그마 제약 세 가지

차트 컴포넌트를 만들면서 시간을 가장 많이 쓴 부분. 다시 삽질하지 않도록 근거 문서와 plan에 남겼다.

**1. 인스턴스 자식의 폭은 못 바꾼다.** `resize()`가 에러 없이 조용히 무시된다. 오늘 `ScoreBar`·`StackBar`·`ProgressBar` 세 번 걸렸다. `padding`은 바뀌므로 경계가 하나면 우회할 수 있지만, 스택 바처럼 경계가 여럿이면 방법이 없다(`layoutGrow`는 0/1뿐이라 비율 가중치를 못 준다).

**2. 라이브러리 전달이 안 되면 마스터 이름부터 본다.** 슬래시는 폴더가 되므로 접두어가 붙으면 게시 목록에서 사라진다. `Chart/ProgressBar`가 `Survey/Option/Chart/ProgressBar`로 중첩돼 있었다.

**3. 배포된 컴포넌트의 구조 변경은 소비 파일에 늦게 내려간다.** 이름·색은 바로 오는데 자식의 auto-layout 사이징과 프로퍼티 바인딩은 한 박자 늦는다. 이걸 몰라서 주미님께 게시를 여러 번 부탁드렸다. 다른 세션에서 같은 증상을 다시 만났고(복제한 variant의 슬롯 `componentPropertyReferences`가 비어 내려옴) **게시를 한 번 더 하니 들어왔다.** "안 내려간다"보다는 **즉시 반영을 신뢰하면 안 된다**가 정확하다. 확인 방법은 게시를 반복하는 게 아니라 소비 파일에서 `importComponentByKeyAsync`로 마스터를 받아 `componentPropertyReferences`를 직접 읽는 것이다. 구조를 바꿀 거면 배포 전에 확정하는 게 여전히 맞다.

그래서 **`Chart/StackBar`·`Chart/ProgressBar`는 규격 참조용으로만 남기고 화면은 손그림 유지**로 정했다. 제품 화면의 스택 바가 손그림인 건 정리 안 된 게 아니라 플랫폼 제약이다. 진행바 12곳은 변수 바인딩된 손그림으로 되돌려 진행률(13%·43%·62%·94%)을 복구했다.

`Chart/LegendRow`는 폭 데이터가 없어 정상 작동한다 — 61곳 치환 완료.

## 정정한 것

**`chart/*` 토큰 선행 조건이 이미 해소돼 있었다.** plan-20이 "차트 색은 하드코딩이라 semantic 승격이 선행"이라며 근거로 "`blue/500`·`orange/500`이 CSS 변수로 없다"를 들고 있었는데, `ScoreBar` 포팅하며 만든 `color.band.*`가 정확히 그 둘을 공개한다. 이름만 달랐지 역할은 채워져 있었다. 낡은 기술 4곳을 고쳤다.

**남은 차트는 포팅이 아니라 피그마 생성 건이었다.** DS 마스터 99개에 `DonutChart`·`LegendItem`·`ProgressBar`·`VoteBar`가 하나도 없다. plan-20은 "신규 포팅"으로 적어 뒀지만 `AGENTS.md` 기준으로 피그마 생성·승인이 먼저다.

**시트 셸 결론을 뒤집었다.** 내가 "피그마 인스턴스는 자식을 못 받으니 시트 셸은 손그림이 정상"이라고 적었는데, 주미님이 만든 `Sheet/Shell`이 **본문을 INSTANCE_SWAP으로** 빼서 해결했다. 나도 같은 패턴을 `Agent/Face`에 쓰면서 시트에는 적용을 못 봤다.

**`DonutChart`·`OpinionRow`는 만들지 않았다.** 393폭 실화면 전수에서 0건 — 스토리북 탐색본에만 있다. 만들면 승인 안 된 시각을 DS 컴포넌트로 굳히게 된다.

## 한 일 (다른 세션) — Deep 전문 시트 3층 슬롯 구조

뒤로가기 하나 고쳐달라는 요청에서 시작해 시트 전체가 컴포넌트가 됐다. 위 「남은 것」에 있던 `Sheet/전문 시트` 10곳 치환도 여기서 끝났다 — 본문 컴포넌트를 같이 만들었다.

요청:

```text
여기 좀 팔로우해줘
그리고 여기서 뒤로가기 디자인이 이게 맞아? 너무 엉성한데..
뒤로가기가 꼭 필요할까?
우리 bottom sheets도 이런식으로 slot으로 조절되게 바뀌어야할까?
로고 아이콘 slot
로고를 면으로 채워야하지 않을까? 그리고 프로필에 뱃지처럼 붙는건어때
이 내용들 ds에 안넣고 디자인파일에 넣은 이유는?
뭐가 낫니 더 관리하기 편한쪽으로
```

### 뒤로가기 — 왜 엉성해 보였나

chevron이 시트 프레임 직속으로 x=16, y=10에 얹혀 있었다. 핸들 영역은 16(패딩 12 + 핸들 4)인데 아이콘은 y=10~34라 18px 삐져나오고, 핸들 중심(y=14)과 아이콘 중심(y=22)이 8px 어긋났다. 아바타와는 6px 붙어 있었다.

진짜 문제는 그 자리다. **시트 좌상단은 시트를 끌어내리는 주 그립 영역**이라 탭과 드래그가 서로 먹는다. 터치 타깃도 24로 iOS 44에 못 미쳤다. 핸들 아래 48 헤더 행에 pill(높이 40, radius full, 정보 카드와 같은 surface 토큰)로 옮겨 탭 타깃 88×48을 확보했다. 라벨은 선택지명("옮긴다")을 그대로 써서 리스트 시트와 같은 단어로 묶었다.

"뒤로가기가 꼭 필요한가"에는 **필요하다**로 답했다. 칩 → 리스트 → 개별은 성격상 여러 AI를 훑는 진입이라, 없으면 시트 닫고 칩 다시 누르고 고르는 3탭 왕복이 된다. 대신 노출 조건을 **"직전에 다른 시트가 있었을 때만"** 한 줄로 못 박았다 — 목적지가 항상 직전 시트 하나라 진입 경로가 늘어도 판단이 안 갈린다.

### 높이 규칙 — 852 전용 수치였다

문서(`bottom-sheet.md` 8/5판)는 펼침을 「인셋 84 / 768」로 적고 있었는데 **실측은 100 / 752**였다. 고정 크롬(프리뷰를 뺀 나머지)도 458이었다.

그래서 인셋 240을 고정으로 쓰면 852에서만 성립한다. iPhone SE(667)는 427, 360×640은 400이라 **둘 다 이미 깨져 있었다** — 프리뷰가 음수가 되고 피드백 버튼까지 잘린다.

```
접힘 = clamp(560, 화면높이 − 240, 펼침값)
펼침 = 화면높이 − (상단 safe-area + 56)
```

하한 560은 크롬 458 + 프리뷰 최소 104(페이드 56 + 두 줄 48)에서 나온다. 852에 대입하면 612/752로 현행 값이 그대로 나온다. 여기에 **두 값 차이가 120 미만이면 접힘 단계를 없애고 펼침으로만 연다**를 붙였다 — SE와 360×640은 자동으로 펼침 전용이 된다.

토스트도 같이 뒤집었다. "시트를 덮지 않는다"는 공간이 있을 때의 배치 규칙이지 불변식이 아니다. 펼침은 위 여유가 100뿐이라 바깥에 두면 상태바를 침범한다. **시트 상단 앵커 — 64 남으면 바깥, 없으면 겹침, 어느 경우에도 밀지 않는다.** 접힘 y=176은 그대로라 기존 7·8번 시안은 수정이 없었다.

### 3층 구조

슬롯 경계를 본문 전체가 아니라 **프로필 블록**으로 잡았다. 접힘/펼침 차이는 전부 답변 전문 블록 안에 있고 프로필·피드백은 세 타입 다 같다. 인스턴스화가 막힌 두 화면이 그 경계를 알려줬다 — 9번은 카드 숨김 오버라이드로 겨우 맞췄고, 11번은 뱃지·캡션이 **값이 아니라 구조 추가**라 아예 불가능했다.

| 층 | 컴포넌트 | 소속 |
| --- | --- | --- |
| 셸 | `Sheet/Shell` (chrome sheet/panel × back off/on) | DS |
| 본문 | `Sheet/전문 본문` (collapsed/expanded) | 제품 파일 |
| 프로필 | `Sheet/Profile` (agent/model/external + 텍스트 4종) | DS |
| 아바타 | `Model Profile` `Model=external` 6종 + Logo 슬롯 | DS |

축이 곱해지지 않고 더해진다. 로컬 세트는 본문이 variant마다 복사돼 8벌이었고 축을 하나 더하면 12·16으로 뛰었다 — 지금은 셸 4 + 본문 2 + 프로필 3이다.

**결과: 손코딩 시트 0, VW `ticker` 직접 인스턴스 0.** 화면 10곳 + 사이드 패널 3곳이 전부 DS 셸 인스턴스다. 본문은 제품 파일에 남겼다 — 2,500자 더미가 펼침 마스터 높이(3021)를 결정하는데, 더미 콘텐츠가 시스템 컴포넌트 크기를 정하게 두면 안 된다.

### 외부 에이전트

제품 화면이 VW 라이브러리의 `ticker/*`를 **직접 인스턴스로 물고 있었다.** 남의 라이브러리에 우리 화면이 묶이고 "Socra 화면엔 Socra 컴포넌트만" 규칙에도 어긋났다. `Model=external` + Logo INSTANCE_SWAP으로 바꿔 **크기 규격만 우리가 소유하고 아트웍은 제휴사 원본을 그대로** 받는다(주미님 결정).

표식은 넷에서 둘로 줄였다. `Provider Badge/Vestway`를 아바타 우하단에 붙이고(60→24, 40→16), 제공처 캡션만 남겼다. 이름 옆 `V.AI` 텍스트 뱃지와 캡션 앞 인라인 마크는 캡션·뱃지와 중복이라 뺐다.

**뱃지를 `Model Profile` 축으로 올리자는 안은 실측으로 기각했다.** 리포트·`Profile Group` 아바타가 26px에 −10 겹침이라 비례 뱃지는 10px 미만이고 하필 우하단이 다음 아바타에 가려진다. variant도 36→72로 뛴다. 근거를 컴포넌트 description에 남겨 재측정을 막았다.

### 정정 — 중복 프레임

구 `8 · 답변 복사`는 7번과 시트(240/612)·토스트(y=176)가 완전히 같고 **문구만 달랐다.** 이름이 "복사하기 버튼 선택"인데 복사 버튼은 `Feedback=none`으로 2번과 동일해 눌린 티도 없었다. `Button/ActionBar`의 Feedback 축이 `none/like/dislike`뿐이라 복사용 상태 자체가 없다. 삭제하고 그 자리에 펼침+토스트 겹침 시안을 넣어 **배치 규칙당 프레임 하나**로 정리했다.

## 배포

`@riiid/prism@0.4.0-canary.c117510f…` 카나리 게시. changeset은 minor(0.3.0 → 0.4.0).

첫 시도는 CI에서 실패했다. 로컬에서 `validate`·`typecheck`·`test`·`build`만 돌리고 **`pnpm lint`를 빼먹어** 토큰 게이트 위반 7건을 올렸다(임의 폰트 크기 9곳, 마스크 hex 2곳). 이후로는 CI와 같은 `pnpm check`로 돌린다.

## 남은 것

- PR #48 머지 → CI가 0.4.0 버전 PR을 자동으로 연다. 39커밋짜리가 돼서 제목·본문을 실제 내용으로 다시 썼다
- PR #49 (`feat/chart-tokens`) 리뷰
- `Sheet/Shell`·`Sheet/Profile` 패키지 포팅 + 계약 신설 (피그마만 있고 코드 없음)
- 리스트 시트 40px 아바타에서 제공처 뱃지 글래스 굴절이 마크를 먹는다 — 사이즈별 보정 판단 대기
- 외부 에이전트 출처 표기의 기획서 반영 (PM 요청 대기)
- 다크 표면 토큰 붕괴(`surface`=`surface-muted`, `surface-raised`=`surface-inset`) — plan-10 기록. 다크 작업의 첫 항목

### 이어서 (같은 세션 후반) — 잔여 4건 중 3건 처리

- **DS `Bottom Sheet` 폭 390 → 393** — 내부 행 FIXED를 전부 FILL로 바꿔 폭이 뷰포트를 따르게 했다. 인스턴스 자식은 `resize()`가 무시되므로 사이징 전환으로 처리(오전 세션 함정 그대로 적용됨).
- **`ModelProfile` external 포팅**(`95bd8ef`) — `model="external"` + `src` prop. 이미지가 원을 채우고, external은 아이콘 색 변수를 안 태운다. `src` 없으면 빈 원 — 피그마 placeholder는 작성용이고 런타임엔 데이터가 온다. 테스트 78건 통과, changeset minor.
- **다크 `surface-inset` 충돌 해소**(`5caefa6`) — 처음엔 표면 4단+경계 2종을 다 재배치했다가 **피그마 실측 후 되돌렸다.** 진짜 충돌은 `elevated`==`sunken`(#191A1D) 하나였고, 코드 `surface`==`surface-muted`는 확정된 매핑이지 버그가 아니다. `elevated`를 올리면 `border-default`와 부딪히고 408곳이 흔들려서 **`sunken`을 `#000000`으로 내렸다.** 다크 3단 분리(#000000 < #0E0F12 < #191A1D), 라이트 무변경, 피그마 변수도 동일 정합. 계약 라인 101의 미결 표시 해소.

- **`SheetShell`·`SheetProfile` 패키지 포팅**(`a90ef83`, 브랜치 `feat/sheet-shell-profile`) — 셸은 chrome(sheet/panel)·backLabel·본문 슬롯, 프로필은 type 3종 + 제공처 뱃지 슬롯. 접힘/펼침을 셸에 안 둔 이유(본문 상태)와 높이 규칙·뒤로가기 근거를 JSDoc과 계약 2개에 옮겼다. 파트너 마크는 코드에서도 노드 슬롯 — 패키지에 자산 없음. 테스트 5건 추가(83 통과), `pnpm check` 전체 게이트 통과. 토큰 게이트에 `text-[15px]`·`text-[13px]`가 걸려 `text-md`·`text-13`으로 교체(오전 세션과 같은 위반을 게이트가 잡아줌).

### 이어서 (3차) — 다크 구간색 · SheetAnswer · 고아 마스터 재집계 (PR #51)

- **다크 구간색**(`6253eba`) — band 4색이 참조하는 primitive 계단을 피그마 Dark 실측값으로 mode-aware 전환. 피그마 램프는 이미 mode-aware였고 코드 primitive가 단일값인 게 드리프트였다 — 신설이 아니라 미러링. bare 참조는 band 4개뿐이고 다른 소비자는 명시적 light/dark 쌍이라 무변화.
- **`SheetAnswer` 포팅**(`64de619`) — 답변 전문 접힘/펼침 블록. 프리뷰 높이는 컨테이너가 준다(고정 px는 852 전용 수치가 된다는 오늘 교훈 그대로). 피드백 행은 컴포넌트화하지 않았다 — 피그마도 Button 인스턴스 조합이다. `FullAssembly` 스토리가 셸+프로필+본문+버튼 전체 조립을 보여준다. 이로써 시트 빌딩블록 코드 포팅이 끝났다.
- **고아 마스터 재집계**(`c4d77d8`) — plan-20의 "543개"는 중첩 포함 집계였고, 직접 스왑 가능한 톱레벨은 **44개·11종**뿐이다. Model Profile 61 같은 수는 전부 부모 안 중첩 참조라 부모를 재연결하면 같이 풀린다. Toast ×2를 파일럿으로 재연결해(크기·문구·아이콘 동일 검증) 파이프라인을 증명했다. Callout Type5(라이브러리에 없음)·ChatInputBar 가이드문구 구판은 재연결이 아니라 **레거시 화면 동결 여부 확인**이 선행 — 주미님 판단 대기.

### 이어서 (4차) — 에러 표면 기준 확인 + 레거시 고아 결정

"저 에러 디자인 기획에 있던가? 기획에선 다 다이얼로그로 본 것 같기도 하고"에서 출발해 기준을 다시 찾았다. **새로 정한 게 아니라 이미 있던 걸 재확인한 것이다** — 기획서 5.1은 네트워크 실패만 팝업(다이얼로그) 우선으로 명시하고, 재시도 3회 상한은 별개다. 8/2 세션이 이미 이 조사를 하고 시안 2컷(3회 이하=닫기/다시 시도, 초과=확인)까지 DS에 그려뒀다.

- 표면 3분법: **다이얼로그**(흐름 끊고 지금 결정 — 네트워크·세션 만료) / **인라인**(콘텐츠 자리에서 실패·복구 — 리포트 실패 계열, 3회 상한은 여기 붙고 초과 시 RetryButton exhausted) / **토스트**(알림만). 주말 quick 에러 화면 4종이 이미 인라인 패턴이다.
- **rules.md에는 안 박는다(주미님).** 기획이 정본인데 옮겨 적으면 두 벌이 된다.
- **Callout Type5 정정** — "라이브러리에 없다"는 내 오판이었다. DS Callout 세트에 Type5가 살아 있고, 고아는 옛 로컬 마스터일 뿐이다. 내용(에러 문구+다시 시도)의 현행 대체는 에러 화면 4종 + RetryButton 패턴이라 **당장 안 쓰는 걸로 확정, 인스턴스 4개는 레거시 참고용으로 동결**(주미님).
- ChatInputBar 가이드문구 구판 ×2 — 재연결하려고 보니 **이미 최신판(remote)으로 연결돼 있었다.** 별도 세션 처리분.
- 남은 고아 30개(Chat History Panel ×16 · Tab Item ×6 · Bottom Bar 구판 ×6 · Message Bubble ×6 등)는 종별 diff 후 재연결 — 다음 세션.
- 남은 설계 구멍: 기획서 5.3 전송 시점 오프라인(입력 보존) 미설계, Deep 쪽 에러 커버리지.

### 이어서 (5차) — 고아 마스터 정리 종료

- **Chat History Panel ×14 재연결**(`c20d06d`) — 현역 화면분(web/chat · Layout 데모 7 · DnD 6) 전부 라이브러리로 스왑. diff는 행 간격·선택 행 표현의 마이너 갱신뿐, 크기·위치 14/14 보존 확인.
- **소속 화면 분류가 작업량을 정했다.** Tab Item ×6 · Message Bubble ×6 · Bottom Bar ×6은 전부 `참고용(정식 디자인 x)` 화면 소속이라 "옛날 참고용은 냅둔다" 결정으로 동결 — 스왑 대상은 결국 CHP 하나였다. 846 → 543 → 톱레벨 44 → 실제 재연결 필요분 16(Toast 2 + CHP 14)으로 줄어든 것: **집계 단위(중첩 포함/톱레벨/소속 화면)를 바꿀 때마다 일이 1/10씩 줄었다.**
- **plan-20 고아 항목 종료.** 잔여 26개는 전부 의도 동결(참고용 화면 + Type5 + plan-10 진행 중 레일)이고 재연결할 고아는 없다.

### 이어서 (6차) — 머지 스택 실행 + 0.4.0 배포

주미님 지시로 머지를 직접 실행했다. **#48 → #49 → (#50·#51 자동) → #52 → #43(0.4.0)**, 전부 squash(레포 관례).

- **#49가 #48 squash와 충돌** — 스택 브랜치 + squash 조합의 예정된 충돌. 공유 워킹트리를 안 건드리려고 **임시 worktree**에서 해소했다. 충돌 15개 파일 전부 "main에만 있는 줄 = 브랜치가 대체한 구버전"임을 파일별로 검증한 뒤 브랜치 판으로 일괄 해소, 머지 트리가 브랜치 팁과 개행 1자 빼고 동일함을 확인하고 커밋.
- **함정: 충돌 목록을 `tail`로 잘라 보다가 마커 든 파일을 스테이징할 뻔했다.** `git diff --name-only --diff-filter=U`로 전체 목록을 먼저 떠야 한다. 두 번째 함정: worktree의 로컬 브랜치가 origin보다 8커밋 뒤였다 — **worktree 만들면 reset --hard origin/<branch>부터.**
- **#50·#51은 누르기 전에 자동 MERGED가 돼 있었다.** 병렬 세션이 내 브랜치들을 chart-tokens에 합쳐둬서, #49 squash 하나에 스택 전체(SheetShell·SheetAnswer·다크 토큰)가 실려 나갔고 GitHub이 base가 head를 포함하게 되자 두 PR을 자동 머지 처리했다. 내용물은 main에서 파일 단위로 전수 확인.
- **squash 꼬리 유실 1건 수습** — 흡수 시점 이후 커밋인 plan-20 고아 종료(c20d06d)가 main에 못 탔다. main 기준 브랜치에 cherry-pick(c4d77d8은 이미 흡수돼 empty)해서 #52로 별도 머지. Step 2.5의 squash 오탐 패턴을 "SHA가 아니라 내용으로 확인"으로 통과했다.
- **#43 머지 → 0.4.0 배포.** changeset 12개(오늘 신설 6개 포함) 소진. release 워크플로 publish-latest 성공(38s). npm 직접 확인은 내 셸 미인증이라 404(프라이빗 스코프 특성) — 인증된 쪽에서 `npm view @riiid/prism dist-tags` 확인 필요.
- 남은 오픈 PR: **#47**(SourceChip — 제목의 "머지 전 확인 2건"이 미해소라 임의 머지하지 않음), #13(codex 레거시).

### 이어서 (7차) — 배포 확인 + 오픈 PR 정리

- **0.4.0 배포 확인 완료.** 내 셸이 npm 미인증이라 404가 떴는데(restricted 스코프라 미인증엔 안 보인다), CI 로그에서 직접 확인했다 — `name: @riiid/prism` / `version: 0.4.0` / `Publishing to registry.npmjs.org with tag latest and restricted access`, `publish-latest` 성공. **레지스트리 확인이 막히면 워크플로 로그의 npm notice를 보면 된다.**
- **#47 SourceChip 클로즈.** 본문의 확인 2건(export 게이트·Figma에 없는 `failed` 축)보다 상위 문제가 있었다 — plan-12 **맨 위 상태 줄**이 "`SourceChip`/`SourceCard`/`SourcePopover`는 7/16 탐색에서 탈락, 정본은 `Markdown Block`"이라고 이미 확정하고 있다. 실측도 일치: DS `✅ Components`에 마스터 0건, 살아 있는 인스턴스 4개는 전부 `Key page_test`(시안·아카이브) 7/16 프레임. **탈락한 탐색물을 패키지 공개 API로 올리는 PR이었다.** §5-2 차단 조건만 보고 상태 줄을 놓치면 반복되므로 사유를 plan-12에 기록(#55 머지).
- **#13 codex 레거시 클로즈.** 6/22 PR인데 핵심(`ChipLocale`·`chipStringTokens`·`locale` prop)이 이미 main에 있다. 파일 80개짜리라 지금 머지하면 6월 상태로 되돌린다.
- **#53 중복 버전 PR 클로즈.** #43 머지(10:57:25) 92초 뒤 봇이 같은 changeset 12개로 브랜치를 다시 밀었다(10:58:57). main은 이미 0.4.0이고 changeset도 전부 소진 — 머지하면 배포된 버전을 다시 만든다. 브랜치는 남겨 다음 changeset 때 정상 재생성되게 했다.
- 남은 오픈 PR은 #54(다른 세션 작업분) 하나. **내 큐는 비었다.**

**사고 2건.** 하나는 과하게 잡은 토큰 변경을 되돌리며 `git checkout -- semantic.tokens.json`을 썼는데, 그 파일에 **다른 세션의 미커밋 layout 토큰이 같이 있어서 함께 날렸다.** `token-contract.md`에 값이 전부 문서화돼 있어 16개 토큰을 복원했고, 매니페스트 개수(13+3+3)가 문서와 일치하는 것으로 누락 없음을 확인했다. 원인은 남의 미커밋 변경이 있는 파일에 checkout을 쓴 것 — **공유 워킹트리에서는 되돌릴 때 checkout 대신 내가 넣은 텍스트만 역치환한다.**

커밋은 세 개로 갈랐다 — 저쪽 layout 작업은 "(병렬 세션분 정리)" 관례(`6272188` 선례)로 별도 커밋(`093a96b`), 내 surface-inset은 따로(`5caefa6`). 생성 파일이 섞여 헝크 분리가 안 되므로, 내 편집을 역치환 → 빌드 → 저쪽 커밋 → 재적용 → 빌드 → 내 커밋 순서로 갈랐다.

## 반성

브랜치 하나가 이틀 자랐다. `AGENTS.md`는 "짧은 feature 브랜치 → PR → main 통합"인데 8/5 Navigation Bar로 연 브랜치에 8/6 작업을 계속 얹어 39커밋이 됐다. 같은 브랜치에 다른 세션이 동시에 커밋하면서 파일이 세 번 휩쓸린 것도 같은 뿌리다. 차트 작업부터는 짧은 브랜치로 끊었다.

게시를 다섯 번 부탁드린 것도 내 탓이다. 원인이 게시 절차가 아니라 컴포넌트 구조 전파 방식에 있었는데, 그걸 확인하지 않고 게시만 반복 요청했다.

다른 세션에서는 **컴포넌트를 지우기 전 참조 검사를 섹션 범위로만 돌려** 사고를 낼 뻔했다. `Sheet/전문 시트`를 "참조 0"으로 판단해 지웠는데, 실제로는 다른 섹션의 사이드 패널 데모 3곳이 쓰고 있었고 그 사이 다른 세션이 `chrome=panel` 4 variant를 더해 8개로 키워둔 상태였다. 페이지에서 떨어진 고아 상태로 남아 복구는 했지만, **삭제 전 검사는 페이지 전수여야 하고 지우기 전에 참조 목록을 사람에게 먼저 보여야 한다.** 같은 파일에 세션이 둘 붙어 있으면 몇 분 사이에 인스턴스 ID가 바뀐다.

둘째는 `git add -A`다. 포팅 커밋에 병렬 세션의 파일 2개(AttachmentDnD 스토리·plan-19)가 딸려 들어갔고, 그 세션이 내 브랜치에 커밋까지 했다 — 같은 워킹트리라 내가 브랜치를 바꾸면 저쪽 커밋도 거기 떨어진다. PR 전이라 soft reset으로 갈라 재커밋하고 force-with-lease로 정리했다. **공유 워킹트리에서는 add -A 금지, 파일을 명시해 스테이징한다.**

---

## 이어서 (6차) — 첨부·드래그앤드롭 트랙

요청:

```text
pc 시안잡아보고있는데
- 드래그앤 드랍 시나리오
    - 드래그앤 드랍일때 지원하지않는 형식 노출
    - 되는것과 안되는것 함께 드래그앤 드랍 한 경우에 대한 케이스 추가
    - 용량 초과일때? 토스트? -> 케이스 구체화될때 다시
- 파일 확장자 디자인 디테일 수정
이쪽 진행해볼래>
```

시작하자마자 **기획서가 오늘 아침 v1.0.6으로 올라간 걸 발견**했다. 첨부 절이 통째로 재작성 — 이미지 10→5장, 문서 20→5페이지, 드롭당 6개 캡 신설, 전송 규칙 반전(성공분만 골라 보내기 → 에러 칩 남으면 전송 불가). 주미님이 미결로 남긴 "용량 초과는 토스트?"에 스펙이 이미 답을 준 상태였다(에러 칩).

- **DS 정합** — ComposerGroup·Group 이미지 슬롯 10→5(전부 hidden이라 시각 변화 0), RetryBtn 잔재 2개 제거. 하나는 좌표 (191,62)로 180×56 칩 **바깥**에 떠 있던 것. 이걸로 재시도 어포던스가 아이콘형(↻)으로 수렴 — plan-19 병목 4 종결.
- **7/28 첨부 산출물 4개가 파일에서 소실**된 걸 확인(케이스 보드·에러 3종·바텀시트·미지원 토스트). plan에 노드 id를 적어둔 덕에 소실을 확인할 수 있었다. 케이스 보드 2개를 새로 깔고 바텀시트는 v1.0.6 한도로 재제작.
- **PC 화면은 새로 안 그렸다** — 제품 파일에 `web / chat` 1440×1024가 이미 완성돼 있었다. 그걸 clone해 상태만 얹었다(변수 바인딩 보존).
- **PC DnD 컷 6장 + 홈 컷.** 드롭존이 피드백 따라 세 번 좁혀졌다: 창 전체 스크림("너무 밤 같다") → 채팅 영역("화면 전체 먹으면 곤란") → **입력창**("홈에서는 인풋 영역만 될 거잖아"). 밤에 판정 범위도 채팅 영역(LNB 제외)으로 확정.
- **슬롯 순서 미결 해소** — 슬롯이 전부 Attachment/Chip 인스턴스라 슬롯별 Type·State 스위칭으로 어떤 순서든 표현된다. `Slot 1~6` 중립화 + 폭 HUG, 컷4를 "파일 에러 앞배치" 시연으로 재작업.
- **칩 인터랙션 3종**(Claude Code 컴포저 레퍼런스) — `State=focused` 변형 마스터 추가 + hover 파일명 툴팁·클릭 미리보기 보드 신설. `Attachment/Preview` 컴포넌트도 신설(280×196 FIT + 캡션).
- **Storybook 탐색 신설** `Explorations/첨부 드래그앤드롭` — 실파일 드래그로 흡수 모션(드롭점→칩 슬롯 비행 + 컴포저 높이 FLIP), 커서 드래그 고스트(카드 스택+개수 배지), 톤 2안·스윕 비교. Chromatic 게시로 모바일 리뷰 → 톤은 체감 무차별이라 브랜드 유지, **스윕 채택**.
- **코드 포팅** — `ChatInputBar` 이미지 행 `flex-wrap`(피그마 WRAP 미러), `AttachmentChip` focused 상태(action-primary 2px 링, 계약·스토리·changeset 동반). 85 테스트 통과.

### 이미지 에러 칩 — 하루에 세 번 뒤집힌 끝에 정책에서 풀렸다

배지(→"글씨가 너무 빽빽") → 파일 칩 형태 전환(→"사진 저렇게 들어가는 거 밤티") → **원래 아이콘 오버레이로 원복**. 세 번째에 주미님이 **"용량 초과면 애초에 안 들어가고 걍 알람이 떠야 맞지"** — 형식·용량·개수는 즉시 차단+알림, 에러 칩은 붙은 뒤 실패 3종만. 그러면 이미지 칩에 남는 사유가 열 수 없음·업로드 실패 둘뿐이라 ⚠/↻ 아이콘으로 충분해진다.

**표기 문제를 표기 방법으로 풀려다 세 번 실패했고, 정책을 바꾸니 문제 자체가 사라졌다.** 다만 이건 v1.0.6(형식·용량=에러 칩)과 정면 충돌이라 모든 보드·컷에 PM 플래그를 박았다.

### 함정 모음

- **한글 스토리 딥링크는 모바일에서 잘린다** — 메신저가 첫 한글에서 URL을 끊어 엉뚱한 스토리로 떨어진다. Chromatic/Storybook 링크는 percent-encode해서 보낸다.
- **비교 시안은 응시할 시간을 줘야 비교가 된다** — 900ms 자동 재생으로 톤 3안을 보여주니 "다 비슷해 보인다"였다. 버튼을 2단(1탭=오버 유지, 2탭=드롭)으로 바꿔야 비교가 성립했다.
- **인스턴스에서 변형을 스왑해도 폭이 안 따라온다** — 외부 마스터가 슬롯 폭을 고정해 `resize()`도 무시된다. 스왑 후 그 슬롯만 `layoutSizingHorizontal='HUG'` 1회 재지정이 레시피(56→180 실측).
- **`setBoundVariableForPaint` 반환 paint에 opacity를 미리 넣으면 안 먹는다** — fills에 대입한 뒤 read-modify-write로 재적용해야 렌더에 반영된다. 두 번 재발했다(스크림이 새까맣게, 틴트가 새파랗게).
- **`createAutoLayout`/`createFrame` 기본 흰 fill이 하드코딩으로 남는다** — 라이트에선 안 보이다가 다크에서 흰 밴드로 드러난다. 컨테이너 만들면 `fills = []`를 습관화.
- **7/28 첨부 산출물 4개 소실**(원인 미상) — plan에 노드 id를 적어두는 관행이 소실 확인의 유일한 근거가 됐다.
- **병렬 세션이 캔버스 좌표를 옮긴다** — 컷이 x=15300에서 20446으로 이동해 내가 붙인 노트 3개가 컷과 분리됐다. 재배치로 복구.

### 회고 및 인사이트

#### AI 도구 활용

- **스펙 최신본 확인이 작업 순서의 0번이다.** 오늘 작업의 절반이 v1.0.6 정합이었는데, 이걸 모르고 시작했으면 낡은 기준으로 컷을 그렸을 것이다. 레포 미러(`docs/product-spec`)는 두 버전 낡아 있었고 Notion이 정본이었다.
- **정지컷의 한계를 인정하고 매체를 바꾸는 판단.** "파일 넣을 때 변하는 느낌"은 피그마로 검증이 안 돼서 Storybook 탐색으로 옮겼고, 모바일에서 보실 수 있게 Chromatic으로 게시했다. 모션은 Storybook이 본판이라는 기존 규칙이 그대로 작동했다.

#### 설계 결정

- **판정과 표현을 가른다** — 드롭 판정(채팅 영역, LNB 제외)과 시각 강조(입력창)를 분리. 드래그 오버는 "입력창의 상태"이지 다른 화면이 아니다. 입력창을 불투명 카드로 갈아끼웠더니 "내 입력창이 사라졌다"로 읽혔던 게 근거.
- **레퍼런스는 형태가 아니라 느낌만 가져온다** — 핀터레스트·reactbits 레퍼런스를 형태 그대로 옮겼다가 두 번 물렸다("느낌 참조하라고 준 거야", "카드 부채꼴이 필수는 아냐").
- **스펙과 디자인이 충돌하면** 디자인 정본은 주미님 판단으로 가되, 충돌 지점을 보드·컷·plan에 전부 플래그하고 PM 정합 항목으로 넘긴다.

### 남은 것

- [ ] **PM 정합 2건** — 즉시 차단 vs 에러 칩 / 드롭 판정 범위 채팅 영역 축소 (둘 다 v1.0.6과 충돌)
- [ ] DS 라이브러리 퍼블리시 — Plugin API에 기능이 없어(import/읽기만) 피그마 UI에서만 가능
- [ ] 스윕 데스크톱 실드래그 최종 확인 (기본값은 채택으로 넣어둠)
- [ ] `plan-19` 6차 마지막 문단(⑤ 잔여 정리)이 merge 충돌 파일에 걸려 미커밋 — merge 해결 후 재반영
- [ ] 미리보기 팝오버 패키지 포팅 / AttachmentChip 나머지 (plan-20 Chip 백로그 묶음)

### 커밋 (prism · feat/dark-band-sheet-body)

| 커밋 | 설명 |
|------|------|
| `a675e11` | docs/storybook: AttachmentDnD 스토리 + plan-19 갱신 (병렬 세션이 정리) |
| `d7bbdab` | docs(plan-19): 첨부 정책 피벗·슬롯 순서 해소 기록 |
| `211bbbf` | feat(storybook)/docs: DnD 커서 고스트 + 판정=채팅영역(LNB 제외) |
| `3c3fbbb` | feat(prism): AttachmentChip focused 포팅 + DnD 스윕 채택 ※merge 충돌로 미푸시 |

### 브랜치 상태 — 손대지 않고 남김

세션 끝 무렵 다른 세션이 `origin/main`(0.4.0 #43)을 이 브랜치로 merge하다 **충돌 11개를 남기고 멈춘 상태**를 발견했다. `.git/MERGE_HEAD`가 살아 있다.

충돌 4개(`AttachmentChip.tsx`·계약·스토리 2종)는 전부 HEAD 쪽이 최신이고, 나머지 7개는 다른 세션 작업분이라 **아무것도 건드리지 않았다.** `plan-19`는 양쪽이 같은 자리에 다른 기록을 써서 한쪽만 취하면 한쪽이 날아가고, manifest·CHANGELOG는 릴리스가 건드린 파일이라 잘못 고르면 버전 상태가 깨진다. 오늘 이미 `checkout`으로 남의 미커밋 토큰을 날린 사고와 `add -A`로 남의 파일을 딸려 커밋한 사고가 있었던 터라 더 보수적으로 갔다. 내 작업은 커밋에 다 들어가 있어 merge가 풀리면 따라온다.
