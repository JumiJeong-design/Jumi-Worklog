# 2026-08-02

피그마 DS `Overlay` 섹션의 행 컴포넌트들이 State 축 없이 단일 컴포넌트로만 있었다. 눌림·비활성·파괴적 상태를 시트/메뉴 조합에서 확인할 방법이 없어서 State 축을 신설하고, 덤으로 문서 룰을 어기고 있던 `Menu Item`과 매트릭스가 비어 있던 마크다운 링크 칩까지 정합화했다.

요청:

```text
[Figma 1350-834] 이거 state별 디자인좀 추가해줘 베리어블
[Figma 1684-1972] 여기에 있는 애들 다 해줘
다 한다음에 [Figma 3490-1490] 얘도 해줘
아~ 메뉴 아이템이 오바스럽게 있었네 걔도 문서 룰대로 맞춰줘
아냐 그거(패키지 계약 동기화) 해야한다고 기록만 해줘
```

---

## 한 일 — Overlay 행 컴포넌트 State 축 신설

DS 파일 `DcYgJjGAfObOIM4IyrQjgj` > `✅ Components` > `Overlay`(`1684:1972`).

read-only 감사 → 변경안 보고 → 승인 3건(축 기준 = `engaged` 통합 / 대상 = 4개 전부 / raw neutral → semantic 동시 교체) 뒤 write했다.

| 컴포넌트 | 세트 | State |
| --- | --- | --- |
| BottomSheet/MenuItem | `3805:2059` | enabled / engaged / selected / disabled |
| BottomSheet/ActionRow | `3805:2002` | enabled / engaged / disabled / destructive |
| ContextMenu/Item | `3805:2021` | enabled / engaged / disabled / destructive |
| Row/Item | `3805:2034` | enabled / engaged / disabled |

- **축 기준은 `design-system/rules.md` #5** — 웹이라 모바일·PC를 함께 대응해야 하므로 hover와 pressed를 나누지 않고 `engaged` 하나로 통합. 값 이름은 rules #5의 `enabled`, 표기는 파일 관례인 `State=`.
- **`engaged` 배경은 알파 시맨틱 토큰 `semantic/pressed/surface`** (rules #23). 불투명 `surface/Inset`이 아니라서 어떤 배경 위에서도, 다크모드에서도 성립한다.
- **새로 만든 변수 0개.** 전부 기존 로컬 변수 바인딩이다. raw `neutral/black`·`neutral/900`·`neutral/600` → `semantic/text-primary`·`semantic/text-secondary`로 교체(부제가 `#30323b` → `#53555c`로 밝아지는 시각 변경이 `enabled`에도 적용된다).

### 곁다리로 정리한 것

- **`Menu Item`(`3352:1374`)** — 6종(`default`/`hover`/`pressed`/…)으로 rules #5를 정면으로 어기고 있었다. `default`→`enabled`, `hover` 삭제, `pressed`→`engaged`로 5종화. 삭제 전 인스턴스 6개의 State를 조회해 `hover`/`pressed` 사용이 0건임을 확인하고 지웠고, 사후 재조회에서 전부 정상 승계됐다.
- **`Type=selection`(`192:38`)** — 선택 시트인데 행 3개가 전부 체크였다. CheckIcon을 `selected` 전용으로 만든 김에 첫 행만 `selected`, 나머지는 `enabled`로 정정.
- **마크다운 링크 칩(`3490:1485`)** — `size=M, state=focus`가 비어 매트릭스가 불완전했다(rules #7 위반). 빠진 조합을 채워 2×2 완성, 속성명을 `Size`/`State`로, 값을 `enabled`/`engaged`로 정규화. `focus`는 포커스 링이 아니라 배경이 진해지는 눌림 처리라 `engaged`가 맞았다.
- **`Markdown Block / Link Preview1`(`3806:1995`)** — State 2종(`enabled`/`engaged`) 추가.

### 함정

- **`combineAsVariants`는 currentPage 컨텍스트를 탄다** — `Grouped nodes must be in the same page as the parent`. 대상이 `✅ Components`(0:1)인데 currentPage가 `thumbnail`이었다. 스크립트가 원자적이라 파일은 무변경이었고, `setCurrentPageAsync`를 앞에 붙여 해결. 이후 모든 write 스크립트에 선행 호출을 넣었다.
- **`loadAllPagesAsync` 미지원** — 인스턴스 사용처를 전 파일에서 찾으려다 막혔다. 페이지별 `findAllWithCriteria`로 대체(`✅ Components` 1062개 조회, 링크 칩 인스턴스 0건 확인).
- **Dev Mode MCP 서버가 꺼져 있었다** — `mcp__Figma__*`가 전부 안내문만 반환. 플러그인 쪽 `mcp__plugin_figma_figma__*`로 우회했다.
- **세트 위치가 저 혼자 밀렸다** — `BottomSheet/ActionRow`를 x=715로 뒀는데 나중에 보니 813. 원인 미상이고 겹침은 없었지만 정렬이 깨져 사후 보정했다. **섹션 안 세트는 생성 후 bbox를 다시 검사하는 게 맞다.**
- **감사에서 걸러낸 것** — `ContextMenu/Item`·`Row/Item`이 고아였다. `Context Menu`(194:38)와 `Dropdown Menu`(194:50)는 이미 `Menu Item` 인스턴스로 갈아탄 뒤였다. 지시가 "다 해줘"였으니 State는 붙였지만, 이 사실을 먼저 보고하지 않았으면 안 쓰는 컴포넌트에 조용히 공수를 넣을 뻔했다.

### 검증

- Overlay 자식 9개 전수 bbox 교차 검사 → 겹침 0건, 섹션 경계(1400×2697) 내 수용(maxRight 1300 / maxBottom 2517)
- 각 세트 생성 직후 스크린샷으로 상태별 렌더 확인
- `scripts/validate-design-system.sh` 통과

---

## 남은 것 — 8/3에 여기서 이어간다

- [ ] **패키지 계약 동기화 (최우선, 오늘 지시로 미착수)** — 지금 피그마와 패키지 계약이 어긋나 있다. 착수 전 범위를 다시 승인받는다.
  - 대상: `packages/prism/component-contracts/bottom-sheet.md`(행 State), `menu.md`·`menu-item.md`(Menu Item 5종), `markdown.md`(링크 칩 Size×State, Link Preview State)
  - 적을 것: Variant 표에 State 축 + "hover/pressed는 `engaged` 하나 — 웹이라 모바일·PC 동시 대응"(rules #5) 근거. 토큰 계약에 `engaged`=`semantic/pressed/surface`(알파, rules #23), disabled=`semantic/disabled/fg`, destructive=`semantic/text-error`, 텍스트=`semantic/text-primary`/`text-secondary`
  - `bottom-sheet.md`의 "피그마 main set에 없는 Variant 축을 먼저 만들지 않는다"는 이제 충족됐다(피그마에 State 축이 생겼으므로 반영 허용)
  - **계약 문서만 갱신할지, `MenuItem`/`BottomSheetMenuItem` 공개 props까지 손댈지는 별개다.** 공개 속성 변경은 브레이킹 체인지 후보라 범위 확정이 먼저
- [ ] **`prism` 레포 미커밋** — `docs/plans/10-design-system-followup-2026-06.md`에 `### 2026-08-02 Overlay 행 컴포넌트 State 축 추가` 섹션을 추가한 상태로 커밋하지 않았다. 브랜치는 `claude/lime-color-palette-6xx9wk`
- [ ] **`ContextMenu/Item`·`Row/Item` deprecate 판단** — `Menu Item`과 역할이 겹친다. State는 붙였고 컴포넌트 description에 미사용 사실을 적어 뒀다
- [ ] **부제 색 변경 실물 확인** — `neutral/600`(`#30323b`) → `semantic/text-secondary`(`#53555c`). 승인된 변경이지만 `enabled` 기본 상태에도 적용된다
- [ ] **`Link Preview1`의 꼬리 `1`** — 오타성 잔재로 보이나 공개 컴포넌트 이름이라 손대지 않았다
- [ ] **마크다운 계열 raw neutral 잔존** — 링크 칩은 흰색→100→150 3단 명도 위계 자체가 디자인이라 semantic 텍스트 토큰과 1:1 대응이 안 된다. Overlay 4개와 달리 자동 교체하지 않았다
- [ ] **라임 팔레트 이월** — 라이브러리 재퍼블리시 필요(라임 변수 값이 바뀌어 Product 파일 반영 안 됨), 스택바 범례 매칭, 기존 케이스 라벨 오류. 상세는 plan 문서 `2026-08-02 라임 재료 팔레트 추가` 섹션

---

## 회고

#### 설계 결정

- **`engaged` 통합은 문서가 이미 정해 둔 답이었다.** 피그마 실물(`Menu Item` 6종)이 rules #5를 어기고 있어서 "실물이 1차 기준"과 "문서 규칙"이 충돌했는데, 결론은 실물을 규칙에 맞추는 쪽이었다. 피그마 우선 원칙은 *디자인 의도*에 대한 것이지, 규칙 위반까지 승계하라는 뜻이 아니다.
- **`engaged` 색은 불투명이 아니라 알파로.** `Menu Item`의 hover가 쓰던 `surface/Inset`은 불투명이라 배경이 다른 곳에 얹으면 깨진다. `semantic/pressed/surface`(알파)로 통일해 시트·메뉴·칩 어디에 얹어도, 다크에서도 성립하게 했다.

#### AI 도구 활용

- **한 개 먼저 만들고 스크린샷으로 검증한 뒤 나머지를 돌린 게 맞았다.** `BottomSheet/ActionRow` 하나로 바인딩 헬퍼와 레이아웃 계산을 검증하고 나머지 3개는 같은 틀로 찍어냈다. 4개를 한 번에 돌렸으면 `combineAsVariants` 페이지 오류를 4배로 만났을 것이다.
- **`use_figma` 스크립트가 원자적이라는 점이 안전망이다.** 실패해도 파일은 무변경이라, 에러 메시지를 읽고 고쳐 재시도하는 루프가 파괴적이지 않다.
- **파괴적 작업 전에 역참조를 먼저 세었다.** `hover` variant를 지우기 전에 인스턴스 6개의 State를 조회해 0건임을 확인했다. 셌으니 지운 것이고, 안 셌으면 인스턴스가 깨졌을 것이다.

### 커밋

없음. 오늘 작업은 피그마 write와 `prism` 레포 문서 수정(미커밋)이다. `claude/lime-color-palette-6xx9wk` 브랜치의 오늘자 커밋(`53d68fa` 등)은 앞선 라임 팔레트 세션의 것이다.
