# 2026-08-12

### 미해결 항목

**오늘 트랙에서 남은 것**

- [x] ~~내 커밋 2개가 로컬에만 있다~~ — **주미님 확인 후 `feat/edge-scrim-exploration` 원격 복원(push 완료).** `f23f86b`(scrim 정합)·`7bec52f`(드로어 스토리) 둘 다 `origin/feat/edge-scrim-exploration` 도달 확인. 원격이 `[gone]`이었던 건 옆 세션이 PR #151로 cherry-pick 분리하며 삭제했기 때문이고, 되살린 브랜치에는 **#151 원본 3커밋이 함께 들어 있다** — 이 브랜치로 PR을 낼 때 rebase로 거른다(8/11 합의 그대로)
- [ ] **드로어 트랙 PR 미개설** — 브랜치만 올렸다. PR을 내려면 Chromatic 베이스라인 승인이 따라오므로 #151 처분과 함께 판단
- [x] ~~socraAI 문서 2건 미커밋~~ — **plan-10은 hunk 필터로 갈라 `fa7c20d` 커밋·push 완료**(드로어 절 85줄, 옆 세션 토스트·팝오버 hunk 2개는 워킹트리 보존). 처음엔 "줄 단위로 섞여 분리 불가"로 판단했는데 재측정하니 **내 절이 독립 hunk였다** — 8/11 시점에 섞여 보였던 건 옆 세션 편집이 진행 중이었기 때문이고, 미커밋 판정도 반나절이면 낡는다
- [x] ~~`chat-history-panel.md` 1건만 미커밋~~ — **`5b02805`로 커밋 완료.** 섞임 판정 자체는 맞았지만(hunk 1·4에 로그인 트랙과 드로어 문장이 한 덩어리) **분리할 이유가 없어졌다** — 로그인 트랙 문서도 같은 브랜치에 올라가야 할 미커밋분이라 함께 올리면 두 항목이 동시에 닫힌다. **"분리 불가"는 두 조각의 목적지가 다를 때만 문제다**
- [ ] **radius 램프 부재** — 드로어 우측 radius 16이 raw 값이다. DS radius 변수는 `radius/full` 하나뿐
- [ ] **코치마크 Dim 3개 표준화** — semantic scrim 바인딩 위에 paint opacity 0.3이 곱해져 실효 12%. 스포트라이트 연출로 보여 보존했고, 코치마크 채택 시 함께 정리

**chrome 더미 제거 트랙 (plan-28)**

- [x] ~~socraAI 미커밋~~ — **hunk 필터로 갈라 `d7267e5` 커밋·push 완료**(11파일). 옆 세션 hunk는 워킹트리에 그대로 두고 인덱스에만 내 hunk를 넣었다(`git apply --cached`, theme.css 12 hunk 중 4). 커밋 후 워킹트리 28건 보존 확인
- [ ] **`toast.md`·`chat-history-panel.md` 2건만 미커밋** — 내 편집이 **옆 세션이 새로 추가한 블록 안쪽**이라(추가 hunk 하나에 두 사람 줄이 섞임) 분리가 원리적으로 불가능하다. 옆 세션이 자기 8/11 절과 함께 올릴 때 따라 올라간다. 내용 = 토스트 헤더 앵커 127→68 + 컴포저 기준 노드 주석, 드로어 절 chrome 문구 대체
  - ⚠️ `toast.md`의 8/11 「주미님 확인」·「적용 기록」 절은 여전히 **127로 적혀 있다** — 8/11 시점 기록이라 그대로 뒀지만, 커밋될 때 새로 넣은 좌표계 주석과 함께 읽히는지 확인 필요
- [ ] **[결정 필요] `Navigation Bar` 마스터에 `StatusBar=on/off` 축을 만들지** — 지금은 인스턴스 override라 **새로 조립하는 화면마다 다시 꺼야 한다.** 축을 만들면 기본값 off로 끝나지만 DS 파일 write라 전파 범위가 커서 승인 게이트 대상
- [ ] 상단 상태바 색은 웹에서 `theme-color` 메타 한 줄 — 개발에 전달 필요
- [ ] 짧은 뷰포트 QA 프레임(393×664 — Safari URL바 펼침 + 키보드) 1~2장으로 컴포저 걸린 화면 스트레스 테스트
- [ ] `➡️ chat/chart`의 `deep` 프레임 2장(`6533:63162`·`6533:63386`)은 `Navigation Bar`가 마지막 자식이라 **화면 맨 아래**에 있다 — 이번 변경과 무관한 기존 레이어 순서 오류

## 8/11 이월 (전수 — 오늘 종결분 제외)

8/11 로그의 미완료 항목 50건을 트랙별로 옮긴다. 오늘 세션에서 상태가 바뀐 것은 각 줄에 실측 결과를 붙였고, **나머지는 8/11 시점 기록 그대로**다(오늘 재검증하지 않음).

**[주미님 손] 승인·결정 대기**

- [ ] **PR #151 Chromatic 베이스라인 승인 1건** — 8/12 새벽 CI 재실행 확정: `check`·`validate`·`storybook`·`Storybook Publish`(295 stories)·`UI Review`(no changes) 전부 pass. `UI Tests`만 신규 `GeneratingRow` 스토리 베이스라인 대기(`mergeState=UNSTABLE`) · 머지 미실행
- [ ] **PR #147 Chromatic 베이스라인 승인 1건** — 신규 `Placement` 스토리 새 스냅샷, 승인 전까지 `UNSTABLE` · 머지 미실행
- [x] ~~PR #148 머지~~ — **`415740b`로 squash 머지 완료.** `mergeable=CONFLICTING`(plan-10 한 파일)이라 격리 워크트리에서 main을 머지해 해소한 뒤 push→squash했다. 충돌은 양쪽이 파일 끝에 서로 다른 절을 덧붙인 형태(PR=바텀시트 chrome 통합 / main=가장자리 처리 탐색·PM 확인 보드)라 **둘 다 보존**, main 대비 순수 추가 50줄·삭제 0 실측. 원격 브랜치 삭제, 로컬 브랜치는 피어 워크트리(`/tmp/socra-sheet-header-wt`)가 쓰고 있어 보존
- [ ] **DS 라이브러리 게시(재게시)** — 8/12에 1회 게시됨(아래 「전건 종결」 절의 헤더 28개 실측이 게시 후 기준). 남은 재게시 1건: `Sheet/Header` 마스터 높이 FIXED→HUG(슬롯이 옛 높이 76을 붙드는 문제, 함정 절 참조). History footer `layout=rail` 축이 이번 게시에 포함됐는지는 미확인
- [ ] DS 승격 ① `Chat Input Bar` `State=locked` 신설 — 제안서 plan-25, variant 추가라 기존 인스턴스 픽셀 변화 0
- [ ] DS 승격 ② `State=disabled`의 중첩 `Mode Toggle` 비활성화 — **픽셀 변화 있는 시각 변경**이라 게시 전 대상 조회 선행
- [ ] `Navigation Bar` 마스터에 `StatusBar=on/off` 축 신설 여부(plan-28 트랙 — 위 참조)
- [ ] empty state 일러스트 — 검토 보드(`4347:2073`)에 넣어 전체 정합 확인, 채택 시 마스터 반영은 별도 승인
- [ ] History/Footer `PlanLabel` "Free" 처분 — 비로그인 variant 재작업 때 함께(비블로커)
- [ ] 코치마크 3종 채택 여부 — 기획서 미반영, 채택 시 기획 반영 요청 필요
- [ ] PM 노트 전달(`7833:97335`) — §5.3 S9-3 정의 한 줄 수정 요청 포함
- [ ] PM 확인 — SA-1a(게이트 닫은 뒤) 화면이 기획서 미규정(§3.2는 모달만 정함). 이번은 디자인 제안, 정본 plan-25
- [ ] 기획 요청 발신 — ① 빠르게 시작하기 칩 9종 변환 문장·유도 질문 데이터셋 ② 딥링크 파라미터↔홈 요소 매핑(§4.1 Open Issues)

**바텀시트 chrome — 8/12 전건 종결(코드 포팅 제외)**

- [x] `7460:13088`(2-b 전문 시트) 재지정 — `chrome=sheet` + 헤더 `뒤로가기=true`. 게시 후 옛 변형에 남아 있던 유일한 인스턴스였다
- [x] 헤더 타이틀 문구 확정 — 전문 시트 계열 전부 `title=on` **"AI 프로필"**. 진입 경로(직접/리스트 경유)와 무관하게 같은 이름을 주고 뒤로가기만 켜고 끈다. 처음엔 에이전트 이름("AI 노무사")을 넣었다가 아래 프로필 카드와 중복이라 되돌렸다 — **헤더는 화면 종류 이름, 카드는 그 화면의 내용**
- [x] 손그림 셸 치환 — **19개가 아니라 11개였다**(`전문 시트 · 펼침` 4·`bottom sheet` 2·`Login Sheet` 2는 컴포넌트 인스턴스를 감싼 래퍼 프레임). `sheet(로그인)` 2 + `sheet(에이전트)` 9 전건 전환, 거터 20→16
- [x] ~~시트 딤 50개를 `overlay/scrim-soft`로 재바인딩~~ → **2단 분리를 철회하고 30 단일로 재확정**(8/11 밤). 딤 50개를 `overlay/scrim`(30)에 묶고 `scrim-soft`는 삭제
- [x] ~~다크 `scrim-soft` 값 확정~~ → 토큰 자체가 사라져 무효. 다크 `overlay/scrim`(현재 30) 검토만 남음
- [x] 헤더 층위 분리 — `Sheet/Header`(크롬)와 `Sheet/Intro`(콘텐츠) 신설·재구성. 아래 본문 절
- [ ] 라이트박스 60% 8개는 컴포넌트 내부라 DS 쪽에서 처리
- [x] **코드 포팅 완료 — PR #153**(`feat/sheet-header-port-2026-08-12`). `SheetHeader`·`SheetIntro` 신설, `SheetShell`은 `header` 슬롯(**BREAKING**: `backLabel`·`onBack`·`onClose` 제거), `BottomSheet` 헤더 교체 + 행 거터 `px-5`→`px-4`(공개 API 무변). 계약 2건 신설·storybook-map 등록, 소비처 스토리 4곳·테스트 2건 동반 수정, changeset minor. 게이트 전 항목 통과(token:build·typecheck·test 112·build·pack:check·storybook:build·문서 검증). **포팅 기준은 `design-system/components/sheet-header.md`(층위 분리판)를 따랐고**, 낡았던 `component-contracts/sheet-shell.md`의 "헤더 `type=back`" 문장도 이번에 갱신했다. ⚠️ **scrim 중복**: 이 PR도 `overlay/scrim`을 0.3으로 내린다(main 기준값 0.48/0.64). 옆 세션 `d16d65c`와 값이 같으므로 충돌해도 해소는 자명하다
- [x] ~~코드 scrim이 피그마와 어긋났다~~ — 주미님 확인("내가 0.3 하자고 함") 후 **`d16d65c`로 0.3/0.3 재정합**(`feat/edge-scrim-exploration` push 완료, plan-10 후속 기록 `b1296ff`). `scrim-media`(라이트박스 60%) 불변, changeset patch. theme.css는 공유 워킹트리라 token:build 후 scrim 4줄만 부분 스테이징(전후 diff로 scrim 외 변화 0 실측). **main 미도달** — 드로어 PR에 함께 실림
- [ ] 데스크톱 패널 닫기 32→44 유지 여부(`Sheet/Header`에 size 축을 얹을지)
- [ ] 시트 바깥 껍데기(radius 24·elevated·핸들·safe-area) 중복 통합 여부 — 합치려면 메뉴 리스트를 본문 슬롯으로 떼고 첨부 피커 4개 재작성
- [ ] 웹 `Login Modal`(`7707:107760`) 종료가 혼자 좌상단 24 — 문법 반대·탭 타깃 미달, 웹 묶어서 볼 때 처리
- [ ] 전문 시트 스택(시트 위 시트) 미채택 — 전환 모션으로 층 이동을 전달하는 안은 Storybook 건으로 분리
- [x] ~~프레임 1개 소실 확인 필요~~ → **의도적 통합으로 판단, 복구 불필요**(주미님 확인). `2 · 전문 시트 · 접힘(기본)`(`6945:13862`, Shell `7460:13014`)이 사라져 14개 중 13개만 반영된 건 맞다. 다만 **화면만이 아니라 라벨(`2 · …`)도 같이 없어졌고**, 지금 그 자리 라벨은 `2-A · 접힘(뒤로가기)`다. 라벨과 화면이 함께 정리된 건 실수가 아니라 통합의 흔적이고, 사본 이름의 "사본 ←"도 그 과정으로 보인다. 되살리면 구멍을 메우는 게 아니라 중복 컷이 는다

**미커밋·브랜치 정리**

- [x] ~~문서 6개 커밋(첨부 팝오버 트랙)~~ — **`0587e9e`로 5건 커밋·push**(`layout.md`·`popover.md` DS/계약·`token-contract.md`·plan-13). 전건 단일 트랙이라 hunk 필터 불필요했고, plan-10에 남아 있던 대체 표 320 줄 1개만 갈라 `cdd6bc7`로 추가(plan-10 토스트 hunk는 피어 몫이라 보존). plan-10 드로어 절은 8/12에 이미 `fa7c20d`
- [x] ~~socraAI 문서 3건(로그인 심야분)~~ — **`5b02805`로 3건 커밋·push**(plan-26 신설 · `user-profile.md` · `chat-history-panel.md`). 커밋처 복원 뒤라 그대로 올라갔다
- [ ] 원본 3커밋(+`b409d03`)이 `feat/edge-scrim-exploration`에 잔존 — **8/12 원격 복원으로 유실 위험은 해소**. #151 머지 후 이 브랜치 PR 시 rebase로 거른다
- [x] ~~옆 세션 `be2fdd1`(라이트박스 안전영역)이 얹혀 있어 이 브랜치를 push하면 안 된다~~ — **8/12에 주미님 확인 후 push했다.** 전제 조건은 충족돼 있었다(실측: `feat/attachment-lightbox-safe-area`가 **origin에 이미 존재**해 옆 작업이 자기 브랜치로 보존됨). 다만 `be2fdd1`은 여전히 이 브랜치에도 얹혀 있으므로 **PR을 열 때 rebase로 걸러야 한다** — 위 항목과 같은 처리

**기획·개발 확인 대기**

- [ ] processing 상태 개발 확인 — 업로드 중 썸네일(30% 스크림 + 흰 링) 유지 여부. 프리뷰를 서버 응답 뒤에만 그리면 processing도 사진이 없어야 하고, 로컬 File로 그리면 현행 유지. "5MB 초과"처럼 파일은 읽히는데 한도만 넘는 케이스 표현도 이 답에 달렸다
- [ ] KR 문구 확정 → `strings` 등재 — SA-1a 4건(`auth/gate-locked-*`)이 하드코딩. JP/EN 초안은 plan-25 표에 있고, `strings`는 DS 원격이라 **변수 신설 = DS write + 게시**
- [ ] 등급 판정 로직 승격·범례 % 노출 (차주 기획 대기 — 착수 금지)
- [ ] ReportCard shell 실측 — 차주 로딩·투표 기획 확정 후
- [ ] 브레이크포인트 구간 경계 확정 — 개발 확정 시 숫자만 갱신
- [ ] 헤드라인 18px — 램프에 없는 off-scale, 타이포 트랙 결정과 함께

**시각·시안 후속**

- [ ] `DS Bottom Bar`의 `Dark` variant 축 → 피그마 모드 전환 — 피그마는 다크를 variant로, 코드는 모드로 뒤집어 색 램프가 두 벌이다. **영향 4개 인스턴스뿐**(`chat/chart` 103개 전부 `Dark=false`, `home/history` 7개 중 4개). 순서 = 마스터 `semantic/surface/fade-*` 재바인딩 → `Dark` 축 제거 → 홈 다크 3장 모드 명시 → 육안
- [ ] 패널 열림 화면 `EdgeBand` 부재 — 스트림 밴드 748 + Side Panel 자기 밴드 필요, 시각 변경이라 승인 대기
- [ ] Side Panel 400 요약 실물 데모 없음 — 옛 `7449:11658`이 파일에서 사라져 재작성 필요
- [ ] History Item `State=loading` — 게시는 완료(세트 CURRENT), 남은 건 제품 `history — 행 상태` 시안의 스피너 오버레이를 정식 인스턴스로 교체(1분 거리)
- [ ] 피그마 `Attachment/Lightbox` 마스터 4종 안전영역 반영 — 병렬 세션 몫, 반영되면 `attachment-lightbox.md`의 "피그마 미반영" 줄 삭제
- [ ] 제품 파일 라이브러리 업데이트 수락 — DS 라이트박스 마스터는 `CURRENT`인데 제품 스냅샷이 `pad 0/0`이라 웹 세로 캡션 16px 겹침 잔존. 수락 후 재측정
- [ ] 스토리북 세로 케이스 육안 확인 — 안전영역 변경은 테스트·typecheck만 통과, 렌더 미확인
- [ ] 세로 원본 사진 없음 — 제품 파일 이미지가 가로 1장뿐이라 세로 2장은 중앙 크롭. 필요하면 올린 뒤 슬롯 paint만 교체
- [ ] 스토리북 첨부 실패 인터랙션 — 남은 건 **processing 전이 데모뿐**이고 "processing 중 전송 처리"가 미결이라 **기획 확정 전엔 후순위**

**토큰·API 판단**

- [ ] `--layout-overlay-popover`(240) 처분 — 소비처 0. 값 변경·삭제는 공개 manifest 변경이라 보류했고 비고만 정정. **Figma `Popover` 마스터(`4276:1991`)와 `Popover.tsx` 기본값을 320으로 볼지가 남은 판단**
- [ ] `ToastViewport`가 안 다루는 것 3종 — 다중 토스트 스택 규칙, 진입/퇴장 모션, `duration` 자동 소멸. 현재는 배치만 소유
- [ ] 로그인 비블로킹 4건 — ① 접힘 레일 헤더 토글(원형 r24) vs 푸터 아이콘(사각 r12) 형태 불일치 ② `User Profile` radius 크기별 불일치(sm/md/lg=16, xl=28) ③ 푸터 `PlanLabel 'Free'` 처분 ④ 패키지 `UserProfileType`의 `"empty"` 제거(브레이킹 후보, 별도 PR)
- [ ] 아이콘 stroke 감사 — 8/11에 전건 종결됐고 **FileCard 3건분이 다음 재게시에 포함**되는 것만 남음

**외부/인프라 블로킹**

- [ ] 제품 파일 REST 토큰 만료(403) 재발급 — 팝오버 전수 감사가 여기 막혀 있음
- [ ] Chromatic 월 스냅샷 쿼터 소진 — 8/10 머지분이 전부 UI Review pending. 해제 시 main 전체 비교 1회 + `Composer` 페이드 육안. **주미님: 당장은 눈으로 QA하니 비블로커.** 육안 우선순위는 `ModelProfile` 소비 5곳(MessageBubble·ProfileGroup·SheetProfile·Comment·ReportParticipants)

---

### 함정 모음

- **화면 프레임이 가로 오토레이아웃이면 삽입한 배경이 화면 밖으로 밀린다** — history 화면 12장이 전부 `layoutMode: HORIZONTAL`이라 `insertChild`로 넣은 배경·스크림·드로어가 플로우에 편입돼 x=393, x=786으로 나란히 배치됐다. 스크린샷을 찍기 전까지 "왜 홈만 보이지"로 보인다. **자식 삽입 후 `layoutPositioning = "ABSOLUTE"`를 반드시 세운다.**
- **피어 세션이 내 미커밋 편집분을 자기 커밋으로 쓸어간다** — 스토리 337 전환분(7곳 + 주석)이 옆 세션 커밋 `dfcac28`에 통째로 들어갔다. 내용 유실은 없지만 커밋 단위가 섞인다. 워킹트리를 공유하는 한 구조적으로 막을 수 없고, 격리가 필요하면 `git worktree`가 답이다.
- **생성 파일은 부분 스테이징으로 갈라야 한다** — `theme.css`에 내 scrim 4줄과 옆 세션의 토스트 gap·첨부 스크림 삭제가 함께 재생성돼 있었다. 통째로 커밋하면 남의 작업을 쓸어가고, 빼면 토큰 소스와 생성물이 어긋난다. `git diff`에서 내 hunk만 필터해 `git apply --cached`로 스테이징했다(12 hunk → 4). 워킹트리는 건드리지 않아 옆 세션 렌더도 안 깨진다.
- **"미결"이라고 쓰기 전에 어제 결정부터 확인한다** — 삭제 팝업 Dim이 `black-alpha/30` 직바인딩인 걸 보고 "plan-22의 단일 scrim 결정과 대비되니 표준화 필요"로 미결에 올렸는데, **8/11에 이미 딤 2단(시트 `scrim-soft` 30 / 모달·차단형 `scrim` 40·60)으로 확정**돼 있었다(CONTEXT 현재 상태). 내가 삭제 팝업(차단형 모달)을 40%로 올린 건 그 규칙에 맞지만, 근거를 "미결"로 적었던 건 오기다. 이월 문서가 반나절 만에 낡는다는 기존 캐비엇의 재발.
- **문서에 적힌 피그마 노드 ID는 파일 개편에서 조용히 죽는다** — `chat-history-panel.md`의 화면 인스턴스 ID(`2048:6500` 등)가 전부 not found였다. DS 파일 시절 것이고 실제 화면은 제품 파일로 이사한 지 오래. 감사 시작 전에 ID 생존부터 확인해야 헛짚지 않는다.
- **이월 항목에 붙은 조건문은 실행 직전에 다시 읽는다** — 8/11 로그에 "옆 세션 `be2fdd1`이 얹혀 있으니 `feat/edge-scrim-exploration`을 **push하면 안 된다**"가 있었는데, 8/12에 브랜치를 복원하면서 그걸 안 보고 push했다. 결과적으로는 무해했다(전제였던 "옆 작업 보존"이 이미 충족 — `feat/attachment-lightbox-safe-area`가 origin에 존재, 게다가 PR을 안 열어 딸려 간 곳도 없다). 하지만 **무해했던 건 운이지 판단이 아니다.** 워크로그의 금지 조건은 "언제 풀리는지"까지 적혀 있는 경우가 많으니, 실행 전에 그 조건의 충족 여부를 먼저 실측한다. 같은 날 "미커밋이라 분리 불가"도 반나절 만에 뒤집혔다 — **조건부 판정은 전부 유통기한이 있다.**
- **레퍼런스는 갈린다 — 갈림 자체가 정보다** — Claude·ChatGPT·X는 드로어인데 Gemini·Grok은 full이었다. "다수결"로 정하면 근거가 없고, 갈림을 설명하는 축(스위처 vs 목적지)을 찾으면 우리 선택의 기준이 된다.
- **flow에서 chrome을 걷으면 절대배치 오버레이만 제자리에 남는다** — 상태바(59)·홈 인디케이터(34)가 오토레이아웃 안에 있어서, 빼는 순간 **본문은 위로 59, 컴포저는 아래로 34** 움직이는데 absolute 오버레이는 안 따라간다. 토스트 8건이 헤더에서 59 떴고, 코치마크 스포트라이트는 실제 입력바와 34 어긋나 **입력바가 두 개로 보였다.** 제거 자체보다 이 후처리가 작업량의 대부분이었다. `cy=MAX`·`cy=CENTER`로 잡힌 것과 화면을 꽉 덮는 Dim·라이트박스는 자동으로 따라오므로, 손댈 대상은 `cy=MIN`이면서 특정 요소를 가리키는 것들이다.
- **"컴포저 상단" 앵커의 기준 노드는 `Bottom Bar`가 아니라 그 안의 `Chat Input Bar`다** — 바깥 프레임으로 재면 6px 어긋나 `toast.md` 확정값 16이 안 나온다. 문서의 "실측 18"이 재현이 안 돼 한참 헤맸는데, 보이는 카드 상단으로 재니 정확히 16이었다. 앵커를 문서에 적을 땐 **어느 노드 기준인지**까지 적어야 한다.
- **피그마에서만 지우면 패키지에 같은 더미가 그대로 남는다** — `BottomBar`가 `--bottom-bar-safe-area`로 빈 div를 그리는데 값이 `theme.css`에 **34px 상수**로 박혀 있었다(= 아이폰 홈 인디케이터 높이). 주미님이 `다 한거야그럼?`으로 되물어서 잡았다. 시안에서 걷어낸 더미는 **코드에도 쌍둥이가 있는지** 확인해야 완료다. `SheetShell`도 `h-[34px]`로 같은 문제였다.
- **jsdom은 `env()`를 못 읽어 인라인 `style`을 통째로 버린다** — `style={{ height: "env(safe-area-inset-bottom, 0px)" }}`로 넣었더니 DOM에 style 속성 자체가 안 남아 테스트가 떨어졌다(브라우저는 정상). Tailwind 임의값 클래스 `h-[env(safe-area-inset-bottom,0px)]`로 두면 클래스 문자열이 남아 검증도 되고, 패키지가 소스 `.tsx`를 배포하므로 소비자 Tailwind가 유틸리티를 생성한다 — 기존 `h-[34px]`와 같은 경로다.
- **원격이 `[gone]`인 브랜치 위에 남의 미커밋이 얹혀 있으면 그건 어디에도 없는 상태다** — #151 분리 과정에서 `feat/edge-scrim-exploration`의 원격이 삭제돼 로컬 브랜치가 `[gone]`이 됐고, 그 위에 피어 세션의 미커밋 25건 + 미추적 6건이 쌓여 있었다. 커밋은 나중에 원격 복원으로 살았지만 **미커밋분은 복원과 무관하게 계속 로컬 디스크에만 있다.** 대응: `GIT_INDEX_FILE`로 임시 인덱스를 떠서 워킹트리·인덱스를 건드리지 않고 통째 스냅샷 커밋을 만들고 백업 브랜치로 push한다(`git stash`는 워킹트리를 되돌려 남의 작업을 깨므로 공유 체크아웃에서 쓰면 안 된다).
---


**바텀시트 chrome 세션 추가 (8/12)**

- **슬롯에 들어가는 컴포넌트는 높이를 FIXED로 두면 안 된다.** `Sheet/Header`를 고정 높이로 만들었더니, Shell 마스터의 헤더 슬롯이 옛 높이(76)를 붙들고 마스터를 60으로 바꿔도 안 따라왔다. 게다가 **중첩 인스턴스라 제품 파일에서 손댈 수도 없다**(인스턴스 자식 지오메트리 override 불가). 마스터를 HUG로 바꾸고 재게시해야 풀린다 — 게시가 한 번 더 든다.
- **인스턴스 padding override가 마스터 수정을 이긴다.** `BottomSheet/MenuItem`·`ActionRow` 마스터를 거터 16으로 고쳤는데 제품은 계속 20이었다. 원인은 `Bottom Sheet` 마스터 **안에 배치된 행 인스턴스 5곳이 padding 20을 override로 들고 있던 것**. 마스터만 고치고 끝냈다고 보면 안 된다.
- **이름 정규식으로 "손그림"을 세면 래퍼 프레임이 딸려 온다.** 손그림 셸을 19개로 집계했는데 실제로는 11개였다 — `전문 시트 · 펼침`·`bottom sheet`는 안에 컴포넌트 인스턴스를 품은 껍데기였다. **"이름이 시트인 프레임"이 아니라 "직접 그린 chrome을 가진 프레임"으로 세야 한다.**
- **오버레이 분류 기준은 상위 프레임이 아니라 형제 노드다.** 딤 census에서 "시트 50개가 30%"라고 보고했는데 그중 32개가 모달이었다. 플로우 보드 한 프레임에 시트 화면과 모달 화면이 같이 들어 있어 상위 프레임 기준이 전부 "시트"로 잡혔다. 이 오분류 때문에 2단 분리를 했다가 철회했다.
- **계약 PR과 코드 PR을 나누면 코드 쪽을 문서 쪽 위에 쌓는다.** 코드 브랜치를 origin/main에서 땄더니 문서 게이트가 "참조한 근거 문서가 없습니다"로 막았다 — 근거 문서가 아직 별도 PR 브랜치에만 있었기 때문이다. 게이트 에러가 브랜치 베이스를 대신 짚어 준 셈. 문서를 먼저 머지하거나 코드를 그 위에 리베이스한다.
- **플로우 보드의 "구멍"은 세는 방식이 만든 착시였다.** 사라진 프레임을 보고 "라벨만 있고 화면이 없다"고 보고했는데, 실제로는 짝이 다 맞았다. 원인은 컷 목록을 `^\d+(-[a-z])?\s*·` 정규식으로 뽑은 것 — 1번 자리의 화면 이름이 `profile list`라 안 걸렸다. **라벨은 번호로 시작하지만 화면 이름은 그렇지 않다.** 좌표 구간으로 훑었으면 바로 보였다. 같은 날 "손그림 19개(실제 11개)", "시트 딤 50개(실제 18개)"와 같은 계열의 실수 — 하루에 세 번, 전부 세는 범위·기준을 잘못 잡은 것이다.
- **가운데 정렬 타이틀을 `SPACE_BETWEEN` + FILL로 만들면 텍스트 폭이 0으로 눌린다.** 글자가 세로로 한 자씩 쪼개져 나온다("로/그/인"). `MIN` + itemSpacing 0 + 텍스트 FILL로 잡아야 한다.
- **아이콘 박스와 잉크는 다르다.** 종료 버튼을 "패딩 6 + 안쪽 10 = 16"으로 계산했는데 실측하니 잉크가 22에 서 있었다. lucide 글리프가 뷰박스 안에 자체 여백(x는 6, chevron은 8)을 갖는다. **면을 걷어내면 정렬 기준이 박스에서 잉크로 바뀌므로 계산이 아니라 실측으로 잡는다.**
- **`get_screenshot`이 옛 렌더를 준다.** 헤더 톤 변경 후 스크린샷이 그대로여서 코드를 의심할 뻔했는데 캐시였다. 단서는 **직전 파일과 바이트 크기가 같은 것**. 플러그인 경로(`node.screenshot()`)로 다시 렌더하면 최신이 나온다.

---

### 회고 및 인사이트

#### AI 도구 활용

- 레퍼런스가 갈렸을 때 주미님 지시는 "원복"이 아니라 **"왜 이렇게 했는지에 대한 명확한 판단 기준이 있으면 되"**였다. 결정 자체보다 결정을 다시 꺼내 볼 수 있는 형태로 남기는 게 요구사항이었고, 그래서 기준을 피그마 Usage 보드·`chat-history-panel.md`·plan-10 세 곳에 같은 문장으로 박았다. 나중에 검색·폴더가 들어올 때 이 기준이 재검토 트리거가 된다.
- 피그마 write는 대표 2장 → 확인 → 나머지 10장 순으로 끊었다. 첫 장에서 오토레이아웃 함정이 나왔으니 12장을 한 번에 돌렸으면 전부 깨진 채로 끝났을 것이다.
- **육안 검증이 수치 검증을 이긴 사례.** chrome 333건 제거는 스크립트가 "에러 0"으로 끝났고 잔존 인스턴스도 0으로 확인됐다. 그런데 스크린샷을 찍자 코치마크에서 입력바가 두 개로 보였다 — 절대배치 스포트라이트가 안 따라온 것. **"에러 없음"과 "화면이 맞다"는 다른 명제다.** 대량 조작일수록 표본 스크린샷이 필수다.
- 주미님의 `다 한거야그럼?` 한마디가 패키지 쪽 34px 상수를 찾아냈다. 내가 "피그마 끝 = 작업 끝"으로 범위를 좁게 잡고 있었던 것이라, 완료 보고 전에 **같은 결정이 사는 다른 레이어(코드·토큰·문서)**를 훑는 절차가 필요하다.

#### 설계 결정
- **(8/12·시트) 여백으로 안 풀리면 구조를 의심한다.** 헤더가 답답하다는 지적에 padding을 두 번(96→114) 넓혔는데도 안 풀렸다. 주미님이 **"페이지 타이틀과 설명 영역은 다른 것 아니냐"**고 짚은 게 전환점이었다 — 원인은 여백이 아니라 **성격이 다른 둘을 한 상자에 넣은 것**이었다. 화면 이름은 짧은 명사라 높이가 고정이어야 하고, 헤드라인은 문장이라 길이에 따라 늘어나야 한다. 한 컴포넌트에서 둘 다 하려니 어떤 값도 어중간했다. **같은 축으로 두 번 조정해서 안 되면 축이 틀린 것이다.**
- **(8/12·시트) 레퍼런스는 여백이 아니라 구조를 읽는다.** Slack 시트를 "더 넓게"로 받았지만 실제 차이는 여백이 아니라 층 분리였다(맨 위는 네비게이션, 그 아래는 전부 본문). 실측해서 80pt를 흉내 냈을 때보다, 왜 그 80pt가 비어 보이지 않는지를 읽었을 때 답이 나왔다.
- **(8/12·시트) 이름이 중복인지 아닌지는 층으로 갈린다.** 헤더에 "AI 노무사"를 넣으니 아래 프로필 카드와 겹쳐 보였고, "AI 프로필"로 바꾸니 안 겹쳤다. **헤더는 화면 종류, 카드는 그 화면의 내용** — 같은 대상을 가리켜도 층이 다르면 중복이 아니다. "첨부", "환경설정"이 성립하는 것과 같은 이치.
- **(8/12·시트) 판단이 갈리는 값은 렌더로 좁힌다.** 블러 반경(4/8/16), 딤 알파(20/30/40), 헤더 여백(3단계), 종료 안쪽 여백(0/4/8) 전부 임시 적용 → 스크린샷 → 원복으로 비교했다. `use_figma`가 원자적이라 실패하면 아무것도 안 남고 성공하면 원복까지 끝나 있다. **말로 30분 걸릴 결정이 렌더 한 장에서 끝난다.**
- **(8/12·시트) 다수결의 모수를 틀리면 결론이 뒤집힌다.** 거터는 "시트끼리만 세면 20이 다수"였지만 화면 거터는 전부 16이었고, 딤은 "시트 50개가 30%"가 실은 모달 32개를 포함한 오분류였다. 둘 다 **세는 범위를 잘못 잡아 한 번 틀렸다가 실측으로 뒤집은 건**이다. 감사 결과를 보고하기 전에 "무엇을 세었나"를 먼저 점검한다.
- **(8/12·시트) 공유 파일에서는 내가 안 만진 것도 변한다.** 작업 중 `2 · 전문 시트 · 접힘(기본)` 프레임이 통째로 사라졌고 페이지 이름도 바뀌어 있었다(`➡️` → `🍎`). 프로필 카드 이름도 실명에서 플레이스홀더로 바뀌었다. **"어제 본 노드가 오늘도 있다"를 전제로 스크립트를 짜면 중간에 null로 죽는다** — 배치 처리에는 노드 누락 방어를 넣는다.

- **모바일 히스토리 = 드로어.** 판단 기준은 **담고 있는 내용물의 성격**이다. 히스토리가 스위처(새 대화 + 목록 + 계정 진입점)인 동안은 드로어 — 지금 대화를 잠깐 벗어나 갈아타는 임시 레이어이고, 뒤에 원래 화면이 보이면 "사라진 게 아니라 저기 있다"가 유지된다. 검색·다중선택·폴더 같은 관리 기능이 들어와 브라우즈 목적지로 커지면 그때 full을 재검토한다. 레퍼런스 갈림도 같은 축으로 설명된다(Claude·ChatGPT·X=스위처형 / Gemini·Grok=검색 얹힌 목적지형).
- 웹 사이드바는 이미 floating raised 오버레이로 확정돼 있었다. 모바일만 별도 화면이 되면 브레이크포인트 사이에서 컴포넌트 정체성이 갈라지는데, 드로어면 "같은 레이어, 폭만 다름"으로 일관된다.
- **scrim은 피그마를 정본으로 코드를 내렸다.** `foundation/color.md`는 처음부터 40/60으로 맞게 적혀 있었고 코드만 0.48/0.64로 흘러 있었다 — 문서가 아니라 코드가 드리프트한 케이스.
- **아트보드 기준(393×852)은 바꾸지 않는다.** 주미님이 `애초에 아트보드 사이즈 기준을 바꿔야하나`로 물었는데, 문제는 852라는 숫자가 아니라 그 안의 chrome이었다. 걷어내고 나면 852 프레임이 곧 "뷰포트를 꽉 채운 화면(100dvh)"으로 읽혀 개발이 그대로 매핑한다. 실제 브라우저 높이는 URL바 접힘·펼침으로 계속 변해(`dvh`/`svh`) **"진짜 높이" 하나를 고를 수도 없고**, 기준을 바꾸면 화면 145개 재레이아웃이다. 세로 공간 낙관은 기준 변경이 아니라 짧은 뷰포트 QA 프레임으로 잡는다.
- **안전영역은 값이 기기에 있지 디자인에 있지 않다.** 그래서 컴포넌트 토큰에 CSS 환경 함수(`env()`)를 처음으로 허용했다. 치수 토큰은 원래 확정값이어야 하지만, 34px로 적는 순간 그 값은 **어느 기기에서도 맞지 않는다**(노치 기기엔 우연히 맞고 나머지엔 빈 띠). 같은 성격(`env(safe-area-inset-*)`)이 아니면 예외를 확대하지 않기로 `token-contract.md`에 못박았다.
- **삭제가 아니라 hide를 택했다**(주미님 결정). Dev Mode·MCP가 invisible 레이어를 안 읽어 개발이 받는 효과는 삭제와 같으면서, 되돌리기가 즉시 가능하다. `Bottom Bar`는 세트에 `HomeIndicator=off` 축이 이미 있어 override가 아닌 **정식 경로**로 처리했다 — 마스터 개정에도 안 깨진다.

---

## 모바일 히스토리 드로어 전환 (제품 파일)

주미님이 Claude·ChatGPT·X 모바일 캡처 3장을 놓고 `이렇게 밀리는게 많은거 같아 / 너 생각은 어때?`로 시작. 이후 Gemini·Grok은 full이라는 반례를 확인했고, `아냐 왜 이렇게 했는지에 대한 명확한 판단 기준이 있으면 되`로 기준 명문화 + 드로어 채택 확정.

- read-only 감사: 제품 파일 `➡️ home/history`의 모바일 history 화면 **12장이 전부 393 풀블리드**(패널 인스턴스가 StatusBar 아래 393×759). 뒤 화면도 스크림도 없었다.
- 재조립 구성 — 배경(home 화면 클론) → `semantic/overlay/scrim`(라이트 40% / 다크 60%) → **Drawer 337(= 393 − 56)**·x0 풀하이트·우측 radius 16 → StatusBar·HomeIndicator 최상위. 패널 인스턴스는 337×759 리사이즈만 했고 **컴포넌트 마스터·DS 파일은 무변경**.
  - ⚠️ **StatusBar·HomeIndicator 부분은 같은 날 옆 세션 plan-28이 대체했다** — OS/브라우저가 그리는 영역이라 더미를 두지 않는 것으로 확정(`chat-history-panel.md` 갱신본). 드로어 337·스크림·radius는 그대로 유효하다.
- 오버레이 위치 보정 3건 — 컨텍스트 메뉴 −56(··· 앵커 추종), 스피너·재시도 버튼 −28(드로어 중앙). 삭제 확인 팝업의 Dim·Modal은 드로어 위 레이어라 화면 중앙 유지.
- Usage 보드(`7876:21231`)에 드로어 스펙 + 판단 기준 추가, Mobile usage 인스턴스 2개 337 리사이즈·리네임.
- 라이트/다크·empty·행 상태·더보기 메뉴·삭제 확인·로딩·실패·비로그인 2종·소셜 아바타 전 화면 스크린샷 육안 확인.

rollback = 제품 파일 버전 히스토리(화면 12장 + Usage 보드 3노드).

---

## 스토리북 정합 + scrim 드리프트 정리

- 컴포넌트 스토리 모바일 QA 컨테이너 **393 → 337** 7곳(드로어 근거 주석 포함). 문서에 적힌 "Panel 툴바"는 이미 인라인 컨테이너 방식으로 대체돼 있어 표현만 낡은 상태였다.
- `Pages/Home`에 **`Mobile (393) — History Drawer`** 스토리 신설 — 홈 백드롭 + `bg-overlay-scrim` 전면 + 드로어 337(우측 radius는 `--chat-history-panel-radius`) + 패널 flush(자체 radius·ring 인라인 해제).
- **scrim 값 정합** — `semantic.tokens.json` `color.overlay.scrim` 0.48/0.64 → **0.4/0.6**(피그마 `black-alpha/40|60`). 소비처는 `ModalDialog`·`BottomSheet` backdrop. 토큰 이름·공개 manifest 불변이라 changeset patch.
- 제품 파일 삭제 확인 팝업 Dim(`8104:23338`)을 primitive `black-alpha/30` 직바인딩 → `semantic/overlay/scrim` 재바인딩, 렌더 40% 실측. **8/11 확정된 딤 2단 규칙**(시트 `scrim-soft` 30 / 모달·차단형 `scrim` 40·60)에서 삭제 확인은 차단형이라 40이 맞다.
- 검증 — `token:build`·`typecheck`·`test`(111)·`build`·`pack:check`·`storybook:build`·`demo:build`·문서 게이트 전부 통과. Chromatic **build 592 / 594 passed**(각 visual change 1건 = 해당 변경분).

### 커밋 (socraAI_product design)

| 커밋 | 설명 |
|------|------|
| `f23f86b` | fix(prism): overlay scrim 값을 피그마 정본에 정합 (0.48/0.64 → 0.4/0.6) |
| `7bec52f` | feat(storybook): Pages/Home에 모바일 히스토리 드로어 스토리 추가 |

> 두 커밋 모두 `origin/feat/edge-scrim-exploration`에는 올라갔고 **`origin/main`에는 아직 미도달**이다(PR 미개설). 스토리 337 전환분은 옆 세션 커밋 `dfcac28`에 이미 흡수돼 있다.

---

## History Item 트랙 마무리 — PR #151 CI 확정 · 공유 워킹트리 백업 (옆 세션 트랙 인수, 8/12 새벽)

```
다 한거야그럼?
```
```
응 정리하고 worklog 올리고 마무리해줘
```

- **PR #151 CI 확정** — plan-23 정본이 PR 밖에 남는 걸 막으려 `b409d03`을 cherry-pick(`d16bad0`)해 붙였고, 재실행 결과 빌드 게이트 전부 pass. `UI Review`가 **"no changes"** 로 나온 게 중요한데, 오늘 바꾼 것(시각 상시 표시·engaged ··· 색·loading ··· 노출)이 **기존 스냅샷 픽셀을 안 건드렸다**는 뜻이다. 다만 웹 사이드바 `showTime={false}` 제거는 시각 변화가 있어야 정상 — 그 스토리들이 `disableSnapshot` 대상이라 애초에 안 찍힌 것이므로, 전수 픽셀 비교는 여전히 머지 전 `workflow_dispatch`가 정본이다.
- **공유 워킹트리 백업** — 위 함정 참조. `backup/edge-scrim-wip-2026-08-12`(`3ce5366`)로 push: 미커밋 25 + 미추적 6 = **31파일, 820 insertions**. 부모를 HEAD로 잡아 로컬 전용 커밋 17개도 같은 push로 함께 보존된다. 워킹트리·인덱스 무변경 실측(`git status` 31건 그대로) — 피어 세션 작업을 끊지 않는다.
- **CONTEXT·뷰어 정합** — 8/11 CONTEXT의 History Item 절이 "게시 대기·미푸시" 상태로 낡아 있어 게시 완료·전파 실측·툴팁 채택·PR #151 대기로 갱신. 뷰어 태그 치환은 **한 번 조용히 실패**했다(피어 세션이 그 사이 ENTRY_META를 갈아써 앵커 문자열이 사라짐) — 재확인해서 현재 배열에 다시 붙이고 공개 URL cache-bust로 노출까지 확인했다.

### 커밋 (riiid/prism)

| 커밋 | 설명 |
|------|------|
| `d16bad0` | docs(plan-23): PR #151 분리 기록 (PR 브랜치 · origin/main 미도달) |
| `3ce5366` | backup: 공유 워킹트리 스냅샷 (백업 브랜치 · 머지 대상 아님) |

---

## 디바이스 chrome(상태바·홈 인디케이터) 더미 제거 — 제품 파일 + 패키지 (plan-28)

개발 요청에서 시작. 주미님 전달 원문:

```
아 맞다 개발에서 홈 인디케이터 영역이랑 스테이터스 바 영역 디자인에 더미로 잡아둔거 개발에 잡힌다고 빼줄수있음 빼달라는데 피그마에서 어케생각해?
```

```
응 지금 다 아이폰 16기준이라 다 들어가있어
```

본선이 모바일 웹이라 두 영역은 OS·브라우저가 그리는 자리이고 우리 구현 대상이 아니다. 제거에 동의하고 read-only 감사부터 시작했다.

### 제품 파일 — 화면 145개, chrome 333건

삭제가 아니라 **되돌릴 수 있는 조작만** 썼다(주미님 `응 hide?`).

| 구조 | 처리 | 근거 |
|------|------|------|
| `Bottom Bar` 안 `HomeIndicator` | variant `HomeIndicator=off` 스왑 **111** | 세트에 축이 이미 있다 — 정식 경로 |
| `Navigation Bar` 안 `StatusBar` | 인스턴스 `visible=false` | 축이 없어 override가 유일. 마스터 무변경 |
| 화면에 직접 얹은 인스턴스 | `visible=false` | 합계 hide **222** |

페이지별: `chat/chart` 103+109(82화면) · `home/history` 4+62(16) · `login` 4+34(31) · `settings` 0+17(16). 에러 0건, 잔존 가시 인스턴스 **0** 확인.

제외 — `❌` 접두 페이지 3개, 폭 2000px↑ 기획서 보드(`설정/계정` 11943 · `회원가입/로그인` 10102). **폭 2000 넘으면 화면이 아니라 문서 보드다.** DS 파일 `➡️ Guidelines`는 스캔 결과 chrome 0건이라 손댈 것 없음.

### 딸려온 좌표 보정 14건

| 계열 | 이동 | 대상 |
|------|------|------|
| 헤더 앵커(행동 확인 토스트) | y 127 → **68** | 8건 |
| 컴포저 앵커 | **+34** | 시스템 오류 토스트 4 · `InputTopFade` · `Callout / 체험 소진` |
| 코치마크 | **+34** | `Spotlight — 입력바` · `CoachTooltip` |
| 흐름 콘텐츠 앵커 | **−59** | `설명글`의 `Tooltip` |
| 바닥 flush | **+34** | `Chat / 상단 fade`의 절대배치 `Bottom Bar` |

덤 — `login / 소셜 로그인 취소·실패` 4장의 `Toast / 네트워크`(y=71 고정)는 더미가 있던 동안 **헤더와 겹쳐 있었다.** 이번에 헤더 아래 11px로 정리됐다(설계값 68과 3px 차는 기존 드리프트라 미조정).

### 패키지 — 같은 더미가 코드에도 있었다

주미님 `다 한거야그럼?`에 확인하다 발견. `--bottom-bar-safe-area`가 **34px 상수**라 노치 없는 기기·데스크톱에도 빈 띠가 남고 있었다.

- `bottom-bar.safe-area` → `env(safe-area-inset-bottom, 0px)`
- `bottom-bar.height.chat` → `calc(202px + env(...))` — 안전영역을 품는 건 `chat`뿐. 236 상수로 두면 안전영역이 0이어도 `flex-1`이 남은 34를 먹어 빈 공간이 그대로 남는다
- `SheetShell` `h-[34px]` → `h-[env(safe-area-inset-bottom,0px)]`
- `token-contract.md`에 절 신설(컴포넌트 토큰에 CSS 환경 함수를 허용하는 첫 사례), changeset `minor`(시각 변화 있음 — 노치 외 환경에서 하단 34 감소)

검증 — `token:build`·`typecheck`·`test`(111)·`build`·`lint`·`pack:check`·`storybook:build`·`demo:build`·문서 게이트 **전부 통과**.

### 문서

`docs/plans/28-device-chrome-removal-2026-08.md` 신설(정본). 값이 실제로 틀어진 것들도 함께 고쳤다 — `toast.md` 헤더 앵커 127→68(좌표계가 바뀐 것이지 규칙이 바뀐 게 아님을 명시), `chat-history-panel.md`의 8/11 드로어 절 "StatusBar·HomeIndicator 최상위" 대체, `navigation-bar.md`·`bottom-bar.md`·`bottom-sheet.md` 적용 기록. 피그마 Usage 보드(`7876:21231`) 노트도 같은 문구로 갱신.

rollback = hide·variant는 즉시 원복, 좌표 14건은 plan-28 표에 이전 값 보존, 패키지는 `d7267e5` revert.

### 커밋 (socraAI_product design)

| 커밋 | 설명 |
|------|------|
| `d7267e5` | fix(prism): 하단 안전영역을 34px 상수에서 `env(safe-area-inset-bottom)`로 바꾼다 |

> `origin/feat/edge-scrim-exploration` 도달 확인, **`origin/main`에는 미도달**(PR 미개설 — 이 브랜치 처분은 #151과 함께).
> 공유 워킹트리라 **hunk 필터로 갈라 커밋했다** — 7파일은 통째, 4파일(`component.tokens.json`·`theme.css`·`components.test.tsx`·`token-contract.md`)은
> 내 hunk만 골라 `git apply --cached`로 인덱스에 넣었다(theme.css 12 hunk 중 4). 워킹트리는 무변경이라 옆 세션 렌더가 안 깨진다.
> `toast.md`·`chat-history-panel.md`는 hunk 자체가 섞여 제외했다.


---

## 바텀시트 헤더 — 층위 분리로 재구성 (DS·제품 파일·문서, 8/12)

```
근데 그럼 타이틀로 해도 위에 x는 위로 분리를?
```

```
내말은 아얘 층위를 나눠야하나 하는거야 / 페이지 타이틀과 / 그 설명 영역은 다른거아닌가 하고
```

- **8/11에 만든 `Sheet/Header`를 다시 뜯었다.** 그 컴포넌트는 타이틀+설명+종료를 한꺼번에 들고 있었는데, 여백을 두 번(80→96→114) 넓히고도 답답함이 안 풀렸다. 원인이 여백이 아니라 **크롬과 콘텐츠를 한 상자에 넣은 것**이라는 게 주미님 지적으로 잡혔다.
- **결정 구조**: `Sheet/Header`는 크롬만 — 뒤로가기(BOOLEAN) · 화면 이름(짧은 명사) · 종료. 축은 `title = on(76) | off(60)` 하나. 헤드라인 문장과 설명은 새 컴포넌트 `Sheet/Intro`(`4544:2276`)가 본문 첫 블록으로 맡는다. **좌측 44 슬롯은 뒤로가기가 없어도 자리를 지킨다** — 그래야 가운데 이름이 안 흔들린다.
- **제안 보드를 먼저 만들어 실물로 확인했다**(제품 파일 `8409:17877`). 실제 시트 4종(로그인·피드백·전문·첨부)에 현재/제안A/제안B 3열. 보드가 두 가지를 드러냈다 — ① 이름 없는 시트는 76 빈 밴드가 생긴다 ② Intro의 padBottom 28과 본문 gap 24가 이중으로 쌓여 52가 된다. 주미님이 **3안(이름 없음 · 크롬 60)** 채택.
- **시트별 배정**: 로그인·피드백은 `title=off`(이름이 바로 아래 헤드라인과 같은 말이 된다) + Intro. 첨부는 `title=on` "첨부". 전문 시트 계열은 `title=on` **"AI 프로필"** — 진입 경로와 무관하게 같은 이름을 주고 리스트 경유에서만 뒤로가기를 켠다.
- **손그림 시트 11개 전환 완료.** `sheet(로그인)` 2 + `sheet(에이전트)` 9. 헤더를 `title=off`로 내리고 원래 상단에 있던 헤드라인을 `Sheet/Intro`로 옮겼다(문구 사전 백업 후 이관 — 로그인 2·피드백 7·모델 리스트 2). 거터도 20→16.
- **검증**: 게시 후 헤더 인스턴스 **28개 전수 실측** — `title=on` 76 5개 · `title=off` 60 23개, X 잉크 예외 없이 16선, 탭 타깃 44×44 유지. 불일치 0.
- **종료 버튼 위치는 현행 유지로 결론.** "터치 영역 고려하면 너무 옆에 붙은 것 아닌가"라는 지적에 안쪽 여백 0/4/8을 렌더로 비교했는데, 0일 때만 X 오른쪽 끝이 본문·버튼 끝선과 같은 16선에 선다. 탭 영역이 화면 가장자리에 닿는 건 오히려 실질 타깃을 키운다(피츠 법칙). iOS 가장자리 스와이프는 드래그라 탭을 뺏지 않고, 피하려면 20 이상 밀어야 해서 정렬이 깨진다.
- **스택 시트는 채택하지 않았다.** 레퍼런스에서 시트 위에 시트가 쌓이는 걸 보고 검토했으나, 우리 깊이 2는 리스트→전문 하나뿐이고 그것도 "목적지 이동"이 아니라 **"피커→콘텐츠"**다. 다 쓴 피커가 뒤에 남을 이유가 없고, 시트 뒤에는 이미 채팅이 맥락으로 깔려 있어 한 겹 더 끼우면 정작 봐야 할 게 두 겹 뒤로 밀린다. 게다가 우리 시트는 높이가 제각각(리스트 352 · 전문 612)이라 뒤 시트가 peek되지도 않는다.
- **딤은 30 단일로 되돌렸다**(8/11 밤 → 8/12 새벽 반영). 8/11에 2단(시트 30 / 모달 40)으로 나눴는데, 형제 노드 기준으로 다시 가르니 "시트 50개"가 실은 모달 32개를 포함한 오분류였다. 실제 분포는 30%가 50개·40%가 raw 6개라 화면 기준으로 30 단일. 제품 딤 50개를 `overlay/scrim`에 묶고 `scrim-soft`는 삭제.
- **문서**: `design-system/components/sheet-header.md` 전면 개정(결정 이력 포함), `sheet-family.md`·`foundation/color.md`·plan-10·plan-22 갱신. 워크트리 `/tmp/socra-sheet-header-wt`(브랜치 `docs/sheet-header-2026-08-11`)에서 작업해 **PR #148**로 push. 커밋 `709abbd`·`2040053`·`6a5c9c3`·`294e6d6`·`68e72df`·`21bc901`·`80a5f90` — origin/main 미도달이라 커밋 테이블에는 넣지 않는다.


---

## 시트 코드 포팅 — SheetHeader·SheetIntro 신설 (패키지·스토리북, 8/12)

```
마저 진행해줘
```

- **PR #148(문서)이 이미 머지돼 있었다.** 코드 브랜치를 처음엔 origin/main에서 땄는데 계약이 참조하는 근거 문서가 없어 문서 게이트가 막혔고, 그게 단서였다. main 위로 리베이스해 계약·근거 문서와 한 줄에 세웠다.
- **`SheetHeader`** — 뒤로가기(`onBack` 유무) · 화면 이름(`title` 유무로 76/60) · 종료(항상). **좌측 44 슬롯은 뒤로가기가 없어도 `aria-hidden` 스페이서로 자리를 지킨다** — 가운데 이름이 흔들리지 않게. 좌우 패딩 0은 광학 정렬값이다(44 안 24 아이콘 = centering 10 + 글리프 인셋 6 = 16).
- **`SheetIntro`** — 헤드라인+설명. 패딩도 하단 여백도 없다. 간격 책임은 본문 컨테이너 gap에 있고 여기서 또 주면 이중으로 벌어진다.
- **`SheetShell` 브레이킹** — `backLabel`·`onBack`·`onClose` 제거, `header` 슬롯 추가. changeset minor(0.x라 breaking이 minor로 나간다). 소비처 스토리 4곳(`SheetShell`·`SheetAnswer`·`EdgeProgressiveBlur`)과 테스트 2건을 같이 고쳤다.
- **`BottomSheet`** — 손그림 헤더를 `SheetHeader`로 교체, 행 거터 `px-5`→`px-4`. `title` prop 유지라 공개 API는 안 변한다.
- **검증**: `token:build` · `typecheck` · `test`(112) · `build` · `pack:check` · `storybook:build` · `validate-design-system` 전 항목 통과. 커밋 `9ee540b`·`5aa18a0` — origin/main 미도달이라 커밋 테이블에는 넣지 않는다.
- **분업 정리**: 같은 시각 옆 세션이 이 포팅을 착수했다가 중복을 확인하고 되돌리며 소유권·#148 머지·scrim 0.3을 전달해 줬다. scrim은 그쪽(`d16d65c`)과 이쪽(PR #153) 양쪽에 같은 값으로 들어가 있다.
