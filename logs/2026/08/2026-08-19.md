# 2026-08-19

### 미해결 항목

> **열린 항목 정본.** 8/18 미체크 **79건을 여기로 이월했다**(주미님 지시) — 8/18 로그에는 그날 닫힌 30건만 남는다.
> 아래는 오늘 세션 트랙 4개가 먼저 오고, 그 뒤에 「8/18 이월」 묶음이 원래 블록 구조 그대로 붙는다.

**오늘 트랙 (Deep 사고과정)**

- [x] ~~**시안 1 최종 확정 선언 대기**~~ — **8/19 종결(주미님 "남은 단계를 미리 보여주지 않는다").** 시안 1 채택. 예정 노출(시안 2·4)은 기획서 §3.4가 펼침을 "지난 단계 이력"으로만 규정해 기각, tick 게이지(시안 3)는 8/18 기각. 보드 라벨에 `[채택]`/`[기각]`을 박았다
- [x] ~~**DS 마스터 반영**~~ — **8/19 완료, 게시·수락까지 내려감.** `4625:2316` 4 variant 헤더(`LeadIcon` + `StepText` HUG·maxWidth 245) · Deep 펼침 이력(마커 슬롯 24 · rail 2px · 도착 카드) · Chat·Quick 펼침 마커 통일 · 도착 줄 문구 · description. 실측 Deep 펼침 361×304(시안1과 동일). 전파 96/96 · `visible` override 27개 생존
- [ ] **`딥 - 사고 디자인 디벨롭 중` 섹션(`9280:110814`) 정리** — 기각 라벨(`[기각 · 예정 노출]` 등)은 박았지만 실제 정리는 안 했다. 도착 줄이 에이전트명이 되며 `9299:22316`이 기본 판과 중복됐고, 실화면 인스턴스가 확정 디자인으로 갱신돼 시안1 detach 사본들도 역할을 다했다. ⚠️ `8988:119796`은 아이콘 축 근거판이었으나 **8/19 판정으로 근거 지위 해제**
- [x] ~~**도착 줄 = 모델명 vs 에이전트명**~~ — **양자택일이 아니었다.** §3.4 원문은 `{AI 이름}의 의견이 도착했어요`이고 Deep 참여자는 모델 3 + 에이전트가 섞인다(`n`=전문가 최대 7 / `k`=AI 최대 10). 문구는 원문으로 정합(마침표 제거), 이름 혼재 반영은 아래 신규 항목으로
- [x] ~~**헤더 아이콘 축(`list-checks`) 판정**~~ — **8/19 종결(주미님 "list-checks 넣기").** 4 variant 전부 `LeadIcon` 16. 딸려온 재계산 = `StepText` maxWidth 269 → 245(= 361 − 16 − 8 − 60 − 8 − 8 − 16)
- [x] ~~**문서 미커밋 — plan-27 · thinking-toggle.md**~~ — **커밋 완료(`ea693c3`).** 8/18부터 이월돼 있던 항목이다. plan-27 변경분이 8/18~19 사고과정 트랙 한 덩어리(앞 세션의 선택지 보드·조합 시안 기록 + 내 8/19 종결분)라 갈라내면 반쪽이 되어 **트랙 통째로 커밋**했고, 커밋 메시지에 그 사실을 적었다. **push는 안 했다**(브랜치 ahead 4).

- [ ] **재게시 필요 (사고과정)** — Deep 펼침 `Progress`(`4935:2482`) 바인딩 수정분이 마스터에만 있다. 게시·수락 전까지 인스턴스 **9개**가 단계 문구는 숨김인데 진행바만 노출된 상태로 남는다
- [ ] **완료 컷 rail 높이** — 주미님이 만든 `Deep — 완료 (펼침·5단계 전체)`(`9418:124825`)의 `Rail`이 70h라 4~5행에 안 닿는다. 완료 상태 기준 약 160. rail은 절대배치라 상태마다 조정 대상
- [ ] **도착 줄 이름 혼재 케이스 미반영** — 시안1 판 6곳이 에이전트 3개만 나열이다. 실제로는 모델명·에이전트명이 섞이므로 혼재 판으로 바꿔야 이름 길이 편차에서 말줄임·정렬이 검증된다

**Sheet Body 트랙 (별도 세션)**

- [ ] **`로그인 게이트` DS 승격 — 승인 대기** — 감사에서 권장으로 냈다. DS에 `Sheet Content / 로그인 게이트` 신설 → 게시 → 제품 인스턴스 2개 스왑 → 297 검증. 부품 7개·문구 4개가 이미 DS 소유라 손실 없이 옮겨진다
- [ ] **인용 부호 로케일 글리프 규칙이 어느 문서에도 없다** — JP `「」` · KR `''` · EN `""`. 토큰이 아니라 렌더 규칙이라 자리가 애매한데 `design-system/foundation/` 쪽이 맞아 보인다
- [x] **plan-10 편집 커밋 완료(`3ff19aa`)** — 같은 파일에 다른 세션 절(`### 보드 전수 대조` 등)이 섞여 있어 경로 지정 커밋으로는 그쪽까지 쓸려간다. **내 절만 갈라 스테이징**해 41줄만 커밋했다(HEAD 사본에 내 절만 얹어 `git hash-object -w` + `git update-index --cacheinfo`로 인덱스에 직접 심는 방식 — `git apply --cached`보다 hunk가 섞였을 때 확실하다). 피어 편집 172줄은 워킹트리에 그대로 남아 있다. **push는 안 했다**(주미님 "커밋만").

**칩 행 스와이프 가이드 트랙 (별도 세션)**

- [ ] **시각 QA 미실행** — `EdgeBand` 렌더 경로(className·placement)를 바꾸고 스토리 1개를 새로 넣었는데 `pnpm visual`(Chromatic)을 안 돌렸다. 기존 `top`/`bottom`은 `inset-x-0`가 placement 맵으로 옮겨간 것뿐이라 출력이 같을 것으로 보지만 **측정 안 했다.**
- [x] ~~**push·PR 미실행**~~ — **8/19 push 완료(전량).** 브랜치는 하루 두 번 밀렸고 지금 ahead는 방금 올라온 피어 커밋이라 「미실행」이 아니라 **상시 흐르는 상태**다. PR은 아래 「PR 미개설」 한 줄로 합친다
- [ ] **오늘 socraAI 커밋 4건이 어느 로그에도 없다** — `fbc6431`·`50d21cc`·`8dd90b9`·`cdbec3a`(스토리북 대기 투표·리포트 모션). 다른 세션 트랙이라 내가 내용을 못 적는다. 소유 세션이 기록해야 한다
- [x] ~~**`QuickStart/Group` 마스터 이름 변경분 확인**~~ — **8/19 확정.** DS 마스터는 `QuickStart/Group`(`4825:2387`)이고, 제품 파일에서 보이는 `Attachment/Lightbox/QuickStart/Group`(`9253:159339`)은 **원격 프록시의 이름**이다(같은 접두가 붙는 다른 프록시들과 같은 현상). 화면 인스턴스의 실제 마스터는 `9469:127684`

**8/18 목록 정리 패스 트랙 (별도 세션)**

- [x] ~~**①미푸시 커밋 push**~~ — **8/19 완료(주미님 "우선순위 높은거부터 하자").** `bd24ef5..3ff19aa` 17커밋을 `origin/feat/edge-scrim-exploration`으로 올렸다. behind 0 · ahead 17이라 fast-forward였고 히스토리 재작성·워킹트리 변경 없이 원격에 추가만 됐다. plan-30 6건 · Callout 2건 · 첨부 칩 2건 · `e7c8372`(8/16) 전부 백업됨. 이어서 `67a9776`(AnswerFeedback 계약)도 push해 현재 ahead 0
- [x] ~~**②#161 머지 → main 전체 비교 1회**~~ — **8/19 완료.** `#168`(CHANGELOG 정정, 화면 변화 0) → `#161`(패널 radius flush) 순으로 스쿼시 머지(main `78eddff` → `da39447`). 이어서 main에 `workflow_dispatch`를 걸어 **Build 704**를 찍었다(run `32210760027`). 로그에서 `VITE_STORYBOOK_FULL_SNAPSHOTS: true` · `forceRebuild: true` · 321 스토리를 확인 — **스킵이 아니라 실제 비교가 성립했다.** 최근 변경이 닿는 스토리 파일 74개
- [ ] **③브랜치 처분** — 되돌리기 어려운 축이라 승인 전 착수하지 않는다. (①②는 종결)
- [ ] **⚠️ #161의 changeset이 다음 릴리스 PR에 실린다** — `.changeset/sheet-shell-panel-radius.md`가 함께 머지됐으므로 `chore: 0.x.0` 릴리스 PR이 새로 열리거나 갱신된다. **8/18 사고(릴리스 PR이 내용 PR을 앞질러 폐기 기능이 발행됨)와 같은 자리**라, 그 릴리스 PR을 머지하기 전에 안에 든 changeset 목록을 먼저 읽는다
- [ ] **미커밋 36건은 그대로다** — 커밋 유실 위험은 위 ①로 사라졌지만 **워킹트리 미커밋 36건은 여전히 백업이 없다.** 전부 옆 세션 작업분이라 내가 커밋할 수 없다(`toast.md`·`storybook-map.md` 등). 소유 세션이 커밋해야 풀린다
- [x] ~~**8/18 미해결 목록 정리**~~ — 실측 대조로 완료 9건 종결·기록성 5건 삭제·12항목을 5개로 합침·누락 8건 추가. 미체크 92 → 79. 정본은 `2026-08-18.md`, 이 로그로 복제하지 않는다

**계약 버킷 감사 트랙 (별도 세션)**

- [x] ~~**피그마 섹션 이동 13건**~~ — **8/19 실행 완료**(주미님 "3개 다 진행해"). 3차로 나눠 write: 모달 8개 → `계약 불필요`, 행 마스터 2개 + `Chart/ProgressBar` + `History/Title` → `계약 불필요`, `Agent/Row` → `Story book 미계약`. **검산이 전부 맞았다** — 이동 전후로 페이지를 같은 방식으로 두 번 스캔해 노드 ID로 대조하니 **버킷이 바뀐 항목 정확히 13개 · 사라진 항목 0 · 의도치 않게 움직인 항목 0**, 섹션별 자식 수도 `+8/−8` · `+4/−2·−1·−1` · `+1/−1`로 일치. `계약 불필요` 섹션 높이 2434 → 3760. 롤백 좌표 13개는 `storybook-map.md` 「제외 항목」 절에 전량 보존(`621c631`)
- [x] ~~**`Row/Item`·`ContextMenu/Item`은 버킷이 아니라 마스터 통합 건**~~ — **8/19 통합 완료**(주미님 "합쳐", `86fc35b`). 두 세트를 삭제하고 `Menu Item`(`3352:1374`) 하나로 모았다. **인스턴스가 0이었다** — 앞 감사는 두 세트가 `Dropdown Menu`·`Context Menu` 바로 오른쪽 같은 y에 놓인 걸 보고 그 메뉴의 행 마스터로 봤는데, `mainComponent` 실측으로는 두 메뉴가 이미 `Menu Item`을 3개씩 쓰고 있었다(제품 3개 페이지도 0건). **좌표 인접은 소속의 근거가 아니다.** State는 3 ⊂ 4 ⊂ 5로 부분집합이고 슬롯 프로퍼티는 `Menu Item`만 갖는다(나머지 둘은 내용 하드코딩) → 재현 불가한 축 0. 복원 데이터는 `storybook-map.md`에 전량 보존
- [x] ~~**`AnswerFeedback` 계약 md가 없다 (역방향 구멍)**~~ — **8/19 반영(`67a9776`).** 계약 md + 근거 문서 신설, `storybook-map` 행 추가, `sheet-answer.md:34` 정정, `SheetAnswer.stories` `FullAssembly` 손조립 → `<AnswerFeedback/>`. **원인이 드러났다** — 8/6에 `sheet-answer.md`가 "피드백 행은 별도 컴포넌트가 아니다"라고 적었고 사흘 뒤(`683ac44`) 실제로 세트·컴포넌트가 생겼는데 그 문장이 안 따라와, 열흘간 문서상으로는 "조합물"이었다. 게이트 3종 통과(validate · typecheck · test 132/132)
- [x] ~~**`design-system/components.md` 섹션 정의가 피그마와 3군데 어긋난다**~~ — **8/19 정정(`67a9776`).** ①하위 섹션 열거에 `Decision` 추가 ②`미계약 컴포넌트`(`1684:1973`) → `Story book 미계약`(`4635:2184`) ③사라진 `Chat_내부(컴포넌트 계약x)`(`3116:8689`)·`Chat History Panel_내부` 2개를 「정정 3건」 절로 명시. 두 섹션을 되살릴지 부품을 `계약 불필요`로 보낼지는 미결로 남겼다
- [x] ~~**역방향 스윕 미실행**~~ — **8/19 완주. 진짜 구멍 0건.** 이름 규칙 표 56행을 피그마 116개 인벤토리와 정규화 대조(`Settings Row`↔`Settings/Row` 류 흡수)해 8건이 남았고, 그중 6건은 이름만 다른 대응이 있었다(`Attachment` · `Chart/StackBar` · `Chart/LegendRow` · `Markdown Rendering` · `Report/Surface`). 남은 2건은 **문서에 이유가 이미 적힌 의도적 예외** — `VoteResult`("폭이 데이터인 바는 코드가 정본, 피그마 신설 마스터 없음", 주미님 2026-08-07 확정) · `SheetAnswer`(제품 파일 소유, "본문 2,500자 더미가 마스터 높이를 정해 DS로 안 올림"). `search_design_system`으로 DS 라이브러리에 두 마스터가 실제로 없음을 확인했고, `Sheet/Content Placeholder` 설명이 "실제 화면에서는 제품 본문 컴포넌트로 교체한다"고 같은 말을 한다. 계약 62개가 두 표에 정확히 55+7로 갈려 고아·유령 0건인 것도 함께 확인
- [x] ~~**미결 5건은 `주미님 확인`으로 분리해 승인 묶음에 안 섞었다**~~ — **8/19 재배치 완료**(주미님 "너가 맞는 기준으로 다시 정리해주라 — 피그마랑 스토리북 둘다", `86fc35b`). 기준을 먼저 세워 `components.md`에 「판정 기준」으로 박았다 — **버킷은 "무엇인가"가 아니라 "공개 계약 3종(계약 md + 패키지 export + `Components/*` 스토리)을 가질 것인가"** 로 가르고, 갈릴 때 결정 신호는 **`index.ts` 공개 export 여부**, 부수 규칙으로 **`_` 접두는 전부 `계약 불필요`**. 적용 결과: `_SettingsTrailing`·`Report/Surface`·`socra`·`Attachment/Logo/Vestway` → `계약 불필요`, `QuickStart/Group` → `Story book 미계약`(plan-10에 컴포넌트화가 미체크로 살아 있어 `Agent/Row`와 같은 처리). 검산 겹침 0 · 자식 수 17→21 · 7→8
- [x] ~~**`/tmp/prism-main-audit` worktree 정리**~~ — 감사용 `origin/main`(`ca92bf8`) 읽기전용 사본. 세션 마감에 `git worktree remove --force`로 제거 완료(목록 0건 확인). 후속 작업은 실제 레포에서 하면 되므로 다시 뜰 필요 없다

> **「레포 문서로 이관할 4건」 — 8/19에 2건 완료(`67a9776`).** `chat-history-panel.md`(hide 상태 · 상한 658)와
> `plans/23`(웹 홈 사이드바 276 손그림)은 대상 문서로 옮겼고 아래 블록에서 지웠다.
> **남은 2건은 둘 다 `toast.md`인데 옮길 자리가 옆 세션이 방금 추가한 108줄 블록 *안쪽*이라 훅 분리가 불가능하다** —
> 지금 쓰면 그쪽 미커밋이 내 커밋에 딸려간다. 소유 세션이 커밋한 뒤에 옮긴다.

**재열람 적재 트랙 (별도 세션)**

- [ ] **Chromatic 재실행 필요 — Build 714는 이제 낡았다** — 그 뒤 면을 복구해 스냅샷 2장의 내용이 바뀌었다. 714 육안 판정은 지금 보는 화면과 다르므로 다시 찍고 봐야 한다. https://www.chromatic.com/build?appId=6a1e5942ec7ecc06429b1040&number=714
- [x] ~~**2차 세션 변경분 5건 미커밋**~~ — **커밋 완료(`2a5a47b`).** 승인 받고 담았다. push는 안 했다. 담은 직후 피어가 `3020c28`을 올려서 워킹트리가 실제로 흐르고 있었다 — 안 담았으면 위험했던 자리
- [x] ~~**`Skeleton` 바 색이 채팅 배경과 같다**~~ — **8/19 정정 완료(`9c35922`).** 처음엔 다른 세션에 넘기기로 했다가 주미님이 "스켈레톤도 너가 봐봐"로 되돌렸다. 컴포넌트 토큰 `skeleton.bar.bg` = `{color.neutral.100}` 신설. **계약에 적힌 "`surface-muted`(Figma `neutral/100` 대응)"가 애초에 틀린 문장이었다** — `surface-muted`는 `neutral/50`이다
- [ ] **Chromatic 전체 비교 미실행 — Build 725는 스냅샷 2장뿐** — TurboSnap이 패키지 변경을 안 따라가 `ChatHistoryPanel` 스토리가 비교 범위 밖이었다. 대비값은 헤드리스로 직접 쟀지만(1.087 → 1.174, 회귀 0) **전체 시각 판정은 머지 전 `workflow_dispatch`가 정본**이라 아직 안 돌았다
- [ ] **`Callout` `warning` × `action`만 피그마에 없다(범위 축소)** — 8/19 실측: `success`는 피어가 오늘 신설했고(`00e302d`) 계약도 "`warning`의 action 조합만 아직 코드에만 있다"로 갱신됐다. 원 항목의 짝(`Row/Item` 통합)은 같은 날 종결. 소비처가 생기면 피그마부터 그린다

- [ ] **§4.7 「결과 3초 자동 전환」 삭제 — 발신만 남았다** — 8/19 주미님 확인("있어도 안 이상한가?"). **어색해서 뺀 게 아니라 기계가 사라진 것**이다: 원문이 「결과 약 3초 → **카드 넘김 애니메이션과 함께** 자동 전환」이라 3초는 넘김 모션에 붙은 시간이었고, v3는 안 넘기고 쌓는다. 되살리면 "3초 뒤 자동 스크롤"이 되는데 ①사연 실측상 3초에 못 읽고(중앙값 147자=393폭 536 · p90 277자=779) ②앞 카드가 위로 밀려 남아 읽는 중 화면이 움직이고 ③이동은 이미 「다음 고민」이 맡는다. 발신 문구 초안은 아래 「기획·개발 발신 초안」 절. 대기가 아니라 **아무도 안 보낸 상태**
- [x] ~~**`DeepWaitVote` 런타임 인터랙션 육안 확인**~~ — **8/19 주미님 판정 완료**(스토리북 직링크로 확인). ①최대 사연에서 투표 후 「다음 고민」까지 스크롤 — **납득된다.** 다만 "텍스트간 줄바꿈 처리가 되어야 할 듯, 가독성이 안 좋아" → 별건으로 **로케일 줄바꿈 규칙 신설**(`3020c28`, `:lang(ko)` keep-all)로 처리했다. ②완성 알림 · Scroll FAB 겹침 — **어수선하긴 하나 문제 없음.** "콜아웃 떴는데도 위로 올라가서 보려는 사람에게만 노출되는 것"이라 판단
- [x] ~~**건너뛴 카드의 상태가 정해지지 않았다**~~ — **8/19 주미님 확정: "못눌러. 건너뛰면 = 결과보기야."** 건너뛴 카드도 투표한 카드와 **같은 경로**를 탄다 — `default` → (건너뛰기) → `result` → (다음 카드로 이동) → `answered`. 못 누르는 건 `answered`가 이미 그렇다. 차이는 **선택지 표시 유무 하나**뿐(투표는 고른 칸 표시, 건너뛰기는 아무것도 안 골라진 채 AI 분포만) — 선택지 상태에서 저절로 갈리므로 **새 상태 0개**로 닫혔다
- [ ] **스토리북에 `answered` 미반영** — 스토리는 result까지만 구현돼 있다. 지나간 카드의 표면 강등·버튼 제거를 넣어야 피그마와 같은 이야기가 된다

**줄바꿈 규칙 트랙 (2026-08-19)**

- [ ] **제품 앱에 `lang` 속성 배선 — 개발 전달** — `:lang(ko)` 줄바꿈 규칙은 소비처가 `lang`을 세팅해야 동작한다. 없으면 종전과 동일하게 렌더되므로 깨지진 않지만 규칙이 통째로 죽는다. 스토리북은 8/19에 배선했고 제품 앱은 미확인


**홈 PC 폭 기준 트랙 (별도 세션)**

- [ ] **레일 접힘 100px 미끄러짐 — 모션으로 푼다(Storybook)** — 주미님 "스르르 하면 좀 거슬리겠다". **실측으로 전제가 바뀌었다: 홈만의 문제가 아니다.** 채팅도 1440에서 레일을 접으면 Main 1164→1364라 스트림 컬럼이 x 182→282로 **같은 100px** 움직인다(이미 그렇게 서 있는 화면). 홈에서 눈에 띈 건 히어로가 한 덩어리라 통째로 밀려서다. 그리고 **지금 구조는 홈→채팅에서 컴포저가 0px 움직인다**(둘 다 Main 중앙 좌우 182) — 홈만 뷰포트 중앙으로 바꾸면 접힘은 안 움직이는 대신 전송마다 138px 튄다. 즉 착지 위치는 지금이 맞고 남는 건 **전환 중 이동감**이라 LAYOUT 09의 「한 모션」(사이드바 접힘·컬럼 리플로우를 한 전환에 처리)과 같은 계열로 본다. 정적 시안으로 판정 불가
- [ ] **칩 행 `clipsContent`가 인스턴스 19곳에서 꺼져 있다 (EdgeBand 트랙)** — **8/19 재실측으로 좁혔다.** 진짜 마스터는 `QuickStart/Group` **`9469:127684`**(DS 소유)이고 chips가 `clipsContent=true` + `overflowDirection=HORIZONTAL`, 즉 계약대로 가로 스크롤 컨테이너다. ⚠️ 내가 처음 적은 `9253:159339`는 `Attachment/Lightbox/QuickStart/Group` **프록시라 비교 대상이 아니었다**(그것도 true라 결론만 우연히 같았다). 제품 페이지 인스턴스 **61개 중 19개가 `clipsContent`를 명시적으로 false로 override**하고 있고(`overrides`의 `overriddenFields`로 확정), 나머지 42개는 중첩 인스턴스라 그 19개를 상속받은 것이다. **스크롤 방향은 켜져 있는데 클립이 꺼져 있어 넘치면 잘리지도 스크롤되지도 않고 삐져나온다.** 웹 800에서는 여유 414~479라 안 드러나지만 모바일 대인관계 행은 여유 7px이다. 되돌릴지(=마스터 값으로 리셋) 의도한 것인지는 EdgeBand 시안을 만든 세션 판단
- [ ] **PR 미개설** — 커밋 2건은 `origin/feat/edge-scrim-exploration`에 push 완료. EdgeBand 작업을 마무리하는 세션이 같이 연다

---

**⤵ 이월분은 여기 없다 — 정본은 `docs/plans/31-open-items-backlog-2026-08.md`** (2026-08-19 이관, 55건)

> 2026-08-18 이전 트랙의 꼬리 55건을 백로그 문서로 옮겼다. **날짜를 넘기는 항목의 정본은 그 문서다.** 이 로그의 「미해결 항목」에는 오늘 연 트랙만 남긴다. plan 번호가 붙은 트랙(28·20·29·30)은 각 plan이 정본이다.

**Deep 대기 투표 완성 알림 트랙 (별도 세션)**

- [ ] **걷힘 방식 미정 — 차주 인터랙션 디자이너 논의** — 시도 3번이 다 반려됐다(`instant`="샥 사라진다", `fade`="얼레벌레 사라진다", `push`=판정 대기). 피그마 `인터랙션 고민` 섹션에 미정 배너·컷 3장·안건 주석. **세 번째도 어색하면 처리가 아니라 배치가 원인** — 시트/오버레이로 빼면 사라짐 문제 자체가 소멸한다(비용: 누적이 스크롤 이동 수단이라 v3를 되돌려야 함)
- [ ] **판정 2건(주미님)** — 이동 순서(`anchor-first` vs `clear-first`) · 등장 커브(`slide`/`fade`/`none`). Chromatic 컨트롤로 비교. FAB 겹침은 `hide` 확정
- [ ] **`VerdictBarMotion` width 393 고정** — v3 판정 기기 360에서 33px 넘친다. 완성 알림 스토리만 393으로 두고 피했다. 히어로의 360 대응은 리포트 히어로 트랙 몫
- [ ] **리포트 아래 빈 공간** — 히어로만 있고 실제 리포트(요약·레전드·참여자·추천 질문)가 없다. "리포트가 화면을 채운다"는 전제는 완성 룩에서 다시 봐야 한다
- [ ] **Callout 하단 배치 계약 표현 정정** — 계약은 "컴포저 상단 +8"인데 프로토타입은 fade **아래**에 놨다. fade 위에 놓으면 카드가 Callout 상단에서 잘린다. `BottomBar`에 알림 슬롯이 필요하다(지금은 FadeZone→Input→HomeIndicator로 닫혀 있어 낄 자리가 없다)
- [ ] **SA-1a 잠금 안내가 fade 위인지 아래인지 실측** — 같은 하단 슬롯이라 어긋나 있으면 거기도 같은 잘림이 있다

---

### 함정 모음

- **기록된 근거도 "어느 상황의 근거인지"를 다시 본다** — 「카드 면을 미리 그리면 약속처럼 읽힌다」는 근거가 피그마 노트·스토리 주석·plan·커밋 메시지 네 곳에 같은 날 적혀 있었고, 나는 그걸 읽고 그대로 읊었다. 그런데 그건 **생성 대기**의 논리였다 — 재열람은 리포트가 서버에 이미 있고 사용자가 그 카드를 본 적도 있어서 전제가 정반대다. 주미님 한 줄("아 근데 이미 레포트가 등장했던건데")로 무너졌다. **근거가 여러 곳에 적혀 있다는 건 합의의 증거이지 참의 증거가 아니다** — 같은 세션이 같은 문장을 네 군데 복사했을 뿐이다.
- **스켈레톤 색이 배경색과 같으면 "안 그린 것"과 구분이 안 된다** — 컷①의 질문 스켈레톤이 스토리북에서 안 보여 렌더 실패를 의심했는데, DOM엔 240×64로 멀쩡히 있었고 `bg-surface-muted`가 채팅 배경과 **같은 #F2F4F8**이었다. 카드 안에서는 흰 면 위라 보이니 "카드 안만 되네"로 오진하기 쉽다. **안 보이면 DOM 존재부터 확인하고, 있으면 자기 색과 부모 배경색을 같이 잰다.**
- **계약 문서가 적어 둔 "토큰 대응"이 틀린 문장일 수 있다** — `skeleton.md`가 "바는 `surface-muted`(Figma `neutral/100` 대응)"이라고 못 박아 뒀는데 **대응이 아니었다**(`surface-muted` = `neutral/50`). 괄호 안 대응 관계는 값을 대조한 흔적 없이 적힌 주장이라, 그대로 믿으면 원인을 코드가 아니라 엉뚱한 데서 찾는다. **토큰 이름 매핑은 값으로 검산한다.**
- **"한 단계 진한 회색"으로 외우면 다크에서 틀린다** — `neutral/100`은 배경과의 명암 방향이 **모드마다 뒤집힌다**(라이트 #EAEDF2 = 배경보다 어둡게 / 다크 #191A1D = 배경보다 밝게). 라이트만 보고 `surface-inset`(라이트 #EAEDF2)을 후보로 잡을 뻔했는데 그건 다크가 #000000이라 방향이 반대다. **모드 있는 램프는 두 모드를 다 재고 고른다.**
- **TurboSnap은 패키지 변경을 안 따라간다** — `Skeleton.tsx`와 토큰을 고쳤는데 Chromatic Build 725가 스냅샷을 **2장만** 찍었다(바뀐 스토리 파일만). 실제 소비처인 `ChatHistoryPanel` 스토리는 비교 범위 밖이었다. "빌드 통과 = 전수 비교"로 읽으면 안 되고, 전체 판정은 `workflow_dispatch`가 정본이다.
- **컨테이너 거터 + 컴포넌트 인셋이 겹쳐 이중으로 먹는다** — 스토리 `Phone`이 스크롤 영역에 `px-4`를 주고 `ReportDeep`도 `surface-inset` 16을 갖고 있어 면이 361이 아니라 **329**로 그려졌다. 스켈레톤만이 아니라 **같은 스토리의 진짜 리포트도** 같이 틀려 있어서 둘이 서로 일치하는 바람에 눈으로는 안 잡혔다. 실제 폭은 피그마 인스턴스를 전수 실측해서(393→361 · 360→328 · 560→528) 확정했다. **"둘이 같다"는 "둘이 맞다"가 아니다.**
- **`Pretendard`(static)가 없는 MCP 서버가 있다** — `claude_ai_Figma`에서 텍스트를 쓰려니 "font family Pretendard does not exist"로 튕겼다(`Pretendard Variable`만 있음). 폰트 설치·재시작 문제가 아니라 **서버 쪽 목록 차이**라, `figma`(데스크톱) 서버로 바꾸니 그대로 통과. 판별은 `listAvailableFontsAsync()`에 있느냐 — 있는데 안 로드되면 그건 세그먼트 전량 로드 문제로 갈래가 다르다.
- **보정 델타를 fill로만 채취하면 의도가 안 옮겨진다** — 주미님이 마커 색을 보정한 걸 다른 판 5곳에 복제하면서 fill과 체크 stroke만 봤다. 실제 의도는 색이 아니라 **마커 외곽의 배경색(`neutral/50`) stroke 1.5로 뒤의 rail을 끊어 띄우는 것**이었고, 그 stroke를 통째로 흘렸다("일부러 외곽에 배경색과 같은 스트로크를 줘서 띄워진 느낌을 준건데 그건 뺏네"). 스피너 `track`의 같은 stroke(OUTSIDE)도 함께 누락. **델타 복제는 fill·stroke·align·두께·effect를 노드 단위로 전량 대조한다** — 색만 맞고 "왜 다르지"가 되는 자리다.
- **레퍼런스는 구조와 스케일을 분리해서 옮긴다** — 1차 조립은 마커·rail·슬롯 구조만 옮기고 스케일·위계를 현행에 두어 "현행에 파란 점 붙인 것"이 됐다("디자인 개구린데"). 2차로 토스 수치를 그대로(마커 44·행간 24) 옮기니 이번엔 "과해". 원본은 **전면 리포트 히어로**고 우리 토글은 **스트림 위 보조 상태 표시**라 위상이 다르다. 3차 = 시각 언어는 그대로 두고 스케일만 우리 위상에 맞춰(마커 18·행간 14) 안착.
- **숨김 노드를 텍스트 donor로 클론하면 hidden이 딸려온다** — `StepText`(visible=false)를 donor로 써서 완료 줄 텍스트가 전부 안 보이는 컷이 나왔다. 구조 리턴값은 정상이라 스크린샷으로만 드러난다. 클론 직후 `visible` 확인.
- **`node.query()` 셀렉터에 `/`가 들어간 이름은 못 쓴다** — `[name=Marker/done]`이 `Invalid selector: unexpected character '/'`로 튕긴다. 슬래시 명명 규칙(`Marker/done`·`Step/done`)을 쓰는 파일에서는 `findAll(n => n.name === ...)`로 우회.
- **"섹션 자식 목록에 없다"는 삭제가 아니다** — 8/14 보드 2장이 섹션 최상위에 안 잡혀 사라진 줄 알았는데, **페이지 직속**이라 목록에 안 나온 것이고 좌표상 새 보드와 겹쳐 z-order로 가려져 있었다. 부모가 아니라 좌표로 판정해야 보인다.
- **8/18 로그가 이 보드를 `plan-29`로 적었다 — plan-29는 settings 문서다** — 실제로 사고과정 탐색을 기록한 문서는 `plan-27`이고, 어제까지 이 보드는 **어느 plan에도 기록이 없었다**(오늘 추가). 인수인계에서 plan 번호를 근거로 찾으면 엉뚱한 문서를 연다.
- **"깨진 문구"처럼 보이는 게 실은 데이터일 수 있다** — `모델 선택 리스트` 제목 `옮긴다`를 결함으로 보고했는데, 그 시트가 선택지 "옮긴다"를 지지한 모델 목록이라 **선택지 텍스트가 제목 자리에 들어간 정상 상태**였다(주미님 정정). 동적 데이터 자리는 마스터에 박힌 값이 곧 예시라 결함과 구분이 안 된다. **인스턴스 override 0은 "전부 같다"이지 "정상이다"가 아니다** — 나는 그 0을 정상 근거로 썼다.
- **원격 부품이라고 다 우리 DS는 아니다** — `mc.remote === true`는 "어떤 라이브러리에서 왔다"일 뿐이다. 모델 리스트 안 `ticker / tsla`는 **[VW] Design System** 소유라 우리 DS 페이지 전수 스캔(`✅ Components` 689개·`✅ Icons` 2085개)에 안 나왔다. remote를 "DS 것"으로 읽으면 외부 의존을 못 본다.
- **`importComponentByKeyAsync` 성공은 "이 파일 소유"의 증거가 아니다** — DS 파일에서 `Sheet/Intro` 키를 import하니 `remote=true` 사본이 잡혔다(같은 파일에 로컬 마스터가 따로 있는데도). 소유 판정은 페이지 스캔으로, import는 "가져다 쓸 수 있나"로만 쓴다.
- **내가 문서에 적어둔 근거가 측정으로 두 개 뒤집혔다** — 8/18 「본문은 제품 소유」의 근거였던 ①재현 손실은 이 2개엔 해당 없고(로컬 의존 0) ②"문구 변수가 제품 로컬이라 끊긴다"는 추정도 틀렸다(`strings` KR/JP/EN 74개는 **DS 소유**). 결론을 인용하기 전에 그 결론이 선 조건이 이 대상에도 성립하는지 다시 잰다.
- **머지 러시 다음 날, 미해결 목록은 절반이 죽은 전제다** — 8/18 92건을 실측 대조하니 완료 9 · 기록성 5 · 중복 12가 나왔다(전체의 29%). 밤사이 PR 11건이 머지되고 0.12.0이 나가면서 항목이 선 조건 자체가 사라졌는데 목록은 그대로였다. **목록을 이어받기 전에 PR·git 상태부터 재는 게 순서다** — 안 그러면 이미 끝난 일을 작업으로 착수한다. AGENTS.md의 "감사표는 count만 보고 일괄 수정하지 않는다"가 시간 경과에도 그대로 적용된다.
- **"이미 덮었다"는 커밋 도달부터 확인한다** — 첨부 칩 재게시 항목을 "Chromatic 빌드 695·698이 찍었으니 종결"로 닫으려다 반박당했다. 그 변경을 담은 `b9aa81a`·`4c3de2a`가 **아직 main 미도달**이라 두 빌드는 그 변경 없이 찍힌 것이었다(`git cherry` `+` · `origin/main`의 `semantic.tokens.json`에 해당 토큰 0건). 빌드가 돌았다는 사실과 그 빌드가 내 변경을 봤다는 사실은 다른 명제다.
- **amend로 밀려난 해시가 「구출 목록」에 유령으로 남는다** — 8/18 로그가 plan-30 커밋을 `0e8aee6`으로 적었는데 `git merge-base --is-ancestor`도 `git branch --contains`도 0건이었다. 같은 메시지의 실제 커밋은 `a95d6ff`다. 커밋 메시지가 같아 눈으로는 안 갈린다 — **구출용 해시 목록은 적을 때 도달성을 한 번 재고 적는다.**
- **삭제 편집이 앞 항목의 줄바꿈을 삼킨다** — 목록에서 항목을 지울 때 `old_string`을 `\n- [ ] …`로 시작하면 **앞 항목을 끝내던 줄바꿈이 사라져** 두 항목이 한 줄로 붙는다. 5곳에서 났고 편집 도구는 전부 성공으로 리턴했다. 마크다운이라 렌더도 조용히 깨진다. **삭제 후엔 `- [ ]`가 행 첫 칸이 아닌 자리에 있는지 정규식으로 훑는다.**
- **push 직후 raw는 옛 내용을 준다 — 실측 270초** — GitHub raw 캐시(약 5분, 쿼리스트링 무시) 때문에 push하고 바로 확인하면 "반영 안 됨"으로 보인다. 코드나 파일을 또 고치기 전에 **API(`gh api contents`)로 먼저 가른다** — API는 캐시가 없어 즉시 새 내용을 준다. 이번엔 45초 간격 폴링으로 270초에 갱신되는 걸 확인했다.
- **릴리스 PR diff만 보면 감사가 반쪽이다** — `#170`(0.12.1)은 자기가 소비하는 changeset 하나만 보여줬고 그건 정확했다. 그런데 **changeset 없이 들어온 공개 토큰 2건**(`skeleton.bar.radius` 4→8 · `chat-history-panel.skeleton-bar-height` 20→27)은 PR 어디에도 안 나타난다. changeset은 **버전 범프와 CHANGELOG 문구만** 정하고 발행 코드는 그 시점 main 전체이기 때문이다. 즉 **배포는 되는데 기록에만 없는** 변경이 생긴다. 감사할 때는 `git diff <직전 릴리스 커밋> origin/main -- packages/*/src`를 같이 떠야 한다.
- **같은 파일 끝을 쓰는 PR이 여럿이면 충돌 해소는 1회로 안 끝난다** — #163·#160을 각각 풀어 둘 다 `MERGEABLE`로 만들어놨는데, **#163을 머지하는 순간 #160이 다시 `CONFLICTING`이 됐다.** 스택 PR도 아니고 서로 참조도 없지만 둘 다 `plan-10` 파일 **끝**에 새 절을 덧붙이는 구조라 앞이 들어가면 뒤가 어긋난다. 30분 전에 푼 충돌이 되살아난 셈이다. **해소 시점이 아니라 머지 시점 기준으로 봐야 한다** — 여러 개면 하나 머지할 때마다 나머지를 다시 재고, 가능하면 한 번에 rebase한다.
- **`gh pr merge`는 draft를 조용히 거절하지 않는다 — 하지만 목록에서는 안 보인다** — #169가 `MERGEABLE/UNSTABLE`로 보여서 머지 가능한 줄 알았는데 실제로는 draft였다(`Pull Request is still a draft`). `gh pr list`의 기본 출력에도 `mergeable` 필드에도 draft 여부가 안 나온다. **`--json isDraft`를 같이 봐야 한다.**
- **충돌 자동 병합이 문서를 두 벌로 만들어도 조용하다** — #160에서 `chat-history-panel.md`가 충돌 없이 자동 병합됐는데, 결과물에 **옛 `loading` 계약 문단과 새 `status` 계약 문단이 나란히** 남아 있었다. 코드는 충돌이 나서 눈에 띄었지만 문서는 서로 다른 줄이라 git이 둘 다 살렸고, 게이트도 전부 통과한다(문법이 깨진 게 아니니까). **충돌 해소 뒤에는 폐기한 API 이름으로 레포를 한 번 훑는다** — `grep -rn loadingListLabel`이 코드 0건 · 문서 2건을 잡아냈다.
- **같은 기능이 두 갈래로 구현됐을 때 순서는 커밋 타임스탬프로만 갈린다** — #160의 `loading` 불리언과 main의 `status` 축은 둘 다 8/18 작업이라 로그만 봐선 어느 쪽이 최신인지 안 보였다. `git log --date=iso`로 재보니 브랜치 18:15 · main 22:34였다. **"내 브랜치가 최신"이라는 기본 가정이 여기서 깨진다** — 브랜치를 오래 들고 있으면 main이 그 사이 같은 자리를 더 낫게 고쳐놓는다.
- **`storybook-map.md`에는 파이프 표를 새로 만들지 않는다** — 이동 기록을 3열 표(`| 이동 | 대상 | 사유 |`)로 적었더니 `validate-design-system.sh`가 그걸 **스토리 매핑 표로 파싱해** 에러 6건을 냈다("Storybook title은 Components/*로 시작해야 합니다: 레포 내 유일한 언급이…"). 검증기는 이 파일의 모든 `| a | b | c |` 행을 컴포넌트↔스토리 매핑으로 읽는다. 서술용 표가 필요하면 **목록으로 쓴다.**
- **`use_figma` 스크립트의 "성공" 리턴값은 그 호출 안에서의 사실일 뿐이다** — 13건을 세 번에 나눠 옮기며 매번 `+N/−N`과 "딸려온 노드 0"을 자체 검산해 전부 통과했는데, 그래도 **이동 전에 썼던 페이지 스캔을 그대로 한 번 더 돌려** 노드 ID 단위로 대조했다. 호출 단위 검산은 "이 호출이 의도대로 됐나"를 보고, 재스캔은 "페이지 전체가 의도대로 됐나"를 본다. 되돌리기 번거로운 write에서는 둘 다 한다.
- **낡은 브랜치를 레포 정본으로 읽고 감사하면 결론이 뒤집힌다** — 계약 버킷 감사를 워킹트리(`feat/edge-scrim-exploration`) 기준으로 돌렸는데 그게 **`origin/main`보다 28커밋 뒤**였다. 그 결과 `Sheet/Header`·`Sheet/Intro`를 "포팅 안 됨"으로, `Callout`을 "계약 제외인데 잘못 올라왔다"로 오판해 주미님께 보고했다. 실제로는 PR #153이 8/18 머지돼 계약·코드·스토리가 전부 main에 있었고, Callout은 같은 날 의도적으로 복귀시킨 건이었다. **"레포에 없다"를 근거로 삼는 감사는 `git worktree add --detach /tmp/... origin/main`으로 정본 사본을 떠서 한다** — 브랜치가 앞선 게 아니라 뒤처졌을 수도 있고, 뒤처진 쪽은 "아직 안 됨"과 구분이 안 된다.
- **브랜치가 앞선 것도 같은 오판을 만든다(방향만 반대)** — `design-system/components/modal-shell.md`는 브랜치에만 있고 main엔 없다. main만 봤으면 `Modal Shell`을 "근거 문서조차 없음"으로 과하게 적었을 것이다. **판정은 main과 브랜치 양쪽을 다 재고, 어느 쪽에서 나온 근거인지 밝혀 적는다.**
- **피그마 좌표가 소속을 말해준다 — 이름만 보면 못 가른다** — `Row/Item`·`ContextMenu/Item`이 어디 소속인지 문서로는 안 나왔는데, `Dropdown Menu`(x=119, y=2074) 옆 x=440 **같은 y**, `Context Menu`(y=2615) 옆 **같은 y**라는 배치가 "컨테이너 왼쪽 · 그 행 마스터 오른쪽" 관례를 그대로 보여줬다. 소속 판정이 막히면 metadata의 x/y를 먼저 본다.
- **워크플로 팬아웃에서 검증을 뒤에 몰면 리밋에 통째로 노출된다** — 감사 5개(전수 실측)는 다 끝났는데 그 뒤에 붙은 **검증 19개가 세션 리밋으로 전멸**했다. 파이프라인이 앞단부터 채워지는 구조라 리밋은 항상 뒷단을 친다. 결과가 남은 건 앞단뿐이라 **가장 중요한 반증 단계를 못 받았고**, 남은 15건은 메인 루프에서 직접 grep·스크린샷으로 다시 쟀다. 되돌리기 어려운 판정이 걸린 단계는 뒤가 아니라 앞에 두거나, 팬아웃 폭을 줄여 앞뒤가 같이 끝나게 짠다.
- **같은 감사자가 같은 종류를 다르게 판정했다** — `Edge Band — 사용 예시`(`4483:2102`)는 "부모 계약이 포섭"으로 ok인데, 똑같이 부모 컴포넌트 바로 아래 붙은 미리보기 보드인 `Light preview`·`Dark preview`(`3192:1404`·`1413`, Prompt Pill 바로 밑)는 오분류로 냈다. 갈린 건 실체가 아니라 **이름이 역할을 밝히느냐**였다. 예시 프레임은 버킷을 옮길 게 아니라 이름을 맞추는 쪽이 맞다.
- **피그마 섹션 ID는 세션 중에도 바뀐다** — 시안 섹션(`9371:23718`)을 문서 3곳에 인용해 커밋했는데, 주미님이 그 사이 섹션을 다시 만들어(`9383:113289`) 옛 ID가 통째로 사라졌다. 자식 노드 ID는 그대로라 `use_figma`는 정상 동작하는데 `get_screenshot`만 "node not found"로 튕겨서 처음엔 도구 문제로 보였다. **부모가 없다고 나오면 자식으로 조상 체인을 거슬러 올라가 현재 부모를 찾는다** — 인용은 컨테이너보다 자식 노드가 오래 산다.
- **`use_figma`의 clone은 appendChild 시점에 폰트를 요구한다** — 텍스트가 든 노드를 클론해 붙이면 `unloaded font "Pretendard Regular"`로 죽는다. 내 스크립트는 클론 뒤에 폰트를 로드하고 있어서 순서가 틀렸다. **클론 대상 안의 폰트를 스크립트 맨 앞에서 로드한다** — 실패한 스크립트는 원자적이라 파일은 무사하지만 한 번 왕복을 버린다.
- **Tailwind `className`으로 `h-*`/`w-*`를 덮으면 진다** — `Skeleton`이 기본 `h-4 w-full`이라 `className="h-9 w-[225px]"`을 얹어도 CSS 출력 순서가 이기는 쪽을 정한다(클래스 나열 순서가 아니다). 패키지가 이미 쓰고 있는 방식대로 `style={{ height, width }}`로 줘야 확실하다(`ChatHistoryPanel.tsx:365`).
- **계약 문서가 예고해 둔 "없음"을 못 보고 손조립할 뻔했다** — `Callout` 계약에 "나머지 톤의 action 조합은 코드에만 있고 피그마엔 없다. 필요해지면 피그마부터 그린다"가 적혀 있었다. 코드에서 `tone="error" variant="action"`이 멀쩡히 동작하니 피그마에도 있으려니 했다가, 세트를 열어보고서야 `Type=info` 한 장뿐인 걸 봤다. **코드에 조합이 있다고 피그마에 그 variant가 있는 게 아니다** — 코드는 조합을 열어두고 피그마는 그린 것만 있다.
- **"게시 = 수락"이 아닌 것의 반대편도 챙겨야 한다** — 새 variant를 게시한 뒤 제품 파일에서 `importComponentByKeyAsync` → `createInstance`가 **한 번에 됐다.** 「업데이트 수락」이 필요한 건 이미 그 마스터를 물고 있는 인스턴스를 갈아끼울 때고, 새로 심는 건 게시만으로 충분하다. 수락 절차를 떠올리며 미리 겁먹으면 안 해도 될 왕복이 생긴다.

- **인스턴스 자식은 크기 override가 조용히 무시된다** — 진행바 fill을 컷마다 손으로 밀어봤지만 안 먹었다(6컷은 `fill`이 FIXED라 sizing 변경조차 거부). 비율은 부모 패딩으로 잡거나, 값이 유한하면 아예 컴포넌트 축으로 올리는 게 답이다.
- **오토레이아웃이 아닌 컴포넌트 세트에 변형을 append하면 기존 변형 위에 정확히 포개진다** — 리턴값(이름·개수)은 정상이라 스크린샷으로만 드러난다. 세트 프레임도 자동으로 안 커져서 새 열이 프레임 밖에 남는다.
- **`figma.skipInvisibleInstanceChildren`을 명시 안 하면 인스턴스 내부 텍스트가 통째로 안 잡힌다** — DS 세트 5변형이 전부 `labels: []`로 나와 "마스터에 버튼 텍스트가 없다"고 오진할 뻔했다. 0건은 "없다"가 아니라 "플래그부터 의심"이다.
- **variant를 바꾸면 하위 인스턴스가 교체돼 텍스트 override가 따라오지 않는다** — `default→answered`에서 선택지가 default에서 selected/unselected로 바뀌며 사연 문구가 기본값("선택지")으로 돌아갔다. 바꾼 뒤 문구를 다시 확인할 것.

- **오토레이아웃이 리플로우를 한 박자 놓친다** — 같은 스크립트로 4 variant 헤더에 아이콘을 넣었는데 `Chat·Quick 닫힘` 하나만 `Label`이 x=0에 남아 아이콘과 겹쳤다(나머지 셋은 24로 밀림). 속성값은 전부 정상이라 읽기로는 안 잡히고 **스크린샷으로만 드러났다.** `appendChild` → `insertChild(0)`로 자식을 뺐다 넣어 리플로우를 강제하니 풀렸다. 여러 variant를 한 번에 손대면 마지막에 좌표를 검산한다.
- **1:1 렌더로 색을 판정하지 말 것** — 361폭 렌더에서 완료 마커가 "속 빈 링 + 회색 체크"로 보여 채움이 안 먹은 줄 알고 진단에 들어갔다. 값은 처음부터 정확했고(`#c9ccd2` 채움 · 흰 체크 · rail `#d9dce2`) `screenshot({scale:3})`으로 뽑으니 의도대로였다. **2px rail과 1.5px 외곽선은 1:1에서 사실상 안 보인다.**
- **override로 덮은 정합은 마스터의 구멍을 숨긴다** — 8/14 진행바 건이 "실측 확인"으로 닫혔는데, 실제로는 제품 파일 인스턴스 override가 가리고 있었고 **마스터의 Deep 펼침 `Progress`는 `단계문구`에 안 묶여 있었다.** 라이브러리 수락 한 번에 인스턴스 9개로 되살아났다. **상태 바인딩은 인스턴스가 아니라 마스터에서 확인한다.**
- **내 이전 기록을 원문 대신 인용했다** — 도착 줄을 "모델명 vs 에이전트명" 양자택일로 물었는데, plan-27에 내가 적어둔 "문구표엔 모델명만 있다"가 오독이었다. 기획서를 열었으면 `{AI 이름}`이고 참여자가 모델+에이전트로 섞인다는 걸 바로 봤다. **선택지를 만들기 전에 원문을 연다** — 내 요약은 근거가 아니다.
- **접힘 시안은 패널 높이가 FIXED면 조용히 빈다** — `web / home`의 사이드바 패널이 세로 FIXED 1008인데 1440에서는 1024−16과 우연히 같아 정상으로 보인다. 768로 리사이즈하면 1008이 남아 `Frame 1`의 counter-axis CENTER 정렬로 위아래로 넘치고, 메뉴는 y −112 · 아바타는 y 812로 **둘 다 화면 밖**에 나가 rail이 백지로 렌더된다. 에러도 리턴값 이상도 없고 스크린샷으로만 드러났다. 해법은 `layoutSizingVertical = 'FILL'`. **우연히 맞는 값은 복제하는 순간 틀린 값이 된다.**
- **예시 행이 정확히 꽉 차 있으면 4번째는 보드를 뚫는다** — Guidelines L06 예시 행이 440×3 + 40×2 = 1400으로 딱 맞아서, HUG인 채로 카드를 추가하면 1880이 되어 보드 1608 컬럼을 넘는다. FIXED 1400 + `layoutWrap=WRAP`으로 먼저 바꾼 뒤 붙여야 한다.
- **DS 파일 노드 조회는 페이지를 로드하기 전엔 전부 not found다** — 계약 md가 인용한 노드 8개를 DS 파일에서 찾으니 하나도 안 잡혀 "8개 다 제품 파일 id"로 결론 낼 뻔했다. `setCurrentPageAsync` 없이 돌리면 첫 페이지 말고는 로드가 안 돼 있어 `getNodeByIdAsync`가 그냥 null을 준다. 페이지 목록을 먼저 받아 페이지별로 다시 돌리니 절반이 살아 있었다. **"DS에 없다"는 페이지를 다 열어보고 나서만 할 수 있는 말이다.**
- **대칭 노드에서 값을 복구하면 그 노드의 오류까지 복제한다** — L06 `Stream 800` 배경을 잃고 옆 `Card 520` 값으로 되살렸는데, `Card 520`도 같은 오류(콘텐츠인데 크롬 색)였다. 한 노드가 아니라 **같은 역할 슬롯 전수**(다른 섹션 8개)와 대조해야 관례가 보인다.- **`constraints`는 라이브러리 업데이트로 기존 인스턴스에 안 내려간다** — 마스터에 `STRETCH`를 넣고 게시·수락까지 했는데 인스턴스는 계속 `MIN`이었다. 수락 실패로 오진할 뻔했는데, **새 인스턴스를 하나 만들어 보니 `STRETCH`를 받았다** — 라이브러리는 정상이고 이미 놓인 인스턴스가 옛 값을 캐시하는 것이다. 직접 쓰기는 `cannot be overridden in an instance`, 자식 `resize()`는 에러 없이 무시, `resetOverrides()`도 안 고친다. **복구법은 인스턴스 재생성뿐.** 판정 순서 = ①키로 재임포트 ②기존 인스턴스 ③**새 인스턴스 대조**.
- **그 함정의 함정 — 폭이 마스터와 같으면 안 드러난다** — 마스터 393에서는 `MIN`이든 `STRETCH`든 chips가 377로 같다. 61개 중 실제로 깨진 건 열 폭이 800인 9개뿐이었고, 모바일만 보고 끝냈으면 못 봤다. **검산을 값이 아니라 식으로 돌려야 잡힌다**(`chips.width === group.width − rail`).
- **원격 프록시 노드는 게시 뒤에도 옛 이름·옛 기하로 남는다** — 제품 파일의 `9253:159339`가 게시 후에도 `Attachment/Lightbox/…` · `16/30/377/40` 그대로였다. 프록시만 보면 "수락 안 됐다"가 되는데 **실제 인스턴스는 이미 갱신돼 있었다.** 수락 판정은 프록시가 아니라 인스턴스 기하로 한다. 마스터에 쓰려다 `Cannot write to internal and read-only node`로 막힌 것도 같은 뿌리 — `remote=true`·`parent=null`이면 프록시고, 진짜 마스터는 키로 DS 파일에서 찾아야 한다.
- **클론한 텍스트의 글리프 누락은 파일 결함이 아니라 렌더 캐시** — 클론 컷에서 일본어 한자만 통째로 안 보이고 가나는 멀쩡했다. `getStyledTextSegments`로 정상 형제와 대조하니 문자열·폰트가 완전히 동일했다. 폰트 재로드 후 `characters`를 같은 값으로 재입력하니 복구됐다. **화면 말고 데이터부터 대조한다** — 스크린샷만 보고 폰트를 바꿨으면 멀쩡한 조판을 망쳤다.
- **`clipsContent=false`를 드리프트로 오진했다 — 그게 그림자를 살려두고 있었다** — 마스터는 `true`인데 인스턴스가 전부 `false`라 "고쳐야 할 드리프트"로 보고했다. 그런데 마스터 클립 박스가 칩 높이(40)에 딱 맞아서, 플래그만 켜면 홈 전 화면의 칩 그림자가 한꺼번에 잘린다. **플래그 값이 어긋난 이유를 묻기 전에 그 플래그가 켜졌을 때 무슨 일이 나는지부터 본다.**
- **절대배치로 빼면 폭 가변성이 사라진다** — 그룹 높이를 안 밀리게 하려고 `chips`를 `layoutPositioning=ABSOLUTE`로 뺐는데, `FILL`이 주던 "컨테이너 따라 늘어남"이 같이 사라져 폭이 377에 박혔다. 모바일 393에서는 안 보이고 웹 800 열에서만 드러났다. 절대배치 자식은 `constraints`로 그 성질을 되돌려야 한다.
- **위키에 이미 있는 문서를 모르고 새로 만들 뻔했다** — 「AI 도입 시 일정 산정」 문서를 제안받고 바로 초안을 잡으려 했는데, `socra-ai-workflow-wiki`의 `guide-ops.html:595`에 이미 같은 주제 섹션이 있었다(사이드바에도 노출). 새로 만들었으면 일정 산정 얘기가 둘로 갈라졌다. **문서 신설 전에 기존 사이트의 사이드바·섹션을 먼저 훑는다.**
- **진단을 원인 하나로 몰았다** — "일정 예측이 어려운 건 작업량이 아니라 도착 패턴"이라고 단정했으나 실제로는 셋이 겹쳐 있다 — ①비동기 도착 ②건당 검수 시간의 분산(AI 품질 편차가 내 시간을 결정) ③범위의 사후 확정. 주미님이 말한 "초반 비용·재작업"은 ②③인데 ①로 뭉갰다. **하나로 깔끔하게 설명되는 진단일수록 나머지를 지웠는지 다시 본다.**
- **담당자가 바뀐 줄 알았는데 원래 그 사람 일이었다** — 시각 QA를 "원래 다른 사람 몫이 디자이너에게 전가된 것"으로 분류했으나 주미님 정정 — **시각 QA는 원래 디자이너의 일**이다. 바뀐 건 담당자가 아니라 시점뿐이다. 오분류하면 처방이 "협의로 잘라낸다"로 잘못 나간다.
- **다이어그램이 문장을 그린 것뿐이었다** — 선형 5박스 vs 방사형 5노드는 "순차에서 동시로 바뀜"이라는 한 문장의 도형 번역이라 새로 알려주는 게 없었고, 트랙 이름(화면 설계·컴포넌트·문서·리서치)도 내가 지어낸 것이라 구체성이 0이었다. **재료가 구체적이지 않은 상태에서 시각화하면 오히려 더 뻔해 보인다.**
- **게시 ≠ 수락 — 제품 파일이 옛 세트 스냅샷을 든다** — DS에 variant를 신설하고 게시까지 했는데 제품 파일에서 `setProperties({Type:"success"})`가 "Unable to find a variant"로 막혔다. `importComponentByKeyAsync`로는 새 variant가 잡히는데도. 제품 파일이 라이브러리 업데이트를 받아야 7변형을 알아본다. **게시했다고 쓸 수 있는 게 아니다.**
- **한글 `textContent`로 DOM 요소를 찾으면 NFC/NFD로 조용히 실패한다** — 헤드리스 클릭 검증에서 「건너뛰기」·「리포트 보기」 버튼을 못 찾아 "렌더가 안 됐다"로 오진했다. 같은 배열을 join해 찍으면 멀쩡히 보이는데 `find`만 실패한다. `trim`·`replace(/\s+/g,'')`·`indexOf` 다 안 통한다. **구조(tone 클래스)로 찾아야 한다.**
- **Storybook 에러 UI 문구는 정상일 때도 DOM에 있다** — body 텍스트에서 `Cannot read properties`를 찾아 크래시를 판정하면 항상 true다(숨겨진 에러 패널이 미리 마운트돼 있다). 판정은 기대 콘텐츠 유무로 한다.
- **`--virtual-time-budget`에서 rAF 폴링은 스토리 마운트 전에 소진된다** — 실제 경과 시간이 거의 없어 3초 타이머를 못 넘긴다. `setInterval(50ms)` + budget 25~30초로 써야 시간이 흐른다.
- **상수를 만들어놓고 참조하지 않으면 두 안이 같아진다** — "이동 먼저"라며 `scrollToReport()` 직후 같은 프레임에서 걷힘을 시작했다. 스무스 스크롤은 300~500ms인데 걷힘이 240ms라 이동 중에 카드가 사라져 채택안과 반례가 구별이 안 됐다("두 개가 별 차이 없어 보여"). `SCROLL_SETTLE_MS`를 상수로 정의해두고 정작 쓰지 않은 자리다. **순서를 비교하는 스토리는 두 동작이 시간축에서 실제로 갈리는지부터 확인한다.**
- **브라우저 `behavior:'smooth'`는 duration을 알 수 없다** — 거리가 멀수록 빨라 보여 순간이동처럼 읽히고("너무 빨리 이동하는데?"), 더 큰 문제는 **몇 ms 걸리는지 모르니 다음 동작 시점을 맞출 수 없다는 것**이다. rAF로 직접 굴려 길이를 고정했다(`scrollMs` 기본 480 · easeOutCubic).
- **카드가 없는 흐름의 구조를 카드가 쌓이는 흐름에 그대로 옮겼다** — 1-13~1-15에서 로딩 스테이지가 리포트로 바뀌는 걸 보고 "리포트는 카드 스택보다 위"라고 판단했는데 그 프레임들엔 대기 투표 카드가 없다. v3에 옮기니 리포트가 카드 더미보다 한참 위에 생겨 사용자가 거슬러 올라가게 됐다("더 내려가야지 위로 올라가는 게 아니고?"). **채팅에서 새로 온 것은 아래다.**

---

### 회고 및 인사이트

#### AI 도구 활용

- **탐색물 조립을 전부 donor 클론으로 했다** — 색·타이포를 새로 만들지 않고 기존 노드에서 페인트 객체를 복사해 붙이는 방식이라 **변수 바인딩이 그대로 살아 있다**(하드코딩 0건). `neutral/100`처럼 파일에 없던 토큰만 `teamLibrary`에서 임포트해 `setBoundVariableForPaint`로 걸었다. 나중에 DS 마스터로 옮길 때 색을 다시 정할 필요가 없다.
- **`await node.screenshot()` 인라인 검증 루프가 판단 속도를 바꿨다** — 조립 → 즉시 스크린샷 → 눈으로 확인이 한 스크립트 안에서 돌아, "구조는 맞는데 안 보이는" 류(숨김 클론, 흰 보드 위 배경색 stroke)를 그 자리에서 잡았다. 구조 리턴값만 봤으면 셋 다 놓쳤다.
- **디자인 피드백은 수치로 되받아야 수렴한다** — "개구린데 / 과해 / 22는 크다"에 대해 매번 마커·행간·rail 수치를 명시해 되물었더니 3턴 만에 스케일이 잡혔다. 형용사로 주고받았으면 더 돌았을 자리.
- **감사를 read-only 실측 5회로 쪼갠 게 결론을 바꿨다** — 문서에 적힌 8/18 결론("본문은 제품 소유")을 그대로 인용했으면 두 후보를 같이 기각했을 텐데, 부품 소유·변수 컬렉션·override·라이브러리 출처를 따로 재니 **로그인 게이트만 조건이 다르다**는 게 나왔다. 감사표의 count가 아니라 항목별 근거를 재는 쪽.
- **전수 대조는 "인용 없는 주장은 버린다"를 규칙으로 박아야 작동한다** — 116개 항목을 섹션별 5개 에이전트로 쪼개면서, 각 항목마다 ①계약 md ②스토리 파일+`title` ③`index.ts` export ④`design-system/` 근거 문서 ⑤`storybook-map.md` 상 위치를 **파일:행 인용과 함께** 내도록 강제했다. 덕분에 "부모 계약이 포섭한다"는 주장이 진짜인지(노드 ID까지 인용) 이름만 스친 건지 사후에 가를 수 있었고, 실제로 `_SettingsTrailing`은 그 차이 때문에 확정이 아니라 미결로 남았다.
- **노드 ID와 이름을 둘 다 grep해야 포섭이 잡힌다** — 이름만 보면 `Scroll FAB`·`SendButton`·`Chart/StackBar`가 전부 "계약 없음"으로 나오는데, 각각 `bottom-bar.md`·`chat-input-bar.md`·`vote-bar.md`가 노드 ID로 포섭하고 있었다. 반대로 `Chart/ProgressBar`는 양쪽 다 0건이라 진짜 구멍이었다. **한 갈래만 훑으면 오탐과 미탐이 동시에 난다.**
- **"지워도 된다"는 판정을 스스로 하지 않고 반박 전담 에이전트에 붙였다** — 목록 정리에서 삭제 후보를 고른 뒤, 그걸 확정하지 않고 **반박을 임무로 받은 에이전트 4개**(PR·git 실측 / 기록성 판정 / 누락 스캔 / 라이브 상태)에 넘겼다. 그 결과 삭제 후보 2건이 되살아났다(첨부 칩 재게시 · 시각 판정 #161 갈래). 혼자 판정했으면 둘 다 지웠을 것이고, 지운 항목은 다음 세션이 다시 발견하기 전까지 없는 일이 된다. **되돌리기 어려운 방향(삭제)일수록 판정자와 반박자를 분리하는 게 값을 한다.**
- **정적 시안이 못 담는 축을 스토리북에 분담시켰다** — 이 건의 판단 대부분은 "언제 띄우고 언제 안 띄우느냐"인데 피그마는 "뜬 순간"만 그린다. 그래서 피그마엔 상태 3컷을 두고, 지연 임계 250ms·최소 표시 400ms는 인터랙티브 스토리로 넘겼다. 120ms 적재에서 **스켈레톤이 아예 안 뜨는 것**은 정지 화면으로는 보여줄 방법이 없다.
- **말로 설득하지 않고 컷을 만들어 판단받은 게 빨랐다** — 페이드 유무·폭 32/40/56·끝 여백 0/16/24를 전부 나란히 놓은 비교 보드를 먼저 만들었다. "40인데 박스들?" "응 갠차나 가려지니 깔끔하고 좋아"처럼 **한 줄로 결정이 떨어졌다.** 형용사로 주고받았으면 여러 턴 돌았을 자리다.
- **실측 6행이 전제를 뒤집었다** — 8/12에 정한 "마지막 칩이 걸쳐 보이는 게 스크롤 신호"를 검증하려고 KR·JP·EN 6행 칩 폭을 쟀더니, 잘린 양이 0~75px로 흩어지고 두 행은 **스크롤이 없는데도 잘려 보였다**(가짜 신호). 규칙이 틀린 게 아니라 **그 규칙이 선 조건(참여자 아바타 = 같은 크기의 원)이 칩에 성립하지 않았다.** 결론을 옮기기 전에 조건을 옮겨 재는 게 순서다.
- **질문 하나에서 시작해 규칙·시안·컴포넌트·코드까지 한 줄로 내려왔다** — "스켈레톤 생기는 경우가 있나"라는 확인 질문이었는데, 기획서 §4.9·§5.1을 근거로 갈라 보니 규칙 등재(`rules.md` 39번) → 피그마 시안 3컷 → DS variant 신설·게시 → 스토리북 참조 구현까지 필요한 게 순서대로 나왔다. **답을 문서에서 찾으면 그다음 할 일도 문서가 정해준다.**

- Figma override가 **부모 경로가 아니라 대상 노드 id 기준**이라, 도착 3줄을 새 카드 아래로 옮겼는데도 `visible` override 27개가 전부 살아남았다(27/27 실측). 구조를 갈아엎을 때 "노드를 지웠다 새로 만들지만 않으면 된다"가 판단 기준이 된다.
- **전수 스캔이 잠복 결함을 잡는다** — 96개를 훑어 `StepText`는 숨김인데 `Progress`는 보이는 9개를 찾았고, 거슬러 올라가니 8/14부터 마스터에 있던 구멍이었다. 인스턴스 몇 개 눈으로 보는 것과 전수 스캔은 다른 도구다.
- **"시각 변화 0"을 말로 하지 않고 해시로 증명했다** — 원본 프레임을 정리한 뒤 수정 전후 렌더 PNG를 각각 받아 `shasum`으로 대조하니 `26474c7e…`로 동일했다. 기하 수치 대조는 항목을 빠뜨릴 수 있는데 렌더 해시는 안 그렇다. 시각 무변경을 주장할 때 쓸 수 있는 가장 싼 증거다.
- **대화만으로 진단이 두 번 정정됐고 그게 결론을 바꿨다** — 내 초안이 ①시각 QA 담당자 이동 ②원인은 도착 패턴 하나, 두 군데서 틀렸다. 두 번째 정정이 특히 컸다 — 주미님 "불안해서 그런건가? 걍 정신이없는건데. 먼가 마무리가 안되는 느낌인거고"로 **불안이 아니라 미결**이라는 게 확정되자, 처방이 "큐를 만들어 안심시킨다"에서 "완료 정의를 갈라 미결을 장부로 꺼낸다"로 통째로 바뀌었다. 감정 원인과 구조 원인은 처방이 다르다.
- **손그림 한 장이 대화 열 턴보다 정확했다** — 기존(선형 체인) vs AX(1인 중심 방사형)를 종이에 그려 보여준 순간 구조가 확정됐다. 내가 텍스트로 "동시성", "병렬도" 같은 말로 돌던 것이 도형 두 개로 정리됐다.

- **검증 도구가 실패할 때 도구를 먼저 의심해야 했다.** 헤드리스 클릭 검증이 계속 버튼을 못 찾자 "virtual time 타이밍이 불안정하다"로 결론내고 육안 확인으로 넘겼는데, 실제 원인은 한글 NFC/NFD 매칭이었다. 그 검증을 포기한 탓에 `last` undefined 크래시를 주미님이 먼저 밟았다. **검증이 실패하면 대상이 아니라 도구부터 본다.**

#### 설계 결정

- **재열람 스켈레톤에 리포트 면을 그린다(같은 날 뒤집음)** — 면을 걷는 근거였던 "약속처럼 읽힌다"는 리포트가 아직 없을 때의 말이다. 재열람은 §5.1이 "다시 읽어 채운다 — 재생성이 아니다"라고 못 박은 자리라 리포트 존재가 확정이고, 사용자는 그 카드를 본 적도 있다. 면을 걷는 건 약속을 아끼는 게 아니라 **아는 걸 숨기는** 쪽이었다. 전환도 면이 없으면 데이터 도착 순간 면·테두리·그림자가 없던 자리에서 튀어나오는데, 면을 깔면 안쪽만 바뀐다.
- **면은 상태와 무관하게 자리를 지키되, 실패하면 통째로 사라진다** — 안쪽만 스켈레톤↔리포트로 교체한다. 재생성이 또 실패하면 면 안에 실패 상태를 만들지 않고 **면째로 사라져 직전 실패 상태로 돌아간다**(주미님 "걍 사라지면 됨"). 슬롯에 에러 층을 새로 만드는 안보다 상태 수가 하나 적다.
- **`ReportSurface` 공개 승격 — 승격 기준은 "소비처 수"가 아니라 "손으로 다시 쓰게 되느냐"** — 하루 전엔 "이 한 자리 때문에 승격할 이유 없다"로 닫았는데, 면을 그리기로 하니 화면이 면 레시피(면·테두리 2px·radius 24·그림자)를 **손으로 베껴 쓰는 것 말고 방법이 없어졌다.** 그러면 리포트와 어긋나는 게 시간문제다. `status` 축은 여전히 안 만든다 — 면은 공유하되 안쪽이 스켈레톤인지 리포트인지는 면이 알 바가 아니다.
- **접힘 헤더는 현행 유지, tick 게이지 기각** — 접힘이 기본값이라 사용자가 보는 시간의 대부분이 여기고, 그 상태는 조용한 게 맞다. 장식성을 올리는 tick은 "과해" 기준과 정면으로 어긋난다.
- **파랑은 "지금 변하는 것"에만** — 완료 마커를 `neutral/200`으로 내려 8/12 「변하는 것이 강하다」 위계를 마커까지 관통시켰다. 8/14에 열어둔 미결("완료 체크만 강조색이라 파랑이 전부 지난 것에 붙어 있다")이 같은 방향으로 닫혔다. 결과적으로 **지나간 것일수록 연해지는 단계**가 생겼다 — 마커 200 → rail 150 → 카드 100.
- **도착 슬롯 카드가 이번 탐색의 실제 수확** — 8/14에 rail 4안까지 그리고도 못 푼 "도착 줄과 완료 줄의 x가 완전히 같아 소속이 안 읽힌다"를 면 하나로 해결했다. 덤으로 도착 인원이 3명이든 7명이든 줄 수 가변을 카드가 흡수한다.
- **흰/회 반전 규칙 폐기** — 카드 색을 배경별로 뒤집던 임시 규칙을 `neutral/100` 단일 토큰으로 고정하고, 보드 배경 자체를 화면 배경 토큰(`neutral/50`)으로 바꿔 **보드에서 본 것 = 화면에서 볼 것**이 되도록 했다. 배경색 stroke 같은 트릭은 배경이 같아야만 검토가 성립한다.
- **로고·아바타는 넣지 않는다(유지)** — 상태 표시에 얼굴이 들어오면 발화로 읽혀 리포트와 위계가 섞이고, Deep 리포트 상단 프로필 캐러셀과 중복된다(`rules.md` 33번과 같은 근거).
- **인용 부호는 문구 변수가 아니라 렌더 규칙이다** — 선택지 제목은 동적 데이터라 `strings`에 못 넣고, 글리프도 로케일마다 다르다(JP `「」` · KR `''` · EN `""`). 부호는 렌더할 때 로케일 보고 붙인다.
- **제목을 `'옮긴다'를 지지한 AI` 한 문장으로 합치는 안은 버렸다** — 선택지(동적) + 뒷말(고정)을 텍스트 노드로 쪼개야 하는데 KR·JP는 어순이 같고 **EN만 뒤집힌다**(`AIs that chose "Move"`). strings 변수엔 보간이 없어 EN 모드에서 깨진다. 대신 관계 설명을 Description 한 줄이 통째로 지게 했다.
- **기록성 항목은 "지운다"가 아니라 "옮길 곳을 적고 지운다"** — 미해결 목록에 실행 액션 없는 메모가 섞여 있으면 목록이 안 줄고, 그렇다고 지우면 다음 사람이 그 파일을 열 때 알아야 할 제약이 사라진다. 그래서 삭제 대상을 두 갈래로 갈랐다 — 같은 내용이 레포 문서에 **파일:행 단위로 이미 있는 것**(5건)은 바로 지우고, 레포에 없는 것(4건)은 **「레포 문서로 이관할 4건」 블록에 대상 파일과 넣을 문장까지 적어** 남겼다. 목록에서 빠지는 것과 정보가 사라지는 것은 다른 일이다.
- **버킷 판정 기준을 문서에서 끌어왔지 내가 정하지 않았다** — `design-system/components.md:18-21`이 이미 네 버킷을 정의해 뒀다(`스토리북 계약` = 계약 md + 패키지 + `Components/*` 스토리 3종 / `계약 불필요` = 조합물, 근거 문서만). 감사는 이 정의에 대는 일이지 새 기준을 만드는 일이 아니다. 판정이 갈린 자리(예시 프레임·비공개 프리미티브)는 정의가 아직 안 다루는 회색지대라 미결로 뒀다.
- **부모 계약의 "명시 포섭"은 인정하되, 인용 수준을 본다** — `Sheet/Content Placeholder`는 `sheet-shell.md`가 children 슬롯으로, `IconBadge`는 `modal-dialog.md`가, `Icon/Brand/*` 4종은 `login-button.md`가 각각 포섭한다. 이건 자기 계약이 없어도 `스토리북 계약`에 있어도 된다. 기준은 **노드 ID까지 인용했는가** — `popover.md:33`처럼 이름+노드를 같이 적은 게 표준형이고, 이름만 스친 건 포섭으로 세지 않았다.
- **시트 본문 소유는 "본문이냐"가 아니라 "무엇이 붙어 있냐"로 가른다** — 부품·문구가 전부 DS면 DS(로그인 게이트), 제품 더미 콘텐츠나 외부 라이브러리가 붙어 있으면 제품(모델 선택 리스트). DS엔 이미 `Modal Shell` + `Modal Content /` 6종 선례가 있어 로그인 계열 본문이 **모달은 DS · 시트는 제품**으로 갈려 있는 게 지금의 비정합이다.
- **생성 대기와 적재 대기는 다른 자리다** — 이미 만들어진 리포트를 다시 여는 건 생성이 아니라 적재다(기획서 §5.1 "서버의 세션 상태를 다시 읽어 채운다 — 재생성이 아니다"). 그래서 재열람에 Deep 사고과정·진행바를 다시 돌리면 없는 생성을 하는 척이 된다. 진행률을 모르고 결과의 모양은 아니까 `rules.md` 39번 사다리의 3단계(스켈레톤)에 떨어진다.
- **리포트는 히스토리 목록처럼 높이를 약속할 수 없다** — 목록 스켈레톤이 성립한 근거는 행 높이 47 실측이었다. 리포트는 선택지 2~5개·요약 길이·참여자 수가 전부 가변이라 전체를 그리면 도착 순간 오히려 튄다. **고정 부분(아이콘 48·제목·요약·분포 바)까지만 그리고 선택지 목록은 비운다.**
- **실패 화면의 무게는 「빠져나갈 길이 있느냐」로 가른다** — 목록 로드 실패는 그 화면에서 재시도 말고 나갈 길이 없어 `Empty State` 3층이 맞고, 대화 하나를 못 읽은 건 목록 복귀·새 질문이 살아 있어 콜아웃 한 줄로 충분하다. 처음엔 대화 화면에도 3층 안을 두 개 그렸는데 주미님이 "굳이야"로 잘랐고, 그 판단을 재현 가능한 기준으로 바꿔 규칙에 넣었다.
- **스켈레톤에 리포트 글래스 면은 그리지 않는다** — 적재 중엔 리포트가 올지 안 올지도 모르는 상태라 카드 면이 곧 약속으로 읽힌다. `ReportSurface`가 비공개라 코드에서 못 쓴다는 제약이 발단이었지만, 승격하지 않고 **면을 빼는 쪽으로 피그마를 코드에 맞췄다.**
- **로딩 자리표시는 컴포넌트 축이 아니라 화면 조립이다(8/6 계약 유지)** — `ReportDeep`/`ReportQuick`에 status 축을 넣지 않았다. 스켈레톤 모양은 영역 레이아웃마다 달라서 컴포넌트가 소유하면 축이 계속 늘어난다. 대신 조립이 실제로 되는지를 스토리북 참조 구현으로 증명했다 — 패키지 export도 계약도 열지 않고 기존 원자 6개 조합으로 끝났다.

- **값이 유한한 상태는 컴포넌트 축으로, 연속값은 손조립으로.** 진행바(5단계)는 축으로 올리자 인스턴스 override 제약이 통째로 사라졌고, 분포 그래프(44/22/16/12/6)는 반대로 컴포넌트화하면 비율을 못 바꾸게 된다. 같은 "그래프"라도 값의 성격이 도구를 가른다.
- **보드는 만든 사람이 아니라 읽는 사람 기준으로 검토해야 한다.** 구조 정합만 보던 리뷰가 "왜 같은 이름의 밴드가 둘이지"라는 첫 질문을 놓쳤다. 지적받고 다시 보니 이름·번호·단계 라벨이 실제 문제였고, 내가 제안했던 치수 요약은 오히려 중복 갱신 사고를 부르는 안이라 철회했다.

- **시안 이름으로 물으면 축이 안 보인다.** 시안 1/2/4 택1을 냈더니 판단이 막혔는데("글로 보니 모르겠어"), 축으로 분해하니 차이는 ①도착 슬롯 카드 ②예정 단계 노출 둘뿐이었고 카드는 이미 채택된 상태라 **실제 미결은 불리언 하나**였다. 선택지가 셋 이상이면 축으로 쪼개지는지 먼저 본다.
- **글로 안 되는 자리는 실화면으로 옮긴다.** 설명을 더 쓰는 대신 같은 화면·같은 스케일에 그 축만 얹은 비교판을 만들었더니 바로 결정이 났다. 세로 비용(+80)도 그때 숫자로 나왔다.
- **같은 층위의 정보는 같은 형태로 맞춘다(주미님).** Chat·Quick 펼침도 Deep과 같은 마커로 통일하되 rail은 Deep에만 뒀다 — Chat은 단일 단계라 이을 것이 없다. 통일의 단위는 컴포넌트가 아니라 **정보의 층위**다.
- **홈도 콘텐츠 영역 기준 중앙으로 뒀다 — 움직임을 없애는 게 아니라 어디서 움직일지 고른 것이다.** rail을 접으면 홈 히어로 중심이 1440에서 858 → 758로 100px 이동한다. 홈만 뷰포트 중앙에 걸면 이 이동은 사라지지만 대신 전송 직후 홈 컴포저 → 채팅 컴포저에서 138px이 튄다. 둘 중 하나는 반드시 움직인다면, 사용자가 의도해서 누른 접힘 토글에서 움직이는 편이 제품의 주 동선인 전송에서 움직이는 것보다 낫다.
- **규칙이 없던 게 아니라 규칙을 보여주는 시안이 없었다** — Guidelines L06은 8/09부터 "홈 컴포저 중심 512=1024/2(Tablet 384=768/2)"로 홈을 이름까지 적고 있었는데 예시 3장이 전부 사이드바 276이라, "**콘텐츠 영역** 기준 중앙"에서 콘텐츠 영역이 실제로 바뀌는 그림이 하나도 없었다. 규칙 문서를 늘리는 대신 예시를 채우는 게 맞는 자리였다.

- **완료를 3단계로 나눈다 — `1차` / `공유 가능` / `픽스`** — 에이전트는 "기획이 그려짐"을 완료로 보고, 그 순간 남은 검수가 **어느 장부에도 없이 사람 머릿속으로 이송**된다. 그래서 모든 트랙이 80%까지 가고 100%가 하나도 없다. 에이전트는 "완료"를 단독으로 쓰지 않고 단계명 + 남은 것을 문장으로 남긴다. `픽스`는 디자이너만 선언한다.
- **업무 완료 기준과 공유 가능 시점 기준은 별개다(주미님)** — "픽스는 아니지만 그래도 공유는 가능한" 중간 지점이 필요하다는 정리에서 3단계가 나왔다. 이게 서면 **디자이너 품질 검토가 팀 전체의 병목에서 빠진다** — 공유 가능 시점에 FE 착수·리뷰가 시작되고 폴리시는 그 뒤로 병렬 진행한다.
- **일정은 두 날짜로 말한다** — 점 추정("화면 1개 2.5일")은 선형 전제에서만 성립한다. 방사형에서는 `공유 가능일`만 약속하고 `픽스일`은 구간으로 둔다. 날짜가 박힌 일(데모·브랜드 구간)은 애초에 병렬 밖으로 빼서 선형으로 처리한다.
- **공유 라벨은 선택이 아니라 장치다** — 「구조·플로우 픽스 / 비주얼·디테일 미픽스」를 안 붙이고 공유하면 미픽스 디테일에 피드백이 쏟아져 부하가 오히려 늘어난다. 중간 단계를 만든 의미가 라벨에 달려 있다.
- **공유 문턱을 9개에서 7개로 낮췄다** — 토큰 준수는 비주얼 디테일이라 `픽스`로 내렸고, 4대 상태 전부 강제와 확정 문구는 완화했다(핵심 상태만 / lorem만 아니면 됨). 문턱이 높으면 아무도 그 단계를 안 쓰고 결국 `1차`에서 바로 `픽스`로 뛴다.
- **QA 축은 복사하지 않고 참조만 한다** — 축 정의 소유는 위키 `33-storybook-qa-harness.md`다. 새 규칙 문서에 옮겨 적으면 이중 관리가 된다(`CONTEXT.md` 하드 룰 「같은 데이터 N군데 = 설계 냄새」).
- **새 개념을 늘리지 않고 기존 사다리에 얹었다** — `공유 가능` ≈ `candidate`, `픽스` ≈ `approved screen`. 화면 상태 체계가 이미 있으니 에이전트 산출물에 같은 사다리를 적용하는 것으로 끝냈다.
- **측정 때문에 새 업무를 만들지 않는다** — 병렬도 조절에 처리율 실측은 필요 없고 **이월 여부**라는 신호 하나면 된다("오늘 끝난 걸 오늘 다 봤나"). 숫자가 필요해지면 세션 기록에서 집계할 일이지 주미님이 손으로 셀 일이 아니다.
- **장단점 표 대신 손익분기로 틀었다** — 장단점 나열은 읽고 나서 행동이 안 바뀐다. 같은 재료를 "언제 구조화하고 언제 하지 말아야 하는가"로 세우면 판단에 쓸 수 있다.
- **같은 위화감이 이름을 바꿔가며 네 번 돌아오면 처리가 아니라 전제가 문제다.** 사라짐이 어색한 이유를 흔적 없음 → 대화처럼 생김 → 목격함 → 급작스러움으로 네 번 다르게 진단하고 그때마다 처리를 바꿨는데(접기·띠·이동 가림·페이드), 매번 다른 이름으로 같은 반려가 왔다. 원인은 **대기 투표가 대화 스트림 안에 있다는 배치** 쪽일 가능성이 크다. 모션을 다섯 번째로 다듬기 전에 배치를 다시 여는 갈림길로 정리해 논의에 넘겼다.
- **알림의 무게는 사건의 무게와 다르다.** 리포트 완성은 이 대화의 목적이 달성된 순간이지만 그 무게는 리포트가 감당하지 알림이 감당할 필요가 없다. 알림이 하는 일은 "도착했다, 준비되면 눌러라"이고 그건 가벼운 일이다. 모달로 올리면 투표를 강제 중단시켜 「자동 전환 안 함」 원칙과 어긋난다. Callout이 중간이라 맞다.
- **이미 정해진 것을 논의 자료 맨 위에 따로 뺀다.** 차주 논의용 주석을 「이미 정해진 것 / 미정 / 갈림길 / 판정 기준」 네 덩어리로 갈랐다. 미정 하나를 논의하려고 모으면 확정된 넷까지 다시 열리는 게 이 트랙에서 이미 여러 번 있었다.

---

## Deep 사고과정 — "확정 안 된 거 맞지" 확인에서 조합 시안까지

시작은 확인 질문 한 줄이었다.

```
https://www.figma.com/design/VHdwVCvG0YG7wucLtOWmOg/...?node-id=8568-20922 여기서 딥 디자인 확정안됬는데 맞지
```

맞다고 답할 근거를 실측했다 — 섹션 `Chat / Quick / Deep 추론 규칙`(`8568:20922`) 맨 아래에 전날 만들어진 `사고과정 표시 — 선택지 보드 (2026-08-18 · 판단 대기)`(`9221:23106`)가 있었고 축 6개가 전부 열려 있었다. **조건(§3.4 문구표·5단계·진행바·% 미표기·접힘 기본)은 고정, 표현만 미정**이라는 게 정확한 상태였다.

그런데 그 보드가 원했던 물건이 아니었다.

```
난 좀 아까 이런식의 예쁜 디자인 안을 보여준건데 (레퍼런스 이미지 5장)
```

축 보드는 현행에서 변수 하나씩 돌린 컷이라 전부 현행처럼 밋밋했다. 원한 건 **완성된 조합 시안**이었다. 레퍼런스(토스 분석 리포트 타임라인 · 온보딩 체크리스트 3종 · 세로 타임라인)에서 시각 언어를 뽑아 새 보드를 만들었다 — `사고과정 조합 시안 — 레퍼런스 방향`(`9278:136505`), 시안 4개·7컷, 전부 모바일 361.

규칙과 충돌하는 것 2개는 이식에서 걸렀다. `2/5`·`25%` 카운터는 % 미표기·분모 미표기 결정에 걸리고, 카드 컨테이너는 "토글은 스트림 위 맨몸" 정본에 걸린다.

### 스케일이 두 번 퇴짜났다

| 판 | 수치 | 판정 |
|---|---|---|
| 1차 | 마커 24 · 행간 8 · 전 텍스트 muted 16 Regular | "디자인 개구린데" — 구조만 옮기고 스케일·위계를 안 옮겨 현행에 파란 점 붙인 꼴 |
| 2차 | 마커 30(헤일로 44) · 행간 24 · 스텝 전부 진하게 | "과해" — 토스 수치를 그대로 옮겼는데 원본은 히어로, 우리는 보조 상태 표시 |
| 3차 | 마커 22 → **18**(슬롯 24) · 2px rail · 행간 14 · 진행 중만 진하게 | 안착. "22도 크다"로 한 번 더 내렸다 |

부수 효과 하나 — 스피너는 20이 하한이라(인스턴스 자식 지오메트리 override 불가) **현재 단계 마커가 완료 마커보다 반 단계 크다.** 위계가 색뿐 아니라 크기로도 한 번 더 적용된 셈.

### 판정 3건이 닫혔다

- **접힘 헤더 = 현행 유지** — 시안 3(tick 게이지) 기각("tick 굳이임")
- **도착 슬롯 카드 = `neutral/100`** — 흰 카드가 오히려 돋보인다는 지적에서 `neutral/150`(눌림·대비 저하) 비교판까지 만든 뒤 "배경은 100이면될듯"으로 확정. 원격 DS `color` 컬렉션에서 임포트해 정식 바인딩
- **완료 마커 = 중립** — 파란 체크 5개가 쌓이는 완료 펼침이 리포트와 시선을 다투는 문제. `neutral/200` 서클 + 흰 체크로 내리고 rail도 `neutral/150`으로

주미님이 직접 색보정한 뒤 그 델타를 나머지 판에 복제했는데, 여기서 **외곽 stroke를 흘렸다**(함정 절 1번). 마커 외곽 `neutral/50` 1.5 INSIDE + 스피너 track 1.5 OUTSIDE — rail을 끊어 마커를 띄우는 처리였다. 실측해서 5판(마커 16 · 스피너 3)에 다시 복제했고, 이 stroke가 흰 보드에서는 안 보이는 문제 때문에 **보드 루트 배경도 `neutral/50`으로** 바꿨다.

### 실화면 컨텍스트 — 흰 보드에서는 판단이 안 선다

"고민이네"에서 나온 진짜 이유가 흰 보드 위라 실감이 안 나는 것 같아 실화면을 복제해 시안을 꽂았다. **원본 무수정** — 기존 토글 인스턴스는 `visible=false`, 시안1 detach 사본을 같은 삽입 지점(`Frame 30` index 0)에 넣었다.

| 노드 | 내용 |
|---|---|
| `9292:23089` | responding 펼침 (원본 `8661:139680` 클론) |
| `9292:23216` | 완료 펼침 (원본 `8661:139772` 클론) |
| `9299:22316` | 에이전트 케이스 · 카드 `neutral/100` (문구 임시) |
| `9299:22411` | 에이전트 케이스 · 어두운 카드 `neutral/150` 비교판 |

에이전트 케이스는 "저기 에이전트가 붙으면 어케되?"에 대한 답으로 임의 추가했다 — `AI 노무사 / AI 재무설계사 / AI 커리어 전환 컨설…의 의견을 받았어요.` 긴 이름은 CTA에서 이미 확정한 규칙(이름만 말줄임, 조사·동사구 보존)을 재사용했다.

### 시안 1 최종 스펙

- 마커 18(슬롯 24) — 완료 `neutral/200` 서클 + `neutral/50` 외곽선 1.5 INSIDE + `neutral/white` 체크 11
- 현재 단계 — blue 스피너 20 (track `neutral/200` + `neutral/50` 1.5 OUTSIDE, arc blue)
- rail 2px `neutral/150` · 도착 슬롯 카드 r12 `neutral/100`
- 파랑은 현재 단계 · 헤더 `{k}명` · 진행바에만 · 접힘은 현행 유지
- **chevron = 문구 바로 뒤**(`StepText` HUG + `maxWidth 269` + 1줄 말줄임) — 아래 절

---

## chevron 위치 — 8/14에 재놓고 결정을 안 한 자리

마무리 직전에 들어온 질문이 8/14 것과 같았다.

```
사고과정 옆에 화살표가 있는데, 그게 모바일 기준에서는 영역이 fill인데
웹에서는 그렇게 하며 인지가 어려울것같아서 텍스트 뒤에 바로 붙는게 나은가 그것도 고민이라
같이 생각해서 처리해주라 / 이건 어떻게 하기로했더라?
```

**답 = 아무것도 안 정했다.** 8/14에 3안을 만들고 실측까지 해서 2안을 추천해놓고 결정이 안 났고, 8/18 축 보드 E축에는 **그 추천안이 아예 빠져** 있었다(E1 우측 끝 · E2 라벨 뒤 고정 = 옛 3안). 같은 질문이 5일 만에 다시 온 이유다.

원인은 구조다 — `Header`가 `HORIZONTAL · gap 8`이고 `StepText`가 **FILL**이라 chevron이 문구가 아니라 **컨테이너를 따라간다.** 361에서만 검증됐기 때문에 웹에서 그대로 벌어진다.

| 안 | 모바일 361 | 웹 800 | 종료(단계문구=false) |
|---|---|---|---|
| 현행 1안 (FILL) | 345 | **784** | 68 |
| **2안 (HUG + maxWidth 269)** | **231** | **231** | 68 |

**2안으로 확정하고 시안1 판 6곳(보드 2컷 + 컨텍스트 4장)에 적용했다.** `layoutGrow 0` + `WIDTH_AND_HEIGHT` + `maxWidth 269`(= 361 − 60 − 8 − 8 − 16) + `textTruncation ENDING` + `maxLines 1`. 검증판 `chevron 위치 — 웹 800 검증`(`9339:24069`)에 현행 vs 2안 × 361/800 × 진행중/종료 8컷을 나란히 뒀다.

남는 흔들림은 단계마다 문구 길이가 달라지는 만큼(종료 시 231 → 68)인데, 현행의 폭 종속 점프(웹 716)보다 작고 **폭이 달라도 위치가 같다**는 게 이 안의 값이다.

---

## 섹션 정리 — 겹쳐 있던 8/14 보드 2장

8/18 축 보드가 8/14 보드 2장과 같은 좌표에 겹쳐 그려져 있었다. 페이지 직속이라 섹션 자식 목록에는 안 잡히고 z-order로만 가려진 상태(함정 절 5번). 둘 다 섹션 자식으로 옮기고 아래로 폈다.

그 뒤 주미님이 새 섹션 `딥 - 사고 디자인 디벨롭 중`(`9280:110814` · 5965×6121)을 만들어 사고과정 탐색물을 전부 모았다 — 실화면 컨텍스트 4장 · 문구표 · 조합 시안 보드 · 축 보드 · 8/14 보드 2장 · **레퍼런스 원본 이미지 6장**. 8/18에 적어둔 "섹션 `8568:20922` 안 y5170~" 배치 기록은 이 이사로 무효라 plan-27에서 정정했다.

### 문서 기록 (socraAI_product design)

`docs/plans/27-spec-v106-inplace-resync-2026-08.md`에 누적 기록(+83줄). 커밋은 하지 않았다 — 미해결 항목 마지막 줄 참조.

| 절 | 내용 |
|---|---|
| 선택지 보드 신설 | 축 6개·실물 25안 표, 8/14 판정과의 차이 3건, 보드 라벨 오류(A는 `10안`인데 실물 9개) |
| 조합 시안 보드 | 레퍼런스 시각 언어 6항목, 스케일 3판 경위와 교훈, 색보정 델타 누락 경위 |
| 판정 진행 | tick 기각 · 카드 `neutral/100` 종결 · 완료 마커 중립화 |
| 주미님 확인 | 도착 줄 모델명 vs 에이전트명 |
| chevron 종결 | 8/14 절에 2026-08-19 종결 블록 + 「남은 판정」 chevron 항목 `[x]` |
| 다음 세션 | DS 마스터 반영 · `9280:110814` 정리 (둘 다 "내일 하자") |

---

## Deep 사고과정 — 미결 3건 종결부터 DS 마스터 반영·게시까지

앞 세션이 시안 1을 만들어 놓고 "확정 선언"만 남긴 상태였다. 이번 세션은 그 확정을 받고 DS 마스터까지 밀어 넣었다.

```
여기서 종합적으로 결론난게 뭐였지?
```
```
응 다 결정하고 진행도 하자
```

### 세 갈래로 보이던 미결이 실은 불리언 하나였다

시안 1 vs 2 vs 4를 물었더니 "이건 뭘 말하는건지 글로 보니 모르겠어"가 돌아왔다. 다시 뜯어보니 시안 셋의 차이는 **축 두 개**였다 — ① 도착 슬롯 카드(1·4) ② 예정 단계 노출(2·4). 카드는 8/19 톤 결정(`neutral/100`)으로 이미 채택된 상태라 **남은 미결은 "남은 단계를 미리 보여줄까" 하나**로 줄었다. 시안 이름으로 물으면 세 갈래처럼 보이는데 실제로는 켜고 끄는 스위치 하나다.

흰 보드에서는 감이 안 오니 실화면 비교판을 만들었다 — `9395:247`(responding 컨텍스트 복제 + 예정 2행). 예정 행은 시안2 것을 그대로 쓰지 않고 시안1 스케일로 다시 지었다(시안2는 마커 28·서클 22 구스케일). 실물 = `Marker/upcoming` 24 슬롯 · 서클 18 · **fill `neutral/50`(배경색) + stroke `neutral/200` 1.5 INSIDE** — 완료 마커(fill `neutral/200` + stroke `neutral/50`)의 **페인트를 서로 뒤집은 것**이라 바인딩이 살아 있다. 세로 비용 실측 = **+80**(History 250 → 330).

### 판정 3건

| 축 | 결정 | 근거 |
| --- | --- | --- |
| 헤더 아이콘 | **`lucide:list-checks` 넣는다** | 펼치면 나오는 게 단계 체크 목록이라 아이콘이 내용을 예고한다. `brain`류는 AI 의인화(`rules.md` 34번)라 제외 |
| 예정 단계 | **안 보여준다** | 기획서 §3.4가 펼침을 "지난 단계 이력"으로만 규정. 예정은 기획에 없는 추가 사양 + 세로 80 |
| 도착 줄 문구 | **`{AI 이름}의 의견이 도착했어요`** | §3.4 원문. 8/12의 `받았어요`를 되돌리고 마침표도 뗐다(다른 이력 줄이 전부 마침표 없음) |

아이콘이 부른 재계산 1건 — `StepText` maxWidth **269 → 245**(= 361 − 16 − 8 − 60 − 8 − 8 − 16). 8/19 chevron 결정의 269는 아이콘이 없던 전제였다.

```
아~ 기획은 뭐더라?
```

**여기서 내 질문 자체가 틀렸던 게 드러났다.** 도착 줄을 "모델명 vs 에이전트명" 양자택일로 냈는데, §3.4 원문은 `{AI 이름}`이고 Deep 참여자는 **모델 3 + 에이전트가 섞여** 있다(`n`=소집한 전문가·에이전트만 최대 7 / `k`=의견이 도착한 AI·모델 포함 최대 10, §4.4 예시 "Gemini·세무 에이전트 둘만"). 어느 한쪽으로 통일할 자리가 아니었다. plan-27에 "문구표엔 모델명만"이라고 적어둔 내 기록을 그대로 믿고 기획서를 안 열어본 게 원인이다.

### DS 마스터 반영 (`4625:2316`)

착수 전에 인스턴스를 먼저 셌다 — **96개**(옛 기록 44는 낡음). Chat·Quick 닫힘 41 · Deep 닫힘 35 · Chat·Quick 펼침 10 · Deep 펼침 10. `visible` override 27개, 도착 줄 텍스트 override 0개(= 마스터에서 문구를 고치면 전부 내려간다).

```
같은 레벨의 정보면 맞추긴해야하게겠는데
```

- 헤더 4 variant 전부 — `LeadIcon`(`lucide:list-checks` `74:4572`) 16 + `StepText` HUG·maxWidth 245·1줄 말줄임
- Deep 펼침 이력 — 행 간격 14 · 상단 16 · 마커 슬롯 24(서클 18 `neutral/200` + `neutral/50` 외곽선 1.5 INSIDE + 흰 체크 11) · 스피너 arc만 `blue/500`(track `neutral/200` + `neutral/50` 1.5 OUTSIDE) · `Rail` 2px `neutral/150` 절대배치
- 도착 3줄을 `ArrivedSlot`(들여쓰기 36) → `card`(`neutral/100` · r12 · 패딩 12/16 · 간격 6)로 감쌈. **기존 `Arrived` 프레임 id는 그대로 두고 부모만 씌웠다**
- Chat·Quick 펼침도 같은 마커로 통일, **rail은 Deep만**(단일 단계라 이을 게 없다)
- 도착 줄 기본 문구 3개 + 세트 description 갱신

실측 = Deep 펼침 **361×304**(시안1과 동일) · Chat·Quick 펼침 361×64.

### 게시·수락 검증

```
게시했어
```

| 확인 | 결과 |
| --- | --- |
| `LeadIcon` 전파 | **96/96** |
| `ArrivedSlot`(도착 카드) | Deep 펼침 **10/10** |
| 행 켜고 끄기 `visible` override | **27개 생존** |
| 도착 줄 문구 | 10/10 새 문구 |
| 높이 | 기본 304 · 완료 이력 2행 더 켠 판 384(= 304 + 40×2) |

도착 3줄을 카드 아래로 옮기며 부모가 바뀌었는데도 override가 전부 살았다. **Figma override는 부모 경로가 아니라 대상 노드 id로 매칭된다.**

### 8/14부터 있던 구멍 1건

96개 전수 점검에서 `StepText`는 숨김인데 `Progress`는 보이는 인스턴스 **9개**가 나왔다. 원인은 마스터였다 — Deep **닫힘**의 `Progress`(`4935:2479`)는 `단계문구#4625:0`에 묶여 있는데 **펼침**(`4935:2482`)만 `componentPropertyReferences`가 `{}`였다. 8/14 진행바 복구 때 닫힘만 다시 묶고 펼침을 빠뜨린 것이다. 그때 "진행바 숨김 유지"로 실측이 통과했던 건 제품 파일 인스턴스에 **직접 override로 숨겨 뒀기 때문**이고, 이번 라이브러리 수락으로 그 override가 정리되면서 마스터의 구멍이 드러났다. 재연결하고 4 variant 연결 상태를 검산했다. **재게시가 한 번 더 필요하다.**

### 주미님이 직접 만든 컷

섹션 `Chat / Quick / Deep 추론 규칙`(`8568:20922`)에 `Deep — 완료 (펼침·5단계 전체)`(`9418:124825`) 신설 — 5단계 전체 완료 이력 + 단계문구·진행바 숨김(`단계문구=false`). 새 마커·카드·문구가 그대로 들어갔다. 다만 **`Rail`이 70 그대로라 4~5행에 안 닿는다**(완료 상태면 약 160 필요) — 미해결로 남겼다.

### 문서 기록 (socraAI_product design)

| 문서 | 내용 |
| --- | --- |
| `docs/plans/27-spec-v106-inplace-resync-2026-08.md` | 판정 3건 종결·rail 축 흡수·마스터 반영·게시 검증·구멍 1건·함정 2건 (+133줄) |
| `design-system/components/thinking-toggle.md` | 구조표를 마커/rail/도착 카드 기준으로 교체, `LeadIcon`·maxWidth 245 추가, 도착 줄 문구·이름 혼재·예정 미노출 원칙, 배경색 외곽선의 목적, `단계문구` 바인딩 주의 (+34줄) |

둘 다 `scripts/validate-design-system.sh` 통과.

### 커밋 (socraAI_product design)

| 커밋 | 설명 |
|------|------|
| `ea693c3` | docs(사고과정): 시안1 확정 → DS 마스터 반영·게시 검증 · 8/14 진행바 바인딩 구멍 정정 |

`feat/edge-scrim-exploration` ahead 4 · **미push**.

---

## Sheet Body 제품 전용 컴포넌트 — "왜 여기 있어?"에서 문구 정합까지

질문 하나에서 시작했다.

```
https://www.figma.com/design/VHdwVCvG0YG7wucLtOWmOg/...?node-id=9266-164437 이건 여기 왜있어? 우리 컴포넌트는 보통 ds에 있자나
```

섹션 `Sheet Body (제품 전용 · Sheet/Shell 본문 슬롯)`(`9266:164437`) — 어제 시트 12장을 DS `Sheet/Shell`로 전환하며 만든 본문 컴포넌트 9개다. 실측으로 답했다. 9개 전부 인스턴스 1~2개, 총 12개이고 **예외 없이 원격 DS `Sheet/Shell` 인스턴스의 본문 슬롯 안**에 있다. 떠도는 인스턴스 0. 컴포넌트가 된 이유는 재사용이 아니라 **`Sheet/Shell`의 `본문`이 INSTANCE_SWAP이라 꽂으려면 컴포넌트여야 하기 때문**이다.

그 자리에서 회색지대 2개(`모델 선택 리스트`·`로그인 게이트`)를 DS 승격 후보로 올렸고, 승인받아 감사했다.

### DS 승격 감사 (read-only, write 0)

| 항목 | 모델 선택 리스트 (348) | 로그인 게이트 (297) |
|---|---|---|
| 노드 / 인스턴스 / 깊이 | 71 / 21(마스터 13종) / 8 | 26 / 7(마스터 7종) / 5 |
| 로컬 부품 의존 | 0 (전부 원격) | 0 (전부 원격) |
| 변수 | 전부 원격 DS | 위 + `strings` 4건 |
| 인스턴스 override | 2개, 0 | 2개, 0 |
| 실사용 화면 | 1 (+사본) | 1 (+사본) |

**전제 두 개가 뒤집혔다.** 8/18에 본문을 제품에 둔 근거였던 "중첩이 깊어 재현하면 흘린다"는 이 2개엔 해당하지 않고(로컬 의존 0), `strings`는 **DS 파일 소유**(KR/JP/EN 74개, `auth/gate-*`·`login/*` 포함)라 승격해도 문구 바인딩이 안 끊긴다. "인스턴스 2개 = 재사용"도 아니었다 — 화면 **원본 + 사본**이라 실사용은 각 1화면이다(`6219:52073` `7539:15491` 둘 다 생존).

**DS에 이미 형제가 있었다.** `Modal Shell` + `Modal Content / Login Methods`(344×160) 외 5종이 `✅ Components`에 있다. 껍데기/본문을 똑같이 가른 선례인데 **본문이 DS에 있다.**

- **로그인 게이트 → 승격 권장.** 부품 7개·문구 4개가 DS 소유, override 0이라 스왑 손실 없음.
- **모델 선택 리스트 → 비권장.** 5행이 GPT·Claude·Gemini + `테슬라 애널리스트` + `AI 노무사` 더미고, `ticker / tsla`가 **[VW] Design System** 부품이라 DS가 외부 의존을 물게 된다.

### 문구 정합 (피그마 write, 주미님 "그래")

감사 중 제목 `옮긴다`를 결함으로 보고했는데 **선택지 텍스트였다**(그 선택지를 지지한 모델 목록). 자리를 오독했다. 다만 "선택지라는 신호가 없어 헷갈린다"는 판단은 주미님도 동의해서 두 줄을 고쳤다.

| | 전 | 후 |
|---|---|---|
| 제목 | `옮긴다` | `'옮긴다'` |
| 설명 | `의견에 가장 많이 지지한 순서대로 AI를 보여드려요` | `이 선택을 지지한 AI를 지지율 순으로 보여드려요` |

설명은 정렬만 말하고 **관계(이 AI들이 왜 여기 있는지)**를 안 말하고 있었다. 마스터(`9252:35283`) 1곳 수정으로 화면 2곳(`profile list`·사본 `7999:18782`)이 override 0이라 그대로 따라왔다. 본문 348 · 셸 458 · 화면 853 전부 불변, 두 줄 다 1행 유지(제목 32 / 설명 26). 스크린샷으로 렌더 확인. 롤백은 두 줄 원복.

### 문서 기록 (socraAI_product design)

`docs/plans/10-design-system-followup-2026-06.md`에 감사 절 신설(+30줄) — 측정표, 뒤집힌 전제 2건, 승격 권장/비권장 근거, `Modal Content /` 선례, 문구 정합 결과와 폐기한 대안(한 문장 합치기). `validate-design-system.sh` 통과. **커밋은 하지 않았다** — 미해결 항목 마지막 줄 참조.

---

## 8/18 미해결 목록 정리 — 92건 중 26건이 죽은 전제였다

시작은 한 줄이었다.

```
https://jumijeong-design.github.io/Jumi-Worklog/worklog.html 오늘 워크 로그 남은 작업중에 지울거랑 해야할것좀 구분해봐
```

뷰어 링크로 들어왔지만 원문을 먼저 읽었다(`logs/2026/08/2026-08-18.md`, 112KB). 뷰어는 원문이 반영됐는지 보는 보조 기준이지 판정 근거가 아니다. 미체크가 **92건**이었다.

문제는 그 92건이 **8/18 심야 머지 러시 이전에 쓰인 목록**이라는 점이었다. 밤사이 PR 11건이 머지되고 0.12.0이 발행됐는데 목록은 그대로였다. 그래서 항목을 읽어 분류하는 대신, **항목이 선 전제가 아직 살아 있는지를 PR·git·계약 문서로 재는** 쪽으로 갔다.

### 판정자와 반박자를 분리했다

삭제 후보를 고른 뒤 그대로 확정하지 않고, 반박을 임무로 받은 에이전트 4개에 넘겼다.

| 에이전트 | 임무 |
|---|---|
| `verify:pr-merge` | "이미 해소됐다" 후보 10건을 `gh`·`git`으로 반박 시도 |
| `verify:notes` | "기록성이라 지워도 된다" 후보 9건이 레포 문서에 실제로 있는지 대조 |
| `scan:missing` | 목록에 없는데 본문에만 떠 있는 열린 작업 찾기 |
| `verify:live-state` | worktree·PR 상태·브랜치 좌표 재실측 |

### 무엇이 바뀌었나

| 분류 | 건수 | 내용 |
|---|---|---|
| 완료 종결 `[x]` | 9 | Chromatic 승인 4건 병목 · 직렬 스택 · `copied` 포팅(#158) · Toast 닫기 44 · 웹 260 상태 프레임(#165) · 라이트박스 안전영역 · Chromatic 쿼터 · 원본 3커밋 |
| 기록성 삭제 | 5 | 같은 내용이 plan-10·계약 md·storybook-map에 **파일:행 단위로** 있는 걸 확인하고 제거 |
| 합침 | 12 → 5 | 미푸시 3→1 · Thinking/Toggle 2→1 · AttachmentChip 2→1 · 시각 비교 5→2 |
| 누락 추가 | 8 | 열린 PR 4건 처분 · 릴리스 순서 규칙 · 스켈레톤 잔여 3건 · `toast.md` 127 좌표 |
| 이관 블록 신설 | 4 | 레포에 근거가 없어 못 지우는 것 — 넣을 파일과 문장까지 적어 남김 |

미체크 **92 → 79건**.

### 반박에 살아남은 삭제 후보 2건

이게 이번 패스의 실제 수확이다. 혼자 판정했으면 둘 다 지웠다.

**① 첨부 칩 Chromatic 재게시** — "빌드 695·698이 이미 찍었으니 종결"로 닫으려 했는데, 그 변경을 담은 `b9aa81a`·`4c3de2a`가 **아직 main 미도달**이었다(`origin/main`의 `semantic.tokens.json`에 `processing-spinner` 토큰 0건). 두 빌드는 이 변경 **없이** 찍힌 것이다.

**② 시각 판정 5건 → 1건 압축** — 접으면 #161 갈래가 사라진다. `forceRebuild` 픽스는 #164로 **main에만** 들어갔고 #161 브랜치의 `release.yml`엔 그 줄이 없어서, 브랜치에 dispatch를 다시 걸어도 또 스킵된다. 그래서 2건으로만 접었다.

### 실측이 잡아낸 사실 오류 2건

**구출 대상 목록의 `0e8aee6`은 브랜치에 없는 유령이었다.** amend로 밀려났고 실제 커밋은 같은 메시지의 `a95d6ff`다. 구출용 목록이라 그 해시로 cherry-pick하면 실패하거나 옛 판을 집는다.

**`plan-30` 문서는 origin 어디에도 없다.** `origin/main` 0건은 8/18에 이미 잡혀 있었는데, 재실측하니 **원격 feature 브랜치에도 0건**이었다(로컬 HEAD blob `377d814`). 지금 브랜치를 지우면 복구처가 없다는 뜻이라 경고를 그만큼 세게 고쳐 적었다.

### 편집 자체가 만든 결함을 자가 수리했다

항목 삭제를 `old_string`이 `\n- [ ] …`로 시작하는 편집으로 했더니 **앞 항목을 끝내던 줄바꿈이 함께 지워져** 두 항목이 한 줄로 붙었다. 5곳. 편집 도구는 전부 성공으로 리턴했다. 정규식으로 "행 첫 칸이 아닌 자리의 `- [ ]`"를 훑어 5곳 전부와 헤더 앞 빈 줄 4곳을 복구했고, 재검사에서 결함 0을 확인했다.

### 검증

- 로컬: 미해결 절 미체크 79 · 구조 결함 0 · 블록 헤더 앞 빈 줄 정상
- 원격: `gh api contents`로 즉시 확인(캐시 없음)
- 라이브: raw 캐시 때문에 push 직후엔 옛 내용이 나와, 45초 간격으로 폴링해 **270초에 갱신**되는 걸 실측. 뷰어 `ENTRY_META`엔 `2026-08-18`이 이미 등록돼 있어 추가 작업 없음

`riiid/prism`은 이번 턴에 건드리지 않았다 — 이관 4건이 그쪽 파일인데 워킹트리에 옆 세션 미커밋 36건이 올라가 있고 `toast.md`도 그중 하나다.

### 커밋 (jumi-worklog)

| 커밋 | 설명 |
|------|------|
| `1f31a91` | log(2026-08-18): 미해결 항목 정리 — 완료 9건 종결·기록성 5건 삭제·중복 합침·누락 8건 추가 |
| `3b56be0` | log(2026-08-18): 정리 패스 요약 숫자 정정 — 합침 12→5, 추가 8건, 미체크 92→79 |

둘 다 `origin/main` 도달 확인(`merge-base --is-ancestor`).

---

## 계약 버킷 감사 — "잘못 나열된 거 있어?"

```
https://www.figma.com/design/DcYgJjGAfObOIM4IyrQjgj/...?node-id=0-1
여기 있는것중에 지금 스토리북 계약과 아닌걸로 구분되어있는데 잘못 나열되어있는거 있어?
```

`✅ Components` 페이지(`0:1`)의 네 버킷(`스토리북 계약` / `Story book 미계약` / `계약 제외` / `계약 불필요`)에 든
**116개 항목 전수**를 레포 실측과 대조했다. 피그마는 **read-only, write 0건**이다.

### 첫 판정이 틀렸고 그 원인이 오늘의 제일 큰 교훈이다

1차로 5건을 보고했는데 그중 **2건이 오판**이었다. 워킹트리(`feat/edge-scrim-exploration`)를 레포 정본으로 읽었는데
그게 `origin/main`보다 **28커밋 뒤**였다. `Sheet/Header`·`Sheet/Intro`는 PR #153이 8/18 머지되면서
계약 md·코드·스토리가 전부 main에 들어와 있었고, `Callout`도 같은 날 의도적으로 복귀시킨 건이었다
(`storybook-map.md:164` — "쓸 자리가 다시 생겨 `Components/Chat/Callout` 스토리를 복구").

정정 후 `git worktree add --detach /tmp/prism-main-audit origin/main`으로 정본 사본을 떠서 전량 재감사했다.
브랜치가 **앞선** 경우도 같은 함정을 만든다는 걸 `modal-shell.md`(브랜치에만 있음)에서 확인해,
최종 판정은 main·브랜치 양쪽을 다 재고 어느 쪽 근거인지 밝혀 적었다.

### 결과 — 116개 중 94개 정상, 22개 적출

| 갈래 | 건수 | 내용 |
|------|------|------|
| 확실한 오분류 | 13 | `스토리북 계약`에 있는데 계약 md도 스토리도 없음 |
| 역방향 구멍 | 1 | 코드·스토리는 있는데 계약 md·map 행이 없음 (`AnswerFeedback`) |
| 예시 프레임 | 2 | 컴포넌트가 아닌 미리보기 보드 (이름 정합 건) |
| 미결 (`주미님 확인`) | 5 | 회색지대 — 승인 묶음에서 분리 |
| 오탐 (내가 냈다가 철회) | 2 | `Sheet/Header`+`Intro` · `Callout` |

가장 큰 덩어리는 **Overlay의 로그인/가입 모달 8개**다 — `Modal Shell`(`4783:2411`) + `Modal Content /` 6종
+ `Deep/Footnote`(`4817:2382`). 계약·코드·스토리 0이고, 유일한 문서인 `design-system/components/modal-shell.md`가
3행에서 **"패키지 계약이 아닙니다"라고 스스로 밝힌다.** `계약 불필요`의 정의(조합물, 근거 문서만)에 정확히 해당한다.
※ 같은 날 다른 세션이 이 클러스터를 "DS 소유 선례"로 인용했는데(위 설계 결정 절), **DS 소유와 계약 보유는 다른 명제다** — 충돌 아님.

나머지 5건은 `Agent/Row`(`4050:1751`, plan-20이 "DS 승격은 별건"으로 남겨둔 상태 그대로) ·
`Chart/ProgressBar`(`4076:1837`, 형제 StackBar·LegendRow는 각각 포섭되는데 이것만 0건) ·
`History/Title`(`1684:2381`, 유일한 언급이 "'오늘' History/Title **삭제**" 기록인 고아 마스터) ·
`Row/Item`·`ContextMenu/Item`(아래 절).

### 행 마스터 2개는 버킷이 아니라 통합 문제였다

`Row/Item`(`3805:2034`)·`ContextMenu/Item`(`3805:2021`)은 어느 문서에도 없어 처음엔 "애매"로 뒀는데,
metadata 좌표가 갈랐다 — `Dropdown Menu`(x=119, y=2074) 옆 x=440 **같은 y**, `Context Menu`(y=2615) 옆 **같은 y**.
"컨테이너 왼쪽 · 그 행 마스터 오른쪽" 관례라 각각의 행 마스터다. 그런데 `bottom-sheet.md:9`가
**"행 컴포넌트를 셸별로 따로 만들지 않는다"**고 못 박아뒀고, 통합 마스터 `Menu Item`(`3352:1374`)이
5 variant(enabled/engaged/selected/disabled/destructive)로 `menu-item.md` 계약과 정확히 일치한다.
반면 이 둘은 3·4 variant짜리 컨테이너 전용판이다. 버킷을 옮길 게 아니라 마스터를 합칠 자리로 보고 주미님 판단에 올렸다.

### 오분류의 뿌리 — 사라진 "내부 부품" 섹션

`design-system/components.md:23`은 `Chat_내부(컴포넌트 계약x)`(`3116:8689`)·`Chat History Panel_내부` 두 섹션을
"이름 그대로 계약 대상이 아닌 내부 부품"이라고 설명하는데, **지금 페이지에 그 섹션이 없다**(metadata 전수 15개 섹션에 0건).
같은 문서가 `미계약 컴포넌트`(`1684:1973`)라고 적은 섹션도 실제로는 `Story book 미계약`(`4635:2184`)이고,
하위 섹션 열거(`:25`)에는 `Decision`이 빠져 있다. 내부 부품을 담던 버킷이 사라지면서 그 부품들이
`스토리북 계약` 밑에 그대로 남은 게 오늘 적출한 것들의 상당수로 보인다.

### 방법과 한계

워크플로로 섹션별 감사 5개 → 오분류마다 반증 에이전트를 붙이는 파이프라인을 짰다.
**감사 5개는 전부 완주했고 검증 19개가 세션 리밋(05:10 리셋)으로 전멸**했다. 살아남은 반증 4건은 전부
"반증 실패"(주장 유지)로 돌아왔고, 나머지는 메인 루프에서 노드 ID grep·좌표 대조·스크린샷으로 직접 다시 쟀다.
**역방향 스윕**("계약·스토리는 있는데 피그마 어느 버킷에도 없는 것")도 같은 이유로 그때는 못 돌았고,
리밋이 풀린 뒤 같은 날 오전에 완주했다(아래).

감사 자체는 `riiid/prism` **파일 변경 0건**이었다(read-only). 만든 것은 `/tmp/prism-main-audit` worktree 하나뿐이고 마감에 제거했다.

### 역방향 스윕 — 진짜 구멍은 0건이었다

`storybook-map.md` 「이름 규칙」 표 56행(= 계약 md + 스토리를 둘 다 가진 정식 목록)을 피그마 116개 인벤토리와 대조했다.
이름 표기가 달라(`Settings Row` ↔ `Settings/Row`) 1차에서 13건이 뜨길래 공백·슬래시·하이픈을 지우고 정규화해 8건으로 줄였고,
그중 6건은 이름만 다른 대응이 있었다 — `Attachment Chip`·`Attachment File` → `Attachment`, `VoteBar` → `Chart/StackBar`,
`LegendItem` → `Chart/LegendRow`, `Markdown` → `Markdown Rendering` 하위 섹션, `ReportDeep` → `Report/Surface`.

남은 2건은 **문서에 이유가 이미 적혀 있는 의도적 예외**였다.

- `VoteResult` — `vote-result.md`가 "**폭이 데이터인 바는 코드가 정본** — 피그마 신설 마스터 없음"이라고 적어뒀다(plan-20 결론, 주미님 2026-08-07 확정).
- `SheetAnswer` — 근거가 제품 파일이다. "본문은 2,500자 더미가 마스터 높이를 정하므로 DS로 올리지 않았다."

둘 다 `search_design_system`으로 DS 라이브러리에 마스터가 실제로 없는 걸 확인했다. 그리고 `Sheet/Content Placeholder`의
설명이 같은 말을 하고 있었다 — "실제 화면에서는 **제품 본문 컴포넌트**(전문 시트 본문, AI 리스트, 사유 선택 등)로 교체한다."
즉 시트 본문이 DS에 없는 건 누락이 아니라 설계다(같은 날 다른 세션의 Sheet Body 감사와 같은 결론에 독립적으로 도달했다).

계약 62개가 두 표에 정확히 55 + 7로 갈리고 **고아·유령·중복 0건**인 것도 함께 확인했다. 이로써 감사가 양방향으로 닫혔다.

---

## 감사 결과 실행 — 주미님 "3개 다 진행해"

승인이 떨어져 세 결정을 한 번에 집행했다. 순서는 **PR 머지 → 전체 비교 트리거(빌드 도는 동안) → 피그마 이동 → 검증**으로 잡았다. 비교 빌드가 8분쯤 걸리는데 그 시간에 피그마 작업을 겹쳐 돌려 대기를 없앴다.

### PR 2건 머지 + 전체 비교

브랜치 삭제가 다른 PR을 닫아버린 8/18 사고 때문에 **base 관계부터 확인**했다 — 열린 5건이 전부 `main` 직결이라 스택이 없었고, 안전하게 `--delete-branch`를 붙였다.

- **#168**(CHANGELOG 정정) → `78eddff`. `UI Review` pending 때문에 `mergeStateStatus=UNSTABLE`이었는데, 그 pending이 곧 결정 3이라 막힐 이유가 아니었다.
- **#161**(패널 radius flush) → `da39447`.
- main에 `workflow_dispatch` → run `32210760027` → **Build 704**. `release.yml`이 dispatch일 때만 `FULL_SNAPSHOTS`·`forceRebuild`를 켜는 구조라 이 경로로만 실제 비교가 성립한다. 로그에서 두 플래그와 321 스토리를 직접 확인했다.

⚠️ **#161의 changeset이 다음 릴리스 PR에 실린다.** 8/18에 릴리스 PR이 내용 PR을 앞질러 폐기 기능이 발행된 그 자리라, 릴리스 PR을 머지하기 전에 안에 든 changeset을 먼저 읽어야 한다.

### 피그마 13건 이동 — 첫 시도가 폰트로 죽었다

1차 스크립트가 `in appendChild: unloaded font "Pretendard Regular"`로 실패했다. **`appendChild`에도 폰트 로드가 필요하다**는 8/18 기록 그대로다 — `characters`를 건드릴 때만 필요한 게 아니다. `use_figma`는 원자적이라 파일은 그대로였고, 옮길 서브트리의 TEXT를 `getStyledTextSegments(['fontName'])`로 전수 수집해 선로드하니 통과했다(`skipInvisibleInstanceChildren = false`로 숨은 텍스트까지 포함).

세 번에 나눠 write 했고 **매 호출마다 자기 검산을 붙였다** — 대상 섹션 `+N`, 출발 섹션 `−N`, 그리고 "이번에 옮긴 ID를 뺀 나머지가 원래 목록과 같은가". 8/12에 `Message Bubble`을 옮기다 옆 섹션이 같은 상대 오프셋으로 딸려온 사고가 있어서다. 세 번 다 딸려온 것 0이었다.

### 검증은 스크립트 리턴값을 믿지 않고 페이지를 다시 스캔했다

각 호출이 "성공"을 리턴해도 그건 그 호출 안에서의 사실이다. 그래서 **이동 전에 썼던 스캔을 그대로 한 번 더 돌려** 노드 ID 단위로 대조했다. 결과 — 버킷이 바뀐 항목 **정확히 13개**(의도한 그 13개) · 이동 전에 있었는데 사라진 항목 **0** · 13건 밖인데 버킷이 바뀐 항목 **0**.

`계약 불필요` 섹션은 자식이 7 → 19가 되어 높이를 2434 → 3760으로 키웠고, 스크린샷으로 겹침·섹션 이탈이 없는 것도 확인했다.

### 문서

`storybook-map.md` 「제외 항목」에 이동 5줄 + 검산 + **롤백 좌표 13개 전량**을 적었고, `components.md`에는 "사라진 내부 부품 섹션을 되살리지 않고 `계약 불필요`로 흡수한다"는 결론을 명시했다. 옆 세션 미커밋이 같은 파일에 있어 `git apply --cached`로 **내 훅만 갈라 커밋**했다(`621c631`).

### 남긴 것

`Row/Item`·`ContextMenu/Item`은 **버킷만 옮기고 통합은 안 했다.** 실은 `Menu Item` 하나로 합칠 건인데(계약이 "행 컴포넌트를 셸별로 따로 만들지 않는다"고 못 박아뒀다) 인스턴스가 딸려 움직이는 작업이라 승인 묶음에 섞지 않았다. 문서에 "임시 배치"로 명시했다.

### 이어서 — #163·#160 충돌 해소 (주미님 "충돌해소해")

**워킹트리를 건드리지 않는 게 먼저였다.** 옆 세션 미커밋 36건이 올라가 있어 브랜치를 갈아타면 그게 다 흔들린다. `git worktree add`로 PR별 격리 사본을 떠서 작업하고 끝나고 제거했다.

**#163은 쉬웠다.** `plan-10` 파일 끝에서 양쪽이 각각 새 절을 덧붙인 것뿐이라 의존이 없었다. 브랜치 293줄 · main 24줄을 **둘 다 보존**하고 main 절을 앞에 뒀다.

**#160은 진짜 충돌이었다 — 같은 기능을 두 갈래로 구현했다.** 브랜치가 8/18 **18:15**에 `loading?: boolean` + `copy.loadingListLabel` + 자리표시 24행(높이 44 · 바 27)을 넣었고, main이 같은 날 **22:34**에 #165로 `status: ready|loading|error`(+ error 상태 · sr-only 라벨 · 토큰 바 높이 · 행 47)를 넣었다. 커밋 타임스탬프를 재보고서야 순서를 알았다.

판단은 셋을 봤다 — ① main이 `error`까지 포함한 **상위집합**이다 ② main은 이미 **0.12.0으로 발행**돼 소비자가 있다 ③ 브랜치가 그 세 파일(`ChatHistoryPanel.tsx`·`components.test.tsx`·`*.stories.tsx`)에 넣은 게 **전부 옛 구현뿐**임을 merge-base 대비 diff로 확인했다. 그래서 세 파일은 통째로 main을 채택했고, 잃은 것이 없다.

**계약 md가 두 벌이 된 걸 잡았다.** 자동 병합이 옛 `loading` 문단과 새 `status` 문단을 나란히 남겨놨다. 옛 문단과 `copy.loadingListLabel` 항목을 걷어내고 통합 경위를 적었다. 코드에는 죽은 참조가 0인데 문서에만 남는 건 흔한 자리다 — **충돌 해소 뒤 옛 API 이름으로 레포를 한 번 훑는 게 값을 했다.**

**브랜치 고유 기여는 전부 살아남았다** — `skeleton.bar.radius` `size/4 → size/8`, `ReportLoading` 스토리, 스켈레톤 계약 88줄, moderator 에셋. 자동 병합이 조용히 처리한 것들이라 하나씩 생존을 확인했다.

⚠️ **한 건은 내가 정하지 않고 남겼다.** 바 높이가 **27(주미님 8/18 지시)** 과 **20(main #165)** 으로 갈린다. 통합에서는 나중이자 발행분인 20을 택했지만 27이 주미님 지시였으므로 되돌릴지 확인이 필요하다. 계약 md와 미해결 목록 양쪽에 `### 주미님 확인`으로 올렸다 — **내 판단으로 덮으면 주미님 지시가 조용히 사라지는 자리다.**

게이트: validate · token:build · typecheck · test **134/134** 통과. 결과는 #163 `e10ff6b` · #160 `651f039`, 둘 다 `MERGEABLE`.

### 시각 판정 → 머지 — 스토리북으로 봤다

크로마틱 diff 화면이 안 열린다고 하셔서 **Build 704가 게시한 스토리북**으로 경로를 바꿨다(`…-wkwuwzyoxi.chromatic.com`, 인증 없이 열리고 399 스토리). 스토리 ID를 index.json에서 뽑아 항목별 직링크를 만들고 6개 전부 200을 확인한 뒤 드렸다. **판정 자체는 크로마틱에서만 되지만 "보는 것"은 스토리북으로 충분하다** — 주미님은 무엇이 이상한지만 말하면 되는 자리다.

판정 결과 — 모델 프로필 OK · 컴포저 페이드 OK · 히스토리 실패 상태 OK · 패널 모서리는 "웹에서 쓰는 거라 각진 거지?"로 확인. **바 두께는 27로 확정.**

**타이틀 질문 하나가 들어왔다 — "원래 저렇게 두껍고 진했나?"** 피그마 마스터 `Sheet/Header`(`title=on`)를 실측하니 **Pretendard SemiBold 17 · 160% · `semantic/text-primary`**였고, 코드 `text-lg font-semibold leading-[1.6] text-fg`가 정확히 같았다(`--color-fg` → `--prism-color-text-primary`, 피그마와 같은 변수). **회귀가 아니라 스펙이다.** 낯설게 느껴진 이유는 따로 있었다 — `SheetShell`에는 원래 **타이틀이 없었고**(핸들 아래 뒤로가기 pill만), 8/18 머지된 #153이 `Sheet/Header`를 포팅하면서 처음 생긴 것이다. "두꺼워졌다"가 아니라 "없던 게 생겼다"였다.

### 머지 — #163이 #160을 다시 깨뜨렸다

`#163 → #169 → #160` 순으로 걸었는데 둘이 걸렸다.

- **#169는 draft**라 GitHub이 막았다. 주미님 본인 PR이라 임의로 ready 전환하지 않고 남겼다.
- **#160은 #163이 머지되는 순간 다시 충돌했다.** 스택 PR이 아닌데도 둘 다 `plan-10` 파일 **끝**에 새 절을 덧붙이는 구조라, 앞이 들어가면 뒤가 어긋난다. 30분 전에 풀어둔 충돌이 되살아난 셈이다. 같은 방식으로 다시 보존 병합해 머지했다.

**교훈: 같은 파일 끝을 쓰는 PR이 여러 개면 충돌 해소는 1회로 안 끝난다.** 머지 순서마다 뒤에 남은 것들이 차례로 깨진다. 미리 알았다면 #160을 먼저 넣거나, 두 PR을 한 번에 rebase했을 자리다.

결과 — main `78eddff`(#168) → `da39447`(#161) → `3c845fa`(#163) → `9b24a9c`(#160). 27px이 main에 도달한 것까지 확인했다.

### 릴리스 PR을 열어보니 기록에 구멍이 있었다

`#169`는 draft라 막혀 있던 것을 `gh pr ready` 후 머지했다(`d762302`). ⚠️ **draft 여부는 `gh pr list`에도 `mergeable` 필드에도 안 나온다** — `MERGEABLE/UNSTABLE`로 보여서 머지되는 줄 알았다가 `Pull Request is still a draft`로 튕겼다. `--json isDraft`를 같이 봐야 한다.

그리고 릴리스 PR **`#170 chore: 0.12.1`**을 감사했다. 8/18에 릴리스 PR이 내용 PR을 앞질러 폐기 기능이 발행된 자리라 머지 전에 읽기로 한 것이었는데, **읽길 잘했다.**

PR 자체는 깨끗했다 — changeset 하나(`sheet-shell-panel-radius`)만 소비하고, patch 범프(0.12.0 → 0.12.1)도, CHANGELOG 문구도 실제 변경과 정확히 맞았다. **문제는 PR 안이 아니라 밖에 있었다.** `ca92bf8`(0.12.0) 이후 main의 `packages/prism/src` 실제 diff를 떠보니 공개 토큰 **두 개가 changeset 없이** 들어가 있었다.

- `skeleton.bar.radius` `{size.4}` → `{size.8}` — **원자 토큰이라 `Skeleton`을 쓰는 모든 자리**가 함께 둥글어진다
- `chat-history-panel.skeleton-bar-height` `{size.20}` → `27px` — 히스토리 로딩 자리표시 한정

changeset은 **버전 범프와 CHANGELOG 문구만** 정하고, 발행되는 코드는 그 시점 main 전체다. 그래서 이 둘은 **배포는 되는데 기록에만 없는** 상태였다. 소비처에서 "왜 갑자기 자리표시가 둥글어졌지"가 나와도 CHANGELOG로 추적이 안 된다.

→ **PR `#171`로 changeset을 보탰다.** 이게 머지되면 `#170`이 두 항목을 포함해 재생성된다.

**교훈: 릴리스 PR 감사는 PR diff만 보면 반쪽이다.** PR은 자기가 소비하는 changeset만 보여주지, 지난 릴리스 이후 changeset 없이 들어온 변경은 안 보여준다. `git diff <직전 릴리스 태그/커밋> origin/main -- packages/*/src`를 같이 떠야 구멍이 보인다.

### 0.12.1 발행 — 8/18과 반대로 끝났다

`#171` 머지(`4e0c4a3`) → `#170`이 changeset **2개**를 소비하도록 재생성되는 것 확인 → CHANGELOG 최종본을 눈으로 읽고 `#170` 머지(`29dd91c`). 발행분 0.12.1이 담은 것은 셋이다 — `SheetShell` 패널 flush, `skeleton.bar.radius` 4→8, `chat-history-panel.skeleton-bar-height` 20→27. 워크플로 `32221499676` **`publish-latest = success`**로 발행을 확인했다(패키지가 `--access restricted`라 미인증 `npm view`는 E404가 정상이므로 판정은 로그로 한다).

**8/18과 정확히 반대 순서로 끝났다.** 그날은 릴리스 PR이 내용 PR을 앞질러 **폐기 확정된 기능이 npm에 나갔고**, 21분 뒤 다음 버전으로 걷어내야 했다. 게다가 그 정정 노트마저 기본값을 반대로 적어 또 한 번 고쳤다. 오늘은 발행 전에 릴리스 PR을 열어보고, 기록에 빠진 것을 먼저 메운 뒤 눌렀다.

차이를 만든 건 규칙 한 줄이었다 — **"릴리스 PR은 머지 전에 안에 든 목록을 읽는다."** 어제 사고 직후 미해결 목록에 올려둔 그 항목이 오늘 실제로 구멍을 잡았다. 사고를 기록으로 남기는 값이 하루 만에 회수된 셈이다.



---

## 재열람 적재 — "이미 만든 딥 리포트를 다시 불러오면 스켈레톤이 뜨나"

```
이미 만들어진 딥 레포트를 다시 불러와서 스켈레톤이 생기는 경우가 있을까?
```

확인 질문 하나에서 시작해 규칙 등재 → 피그마 시안 → DS variant 신설 → 스토리북 참조 구현까지 내려갔다.

### 답은 "있다, 다만 생성 로딩과 다른 자리"

기획서 v1.0.6이 이미 두 경우를 갈라 뒀다. §4.9는 "다시 열었을 때 이미 끝나 있으면 완료된 답이 그대로 보이고, 아직 생성 중이면 로딩(진행 단계)", §5.1 재연결 복구는 **"서버의 세션 상태를 다시 읽어 채운다 — 재생성이 아니다"**라고 못을 박는다. 그래서 이미 만들어진 리포트에 Deep 사고과정·진행바를 다시 돌리면 없는 생성을 하는 척이 된다.

인디케이터 선택은 `rules.md` 39번 사다리로 기계적으로 떨어졌다 — 진행률을 계산할 수 없어 2단계 탈락, 결과의 모양은 아니까 **3단계 스켈레톤**. 범위는 `SOCRA-TUTOR-022`("데이터를 제외한 UI는 유지하고, 데이터 영역에 스켈레톤을 추가한다")를 히스토리 목록과 똑같이 적용했다.

### 피그마 시안 — 컷 3장

제품 파일 `✅ chat/report`에 섹션 신설(현재 `9383:113289`). 기존 노드는 수정하지 않고 추가만 했다.

- **컷① 재열람 적재** — 크롬(상단 바·입력창) 유지, 대화 영역만 스켈레톤. 재열람은 대화의 끝에서 열리므로 콘텐츠를 하단에 붙였다.
- **컷② 종합만 재생성** — 말풍선은 실제 데이터로 두고 리포트 자리만 스켈레톤.
- **컷③ 적재 실패** — 콜아웃 한 줄.

중간에 `Empty State` 3층 안 두 개(ⓐ 크롬 유지 · ⓑ 전용 화면)를 그렸다가 **주미님이 기각**했다("채팅방 안에서 저렇게 뜬다는 게.. 콜아웃 정도로도 충분한거같은데"). 지우면서 그 판단을 재현 가능한 기준으로 바꿔 규칙에 넣었다 — **빠져나갈 길이 있느냐**.

### DS `Callout` `Type=error, Layout=action` 신설

콜아웃으로 가기로 하니 정작 그 조합이 피그마에 없었다. `Layout=action`은 `Type=info` 한 장뿐이고, 계약 `callout.md`가 "필요해지면 피그마부터 그린다"고 예고해 둔 자리였다.

`2570:1312` 클론 + error 톤 3개 재바인딩(면 `595:867` · 보더 `595:870` · 제목 `927:14`)으로 variant 1장 추가(`4940:2349`). **아이콘은 색만 바꾸면 안 됐다** — glyph가 `info`(ⓘ)에서 `alert`(⚠)로 달라져야 해서 `Type=error, Layout=inline`의 아이콘 인스턴스를 통째로 옮겨 심었다. 기존 5변형 무수정, 세트 높이만 426→572.

주미님 게시 후 제품 파일 시안의 손조립(inline 콜아웃 + 별도 버튼)을 인스턴스 1개로 교체했다.

### 스토리북 참조 구현 — 축은 안 넣는다

8/6 계약("로딩 자리표시는 컴포넌트 상태가 아니라 화면 조립 몫")을 유지해 `ReportDeep`/`ReportQuick`에 status 축을 넣지 않았다. 대신 조립이 실제로 되는지를 `Explorations/재열람 적재`로 증명했다. **패키지 export·계약 무변경** — `Skeleton`·`Callout`·`Button`·`HeaderNavBar`·`BottomBar`·`MessageBubble`만 조합했다.

스토리 2개 중 본판은 **인터랙티브** 쪽이다. 120ms 적재는 지연 임계(250ms) 안에 끝나 스켈레톤이 **아예 안 뜨고**, 1200ms는 뜬 뒤 최소 표시 400ms를 채우고 교체한다. 어느 경로였는지 로그로 남겨 눈으로 안 잡히는 차이도 읽히게 했다.

조립하면서 나온 미결이 하나 있었는데(리포트 글래스 면을 `ReportSurface` 비공개라 못 쓴다) **면을 빼는 쪽으로 종결**됐다(주미님 "면없이 가자"). 적재 중엔 리포트가 올지 안 올지도 모르는 상태라 카드 면이 곧 약속으로 읽힌다는 게 근거. 피그마 컷①·②에서도 면을 걷어내 코드와 맞췄다.

### 검증

`pnpm typecheck` · `pnpm storybook:build` · `validate-design-system.sh` 통과. Chromatic **Build 714** 통과(신규 스냅샷 2장, 렌더 에러 0). **육안 판정은 미해결 항목으로 남겼다.**

### 커밋 (socraAI_product design)

`origin/feat/edge-scrim-exploration`에 push 완료(`621c631..85a9f5e`). **main 미도달** — 브랜치 처분은 별도 트랙의 열린 항목이다.

| 커밋 | 설명 |
|------|------|
| `291d209` | docs(재열람 적재): 생성 대기와 적재 대기 분리 + Callout error/action 신설 기록 |
| `33ac272` | feat(storybook): 재열람 적재 탐색 스토리 — 축 신설 없이 화면 조립으로 |
| `85a9f5e` | docs(재열람 적재): 스켈레톤에 리포트 글래스 면을 그리지 않기로 확정 |

`rules.md`는 세션 시작 전부터 다른 세션의 미커밋 훅이 섞여 있어 **내 훅만 `git apply --cached`로 갈라 담았다.** 피어 편집은 워킹트리에 그대로 남아 있다.

### 2차 세션 — 면을 다시 그렸다

```
이거 ㄷ이미 만들어진 딥레포트 불러올때인데 왜 스켈레톤이 레포트 카드안에 없어?
```

같은 날 확정한 걸 같은 날 뒤집었다. 처음엔 노트에 적힌 근거를 그대로 읊었다 — "적재 중엔 리포트가 올지 안 올지도 모르는 상태라 카드 면이 곧 약속으로 읽힌다". 주미님이 한 줄로 무너뜨렸다.

```
아 근데 이미 레포트가 등장했던건데 ㅡㅡ
```

**그 논리는 생성 대기의 것이었다.** 재열람은 §5.1이 "서버 상태를 다시 읽어 채운다 — 재생성이 아니다"라고 못 박은 자리라 리포트 존재가 확정이고, 사용자는 그 카드를 본 적도 있다. 면을 걷는 건 약속을 아끼는 게 아니라 아는 걸 숨기는 쪽이었다. 전환도 더 나쁘다 — 면이 없으면 데이터 도착 순간 면·테두리·그림자가 없던 자리에서 튀어나오는데, 면을 깔아두면 안쪽만 바뀐다.

같이 통과됐던 두 번째 근거("높이를 약속하지 않는다")는 실은 **면이 아니라 선택지 목록**을 정당화하는 말이었다. 한 문단에 붙어 있어서 묶여 넘어갔다. 선택지 목록을 안 그리는 건 지금도 유효하다.

### 무엇으로 갈지 — 1안/2안

물었더니 답이 짧았다("12둘다지 당연히 / 2에서 실패하면 걍. 사라지면됨"). 컷①②를 다 면으로 가되, 면 안에 실패 상태를 새로 만들지 않고 **재생성이 또 실패하면 면째로 사라져 직전 실패 상태로 돌아간다**. 상태가 하나 안 늘었다.

### 반영 — 피그마 → 패키지 → 스토리북

**피그마.** 컷①(`9372:22403`)·컷②(`9373:120640`)에 면 복구. 값은 새로 정하지 않고 실제 `Report/Surface` 인스턴스(`8553:17788`)에서 그대로 가져왔다 — `semantic/surface/glass` · `semantic/surface/glass-strong` 2px INSIDE · `size/24` · 그림자 공유 스타일, 패딩 40/20. 내부 리듬도 실제 토큰에 맞춰 `hero 자리` 프레임을 신설(`9579:2`·`9579:3`)해 제목→요약을 `hero-gap` 8로 정정했다(전엔 12). 슬롯 370 = 40 + 198 + 24 + 68 + 40, 안쪽 본문 321.

**패키지.** `ReportSurface`·`ReportSurfaceProps` export 추가. 승격 기준을 "소비처가 몇이냐"가 아니라 **"화면이 손으로 베껴 쓰게 되느냐"**로 봤다 — 면을 그리기로 한 순간 화면엔 레시피를 복사하는 것 말고 길이 없어졌고, 그러면 리포트와 어긋나는 게 시간문제다. `status` 축은 여전히 안 만든다.

**스토리북.** 스켈레톤을 면 안으로 넣고 내부 간격을 `ReportDeep`과 같은 토큰으로 바꿨다.

### 하다가 나온 것 — 면 폭이 32 틀려 있었다

헤드리스로 재니 면이 361이 아니라 **329**였다. 스토리 `Phone`이 스크롤 영역에 `px-4`를 주는데 `ReportDeep`도 `surface-inset` 16을 갖고 있어 인셋이 두 번 걸린 것. 피그마 `Report/Surface` 인스턴스를 전수 실측해 실제 값을 확정하고(393→**361** · 360→328 · 560→528) 거터를 말풍선·콜아웃·질문 스켈레톤 쪽으로 옮겼다.

**이건 스켈레톤만의 문제가 아니었다** — 같은 스토리의 진짜 `ReportDeep`도 329로 서 있었다. 둘이 서로 일치해서 눈으로는 안 잡히는 자리였다.

### 검증

`typecheck` · `test`(134) · `build` · `pack:check` · `storybook:build` · `validate-design-system.sh` 통과. 헤드리스 실측으로 면 361 · radius 24 · border 2 `rgba(255,255,255,.75)` · bg `rgba(255,255,255,.7)` · shadow `0 3px 16px rgba(0,0,0,.06)` · padding 40/20 확인(컷①② 둘 다 + `ready` 단계의 실제 리포트도 361).

### `Skeleton` 바 색 — 넘기려다 되받았다

미해결로 올렸더니 주미님이 되돌렸다("스켈레톤도 너가 봐봐 그럼").

**계약이 이미 틀린 문장을 갖고 있었다.** `skeleton.md`가 "바는 `surface-muted`(Figma `neutral/100` 대응)"이라고 적어 뒀는데 대응이 아니다 — `surface-muted`는 `neutral/50`(#F2F4F8)이고 `neutral/100`은 #EAEDF2다. 그래서 이건 신설이 아니라 **드리프트 동기화**였다.

DS 원본(`240:87`)을 재니 `Rectangle`이 `neutral/100`에 바인딩돼 있고 모드값이 Light #EAEDF2 · Dark #191A1D였다. **배경과의 명암 방향이 모드마다 뒤집힌다** — 라이트는 배경보다 어둡고 다크는 밝다. 이걸 안 봤으면 `surface-inset`(라이트만 #EAEDF2, 다크 #000000)을 골라 다크를 반대로 만들 뻔했다.

코드 primitive `color.neutral.100`이 두 모드를 그대로 갖고 있어서 컴포넌트 토큰 `skeleton.bar.bg`를 거기 붙였다(`comment.bg`가 이미 같은 primitive를 쓰는 선례).

**영향 범위는 2곳뿐이었다** — 실사용은 `ChatHistoryPanel`(`:366`)과 이 탐색 스토리. `Button`·`DeepCTA`는 주석에만 `Skeleton`이 나온다. "모든 사용처에 걸린다"고 지레 크게 잡았던 게 실측으로 줄었다.

대비 실측: 재열람 배경 위 **1.000 → 1.066**(라이트) · **1.101**(다크), `ChatHistoryPanel` 흰 면 위 **1.087 → 1.174**(바 17개). 회귀 없이 전부 올랐다.

### 커밋 (socraAI_product design)

`feat/edge-scrim-exploration`에 **커밋만 하고 push는 안 했다**(주미님 지시).

| 커밋 | 설명 |
|------|------|
| `2a5a47b` | feat(prism): ReportSurface 공개 — 재열람 적재 스켈레톤이 리포트와 같은 면 위에 |
| `9c35922` | fix(skeleton): 바 색이 채팅 배경과 같아 배경 위 스켈레톤이 투명했다 |

담은 직후 피어가 `3020c28`을 올렸다 — 워킹트리가 실제로 흐르고 있었고, 안 담았으면 날아갔을 수 있는 자리였다.

---

## 대기 투표 무클램프(v3) — 안 채택부터 DS 상태까지

8/18 밤 "여기 보면 2개의 옵션이 있어"로 시작해 날짜를 넘겨 이어진 트랙. 보드 `Deep - 로딩에서 결과 시나리오`에
클램프안(v2)과 무클램프안(v3)이 나란히 서 있었고, 채택 → 보드 정리 → DS 반영까지 갔다.

### 두 안의 실제 차이는 클램프 하나였다

상단 앵커도, 앞 카드가 위로 밀려 남는 것도 두 안이 같았다. 카드 높이는 v2가 352~536(접힘 509·펼침 536·결과 401),
v3가 379~1034(360dp·155건 실측 최소 338·중앙값 532·p90 740). v2의 "건너뛰기가 크롬선 안쪽"이라는 이득은
393×852에서만 성립하고 v3 기준인 안드 최소 360×640으로 내려가면 근거가 사라진다. 주미님 "사연은 다 읽히게" →
무클램프 채택, 클램프안 섹션 삭제(최상위 53·프레임 24, 마스터 0개·인바운드 링크 0 확인 후).

### 카드 넘김이 없어지니 3초 자동 전환도 없어졌다

주미님 "카드 넘김 애니메이션을 안 하고 그래서 3초 기준도 사라지고". §4.7은 「결과 약 3초 → 카드 넘김
애니메이션과 함께 자동 전환」인데 v3에는 넘길 카드가 없다. 남용 방지의 나머지 절반인 「건너뛰기는 카드 등장
3초 후 노출」은 유지. 노션 캐논 v1.0.7까지 확인했고 §4.7 대기 투표 문구는 v1.0.3부터 한 글자도 안 바뀌었다.

### DS 반영 3건

- **result 라벨 「다음으로」 → 「다음 고민」.** 카드가 넘어가는 모델이 아니라 스트림이므로. 「넘어가기」는
  건너뛰기와 의미가 겹쳐 탈락. 카드 높이 불변(330/330·516/516).
- **마스터에 남아 있던 클램프 해제.** `textTruncation=ENDING·maxLines=7`이 마스터에 그대로였다 — v3 컷들이
  무클램프로 보인 건 인스턴스마다 덮어놨기 때문이고, **새 인스턴스는 전부 7줄로 태어나고 있었다.** result 변형은
  `maxLines=3`(투표 후 더 접던 v2 규칙 잔재)까지 있어 함께 제거.
- **`State=answered` 신설**(binary·nonbinary) — 아래 절.

### 보드 재편 — 판정은 하한에서, 이야기는 정상 기기에서

기준이 밴드마다 섞여 있던 게 혼란의 원인이었다. 시나리오 17컷을 360 → **393×852**로 이관하고, 정상 기준
밴드(짧은 36자·중앙값 147자·p90 277자)를 신설했다. 폭에 따른 카드 높이 실측 — 중앙값 147자는 360폭 563 /
393폭 536 / 520폭 438, p90 277자는 360폭 779 / 520폭 546. **정상 기기에서도 중앙값 사연이면 카드 하단이
852 밖으로 걸친다** — 스크롤 전제가 하한만의 이야기가 아니라는 게 이 밴드의 값어치다.

웹 보드(`✅ chat/chart`)도 맞췄다. 모바일 컷이 삭제된 클램프안 프레임의 사본이라 360 v3 컷으로 교체했고,
데스크톱은 주석이 "inline 최대 폭 520"인데 실제 카드가 361이라 래퍼 4개를 FILL로 풀어 520으로 맞췄다.

### 조립 결함 5건

- **인풋 영역이 2개** — 4컷에 ABSOLUTE Bottom Bar가 둘씩(360 시절 y=302 잔재 + 393 전환분 y=642).
- **프레임이 허그가 아님** — 전부 FIXED라 942 프레임에 내용 649, 293이 빈칸. `max(852, 실제 내용)`으로 재계산.
- **빨간선이 아무것도 안 가리킴** — 밴드 선이 컷 위(y=4394)에 떠 있었다. 852를 넘는 컷 위에만 두는 걸로.
- **그래프가 카드 밖으로 샘** — 막대가 카드(`Stage`) 안에 있었지만 **절대배치**라 HUG 계산에서 빠졌고,
  `clipsContent=false`라 43px 삐져나왔다. 흐름으로 되돌리자 카드가 228 → 280으로 자라 품었다.
- **사고 과정이 한 컷만 카드 안** — 15컷은 토글이 361 폭으로 `Loading Indicator`(채움 없음)에 있는데
  한 컷만 321 폭으로 흰 카드 안에 있었다. **폭이 40 차이나는 게 곧 컨테이너 소속이 다르다는 신호**(패딩 20×2).

전수 대조(28컷 × 6축) 후 구조 이탈 0건. 남은 플래그 4건은 "스트림이 852를 넘는다"는 의도된 넘침.

### 진행바를 DS 축으로 — 주미님 "그래프를 여러개 만들면 되잖아"

17컷 토글 진행바가 전부 160/361(44%) 고정이라 1단계나 완료나 같은 그림이었다. 손으로 고치려다
**인스턴스 자식 크기 override가 무시되는 벽**에 걸렸다(6컷은 `fill`이 FIXED라 아예 불가). 값이 유한한 상태는
컴포넌트 축으로 올리면 이 제약을 아예 피한다 — DS에 `Thinking/Progress`(`stage` 6값: analyzing 20 / gathering 40 /
responding 60 / synthesizing 80 / reporting 90 / done 100%)를 만들고 `Thinking/Toggle`의 Deep 2변형 안
손조립 진행바를 그 인스턴스로 교체했다. 축을 토글에 직접 붙였으면 4×6=24조합이 될 뻔했다.

반대로 **분포 그래프는 축으로 못 담는다** — 44/22/16/12/6 같은 연속값이라 컴포넌트화하면 오히려 비율을
못 바꾸게 된다. 손조립을 유지하고 규칙(등급색·합=트랙폭·항목 수=리스트 수)만 계약에 적기로 했다.

### 지나간 카드 `answered` — 주미님이 직접 그린 상태

주미님이 스트림 누적 컷에 "답변하고 지나간 카드"를 넣었다(결과 유지·배경 변경·건너뛰기 제거). 의도는
**못 누르는 카드를 카드처럼 보이지 않게** 하는 것, 「다음 고민」이 없는 건 **이미 다음 카드가 나와 버튼의 목적이
사라졌기 때문**. 그래서 투표한 카드는 두 벌이 된다 — `result`(버튼 있음) / `answered`(버튼 없음·표면 `neutral/100`).
전환 시점은 "다음 카드 도착"이 아니라 **"사용자가 다음 카드로 이동"** 이다(도착 기준이면 버튼이 뜨자마자 사라진다).

### 읽는 사람 기준 정리 — 주미님 "너 기준말고 이 가이드를 읽는 사람 기준으로"

내 리뷰가 구조 정합에 머물러 있다는 지적. 다시 보니 밴드 이름이 「대기 투표 카드 인터랙션」과
「…- 안드 최소 360*640 기준」으로 거의 같아 "왜 같은 게 둘이지"가 첫 질문이 되는 상태였다.
`1. 흐름 · 393×852` / `2. 길이 편차 · 393×852` / `3. 하한 판정 · 360×640`으로 바꾸고, 담백하게 줄이니
주제가 사라져 섹션명을 `Deep 대기 투표 — 무클램프 스트림`으로 올렸다. 흐름 밴드에 단계 라벨 6개,
컷 이름에서 `1-v3`·`v3-n`·`정상-n` 같은 내부 표기 제거(비교 대상이 삭제된 뒤로 가리킬 게 없다).
**보드 머리 치수 요약은 철회했다** — 같은 숫자가 두 곳에 살면 한쪽만 갱신되는 사고가 난다(오늘 그래프 4칸/5칸이 그 예).

### 검증

- Storybook `DeepWaitVote` v3 재작성 — `pnpm typecheck` · `pnpm lint` · `pnpm storybook:build` 통과, Chromatic 빌드 701.
- 피그마 변경은 전부 스크린샷 실측으로 확인. DS 재게시 2회(라벨·클램프 / 진행바·answered) 후 제품 파일 반영 실측.
- **런타임 인터랙션 육안 확인은 아직 안 했다** — 최대 사연에서 「다음 고민」까지 스크롤, FAB 동시 노출 2건.

### 커밋 (socraAI_product design)

| 커밋 | 설명 |
|------|------|
| `bca080e` | docs(plan-10): 대기 투표 v3 — 보드 재편·진행바 DS 축·지나간 카드 상태 기록 |


---

## 홈 PC 폭 기준 — 채팅에만 있던 기준을 홈에도

주미님이 채팅의 `PC Layout 기준 적용 / Chat (mo 393 / tablet 1024 / desktop 1440)` 밴드(`7228:12095`)를 가리키며 홈에도 필요하다고 했다. 특히 레일 접힌 케이스.

읽어보니 새 규칙을 만들 일이 아니었다. DS Guidelines `Layout — Responsive Foundation (Web)`(`4057:2`)의 L06이 이미 "고정 카드 중앙 — 홈 컴포저 중심 512=1024/2(Tablet 384=768/2)"로 홈을 이름까지 적어두고 있었다. 규칙은 있는데 그걸 보여주는 시안이 1440 expanded 한 장뿐이라 rail 접힘이 통째로 미검증이었다.

### 시안 2장

| 프레임 | zone | 콘텐츠 영역 | 블록 800 | 세로 |
| --- | --- | --- | --- | --- |
| `web / home` (1440 펼침, 기존) | 276 | 1164 | 좌우 182 | 중심 512 |
| `home / Tablet 1024` (`9500:3557`) | 76 (rail 60) | 948 | 좌우 74 | 중심 384 |
| `home / Desktop 1440 · rail 접힘` (`9500:3886`) | 76 (rail 60) | 1364 | 좌우 282 | 중심 512 |

`web / home`이 가로 오토레이아웃이고 사이드바 zone이 HUG · Main이 FILL이라, 패널만 `collapsed`로 바꾸니 나머지 폭이 전부 자동으로 떨어졌다. 손으로 계산해 박을 자리가 없었다. 두 장 다 히어로 블록 width를 `layout/column/text`에 바인딩해 폭 계약 안에 넣었고, 페이지 기존 스타일로 주석 2개를 붙였다.

### 접힘은 홈에서 실제로 움직인다

1440에서 블록 중심이 펼침 858 → 접힘 758. 채팅 스트림은 긴 세로 흐름이라 덜 보이지만 홈은 로고·문구·컴포저·칩이 한 덩어리라 그대로 보인다. 그래도 채팅과 같은 규칙으로 뒀다 — 홈 컴포저가 전송 후 채팅 컴포저로 이어지기 때문이다(회고 참조).

### 원본 정리와 대조

- **원본 `web / home`(`7860:3270`)** — 히어로 블록이 `layout/column/text` 미바인딩(폭 계약 밖) + 패널 세로 FIXED였다. 둘 다 1440에서는 증상이 없어(1008 = 1024−16 우연 일치) 안 보였다. 승인받고 정리했고 렌더 PNG 해시가 전후 동일해 시각 변화 0을 확인했다.
- **`web / history` 4장 — 수정 0.** 패널 세로 FILL · 스트림 컬럼 `column/text` 바인딩 · zone 276 · 컬럼 800(x 182)로 이미 정합이었다. 이 페이지에서 계약 밖이던 건 `web / home` 한 장뿐이다.
- **칩 행 Edge Band — 웹/태블릿 불필요.** 800 컬럼 여유가 투자 449 · 커리어 479 · 대인관계 414px다. `edge-band.md`가 "등장 판정은 소비처가 한다(스크롤 컨테이너 `scrollTop > 0`)"이므로 넘치지 않으면 띠도 없다. 카피가 2.2배가 돼야 켜지는데 그 폭이면 이미 칩이 아니다. **모바일 393은 여유 42 · 72 · 7이라 대인관계 행이 JP/EN에서 바로 켜진다** — 그쪽이 지금 진행 중인 EdgeBand 작업의 자리다.

### Guidelines L06 예시 보강

- 4번째 카드 `ex-home-tablet`(`4981:25`) 추가 — 캡션 "고정 카드 중앙 — home / Tablet 1024 (중심 384=768/2 · 콘텐츠 영역 948)", 이미지는 Tablet 렌더 업로드(`4981:27`, 440×330으로 4:3 맞춰 FILL 크롭 제거).
- 기존 카드 캡션(`4269:7`) "home 컴포저" → **"home / Desktop 1440"**. 태블릿 형제가 생겨 캡션만 보고 어느 폭인지 갈리게 했다.
- 예시 행(`4269:2`)을 HUG → FIXED 1400 + `layoutWrap=WRAP`(행 간격 40).

### 검증

- 두 시안 모두 스크린샷 실측 — rail 아이콘·아바타·칩 3행 정상 렌더. 기하는 스크립트 리턴값으로 대조(zone/Main/블록 x·y·w·h).
- 원본 정리는 렌더 PNG 해시 동일(`26474c7e…`)로 시각 무변경 확인.
- L06 보드 리플로우 — 예시 행 363 → 759, 보드 10454 → 10850, 섹션 12개 순서·폭 불변.
- 문서 게이트 `validate-design-system.sh` 통과.

### 커밋 (socraAI_product design)

| 커밋 | 설명 |
|------|------|
| `a5e4229` | docs(plan-10): 홈 PC 폭 기준 시안 2장 — Tablet 1024·Desktop 1440 rail 접힘 |
| `6df4d5a` | docs(plan-10): 칩 Edge Band 판정(수정 0) + Guidelines L06 예시에 홈 Tablet 추가 |

※ 둘 다 `origin/feat/edge-scrim-exploration`에 push 완료 — `origin/main` 미도달(브랜치 PR 대기). 워킹트리의 다른 세션 미커밋 26건은 손대지 않으려고 `git apply --cached`로 내 hunk만 갈라 커밋했다.

### 이어서 — 열린 항목 3건 종결

주미님 "남은 작업 중에 할 수 있는 거"로 이어 잡았다. 열린 항목을 전부 훑어 **승인 없이 지금 끝낼 수 있는 것**만 골랐다 — 나머지는 육안 판정 대기(Chromatic 704·714·701)·기획 확정 대기·피그마 시각 변경(승인)·브랜치 처분(되돌리기 어려움)이고, 무엇보다 **피어 세션이 39개 파일을 워킹트리에 잡고 있어** 손대면 남의 작업이 딸려간다.

**① 릴리스 PR 머지 순서를 규칙으로 박았다.** `docs/workflows.md` 「정식 릴리즈」 절에 `### 릴리스 PR 머지 순서`. 머지 전에 릴리스 PR 본문의 changeset 목록을 읽고 **빠진 것**(나가야 하는데 없음)과 **들어가면 안 되는 것**(폐기·반려분)을 둘 다 본다. 8/18에 이 순서를 어겨 폐기 확정 기능이 발행됐고 21분 뒤 걷어내야 했다.

**② 계약 md 피그마 인용 감사 — 8개 중 진짜 결함은 3건이었다.** 워크로그에 "8개가 제품 파일 id로 잘못 인용"으로 적혀 있었는데 실측하니 셋으로 갈렸다.

| 계약 md | 인용 id | 실체 | 판정 |
| --- | --- | --- | --- |
| attachment-lightbox | `7708:60662` | 제품 스펙 보드 FRAME | 의도된 인용 |
| sheet-answer | `7460:12705` | 제품 **로컬** 세트 `Sheet/전문 본문` | 의도된 인용 |
| vote-result | `2990:11945` | 제품 **로컬** 컴포넌트 `VoteResult` | 파일명만 DS로 오기 → 정정 |
| markdown | `1215:1756` | DS 4개 페이지·제품 어디에도 없음 | 죽은 id → `Markdown Block / *` |
| report-participants | `7759:174089` | 없음 | 죽은 id → DS `4053:1747` |
| prompt-pill | `7755:7296` | 제품의 **원격 프록시**(remote=true) | DS `3190:1395` — 피어 점유, 미수정 |
| report-deep | `7462:128103` | 제품 화면 FRAME | 피어 점유, 미수정 |
| report-option | `7462:128132` | 없음 | DS `4572:2` — 피어 점유, 미수정 |

`design-system/rules.md` 룰 30(원격 컴포넌트는 component key로 인용)대로 key를 정본으로 적고 노드 ID는 보조로 남겼다. `prompt-pill`은 감사 시작 때 clean이었는데 고치려는 사이 피어가 잡아서 뺐다.

**③ Guidelines L06 다이어그램 색 — 버전 기록 없이 갈렸다.** 열린 항목은 "덮기 전 값을 안 읽어서 파일 버전 기록으로만 확인된다"였는데, **관례 전수 대조로 답이 나왔다.** WEB SHELL·L01·L02·L03의 콘텐츠 슬롯 8개가 전부 피치 `#FBE9DA` · 크롬 8개가 전부 파랑 `#DCE7F8`, 예외 0건. L06만 콘텐츠가 파랑이라 **크롬과 콘텐츠가 같은 색**이어서 다이어그램이 자기가 보여줄 구분을 못 보여주고 있었다. `Stream 800`(`4266:11`)·`Card 520`(`4266:16`) 둘 다 피치로 정정했다(주미님 승인, donor 클론으로 페인트 객체를 그대로 복사). L06 Viewport만 `#F2F5FC`인 건 덮어쓰기와 무관한 별개 드리프트라 기록만 남겼다.

### 커밋 (socraAI_product design)

| 커밋 | 설명 |
|------|------|
| `a52f0ae` | docs: 릴리스 PR 머지 순서 규칙 + 계약 md 피그마 인용 정정 3건 |

### 이어서 — 마스터 통합 + 버킷 기준 확정

**주미님 결정 3건을 받아 집행했다.** ①접힘 미끄러짐은 "거슬리겠다" ②행 마스터는 "합쳐" ③미결 5건은 "너가 맞는 기준으로 다시 정리해주라 — 피그마랑 스토리북 둘다".

**①은 전제가 바뀌어 레이아웃을 안 건드렸다.** 채팅도 접으면 같은 100px을 움직인다(Main 1164→1364, 컬럼 x 182→282). 홈만의 결함이 아니라 셸의 성질이고, 지금 구조라야 홈→채팅에서 컴포저가 0px로 이어진다(홈만 뷰포트 중앙으로 바꾸면 전송마다 138px). 착지 위치는 지금이 맞고 남는 건 전환 중 이동감이라 모션 항목으로 넘겼다.

**②행 마스터 통합 — 앞 감사의 근거가 좌표 추정이었다.** `Row/Item`·`ContextMenu/Item`이 `Dropdown Menu`(`194:50`)·`Context Menu`(`194:38`) 바로 오른쪽 같은 y에 놓여 있어 그 메뉴의 행 마스터로 기록돼 있었는데, `mainComponent`로 실측하니 **두 메뉴는 이미 `Menu Item`을 3개씩 쓰고 있었고 두 세트의 인스턴스는 DS·제품 전부 0건**이었다. 두 세트 description에도 "이 컴포넌트는 미사용 상태다"가 이미 적혀 있었다. 즉 통합이 아니라 **고아 삭제**였다.

축도 전부 부분집합이었다 — State는 `Row/Item` 3 ⊂ `ContextMenu/Item` 4 ⊂ `Menu Item` 5, 슬롯 프로퍼티(`Leading#`·`Description#`·`Trailing#` 등)는 `Menu Item`만 갖고 나머지 둘은 내용이 하드코딩(`Opus`/chevron, `복사하기`/copy)이었다. 폭·높이 차이(220×71 · 200×51 · 240×51)도 그 더미 때문이라 폭 축이 필요 없다. 지우기 전에 key·좌표·variant·색까지 전량 기록해 `storybook-map.md`에 남겼다.

**③기준을 먼저 세우고 5건을 판정했다.** 기준이 없으면 다음에 또 갈린다 — `_TextBubble`은 `계약 불필요`인데 같은 `_` 접두 `_SettingsTrailing`은 `스토리북 계약`에 있던 게 그 증거다. 확정한 기준은 **"공개 계약 3종을 가질 것인가"** 이고, 결정 신호는 **`index.ts` 공개 export**다. `Report/Surface`가 정확히 그 케이스 — `ReportSurface.tsx`는 있는데 export가 없어 비공개 프리미티브다. 파일 존재로 판정했으면 반대로 갔다.

### 커밋 (socraAI_product design)

| 커밋 | 설명 |
|------|------|
| `86fc35b` | docs(components): 버킷 판정 기준 확정 + 행 마스터 통합·미결 5건 재배치 |

### 기획·개발 발신 초안 (2026-08-19)

주미님 결정으로 「대기」였던 항목 넷이 닫히거나 발신 건으로 바뀌었다. 둘은 **이미 답이 있는데 항목만 낡아** 있었다 — 스켈레톤 타이밍(같은 날 옆 트랙이 250/400으로 확정·구현)과 등급 판정 로직(기획서 v1.0.6에 완비). **"기획 대기"라고 적힌 것을 그대로 믿지 말고 원문을 열어보는 게 여기서도 맞았다.**

**A. 기획 발신 — §4.7 자동 전환 조항 삭제**

> §4.7 「결과 약 3초 → 카드 넘김 애니메이션과 함께 자동 전환」 조항을 삭제 요청드립니다.
>
> 대기 투표를 무클램프(v3)로 확정하면서 카드 넘김이 없어졌습니다. 3초는 넘김 애니메이션에 붙은 시간이었고, v3는 카드를 넘기지 않고 스트림에 쌓으므로 이 조항은 전제가 사라졌습니다.
>
> 되살릴 경우 "3초 뒤 자동 스크롤"이 되는데, 사연 길이 실측상(중앙값 147자 = 393폭 카드 536 / p90 277자 = 779) 3초 안에 결과를 못 읽고, 앞 카드가 위로 밀려 남는 구조라 읽는 도중에 화면이 스스로 움직입니다. 다음 카드로 가는 이동은 result 상태의 「다음 고민」 버튼이 맡습니다.
>
> **유지:** 「건너뛰기는 카드 등장 3초 후 노출」 — 남용 방지 목적은 그대로입니다.
> **함께 확정:** 건너뛰기 = 결과 보기. 건너뛴 카드도 투표한 카드와 같은 경로(result → answered)를 타고, 차이는 선택지 표시 유무뿐입니다(투표는 고른 칸 표시, 건너뛰기는 AI 분포만).
>
> 현재 디자인·스토리북은 이미 자동 전환 없이 서 있고, 기획서만 v1.0.3부터 문구가 그대로입니다(v1.0.7까지 확인).

**B. 개발 공유 — 첨부 업로드 기준 3건**

> 첨부 업로드에서 디자인이 전제하고 있는 기준 3건을 공유드립니다. 다르게 구현돼 있으면 알려주세요 — 화면이 갈립니다.
>
> **1. 업로드는 첨부하는 순간 시작합니다.** 전송 버튼에 몰면 전송이 느려지고, 실패가 전송 직후에 몰려 "보냈는데 안 갔다"로 읽힙니다. 첨부 순간에 시작하면 칩 안에서 진행 → 실패 → 재시도를 그대로 보여줄 수 있고, 현재 디자인이 이미 그 전제로 서 있습니다.
>
> **2. 실패 사유를 사유별로 내려주세요.** 최소 3갈래가 필요합니다 — 용량 초과 / 형식 불가 / 네트워크·서버 실패. 앞의 둘은 다시 눌러도 결과가 같아서 재시도 버튼을 주면 안 되고, 세 번째만 재시도 대상입니다. 사유가 하나로 뭉쳐 오면 UI가 **틀린 어포던스**를 주게 됩니다.
>
> **3. 5MB를 넘겨도 로컬 프리뷰(썸네일)는 그립니다.** 프리뷰는 파일을 고른 즉시 아는 정보라 업로드 성공 여부와 독립이어야 합니다. 초과 파일에 프리뷰가 없으면 사용자가 "무엇이 왜 실패했는지"를 대조할 수 없습니다. 기술적으로 불가한 제약이 있으면 알려주세요 — 스토리 두 곳이 「5MB 초과 + 썸네일」을 대표 예시로 쓰고 있어 그쪽을 고쳐야 합니다.


---

## 칩 행 스와이프 가이드 — "잘려 있으면 신호가 되나"

```
여기보면 일본어, 영어는 칩 길이가 길자나 그래서 옆으로 넘어가는데 이럴때 스와이프 가이드도 추가되어야할것같아
```

홈 '빠르게 시작하기' 칩 행 이야기다. 8/12에 "라벨은 레일 16에 두고 칩 행만 화면 오른쪽 끝까지 흘려 잘리게 하며, 마지막 칩이 걸쳐 보이는 것이 스크롤 신호"로 정해 뒀던 자리.

### 실측이 그 전제를 뒤집었다

칩 뷰포트 377 기준으로 6행을 쟀다. **JP** 투자 433(+56) · 커리어 377(0) · 대인관계 377(0) / **EN** 투자 513(+136) · 커리어 343(−34) · 대인관계 455(+78).

잘린 양이 카피 길이에 완전히 종속돼 0~75px로 흩어졌다. 두 갈래로 나쁘다 — **JP 커리어·대인관계는 정확히 377로 끝나 마지막 칩 오른쪽 여백이 0**이라 잘린 것처럼 보이는데 스크롤할 게 없고(가짜 신호), **EN 투자는 마지막 칩이 24px만 남아** 칩이 아니라 렌더 결함으로 읽힌다.

`foundation/layout.md`의 "가로 스크롤 목록은 풀블리드, 마지막 항목 잘림이 어피던스"는 참여자 아바타에서 온 규칙이다. **원이 다 같은 크기라 반쪽만 보여도 "하나 더"로 읽히는 것**이고, 가변 폭 텍스트인 칩에는 그 조건이 안 선다. 8/12에 적힌 수치(JP 464/354/407 · EN 453/329/462)도 그 뒤 카피가 바뀌어 낡아 있었다.

### 비교 보드를 먼저 만들었다

말로 설득하지 않고 제품 파일에 컷을 깔았다 — JP/EN 현행·페이드 4컷 + 폭 비교 스트립(현행/32/40/56) + 스크롤 끝 여백 3컷(0/16/24). 페이드는 컴포넌트가 아니라 시안용 사각형으로 두되 그라데이션 3스톱만 `Edge Band`와 같은 `semantic/surface/fade-10·50·90`에 바인딩해 하드코딩 0으로 뒀다.

주미님이 그걸 추려 `Section 1`(`9469:127503`, 헤더 "다국어 전환 : 버튼 길어지는 경우의 케이스")로 재구성했고, 남긴 것이 곧 결정이었다 — 끝 여백은 16만, 폭 비교는 32만.

그 과정에서 주미님이 만든 전체 화면 컷의 **왼쪽 페이드가 오른쪽 페이드 복제라 그라데이션이 반대**였다(잘린 바깥쪽이 옅고 안쪽이 짙어 멀쩡한 칩을 지우고 있었다). 뒤집고 위치를 정렬했다.

### 방향이 두 번 바뀌었다

```
아 근데 끝으로 다가갔을때 여백좀 있어야하는거아냐?
```

끝까지 밀면 마지막 칩이 화면 오른쪽 끝에 붙어 끝났다. 앞은 레일 16인데 끝은 0이라 비대칭. 처음엔 **콘텐츠 안쪽 패딩**으로 16을 넣었는데 되받았다.

```
아니 끝나는 시점에만 16 여백이 더. 뜨는거라니?
스크롤 한 경우에만
```

맞는 지적이었다. 콘텐츠 패딩이면 넘치는 행에서 끝까지 밀어야 여백이 보이고, 밀어서 나오는 것도 여백뿐이다. **여백을 뷰포트로 옮겼다** — 칩 행 뷰포트를 화면 끝(393)이 아니라 16 안쪽(377)에서 끊는다. 그러면 오른쪽 16이 스크롤 여부와 무관하게 항상 비고, 끝까지 밀었을 때 나오는 게 여백이 아니라 **칩의 남은 부분**이다(JP 커리어·대인관계는 16px 밀면 `退職の悩み`·`家族との葛藤`이 온전히 드러난다).

대가는 8/12 풀블리드 결정이 깨지는 것. 걸쳐 보이는 양이 16 줄고 행이 상자에 든 것처럼 보인다.

### 그림자가 잘렸다 — 클립 박스를 콘텐츠와 분리

```
이게 근데 clip content 되서 끝에 여백이 됭보이는건데 이러니까 쉐도우도 짤려서 어색해
```

칩 그림자를 재니 blur 12 / offset (0,2) / 알파 0.06인데 클립 박스가 **361×40으로 칩에 딱 붙어** 있었다. 네 면 다 잘리고 있었다. 클립 박스를 콘텐츠에서 떼어 `x 0~377` · 높이 66(위 12 / 아래 14) · 콘텐츠 `padding-left` 16으로 바꿨다. 잘림은 오른쪽 377에서만 일어나고, 왼쪽은 0에서 클립되니 첫 칩 그림자가 안 잘린다.

여기서 앞서 내가 낸 진단 하나가 뒤집혔다. 인스턴스의 `clipsContent=false`를 "고쳐야 할 드리프트"로 보고했는데, **그게 그림자를 살려두고 있던 것**이었다. 마스터 클립 박스가 칩 높이에 딱 맞아서, 플래그만 켰으면 홈 전 화면의 칩 그림자가 한꺼번에 잘렸다.

마지막 미결이던 페이드 폭 클램프는 컷으로 판정받았다 — 16px만 넘치는 행에 페이드 40이 걸리면 페이드가 넘침보다 넓은데, `응 갠차나 가려지니 깔끔하고 좋아`로 **고정 40, 클램프 없음** 확정.

### 집행 — `다 진해앻`

DS `Edge Band` 세트(`4432:2063`)의 `Edge` 축을 `top|bottom` → `top|bottom|left|right`로 넓혔다. `Surface` 2축과 곱해 variant 8개. **기존 축에 옵션을 더한 것이라 top/bottom 인스턴스는 무영향이고 컴포넌트 key도 불변**(`2d37cb7b…`)이다. `layoutMode:NONE` 세트라 클론이 원본 위에 포개질 수 있어 배치 후 겹침 검산을 넣었다(겹침 0).

코드는 `edge`에 `left|right`, 가로 두께용 `width`(기본 40)를 추가했다. CSS 각도가 곧 짙어지는 쪽이라 `top 0 / bottom 180 / left 270 / right 90`으로 읽힌다. 테스트 2개, 스토리 「가로 스크롤 행」 1개 추가.

`QuickStart/Group` 마스터는 **제품 파일이 아니라 DS 파일에 있었다**(`4825:2387`). 제품 쪽 `9253:159339`에 쓰려다 `Cannot write to internal and read-only node`로 막혀서 키로 찾아갔다. `chips`를 절대배치로 빼 그룹 높이 70을 유지했다 — 흐름에 두고 세로 패딩을 주면 그룹이 70→84가 되어 칩 블록 전체가 42px 밀린다. 이름의 `Attachment/Lightbox/` 접두사(클론 출처 잔재)도 뗐다.

게시·수락 뒤 인스턴스의 `clipsContent` 오버라이드를 정리했다(`home/history` 61개 중 49개 수정 / 12개 이미 정상, `Login/Setting` 9개 정상, `chat/report` 0). `resetOverrides()`는 안 썼다 — 이름·채움·텍스트까지 되돌린다. 홈 레이아웃 무변경도 실측했다(화면 852 · 블록 242 · 첫 칩 `x 16` / `y 440·526·612`).

### 폭 고정 회귀 — 규격을 값이 아니라 식으로

```
아 근데 너가 확정 규격 적어둔게 지금 그려둔 아이폰 16사이즈 기준이라 혹시 360 최소사이즈 기준이나 큰 사이즈도 허용 가능한 제네럴한 치수 가이드로 다시 써줄래?
```

문서만 문제가 아니었다. 절대배치로 빼면서 **폭이 377에 박혀** `FILL`이 주던 가변성이 사라졌고, 열이 800인 웹·태블릿·데스크톱 홈에서 칩 행만 377로 남아 오른쪽이 423 비어 있었다. 내가 넣은 회귀다.

`chips.constraints`를 가로 `STRETCH`로 걸어 고쳤다. 마스터를 800·360으로 늘렸다 줄여 검산 — 784 / 344, 오른쪽 여백은 양쪽 다 16. `FILL`로는 안 된다(부모 패딩을 따라가 왼쪽 0을 못 만든다).

규격을 식으로 다시 썼다. 클립 폭 = `컨테이너 − R`, 넘침 기준 = `컨테이너 − 2R`(`R`=레일 16), 높이 66은 폭과 무관(그림자에서 나온 값). 폭별로 **360 → 344/328 · 393 → 377/361 · 430 → 414/398 · 800 → 784/768**.

여기서 새 사실이 나왔다 — **360에서는 KR 대인관계 행(354)도 넘친다.** 393에서 여유가 7px뿐이던 행이다. 반대로 800에서는 3행 모두 안 넘쳐 띠가 안 뜬다. **띠는 좁은 폭에서만 나타나는 장치**고, plan-23의 "웹은 잘림 신호 불요"와 같은 결론에 닿는다.

인스턴스 복구는 재생성으로 했다(위 함정). 61개 중 열 폭 800인 9개만 깨져 있었고, 문구 캡처 후 새 인스턴스로 교체한 뒤 전수 재검증에서 불일치 0.

### 검증

두 커밋 모두 `git stash --keep-index`로 **staged 단독** typecheck·prism 134 tests를 재고 커밋했다. 워킹트리 전체로는 `token:build`·`build`·`pack:check`·`storybook:build`·`demo:build`·문서 게이트 통과.

커밋 가르기는 두 갈래였다. `docs/plans/10`은 내 훅만 `git apply --cached`로 담았고(피어의 헤더 재작성·attachment-chip 트랙은 워킹트리에 존치), **`EdgeBand.tsx`와 `component-contracts/edge-band.md`는 못 갈랐다** — 다른 세션의 `Surface` 축 작업이 같은 줄에 얽혀 있다. 함께 담고 커밋 메시지에 명시했다. 그쪽 나머지(토큰 3종·`ChatHistoryPanel`)는 안 건드렸고 내 코드가 거기 의존하지도 않는다(`raised` 램프는 CSS 변수 이름 문자열뿐).

### 커밋 (socraAI_product design)

| 커밋 | 설명 |
|------|------|
| `6a99fc8` | feat(edge-band): 가로 축 left\|right — 홈 칩 행 스와이프 가이드 |
| `f8134dd` | fix(edge-band): 칩 행 규격을 폭 무관 식으로 — 클립 폭 고정 회귀 정정 |

⚠️ 둘 다 **origin/main 미도달**이다. `6a99fc8`은 `origin/feat/edge-scrim-exploration`에 올라가 있고 `f8134dd`는 **아직 미푸시**(브랜치 ahead 3).

### 이어서 — 남은 2건 (계약 문서 정정)

블로커 넷(주미님 판정 · 외부 회신 · 피어 점유 · 되돌리기 어려움)에 안 걸리는 마지막 2건. 둘 다 오늘 작업에서 파생됐고 파일이 clean이었다.

**`socra` 마크 — 인용이 실제와 네 줄 다 달랐다.** `answer-footer.md`(계약·근거 둘 다)가 「외부 published 라이브러리의 remote 인스턴스 · 마스터 `3876:2400`(`remote: true`, key `b60b6f4f…`) · 사용처 3곳 전부 `Property 1=Default` · 로컬 사본 없음」으로 적고 있었다. `Answer/Footer`(`3876:2422`) 안을 열어보니 socra 인스턴스는 **2개**(둘 다 `Face`)고 전부 **DS 로컬** 세트 `4048:1726`의 **`Property 1=Spark`**(`4841:2381`, key `8476aacd…`, `remote: false`)를 가리킨다. `3876:2400`은 실재하지만 **별개의 원격 프록시**이고 실제 key는 `49ec1da5…`라 문서의 `b60b6f4f…`는 **어느 노드와도 안 맞는다.** 원격을 참조하던 시절 기록이 로컬 전환 뒤 안 따라온 것으로 보인다.

**`Menu Item` State 축 — 문서와 마스터가 반대였다.** 문서는 「6 State(`default/hover/pressed/selected/disabled/destructive`) · `engaged` 병합(History Item 방식)은 **쓰지 않는다**」(2026-07-03 결정)인데, 실제 마스터(`3352:1374`)는 **5 State에 `engaged`**다. 오늘 삭제한 `Row/Item`·`ContextMenu/Item` description에도 "hover와 pressed는 engaged 하나로 통합(모바일/PC 공통)"이 적혀 있어 병합 쪽이 관례로 굳은 것으로 보인다. **피그마가 1차 기준이라 문서를 마스터에 맞췄고, 되돌리려면 마스터를 6 State로 바꾸는 게 먼저라는 것도 같이 적었다** — 문서만 되돌리면 같은 어긋남이 재발한다. 코드 공개 `state` 4개는 불변(Figma `enabled` ↔ 코드 `default`, `engaged`는 CSS 의사상태).

오늘 계약 문서 인용을 고친 게 이걸로 **6건**이 됐다(markdown · report-participants · vote-result · answer-footer ×2 · menu-item). 전부 같은 원인 — **노드 ID로 인용해두고 노드가 움직인 뒤 문서가 안 따라왔다.** 룰 30(component key로 인용)이 이미 있는데 지켜지지 않은 자리들이다.

### 커밋 (socraAI_product design)

| 커밋 | 설명 |
|------|------|
| `15f578c` | docs: socra 마크 인용 정정 + Menu Item State 축을 마스터에 맞춤 |

### 열린 항목 91건 — 성격이 다른 넷이 섞여 있다 (2026-08-19 분류)

주미님 "91건이나 있는 게 말이 안 돼". 맞다. 세어보니 **목록이 성격이 다른 넷을 한 줄에 섞어놨다.**

| 성격 | 대략 | 예 |
| --- | --- | --- |
| **지금 막혀 있는 것** | ~10 | Chromatic 704·714·701 육안, 접힘 미끄러짐 모션, 브랜치 처분 |
| **남의 답을 기다리는 것** | ~8 | 첨부 3건 회신, PM 노트 2건, 스켈레톤 스트리밍 전환 |
| **피어가 파일을 잡고 있어 지금만 못 하는 것** | ~6 | `toast.md` 이관 2건, `prompt-pill`·`report-deep`·`report-option` 인용 |
| **언젠가 할 백로그** | **~60** | empty state 일러스트, 전문 시트 스택 미채택, 헤드라인 18px, 다크 묶음 |

**91이 큰 게 아니라 네 번째가 섞여 있는 게 문제다.** 백로그는 줄지 않는 게 정상인데 「열린 항목」에 같이 있으니 매일 90대가 나오고, 그러면 목록이 신호를 잃는다.

**8/19 정리 패스 — 실측으로 5건 종결·3건 중복 확인**

- 죽은 전제 4 — `push·PR 미실행`(전량 push됨) · `미푸시 16건 백업 없음`(같은 사실) · `edge-band.md 낡은 문장`(이미 걷힘, grep 0) · `QuickStart/Group 이름 변경분`(프록시 이름이었음)
- 범위 축소 1 — `Callout success·warning × action` → **`warning`만** (success는 `00e302d`로 신설됨)
- 중복 3 — `PR 미개설`(홈) = `push·PR 미실행`(칩) / `미커밋 36건` = `히스토리 fade 전건 미커밋` / `Side Panel 400 데모 없음` = plan-10:1526 (정본은 plan-10)

**살아 있음을 확인한 것도 있다** — `ChatHistoryPanel.tsx:392`의 180×131 플레이스홀더, 같은 파일 `isEmpty` 기준 fade(435행), `HomePage.stories.tsx`의 EdgeBand 0건. 셋 다 코드에 그대로다.

**제안.** 「언젠가 할 백로그」를 `docs/plans/`로 내리고 워크로그 열린 항목은 **지금 막힌 것 + 답 기다리는 것**만 남긴다. 그러면 이 목록이 다시 읽을 값어치가 생긴다. 주미님 승인 필요.

---

## AX 프로세스 — "일정 예측이 어렵다"에서 완료 정의 불일치까지

기록 상담으로 시작해 규칙 신설로 끝났다. 코드 변경은 없고 판단만 남은 세션이다.

```
AX 프로세스 - 일정 산정 - 구조화 시스템화 장단점 기록하려눈데 어떻게 생각하니
```

### 장단점 표를 버리고 손익분기로 틀었다

먼저 위키를 확인하니 `guide-ops.html:595`에 이미 「AI 도입 시 디자이너 일정 산정 가이드」가 있었다 — 업무 유형별 단축률(반복 UI 20~40% 등), 추가 고려 항목, 화면 1개 예시 표(2.5일 → 2.1~2.7일)까지. 여기에 장단점 문서를 또 만들면 일정 산정 얘기가 둘로 갈라진다.

장단점 나열은 읽고 나서 행동이 안 바뀌는 문서가 되기 쉬워, **"언제 구조화하고 언제 하지 말아야 하는가"의 손익분기** 형태로 방향을 틀었다.

### 프로세스가 선형에서 방사형으로 바뀌었다 (주미님 손그림)

주미님이 종이에 그려 보여줬다.

| | 모양 | 도착 |
| --- | --- | --- |
| **기존** | 순차 체인 — 기획 → 디자인 → 개발 → QA → 배포 | 앞 단계가 끝나면 온다. **내 차례가 언제인지 안다** |
| **AX** | 1인 중심 방사형 — 여러 갈래가 동시에 한 사람에게 수렴 | 에이전트 완료 시점에 **비동기로** 도착. 통제 불가 |

말로 "동시성·병렬도"를 돌던 것이 도형 두 개로 확정됐다.

### 일정 예측이 어려운 진짜 이유 — 셋이 겹쳐 있다

첫 진단은 "작업량이 아니라 도착 패턴"이었는데 그건 셋 중 하나만 본 것이었다.

1. **비동기 도착** — 겹치면 큐가 생기고, 대기는 가동률이 오를수록 비선형으로 늘어난다
2. **건당 검수 시간의 분산** — 10분짜리 확인인지 2시간짜리 재작업인지 열어보기 전엔 모른다. **AI 품질 편차가 내 시간을 결정**한다
3. **범위의 사후 확정** — 엣지 케이스가 진행 중에 발견돼 총량이 시작 시점에 안 정해진다

②③이 주미님이 말한 "초반 비용·재작업"이고, 체감상 ②가 제일 크다.

→ 그래서 **점 추정이 원리적으로 불가능**하다. 기존 산정법(단계별 소요시간 합)은 선형 전제에서만 성립한다. 가능한 건 구간 추정 + 진행하며 갱신뿐이다.

### 정신없음의 뿌리는 불안이 아니라 미결이었다

내가 "안 보고 있어도 되나 하는 불안 때문에 자꾸 열어보게 된다"고 진단했는데 주미님이 잘랐다.

```
불안해서 그런건가? 걍 정신이없는건데. 먼가 마무리가 안되는 느낌인거고. 마무리까지 오래걸리는걸수도있고. 그리고 ai가 예측하는 마무리랑 내가 예측하는 마무리가 다른것도 문제임 얜ㄴ 기획서 내용이 그려지면 끝이라 생각하는데 내가 보기엔 디자인 퀼리티나 ux 검토할게 아직 많아서
```

여기서 원인이 확정됐다 — **AI의 "완료"와 디자이너의 "완료"가 다르다.**

- AI의 완료 = 기획이 그려짐
- 디자이너의 완료 = 품질·UX 검토까지 끝남

에이전트가 "완료"라고 말할 때마다 남은 작업이 **AI 장부에서는 지워지고 사람 머릿속으로 이송**된다. 어디에도 기록이 없으니 마무리 감각이 안 생기고, 병렬이 이걸 증폭한다 — 모든 트랙이 80%까지 가고 **100%에 도달하는 게 하나도 없다.**

### 3단계로 갈랐다 (주미님 정리)

```
아 그치 어케보면 업무 완료 기준과, 타인에게 공유할 수 있는 완료 시점의 공유 기준이 필요한거지(픽스는 아닌거지만 그래도공유는 가능한?)
```

| 단계 | 뜻 | 누가 선언 | 공유 |
| --- | --- | --- | --- |
| `1차` | 기획·구조가 그려짐 | 에이전트 | 공유하지 않음 |
| `공유 가능` | 구조·방향이 섬. 디테일은 미픽스 | 에이전트 자가 검사 → 디자이너 확인 | 팀 공유 OK |
| `픽스` | 품질·UX 검토 끝 | 디자이너만 | 변경 시 공지 |

중간 단계가 생기며 두 개가 풀린다 — ①디자이너 품질 검토가 팀 병목에서 빠진다(공유 가능 시점에 FE 착수·리뷰 시작, 폴리시는 병렬) ②일정을 두 날짜로 말할 수 있다(공유 가능일 = 약속 / 픽스일 = 구간).

### 규칙 신설 (jumi-worklog)

- `docs/20-completion-criteria.md` **신설** (55줄) — 3단계 정의 · 에이전트 보고 규칙 · 공유 가능 기준 7 · 픽스 기준 6 · 근거
- `AGENTS.md` — 「완료 기준」 절 추가. 세션마다 자동 로드되는 진입 파일이라 여기 있어야 실제로 지켜진다
- `docs/00-document-role-map.md` — `20` (Rules/Guardrails) 매핑 1줄

공유 가능 기준 초안은 9개였는데 7개로 낮췄다 — 토큰 준수는 `픽스`로 내리고, 4대 상태 전부 강제와 확정 문구는 완화(핵심 상태만 / lorem만 아니면 됨). 문턱이 높으면 아무도 그 단계를 안 쓰고 `1차`에서 `픽스`로 바로 뛴다.

### 다이어그램은 반려됐다

`AI_product design_guide/artifacts/preview-ax-workflow.html`로 4블록 미리보기를 만들었다(선형/방사형 SVG 대비 · 예측 어려운 이유 3 · 완료 3단계 · 두 날짜 타임라인).

```
별로 인사이트 있어보이진 않네..
```

원인은 그림이 아니라 재료였다. 선형 5박스 vs 방사형 5노드는 한 문장의 도형 번역이고, 트랙 이름도 내가 지어낸 것이라 구체성이 0이다. **실제 트랙·검수 건수가 쌓인 뒤 구체 숫자로 다시 그린다.** 각도는 「완료 선언 = 장부에서 지워져 사람 머릿속으로 이송」 한 장면으로 좁힌다.

### 남긴 것 / 안 한 것

- **안 함** — `review-queue.md`, 뷰어 「지금」 스트립, 위키 승격. 전부 3단계 규칙이 며칠 굴러 실제로 작동하는지 본 뒤다. 순서를 뒤집으면 이 세션이 경계한 "구조화 초반 비용"을 그대로 다시 낸다
- **기각** — 별도 대시보드 페이지(아침 1차 화면이 갈라짐), % 프로그래스(범위가 사후 확정이라 분모가 없다), Notion/FigJam 저장(결정·회고의 정본은 worklog)

### 열린 항목 92 → 74 (2026-08-19 이관 패스)

주미님 "다크 관련된 거니?" → **아니다. 다크는 6건뿐이다.** 세어보니 원인은 둘이었다.

**① 트랙이 27개였다.** 92 ÷ 27 = 트랙당 평균 3.4건. 최대가 8건이고 대부분 2~4건이다. **덩어리가 큰 게 아니라 작은 꼬리가 27개**다. 트랙이 끝날 때마다 못 끝낸 걸 2~4건 남기는데 그 트랙을 이어받은 사람만 닫을 수 있으니 아무도 안 닫는다. 오늘 하루에만 트랙이 8개 새로 생겼다.

**② 같은 항목이 두 문서에 다른 개수로 있었다.** 트랙 4개는 이름에 plan 번호가 붙어 있는데 그 plan에도 자기 미체크가 따로 있었다 — plan-29는 **plan 0 / 워크로그 3**, plan-28은 **plan 3 / 워크로그 8**, plan-20은 **plan 51 / 워크로그 3**. 어느 쪽이 정본인지 정해져 있지 않았다.

**집행(주미님 승인) — plan이 정본, 워크로그엔 링크 한 줄.**

| 트랙 | 처리 |
| --- | --- |
| plan-28 chrome 더미 제거 | 8건 이관 (`0b8ea70`) |
| plan-20 리포트 컴포넌트화 | 3건 이관 — 그 문서엔 미체크 51건이 따로 있다 |
| plan-29 설정 페이지 | 3건 이관 — 그 문서는 미체크 0이었다 |
| plan-30 로그인 에러 | **이관 없이 4건 종결** — C4는 오늘 집행 완료, 나머지 3건은 plan-30에 이미 열려 있던 중복 |

결과 **92 → 74 · 트랙 27 → 23.** plan-20의 51건까지 세면 실제 열린 항목 총량은 워크로그 숫자보다 훨씬 크다 — 줄어든 건 총량이 아니라 **중복과 정본 없음**이다.

**남은 구조 문제.** plan이 없는 트랙 19개(≈74건)는 갈 곳이 없어 워크로그에 남는다. 세션이 매일 트랙을 5~8개 새로 열고 각자 꼬리를 남기는 한 이 숫자는 다시 90대로 돌아온다. 트랙을 열 때 plan을 같이 열든지, 꼬리를 남길 자리를 정하든지 규칙이 필요하다.

---

## Deep 대기 투표 완성 알림 — 결정 3건에서 차주 논의 준비까지

피그마 보드 링크 하나와 함께 시작했다.

```
여기서 몇개 정할게있어
```

### 결정 3건 — 둘은 닫고 하나는 열어뒀다

**① 대기 투표는 채팅방에 남지 않는다.** 대기 중에는 카드가 스트림에 누적되고 리포트를 여는 순간 통째로 사라진다. 누적은 보관이 아니라 **이동 수단**이다 — 자동 전환을 없앤 결과 다음 카드로 가는 수단이 스크롤뿐이라 아래에 쌓여 있어야 한다. 사라짐이 어색해 보이는 원인은 사용자가 그 장면을 **목격한다**는 전제였고, `리포트 보기`의 동작을 「이동 먼저 → 정리 나중」으로 두면 전제가 없어진다.

**② 건너뛰기해도 결과를 노출한다.** 되돌아가 이전 답을 다시 보는 케이스는 없다. 어느 경로로 오든 결과를 본 뒤에만 다음으로 가므로 「탭 투표 → 결과 → 다음」과 「건너뛰기 → 결과 → 다음」이 같은 모양이 된다. 투표율은 떨어질 수 있으나 대기 투표의 목적이 데이터 수집이 아니라 체감 대기 단축이므로 손해로 보지 않는다.

**③ 완성 인지는 하단 콜아웃 한 채널.** 추론 과정은 스트림 안 인라인에 두고 위로 밀리게 둔다. 상단 스티키로 올리자는 안이 나왔지만 **진행률 상시 노출은 대기 투표의 목적과 반대로 작동한다** — 시간을 잊게 하려고 넣은 화면인데 카운트다운을 계속 보여주는 셈이다(7초 진행바에서 초 숫자를 뺀 「재촉 방지」와 같은 이유). 상단 크롬도 Navigation 60 + Thinking 40 = 100이 되어 카드 779 케이스가 잘린다.

세 안 비교 — 주미님 원안(`action` 136) · A안(`inline` 46) · B안(`action` 114). **B안 채택.** A안이 90px을 카드에 돌려주지만 "지금 안 누르면 놓치나", "누르면 투표는 어떻게 되나"에 답을 못 한다. 원안에서 고친 건 셋이다 — 보조 설명 2줄→1줄, "기존의 투표 항목은 사라집니다" 삭제(손실 프레이밍), 표기 "레포트"→"리포트".

### Callout `Type=success, Layout=action` 신설

`Type=info`는 톤이 안 맞는다 — info는 "참고하세요"인데 이 알림은 "다 됐어요"다. 세트에 success+action 조합이 없어 신설했다(`4977:2392`). `2570:1312` 클론에 green 3토큰 재바인딩 + 아이콘 `lucide:info`→`circle-check` 스왑. 세트 572→730, 기존 6변형·인스턴스 영향 0.

**제품 파일 Callout이 `remote: true`라 거기서는 못 고친다** — DS 파일(`DcYgJjGAfObOIM4IyrQjgj` `✅ Components` > `Chat` > `1198:1533`)에서 만들어야 했다.

### 하단 배치 계약

컴포저 위 하단 슬롯 소비처가 둘이 되면서(SA-1a 잠금 안내 · 완성 알림) 규칙이 공통이 돼 계약으로 올렸다. 새로 정한 값은 없고 잠금 안내가 쓰던 걸 넓혔다 — 컴포저 상단 +8 고정(절대 y 금지), 스크롤 비추종, 하단 슬롯 한 장, 폭은 스트림 컬럼, `action` 보조 설명 한 줄 유지(점유 236+8+114=358).

**Toast·Scroll FAB 스택 순서는 처음에 "동시 발생 없음"으로 적었다가 프로토타입에서 반증됐다.** 카드가 화면보다 길면 카드 상단을 읽는 동안 `atBottom=false`라 FAB이 상시로 떠 있다. 계약·근거·plan 셋을 정정하고 `hide`/`above` 두 안을 만들었다 — **`hide` 확정**(주미님).

### 프로토타입 — 정지컷으로는 판정이 안 되는 자리

`DeepWaitVote.stories`에 스토리 5개. **리포트를 오버레이에서 스트림 안으로 옮겼다** — 화면을 덮으면 "카드가 걷히는 걸 목격하는가"를 물을 수 없다.

반려가 세 번 있었다. 즉시 제거는 "샥 사라진다", 페이드아웃은 "얼레벌레 사라진다"(흐려짐은 방향이 없어 '치웠다'로 안 읽힌다), `push`(아래로 밀려 컴포저 뒤 scrim으로 빠져나감)는 판정 대기다. 중간에 잃은 자산도 되찾았다 — 리포트 등장을 단순 페이드로 대체했다가 `VerdictBarMotion`(버블 부유 → 세그먼트 착지 → 바가 차오름)으로 되돌렸고, 하단 scrim fade가 빠져 카드가 뚝 잘리던 것도 복원했다.

주미님 지적 두 개가 구현 결함을 잡았다. "두 개가 별 차이 없어 보여"는 채택안이 실제로는 동시 실행이었기 때문이고, "더 내려가야지 위로 올라가는 게 아니고?"는 리포트를 카드 스택보다 위에 둔 내 오판 때문이었다(둘 다 함정 모음).

### 차주 논의 준비

주미님이 보드를 정리해 A/B 비교 컷은 지우고 논의용 3컷·배너·주석을 `인터랙션 고민` 하위 섹션으로 묶었다. 안건 주석은 네 덩어리다 — **이미 정해진 것**(채팅방에 안 남음 / 건너뛰기도 결과 노출 / 완성 인지는 Callout / 이동 먼저·정리 나중)을 맨 위에 따로 빼 논의가 원점으로 돌아가지 않게 했고, 미정은 걷힘 방식 하나, 갈림길은 A(모션을 다듬는다) B(배치를 되돌린다 — 시트/오버레이)로 갈랐다. Chromatic 공유 링크를 주석에 하이퍼링크로 박았다(빌드마다 주소가 바뀌어 상시 라이브러리 링크 병기).

### 커밋 (socraAI_product design)

| 커밋 | 설명 |
|------|------|
| `00e302d` | docs(callout): success/action variant 신설 + 하단 배치 계약 |
| `098cd4c` | feat(storybook): 대기 투표 완성 알림 프로토타입 — 이동→정리 순서 판정용 |
| `fbc6431` | fix(storybook): 건너뛰기도 결과를 노출 — 8/19 결정 반영 |
| `50d21cc` | fix(storybook): 대기 투표 프로토타입 하단에 scrim fade 복원 |
| `8dd90b9` | fix(storybook): 카드를 전부 걷은 뒤 last가 undefined라 터지던 것 |
| `cdbec3a` | fix(storybook): 리포트 등장에 흡수 모션 복원 + 대기 구간 페이드아웃 |
| `1ce4eef` | feat(storybook): 걷힘 모션 3안 — push 추가 |
| `08c43a5` | docs(plan-10): 대기 투표 걷힘 — 미정 처리 + 차주 논의용 피그마 정리 |
| `f88baa6` | docs(plan-10): 라이브러리 수락 완료 — Callout 4개 success 전환 + 보드 정리 반영 |
| `81e2ccc` | fix(storybook): "이동 먼저"가 실제로는 동시였다 — 걷힘을 스크롤 뒤로 |
| `a4c906b` | fix(storybook): 리포트를 스트림 맨 아래로 — 방향 정정 + 이동 시간 제어 |

`feat/edge-scrim-exploration` push 완료. **origin/main 미도달** — 이 브랜치가 main과 130커밋 차라 트랙 단위 PR은 별도다. 브랜치 분리를 검토했다가 접었다(내 커밋이 같은 브랜치의 선행 작업에 기대고 있고, 11개 중 7개는 이미 피어 push에 실려 원격에 있었다).

### 이어서 — 줄바꿈을 로케일로 가르는 규칙

주미님이 스토리북 「최대 사연」을 보고 "스크롤은 납득되는데 텍스트 줄바꿈 처리가 되어야 할 듯, 가독성이 안 좋아". 원인을 찾다 **그 스토리만의 문제가 아니라는 게 드러났다.**

**직접 원인.** 사연 본문이 `fontSize 17 · lineHeight 1.6 · whiteSpace: pre-line`만 걸고 `wordBreak`가 없다(`DeepWaitVote.stories.tsx:208`). 한글은 브라우저 기본값에서 **CJK로 취급돼 어느 글자에서나 끊긴다** — 어절 중간에서 잘려 읽기가 나빠진다. 카드 폭 361에 17px이라 한 줄이 짧아 더 자주 걸린다.

**DS 전체 구멍이었다.** `break-keep`(`word-break: keep-all`)을 쓰는 데가 **패키지·스토리 통틀어 0곳**이다. `MessageBubble`·`Callout`이 `text-pretty`(고아 단어)만 걸고 줄바꿈 자체는 어디서도 안 다룬다.

**그런데 전역으로 걸면 JP가 깨진다 — 둘이 정반대다.**

| 로케일 | 처리 | 이유 |
| --- | --- | --- |
| 한국어 | `word-break: keep-all` + `overflow-wrap: break-word` | 어절 단위로 끊어야 읽힌다. `break-word`는 URL·긴 라틴 토큰이 `keep-all` 때문에 넘치는 걸 막는 짝 |
| 일본어 | **기본값 유지(keep-all 금지)** | 글자 사이에서 끊는 게 정상. `keep-all`을 걸면 안 끊기는 덩어리가 생겨 넘친다 |
| 영어 | 기본값 유지 | 공백이 있어 기본 동작이 맞다 |

주 타겟이 JP라 전역 적용은 더 위험하다. 그래서 `:lang()`으로 가른다 — **클래스가 아니라 `:lang()`인 이유**는 한 화면에 언어가 섞여도(일본어 UI 안의 한국어 인용) 각 요소가 자기 언어 규칙을 받기 때문이다.

**빠져 있던 전제 — `lang` 속성이 어디에도 없었다.** 패키지·스토리북 전수 확인 결과 `lang`을 세팅하는 코드가 0곳이었고, 스토리북은 로케일을 **React 컨텍스트로만** 넘기고 HTML에는 안 붙이고 있었다. 즉 `:lang()`이 애초에 못 걸리는 상태였다 — `typography.md`가 예고해둔 `auto-phrase`(문절 줄바꿈, `lang=ja` 필요)도 같은 이유로 막혀 있었다. **규칙보다 배선이 먼저 없던 것**이고, 이걸 안 보고 스토리에 `break-keep`만 박았으면 같은 문제가 다른 화면에서 계속 나왔다.

**집행 3건**(`3020c28`) — ①`typography.md`에 「줄바꿈」 절 신설(로케일 표 + "개별 화면에서 손으로 박지 않는다") ②`styles.css` `@layer base`에 `:lang(ko)`만 ③스토리북 `preview.tsx`가 로케일 글로벌을 `document.documentElement.lang`에 반영. 게이트 전부 통과(validate · typecheck · test 134/134 · build · pack:check 279파일).

소비처에 `lang`이 없으면 종전과 동일하게 렌더되므로 **non-breaking**이다. 대기 투표 카드는 스토리를 안 고쳐도 Locale=ko에서 규칙을 자동으로 받는다.

### 커밋 (socraAI_product design)

| 커밋 | 설명 |
|------|------|
| `3020c28` | feat(typography): 줄바꿈을 로케일로 가른다 — :lang(ko) keep-all + lang 배선 |
