# 2026-07-31

어제 배포의 후속. 역할이 끝난 `prism-dist` 브랜치를 은퇴시키고 배포 경로를 npm 한 갈래로 정리했다.

요청:

```text
prism-dist 브랜치 지워줘
기록남겨줘
```

---

## 한 일 — `prism-dist` 브랜치 은퇴

원격·로컬 모두 삭제. 마지막 커밋은 `4739e39 chore(dist): @riiid/prism 0.1.0 빌드 결과물 (git 설치용 브랜치)`. 어제 `0.2.0`이 npm에 올라가면서 역할이 끝났고, 깃헙을 정적 저장소로 쓰는 방식은 유지 비용이 크다는 지적에 따른 정리다.

`.changeset/readme.md`의 배포 금지 방침 블록은 따로 지울 필요가 없었다. Version PR #34에 이미 포함돼 main에 반영돼 있었다. main을 당겨보기 전에 편집부터 해서 중복 변경을 만들 뻔했다 — 고치기 전에 원격 최신을 먼저 읽는 순서를 지켰으면 없었을 일이다.

### 함정

- **한 폴더에서 두 세션이 동시에 git 작업** — `socraAI_product design`에서 다른 세션이 11:20·11:42에 커밋과 브랜치 전환을 했고, 이쪽 세션이 `main`으로 체크아웃해 둔 상태가 `explore/deep-wait-vote`로 바뀌어 있었다. AGENTS.md가 "기본은 한 repo에 한 에이전트"로 금지한 패턴이 실제로 발생했다. 이번엔 유실 없이 지나갔지만 같은 조건이면 다음엔 안 그럴 수 있다.
- **stash 잔여** — `stash@{0}`에 중복 readme 변경분이 남아 있다. 삭제 명령이 권한 게이트에서 막혀 그대로 뒀다. 영향은 없다.

---

## 남은 것

- [ ] 오형남님께 설치 방법 공유 — 슬랙에 올린 `git+ssh://...#prism-dist` 주소가 죽었다. 월요일에 그대로 시도하면 실패한다. `pnpm add @riiid/prism@0.2.0`으로 대체
- [ ] `git stash drop`으로 잔여 stash 정리
- [ ] 디자인 픽스 후 다음 버전 배포 (7/30에서 이월)

---

## 회고

#### 설계 결정

- 배포 경로 정리 완료. 앞으로 내부 공유는 npm 한 갈래(canary → latest)로 간다. 산출물 브랜치는 되살리지 않는다.

#### AI 도구 활용

- 브랜치를 지우기 전에 SHA를 먼저 기록해 두면 되돌릴 여지가 남는다(`4739e39`). 원격은 다시 push해야 하지만 로컬 reflog로 복구는 가능하다.

### 커밋

없음. 오늘 `socraAI_product design`에 올라간 `95ef95d`·`45b0b29`는 다른 세션의 plan-20 재집계 작업으로 `explore/deep-wait-vote` 브랜치에 있고(main 미반영), 내용은 7/29 로그에 이미 기록돼 있다.
