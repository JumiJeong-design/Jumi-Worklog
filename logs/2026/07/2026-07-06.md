# 2026-07-06

7/03 마감 이월분을 정리한 하루. 미커밋 타이포 작업 마감 → MenuItem 가시성(PR) → AI 처리분 4영역 재검토 → 주미님 결정 4건 실행(PR #24·#25 머지, 다크 shadow 통합, 채팅 행간 1.7) → 일본 UX 레퍼런스 반영 → fontFamily 회귀 재바인딩 → markdown 헤딩 정합 → Menu 컨테이너 포팅. Figma write는 read-only 감사·승인·스크린샷 게이트, 코드는 매 묶음 typecheck/test/build/pack/storybook green.

요청:

```
너한테 남은작업뭐있어?
응 너가 처리할수있는것부터 해줘
AI가 처리한 내용(헤딩 폰트, 반응형 pc-mo, 변수-스타일 싱크, 다크 쉐도우) 재검토 이건 된건가? / (Notion 일본 UX 레퍼런스) 반영할부분있나 검토
폰트 패밀리 회귀 재바인딩은 뭐야? / 응 해야할거 해줘
내가 뭐 결정해야하는지 하나씩 제안해봐 → (결정 4건 답변)
(worklog) 여기서 한거 채크하고 남길거있음 적어놔, Dropdown/Context 포팅 + markdown 헤딩 Phase 2 둘도 하면 좋자 / 마저 ㄱㄱ
```

---

### 미해결 항목

- [ ] **반응형 Phase 1c(GATED)** — Figma `heading/1..4`→`text/heading-*` 재바인딩 + 웹 프레임 Web 모드 + `heading/web-1..4` 삭제. 오조작 시 웹 텍스트 깨짐 → 별도 세션, 페이지 단위 점진+스크린샷. PR #24 머지로 전제조건은 충족.
- [ ] **반응형 Phase 2(전체)** — 컴포넌트 `text-[Npx]`→역할/스텝 토큰 이관. 오늘 markdown 헤딩은 **드리프트 고정값 정합**만 했고(아래) 토큰 이관은 아님. 진짜 이관은 `plan/parallel-codex`에 main 동기(`--text-heading-*` 확보) + Phase 1c mo/pc 매핑 확정 후.
- [ ] **main ↔ plan/parallel-codex 동기화** — PR #24·#25 머지로 main이 앞섬(plan/parallel-codex 기준 19앞 2뒤). `--text-heading-*`가 main에만 있음. 언젠가 rebase/merge로 정합 필요(오늘 범위 밖).
- [x] **Figma Menu Item 캐비엇** (7/6 감사로 확정) — 마스터 아이콘 stroke는 상태별 semantic 변수에 이미 정상 바인딩(default=icon-default/disabled=icon-muted/destructive=text-error). 스왑 시 icon-default로 되돌아가는 건 Figma 인스턴스 스왑 플랫폼 한계(중첩 오버라이드 리셋) → 구조 변경 없이 재바인딩 규약. 코드는 아이콘 색 부모 상속이라 무영향. menu-item.md에 확정(`7100e1f`).
- [ ] **fontFamily 회귀 재발 원인** — 82/82였던 fontFamily 바인딩이 78/82로 풀려 있었음(오늘 재바인딩으로 복구). 어느 편집에서 4개(`body/base`·`body/xs`)만 풀렸는지 원인 불명 → 재발 방지책 미확인.
- [ ] **Chromatic 빌드 #99 component-error 원인** — 오늘 빌드(105/106/110)는 정상이나 #99 자체는 미조사.
- [ ] **plan-16 §3 Quick/Deep 결과 화면 7항목** — 주미님이 Figma로 직접 진행하기로. 일본 UX(% 병기 지지) 인풋만 추가.
- [ ] **병렬 WIP(내 것 아님)** — 체크아웃에 ChatInputBar 2line·아이콘 마이그레이션 WIP(`Icon.tsx`·`package.json`·`pnpm-lock.yaml`·chat-input-bar 문서·changeset·icon-migration-review.html)가 미커밋 상태. 명시적 `git add`로 안 쓸어담음. 커밋은 주미님/해당 트랙 몫.

---

### 함정 모음

- **`pnpm visual` 로컬 실행이 깨져 있었음** — 7/01 TurboSnap(`--only-changed`) 도입 후 로컬에서 두 번 실패: ① 스토리북 빌드가 `preview-stats.json`을 안 만들어 준비 단계 exit 255 → `storybook build --stats-json` ② 모노레포라 base dir 못 찾아 exit 254 → `chromatic --storybook-base-dir apps/storybook`. 둘 고친 뒤 빌드 105 published. CI(`release.yml`)는 chromaui/action이 알아서 처리하므로 안 깨졌고 **로컬 스크립트만** 깨져 있었다.
- **PR #25 CI validate 실패** — `menu-item.md`가 `validate-design-system.sh` 형식 위반 2건: `- 노드: 3352:1374` 라인 형식 필요(`- 컴포넌트 세트:` 아님), `## 상태` 정확 매칭 필요(`## 상태 (semantic…)` 부제 불가). 로컬에서 스크립트 돌려 재현 후 수정. 근거 문서 형식은 CI가 강제하므로 새 컴포넌트 문서 작성 시 스크립트 선실행.
- **markdown Phase 2 방식 판단** — "토큰 이관"의 대상 `--text-heading-*`가 main에만 있고(plan/parallel-codex 미보유) + 반응형 mo/pc 매핑이 GATED Phase 1c와 얽혀 모호. 추측 이관 대신 **드리프트 고정값 정합**(Figma `chat/h1~4` = 28/700/1.3, 22/600/1.35, 18/600/1.4, 16/600/1.45)으로 스코프 축소. 코드 컨벤션(전 컴포넌트 arbitrary px)과도 일치. 진짜 반응형 이관은 전제 충족 후로 명시 분리.
- **토큰 드리프트 2건(내가 만든 것 아님)** — Figma `shadow/lg` blur 20 vs 코드 `--shadow-lg` blur 16(다크 shadow 트랙 결정값), Figma border/divider `neutral/100`(#EAEDF2) vs 시맨틱 `border-divider`(#C9CCD2). Menu 컨테이너는 다크 대응 우선이라 토큰 기반 `shadow-lg` 사용, 하드코딩 대신 시맨틱 `border-divider` 사용. 드리프트는 menu.md에 기록.
- **Storybook 스토리 required children** — `MenuProps.children` 필수라 render-only 스토리가 `args` 요구(TS2322). meta에 기본 `args.children` 제공해 해소(MenuItem 스토리와 동일 패턴).
- **아이콘 레지스트리 제한적** — `Icon.tsx`의 `lucideIcons`는 약 30개만. `edit-2`·`trash-2` 없음(`copy`·`share-2`·`check`·`more`는 있음). 스토리 아이콘은 실제 레지스트리 확인 후 사용.
- **Notion 인증벽** — `app.notion.com ↔ notion.so` 리다이렉트 루프로 WebFetch 불가(이전 Socra Core 문서와 동일). 주미님이 내용 붙여넣기로 우회 → 요지를 메모리 보관(`reference_jp_ux_localization`).
- **재검토 workflow 세션 한도** — 4영역 재검토 중 2개(다크 shadow·스토리북 렌더) 에이전트가 세션 한도로 죽음 → 인라인 Bash/Read로 직접 검증(재실행하면 같은 한도). 병렬 에이전트 실패는 인라인 폴백.

---

### 회고 및 인사이트

#### AI 도구 활용

- **Workflow로 스캔·재검토 병렬화** — "남은 작업"은 4에이전트(plan 14/15/16·plan-10·git 미커밋·워크로그) 병렬 스캔으로 취합. "AI 처리분 재검토"도 4영역 병렬(Figma 실측 + git/코드 교차). 결과가 커밋/문서로 이미 흩어진 상태를 한 번에 지도화하는 데 유효.
- **Figma 재바인딩 전 read-only 실측** — fontFamily 회귀 4개는 쓰기 전에 대상 변수(`font/family/base` KR="Pretendard"=현재 literal과 동일)를 확인해 픽셀 0을 보장한 뒤 `setBoundVariable`. 전수 재검증 82/82.
- **컨테이너 포팅 전 `get_design_context` 실측** — Dropdown(194:50)·Context(194:38)의 표면/border/radius/py/shadow/섹션헤더/divider 수치를 뽑아 시맨틱 유틸리티로 매핑. 코드베이스 패턴(ModalDialog/BottomSheet)도 읽어 하우스 스타일 정합.

#### 설계 결정

- **결정 vs 인풋 분리** — 일본 UX 레퍼런스는 `rules.md` 27·28(규칙)로 확정 반영하되, plan-16의 정성 라벨 %/도트 건은 "결정 아님, 인풋"으로만 추가. 브랜드 방향과 상충하는 원색/캐릭터는 미채택(문서 스스로 하이브리드 권장).
- **Menu = 프레젠테이션 컨테이너** — 트리거·앵커링·포털·키보드 메뉴 내비게이션은 범위 밖(Figma 미정의). `role=group`+`aria-labelledby`로 정직하게(구현 안 한 `role=menu` 미주장). 앵커 팝오버는 별도 배치 후보.
- **행간 1.7 통일** — Figma `body/chat`=1.7과 완전 정합 + 일본어 권장(1.7~1.9). user 버블 1.6→1.7, AI 본문·AnswerCard 1.75→1.7.

---

## 미커밋 타이포 작업 마감 + `pnpm visual` 수리

- 7/02~03 타이포(Figma 라이브 반영분)의 로컬 미러가 미커밋이라 Figma↔repo가 어긋나 있던 것을 4커밋으로 마감: `b35cb82`(tokens.json 숫자 스케일·typography.md 채팅 타이포 규칙·typography-style-spec.md·markdown.md 수직 리듬·plan-15) / `07c57ea`(MessageBubble·AnswerCard 17px) / `0f60dfd`(plan-14) / `7b06845`(plan-16).
- `pnpm visual` 수리(`8c38935`): `--stats-json` + `--storybook-base-dir apps/storybook`. Chromatic 빌드 105 published.

## MenuItem 가시성 — PR #25

- `feat/menu-item` 브랜치를 origin/main 기반 worktree에서 `a888081` 단일 cherry-pick → PR #25. `release.yml`이 `on: pull_request`라 PR로 CI Chromatic 빌드·published Storybook에 MenuItem 노출.
- CI validate 실패(menu-item.md 형식) 수정 `880aaa3`, plan/parallel-codex에도 cherry-pick. PR #25 squash 머지(main `c13076f`).

## AI 처리분 4영역 재검토

- **변수↔스타일 싱크**: fontSize 67/82·자간 0 19개·lineHeight/letterSpacing 숫자 스케일·코드 미러 전부 정합. **신규 갭 = fontFamily 바인딩 회귀 4개**(`body/base-r/m`·`body/xs-r/m`, 78/82).
- **반응형 mo/pc**: Figma 골조 값 일치(무바인딩=시각 0, Phase 1c 대기). PR #24 당시 OPEN(이후 머지). 현 스토리북 theme.css `--text-heading-*` 0건.
- **다크 shadow**: 정정분이 로컬 `track/shadow-dark-tokens`에만(원격 미push). 현 다크 theme.css는 md만 다크값, xs/sm 드리프트 잔존 → 오늘 통합으로 해소.
- **스토리북 렌더**: 본문 17px 번들 반영 확인. markdown h1~4가 Tailwind 기본값이라 Figma `chat/h1~4`와 드리프트 → 오늘 수정.

## 일본 UX 레퍼런스 반영 + fontFamily 재바인딩

- `rules.md` **27**(일본어 마이크로카피=안심 설계: 경어체·이유+다음 행동·실수 방지 정보 비은닉)·**28**(정보 밀도 locale 슬롯 분리, 하이브리드: 골격 글로벌 고정·밀도 요소 on/off·i 툴팁·프로그레스) 신설. plan-16 §3에 % 병기 지지 인풋. 요지 메모리 보관.
- fontFamily 회귀 4개 → `font/family/base`(VariableID:5:3) 재바인딩(주미님 승인). 전수 82/82, fontName 무변화(픽셀 0), 롤백=바인딩 해제. 기록 `9e86a5b`.

## 주미님 결정 4건 실행

- ① PR #25 머지 ② PR #24 머지(→ Phase 2 착수 가능) ③ 다크 shadow 트랙 cherry-pick 통합(`1a1792e`·`58ecb4a`) ④ 채팅 본문 행간 1.7 통일(`e951819`). token:build 산출물 diff 0·typecheck·test·build 통과.

## markdown 헤딩 드리프트 정합 + Menu 컨테이너 포팅

- markdown 헤딩(`bcf8bb9`): h1~4를 Figma `chat/h1~4` 정합(28/700/1.3, 22/600/1.35, 18/600/1.4, 16/600/1.45, 자간 0). h5~6·mt-* 유지. 반응형 토큰 이관은 별도.
- `Menu` 컨테이너(`ebd723d`): Dropdown(194:50)·Context(194:38) 플로팅 카드. `sectionHeader`·`divided` prop, `surface-raised`/`border-divider`/`rounded-12`/`py-2`/`shadow-lg`(다크 대응), `role=group`+`aria-labelledby`. 스토리 4종·계약·근거·테스트 1건. README 승격(Context/Dropdown Menu를 다음 배치 후보에서 제거). 검증: typecheck·test(51)·build·pack:check·storybook:build green.
