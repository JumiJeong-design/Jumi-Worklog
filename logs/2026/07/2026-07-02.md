# 2026-07-02 기록

Figma `Socra Design_basic`(`DcYgJjGAfObOIM4IyrQjgj`) 컴포넌트 감사 요청에서 시작 → 손그림·하드코딩·"우리 것 아님" 점검, 소소한 토큰화·오버레이 다크 표면 수정까지. **실제 파일 write는 게이트 통과분만, 매 단계 스크린샷 검증.**

## 컴포넌트 감사 (직접그림 · 하드코딩 · 비정식 컴포넌트)

- 초반 오판 정정 — `get_metadata`가 페이지를 `💻 Links / Reference` 하나만 반환해 "컴포넌트 없음"으로 헛짚음. `0:1`(`✅ Components`) 직접 조회하니 **73개 유닛** 존재. (메모리 `reference_figma_file_key` 갱신: 파일명 `Socra Design_basic`, 컴포넌트는 `0:1`, 페이지목록 API 불완전)
- **Button 로딩 = 손그림** — `State=loading`이 점 3개(`Dot 1/2/3`, ellipse 총 87)를 직접 그림. 이미 `ThinkingIndicator`(`_Dot`×3)·`Spinner` 컴포넌트 존재. → 로딩은 본질이 모션이라 **Storybook 인터랙션**으로(백로그), Figma 정적 점3개는 유지.
- **Modal/Dialog가 우리 Button이 아님** — 액션 버튼이 정식 `Button` 세트(`17:184`)가 아니라 **고아 레거시 `17:44`(solid/l)·`17:104`(outline/l)** + 고아 `Icon/Placeholder`(`17:2`)를 참조(페이지에 없는 orphan·슬래시 네이밍). 대조: Callout은 정식 Button 사용 → Modal만 예외.
- 전수 스윕(인스턴스 875 · 고유 마스터 100): 그 외 고아 참조는 Reference/Exception 키보드·Apple 로고(의도), 🧪 draft 보드 QuestionItem, **Message Bubble `_TextBubble`(1342:727) 고아 프라이빗 마스터**(medium·백로그)뿐.

### 한 일 (Figma write, 검증 완료)

- **Modal 버튼 5개 → 정식 Button 세트** — `312:550/552/554/556/558`를 `589:909`(outline/l)·`589:873`(solid/l)로 swap. 라벨(취소/확인/삭제)·폭·룩 **픽셀 동일**. Modal 참조 = `Button @✅ Components`만 남음(고아 0).
- **radius 하드코딩 토큰화** — Favicon(`2651:113`)·Markdown Table(`1180:3`) radius 8 → `radius/lg`(값 동일).
- **Link Preview 배경** — `2651:115` fill → `semantic/surface/Floating`(라이트 #fff 유지, 다크 대응). History/Header "Socra AI"는 Fractul 브랜드 로고타입이라 **안 건드림**.

## 시스템 전반 불일치 감사 (primitive vs semantic)

- 70개 계약 컴포넌트 중 **52개**가 색을 semantic이 아니라 primitive 램프(neutral/red/blue…)에 직접 바인딩. 아이콘(`semantic/icon-default`)만 준수.
- 역할별 불일치: 흰표면 `neutral/white`×85 vs `surface/Floating`×20 · 본문텍스트 `neutral/900`×54·`neutral/black`×43 vs `text-primary`×12 · 에러 `red/600·700`(기존 semantic error 토큰 사용 **0**) · 기본 보더 `neutral/200`×36 vs `border-default`×6.
- **정정(중요):** `color` 컬렉션이 Light/Dark 2모드라 **neutral 램프 자체가 자동 반전**(neutral/900→다크 #f3f6f9, neutral/100→#191a1d 등). 앞서 말한 "다크 광범위 깨짐"은 **과장**이었음. 실제 다크 결함은 `neutral/white`(→다크 순수 #000, elevated 아님) 오버레이뿐.

### 한 일

- **오버레이 9곳 다크 표면 수정** — `neutral/white → semantic/surface/Floating`: Modal(`244:77/87/97`) · Bottom Sheet(`192:38/64`) · SourcePopover(`3041:1354`/`3042:1364`) · Dropdown Menu(`194:50`) · Context Menu(`194:38`). 라이트 #fff 유지, 다크 `#000→#191a1d`. 컨트롤 knob·아바타·행·버튼·테이블은 제외(floating 아님), Chat Input Bar는 글래스 다이버전스라 제외.
- **판단: 새 토큰 안 만듦** — status(success/info/warning)·surface-inverse. 이유 ① status primitive는 이미 다크 대응 ② 기존 semantic error 토큰조차 미사용(Callout=`red/600` 직접) → 새로 만들면 똑같이 미사용 클러터. 토큰 드리프트 finding은 메모리 `project_semantic_token_drift`에 semantic 카탈로그값과 함께 저장.

## Dropdown / Context Menu 승격 검토 → 통합 플랜 확정 (A안)

- 실측: `Row/Item`·`ContextMenu/Item` = variants 0·props 0(정적 예시, 상태축 없음). 반면 `History Item`·`History/Nav Item`·`Tab Item`·`Settings/Row`엔 State 축이 이미 있음 → 상태 패턴은 파일에 존재.
- 확정(운영관점): 단일 **`Menu Item`**(납작 행 + 상태 틴트, semantic)로 수렴. `History/Nav Item`(둥근 채운 타일)은 별도 패러다임이라 스코프 아웃. destructive = `semantic/text-error`, selected = 체크 인디케이터(전용 배경 토큰 X).

## 타이포 — "헤딩 웹/그냥 차이?" 질문 → AI 채팅 타이포 규칙 조사·문서화

- **발단:** "헤딩에 web도 있고 그냥도 있는데 차이?" → Figma Text Styles 실측. `heading/web-1~4`(SemiBold 28/24/20/18, lh 1.6) = 웹/PC 헤딩, 문서상 `heading/1~4`는 실제로 `chat/h1~4`(Bold/SB 28/22/18/16, lh 1.3~1.45) = 앱·채팅 헤딩. 웹=레벨별 한 단계 큼·행간 160%, 그냥=채팅 본문 흐름용 타이트. 라벨 `heading/1~4` ≠ 실제 스타일명 `chat/h1~4` **드리프트** 발견.
- **본문은 웹 미분리:** `body/*`에 web 변형 없음. "화면 커도 본문은 안 키우고 measure(줄 폭)로 조절"이 정석이자 조사 4개 제품 공통.
- **자간 단위 감사:** `letterSpacing -1.2`는 -1.2**px**가 아니라 **-1.2%**(16px→-0.192px). 미세 음수라 라틴 무해, 일본어 본문 이상값 0은 저우선.
- **조사(워크플로 6갈래):** Claude·ChatGPT·Perplexity·Gemini 채팅 타이포 + 장문 가독성/스트리밍/CJK 원칙. (검증·종합은 세션 한도로 중단 → 조사 데이터 직접 읽어 메인에서 종합.) 공통 패턴: 본문 ~16px·읽기폭 ~66자 캡, user=버블/AI=무버블(크기 동일), 코드 전용 모노 0.875em, 출처=번호배지/하이퍼링크, 스트리밍 CLS 방지, CJK 행간 1.7↑.

### 한 일 (문서)

- `foundation/typography.md`에 **「채팅 타이포 규칙」** 섹션 추가(본문·읽기폭 / 일본어 CJK / 코드·표·인용 / 스트리밍 안정성) + plan 14 교차참조. 미확정 4건 등록.
- `components/markdown.md`에 「간격·수직 리듬(미확정)」 note 추가.
- **plan 14(반응형 mo↔PC)와 정합 확인** — D1(본문 고정·heading/display만 mo/pc) = 채팅 규칙과 동일 결론. `heading/web-*`가 현재 수동 PC 헤딩 세트임을 문서에 명시.

## 다크 shadow 토큰 Figma `shadow/dark/*` 정합 + 병렬 트랙 분리

- **발단:** "다크에서 쉐도우가 안 보인다, 하얗게 해야 하나?" → 확인 결과 **하얀 드롭섀도우 아님**. Figma가 이미 `shadow/dark/*` Effect 스타일(검정 drop + 흰 inner highlight)로 규정(`foundation/shadow.md` 계약 + 라이브 Figma에 스타일 존재 확인). 다크 elevation의 1차 단서는 그림자가 아니라 밝은 surface + 흰 inner highlight.
- **원인(왜 다름):** `theme.css`=`build.ts` 산출물, 소스=`component.tokens.json` shadow 그룹. 코드가 Figma `shadow/dark/*`에서 **동기화된 적 없음** — `md` 다크값만 임의(0.32)로 손세팅(엉뚱한 컴포넌트 커밋 `f7ebe8f`), `xs`/`sm`은 다크값 자체가 없어 라이트와 동일(검정 4~6%) → near-black(#0E0F12/#191A1D) 위에서 사실상 안 보임(대비 계산 확인). `lg`/`xl` 없음, inner highlight 전무.
- **한 일:** `component.tokens.json` shadow 1파일 포팅 — xs/sm/md 다크 = 검정 drop(.55~.66) + `inset` 흰 하이라이트(6~8%), lg/xl 신설, 라이트 blur 정정(xs 6→4·sm 12→8). `Foundations/Shadow & Elevation` 스토리 추가(라이트/다크 xs→xl QA 매트릭스 — Figma엔 있는데 Storybook만 비어있던 갭).
- **병렬 충돌 처리(중요):** 이 체크아웃에 오늘 반응형 타이포 WIP(`build.ts` breakpoint 리팩터=컴파일 안 됨)가 섞여 있어 AGENTS.md "한 체크아웃 한 에이전트" 위반 정황. **worktree 분리**(`../prism-shadow-track`, `track/shadow-dark-tokens`)로 내 소스만 옮겨 clean build.ts로 재검증 → **typecheck 전체·test 49 green**, 커밋 `2936f25`. shared 트리는 내 흔적 0으로 정리(Codex WIP 보존, `git stash -u`/blanket checkout 금지 준수).

## 타이포 — 폰트 변수 ↔ Text Style 바인딩(sync) 감사·정합

- **발단(별개 세션):** "폰트–변수–Figma 스타일 연동 잘 됐나? 변수 바꾸면 스타일이 따라 바뀌나? h1/h2가 변수인가 스타일인가?" → `use_figma` read-only 실측.
- **실측:** typography 컬렉션(로컬, 모드 KR/JP) 변수 33개 전부 존재(size 2xs~7xl·weight·lineHeight·letterSpacing·family). Text Style **82개**. **핵심: 82개 전부 `fontFamily`만 변수 바인딩 — `fontSize`·`lineHeight`·`letterSpacing`·weight는 전부 숫자 리터럴** → "변수 바꿔도 크기·행간 안 따라옴"의 원인. h1/h2는 변수가 아니라 스타일(`heading/1·2`, `chat/h1~4`)이 정상 구조.
- **한 일(Figma write, 검증):** **`fontSize` 바인딩 67/82** — 값이 정확히 일치하는 `font/size/*` 변수에만 물려 **픽셀 무변화**·롤백 가능. 재조회로 유지·무드리프트 확인. 이제 `font/size/*` 변경이 스타일에 자동 전파(= 사이즈 축 sync 성립). off-scale 15개(display/1 56·body/xs 13·foundation 문서용 9~25)는 미바인딩. **publish 미실행(디자이너 판단).**
- **핵심 제약:** `lineHeight`/`letterSpacing` FLOAT 변수는 바인딩 시 Figma가 **px 강제**(160%→1.6px 붕괴, 실험 후 원복). 변수=px vs 스타일=% → **단위 모델 재설계 후 바인딩**(주미님 결정). weight는 `fontName.style` 내장이라 별도.
- **기록:** prism 레포 `docs/plans/15-typography-variable-binding-2026-07.md`(상세·롤백·내일 과제) + `foundation/typography.md` 체크리스트 append.

## 반응형 타이포 mo↔PC — Phase 1 메커니즘 구현·검증 (worktree)

- **발단:** "폰트가 웹/모바일에서 다른 크기로 반응형 치환되게 베리어블 세팅돼 있나?" 감사 → **미지원 확정**(모드 축=Light/Dark뿐, `@media(min-width)`·`clamp()`·디바이스 변수 0개, 3-에이전트 팬아웃 수렴). 과거 `device=mo|pc`(16↔15px)는 6/25 제거됨.
- **구현(build.ts):** 토큰 `$value`에 반응형 축 `{mo,pc}` 추가(문자열·`{light,dark}`와 함께 3형식). `pc`는 생성 CSS 맨 끝 `@media(min-width:768px)` 블록으로 방출 → 색 모드·명시 클래스보다 뒤라 항상 이김. 반응형×모드 혼용은 build 에러로 차단. 계약: `token-contract.md` "반응형(mo↔PC) 토큰 값 형식".
- **검증:** typecheck 3개·**test 49 green**·`token:build --check` 통과. **하위호환**=반응형 토큰 없을 때 theme.css 바이트 동일(원본 build.ts와 diff 0으로 격리확인). 실증=`tooltip.text` 임시 `{mo:14,pc:15}` rebuild로 `@media` 방출 확인 후 원복.
- **범위 판단(주미님 위임):** "능력=전체 스케일 반응형 가능(토큰별 opt-in), 값=보수적(본문·캡션·UI 고정, 제목/display만 pc↑, 사이드바 등 밀도 예외는 개별)". **D1 갱신** — 위 「채팅 타이포」에 기록된 heading-only는 **하드룰로 박지 않음**(웹 사이드바 15px<16px처럼 pc가 더 작은 케이스 존재).
- **병렬 분리:** shadow 트랙과 충돌 방지로 **worktree `../prism-responsive-typo`(`feat/responsive-typography-mo-pc`)** 로 옮겨 작업·검증. 앞 기록의 "반응형 build.ts 컴파일 안 됨"은 **이후 완성됨(green)**. 메인 체크아웃의 내 build.ts는 되돌려 Codex WIP 보존. **미커밋**(값·Figma 게이트 대기). 정본: `prism` `docs/plans/14-responsive-typography-mo-pc.md` + `token-contract.md`.

## 다음 (→ 7/03 계획)

- **반응형 타이포 Phase 1b (내일 이어서):** plan 14 §3-1 v1 값표(3xl 24→28, 5xl 32→40 등) 사인오프 → Figma size 컬렉션 **Device(mo/pc) 모드** 신설(read-only 감사 → 변경안/rollback 보고 → 승인 게이트) → `font.size`를 공개 `--font-size-*`로 승격할지 결정(전체 스케일 소비 전제) → 코드 토큰 `{mo,pc}` 반영 → worktree 커밋/PR.
- **주미님 결정 대기(타이포):** 채팅 본문 16 vs 17px 정합 / `heading/1~4`↔`chat/h1~4` 라벨 드리프트 정리 / plan 14 사인오프 여부 / 일본어 본문 자간 0 분기(저우선).
- **타이포 변수 바인딩 마저(prism `docs/plans/15`):** `lineHeight`/`letterSpacing` 단위 모델 재설계 후 바인딩 — Figma가 px 강제라 (1) unitless(multiplier) 지원 확인 → (2) 미지원 시 px 램프 전환 vs 미바인딩 유지 결정. weight 바인딩 방식·off-scale `body/xs`(13px) 정리 포함. 사이즈 축은 67/82 완료.
- **② 메뉴 통합 실제 제작** — `Menu Item`(6상태 × 슬롯) → Dropdown/Context 재조립 → Overlay 섹션 이동 + 계약 문서.
- **다크 shadow 트랙 마무리** — `track/shadow-dark-tokens`(`2936f25`, green) 시각 검증(다크 눈검·inner highlight·글래스 이중 하이라이트) → Codex build.ts green 후 rebase 통합(생성물 단일 소유 재빌드).
- neutral→semantic 재바인딩은 컴포넌트 만질 때 **기회적으로만**(빅뱅 X). 백로그: Button 로딩→Storybook · `_TextBubble` 정식화 · Chat Input Bar 글래스 표면 · History Item/SourceCard/Callout primitive(다크 elevated) 검토.
