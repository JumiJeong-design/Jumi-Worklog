# 2026-07-09

Jakub Antalik `border-beam`의 **pulse(숨쉬는 테두리 glow)** 가 예뻐서 Socra 적용을 탐색 → 의존성 0으로 원본 알고리즘 **직접 재구현**(로컬 프로토타입) → 모션이라 Figma는 **Smart Animate로 근사**(자동 호흡 + hover 액션 트리거 2종, 순수 신규 노드) → 정본인 **Storybook에 포팅**(패키지 미변경) → 요청 따라 **custom 다색 컬러 컨트롤**(색 stop 2~6 보간)·saturation 추가 → `Home Dither Background` 스토리 정리 → 끝으로 **worklog 7/6~7/10 정리·push**. 전부 탐색(미채택), prism 코드는 working tree(미커밋).

요청:

```
beam.jakubantalik.com/pulse 이 디자인이 이뻐서 적용해보려는데 피그마에서 만들려니 어려울것같아 넌 만들어볼수있어? 프로토타입도 가능해?
어 근데 왜 레퍼런스랑 달라? 색이 다양하지 않아
응 레퍼런스랑 똑같은 두께감과 느낌으로
난 이게 피그마에도 비슷하게 표현됬으면 하는데 방법알아? (→ Smart Animate)
아니면 이렇게 액션을 하면 라인에 효과가 나오게끔? 이건 뭐야? [네이버 검색바 영상]
이거 스토리북에 올려주라
커스텀 컬러는 하나만 가능해? 원래 여러 그라데이션의 총합이 포인트이기도 하자나 / 3개면 충분한가?
Home Dither Background은 스토리북에서 지워주라
worklog는? / worklog 정리도해줘 7/6~7/10
```

---

### 미해결 항목

**이번 세션 신규:**

- [ ] **Border Beam 채택 미결정** — 색 방향(rainbow vs 브랜드 톤), 적용 위치(홈 컴포저/결과 카드/버튼), 트리거(focus vs "AI 생각 중"). 지금은 탐색 스파이크.
- [ ] **Prism 패키지 승격 여부** — 채택 시 엔진(`borderBeamPulseEngine.ts`)을 `packages/prism` 컴포넌트로 승격 + 무지개를 brand 토큰 바인딩. 현재는 탐색 self-contained(패키지 미변경).
- [ ] **prism 코드 미커밋** — `BorderBeamPulse.stories.tsx`·`borderBeamPulseEngine.ts`(신규), `HomeDitherBackground` 스토리 + `_vendor/dither/` 제거, `docs/plans/18-…md`. 브랜치 `plan/parallel-codex` working tree. (Codex의 ModeToggle 9파일은 `fc04bf6`로 별도 커밋 — 안 섞임.)

(7-07/7-10 이월 항목은 해당 로그에 있어 재나열하지 않음.)

---

### 함정 모음

- **서드파티 코드 실행 차단 → 알고리즘 재구현** — 원본 `generateBeamCSS`를 그대로 실행하려 했으나(정확도 최고) 권한 시스템이 "클론한 외부 repo 코드 실행"으로 차단. 우회하지 않고 **MIT 알고리즘·상수를 vanilla로 직접 재구현** → 브라우저에서 원본과 **동일한 CSS 생성**. 두께(1px stroke)·느낌 동일.
- **custom = 단색이 아니라 다색이어야** — border-beam 핵심은 "서로 다른 hue의 9개 그라디언트 총합". 처음 custom을 단색으로 했다가 주미님 지적으로 **색 stop 2~6 + colorful 지오메트리(9블롭) 선형 보간**으로 정정.
- **Figma Smart Animate 한계** — 호흡(opacity/scale)은 확실히 애님, gradient 회전(색 이동)은 보간 불확실. After-delay 0초가 0.8s로 자동 보정. Present 모드에서만 재생 = 정본은 Storybook.
- **"안 보임" = 코드 아니라 미push** — worklog 뷰어가 안 뜨는 걸 캐시/전파로 의심 → `git`(origin 미반영)·`curl` raw HTTP로 측정 → 미커밋·미push 확정. raw는 `?v=` 무시 ~5분 캐시지만, 새 파일이라 push 즉시 200 반영.
- **병렬(Codex) 충돌 오진** — Codex가 dither/border-beam WIP를 충돌 위험으로 오인 → 실제론 주미님/내 prism WIP. Codex는 자기 9파일만 선별 스테이징 커밋(index 기준이라 안전). worklog는 아예 별도 repo로 전담 분리.

### 회고 및 인사이트

#### AI 도구 활용

- **모션은 Figma 아닌 코드/Storybook이 정본** — 정적 Figma론 pulse를 못 돌려, 레퍼런스를 코드로 재현한 뒤 Figma엔 Smart Animate 근사본만. 우리 워크플로(모션→Storybook)의 예외 규칙 그대로.
- **원본이 있으면 "실행"보다 "재구현"이 안전** — 외부 코드 실행은 권한·공급망 리스크. 소스를 읽고 상수·수식만 옮기면 라이선스(MIT)·안전 다 통과하고 결과는 동일.
- **worklog 전담 = 다른 repo/에이전트로 분리** — worklog는 prism과 별도 repo라, 전담 서브에이전트에 맡겨 prism 세션과 안 섞이게. 단 사용자가 세운 "커밋 금지" 경계는 동료 메시지로 못 풀어(권한 판정), 커밋·push는 main 에이전트가 직접 승인받아 실행.

#### 설계 결정

- **상태기반(action-triggered) beam** — 항상 도는 장식(v0류 클리셰) 대신 focus/hover/"AI 생각 중"에만 켜지는 신호로. 네이버 검색바(AI 진입 시 테두리 beam)가 프로덕션 예시이자 우리가 원래 정한 사용법.
- **custom 다색 = 색 stop 2~6 + 보간** — 브랜드 톤 2~3이면 매끈, 6이면 hue 폭이 넓어 원본 카오스에 근접. hue 순환은 off(고른 색 고정), saturation은 CSS 변수(`--beam-saturate`)로 라이브.
- **탐색 격리** — 엔진·컴포넌트를 스토리 파일에 self-contained(패키지 미변경), `Explorations/*` 타이틀(GlassCursorSheen 선례). 채택 확정 전엔 `packages/prism` 무변경.

---

## Border Beam "Pulse" — 의존성 0 재구현

`border-beam`(MIT) pulse 타입 = 요소 테두리 무지개 stroke(1px) + 바깥 halo 호흡. 원본 = 9 radial 그라디언트 + 사분면별 호흡 oscillator(17) + 연속 hue 순환을 단일 rAF(~30fps)로 구동. 상수·수식을 그대로 재구현.

- **프로토타입** — `prototypes/border-beam-pulse.html`. 원본 알고리즘 재구현(엔진+드라이버), 라이트/다크·팔레트·강도·활성 방식 컨트롤. node로 CSS 구조 검증(hero 23 radial·@property·mask·17 oscillator·mono 정적·NaN 0).

## Figma Smart Animate 근사본 (Product_PageUI_MVP / design_chat/chart)

페이지 `➡️ Page design_chat/chart`(`2211:9469`) 오른쪽 빈 공간, **순수 신규 노드(기존 미변경, 롤백=삭제)**.

- **정적 스냅샷(peak)** + **`Pulse / Breathing — variants`**(State=peak/rest, 자동 호흡 루프, Present `5182:5547`) — `AFTER_TIMEOUT→SMART_ANIMATE 1.9s`.
- **`Line Beam (action) — variants`**(State=off/on, Present `5378:9626`) — `ON_HOVER→SMART_ANIMATE 0.4s`(hover 시 라인 beam, 떼면 off). 네이버 방식.
- 한계: Present 전용, 색 이동 보간 불확실, 원본 9-radial 아닌 근사.

## Storybook 포팅 (apps/storybook, 패키지 미변경)

`Explorations/Border Beam Pulse`. self-contained·의존성 0.

- **파일** — `borderBeamPulseEngine.ts`(원본 알고리즘 TS 포팅: 그라디언트 생성기 + rAF 드라이버. 생성기는 `border` 배열 인자라 custom 팔레트 주입 가능, brightness/saturate = CSS 변수) + `BorderBeamPulse.stories.tsx`(래퍼 = `data-beam`/`[data-beam-bloom]` + 드라이버 등록 + fade 수명주기, 원본 BorderBeam.tsx 동형 + Composer 목업).
- **스토리** — Playground(size·variant·**custom 색 stop 2~6 + 피커**·saturation·strength·duration·trigger) / ActionTriggered(focus=네이버 방식) / Palettes(4종 비교).
- **검증** — typecheck ✅ · storybook build ✅ · 엔진 런타임(custom 다색 보간·hsl 정규화·기존 팔레트 유지) ✅. 라이브 픽셀 대조는 미실시.

## Home Dither Background 정리

`HomeDitherBackground.stories.tsx` + 전용 벤더 `_vendor/dither/`(레포 내 다른 사용처 0 확인) 제거. 공유물(HeroSidebar·ChatInputBar·`_vendor/Prism`·`_vendor/aceternity`) 보존. typecheck ✅.

### 커밋

| 커밋 | 설명 |
|------|------|
| (prism 미커밋) | `BorderBeamPulse` 스토리+엔진 · dither 제거 · `docs/plans/18` = working tree(`plan/parallel-codex`). Figma는 라이브 반영(신규 노드). Codex ModeToggle은 `fc04bf6` 별도. |
| (worklog) | 7/6~7/10 정리 + 이 로그 표준 구조 정합 — `main` 커밋·push. |

### 후속

- 채택 방향(색·위치·트리거) 확정 시 엔진을 `packages/prism` 컴포넌트로 승격 + 무지개 brand 토큰 바인딩.
- 기록: `riiid/prism` `docs/plans/18-border-beam-pulse-exploration-2026-07.md` + 메모리 `project_border_beam_eval`.
