# 2026-07-29

하루 종일 하네스 한 갈래. 어제 쓴 AX 기록(06 Deep 대기 투표) 말고, 7/28 저녁에 한 스킬 정비를 사례글로 다시 쓰기로 하고 07을 새로 썼다. 독자를 프로덕트 디자이너로 잡아 "하네스가 뭔지"부터 깔고, 공식 문서 6개를 참조 링크로 달았다. 쓰다가 "검증까지 하고 쓰자"로 방향이 바뀌어 미검증 스킬 3개를 실제로 돌렸고, 거기서 `sync-entry` 오탐이 나와 글의 뼈대가 바뀌었다("스킬이 참조하는 대상이 바뀌면 스킬도 썩는다"). 고치고 재실행해 6건 → 0건 확인. 마지막에 "다른 디자이너가 따라 하려면"을 붙였는데, 방법을 문법 설명이 아니라 실제로 쓰는 방식(링크 주고 "참고해!")으로 잡았다. 워크로그를 쓰는 도중 `write-worklog`에서도 같은 드리프트가 나와 **같은 유형이 3건**이 됐고, 안 하고 있던 Notion 업로드 단계도 폐지했다.

요청:

```text
어제 ax기록 다른거 썼었는데 이번에 opus5나오면서 하네스 수정한걸로 다시 공유할까싶어서
그리고 독자 대상이 프로덕트디자이너들이라 그 기준으로 써줘
그리고 하네스 어떤링크 참조했는지 원본 링크랑 내용도 일부 요약해줘
노션에도 써줘
잘했네 그럼 다른 디자이너가 이대로 해보려면 어떻게 해야하는지 방법도 적어줄수있어?
내가 너한테 링크주고 참고해! 했던거처럼
오늘자 worklog 올려
```

## 한 일 — AX 07 「하네스 정비」 작성 + 노션 게시

`~/Desktop/작업 로그/ax-log/07-claude-code-harness-tuning.md` 신규. 근거는 7/28 커밋 `b92d3e0`(스킬 6개가 없는 MCP 호출 — github 16 / figma 해시ID 4 / notion 5, AGENTS.md 113→85줄, 스킬 등록을 표에서 심링크로, CLAUDE.md @임포트 신설).

세션 로그를 뒤져보니 7/28 작업 때는 공식 문서를 안 보고 로컬 레포만 보고 했더라. 그래서 문서를 새로 확인해 참조 링크 6개를 달고 각각 핵심을 요약했다. 축은 "언제 컨텍스트에 올라오느냐" — CLAUDE.md(항상) / Rules / Skills(이름·설명만, 본문은 호출 시) / Subagents / Hooks / Output Styles. 여기서 디자이너용 규칙 하나가 나온다: 상시 지침의 한 섹션이 "사실"이 아니라 "절차"로 자랐으면 그때가 스킬로 뺄 시점.

노션은 `디자인팀 AI 활용 실험 기록` DB에 페이지 생성(05 옆). 속성은 라인업 공통 / 업무유형 자동화 / 상태 작성완료 / 효과 수준 바로 도입가능 / 성공사례 미체크(미검증 5개 남음).

## 한 일 — 미검증 스킬 3개 실행 검증

전수 재확인부터: 스킬 9개에서 `mcp__` 참조를 다시 뽑아 3곳만 남았고 전부 유효한 도구(`save-ideation`의 `mcp__figma__use_figma`·`get_figjam`). 7/28에 고친 25곳은 되살아나지 않음.

- `session-snapshot` — 정상. 부수로 `socraAI_product design`이 `explore/deep-wait-vote` 브랜치인데 업스트림은 `origin/explore/report-hero`를 보고 4커밋 앞선 걸 잡음.
- `handoff-check` — 정상. Step 0 게이트가 `check-context-freshness.sh`까지 실제 실행해 ok 반환.
- `sync-entry` — **결함.** 돌긴 하는데 6건 전부 오탐.

## 한 일 — 검증 스킬 2개 오탐 수정

`sync-entry`: 뷰어 코드를 읽어보니 구조가 바뀌어 있었다. `ensureLog()`가 로그 본문을 GitHub raw에서 fetch하고(`origin/main` 기준), `getMarkdown()`은 **fetch 실패 시에만** `#entry-날짜` 폴백을 쓴다. 날짜 노출은 `ENTRY_META` 키 또는 embed 블록으로 결정되고, `getPlan()`은 raw fetch가 없어 plan 블록만 embed 필수. 그런데 스킬 Step 2는 여전히 원문 `##` 헤더 수와 `entry-*` embed를 비교하고 있었다. embed 마지막이 7/16이라 7/20~7/28 6건이 전부 "뷰어 누락" — `ENTRY_META`엔 전부 등록돼 있어 실제로는 정상이었다.

고친 방식은 비교 대상 교체가 아니라 **확인 항목 자체를 재정의**. 로그 본문 = origin/main push 여부 / 날짜 노출 = ENTRY_META 등록 여부 / plan 블록 = HTML 존재 여부. 원인별로 고칠 곳이 다르다(push냐 키 추가냐)는 것도 보고 형식에 넣고, "`entry-` embed를 새로 만들지 말 것"을 규칙으로 박았다. 재실행해 오탐 6건 → 0건 확인.

`write-worklog`: 어제 쓰다 만 squash 머지 오탐 수정을 마저 커밋. SHA가 main 조상이 아니어도 내용이 main에 있으면 유실이 아니므로 `git diff --stat origin/main` + `gh pr list`로 재확인하고, 원격 브랜치가 `[gone]`인데 그 뒤 커밋이 있는 경우만 진짜 미반영으로 보고한다. 이 워크로그가 수정본으로 처음 돌았고, Step 2.5가 `55b6ca9` 미푸시를 정확히 잡아냈다.

## 한 일 — 「다른 디자이너가 따라 하려면」 섹션

07과 노션 양쪽에 추가. 문법 설명이 아니라 링크 주고 시키는 방식으로 잡았다. 0단계 이미 반복하는 일 고르기 → 1단계 문서 링크 붙여 프롬프트 통째로 던지기("만들기 전에 구조 먼저 말해줘"가 핵심, 안 넣으면 안 시킨 절차까지 들어감) → 2단계 저장 위치 두 곳 → 3단계 반드시 한 번 돌리기 → 4단계 심링크로 옮기기 → 5단계 주기 점검. 함정 3개도 붙였다.

공식 문서로 확인한 사실 2개: 심링크는 공식 지원("Claude Code follows the symlink")이라 현 셋업이 편법이 아니라 정석. 스킬 파일 수정은 재시작 없이 반영되지만 **skills 폴더 자체를 처음 만든 경우엔** 재시작 필요.

### 함정

- **스킬은 참조 대상이 바뀌면 조용히 썩는다** — MCP 도구 이름이 썩은 것(7/28), `sync-entry`가 옛 뷰어 구조를 보던 것, `write-worklog`가 안 만들어도 되는 embed를 만들던 것. 셋 다 같은 유형이다. 자연어 절차라 문법 오류로 안 걸리고, 결과만 틀리게 나온다. 고친 뒤 반드시 한 번 실행해야 한다.
- **오탐만 티가 난다** — `sync-entry`는 없는 문제를 보고해서 바로 걸렸지만, `write-worklog`는 불필요한 산출물을 더 만드는 방향이라 아무도 안 걸었다. 조용한 쪽이 더 오래 남는다. 스킬을 실행할 때 "결과가 맞나"뿐 아니라 "안 해도 될 일을 하고 있지 않나"도 봐야 한다.
- **노션 자동 링크화** — 본문의 `CLAUDE.md`·`AGENTS.md`·`claude.ai`를 도메인으로 오인해 `http://CLAUDE.md` 같은 깨진 링크로 바꾼다. 백틱으로 감싸면 안 걸린다. 4군데 사후 수정.
- **노션 MCP 긴 페이로드 잘림** — `content_updates`에 긴 본문을 실으면 JSON이 잘려 파싱 에러. 유니코드 이스케이프 없이 평문으로 보내니 통과.

## 한 일 — `write-worklog`도 같은 드리프트, 수정 + Notion 단계 폐지

이 워크로그를 쓰는 도중에 세 번째가 나왔다. `write-worklog` Step 4.6이 "새 날짜는 `entry-YYYY-MM-DD` embed 블록을 추가하라"고 지시하고 있었다 — 오늘 고친 `sync-entry`와 정반대고, 7/28 관행(`ENTRY_META` 등록)과도 다르다. 뷰어가 raw fetch로 바뀐 걸 `sync-entry`만 모르던 게 아니라 `write-worklog`도 모르고 있었던 것. 이쪽은 오탐이 아니라 "쓸데없는 embed 블록을 계속 만들게 하는" 방향이라 티가 안 났을 뿐이다. **같은 유형이 2건이 아니라 3건이었다.**

Step 4.6을 "본문 복사"에서 "`ENTRY_META` 키 등록"으로 바꾸고, `entry-` 블록 생성 금지를 명시했다. plan 블록은 `getPlan()`이 raw fetch를 안 쓰므로 예외로 남겼다. 공개 URL 확인에 cache-bust 재확인을 넣고(오늘 실제로 첫 조회가 캐시였다), 월 검증 보고를 "오늘 기여분 vs 과거 누적분"으로 나누게 했다.

Notion 동기화 단계는 폐지. 실제로는 2026-06-08이 마지막이고 7월 워크로그는 Notion에 한 건도 없었다. 스킬은 지시하는데 실무는 안 하는 상태라 단계를 지우고 "하지 않는다"로 못박았다. AX 사례글처럼 별도 문서를 올리는 건 `ax-log` 스킬 범위라고 경계도 적었다.


## 한 일 — 7/28 후속: 브랜치 정리 · DeepCTA 포팅 · 개편 후 재집계

**브랜치** — `explore/deep-wait-vote`가 `origin/explore/report-hero`를 추적하고 있어 7/28 커밋 4건이 원격 어디에도 없었다. `git push -u origin explore/deep-wait-vote`로 같은 이름의 원격 브랜치를 새로 만들어 추적 불일치를 풀고 밀었다. 기존 `explore/report-hero`는 무변경.

**`DeepCTA` 패키지 포팅**(`1f8e1dc`) — `state=enabled|skeleton`, 전체폭이라 전문가 이름 길이가 버튼 크기를 바꾸지 않는다. 토큰 신규 `color.gradient.deep-cta`(conic)·`color.cta.deep.*`·`deep-cta.*`, `Icon`에 `arrow-right` 추가(피그마 `lucide:arrow-right`와 이름 일치). 무지개 테두리는 padding-box/border-box 이중 배경. 계약에 **라벨 규칙을 실측 숫자와 함께 명문화**했다 — 코드만 옮기면 다음 사람이 또 접두를 붙인다. skeleton은 내용 없는 자리표시자라 `disabled`+`aria-hidden`+`tabIndex=-1`로 포커스·낭독에서 뺐다. typecheck·test 59·build·storybook·문서검증 통과, 생성 CSS에 `deep-cta-*`·`gradient-deep-cta` 확인. **브라우저 렌더는 미확인**(헤드리스 브라우저 없음) — conic 그라데이션 테두리는 눈으로 봐야 한다.

**색 규칙 보드 CTA 3개 정합** — `6397:13101`은 접두 제거 후 교체, 나머지 둘도 인스턴스로 교체. 작업 중 새 보드에 떼어낸 CTA 2개가 또 생겼다가, 주미님이 화면을 크게 개편하면서 정리됐다.

**개편 후 재집계** — 컴포넌트화는 유지됐고 사용이 늘었다. `Badge/Label` 104 → **160**, 떼어낸 CTA 9 → **0**, `Type=info` 고아 16 → **0**. `Badge`(숫자 카운트)는 74 → **3**인데 순위 번호·"추가 코멘트 N"을 디자인에서 뺀 결과라 드리프트가 아니다(주미님 확인). 남은 `Chip` 31개는 전부 숨김 레이어로 화면엔 0개 — "눌리면 Chip, 읽는 라벨이면 BadgeLabel"이 개편 뒤에도 지켜지고 있다. 상세는 plan-20 §5-4.

### 함정

- **숫자만 보고 회귀로 단정할 뻔했다** — 남은 `Chip` 31개를 보고 "안 써야 할 칩이 다시 들어왔나"로 의심했는데, 열어보니 전부 `visible:false`였다. 7/28에 남겨둔 숨김 잔재를 계산에 안 넣고 센 내 실수다. 카운트 비교 전에 가시성 필터를 먼저 걸어야 한다.
- **감사 범위를 페이지 전체로 잡으면 오염된다** — 손코딩 라벨을 세려고 느슨한 탐지식을 돌렸더니 113개가 잡혔는데, 인스턴스 내부 레이어(`StatusBadge`)와 타 제품 보드(`vai-home`의 "팔로우" 버튼)가 섞여 있었다. 이 페이지엔 최상위 노드가 222개다.
- **편집 중인 파일은 세는 순간마다 답이 다르다** — CTA를 쫓는 동안 6 → 9 → 12 → 7로 계속 바뀌었고 노드 ID가 조회 중에 사라지기도 했다. 남이 편집 중일 땐 정리를 미루고, 끝난 뒤 한 번 훑는 게 맞다.

## 남은 것

- **미검증 스킬 5개** — `bump-version`·`record-trap`·`save-ideation`·`prep-meeting`·`ux-review-gate`. 억지로 돌리면 없는 미팅을 준비하고 안 할 배포를 하게 되므로, 실제로 그 일을 할 때 확인하는 걸로 둔다.
- **색 규칙 채택 여부** (7/28 이월) — 시안은 다 그렸고 채택만 남았다. 정해지면 `chart/*` 토큰 승격 · 대기 투표 바(현 1.17:1) · 히어로 7%p 통일 · Storybook 정합이 줄줄이 풀린다. ※ `Type=info` 인스턴스 16개 고아는 7/29 보드 정리 과정에서 **해소**(현재 0개).
- ~~**socraAI 브랜치·업스트림 불일치**~~ — 해소. `origin/explore/deep-wait-vote`를 새로 만들어 추적을 맞추고 밀었다. 로컬에만 있던 4건(7/28 라벨 뱃지·CTA 3건 + plan-16 §8 1건)이 원격에 올라갔다. 기존 `explore/report-hero`는 무변경. 남은 판단은 main 병합/PR 시점뿐.
- **라이브러리 재게시 2건 대기** (7/28 이월) — `Badge/Label` 색 톤 10개(pink~orange)와 `Button/DeepCTA` SemiBold 전환이 아직 제품 파일에 안 왔다. 인스턴스 6개는 게시 시점 상태(Medium)를 들고 있는 정상 상태. 게시는 플러그인 API에 없어 주미님 손이 필요하다.
- **CTA 후속 2건** (7/28 이월, 2건은 7/29 해소) — ~~① `Button/DeepCTA` 패키지 포팅~~ 완료(`1f8e1dc`) · ② 전문가 이름 최댓값 미확인 — 잰 건 파일의 10개뿐이고 카탈로그는 인증 벽, 여유 36px이면 한글 3자쯤 더 길 때 재발 · ③ 그라데이션 7스톱 중 토큰에 있는 건 `cyan/400` 하나뿐, 초록 `#4dd959`·파랑 `#3d80ff`는 팔레트 밖 · ~~④ 색 규칙 보드 CTA 3개 미정합~~ 완료(접두 제거 + 인스턴스 교체, 현재 떼어낸 CTA 0개).
- **라벨 뱃지 후속 3건** (7/28 이월) — ① 색 톤 램프에 다크 값이 없어 다크에서 라이트 값이 그대로 나온다(중립 톤은 반전됨). 뱃지만 고치면 반쪽이라 plan-17에서 통으로 ② `subtle` sm 대비 3.06(`neutral/400` on `neutral/100`) — 시안이 쓰던 값 그대로라 교체로 바뀐 건 없고, 부가 메타라 일부러 흐린 건지 확인 필요(올리면 `neutral/500` 6.34) ③ `Badge`·`Badge/Label`이 "미계약 컴포넌트" 섹션에 있는데 이제 패키지 계약이 생겼다. 옮길 섹션 미정.

## 회고

#### AI 도구 활용

- **"고쳤다"와 "돈다"는 다르다.** 스킬은 코드가 아니라 자연어 절차라 실행 전 검증 수단이 없다. 실행이 유일한 검증인데, AI는 파일을 쓰면 "완료"라고 보고한다. 하루 뒤 돌려보자고 한 건 사람 쪽이었고 그래서 결함 1건이 더 나왔다.
- **글을 쓰려다 검증이 나왔다.** 사례글로 정리하려고 근거를 다시 파는 과정에서 미검증 구멍이 드러났다. 기록이 점검을 유발하는 구조.

#### 설계 결정

- **오탐 수정은 비교 대상 교체가 아니라 확인 항목 재정의였다.** `sync-entry`를 "원문 vs embed 비교"로 두는 한 뭘 비교하든 틀린다. 질문을 "뷰어에서 실제로 보이는가"로 바꾸고 셋(push / 등록 / plan)으로 쪼개니 원인별로 고칠 곳이 달라졌다.
- **따라 하기 안내를 문법이 아니라 방식으로 썼다.** 디자이너 독자에게 `SKILL.md` 규격을 가르치는 대신 "문서 링크 주고 시켜라"를 1단계로 뒀다. 실제로 그렇게 만들어왔고, 규격은 바뀌어도 이 방식은 안 바뀐다.

## 커밋

| 커밋 | 설명 |
|------|------|
| `55b6ca9` | fix(skills): 검증 스킬 2개의 오탐 제거 — 참조 대상이 바뀐 걸 반영 |
| `b427638` | log: 7/29 하네스 스킬 검증·오탐 수정 + AX 07 작성 기록 |
| `2c3a531` | fix(write-worklog): entry- embed 생성 → ENTRY_META 등록, Notion 단계 폐지 |

`riiid/prism` (브랜치 `explore/deep-wait-vote`, push 완료)

| 커밋 | 설명 |
|------|------|
| `1f8e1dc` | feat(prism): DeepCTA 포팅 — Quick→Deep 전환 CTA |
