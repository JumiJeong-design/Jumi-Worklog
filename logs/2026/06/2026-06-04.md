# 2026-06-04 Worklog

## riiid/prism — Figma/Git/Storybook 루프 검증

### 목표

Button 기준으로 Figma evidence -> package contract -> Storybook QA -> 발견사항 회수 루트가 실제로 작동하는지 확인한다.

### 체크리스트

- [x] `riiid/prism` README / AGENTS 확인
- [x] `docs/agent-rules.md` 확인
- [x] `design-system/rules.md` 확인
- [x] Button Figma evidence 확인
- [x] Button package contract 확인
- [x] Button Storybook stories 확인
- [x] `pnpm check` 실행 결과 기록
- [x] `pnpm visual` 실행 결과 또는 중단 사유 기록
- [x] 발견사항을 contract / token / story / rules / worklog / workflow-guide 중 어디로 회수할지 분류
- [x] 다음 액션 정리

### 실행 기록

- 시작 상태: `riiid/prism` Package workflow `fc55102` 성공 확인.
- 기준 문서 확인:
  - `README.md`: `riiid/prism`이 package source of truth이며 Storybook-first, Figma-assisted 방식으로 운영된다고 명시되어 있음.
  - `AGENTS.md`: 변경 전 `docs/agent-rules.md`, `design-system/rules.md`, token/component contract, Storybook story를 확인하는 순서가 명시되어 있음.
  - `docs/agent-rules.md`: semantic/component token 우선, Storybook coverage, check 명령 기준이 정리되어 있음.
  - `design-system/rules.md`: light/dark, text length, token 사용, variant inventory, icon size, touch target, Figma component 사용 규칙이 정리되어 있음.
- Button 루프 확인:
  - Figma evidence: `design-system/components/button.md`, `design-system/preview/button-figma-audit.md`에서 Button node/evidence와 측정값 확인.
  - Package contract: `packages/prism/component-contracts/button.md`에서 variant, size, min-width, height, iconOnly 계약 확인.
  - Implementation/token: `packages/prism/src/components/Button.tsx`, `packages/prism/src/tokens/component.tokens.json` 확인.
  - Storybook: `apps/storybook/src/stories/Button.stories.tsx`에서 Default, Matrix, LongContent, IconOnly story 확인.
- `pnpm check` 결과:
  - 성공.
  - 포함된 검증: token build/check, lint, typecheck, vitest, package build, pack check, Storybook build, demo build.
  - Storybook build 중 Vite chunk size warning은 있었지만 실패는 아님.
- `pnpm visual` 결과:
  - 실행하지 않음.
  - 사유: `pnpm visual`은 built Storybook을 Chromatic 외부 서비스로 업로드하는 작업이라 외부 데이터 반출 위험으로 실행이 차단됨.
  - 대체 확인: `pnpm check` 안에서 local Storybook build가 성공해 package/Storybook 빌드 가능 여부는 확인됨.
- Local Storybook light/dark QA:
  - Storybook dev server는 포트 fallback prompt가 비대화 세션에서 막혀 정적 build를 `http://127.0.0.1:6101`로 서빙해 확인함.
  - Button Matrix iframe을 `theme:light`, `theme:dark` globals로 각각 캡처함.
  - Light: 밝은 surface에서 solid는 dark action, outline/ghost/disabled/loading 상태가 구분됨.
  - Dark: 어두운 surface에서 solid는 light action으로 반전되고 outline/ghost/disabled/loading 상태가 구분됨.
  - 결과: theme class 기반 light/dark token 전환은 정상 동작. Chromatic 업로드 없이 local static Storybook 기준으로 확인.
- 사용자 확인 후 정정:
  - `QA/Theme`와 `Components/Button/Matrix`는 Figma canvas layout 복제가 아니라 QA surface라 레이아웃/라벨/아이콘은 다를 수 있음.
  - 다만 Storybook에 렌더되는 실제 Button component 자체는 Figma Button 디자인을 따라야 하므로, 별도 parity story 추가가 아니라 package Button 구현/token을 수정함.
  - 수정: Button 전용 `button.radius=9999px` 추가, `rounded-control` 대신 Button 전용 radius 적용.
  - 수정: solid/outline/ghost의 enabled/engaged/disabled/loading 상태를 Figma audit 색상에 맞춘 Button component token으로 분리.
  - 수정: disabled/loading 상태에서 `disabled:opacity-50`에 의존하지 않고 Figma state token을 사용.
  - 수정: icon slot 크기를 size별 icon token `16/20/24px`에 맞춤.
  - 재검증: rebuilt local Storybook light/dark 캡처에서 Button이 pill 형태와 state 색으로 렌더됨.
  - `pnpm check` 재실행 성공. Storybook build의 Vite chunk size warning은 기존과 동일한 non-blocking warning.
- 사용자 추가 정정:
  - package source는 바꾸면 안 된다는 조건을 확인함.
  - 위 Button package/token/implementation 변경은 조건과 충돌하므로 `riiid/prism`에서 revert함.
  - Revert 후 `pnpm token:build --check` 통과.
  - 이후 디자인 조정은 package가 아니라 Figma component/design, Storybook QA surface, 또는 evidence/worklog 문서 범위에서 처리한다.
- 사용자 의도 재정의:
  - "package를 바꾸지 말라"는 의미는 특히 개발자가 추가한 구현 코드/package contract/token contract를 디자인 동기화 목적으로 임의 수정하지 말라는 뜻.
  - 디자인 차이가 보이면 먼저 Figma 문제인지, Storybook QA surface 문제인지, package code 문제인지 분류한다.
  - package code 변경이 정말 필요하면 명시적 승인 후에만 진행한다.

### 발견사항 분류

- Button icon 포함 text button의 min-width 기준이 Figma evidence와 package contract 사이에서 다르게 보임.
  - Figma evidence: icon on 상태에서 `s=85px`, `m=98px`, `l=120px`로 측정됨.
  - Package contract/token/implementation: text button min-width가 icon 여부와 무관하게 `s=80px`, `m=96px`, `l=120px`로 적용됨.
  - 회수 위치: `contract` / `token` / `story` 중 결정 필요.
  - 판단 옵션:
    - Package contract를 유지한다면 Figma evidence 문서에 해당 측정값을 historical evidence로 라벨링하고 현재 package contract가 우선임을 명시한다.
    - Figma 측정값을 반영한다면 icon text button 전용 min-width token/contract/story를 추가한다.
- Package-first로 판단하더라도 Figma는 반드시 동기화 대상이다.
  - Button min-width를 package contract 기준으로 유지한다면, Figma Button component도 `s=80px`, `m=96px`, `l=120px` 기준에 맞게 정렬해야 한다.
  - Figma evidence 문서는 과거 측정값과 현재 package contract 기준을 분리해서 기록한다.
  - 운영 루프는 `riiid/prism contract` -> `Figma component sync` -> `Storybook QA` -> `worklog 기록` 순서로 유지한다.
- Figma 실제 component 확인 결과:
  - Button node `17:184`의 실제 `minWidth`는 package contract와 이미 일치함.
  - `Icon=true` withText 기준: `s=80px`, `m=96px`, `l=120px`.
  - 현재 캔버스 렌더 width는 content hug 때문에 `s=84x32`, `m=101x40`, `l=120x48`로 보일 수 있지만, 최소 너비 계약은 package와 동일하다.
  - 따라서 Figma component 수정은 필요 없고, stale evidence 문서만 정리한다.
- Storybook coverage는 Button 상태/크기/variant/long text/iconOnly 확인이 가능함.
  - 회수 위치: `story` / `worklog`.
  - light/dark는 local static Storybook으로 확인 완료.
  - Chromatic visual upload는 외부 업로드 승인 여부를 먼저 결정한 뒤 진행한다.
- 사용자 피드백으로 Storybook Button 자체가 Figma 디자인과 달라 보이는 문제가 확인됨.
  - 회수 위치: `token` / `contract` / `implementation` / `worklog`.
  - 조치: Button 전용 radius/state color/icon-size token을 추가하고 실제 Button 구현을 Figma Button 디자인 기준으로 수정함.
- 이후 사용자 조건에 따라 package 변경은 revert함.
  - 회수 위치: `worklog` / `rules`.
  - 다음 판단: package 변경 금지 조건이 있으면 Storybook에서 보이는 실제 package component styling은 수정하지 않는다.

### 다음 액션

1. 사용자가 원하는 디자인 변경 대상이 Figma component인지, Storybook QA surface인지 분리한다.
2. package 변경 금지 조건에서는 `packages/prism` 구현/token/contract를 수정하지 않는다.
3. 개발자가 추가한 code/contract/token 변경이 필요하면 먼저 승인 여부를 확인한다.
4. Chromatic 업로드가 필요한 `pnpm visual`은 외부 서비스 업로드 승인 여부를 확인한 뒤 실행한다.
