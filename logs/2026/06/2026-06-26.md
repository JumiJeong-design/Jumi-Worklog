# 2026-06-26 기록

작업하며 채움 - **할 일은 계획 탭** 참조.

## Storybook Components 하위 분류 + Figma 재섹션화 정합

### 요청 범위

백로그 "Storybook 정보구조 재정리"(결정 대기)에서 출발. 평면 `Components/*`(24개)를 확장 가능한 하위 버킷으로 나눌 수 있는지 결정 → "제안 5버킷(기본UI/채팅/설정/셸/콘텐츠)이 최선인가" 재검토. 주미님이 "확장성 기준으로 더 나은 분류를 제안하라(Figma도 맞춤)"로 설계 위임.

### 완료 (riiid/prism `plan/parallel-codex` 로컬 미커밋 + Figma `Socra Design system test` write)

- **분류 결정(2-tier)** — 경계 = "제품 무관 재사용 여부". 재사용 프리미티브 `Base UI`(Button/Checkbox/Radio/Toggle/Chip/TabItem/Spinner/Toast/Tooltip) + 제품 표면 `Chat`/`History`/`Navigation`/`Settings`, 예약 `Citation`/`Decision`(계약 포팅 시 활성). 백로그 초안 `Common/Chat/Layout`보다 정교화(History 1급화, Layout→Navigation/Settings 분리, 빈 버킷 미생성).
- **Storybook(코드)** — 스토리 24개 `title` prefix만 변경(시각 0, 렌더 동일), `Markdown`은 Components→Chat 이동(파운데이션 아니었음 정정). 문서 3개 정합(`storybook-map.md` 하위 버킷 절 + 이름 규칙 표 14행 story ID, `usage.md`, `docs/plans/10`). `pnpm storybook:build` 성공 — 새 title 24개 인덱서 충돌·중복 ID 0.
- **Figma 재섹션화(주미님 승인 "진행 — 잔여는 Overlay로" 후 write)** — Components 페이지(`0:1`) 섹션을 코드 분류와 정합. rename 3: UI Components common→**Base UI**, UI Layout→**Overlay**, Citation/Source→**Citation**. 신설 2: **Navigation**(`3114:1389`)←Header Nav Bar/Bottom Navigation/BottomNav/Tab, **Settings**(`3115:1372`)←Settings/Row·Settings/SectionHeader·_SettingsTrailing. Reference/Exception·iPhone16·Chat·History 미변경. `figma-use` 스킬 선행, 이동 전 서브트리 폰트 선로드(appendChild가 폰트 touch → 첫 시도 실패 후 보강). **인스턴스 영향 0**(컴포넌트 key 참조), 컴포넌트 픽셀 불변(통째 이동). 검증: 섹션 재감사 10개 + Navigation/Settings 스크린샷에서 변형 정상 렌더.

### 보류 / 다음

- **백로그 잔여(슬림)** — "개발자가 자주 보는 API/Docs 위치"는 명시 감사 안 함(현 구조상 소비면 Foundations/Components vs 검토면 Pages/Flows/Patterns 충돌은 없음). `Flows/Prototype` 세부 명명 미적용(현재 `Flows`). → `content/backlog.md`에 잔여로 축소 등재.
- **미커밋** — prism 변경(스토리 24 + 문서 3)은 로컬 미커밋(주미님 지시 대기). Figma는 반영됨(version history 롤백 가능).
- **다음 후보(별건, 주미님 "나중에")** — ①미완성 계약 5개(citation/vote-bar/vote-result/question-item/recommended-questions), ③ChatInputBar 전송·타이핑 마이크로 인터랙션 파일럿.

## GlassSurface 커서 추적 sheen 프로토타입 → 테스트 후 보류

### 요청 범위

백로그 "GlassSurface 커서추적 sheen"(의존성0·데스크톱한정·별도 데모) 착수. 21st.dev "Apple Tahoe" 버튼의 "렌즈가 커서 따라 출렁" 하이라이트 1개만 우리 글래스 위에 재구현해 채택 여부 판단.

### 완료 (riiid/prism Explorations · 커밋 대기)

- **데모 완성** — `Explorations/GlassSurface Cursor Sheen`(Interactive + Comparison). 의존성0(pointermove + rAF lerp "출렁" 트레일링 + CSS 변수 `--sheen-x/y`), 데스크톱 한정(`hover:hover`/`pointer:fine`), reduced-motion 스냅, GlassSurface 본체·public API 미변경(탐색 스토리에서만 internal 소스 참조), fallback 경로 무관 동작. typecheck + `storybook build` 통과.
- **결정: 보류(미채택)** — Socra 모바일 퍼스트. 커서(hover)가 전제라 모바일에선 가치 0(터치=추적 포인터 없음→기본 글래스 폴백). 데스크톱/웹 표면 커지면 재고. 기술 검증 끝나 학습 박제(같은 탐색 반복 방지). 보존 방식 = A(repo 파킹, 스토리에 "보류" 헤더).
- **커밋 대기(동시 작업)** — prism 체크아웃을 다른 에이전트(`feat/base-ui-text-field`, TextField 작업)가 점유 중 발견. AGENTS.md 규칙0 따라 공유 체크아웃 동시 git 금지 → 스토리 커밋은 체크아웃이 비거나 `git worktree`로 처리 예정. 스토리는 untracked라 브랜치 전환에도 안전 보존됨.

### 다음

- **홈 배경 임팩트** — 21st.dev 4종 검토(Aurora=CSS / dark-gradient=CSS추정 / grid-glow=Canvas+framer-motion / animated-background=Canvas+rAF 별). canvas 2종은 상시 페인트·모바일 비용·6/25 결론 충돌로 제외 추천, **가벼운 CSS 오로라/그라디언트** 방향 추천(브랜드 cyan→blue, reduced-motion 정적, 라이트/다크). Socra 로컬 재구현 비교 데모 만들 차례(import 금지).

## 홈 배경 임팩트 — CSS 오로라 후보 구현 + Chromatic 프리뷰

### 완료 (riiid/prism, Chromatic 발행)

- **방향 = option 2(가벼운 CSS)** — 21st.dev 4종 중 canvas 2종(grid-glow=Canvas+framer-motion, animated-background=Canvas+rAF 별)은 상시 페인트·모바일 비용으로 제외. CSS 오로라 채택(import 아님, Socra 로컬 재구현).
- **구현** — `Explorations/Home Aurora Background`: 순수 CSS 오로라(의존성0), 브랜드 cyan→blue 절제, 라이트/다크, `prefers-reduced-motion` 정적, 미세 노이즈(밴딩 방지). `Default`(중앙 히어로)/`BackgroundOnly`/`Web`(ChatHistoryPanel 글래스 사이드바, Pages/Home Web 미러). 레이아웃 반복: ChatInputBar 중앙정렬+`md:max-w`, placeholder `text-left`.
- **발행(로컬호스트 없이 보기)** — plan/parallel-codex가 main과 CONFLICTING이라 트랙 PR(#21)이 CI 미동작 → main 기준 깨끗한 `explore/aurora-home-bg`로 분리, PR #22 → Chromatic 빌드 #94. URL: `https://6a1e5942ec7ecc06429b1040-pbibehtciy.chromatic.com/`
- **확정 아님**(스냅샷). 잔여: WebGL 탐색물(`ogl`·`_vendor/Prism.tsx`·`Home Prism Hero` 스토리) 롤백/유지 결정은 별도.

### 짚어둔 것 (별건)

- **plan/parallel-codex 트랙이 main과 충돌**(46 vs 5) — Codex가 Base UI/Overlay 컴포넌트(Skeleton #18·Modal #20·TextField, 내 버킷명 사용)를 main 직접 머지 중. 내 트랙 작업(Components 분류·GlassSurface 등)이 거기 묶여 있어 **트랙 rebase/재정렬 필요**(오늘은 아님).

## Base UI · Overlay 컴포넌트 포팅 (병렬 트랙 — Skeleton/TextField/Modal/Bottom Sheet) → main 머지

### 요청 범위

Storybook QA 중 노드 확인(Toast=Base UI 이유, Scroll FAB→BottomBar 흡수, History 내부 4종·`_SettingsTrailing` 흡수, `_`/`(컴포넌트 계약x)` = 흡수 신호)에서 출발. "노말한 건 미리 Base UI에" 흐름으로 Skeleton·TextField 신규 → Overlay 섹션(1684:1972) 4종 검토 후 Modal→Bottom Sheet 순 포팅. 주미님 "한 개씩, Bottom Sheet까지 봐줄게".

### 완료 (riiid/prism main 머지 — 각 PR Chromatic 검증, validate/typecheck/test/build/storybook 5종 그린)

- **분기 대정리(#17)** — plan/parallel-codex가 main과 43↑/2↓·86파일·add/add(#14·#15 squash 후 장기 브랜치 잔류)로 CONFLICTING → CI 불가. origin/main 위에 "진짜 새 델타"만 단일 커밋 재적용(release.yml=#15 유지, 미빌드 초안 계약 5종 제외).
- **Skeleton(#18, Base UI)** — Figma 240:87 text-line/text-block/card, `surface-muted`+pulse(reduced-motion 정지), shimmer는 계약 예약(Figma 모션 미정의). AnswerCard 인라인 스켈레톤도 이 컴포넌트로 통일.
- **TextField(#19, Base UI)** — Figma 52:49 box/line × 5상태 × label/leadingIcon. radius16 = Tailwind `rounded-2xl`(커스텀 `--radius-2xl`은 Tailwind 기본과 충돌→미채택), disabled=`state.disabled`(6/18 결정), focus=`fg-muted`. storybook-map 제외 항목→Base UI 승격.
- **Modal/Dialog(#20, Overlay 신설)** — Figma 244:105 confirm/destructive/info. **오버레이 인프라 신규**(createPortal·focus-trap·ESC·body 스크롤락·포커스 복원·`role=dialog`/`aria-modal`), 버튼은 패키지 `Button` 합성. **`Components/Overlay/*` 버킷 신설**(예약 IA 진화 경로 실행).
- **Bottom Sheet(#23, Overlay)** — Figma 192:78 selection/action, `BottomSheet` shell + `BottomSheetMenuItem`/`BottomSheetActionRow` 합성, Modal 인프라 재사용. 핸들·safe-area·rounded-t-3xl.
- 4 PR 순서 머지(#18→#20→#19→#23), 충돌은 rebase "둘 다 살리기". 컴포넌트마다 계약+근거문서+storybook-map+스토리+테스트 동반. **규칙0 따라 격리 `git worktree`에서 작업**(주미님 plan/parallel-codex 체크아웃 무관).

### 남은 것 / 보류

- **Overlay 나머지: Dropdown → Context Menu** — 앵커 위치 계산 + 키보드 화살표 내비라 별도 집중 세션 권장. Empty State(241:95)는 "필요없음"으로 보류.
- **Button pill 드리프트** — Figma 버튼 pill(`radius/full`) vs 패키지 `Button` `rounded-control`(8px). 모달/시트/전반 버튼 영향 → 별도 정합 결정 필요.
- **badge 보류(제품 결정 대기)** — Badge(125:94) + Scroll FAB badge(170:89) + History Nav Item `HasBadge`(243:80·243:88). "앱에서 알림 역할 미정"이라 의도적 미포팅(드리프트 아님). `bottom-bar.md` 계약 메모는 중단(미작성).
- **주미님 plan/parallel-codex 트랙**(Storybook 분류·Figma 재섹션화·GlassSurface sheen·Home Aurora)은 이 main 머지분과 rebase 재정렬 필요(위 GlassSurface/Aurora 섹션 line 50 참조).
