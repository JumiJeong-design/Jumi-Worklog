# 2026-09-07

### 미해결 항목

**9/4에서 사흘째 이월되지 않았다.** 9/5·9/6 로그에 이 절이 없어서 그동안 계획 탭이 비어 있었다. 계획 탭은 2026-08-10부터 별도 embed가 아니라 **그날 로그의 이 절**에서 뽑아 쓴다(뷰어 `extractPlanFromLog`). 절이 없으면 탭도 빈다.

9/4에 열려 있던 9건 중 **둘은 이미 닫혀 있어** 아래 「해소」로 적고 이월하지 않는다. 나머지 일곱은 옮기되, **9/4 이후로 상태를 잰 적이 없다.** 줄 끝의 `(미재측)`이 그 뜻이다. 착수 전에 먼저 잰다 — 오늘 plan-54에서 「열려 있다」고 본 넷이 정본에서는 이미 닫혀 있었다.

#### 이미 닫혔다 — 이월하지 않는다

- [x] ~~**무대 버블의 출발 간격을 135ms로 둘지 80ms로 줄일지**~~ — **해소(9/5): 135ms 유지로 결정.** 스토리북 정적 빌드에서 번들 상수만 80으로 바꾼 사본을 따로 띄워 대조했더니, 흡수 전체가 908ms에서 743ms로 165ms 짧아지는 데 그치고 착지가 겹쳐 「각 막대 왼쪽으로 모인다」가 흐려졌다. 80ms는 첨부 서너 장 기준이라 열 명 무대에 옮길 값이 아니다. 근거는 `plan-46` 648줄과 `deep-agent-stage.md` 「모션」
- [x] ~~**DS에 `Deep/Stage` 컴포넌트를 만들지**~~ — **해소(9/5): 만들지 않기로 확정.** 9/4 감사가 낸 「만들지 않기를 권고한다」를 그대로 채택했다. 버블 좌표가 컷마다 달라 어차피 override로 다시 놓아야 하고, 카드는 모프 연속성 때문에 화면이 소유한다. `plan-46` 664줄 · plan-10 항목

#### 게시 뒤 이어서 할 것

- [x] **`heading/web-2·3·4` 스타일 삭제** — 9/4 저녁에 `web-3`→`heading/2` · `web-4`→`heading/3` · `web-2`→`heading/0`로 이관했다(DS 15 · 제품 7, 크기 변화 0). `web-1`(28)·`web-0`(26)은 같은 크기의 짝이 없어 남긴다. **해소(9/7): 게시는 오늘 10:36에 있었다.** 마스터를 따라오지 않은 잔여 6곳을 교체하고 셋을 지웠다 — 아래 「(다른 세션) `heading/web-2·3·4`를 지웠다」 절
- [x] **`Sheet Body` 고아 마스터 4개** — DS `계약 불필요`에 재구성해(`6405:3531`·`3556`·`3581`·`3606`) 게시 뒤 제품 `Sheet/Shell` 본문 슬롯 3곳을 갈아 끼웠다. 고아라 옮길 수 없어 「구판」 이름만 붙였다. **해소(9/7 재측): 「지워도 되면 지운다」가 성립하지 않는 항목이었다.** 구판 고아는 넷이 아니라 둘(`9278:185810`·`9278:185874`)이고, 그 둘은 같은 날 참조를 0으로 끊어 무력화까지 끝나 있었다 — 아래 「`Sheet Body` 고아는 지울 수 있는 항목이 아니었다」 절

#### 주미님 판단이 필요한 것

- [ ] **모션을 어디까지 매끄럽게 다듬을지** — 제안은 흡수→리포트 경계 하나만이다(STG 9/15). 컷 경계 전반은 사용성 재점검 때. PR #372 본문 결정 항목 첫 줄 (9/2 → 9/3 → 9/4 이관) **(미재측)**
- [ ] **다크 회색 램프에 스크롤바 자리가 없다** — 알고 고른 값. 다크 회색이 한 단 늘면 그때 다시 잰다 (9/2 → 9/4 이관). **추적 정본은 이제 `plan-30` §4-12 B행이다(9/7 이관).** 닫을 때 그쪽도 함께 닫는다. **9/7 저녁에 조건이 한 번 발동했는데 안 풀렸다** — `neutral/450`이 늘긴 했지만 다크 450은 `#939599`로 `#86888C`보다 **밝은** 쪽이라 빈 구간(`#303235`~`#86888C`)의 반대편이다. 평소 막대는 `state-disabled-border`라 1.35:1 그대로다. 다음에 램프가 늘 때도 **어느 쪽으로 느는지**를 먼저 본다

- [ ] **색 램프 76칸을 코드로 포팅할지 — 범위 결정** — 피그마에는 다크 값이 80칸 전부 있는데 코드는 4칸만 가져왔다(9/7 실측). 전부 넣으면 라이트로 굳어 있던 다른 소비처도 함께 뒤집혀 시각 QA 범위가 넓어진다. 아래 「색 램프 다크 값은 이미 피그마에 다 있었다」 절
- [ ] **`orange/100` + `/800`이 다크에서 3.83으로 AA 미달 — 30곳** — 글자를 순백으로 해도 4.49라 fg로는 못 넘긴다. 배경을 `/50`으로 내리면 8.37이 되지만 피그마 마스터 바인딩을 바꾸는 write다(규칙 58 — 계약·코드까지 같은 턴에)

#### 나중에

- [x] ~~`surface/sunken`이 다크에서 순검정 — 다크 일괄 점검 때 (plan-52)~~ — **해소(9/7 재측).** 9/4 램프 재설계가 `neutral/white`의 다크를 `#000000`에서 `#141518`로 올려 순검정이 아니게 됐다. 검토할 구조가 남지 않았다. `plan-52` §7 표 · `plan-30` §4-12

#### 다른 세션·조건부

- [x] **웹 설정 「정보」 탭 다이얼로그(`11183:13664`) 포팅** — 다른 세션 몫(9/3 주미님 지시). **해소(9/7 재측): 그 세션이 이미 했다.** 그 노드 안이 `Settings/Dialog Shell` 구조의 인스턴스(`11870:8277` · 660×540)로 바뀌어 있다
- [ ] **판단 규칙 중 훅으로 옮길 다음 것** — 44-5-2 훅(#418)이 헛걸림 없는지 며칠 보고 넓힌다. `record-trap` 스킬에는 9/4에 Step 0(「코드로 막을 수 있나」)을 넣었다. **9/7 재측: 「사흘치 발화 기록」이라는 것이 없었다** — 발화 로그는 오늘 05:07Z에 처음 쓰였고 한 줄뿐이다. 세션 4,997건을 되재서 대신 쟀고, 2차 좁힘 뒤 실사용 헛걸림 후보는 `미커밋: 0건` 세 건이었다. 넓힐지는 로그가 며칠 쌓인 뒤에 본다 — 아래 「훅 발화 기록은 사흘치가 아니라 한 줄이었다」 절

#### 인수인계 — 여기서 끊었다 (9/7)

**이 세션은 여기서 끝낸다(주미님 지시). 위 일곱은 손대지 않았다.** 재는 법만 적어 둔다.

- **가장 싸게 재지는 둘**: 「판단 규칙 중 훅으로 옮길 다음 것」은 `#418` 훅의 사흘치 발화 기록만 보면 된다. 「웹 설정 「정보」 탭 다이얼로그(`11183:13664`) 포팅」은 그 노드가 인스턴스로 바뀌었는지 한 번 조회하면 끝난다. 둘을 재면 열린 항목이 실제로 몇 개인지 윤곽이 잡힌다.
- **`heading/web-2·3·4` 삭제**와 **`Sheet Body` 고아 마스터 4개**는 게시가 선행이다. 9/4 이후 게시가 있었는지부터 본다.
- **미재측 일곱을 그대로 믿지 말 것.** 오늘 plan-54에서 「열려 있다」고 본 넷이 정본에서는 이미 닫혀 있었다. 9/4 항목 아홉 중 둘도 9/5에 이미 닫혀 있었다. **비율로 보면 절반 가까이가 이미 끝나 있을 수 있다.**
- **9/5·9/6 로그에는 이 절이 없다 — 이대로 둔다.** 그 이틀은 주말이라 계획 절이 없는 것이 정상이다(9/7 주미님 확인). 소급 작성은 하지 않는다. 기록은 두 날 모두 들어 있다.

**누가 이어받나 — 이 세션이 아니라 새 세션이다.**

- **일곱 중 에이전트가 지금 잴 수 있는 것은 넷뿐이다.** 「모션을 어디까지 다듬을지」와 「다크 회색 램프 스크롤바 자리」는 주미님 결정이 있어야 닫히고, `surface/sunken`은 다크 일괄 점검이 열려야 잰다. 그래서 재측으로 줄일 수 있는 폭은 일곱에서 넷까지다. **「일곱을 다 재겠다」로 잡으면 셋에서 막힌다.**
- **이 세션에 남기지 않는 이유는 브랜치와 워킹트리다.** 이 세션은 `docs/contract-usage-v109`에 있고 워킹트리를 옆 세션과 공유한다. 오늘 실제로 옆 세션의 미커밋 워크로그 절이 내 커밋에 딸려 갔다. 남은 넷 중 둘(`heading/web-*` 삭제 · `Sheet Body` 고아 마스터)은 재고 나면 **피그마 쓰기와 커밋으로 이어진다.** 커밋할 일이 있는 작업은 세션 시작 때 `git worktree`로 끊는 편이 싸다 — 시작한 뒤에는 못 끊는다.
- **맥락도 안 맞는다.** 이 세션이 쥔 것은 워크로그 뷰어의 추출 로직인데, 남은 넷은 피그마 게시 상태와 훅 발화 기록이다. 이어 붙이면 안 쓸 맥락을 그대로 끌고 간다.
- **서브에이전트로 쪼갤 일도 아니다.** 넷이 서로 독립이지만 각각 한두 번 조회로 끝나서, 팬아웃 준비 비용이 일보다 크다. 한 세션에서 차례로 잰다.
- **순서는 싼 것부터다.** ① `#418` 훅 사흘치 발화 기록 ② 노드 `11183:13664`가 인스턴스로 바뀌었는지 ③ 9/4 이후 게시 여부 — ③이 나와야 `heading/web-*`와 `Sheet Body` 둘을 한꺼번에 판정한다.

**작업 폴더 상태 (9/7 늦게 해소):** 워크트리 `stage-resize-story`와 브랜치 `docs/plan46-resize-story`·`feat/deep-stage-resize-story`를 지웠다. 아래 「(다른 세션) 머지 끝난 워크트리·브랜치 정리」 절에 있다. **미커밋 변경은 없었다.**

**드로어 폭 변경 다섯 파일은 미커밋으로 둔다 (9/7).** `docs/contract-usage-v109` 워킹트리에 `HomePage.stories.tsx` · `ChatHistoryPanel.stories.tsx` · `chat-history-panel.md` · `layout.md` · `plan-10`이 남아 있다. 스토리 둘은 전부 내 줄이지만 문서 셋은 옆 세션 미커밋 변경과 같은 헝크라 끊을 수 없다. **문서 셋을 만진 세션이 자기 몫을 커밋한 뒤에 얹는다.** 자세한 내용은 아래 「모바일 히스토리 드로어 폭」 절에 있다.

- [x] ~~**PR [#534](https://github.com/riiid/prism/pull/534) 머지** — 에이전트 버블 흡수 대상을 「대표 선택지 한 곳」으로 확정한 계약·plan-16 기록~~ — **해소(9/7): squash 머지 완료**(`a1b7364e`). `origin/main`에서 계약 절과 plan-16 §7 기록 둘 다 확인했고, 워크트리와 브랜치도 지웠다

#### 인수인계 — plan-59 세션 (9/7 밤)

**이 세션은 `plan59-close-five` 워크트리에서 돌았고 커밋은 다 끊어 냈다.** 워크로그 레포도 파일을 지정해 add했다.

- [ ] **첨부 한도 문구를 v1.0.10 값으로 — 피그마가 먼저다.** 정본 §3.4.2가 이미지 장당 10MB · 문서 2MB로 올라갔고 페이지 제한은 없어졌는데, 제품 파일 `✅ chat/report_1.09`에 옛 문구가 10곳 남아 있다(안내 5 + 5, 에러 칩 「5MB 초과」·「5페이지 초과」). 코드도 같은 옛 값이다(`ChatInputBar`·`AttachmentChip`·`AttachmentDnD`·`Prototype` 스토리). **풀리는 조건:** 피그마 문구가 v1.0.10으로 갱신되면 코드가 따라간다. 코드부터 고치면 순서가 뒤집혀 두 번 고친다
- [ ] **v1.0.10 전체 델타 재판정 — 아직 승인 안 받았다.** 정본이 v1.0.9에서 v1.0.10으로 올라갔는데 레포 미러 `docs/product-spec/`는 v1.0.9에 멈춰 있고, plan-59의 표도 v1.0.9 기준으로 짜여 있다. 첨부 한도 하나가 바뀐 것을 확인했을 뿐이라 **다른 절도 바뀌었는지는 안 쟀다.** 착수 전에 주미님 승인이 필요하다. 다른 세션이 이미 하고 있는지도 먼저 본다

**어디서 끊었나 — PR 둘 다 머지했다.** [#543](https://github.com/riiid/prism/pull/543)(plan-59 재측 + `ModalShell` 간격)이 `bf25b2d7`, [#548](https://github.com/riiid/prism/pull/548)(`LoginButton` 디센더 잘림)이 `1df900e8`로 squash 머지됐다. `UNSTABLE`이었지만 그건 Chromatic `UI Review`·`UI Tests`가 `PENDING`이라 붙은 것이고 **플랜 한도로 막혀 있어 필수가 아니다** — 필수 게이트 `validate`·`check`·`storybook`은 셋 다 SUCCESS였다.

**머지 뒤 `origin/main`에서 실제 내용까지 확인했다** — plan-59가 열림 12 / 닫힘 49로 브랜치와 같고, `gap-2.5` 2곳 · `leading-5` · 「첨부 한도 문구를 v1.0.10」 항목이 다 들어가 있다. 워크트리 `plan59-close-five`와 브랜치 둘, `backup-p59*` 태그 다섯을 지웠고 **미커밋 변경은 없었다.** 원격 브랜치는 자동으로 지워졌다.

#### 체크박스로 두지 않은 것 둘 (9/4에서 그대로 승계)

- Android 짧은 뷰포트 레버(카드 축소 · 건너뛰기 스티키) — **실기기 확인 뒤**(주미님 「예측이 어려운 거 아님?」). 개발 참고값은 plan-25 §8-5 하한
- **`Settings/Dialog Shell` 세트 게시가 `2026-09-04T01:36:56Z`에 멈춰 있다** — 이미 놓인 제품 16장은 슬롯 override로 다 닫혀 화면에 문제가 없다. 막히는 자리는 셸을 새로 꽂을 때 하나뿐이고, 꽂는 사람이 슬롯을 세로 `HUG`로 두면 끝난다. 게시를 다시 부탁하지 않기로 했다(9/4 「게시를 뭐자꾸해?」)

#### 인수인계 — Quick 카드·JP 언어 모드 세션 (9/7 밤)

**이 세션은 `docs/contract-usage-v109` 워킹트리에서 돌았다.** 커밋은 `94616ab2` 하나뿐이고 파일 둘을 지정해 add했다. **푸시는 안 했다.**

**이 커밋으로 PR을 열면 안 된다.** 원격 `origin/docs/contract-usage-v109`는 이미 지워졌고(`[gone]`), 이 브랜치는 `origin/main`보다 **27 커밋 앞서 있다.** 그중 오늘 것은 내 `94616ab2` 하나고 나머지 26은 9/2~9/3에 다른 세션들이 쌓은 것이다. 그대로 푸시하면 남의 커밋 26개가 딸려 간다. **올릴 때는 `origin/main` 위에 새 워크트리를 떼고 그 커밋만 cherry-pick한다.** 두 파일 모두 오늘 나만 만졌으니 충돌은 없을 것으로 본다.

- [ ] **KR 페이지 `✅ chat/report_1.09`의 모드 지정이 세 개에서 0개로 바뀌었다** — 첫 조회에서는 세 개였는데 재조회하니 비어 있었다. 주미님 실시간 편집으로 보고 그 페이지는 건드리지 않았다. **풀리는 조건:** 주미님이 의도한 것인지 답하시면 끝난다. 의도치 않게 풀린 것이면 세 컬렉션에 KR 모드를 다시 지정한다
- [ ] **`design-system/`과 `packages/prism/component-contracts/`의 같은 수치를 훑어 어긋난 곳 세기** — 아직 승인받지 않았다. 오늘 하루에 두 건이 나왔다(`score-bar`는 양쪽 다 낡음, `deep-cta`는 계약만 낡음). **재는 법:** 계약 문서가 px·개수를 적은 줄을 뽑아 같은 이름의 근거 문서와 대조한다. 어느 쪽이 맞는지는 피그마 노드에서 읽는다

- **JP 검수 페이지 두 곳의 텍스트 스타일 끊김 210여 개는 안 해도 되는 일일 수 있다.** 그 페이지는 원래 스타일이 통째로 끊긴 사본이라, JP 검수가 끝나고 페이지를 정리할 계획이면 손댈 이유가 없다. 정리 계획이 없을 때만 잰다
- **CTA 두 줄 넘침은 고치지 않기로 확정했다.** 되돌아볼 항목이 아니다. 근거는 아래 「모드를 걸자 CTA가 두 줄로 넘쳤다」 절에 있다


## 어제 「열려 있다」고 본 네 항목이 정본에서는 이미 닫혀 있었다

plan-54를 읽고 열린 항목 넷을 재측했는데, 내가 읽은 파일이 정본이 아니었다.

작업 폴더의 브랜치가 `docs/contract-usage-v109`고 `origin/main`보다 뒤처져 있었다. 그래서 plan-54가 `??`(untracked)로 보였다. 커밋 안 된 새 파일이라 읽었는데, 실제로는 **origin/main에 이미 커밋된 문서의 옛 사본**이었다. 로컬 284줄 · 열린 체크박스 4개, 정본 359줄 · 열린 체크박스 0개. 정본 상태 줄은 「완료 (2026-09-02) … 컷 부재 여섯 전건 닫힘. 열린 항목 0」이다.

넷이 정본에서 어떻게 닫혀 있었는지는 이렇다.

- `Answer Card` 361은 `#478`로 닫혔다. 「카드가 아니라 토글을 감싼 26px 빈 프레임이고 이름만 그렇다」. 어제 내가 노드를 열어 보고 똑같은 결론에 다시 도달했다.
- `AI 헤드헌터` 카탈로그 대조는 주미님이 「더미 그대로 둔다」고 이미 정하셨다.
- DS 마스터 description 정본 표기는 v1.0.9로 이미 갱신돼 있었다.
- 컷 부재 여섯은 전건 닫혔다.

`??`를 「아직 아무도 커밋 안 한 새 문서」로 읽은 것이 화근이다. 공유 워킹트리에서는 브랜치가 뒤처져 있을 때 **이미 머지된 문서도 untracked로 보인다.** 파일을 읽기 전에 `git log origin/main -- <경로>`를 한 번 봤으면 걸렀다.

작업 폴더의 낡은 사본은 정본 내용으로 덮었다. 지우면 그 폴더에서 plan-54를 아예 못 보게 되고, 그냥 두면 다음 사람이 또 같은 것을 읽는다.

## 9/6 피그마 반영을 plan-54에 한 줄로 남겼다

정본이 「완료 · 열린 항목 0」이라 체크박스를 다시 열지 않았다. 열면 집계만 부푼다. 완료 항목 목록에 `- [x]`로 한 줄 붙였다 — 어느 범위가 빠져 있었는지, 왜 부모부터 풀었는지, 되돌리는 방법, 숨은 형제 26건을 남긴 것까지.

## 숨은 형제 26건도 고칠 것이 없었다 — 사흘에 세 번 같은 자리에서 넘어졌다

주미님이 「숨은 형제」가 뭐냐고 물으셔서 열어 봤다. 내가 만든 말이라 뜻이 안 통했고, **나도 그게 뭔지 안 보고 있었다.** 「켜면 어긋난다」고만 적었지 안에 뭐가 들었는지 확인한 적이 없다.

토글 자리에는 프레임이 둘 겹쳐 있고 하나만 켠다. 꺼진 쪽에 든 것은 문구 하나였다 — 「이건 제가 바로 답해 드릴 수 있겠어요.」 사고 과정 없이 바로 답할 때 토글 대신 보여 주는 대체 문구다.

재 보니 768로 고칠 이유가 없었다. 그 프레임은 배경도 테두리도 없고(`fills` 0 · `strokes` 0), 문구는 왼쪽 정렬에 x=0이다. 폭이 361이든 768이든 글자가 시작하는 자리가 같고 보이는 픽셀이 같다. 17px에 줄 높이 170%면 한 줄이 29px인데 실제 높이가 정확히 29라 한 줄로 들어간다. 22자짜리다. 문구가 길어지면 361에서만 두 줄이 되는 것이 유일한 차이다.

**같은 실수를 사흘에 세 번 했다.** `StepText`는 `maxWidth: 245`라는 값을 보고 「잘린다」고 했고(실제로는 41px 여유), 이번엔 `361 FIXED`를 보고 「어긋난다」고 했다(실제로는 픽셀이 같다). 둘 다 **설정값을 읽고 화면 결과를 짐작한 것**이다. 노드를 열면 5분이면 끝났다.

plan-54와 9/6 기록에 남은 「켜면 어긋난다」를 실측으로 고쳤다. 틀린 근거를 두면 다음 사람이 26건을 또 고치려 든다.

## 무대 폭을 실제로 흔드는 스토리를 만들었다 — 고친 것을 볼 자리가 없었다

주미님이 창 너비를 1600 → 1150 → 1600으로 흔든 컷을 주시면서 「이런 경우엔 어떻게 변해야 맞냐」고 물으셨다. 컷에서는 아무것도 안 변했다.

**무대가 안 변한 게 아니라 폭이 안 변했다.** 흐름 스토리(`DeepJourney`·`DeepScenarioFlow`)는 기기 화면을 흉내 내는 고정 폭 틀이라, 창을 좁히면 화면이 좁아지는 게 아니라 잘린다. 안쪽 무대 폭은 그대로니 `ResizeObserver`가 발동하지 않고, `#501`에서 고친 코드가 실행될 일도 없다. **교체 전 빌드에서도 안 깨진 것이 그 근거였다.** 계약의 「정착한 뒤 폭이 바뀌면 애니메이션 없이 최종 프레임으로 다시 앉는다」가 여태 회귀 테스트로만 담보돼 있었던 것이다.

그래서 폭이 실제로 창을 따라 움직이는 스토리를 하나 만들었다. 「폭 흔들기」로 계단 여섯 단(880 → 520 → 361 → 720 → 440 → 640)을 한 번에 밟고, 화면 아래에 무대 폭·단계·판정이 실시간으로 뜬다. 정착한 뒤 부유로 떨어진 적이 있으면 그 자리에서 빨갛게 바뀐다. 2지선다를 같은 시험대에서 재려고 선택지 토글도 넣었다. 기존 2지선다 스토리는 고정 폭이라 흔들 수가 없다.

### 시험대가 빨간불을 낼 수 있는지 먼저 봤다

초록불만 내는 시험대는 시험대가 아니다. 픽스를 잠깐 끄고 같은 순서를 밟으니 폭을 바꾼 순간 단계가 `floating`으로 떨어지고 구간 다섯 칸이 전부 폭 0이 됐다. 확인하고 컴포넌트는 원본 그대로 되돌렸다.

여기서 하나 알았다. **깨져도 버블은 도로 뜨지 않는다.** 흡수를 마친 DOM이 `opacity: 0`을 그대로 쥐고 있어서, 화면에 남는 증상은 「막대가 빈다」 하나뿐이다. 안내 문구를 「버블이 도로 뜨면」으로 적어 놨었는데 실측에 맞춰 고쳤다.

### 안 재고 쓴 문장이 하나 더 있었다

스토리에 「정착 전에 폭을 바꾸면 부유부터 다시 돕니다」라고 적었는데, 이것도 코드를 읽고 짐작한 것이었다. 재 보니 **흡수 한복판에서는 그렇게 안 된다.** 남은 활공을 접고 곧바로 최종 프레임에 앉는다. 즉시 재착석 분기가 보는 것이 무대 내부 단계가 아니라 바깥에서 넘겨주는 `settled` 값이라, 그 값이 켜진 뒤에는 활공이 남아 있어도 남은 몫을 접는다. 부유 중이면 계속 떠 있다가 새 폭으로 정상 흡수하는 것이 맞다.

**위에 「사흘에 세 번」이라고 적어 놓고 같은 날 네 번째를 했다.** 다만 이번엔 내가 쓴 문장을 재 보다가 잡았다.

### 실측 (Playwright CDP · 16건 전부 통과)

주미님이 「사파리도 봐봐 2지선다랑 다크도 모바일도」라고 하셔서 브라우저 2 × 테마 2 × 선택지 2 × 화면 2로 돌렸다.

| 화면 | 무대 폭 변화 | 5지선다 최소 구간 | 2지선다 최소 구간 |
|---|---|---|---|
| 모바일 393 → 360 → 393 | 317 → 284 → 317px | 16px | 107px |
| 데스크탑 1440 → 1280 → 1440 | 1364 → 1204 → 1364px | 72px | 456px |

크롬·사파리, 라이트·다크 어느 조합에서도 단계가 「정착」을 벗어나지 않았고 빈 구간이 남지 않았다. 다크는 URL globals가 실제로 걸렸는지 `html`의 `dark` 클래스로 매번 확인했다.

**처음엔 375/414/320으로 쟀는데 320은 규격 밖이었다.** DS 레이아웃 문서의 모바일 기준은 393이고 360까지만 나온다. 그 아래 수치로는 판단하지 않고 규격 폭으로 다시 쟀다.

**사파리는 처음에 아예 못 돌렸다.** 스크립트를 `/tmp`에 둬서 워크트리가 아니라 전역 플레이라이트(1.62.1)를 잡았고, 그쪽이 찾는 웹킷 빌드가 없었다. ESM은 스크립트가 놓인 자리를 기준으로 `node_modules`를 찾는다. `createRequire`로 워크트리 것(1.60.0)을 가리키게 고쳐서 해결했다.

안 잰 것: reduced motion · 파이어폭스.

## 이름만 남은 브랜치 셋을 지웠다

`docs/deep-stage-decisions` · `docs/deep-stage-visual-qa` · `fix/deep-stage-resize-changeset`. 원격에는 이미 없고 로컬에만 남아 있었다.

앞의 둘은 `origin/main..브랜치`에 커밋이 1개씩 잡혀서 언뜻 미머지로 보였다. **스쿼시 머지라 해시만 다르고 내용은 들어가 있었다.** 그 커밋이 추가한 줄을 한 줄씩 main과 대조해(각 6줄) 전부 있는 것을 확인하고 지웠다. 해시가 조상인지만 봤으면 「아직 안 머지됐다」로 잘못 읽을 뻔했다.

### 커밋 (socraAI_product design → riiid/prism)

| 커밋 | 설명 |
|------|------|
| `8a8d2225` | docs(plan-54): home/history 웹 컷 11장 토글도 768로 (#521 squash) |
| `83ec7826` | feat(storybook): 무대 폭을 실제로 흔드는 스토리 (#522 squash) |
| `1f3fde3f` | docs(plan-46): 「재현이 안 된다」를 닫았다 (#525 squash) |

## 마무리

PR #521을 머지했다(squash `8a8d2225`). 커밋 둘이 문서 한 줄을 고친 것이라 squash로 합쳤다. 작업용 워크트리와 원격 브랜치를 지웠고, 공유 작업 폴더의 plan-54 사본도 머지된 정본으로 다시 덮었다 — 첫 커밋 시점에 한 번 덮었는데 그 뒤 정정 커밋이 들어가 또 어긋나 있었다.

PR #522도 머지했다(squash `83ec7826`). 게이트는 전부 통과했다. Chromatic `UI Review`·`UI Tests`는 플랜 제한으로 보류지만(「Update your plan to resume testing」) 게시는 정상이라 화면은 볼 수 있다. 502개 스토리가 올라갔고 새 스토리도 `index.json`에서 확인했다. 로컬 스토리북 서버는 내렸다 — 옆 세션이 쓰는 6021 포트는 살려 두고 내 6006만 껐다.

PR #525로 plan-46의 낡은 문장도 닫았다(squash `1f3fde3f`). 9/5 절이 「#501 픽스는 회귀 테스트와 최초 발견 때의 실측으로만 담보돼 있다」로 끝나 있었는데, 이제 재현할 자리가 생겼으니 그 문장에 취소선과 해소 표시를 붙이고 실측을 담은 9/7 절을 더했다. **낡은 근거를 그대로 두면 다음 사람이 「재현이 안 된다」를 믿고 또 고정 폭 흐름 스토리에서 재려 든다.** plan-46은 열린 체크박스가 0이라 항목을 다시 열지 않고 새 절 머리에 `- [x]` 한 줄만 적었다. 문서만 바뀌어 CI는 `validate`만 돌고 나머지는 건너뛰었다.

**피그마는 오늘 손대지 않았다.** 숨은 형제 26건은 고칠 이유가 없다는 것이 결론이라 그대로 둔다.

---

## (다른 세션) 프로토타입에서 주미님이 눈으로 잡은 결함 여덟

`plan-59` 트랙이다. 위 plan-54 기록과는 별개 세션이고, 작업 폴더도 워크트리(`.claude/worktrees/plan60-rules`)로 갈라 뒀다.

### 게이트가 다 통과했는데 결함이 여덟 나왔다

빌드·타입체크·테스트 337개·문서 검증이 전부 통과한 상태에서 주미님이 프로토타입을 열어 보시고 여덟을 잡으셨다. **렌더 검증도 이미 돌렸는데 안 걸렸다** — 화면을 띄우는 것과 보는 것이 다른 일이라는 자리다.

앞의 다섯은 이렇다.

- **로그인 버튼이 모달 안에서 오른쪽으로 쏠렸다.** `LoginButton`이 `w-[320px]`로 폭을 자기가 쥐고 있었다. 320은 피그마 마스터의 기본 크기이고 **실제 배치는 언제나 FILL**이다(시트 361 · 모달 344). `w-full`로 풀고 폭은 감싸는 쪽이 준다.
- **로그인 버튼 사이가 8이었다.** 피그마 `auth-actions`가 높이 164에 48짜리 셋을 담아 y가 0·58·116이니 **10**이다.
- **메시지 사이가 16이었다.** 대화 컷 둘에서 버블이 167에서 끝나고 답변이 187에서 시작하니 **20**이다. 종전 값은 근거 없이 좁았다.
- **모바일 대화 헤더에 「+ 새 대화」가 없었다.** DS가 8/31에 `Right=new-chat`을 신설하고 기본값까지 바꿔 **모바일 153장이 전부 그 값인데 패키지에 값이 없었다.** 구조적 원인은 넘긴 곳이 비어 있던 것이다 — 계약이 「포팅은 plan-49 소관」이라고 넘겼는데 plan-49에는 그 항목이 없었고 「열린 항목 0」으로 종결돼 있었다.
- **스크롤 막대가 늘 떠 있었다.** 「포커스할 때만 뜨는 거 아니었나」로 잡으셨다. `prism-scroll`이 `:hover`도 조건에 두고 있었는데, **마우스가 대화 열 위에 놓여 있는 것은 읽는 내내 이어지는 상태**라 사실상 늘 켜져 있었다. `hover`를 걷고 `focus-within`만 남겼다. 부작용을 규칙과 코드 주석 양쪽에 적었다 — **휠로만 굴리면 막대가 안 보인다.** 휠은 포커스를 만들지 않는 입력이다.

### 나머지 셋은 끝까지 내리거나 오버레이를 띄워야 보이는 자리였다

- **웹 홈에서 약관 고지가 바닥에서 130px 떠 있었다.** 기획서 §4.1.4가 「홈 하단」으로 정하는데, 가운데 정렬된 열의 마지막 요소로 둬서 히어로를 따라 올라와 있었다. 행을 둘로 갈라 히어로는 가운데, 고지는 바닥에 뒀다.
- **채팅을 끝까지 내리면 마지막 메시지와 입력창 사이가 14px였다.** 스크롤 여백을 `바 높이 − 페이드 50`으로 잡아서 마지막 메시지가 페이드를 통과해 **그 아래 끝에서 멈췄다.** 페이드는 스크롤 중에 지나가는 콘텐츠를 가리는 장치이지 쉬는 자리의 여백이 아니다. 바 높이 전체로 바꿔 63px가 됐다.
- **약관 동의 모달이 `TermsRow`를 안 쓰고 손그림이었다.** 체크박스 20(정본 28) · 라벨 14 Medium(정본 17) · 행 높이 없음 · 전체 동의 행의 면과 radius 없음 · 약관 본문 chevron 없음. **`TermsRow`는 9/4에 포팅이 끝나 있었고** 계약과 `TermsSheet` 스토리가 조립 구조까지 적어 뒀는데 이 화면만 옛 손그림으로 남아 있었다.

### 고친 커밋이 plan 문서를 하나도 안 건드렸다

`75838e98`과 `5e33f3a9`가 코드와 계약만 고치고 plan을 안 건드린 것을 뒤늦게 봤다. AGENTS.md가 「작업 완료 또는 검증 직후 체크리스트를 갱신」을 요구하는 자리다. 여덟을 「주미님이 컷에서 잡은 것」 절로 묶어 각 줄에 피그마 근거와 고친 방법을 적었다.

**셋은 감사표에 「조립됨」으로 적혀 있던 자리다.** 화면이 있는지만 봤고 그 안의 값은 안 봤다. **「조립됨」은 그 화면이 있다는 뜻이지 정본과 같다는 뜻이 아니다.**

### 커밋 (socraAI_product design → riiid/prism)

| 커밋 | 설명 |
|------|------|
| `58e4350d` | fix(prism): 히트 영역 「피그마 선행」 넷을 다시 재니 셋이 코드로 풀렸다 |
| `c2c66d6b` | feat(prism, storybook): 언어 선택 화면을 붙이고 읽기 전용 행의 chevron을 걷었다 |
| `96014f09` | docs(plan-59): 「컴포넌트 없음」 일곱을 다시 재니 다섯이 다른 절로 갔다 |
| `75838e98` | fix(prism, storybook): 주미님이 컷에서 잡은 셋 — 버튼 폭·메시지 간격·새 대화 버튼 |
| `5e33f3a9` | fix(prism): 스크롤 막대에서 hover를 걷고 포커스에만 남겼다 |
| `087aec25` | chore(sync): 조립 단위 스냅샷 갱신 — 언어 선택 화면이 목록에 없었다 |
| `b5e04e0d` | docs(plan-59): 컷에서 잡힌 다섯을 기록했다 — 고친 커밋이 문서를 안 건드렸다 |
| `8ad665b8` | fix(storybook): 컷 세 장 — 약관 모달을 TermsRow로, 하단 텀, 웹 홈 고지 위치 |
| `d0826713` | docs(plan-59): 인수인계 절 — 여기서 멈췄고 다음 사람이 볼 것을 적었다 |

### 마무리 — 여기서 접고 넘긴다

주미님이 「너무 오래한 듯, 다른 에이전트에 맡기겠다」고 하셔서 남은 것을 처리하지 않고 기록만 남겼다. 인수인계는 `plan-59` 맨 아래 「인수인계 — 여기서 멈췄다」 절에 있다.

**PR #518이 `CONFLICTING`이다.** 마지막 push 뒤 `origin/main`이 열다섯 커밋 넘게 앞서 갔다. 충돌은 `plan-59` 한 곳뿐이라는 것까지 확인해 뒀다. 리베이스할 때 **양쪽 항목을 다 살려야 한다** — 같은 자리에 다른 날짜의 재측이 들어와 있어서 한쪽을 버리면 이미 닫힌 항목이 다시 열린다.

**머지는 주미님 판단으로 남겨 뒀다.** 시각 변경이 든 PR인데 Chromatic `UI Review`가 플랜 한도로 pending이라 자동 비교 자리가 없다.

**열어 둔 판단 둘.** 홈 헤더의 「+ 새 대화」는 홈만 제목이 꺼져 있어 넣으면 정렬이 깨진다 — 제목을 살릴지 슬롯 폭을 고정할지가 피그마에서 정해져야 닫힌다. 프로토타입의 「시나리오 도구」 디버그 패널이 웹 헤더 제목을 덮는 것도 고칠지 여쭀는데 답을 못 받았다.

**피그마는 이번에도 손대지 않았다.**


---

## (다른 세션) 머지 끝난 워크트리·브랜치 정리

위 「이름만 남은 브랜치 셋」과 같은 갈래인데 별개 세션이고, 대상도 다르다. 워크트리 5개와 브랜치 6개를 지웠다.

| 워크트리 | 브랜치 | 근거 |
|---|---|---|
| `stage-resize-story` | `docs/plan46-resize-story` | #525 squash |
| `feat-deep-agent-stage` | `worktree-feat-deep-agent-stage` | 전건 main이 나중에 덮음 |
| `overlay-restore-rule` | `worktree-overlay-restore-rule` | #348 머지 |
| `absorbhero-0904` | `docs/plan58-headline-audit` | #526 squash (`a96135ba`) |
| — | `feat/deep-stage-resize-story` | #522 squash |
| `plan58-close-item4` | `docs/plan58-close-headline-item` | #531 squash (`55f43527`) |

이름이 비슷한 `docs/deep-agent-stage-theme-review`·`feat/journey-deep-agent-stage`는 대상이 아니라 건드리지 않았다.

**여섯 다 「커밋이 더한 줄」을 main과 한 줄씩 대조하고 지웠다.** 스쿼시라 해시는 다르고 `origin/main..브랜치`에 커밋이 잡힌다. 30줄·189줄·2줄과 파일 2개는 누락 0이었다.

`feat-deep-agent-stage`만 안 맞는 줄이 26개 나왔는데 열어 보니 전부 main이 나중에 덮은 것이었다 — 열린 판단 둘은 `[x]`로 닫혔고(#511·#517), 릴리스 메모는 CHANGELOG에 흡수됐고, 무대 배경색은 다크가 `#242528`에서 `#303134`로 갱신돼 있었다.

`docs/plan58-headline-audit`도 11줄이 안 맞았는데 **최종본끼리 대조하니 main과 완전히 같았다**(503줄, 파일 1개). 리베이스 과정에서 같은 브랜치가 다시 쓴 중간 문장이라 유실이 아니다. **중간 커밋의 줄만 세면 리베이스한 브랜치는 늘 「누락 있음」으로 나온다 — 최종본 diff를 함께 본다.**

`plan58-close-item4`는 트랙 F가 plan-58 「작업」 4번(Headline 9곳, #5)을 닫는 후속 PR #531을 머지한 뒤 넘겨받은 것이다. 더한 줄 2개가 둘 다 `origin/main`의 37·38행에 그대로 있었고 최종본도 main과 같아서 지웠다. plan-58의 열린 체크박스는 2건에서 1건으로 줄었다(남은 하나는 override 건이라 별개다).

**「locked라 unlock부터 해야 한다」는 전제는 틀렸다.** `.git/worktrees/*/locked`가 둘 다 없었고, 저장소 전체에도 잠긴 워크트리가 0개였다. 잠금 사유로 적혀 있던 `claude session pid 55531 / 8176`은 `ps`로 보니 둘 다 죽어 있었다. `unlock`을 건너뛰고 바로 지웠다(잠긴 것으로 본 것은 실제로는 `plan59-close-two` 워크트리였다).

### plan-10 아카이브 분리 — 만들었다가 되돌렸다

공유 폴더에 `plan-10`을 쪼갠 미커밋 변경이 떠 있었다(본문 −4,445줄 + `archive-2026-09-03.md` 4,357줄). 내용이 새지 않았는지 세어 보니 분리 전 5,013줄(빈 줄 제외) = 본문 1,961 + 아카이브 3,071로 맞았고, 차이 19줄은 낡은 머리말이 빠지고 새 머리말과 「열린 항목 24건」 표가 들어온 것이었다. 주미님 승인을 받아 두 파일만 지정해 커밋했다(`a36a78ba`).

**그런데 main에 올리려고 대조했더니 올릴 것이 아니었다.** main은 이미 `archive-2026-08.md`(4,311줄)로 같은 분리를 해 놨다. 우리 두 파일 5,032줄 중 **main에 없는 줄은 55줄뿐**이었고, 그 55줄의 핵심인 「열린 항목 24건」 표조차 main에서는 **이미 0건**으로 다 닫혀 있었다(표의 29행 중 4행은 가리키는 절이 main에 없다). 브랜치가 221커밋 뒤처져 있어서 **그대로 밀었으면 main이 그동안 쌓은 1,262줄이 되돌아갔다.** 커밋을 되돌리고(`git reset HEAD~1`) 파일은 미커밋 상태로 돌려놨다. **살아남은 커밋이 없어 커밋 표는 없다.**

### 루트 작업 폴더 — 보고만, 손대지 않았다

브랜치 `docs/contract-usage-v109`(원격 gone)에 수정 16건이 떠 있다. 다른 세션 것이라 그대로 뒀고 `origin/main`과 나누기만 했다.

- **main과 내용이 같은 5개** — `ScrollReturn.stories.tsx` · `WebSidebar.tsx` · `chat-history-panel.md` · `thinking-capsule.md` · `plans/37`
- **다른 11개** — `.gitignore` · `AGENTS.md` · `WebShellFlow.stories.tsx` · `score-bar.md` · `thinking-toggle.md`(DS/계약 양쪽) · `layout.md` · `rules.md` · `plans/10` · `thinking-progress.md` · `token-contract.md`

「같다」가 「안 고쳤다」는 뜻은 아니다. 예를 들어 `ScrollReturn.stories.tsx`는 +150/−15가 얹혀 있는데 결과물이 main과 같아진다. 반대로 다른 11개의 큰 `−` 수치는 삭제가 아니라 **뒤처진 만큼 main 쪽 내용이 아직 없다는 뜻**이다.

### `/private/tmp` 워크트리 셋 — 잃을 것은 없었다

남은 워크트리 28개 중 셋이 `/private/tmp` 아래에 있다. 재부팅하면 사라지는 자리라 커밋이 떠 있으면 그대로 잃는다. **셋 다 미커밋 0건이고, 커밋도 전부 갈 곳에 가 있었다.**

| 워크트리 | 브랜치 | 상태 |
|---|---|---|
| `wt-plan59` | `docs/plan59-figma-track-remeasure` | 원격 살아 있음, 미푸시 0 |
| `wt-plan60` | `docs/plan60-tab-height-headroom` | 원격 살아 있음, 미푸시 0 |
| `prism-glass-fade` | `fix/glass-fade-opacity` | 원격 `[gone]`, PR #485 머지됨 |

앞의 둘은 원격 브랜치가 살아 있고 `@{u}..HEAD`가 0이라 볼 것이 없었다. **`prism-glass-fade`만 원격이 `[gone]`이라 따로 쟀다.** 커밋 `4d3458d3`이 소스 파일까지 5개를 건드리고 있어서 유실 후보로 보였는데, PR #485가 머지돼 있었고 `BottomBar.tsx`는 main과 완전히 같았다. main에 없는 changeset 파일 하나는 릴리스가 소비한 것이라, 그 문장이 `CHANGELOG.md` 150행에 `89dd362`로 들어가 있다. `bottom-bar.md`가 6줄 다른 것도 **main 쪽에만 있는 줄**이라 브랜치가 뒤처진 것이지 빠진 것이 아니다.

**그래서 셋 다 지금 사라져도 잃는 것이 없다.** 옮기거나 지우지는 않았다 — 읽기만 했다.

### 회고 및 인사이트

#### 설계 결정

**「이미 돼 있는데 낡은 채로 남아 있는 것」이 한 세션에 두 번 나왔다.** 지운 브랜치들이 그랬고, plan-10 분리도 그랬다. 원인은 같다 — 이 워킹트리를 여러 세션이 공유하고 브랜치가 200커밋 넘게 뒤처지기 때문에, **내 트리에서 「안 돼 있다」로 보이는 것이 main에서는 이미, 그것도 더 최신으로 끝나 있다.**

주미님이 이걸 보고 「메인이랑 먼저 대조」를 지시하셨고, 프로젝트 메모리 `feedback_compare_with_main_before_starting`에 남겼다. 판정은 눈대중이 아니라 **줄 단위 다중집합 대조로 「우리에만 있는 줄」을 센다** — 수천 줄 작업인데 고유 줄이 55개면 그건 중복이다.

#### AI 도구 활용

**측정 코드 자체도 두 번 틀렸다.** `grep -qxF "$line"`이 `-`로 시작하는 줄을 옵션으로 먹어 대조가 통째로 깨졌고(`--` 필요), f-string 안에 백슬래시를 넣어 파이썬이 죽었다. **「0건」이나 「전부 누락」이 나오면 결론 전에 판정 코드부터 의심한다.**
---

## 계획 탭이 이틀 비어 있던 건 결함이 아니라 주말이었다

어제 인수인계에 「9/5·9/6 로그에 `### 미해결 항목` 절이 없어 계획 탭이 비었다」를 주미님 판단 사안으로 올려 뒀다. 오늘 두 안을 만들어 뷰어에 실제로 먹여 보고 골랐다.

```
Jumi-Worklog의 9/5·9/6 로그에 「### 미해결 항목」 절이 없어 계획 탭이 비어 있다.
두 안을 비교해라: (가) 그날 실제로 열려 있던 항목을 커밋 시각 기준으로 복원해 절을
넣되 「9/7에 소급 작성」을 절 머리에 적는다, (나) 비워 두고 9/7 로그에 「이틀 비었다」만
남긴다. 각 안에서 뷰어 extractPlanFromLog가 어떻게 보이는지 로컬 렌더로 찍어라. 쓰지는 마라.
```

### 레포를 안 건드리고 두 안을 렌더했다

변형본을 `/tmp`에 두고 Playwright 라우트 가로채기로 뷰어가 `RAW_BASE`로 보내는 요청만 바꿔치기했다. 9/5·9/6 두 파일만 변형본으로 응답하고 나머지 날짜는 실제 레포 파일을 그대로 돌려줬다. 그래서 **레포 파일을 한 글자도 고치지 않고** 두 안을 나란히 찍을 수 있었다. 조건은 현재 main의 `site/worklog.html` · 로컬 정적 서버 · Chromium 1.60(레포 pnpm 버전으로 고정) · 1280×1000 · dpr 2다.

| | (나) 비워 둠 | (가) 소급 복원 |
| --- | --- | --- |
| `extractPlanFromLog('2026-09-05')` | `''` (0자) | 1,367자 |
| `extractPlanFromLog('2026-09-06')` | `''` (0자) | 1,251자 |
| `hasPlan` | `false` | `true` |
| 탭 바 | **안 그려짐** | 계획 / 기록 |
| 기본 탭 | (탭 없이 본문 바로) | `plan` |
| 계획 탭 체크박스 | 0 | 각 12개(열림 10) |
| 기록 탭에 미해결 절 | 없음 | 없음(`getMarkdown`이 걷어냄) |
| 캘린더 계획 점 | 1·2·3·4·7 | 1·2·3·4·**5·6**·7 |

### 「계획 탭이 빈다」가 아니라 「탭이 안 생긴다」였다

`renderEntry`는 `planHtml`이 비면 탭 마크업을 통째로 건너뛰고 본문만 그린다(`site/worklog.html:6546`). 그래서 9/5를 열면 태그 줄 다음에 기록 본문이 바로 나오고, 9/4·9/7과 페이지 구조 자체가 다르다. **뷰어만 보는 사람에게는 누락이 누락으로 안 보인다** — 원래 그런 날처럼 읽힌다. 어제 「계획 탭이 비어 있었다」고 적은 것은 증상을 한 칸 덜 본 서술이었다.

### 소급 표시는 뷰어가 알아서 맨 위에 띄운다 — 제목 뒤에 붙일 때만

`extractPlanFromLog`가 절 제목의 `#`을 `> `로 바꿔 계획 탭 첫 줄에 붙인다(`:6116`). 그래서 제목을 `### 미해결 항목 (9/7에 소급 작성)`으로 쓰면 인용 박스가 「미해결 항목 (9/7에 소급 작성) — 로그 원문에서 추출(정본 = logs/*.md)」이 되어, 본문을 읽기 전에 소급이라는 사실이 먼저 보인다. 표시를 제목 **앞**에 붙이면 두 군데가 같이 깨진다(아래 함정).

### 결정 — 주말이라 소급하지 않는다

주미님 「9/5-6은 주말이였어」. 9/5는 토요일, 9/6은 일요일이라 계획 절이 없는 것이 정상이다. **결함으로 잡고 복원안까지 만들었지만 애초에 잴 일이 아니었다.** 기록은 두 날 모두 들어 있다(9/5 네 꼭지 + 커밋 여덟, 9/6 세 꼭지) — 없는 것은 계획 절뿐이다.

어제 인수인계의 「주미님 판단 사안으로 남긴다」 한 줄을 「이대로 둔다」로 닫았다. 고친 뒤 절 추출을 다시 돌려 체크박스 9개·열림 7개로 이전과 같은 것을 확인했고(본문 3,077→3,035자), 라이브 raw를 `?v=`로 cache-bust해 반영까지 봤다.

메모리 `worklog-plan-log-model`도 고쳤다. 계획을 옛 `plan-YYYY-MM-DD` embed에서 읽는다고 적혀 있었는데, 2026-08-10부터는 로그 안의 절에서 뽑는다.

### 함정 모음

- **절 제목 주석은 뒤에만 붙는다** — `findPlanSection`의 정규식이 `/^#{1,4}\s*미해결 항목/`이라 `### (9/7 소급 작성) 미해결 항목`은 매칭에 실패한다. 계획 탭이 안 생기는 것으로 끝나지 않는다. `getMarkdown`도 같은 함수로 절을 걷어내기 때문에 **소급 목록이 기록 탭 맨 위에 통째로 남고 그날 실제 기록이 그 아래로 밀린다.** 렌더로 확인했다(`logHasPlanText: true`).
- **캘린더 계획 점은 그 날짜를 한 번 열어야 켜진다** — `ensureLog` 콜백에서 플래그를 올린다(`:6063`). 첫 화면에 점이 없다고 계획 절이 없는 것이 아니다. 위 표의 값은 이레를 모두 방문한 뒤 잰 것이다.
- **커밋 시각 기준 복원과 소급 표시는 같이 못 간다** — 9/6 커밋 시각으로는 「숨은 형제 26건」과 「AI 노무사 카탈로그 대조」가 열려 있는데 둘 다 9/7에 취소됐다. 그대로 두면 열린 항목이 부풀고, 미리 닫으면 그날 상태가 아니다. **소급 복원은 그날의 오판까지 복원한다.**
- **없는 절을 결함으로 잡기 전에 요일부터 본다** — 「어제 대비 빠졌다」는 신호가 실제로는 주말이었다. AGENTS.md의 「대기 사유부터 재고」와 같은 축이다.

### 커밋 (Jumi-Worklog)

| 커밋 | 설명 |
|------|------|
| `2418165` | docs(worklog): 9/5·9/6은 주말이라 미해결 항목 절을 소급하지 않는다 |

---

### 회고 및 인사이트 — 계획 탭 판정

#### AI 도구 활용

- **라우트 가로채기가 「쓰지 마라」와 「렌더로 찍어라」를 동시에 만족시킨다.** 뷰어가 원격 raw를 fetch하는 구조라, 응답만 바꿔 끼우면 레포 파일을 건드리지 않고도 두 안의 실제 화면을 얻는다. 파일을 고쳤다 되돌리는 방식이면 중간에 세션이 끊길 때 흔적이 남는다.
- **classic script의 top-level `const`는 `window`에 안 붙는다.** `page.evaluate`에서 `window.ENTRY_MAP`으로 부르면 `TypeError`가 난다. 접두를 떼고 bare identifier로 참조해야 한다. 함수 이름도 추측하지 말고 파일에서 확인할 것 — `setViewMode`가 아니라 `setView`였다.
- **`/tmp`에 둔 측정 스크립트는 전역 playwright를 잡는다.** `createRequire`로 레포의 1.60.0 경로를 절대 경로로 고정했다.

#### 설계 결정

- **비교안을 만들었다고 그중 하나를 골라야 하는 것은 아니다.** 두 안을 다 렌더해 놓고도 답은 「전제가 틀렸다」였다. 판단을 올리기 전에 요일 하나만 봤으면 렌더가 필요 없었다.
- **뷰어의 「없음」 표현이 약하다.** 계획 절이 없으면 빈 탭이 아니라 탭 바가 사라져서, 진짜 빠진 날과 원래 없는 날이 화면에서 구분되지 않는다. 지금은 주말이라 문제가 아니지만 평일에 빠지면 같은 방식으로 안 보인다. 고치지는 않았고 사실만 남긴다.

---

## (다른 세션) PR 없는 브랜치 셋을 판정했다 — 하나만 살아 있었다

`docs/plan58-headline-audit` · `worktree-feat-deep-agent-stage` · `worktree-overlay-restore-rule`. 셋 다 PR이 없어서 「무엇이 남았는지」부터 재야 했다.

**`worktree-overlay-restore-rule`(커밋 1개, 9/1)은 이미 머지돼 있었다.** 그 커밋이 더한 두 문장이 main의 `bottom-sheet.md` 81행과 `modal-shell.md` 30행에 글자 그대로 있다. PR #348(`3dda9342`, 9/3)로 들어갔다.

**`worktree-feat-deep-agent-stage`(커밋 7개, 9/4)도 옮길 줄이 0이었다.** main에서 못 찾은 18줄을 하나씩 열어 보니 전부 main이 더 나중에 덮은 것이었다 — plan-10 「`Deep/Stage`를 만들지」와 plan-46 「135ms를 80ms로 줄일지」는 9/5에 `[x]`로 닫혔고(#517 · #511), changeset은 CHANGELOG에 흡수됐고, `--deep-agent-stage-track`은 `#242528`에서 라이트 `#EAEDF2` · 다크 `#303134`로 갱신돼 있었다. **착수할 때 받은 전제도 틀렸다** — 포팅 본체는 #501이 아니라 **#480**(`26c1c2dd`)이고, 문서 결정 넷은 #511 · #515 · #516 · #517이 이어서 닫았다.

**살아 있는 것은 `docs/plan58-headline-audit` 하나였다(커밋 11개).** `origin/main` 위로 리베이스하니 세 커밋에서 충돌했다 — main이 PR #490으로 같은 문서의 `상태:` · `작업` · `주미님 확인` 절을 먼저 고쳐 뒀기 때문이다. 양쪽을 다 남기는 쪽으로 합치고 main 쪽 39줄이 안 빠졌는지 줄 단위로 검산한 뒤 PR #526을 열어 머지했다(squash `a96135ba`, 파일 1개 +390/−1). 워크트리·브랜치 삭제는 하지 않고 정리 세션에 넘겼다(위 「(다른 세션) 머지 끝난 워크트리·브랜치 정리」 절).

### 두 판정이 부딪히는 줄 알았는데 아니었다

리베이스가 09-04 판정과 09-05~07 재측을 한 목록에 합치면서, `작업` 절 4번(Headline 9곳)이 「어느 쪽이 정본이냐」로 남았다. 09-04는 「컴포넌트로 올리지 않는다」(주미님 「굳이인 건 안 해도 될 듯」)인데 그 뒤 실제로 크기 축을 넣고 손그림 9곳을 인스턴스로 회수했으니 반대로 보였다.

**다시 읽어 보니 둘이 같은 것을 묻고 있지 않았다.** 09-04는 **새로 만들 것이냐**에 대한 답이고, 09-05 재측이 찾은 것은 **`Sheet/Intro`가 이미 있다**는 사실이다. 그러면 §5-1 · §10 · §11에서 한 일은 신설이 아니라 있는 세트를 쓴 것이라 앞 결정을 뒤집지 않는다. 주미님 확인을 받아 PR #531로 4번을 닫았다(squash `55f43527`). **09-04 줄은 지우지 않았다** — 앞줄이 「왜 안 만들었나」, 뒷줄이 「그럼 무엇을 썼나」를 적고 있어 둘 다 쓸모가 있다. plan-58의 열린 항목은 2건에서 1건으로 줄었고, 남은 하나는 override 두 개를 걷는 방법이라 별건이다.

### 커밋 (socraAI_product design → riiid/prism)

| 커밋 | 설명 |
|------|------|
| `a96135ba` | docs(plan-58): 손그림 Headline 회수 기록 §5~§12 — 리베이스로 09-04 판정과 합쳤다 (#526 squash) |
| `55f43527` | docs(plan-58): #5 두 판정은 안 부딪힌다 — 4번을 닫았다 (#531 squash) |

### 회고 및 인사이트 — PR 없는 브랜치 판정

#### 설계 결정

- **모순처럼 보이는 두 판정은 「같은 것을 묻고 있나」부터 본다.** 4번은 결론이 반대라서 사람 몫으로 올렸는데, 실제로는 물음이 달라서 둘 다 맞았다. 어느 쪽을 지울지 고르기 전에 물음을 맞춰 보면 지울 일이 아닌 경우가 있다.
- **문서 충돌은 「합쳤다」로 끝내지 않고 합친 자리에 왜 겹쳤는지를 적었다.** 리베이스로 두 시점이 한 목록에 붙으면 다음 사람은 앞뒤 줄이 왜 반대인지 알 방법이 없다. 그 한 줄이 있었기 때문에 하루 안에 닫을 수 있었다.

#### AI 도구 활용

- **판정 근거를 넘길 때 대조 방법과 PR 번호를 같이 넘긴다.** 정리 세션에 「전부 main에 있다」만 보냈으면 그쪽이 처음부터 다시 재야 했다. 방법을 같이 넘겼더니 그쪽은 26줄로 세고도(내 계산은 18줄) 같은 결론에 금방 닿았다. **수가 다른 것은 문제가 아니다 — 무엇을 열어 봤는지가 겹치면 된다.**
- **공유 워킹트리에서는 아무것도 커밋하지 않았다.** 루트 폴더에 옆 세션의 미커밋 변경이 16개 떠 있어서, 커밋은 전부 격리 워크트리에서만 했고 커밋 직전에 `git branch --show-current`, PR 직전에 `git log --oneline origin/main..HEAD`를 다시 읽었다.

---

## 모바일 히스토리 드로어 폭을 「화면 폭 − 56」으로 확정했다 — 393 하나만 봐서 갈린 적이 없었다

「337 고정」이냐 「화면 폭 − 56」이냐를 정하려고 재료를 만들었는데, **두 규칙이 393에서 완전히 같은 값이라 여태 갈린 적이 없다**는 것이 먼저 나왔다. 두 규칙으로 렌더한 393 컷은 바이트까지 같았다(sha1 `745f6105…`). 값 표기(337)는 고정 쪽을, 8/12에 푸시를 고르며 적은 근거 문장(「슬리버 56에 홈의 왼쪽 끝이 남는다」)은 파생 쪽을 가리키고 있었다. 코드는 상수 `DRAWER_WIDTH = 337`이고 컨테이너도 393 고정이라 다른 폭을 아예 안 탄다.

**360·393·430 세 폭으로 갈라 찍으니 답이 하나로 모였다.** 360에서 337을 고정하면 슬리버가 23으로 줄어 홈 햄버거 아이콘이 24px 중 5px만 남는다 — 8/12에 오버레이를 기각한 바로 그 증상(조각으로 읽힌다)이고, 규칙 12(44×44)에도 미달한다. 430에서는 반대로 슬리버가 93이라 헤드라인이 문장 중간에서 끊겨 「읽다 만 문장」이 된다. 파생 쪽은 세 폭 모두 슬리버 56으로 같다. 게다가 `layout.md` 채팅 버블 절이 9/2에 이미 **「모바일 가로 치수를 숫자로 적는 것 자체가 틀렸다 — 화면 폭에서 빼서 얻는다」**를 원칙으로 세워 뒀다. 세 근거가 같은 방향이라 파생 쪽으로 확정했다.

반영은 `chat-history-panel.md`(폭 규칙 불릿 신설 + 문장 3곳) · `layout.md` 표 셀 · `HomePage.stories.tsx` 상수를 `SCREEN_WIDTH − SLIVER_WIDTH`로. **시각 변화는 0이다**(393에서 337 그대로 접힌다). 8/11·8/12 이력 서술은 그날의 기록이라 그대로 뒀다.

### 정할 자리가 비어 있어서 만들었다

규칙 51대로 폭·오버레이 규격의 정본은 DS `➡️ Guidelines` 보드인데, **여섯 보드에 드로어 규격이 0건이었다**(페이지 전수에서 「드로어」·「슬리버」 0건). `Overlay` 보드 B절 「대체 규칙 표」에 일곱 행이 있었지만 히스토리 드로어 행이 없었다. 승인을 받아 그 표에 `History Drawer — 대화 목록` 행을 신설했다(`6565:25`). 마지막 행을 복제해 스타일을 승계했고, 표·절·보드가 전부 세로 auto-layout HUG이라 아래가 통째로 밀렸다(표 400→460 · 보드 3600→3660 · C절 y 2848→2908, 겹침 0).

제품 파일에도 노트 한 장을 남겼다(`✅ home/history_1.09`, `12638:160281`). 컷 11장이 전부 393이라 값은 안 바뀌지만, **다른 폭 컷을 새로 만들 때 337을 그대로 옮기면 안 된다는 사실이 컷 자체에는 안 적혀 있었다.** 값은 이 한 곳에만 두고 문구 끝에서 보드 행을 가리킨다.

### 겹침을 섹션 자식으로만 재서 한 번 놓쳤다

노트를 폭 1053으로 놓고 「겹침 0」을 받았는데 렌더에 남의 글자가 들어왔다. 검사 범위를 `Chat History Panel` 섹션의 자식으로 잡아서, 섹션 **밖** 프레임 `web / home — 비로그인`(x=1537)이 후보에 아예 없었다. 실제로는 오른쪽 끝 1604와 67px 겹치고 있었다. 폭을 940으로 줄여 페이지 전수 기준으로 다시 쟀다. **범위를 좁게 잡으면 0이 나온다** — 규칙 44-5-2와 같은 축이고, 이번엔 렌더가 잡아 줬다.

### 커밋하지 않았다 — 옆 세션 변경 위에 얹혀 있다

레포 다섯 파일이 미커밋 상태다. 스토리 둘(`HomePage.stories.tsx` · `ChatHistoryPanel.stories.tsx`)은 전부 내 줄이지만, 문서 셋은 옆 세션의 미커밋 작업과 **같은 헝크 안에** 섞여 있다. `layout.md`에서 내가 고친 표 셀은 옆 세션이 아직 커밋 안 한 「사이드바를 펴면」 절 22줄 안에 있고, `chat-history-panel.md`도 9/2 「푸시는 모바일 한정」 줄과 한 헝크다. `plan-10`은 옆 세션이 4,442줄을 아카이브로 떼어 내는 중이라 헝크가 수십 개로 얽혀 base가 어긋난다. 지금 끊으면 남의 작업이 내 커밋 메시지 아래로 들어가고, 나중에 이력에서 그 판단을 찾을 수 없다(AGENTS.md가 8/27 사고로 적어 둔 그 실패). **문서 셋을 만진 세션이 자기 몫을 커밋한 뒤에 얹는 것이 맞다.**

### 「360을 지원 폭으로 보는가」를 물었다가 지적받았다

판단 재료를 내면서 이 질문을 주미님 몫으로 올렸는데 「360 지원이라니」로 닫혔다. 스토리북 뷰포트 프리셋에 `mobile360`(360×800)이 있고, `layout.md`가 버블 상한을 이미 「393에서 361 · 360에서 328」로 360에서 계산하고 있었다. **대표 폭이 393 하나라는 것과 360을 안 본다는 것은 다른 이야기인데 붙여 읽었다.** 묻기 전에 문서를 더 읽었으면 나오는 답이었다.

---

## (다른 세션) 열린 PR 셋을 다 닫았다 — 「깨끗하다」던 것 하나가 충돌이었다

#523 · #519 · #518을 넘겨받았다. 「#523은 문서 한 파일이니 본문만 읽고 머지, #519는 본문 한 줄만 재측, #518만 리베이스」로 받았는데 **#523도 충돌이었고 #519의 그 한 줄은 이미 낡아 있었다.**

**#519의 「제품 파일 수락 대기」는 더 이상 참이 아니었다.** 본문이 「게시는 갔는데 제품 파일은 아직 옛 값」으로 적혀 있었는데, 그사이 주미님이 수락을 누르셔서 이미 내려와 있었다. 제품 파일 `VHdwVCvG0YG7wucLtOWmOg`의 `✅ home/history_1.09` 한 장을 `skipInvisibleInstanceChildren = false`로 10,235노드 훑고 노드마다 `figma.variables.getVariableByIdAsync`로 이름을 풀어 셌다 — `surface/placeholder` 0→110, `selected/surface` 0→8, `engaged/surface` 0→17, `neutral/100` 199→71. 본문을 재측 결과로 고쳐 쓰고 머지했다.

**#518은 커밋 아홉을 `origin/main` 위로 옮기면서 `plan-59` 한 곳에서만 충돌했다.** 리베이스 전에 백업 태그 `backup/pr518-preRebase-20260907`을 걸었다. **중간 단계에서는 합치지 않고 브랜치 쪽만 취한 뒤, 마지막에 커밋 하나(`b4b63083`)로 main 쪽 내용을 되접어 넣었다.** 되접은 자리는 다섯이다 — 언어 선택 화면 행 · 마크다운 세 행(답변 마크다운 · 표 끝단 페이드 · 수식) · 읽기 전용 행의 ㉠㉡㉢ 분기 · 「피그마로 보낼 것」 넷 · #523이 넘긴 인계 항목 둘.

**#523이 넘긴 둘은 넘어온 자리에서 이미 끝나 있었다.** #518이 `c2c66d6b`로 먼저 했는데 그 커밋이 아직 `origin/main`에 없어서 #523 쪽 세션은 볼 방법이 없었다. 둘을 `[x]`로 닫고, #523이 새로 찾은 것 하나(언어 순서가 v1.0.8과 v1.0.9에서 다르다)만 열린 항목으로 살렸다.

열린 항목은 본문 서술 말고 체크박스로 셌다 — 15개(「피그마로 보낼 것」 넷 · PM 확인 여덟 · 첨부 에러 칩 하나 · 언어 순서 하나 · 육안 확인 하나). 커밋 전과 머지 뒤 `origin/main`에서 두 번 셌다.

검증은 CI와 같은 `pnpm check`로 돌렸다 — `lint` · `typecheck` · `test`(3파일 364개) · `build` · `pack:check`(378파일 712.8 KiB) · `storybook:build` · `demo:build` 일곱 게이트가 전부 exit 0이다. 받은 지시는 다섯이었는데 CI는 `lint`와 `demo:build`를 더 돈다. 깃헙 액션 쪽도 `check` · `validate` · `storybook` · `Storybook Publish`가 전부 통과했다.

**`gh pr merge`가 「이미 머지됐다」로 끝났을 때 다시 부르지 않았다.** state로 재니 `MERGED` · `2026-09-07T05:13:47Z` · `e24f9c5a`였다. 머지한 PR의 워크트리와 브랜치는 정리 세션 몫이라 손대지 않았다.

### 커밋 (socraAI_product design → riiid/prism)

| 커밋 | 설명 |
|------|------|
| `141052fa` | docs(plan-59): 언어 선택 화면은 피그마에 있다 · 읽기 전용 행은 세 갈래로 갈렸다 (인계) (#523 squash) |
| `42f0d1de` | 다크에서 안 보이던 자리표시 바를 드러냈다 — surface/placeholder 신설 + 제품 파일 수락 대기 확인 (#519 squash) |
| `e24f9c5a` | fix(prism, storybook): 화면에서 잡힌 결함들과 「피그마 선행」 재측 (#518 squash) |

### 남긴 것 셋 — 하나는 막고 있던 것이 이미 없어졌다

- **`plan-30`의 「주미님 확인 — Button 76개 `swapComponent`」는 근거가 사라졌다.** 살아 있는 네 페이지에서 `Button` 인스턴스 388개가 보는 프록시 세트를 세니 낡은 벌 `12521:37845`를 보는 것이 **0개**였다. #519 본문에 「그 브랜치를 옆 워크트리가 쥐고 있어 다음 턴에 닫는다」고 적었는데, **#519가 머지되면서 그 문서가 `origin/main`에 올라와 막힘이 없어졌다.** 새 워크트리에서 1308행과 `상태:` 줄을 고치면 닫힌다.
- **#518 시각 변경의 육안 확인**(로그인 모달 · 대화 하단 간격 · 약관 모달)은 주미님 몫이다. Chromatic `UI Review`가 플랜 한도로 계속 pending이라 자동 비교 자리가 없다.
- ~~**언어 선택 화면의 언어 순서를 v1.0.9 정본에서 다시 읽는 일**은 피그마 read가 필요해 남겼다. 근거는 `plan-29` 36행이다.~~ — **해소(9/7): 아래 「언어 순서를 재러 갔다가 화면 방식이 바뀐 것을 봤다」 절.** 순서만이 아니라 「적용하기」 방식 자체가 폐지돼 있었다

### 회고 및 인사이트 — 열린 PR 셋 처리

#### 설계 결정

- **스쿼시 머지에서 PR 본문은 커밋 메시지다.** #519 본문의 「제품 파일은 아직 옛 값」은 쓸 때는 맞았고 머지 시점에는 틀렸다. 그대로 머지했으면 틀린 문장이 main 이력에 영구히 박힌다. 머지 직전에 본문을 다시 읽는 일은 문서 손질이 아니라 커밋 메시지 교정이다.
- **여러 단계에 걸친 충돌은 단계마다 합치지 않는다.** 아홉 커밋에서 각각 판단하면 이미 닫힌 항목을 다시 열 기회가 아홉 번 생긴다. 한쪽만 취해 리베이스를 끝내고 마지막에 한 커밋으로 되접으면, 검산할 자리가 하나로 줄고 그 커밋의 diff가 그대로 「무엇을 되살렸나」의 목록이 된다.
- **열린 항목 수는 서술 말고 체크박스로 센다.** 문서가 「넷이 열려 있다」고 적어도 실제 `- [ ]` 수는 다를 수 있다. 커밋 전과 머지 뒤에 각각 셌다.

#### AI 도구 활용

- **넘겨받은 PR 상태를 그대로 믿지 않는다.** #523은 「문서 한 파일이라 깨끗하다」로 왔는데 `CONFLICTING`이었다. 넘길 때 잰 상태와 착수할 때 상태는 다르다.
- **Bash 출력 압축이 한글을 뭉갠다.** 충돌 자리와 스킬 문서는 글자 단위로 대조해야 하는데 압축된 출력으로는 비교가 안 됐다. 그 자리만 Read 도구로 바꿔 읽었다.
- **`gh pr merge`가 에러로 끝나도 머지는 됐을 수 있다.** 다시 부르지 않고 `state`부터 쟀다(메모리 `reference_gh_merge_delete_branch_worktree`).

---

## 에이전트 버블이 어느 선택지로 흡수되는지 정했다 — 대표 한 곳

```
주미님 에이전트버블 흡수는 해당 에이전트가 가장 많이 지지한쪽으로 흡수되면 되는 걸까요? 아니면 지지한 모든 선택지로 흡수되면 되는 걸까요? 후자일 경우 동일 에이전트버블이 여러개 생깁니다.
```

**답은 이미 레포 안에 반쯤 적혀 있었다.** `plan-16` §7에 주미님이 「지지도는 분산된다 — 한 모델·에이전트가 한 선택지에만 답하지 않는다(기획서 4.8 「노무 에이전트 → 대기업 90% · 스타트업 10%」)」를 적어 두셨고, 같은 문서 §9에서 「같은 AI가 여러 선택지 지지자 목록에 중복 등장하는 것」까지 수용해 둔 상태였다. 즉 **데이터 실체는 후자**다.

**그런데 무대는 지지 배분표가 아니라서 전자로 확정했다.** 후자로 가면 둘이 깨진다. ① 10명이 평균 2~3곳을 지지하면 버블이 25개가 되어, 같은 화면 아래 `Report Participants`의 「10명의 AI 전문가들이 답변에 참여했어요」와 어긋난다. ② 5지선다에서 대부분의 에이전트가 모든 선택지에 조금씩 걸리면 버블이 구간마다 고르게 깔려, 「의견이 어디로 기울었나」라는 이 무대의 유일한 메시지가 지워진다.

**코드는 이미 이 결정을 감당할 구조였다.** `DeepAgentStage.tsx`의 `layoutSegments`가 최종 구간 폭을 `segments[i].value`(가중 평균)로 정하고 버블 수와 분리해 둔다. 그래서 「버블 3개가 들어간 구간이 63%」여도 화면이 어긋나지 않는다. 계약의 `agents.option`도 처음부터 단수였다 — 다만 **그게 대표값이라는 말이 어디에도 없어서** 이번 질문이 나왔다.

### 남긴 자리 셋

- `packages/prism/component-contracts/deep-agent-stage.md` — `agents.option` 설명에 「대표 선택지 한 곳이지 지지 배분이 아니다」를 붙이고, 「대표 선택지 한 곳으로만 흡수한다 — 버블은 참여자를 세는 표현이 아니다」 절을 신설했다.
- `docs/plans/16-quick-deep-result-ui-2026-07.md` — 지지도 분산을 지적한 §7 바로 뒤에 확정 근거와 접은 안을 적었다. 결정이 살 자리는 그 지적 옆이다.
- 프로젝트 메모리 `project_deep_agent_highlight` — 맨 앞에 얹었다.

**무대에 「n명 · %」 같은 수를 세는 표기는 계속 두지 않는다.** 흡수된 버블 수가 실제 지지 분포와 다르기 때문이고, 이건 §7 ①번 결론 그대로다.

### 함정 모음

- **확정 사항이 계약에 없으면 같은 질문이 다시 온다** — 흡수 방식(B안)은 9/3에 정해져 계약에도 적혀 있었는데, 「무엇이 흡수되는가」는 `option` 단수라는 타입으로만 존재했다. 타입은 규칙을 적지 못한다. 결정문 한 줄이 없으면 읽는 사람은 데이터(분산)와 코드(단수) 사이에서 다시 물을 수밖에 없다.
- **작업 브랜치에 파일이 아예 없을 수 있다** — `docs/contract-usage-v109`는 원격이 이미 지워진(`[gone]`) 뒤처진 브랜치라 `deep-agent-stage.md`가 없었다. 뒤처진 브랜치에서 파일을 통째로 옮기지 않는다는 규칙대로, `origin/main` 기준 워크트리를 새로 따고 plan-16 수정분은 `git diff` → `git apply --3way`로 옮긴 뒤 본체는 `checkout --`으로 원복했다.

### 커밋 (socraAI_product design → riiid/prism)

| 커밋 | 설명 |
|------|------|
| `a1b7364e` | [#534](https://github.com/riiid/prism/pull/534) squash 머지 — 계약 절 신설 + plan-16 §7 근거 기록 |

원본 커밋 둘(`ab8ee63d`·`4ff0ef4a`)은 squash라 `origin/main`의 조상이 아니다. 내용 기준으로 다시 재서 계약의 「대표 선택지 한 곳」 2건과 plan-16 §7 소절이 main에 있는 것을 확인했다. `validate`는 11초에 통과했고 문서 변경이라 나머지 검사는 skip이다. 워크트리 `stage-option-semantics`와 브랜치를 지워 흔적 0을 확인했다(원격 브랜치는 머지 때 자동 삭제됐다).

### 회고 및 인사이트

#### AI 도구 활용

- **질문에 답하기 전에 레포를 먼저 뒤진 것이 맞았다.** 「분산이냐 단수냐」는 언뜻 새 판단으로 보였지만, 실제로는 주미님이 7월에 적어 둔 §7 문단이 절반을 이미 답하고 있었다. 새로 판단했다면 「분산이니 후자」로 갔을 것이고, 그건 참여자 수 표기와 정면으로 부딪혔다.
- **남은 일을 다른 에이전트에 넘길지 판단할 때는 「조사 비용 대 편집량」으로 잰다.** 이번 잔여 작업은 계약 문단 하나였는데, 그걸 쓰려면 plan-16 §7·`layoutSegments`·`agents.option`을 다시 다 읽어야 했다. 편집량보다 재조사 비용이 커서 넘기지 않았다.

#### 설계 결정

- **버블은 참여자를 세는 표현이 아니다.** `option` 단수는 연출상의 대표값이고, 정확한 배분은 리포트 안 선택지별 지지자 목록이 맡는다. 90/10에서 잃는 10%는 그 목록이 회수한다.
- **연출의 개수와 데이터의 폭을 분리해 둔 것이 이 결정을 가능하게 했다.** 흡수가 「닿을 때마다 한 칸」이어도 최종 폭은 데이터가 정한다. 둘을 묶어 뒀다면 대표값 흡수가 곧 막대 왜곡이 됐을 것이다.

---

## (다른 세션) `heading/web-2·3·4`를 지우고 정본 문서 여덟 곳을 따라 고쳤다

계획 절 「게시 뒤 이어서 할 것」의 첫 항목이다. 9/4에 이관해 두고 게시를 기다리던 것을 오늘 닫았다. **게시는 오늘 10:36에 있었다.**

### 잔여 6곳은 성격이 둘이었다

네 페이지를 다시 훑어 나온 잔여는 `✅ Login/Setting_1.09` 6곳뿐이었다(`✅ chat/report_1.09` TEXT 7,698개 중 0 · `✅ home/history_1.09` 2,007개 중 0 · `✅ Login/Setting_1.09` 2,297개 중 6 · `_디테일 수정중` 316개 중 0). **범위에서 뺀 것은 `❌` 세 페이지 · `thumbnail` · `💻 Links / Reference` · 구분자 2장이다.**

여섯이 게시로 안 풀린 이유가 서로 달랐다.

- **다섯은 `Login Modal` 제목의 `textStyleId` override였다.** 인스턴스가 마스터를 안 따라오는 정상 동작이라 게시를 몇 번 해도 안 풀린다. 24 Medium 그대로였으니 `heading/0`으로 다시 걸었다.
- **하나는 낡은 프록시 세대였다.** 같은 페이지의 다른 아바타 13개는 이미 `heading/2`인데 하나만 옛 스타일이었다. 규칙 43-2대로 재니 `Settings/ProfileCard`가 파일 안에 두 벌 살고 있었다 — 낡은 벌 `12352:8318`(인스턴스 1건)과 최신 `12539:9496`(11건). key도 variant 개수도 대표 크기도 같았다. **낡은 층은 처음에 지목한 `Avatar`가 아니라 그것을 감싼 `ProfileCard`였다.** ProfileCard를 `swapComponent`로 바꾸니 Avatar가 따라왔다.

### 렌더로 재고 지웠다

바꾸기 전후로 여섯 자리를 찍어 대조했다 — 모달 다섯이 0/188,784픽셀, 시트 하나가 0/98,643픽셀이다. **대조군으로 한 픽셀을 빨강으로 바꿔 넣어 판정 코드가 차이를 잡는지 먼저 확인했다.**

지우기 전에 셋의 값을 롤백용으로 적었다(전부 Pretendard · 행간 160% · 자간 0). `web-2` 24 Medium `e07f9765…` · `web-3` 20 Medium `607c061e…` · `web-4` 18 SemiBold `bd568171…`. DS 텍스트 스타일이 58종에서 55종으로, heading이 10종에서 7종으로 줄었다. Foundation 정리표(`70:369`)에서도 3행을 지웠다(50행→47행, 컨테이너 높이 2642→2490).

### 정본 문서가 지운 스타일을 살아 있는 것처럼 적고 있었다

레포를 훑어 정본 여덟 곳을 고쳤다 — `typography-style-spec.md` · `typography.md` · `report-option.md`와 `score-bar.md`(피그마 근거·패키지 계약 각 한 벌) · `token-contract.md` · `ModelProfile.tsx` 주석이다. 계약에 적힌 스타일 이름은 문서만 보고 바꾸지 않고 **피그마 마스터를 열어 확인했다** — `Report/Option`(`4572:2`) 라벨이 20 Medium `heading/2`, `ScoreBar` 라벨이 18 SemiBold `heading/3`, `Attachment/Logo/External Initials`가 20 Medium `heading/2`였다.

`docs/plans/` 아래 옛 판단 기록 일곱(20 · 36 · 41 · 44 · 51 등)은 그때의 기록이라 손대지 않았다.

### 굵기 열이 결정 전 상태로 남아 있었다

스펙 표를 고치다 보니 heading 일곱 줄이 전부 SemiBold로 적혀 있었다. 실측은 `heading/0`(24) · `1`(22) · `2`(20) · `web-0`(26) · `web-1`(28)이 Medium이고 `3`(18) · `4`(16)만 SemiBold다. **8/25에 「20px 이상만 Medium」을 결정하고 스타일까지 고쳤는데 이 표만 안 따라온 것**이라, 같은 레포의 `token-contract.md`와 두 정본이 서로 다른 말을 하고 있었다.

heading만 고치면 다른 표에 같은 오기가 남을 수 있어서 **표 51행 전체를 실측 55종과 네 항목(크기 · 행간 · 굵기 · 자간)으로 대조했다.** 어긋난 것은 이 굵기 5행뿐이었다.

### 표에 4종이 아예 빠져 있다

같은 대조에서 나왔다. DS에 있는데 문서 표에 없는 것이 `body/sm-m-tight` · `body/xs-r-tight` · `caption/2-tight` · `tag/chip` 넷이다. 값은 실측에 있지만(차례로 14/1.4/Medium · 13/1.4/Regular · 12/1.4/Regular · 13/AUTO/Medium) **용도 칸은 값만으로 못 적는다.** 스펙 문서 §미결에 발견 사항으로 남겼다.

「등록 총 50개」도 어느 수치와 맞지 않았다. 삭제 전 58종이고 지금 55종인데 50은 정리표 행 수였다. 47로만 고치면 틀린 수를 다른 틀린 수로 바꾸는 것이라, 실측 55를 적고 표·정리표와의 차이를 함께 적었다.

### 남은 것

- ~~**DS 라이브러리 재게시(주미님 몫).**~~ **필요 없었다.** 주미님이 「게시할 게 없다는데」라고 하셔서 다시 쟀다. 제품 파일에서 지운 세 key는 `importStyleByKeyAsync`가 「not found」로 실패하는데, **대조군인 `heading/web-0`·`web-1`·`heading/2`는 임포트된다.** API가 죽은 게 아니라 삭제가 이미 게시본까지 내려간 것이다. 전 7페이지에서 세 스타일을 쓰는 노드도 0곳이다.

  **내가 틀린 지점은 대조군을 안 세운 것이다.** 삭제 직후 임포트가 한 번 성공한 것을 보고 「게시본 스냅샷이 옛것이라 재게시가 남았다」로 적었다. 그때 남긴 둘을 같이 재 봤으면 성공/실패가 갈리는지 아닌지로 바로 판정이 섰다. 규칙 43-1이 말하는 「상태값 말고 소비 파일로 잰다」의 변형인데, **임포트 성공 여부도 그 자체로는 상태값**이라 대조군이 있어야 뜻이 생긴다.
- ~~**Figma 정리표(`70:369`)가 아직 47행이다.**~~ **같은 날 닫았다.** 주미님 승인을 받아 빠진 8행을 채우고 굵기 5행을 고쳐 55행으로 맞췄다 — 아래 「정리표를 55행으로 맞췄다」 절에 적었다.

### 표에 빠져 있던 4종을 채웠다 — 「0곳」이 판정 코드 탓이었다

주미님이 ㉮(소비처를 훑어 근거까지 적기)로 정하셔서 이어서 쟀다. DS·제품 두 파일 전 페이지(각 7페이지 · `skipInvisibleInstanceChildren = false`)를 훑었다.

| 스타일 | 값 | 소비처(제품 파일) |
|----|----|----|
| `tag/chip` | 13 / AUTO / Medium | 300곳 — 모드 칩 「Deep」(`Bottom Bar` 180 · `Chat Input Bar` 73) |
| `body/sm-m-tight` | 14 / 1.4 / Medium | 189곳 — 첨부 카드 파일명 |
| `caption/2-tight` | 12 / 1.4 / Regular | 185곳 — 같은 카드의 파일 크기 |
| `body/xs-r-tight` | 13 / 1.4 / Regular | 79곳 — 답변 하단 면책 문구(`Answer/Footer`) |

**처음엔 네 종 다 「0곳」으로 나왔다.** 원격 텍스트 스타일 id가 `S:<key>,<프록시노드id>` 형태인데 끝 쉼표만 지우고 key와 대조했기 때문이다. 실제로는 753곳이었다. 규칙 44-5-2(0은 검산할 대상이 없다)가 그대로 걸린 자리다.

앞의 세 종은 description에 「행간 140% 촘촘 변형(칩·푸터류) — 2026-08-07 주미님 확정」이 이미 적혀 있었다. 표에만 없었던 것이다.

### 커밋 (socraAI_product design → riiid/prism) — [PR #540](https://github.com/riiid/prism/pull/540)

워크트리 `.claude/worktrees/heading-web-cleanup` · 브랜치 `docs/heading-web-style-removal`. `origin/main`에 리베이스했고 충돌은 없었다.

| 커밋 | 설명 |
|------|------|
| `7e14e671` | docs(plan-10): heading/web-2·3·4 삭제를 닫았다 — 남은 6곳은 override 다섯과 낡은 프록시 하나였다 |
| `bc175e1c` | docs(typography, score-bar, report-option): 지운 스타일을 정본에서 걷어내고 남은 짝을 실측값으로 적었다 |
| `217f3fd5` | docs(typography-style-spec): 헤딩 굵기 열이 8/25 결정 전 상태로 남아 있어 실측으로 정정했다 |
| `4feeda71` | docs(typography-style-spec): 표에서 빠져 있던 4종을 실측해 채웠다 |

리베이스로 SHA가 갈렸다(`3d655a6d`·`d7941e55`·`694065b3`·`4feeda71`).

게이트는 `validate-design-system.sh` · `check-unfounded-claims.sh` · `pnpm typecheck` · `pnpm test`(364건)를 전부 통과했다.

### 정리표를 55행으로 맞췄다 — 「왜 중요하냐」가 규칙 하나를 끌어냈다

주미님이 「이게 왜 중요한거야?」라고 물으셔서 무게를 갈라 봤다. **지금 깨진 화면은 0이다** — 화면은 텍스트 스타일을 직접 물고 있어서 정리표가 낡아도 렌더가 안 틀린다. 급한 일이 아니라고 먼저 말씀드리고, 그 안에서 굵기 5행 > 빠진 8행 > 되풀이를 막는 규칙 순으로 무게를 적었다. 주미님은 「다」 하라고 하셨다.

**굵기 5행(600 → 500).** `heading/web-0`·`web-1`·`heading/0`·`1`·`2`. 8/25에 「20px 이상은 Medium」으로 정하고 스타일은 바꿨는데 정리표 표기만 안 따라왔다. `heading/3`·`4`는 실제로도 SemiBold라 뒀다. **빠진 행보다 틀린 값이 나쁘다** — 없는 정보는 되묻게 되지만 틀린 정보는 그냥 믿는다. 정리표를 보고 개발에 「600」이라고 넘기면 화면과 어긋난 스펙이 나간다.

**빠진 8행.** `display/1-m`~`4-m`(8/25 신설) 넷과 tight 3종 + `tag/chip`(8/07 확정) 넷. 같은 계열 행을 복제해 이름·견본·spec 세 텍스트만 갈아 끼웠다 — 새로 만들면 라벨 색 변수 바인딩과 spec 서식(130×31 고정, 행간 15)을 다시 세워야 한다. 컨테이너 2490 → 2973, 보드 `70:366` 2963 → 3446.

**검산.** 55행 전수로 행 이름 ↔ 견본 `textStyleId` ↔ spec 표기를 대조해 불일치 0. `button/*`의 `1.0` 대 `1`과 `display/4-*`의 `-1.2%` 대 `-1.2000000476837158%`는 표기 차이라 오탐으로 뺐다. 이름 중복 0, 페이지 안 겹침 0. 렌더는 전체 한 장에 더해 **용도 문구가 가장 긴 두 행을 원본 크기로** 찍어 spec 칸 잘림이 없는지 봤다 — 칸이 고정 폭이라 설정값으로는 못 잰다.

### 왜 두 번 연속 빠졌나 — 규칙 64로 적었다

**정리표는 낡아도 아무도 안 깨진다.** 그래서 낡았다는 신호가 어디에도 안 뜬다. 반대로 **삭제는 티가 난다** — 견본 행이 스타일 없는 텍스트로 남아 눈에 걸린다. 오늘 아침 `heading/web-2·3·4`를 지웠을 때 정리표가 곧바로 따라온 것도 그래서다. 삭제만 반영되고 추가·값 변경이 두 번 연속 빠진 이유가 이 비대칭이다.

`design-system/rules.md` 64번으로 적고 AGENTS.md 색인에 올렸다. 규칙 본문만 있으면 찾을 단어를 이미 알 때만 걸린다 — 이번 건도 「정리표가 낡았다」로 문제를 잡을 이유가 없는 자리였다.

**번호를 잘못 잴 뻔했다.** 메인 워킹트리에서 재니 58까지라 59번으로 봤는데, `origin/main` 기준으로는 63까지였다. 메인 워킹트리 브랜치가 뒤처져 있었던 것이다. `git show origin/main:<경로>`로 다시 재서 64로 잡았다.

### 커밋 (socraAI_product design → riiid/prism) — [PR #547](https://github.com/riiid/prism/pull/547)

| SHA | 메시지 |
| --- | --- |
| `20b528dd` | docs(rules-64, typography-spec): 정리표를 55행으로 맞추고 왜 안 따라오는지를 규칙으로 박았다 |
| `be9b4231` | docs(AGENTS): 규칙 64를 색인에 올렸다 — 표에 없으면 안 읽힌다 |

작업 중에 **PR #540이 머지된 것을 알았다.** 워크트리가 `origin/main`보다 5커밋 뒤처져 있었고 그중 `1a079a42`가 #540이었다. 그래서 기존 브랜치를 잇지 않고 `origin/main`에서 `docs/typography-table-sync`를 새로 땄다.

### PR #547을 머지했다 — 이 트랙은 닫혔다

머지 직전에 재니 `origin/main`이 **또 3커밋 앞서 있었다**(#529 · #544 · #546). 오늘 이 워크트리에서 세 번째다. 겹치는 파일이 없어서 리베이스는 깨끗했고 SHA만 갈렸다(`20b528dd`→`be1c1f0e` · `be9b4231`→`b23d3dbd`). 리베이스 뒤에 **규칙 번호 64가 여전히 유일한지**와 게이트 둘을 다시 돌렸다 — 번호는 리베이스로 겹칠 수 있는 값이라 한 번 더 잰다.

CI `validate` 통과, `MERGEABLE`·`CLEAN` 확인 후 스쿼시 머지했다 — `78fe3429`.

**머지 검산은 PR 상태값이 아니라 `origin/main` 본문으로 했다.** `rules.md`의 `## 64.` 1건, `AGENTS.md` 색인의 `**64**` 1건, 스펙 문서의 「해소(2026-09-07)」 1건. 원격 브랜치는 GitHub이 스쿼시 머지 때 자동으로 지웠고(그래서 `push --delete`가 「remote ref does not exist」로 실패한다 — 사고가 아니다), 로컬 브랜치 둘도 지우고 워크트리는 detached로 풀었다.

**이 트랙에 남은 것은 없다.** 피그마 정리표 55행 = 스펙 문서 55행 = 실제 스타일 55개.

### 회고 및 인사이트 — heading/web 삭제

#### 설계 결정

- **「A 때문에 대기」 항목은 A가 풀린 뒤에도 아무도 안 닫는다.** 이 항목은 9/4부터 「게시 뒤」로 열려 있었고 게시는 오늘 아침에 있었다. 계획 절이 `(미재측)`이라고 적어 둔 것이 실제로 값을 했다.
- **잔여를 세는 것과 왜 남았는지 아는 것은 다른 일이다.** 여섯이 한 덩어리로 보였지만 다섯은 override고 하나는 프록시 세대라 고치는 방법이 완전히 달랐다. 수만 세고 일괄로 갔으면 하나가 안 풀렸다.
- **오기를 하나 고칠 때 같은 표 전체를 재는 값이 있다.** 굵기 5행은 지운 스타일과 무관한데 같은 표에 있었다. 51행을 다 대조하는 비용이 작았고, 덕분에 다른 표에는 없다는 것까지 말할 수 있다.

#### AI 도구 활용

- **픽셀 대조는 판정 코드부터 시험한다.** 한 픽셀을 일부러 바꿔 넣었더니 카운트는 1을 잡았는데 `getbbox()`는 `None`을 돌려줬다. 알파 차이가 0이면 그렇게 된다. 검산 없이 「차이 0」을 믿었으면 지표가 죽은 것을 몰랐다.
- **중첩 인스턴스는 층마다 세대가 다르다.** 낡은 것이 안쪽 `Avatar`인 줄 알았는데 감싼 `ProfileCard`였다. 안쪽부터 고치려 들면 안 바뀐다.

## 색 램프 다크 값은 이미 피그마에 다 있었다 — 코드가 4칸만 가져왔다

먼저 `plan-52` §7의 「다크 일괄 점검」 모음 아홉 줄을 전부 재측했다. **여섯은 열린 일이 아니었다.** 진한 글자 넷, `surface/sunken` 순검정, 시각 QA, 내 정보 화면, `disabled` 라이트값, 그룹 B가 그랬다. 살아 있는 넷만 `plan-30` §4-12로 옮기고 원 위치에 이관 표기를 남겼다(PR [#532](https://github.com/riiid/prism/pull/532)). `plan-52`의 열린 체크박스는 0이 됐다.

**그다음 A행(색 램프 14계단 다크 값)을 착수하려다 전제가 틀린 것을 발견했다.** 피그마 write 승인을 받으려고 read-only 감사를 먼저 돌렸는데, DS 파일 `color` 컬렉션은 애초에 `Light`/`Dark` **2모드**였고 변수 181개 중 **108개가 모드별로 갈려 있었다.** 색 램프 8종 **80칸은 하나도 빠짐없이 다크 값을 갖고 있다.**

다크 값은 **램프를 위아래로 뒤집은 것**이다. `lime`이 가장 깨끗하다 — `/50`의 다크는 `/900`의 라이트, `/100`의 다크는 `/700`의 라이트, `/400`은 자기 자신이다. `/800`만 대칭 짝이 없어 따로 채운 값이다.

**그래서 이 일은 피그마 write가 아니라 코드 포팅이다.** `primitive.tokens.json`은 80칸 중 `blue/500`·`orange/500`·`purple/400`·`lime/200` **4칸만** mode-aware로 가져왔고, 나머지 76칸은 문자열 단일 값이라 다크에서 라이트 색이 그대로 나온다. 필요해진 칸만 하나씩 가져온 흔적이다.

### 포팅하면 어떻게 되나 — 활성 화면이 실제로 쓰는 조합

| 조합 | 곳 | 라이트 | 다크 | 판정 |
|----|----|----|----|----|
| `lime/100` + `/900` | 89 | 9.26 | 4.90 | AA |
| `blue/100` + `/800` | 53 | 6.31 | 4.74 | AA |
| `blue/50` + `/700` | 43 | 4.94 | 8.85 | AA |
| `orange/100` + `/800` | 30 | 5.24 | **3.83** | **미달 0.67** |
| `purple/100` + `/700` | 23 | 6.55 | 6.81 | AA |
| `green/50` + `/800` | 8 | 5.49 | 6.92 | AA |
| `green/50` + `/700` | 8 | **3.52** | 6.60 | **라이트가 미달** |

면 분리는 전부 넉넉하다(다크 `surface/base` 기준 ΔE2000 18~46, 계약이 탈락시킨 선은 3.46).

`orange`는 **글자를 순백으로 해도 4.49**라 fg 단계를 올려서는 못 넘긴다. 배경을 `/50`으로 내려야 8.37이 되는데 그건 피그마 마스터의 변수 바인딩을 바꾸는 write다.

덤으로 `green/50` + `/700` 8곳이 **지금 라이트 화면에서 이미 3.52로 AA 미달**인 것이 나왔다. 다크와 무관한 별건이다.

### 함정 모음

- **컴포넌트 인스턴스 수로 토큰 영향 범위를 세면 안 된다.** 제품 파일 `Badge/Label` 인스턴스 584개가 전부 `❌` 폐기 페이지에 있고 활성 `✅` 페이지는 0개였다. 거기서 멈췄으면 「정본 화면은 색 뱃지를 안 쓴다」로 결론 낼 뻔했다. 같은 범위를 `boundVariables`로 다시 재니 **활성 페이지가 램프를 558곳에서 물고 있었다**(`✅ chat/report_1.09` 552 · `✅ Login/Setting_1.09` 6). 뱃지가 아닌 다른 요소들이 같은 색 쌍을 직접 바인딩해 쓴다. 규칙 44-5-2(「0개」는 검산할 대상이 없다)가 정확히 걸린 자리다.
- **계약에 적힌 「값이 없다」를 그대로 믿으면 안 된다.** `badge-label.md` 「알려진 제약」이 "색 톤 7종의 램프는 다크 모드 값이 없어"라고 적었고 `plan-30` §4-12 A행과 `plan-17` §1-1의 defer 결정이 전부 그 전제 위에 서 있었다. **피그마를 먼저 쟀기 때문에 잡혔다.** 문서부터 읽고 착수했으면 없는 값을 새로 만드는 write 승인을 받으러 갔을 것이다.
- **MCP 응답은 조용히 잘린다.** 80칸 덤프를 한 번에 요청했더니 16줄만 돌아왔는데 개수 필드(`rampCount: 80`)는 멀쩡했다. 개수만 보고 넘어갔으면 64칸을 못 본 채 진행했다. 색을 넷씩 나눠 두 번 부르니 전부 나왔다.

### 검산

`plan-21` §138이 예측치로 적어 둔 「lime 다크 4.90」이 피그마 실측값으로 계산했을 때 정확히 4.90으로 재현됐다. 계산 경로(sRGB 상대 휘도 + ΔE2000)가 맞다는 증거다.

### PR (socraAI_product design → riiid/prism)

| PR | 상태 | 내용 |
|----|------|------|
| [#532](https://github.com/riiid/prism/pull/532) | OPEN | 다크 모음을 `plan-30` §4-12 한 곳으로 통합, 죽은 여섯 행을 원 위치와 함께 닫음 (5파일, 문서만) |

### 남은 일을 누가 이어받나 — 새 세션이다

- **재조사 비용이 낮다.** 값 80칸은 피그마 호출 두 번이면 다시 뽑히고, 뒤집기 규칙과 대비 수치는 이 절과 프로젝트 메모리 두 건에 남겼다. 어제 기준(「조사 비용 대 편집량으로 잰다」)으로 재면 이번엔 **편집량과 QA 쪽이 훨씬 크다** — 토큰 JSON 76칸 + `token:build` + 라이트/다크 시각 QA다.
- **성격도 다르다.** 이 세션이 쥔 것은 감사 수치와 문서 정리 맥락인데, 남은 일은 토큰 빌드와 시각 QA다. 이어 붙이면 안 쓸 맥락을 그대로 끌고 간다.
- **`orange` 30곳은 누구도 지금 못 한다.** 배경을 `/50`으로 내릴지가 주미님 결정이고, 결정 뒤에는 피그마 write + 계약 + 코드가 한 턴에 묶인다(규칙 58).
- **커밋할 일이 있으니 새 세션은 시작 때 `git worktree`로 끊는다.**

### 회고 및 인사이트

#### AI 도구 활용

- **승인 게이트가 오히려 오판을 막았다.** 피그마 write라서 read-only 감사를 먼저 하게 돼 있었고, 그 감사에서 「값이 없다」는 전제가 깨졌다. 게이트가 없었으면 이미 있는 값 위에 새 값을 덮어썼을 것이다.
- **「0개」가 나왔을 때 멈추지 않은 것이 결론을 바꿨다.** 판정 경로를 컴포넌트 이름에서 변수 바인딩으로 갈아 다시 재니 0이 558이 됐다. 경로를 바꾸지 않은 재확인은 검산이 아니다.

#### 설계 결정

- **다크 램프를 「뒤집기」로 정의해 두면 새 칸을 만들 필요가 없다.** 라이트에서 `/100` 배경 + `/800` 글자로 쓰던 쌍이 다크에서 자동으로 어두운 배경 + 밝은 글자가 된다. 대비가 라이트와 정확히 같지는 않은데, 램프 간격이 균등하지 않아서 색마다 4.74~6.81로 갈린다.
- **그 갈림이 `orange` 하나를 통과선 아래로 떨어뜨렸다.** 뒤집기가 대비를 보존한다는 보장은 없으므로, 램프를 새로 뒤집을 때는 색마다 다시 재야 한다. 지금까지 잰 것은 `lime` 하나뿐이었다(`plan-21` §138).

---

## `fg-subtle` 라이트 대비를 `neutral/450`으로 풀었다 — 「어긋난 자리 넷」 중 셋이 오판이었다

`plan-60` §6-6에 A·B·C 세 안으로 열려 있던 결정을 A로 정하고 그날 안에 코드까지 내렸다. 옛 값 `neutral/400`이 라이트 `surface`(#FFFFFF) 위 3.59:1, `surface-muted`(#F2F4F8) 위 3.26:1이라 본문 기준 4.5:1을 못 넘겼다.

**B는 실행하면 A와 결과가 같아진다.** 라이트에서 4.5:1을 넘기는 가장 밝은 램프 칸이 `neutral/500`인데 그게 곧 `fg-muted` 값이라, B(「있는 값 중에서 고르기」)로 풀면 화면이 A와 구분되지 않는다. 그래서 램프에 칸을 하나 끼웠다.

`neutral/450` = 라이트 `#6B6E76` · 다크 `#939599`. 400과 500 사이는 L\* 간격이 20.1로 이 램프에서 가장 넓고(300→400은 10.3, 500→600은 15.3), 값은 그 구간의 L\* 중점이다.

| 모드 | 배경 | 옛 400 | 새 450 |
|---|---|---|---|
| 라이트 | `surface` `#FFFFFF` | 3.59 | **5.10** |
| 라이트 | `surface-muted` `#F2F4F8` | 3.26 | **4.63** |
| 다크 | `surface-raised` `#27282B` | 4.15 | **4.91** |

미달이던 네 줄이 다 닫혔다. 제일 빠듯한 것이 라이트 `surface-muted`의 4.63이다.

피그마는 `semantic/text-tertiary` 하나만 옮겼다. `neutral/400`을 alias하던 변수가 셋이었는데(`text-tertiary` · `icon-muted` · `disabled/fg`), 뒤 둘은 3:1 기준이라 400이 최저 3.26으로 통과하므로 그대로 뒀다.

### 보고한 「어긋난 자리 넷」 중 셋이 틀렸다

값을 옮긴 뒤 「`fg-subtle`을 빌려 쓰던 아이콘·placeholder·비활성 **네 자리**가 마스터보다 진해졌다」고 적고 계약·plan 세 곳에 그대로 내렸다. 그런데 `chat-input-bar.md`를 고치려고 열었더니 「2026-07-06: Figma는 placeholder 색을 semantic `text-tertiary`에 바인딩」이라는 줄이 있었다. 그 줄이 걸려서 마스터를 열었고, **셋이 오판이었다.**

- **`ChatInputBar` placeholder는 안 갈렸다.** 마스터가 `semantic/text-tertiary`를 물고 있어 피그마도 함께 450으로 갔다. 7월에 primitive 직바인딩을 semantic으로 바꿔 둔 것이 이번에 값을 자동으로 따라오게 했다.
- **`Thinking/Toggle`은 같은 컴포넌트 안에서 갈렸다.** `Label`「사고 과정」은 semantic을 거쳐 450으로 따라 내려왔는데, chevron `Vector`만 `neutral/400`을 **직접** 물어 400에 남았다. 한 컴포넌트가 통째로 따라오거나 통째로 안 따라오는 게 아니다.
- **`text-link.disabled.fg`는 원인이 다르다.** 마스터 `State=disabled`가 `neutral/200` 직바인딩이라, 코드가 400이던 시절에도 이미 두 단계 어긋나 있었다. 450 이동이 만든 문제가 아니고 간격만 한 단계 더 벌어졌다. 계약에 「이름이 다를 뿐 값은 같다」고 적혀 있던 것도 사실이 아니어서 고쳤다.

그래서 이번 변경으로 새로 갈린 곳은 **아이콘 두 자리**다: `SheetHeader`의 `chevron-left`·`close`, `thinking-toggle.header.icon`. 코드에 피그마 `icon-muted`에 대응하는 토큰이 없어 아이콘이 `fg-subtle`을 빌려 쓰는 구조 때문이다.

### 함정 모음

- **컴포넌트 단위로 「갈렸다/안 갈렸다」를 판정하면 안 된다.** `Thinking/Toggle`이 그 반례다. 라벨은 semantic을 거치고 chevron은 primitive를 직접 물어서 같은 헤더 안에서 한쪽만 움직였다. 토큰 값을 옮길 때 영향 범위는 **노드의 바인딩 대상**으로 세야 하고, 컴포넌트 이름으로 세면 절반이 틀린다.
- **`theme.css`는 생성물이다.** 처음에 `sed`로 4줄을 고쳤다가 파일 머리의 "Generated by src/tokens/build.ts. Do not edit directly."를 봤다. `checkout --`으로 되돌리고 소스 JSON을 고쳐 재생성했다. 그랬더니 `text.subtle`을 참조하던 컴포넌트 토큰 7개가 함께 따라 내려왔다 — 손편집이었으면 그 7개가 옛 값에 남았다.
- **JSON을 파이썬으로 재직렬화하면 무관한 포맷까지 정규화된다.** `primitive.tokens.json`을 `json.load`/`json.dump`로 돌렸더니 원본의 들쭉날쭉한 들여쓰기가 정리되면서 diff가 6줄에서 32줄로 부풀었다(blue/500·orange/500·purple/400 등). 되돌리고 앵커 문자열 기반 텍스트 삽입으로 다시 했다.
- **오판을 계약에 먼저 내리면 정정할 자리가 늘어난다.** placeholder 오분류 하나가 `token-contract.md`·`plan-60` 두 곳·`chat-input-bar.md`까지 네 자리에 퍼졌다. 마스터를 먼저 열었으면 한 자리도 안 썼을 일이다.

### PR (socraAI_product design → riiid/prism)

| PR | 상태 | 내용 |
|----|------|------|
| [#537](https://github.com/riiid/prism/pull/537) | MERGED | 토큰 소스·생성물 + 계약·근거 문서 (커밋 2개, 25파일) |
| [#542](https://github.com/riiid/prism/pull/542) | MERGED | `color.icon.muted` 신설 — 아이콘 두 자리를 `fg-subtle`에서 뗐다 |
| [#546](https://github.com/riiid/prism/pull/546) | MERGED | `TextLink` 비활성을 마스터 표준(`semantic/disabled/fg`)으로 |

커밋 SHA는 적지 않는다 — `origin/main`에 아직 없다.

### 남긴 것

- **피그마 게시는 주미님 몫이다.** 플러그인 API에 게시 기능이 없다. 변수는 게시하면 수락 없이 자동 전파된다.
- `plan-60`에 승인 대기 둘을 **갈라서** 올렸다. 아이콘 두 자리를 `fg-subtle`에서 뗄지, 그리고 `text-link.disabled.fg`를 어느 쪽으로 맞출지. **원인이 다르니 한 항목으로 묶지 않았다.** — **둘 다 같은 날 승인받아 닫았다.** 아래 「승인 사안 둘을 닫았다」.

### 뒤이어 — CI가 아이콘 갈림을 잡았고, 마스터를 전수로 재니 `TextLink`가 소수파였다

PR을 열고 나서 `storybook` 잡이 빨간불이 됐다. `pnpm parity --strict`가 `sheet-header`의 「닫기 아이콘 색(icon-muted)」을 피그마 `rgb(132,135,143)` 대 코드 `rgb(107,110,118)`로 잡았다. **위에 「승인 사안」으로 올려 둔 바로 그 자리를 게이트가 독립적으로 짚은 것이다.**

값만 따로 되돌릴 수는 없다. 코드에 `icon-muted` 대응 토큰이 없어 아이콘이 `fg-subtle`을 빌려 쓰는 구조이고, 어느 쪽을 정본으로 삼을지가 승인 사안이기 때문이다. 스크립트에 이 상황을 위한 칸이 이미 있었다 — 정의 파일의 `known.code`에 지금 코드 값을 적으면 「판단 대기」로 분류돼 strict를 안 깨뜨리고, 값이 제3의 것으로 또 움직이면 그때는 다시 빨간불이 된다. **방치가 아니라 고정이다.** 스크립트 주석이 그 의도를 적어 두고 있었는데(2026-09-03 `ModalDialog`가 그 경우였다고 한다) 정의 파일에서 쓰인 것은 이번이 처음이다.

승인 자료로 마스터를 전수로 쟀다(`✅ Components` 7,051노드). `disabled` variant 안 TEXT 47건이 물고 있는 변수는 이렇다.

| 변수 | 건수 | 어디 |
| --- | --- | --- |
| `semantic/disabled/fg` | 10 | `Button` · `Button/DeepCTA` · `History Item` · `Menu Item` · `BottomSheet/ActionRow` · `BottomSheet/MenuItem` |
| `neutral/300` | 5 | `Button` · `Text Field` · `Mode Toggle` · `Chat Input Bar` |
| `semantic/text-tertiary` | 4 | `Text Field` · `Chat Input Bar` |
| `neutral/200` | 3 | **`TextLink`「재전송」** · `Mode Toggle`·`Chat Input Bar`의 「Deep」 |
| `neutral/500` | 1 | `History Item` |

**`TextLink`가 마스터 안에서도 표준에서 벗어나 있다.** 200을 쓰는 나머지 둘은 같은 라벨이 300에도 있어 variant마다 갈린 경우라, 단독으로 200을 쓰는 것은 `TextLink`뿐이다. 대비도 라이트 1.46~1.61, 다크 1.34~1.51로 `disabled/fg`(3.26~4.69)와 계통이 다르다. 비활성 텍스트는 WCAG 대비 면제라 위반은 아니지만, **「코드를 마스터에 맞춘다」가 곧 정답은 아니라는 뜻이다.** 이 실측을 `plan-60` §6-10과 열린 항목 두 줄에 내렸다.

아이콘 쪽 판단 자료도 같이 냈다. `neutral/400`은 네 배경 조합에서 3.59·3.26·4.69·4.15로 아이콘 기준 3:1을 다 넘는다. **아이콘용 토큰을 새로 파서 400으로 되돌려도 대비는 걸리지 않는다.**

### 승인 사안 둘을 닫았다 — 하나는 코드에 토큰을 만들고, 하나는 마스터를 옮겼다

주미님이 두 권고안을 다 승인해서 같은 날 반영했다. **고치는 방향이 서로 반대였다.**

**아이콘 쪽은 코드에 토큰을 만들었다**([#542](https://github.com/riiid/prism/pull/542)). 피그마 `semantic/icon-muted`에 대응하는 코드 토큰이 없어 아이콘이 `fg-subtle`을 빌려 쓰던 구조였다. `semantic.tokens.json`에 `color.icon.muted`(`neutral/400`)를 넣고 `--color-icon-muted`를 내보낸 뒤, `SheetHeader`의 `chevron-left`·`close`와 `thinking-toggle.header.icon`을 그쪽으로 옮겼다. **값이 아니라 기준이 다르다는 것이 갈라 낸 이유다** — 본문은 4.5:1, 아이콘은 3:1이라 같은 칸을 오래 공유할 수 없었고 이번에 실제로 갈렸다. 공개 토큰은 추가만 5줄이라 브레이킹이 아니다.

**`TextLink`는 반대로 마스터를 옮겼다**([#546](https://github.com/riiid/prism/pull/546)). 「코드를 마스터에 맞춘다」가 기본값처럼 보였는데, 마스터를 전수로 재니 `TextLink`가 마스터 안에서도 소수파였다(47건 중 `neutral/200`은 3건이고 그중 단독으로 쓰는 것은 `TextLink`뿐). 그래서 `5583:2700`의 fill을 `semantic/disabled/fg`로 바꾸고 코드 `text-link.disabled.fg`를 거기에 맞췄다. 인스턴스는 DS·제품 통틀어 0개라 화면 영향이 없다. **근거 문서가 `semantic/text-tertiary`로 적고 있었는데 마스터는 한 번도 그 값을 문 적이 없었다** — 실측 없이 적힌 값이 남아 있던 것이라 같이 고쳤다.


둘 다 머지했다(`b2eb98a4` · `56f8c0b7`). **같은 plan 문서에 각자 절을 더한 두 브랜치라 리베이스에서 충돌이 났는데, 양쪽을 다 살리는 것이 답이었다** — 체크리스트는 아이콘 줄과 `text-link` 줄을 나란히, 본문은 §6-11 다음에 §6-12를 이어 붙였다. 겹친 것은 내 브랜치가 갖고 있던 아이콘 체크박스 한 줄뿐이라 그것만 버렸다. `theme.css`는 생성물인데 **자동 병합이 정확했다** — 믿지 않고 재빌드했더니 변경이 0이었고, 라이트·다크 양쪽에 `icon-muted`와 `text-link-disabled-fg`가 다 살아 있었다.

**`plan-60`은 이걸로 열린 체크박스가 0이 됐다.** 남은 것은 주미님 판단 절 둘이다 — 「폼 컨트롤 경계를 3:1로 올릴지」(§231)와 「`SettingsRow`의 `value`가 행보다 길 때 무엇을 줄이나」(§322, A·B·C 안 제시됨). 둘 다 교환 관계를 정하는 문제라 실측으로는 못 닫는다.

**CI 실패를 「고칠 것」이 아니라 「대기로 고정할 것」으로 분류했다.** #537에서 `pnpm parity --strict`가 아이콘 갈림을 잡았는데, 그건 승인 사안이라 값을 되돌릴 수 없었다. 스크립트가 그 용도로 만들어 둔 `known.code` 칸에 지금 코드 값을 적어 「판단 대기」로 넘겼다. 방치와 다른 점은 **값이 제3의 것으로 또 움직이면 다시 빨간불이 된다**는 것이다. 결정이 난 뒤 #542에서 그 칸을 걷었다.

### 회고 및 인사이트

#### AI 도구 활용

- **계약 문서의 옛 기록이 오판을 잡았다.** placeholder를 고치려고 파일을 연 것이지 검산하려던 게 아니었는데, 7월에 적어 둔 「semantic `text-tertiary`에 바인딩」 한 줄이 눈에 걸려서 마스터를 다시 열었다. **기록을 남길 때 「무엇을 어디에 물렸는지」까지 적어 두면 나중에 이런 식으로 걸린다.**
- **잔여 작업을 다른 에이전트에 넘길지는 조사 비용으로 갈랐다.** 남은 것은 커밋과 PR뿐이었지만, 25개 파일 중 상당수가 피그마 실측 결과에 근거를 두고 있어서 넘기면 그 판단을 처음부터 다시 세워야 했다. 편집량은 작고 재조사 비용이 커서 안 넘겼다 — 9/7 앞 항목과 같은 기준이다.

#### 설계 결정

- **접근성 기준이 다르면 토큰도 갈라야 한다.** 텍스트는 4.5:1, 아이콘·UI 요소는 3:1이다. `text-tertiary`와 `icon-muted`가 값이 같다는 이유로 한 칸을 공유하고 있었는데, 기준이 다르니 언젠가 갈릴 수밖에 없었다. 이번에 실제로 갈렸고, 코드 쪽에는 아직 `icon-muted`에 대응하는 토큰이 없어서 아이콘이 텍스트 토큰을 빌려 쓰는 상태가 남았다.
- **`fg-muted`로 바꾸는 것은 답이 아니다.** 피그마와 코드의 회색 이름이 한 칸 밀려 있다. 코드 `fg-muted`는 `neutral/500`이라 피그마 `text-secondary`·`icon-default` 쪽이다. 이름만 보고 맞추면 아이콘이 훨씬 진해진다 — 계약 세 곳에 이 경고를 같이 적었다.

## 이미지 에러 칩이 사유를 툴팁으로 알린다 「PM 판단 대기」로 묶여 있던 하나가 실은 코드가 정본을 안 따라간 자리였다

`plan-59`(전체 시나리오 프로토타입 감사)에서 사람 판단이 필요 없는 항목만 닫는 작업을 이어받았다. 「첨부 에러 칩 사유 문구」는 종전에 **「코드 결함이 아니라 PM·디자인 결정 사항」**으로 분류돼 「정본 확인이 필요한 것」에 올라가 있었는데, 제품 파일 `✅` 세 페이지를 전수로 재 보니 그 분류가 반쪽만 맞았다.

- 범위는 `✅ chat/report_1.09`(TEXT 7,698 · INSTANCE 10,897) · `✅ home/history_1.09`(2,007 · 2,299) · `✅ Login/Setting_1.09`(2,297 · 3,084)이고 `skipInvisibleInstanceChildren=false`로 숨은 층까지 훑었다. 에러 문구는 `✅ chat/report_1.09` 한 페이지에만 있다.
- **칩 종류에 따라 표면이 갈린다.** 파일 칩은 사유가 칩 안 텍스트라서 코드가 이미 정본대로였다. **이미지 칩은 56×56이라 자리가 없어 정본이 「사유는 툴팁으로 전달한다」고 정해 뒀는데, 코드는 `errorMessage`를 `sr-only`로만 넣고 툴팁을 그리지 않았다.** 눈으로 보는 사용자에게는 왜 실패했는지가 전달되지 않던 상태다.
- 정본은 `보드3 · 첨부 칩 — 인터랙션`의 `④ 에러 사유 툴팁 — 판정 시 1회 자동 노출`(`7708:60662`)과 오류 동작 설명 `7487:13994`이다. 웹도 같다 — `web / chat — DnD 5. 용량 초과` 컷에 같은 툴팁이 있다.

용량·페이지 제한 값이 기획서와 화면 정의서에서 다른 것은 **그대로 PM 확인 항목으로 남겼다.** 그쪽이 정본이면 문구 셋이 통째로 바뀌는 사안이라 판단 대상이 맞다. 「툴팁을 그리는가」와는 다른 축이었다.

### 포팅한 동작

`AttachmentChip`이 이미지 실패 상태에서 `Tooltip`을 직접 쥔다.

- 판정 순간 1회 자동 노출하고 3초 뒤 스스로 닫힌다. `error`↔`error-retry` 전환도 판정이 새로 난 것이라 다시 연다.
- **재노출 경로가 두 실패 상태로 갈린다.** `error`(⚠·복구 불가)는 hover(웹)와 칩 탭(모바일)으로 다시 열고, `error-retry`(↻·업로드 실패)는 칩 탭이 재시도라서 hover만 쓴다.
- 입력 영역이 잘라내지 않도록 `portal`로 띄운다. 소비처가 할 일은 없다.
- 스토리 `Explorations/첨부 드래그앤드롭 > 에러사유툴팁`의 손그림 툴팁(인라인 말풍선 · 자체 타이머 · `@keyframes`)을 걷어내고 컴포넌트가 내는 동작을 그대로 보게 했다.

### 「약 2.5s」를 3초로 옮겨 적었다

정본 주석은 「약 2.5s」인데 코드는 3000ms다. 처음에는 정본만 보고 2500으로 코드·계약·테스트를 다 짰다가, 스토리 주석에서 3000의 근거를 읽고 되돌렸다. `toast.md`와 `plan-22`(2026-08-09 확정)가 **오류처럼 읽어야 하는 알림의 하한을 3초**로 정하고 있어서, 2.5초로 두면 우리 기준을 우리가 깬다. 6초를 안 쓰는 이유는 칩에 앵커된 툴팁이라 컴포저를 그만큼 가리고 hover·탭으로 언제든 다시 열리기 때문이다.

**피그마 주석을 3s로 맞추는 것은 시각 변경 승인 사안이라 손대지 않았다.** 이번 작업의 피그마 write는 0건이고, 이 어긋남은 `plan-59`에 적어 뒀다.

### 브라우저로 여덟 경로를 쟀다

jsdom은 CSS를 안 그려서 게이트 통과만으로는 hover와 타이밍에 대해 아무것도 보장하지 못한다. 정적 스토리북을 띄워 실측했고 전부 정본대로였다.

| 경로 | 결과 |
|------|------|
| 첨부 전 | 툴팁 없음 |
| 실패 판정 순간 | 「5MB 초과」·「업로드 실패」 둘 다 `data-open="true"` · opacity 1 |
| 3초 뒤 | 둘 다 닫힘 |
| `error` hover / 이탈 | 열림 / 닫힘 |
| `error-retry` hover | 열림 |
| `error-retry` 탭 | 안 열림(재시도가 먹는다) |
| `error` 탭 | 열림 |

게이트는 `pnpm typecheck` · `pnpm test`(367개, 3개 추가) · `pnpm build` · `pnpm pack:check` · `pnpm storybook:build` · `pnpm demo:build` · 문서 검증 스크립트가 모두 통과했다.

### PR (socraAI_product design → riiid/prism)

| PR | 설명 |
|------|------|
| [#536](https://github.com/riiid/prism/pull/536) | feat(attachment-chip): 이미지 에러 칩이 사유를 툴팁으로 알린다 — MERGED (`077e7fc8`) |

CI는 `validate`·`check`·`storybook` 모두 통과했다. Chromatic `UI Review`·`UI Tests`는 「Update your plan to resume」로 멈춰 있어 판정에 쓰지 않았다.

### 남긴 것

- **전체 파일명 hover 툴팁(정본 `7708:60662` ①, 500ms 유지)은 아직 코드에 없다.** 사유 툴팁과 별개 기능이라 이번 범위 밖으로 두고 계약 `attachment-chip.md`에 사실만 적었다. 체크박스는 열지 않았다.
- 피그마 주석의 「약 2.5s」와 코드 3초의 차이. 정본을 고치려면 승인이 필요하다.
- `plan-59`의 열린 항목은 15에서 14로 줄었다. 나머지는 「피그마로 보낼 것」 넷 + PM 확인 여덟 + 언어 순서 정본 대조 하나 + 스토리북 게시본 육안 확인 하나다.

### 회고 및 인사이트

#### 분류가 일을 가린다

**「PM 판단 대기」로 묶인 항목 안에 코드 결함이 섞여 있었다.** 문구 값(어느 제한이 정본인가)과 표면(툴팁을 그리는가)이 한 줄에 들어가 있었고, 앞쪽이 판단 사안이라 뒤쪽까지 같이 대기로 넘어갔다. **전수로 재 보고 나서야 갈렸다.** 열린 항목을 착수 전에 다시 재라는 규칙이 이 경우에도 그대로 들어맞았다.

#### 정본만 보고 맞추면 우리 기준을 깬다

「약 2.5s」에 그대로 맞췄다가 되돌렸다. 정본이 1차 기준인 것은 맞지만, 그 값이 우리가 이미 정해 둔 기준(`toast.md`의 3초 하한)과 부딪히면 **어느 쪽을 따를지가 판단 사안이다.** 값을 옮겨 적기 전에 같은 성격의 값이 레포에 이미 있는지 먼저 봐야 했다. 근거는 스토리 주석 세 줄에 있었다.

#### 게이트 통과는 호버에 대해 아무것도 말하지 않는다

테스트 367개가 통과해도 jsdom은 CSS를 안 그리므로 `:hover`와 전환 타이밍은 검사되지 않는다. 브라우저로 여덟 경로를 재는 데 든 시간이 게이트를 다 돌리는 것보다 짧았다.

#### 공유 워킹트리 격리가 값을 했다

이 작업은 세션 시작 때 `git worktree`로 갈라 뒀다. 옆 세션이 같은 `plan-59` 문서를 계속 고쳐서 리베이스 때 상태 줄 하나가 충돌했는데, **격리돼 있어서 내 파일 일곱 개만 끊어 PR을 열 수 있었다.** 충돌한 것은 「열린 항목이 몇 개인가」를 적은 줄 하나뿐이라 양쪽 판정을 합쳐 14로 다시 적었다.

---

## 인수인계로 넘긴 넷을 다 쟀다 — 둘은 이미 끝나 있었고 하나는 애초에 잴 항목이 아니었다

위 「인수인계」 절이 새 세션에 넘긴 넷을 그 절이 적어 둔 순서대로 쟀다. 결과는 재측 셋과 판정 하나다. **닫힌 것이 셋이고 남은 열린 항목은 훅 하나인데, 그 하나도 「사흘 뒤에 본다」는 조건이 성립하지 않아 조건 자체를 다시 적었다.**

인수인계 절이 「비율로 보면 절반 가까이가 이미 끝나 있을 수 있다」고 적어 둔 것이 그대로 맞았다. 넷 중 둘이 이미 끝나 있었다.

### 훅 발화 기록은 사흘치가 아니라 한 줄이었다

`#418` 훅은 9/3에 머지됐지만 **발화 로그(`~/.claude/hook-logs/zero-result.log`)는 오늘 05:07Z에 처음 쓰였고 한 줄뿐이다.** 훅이 머지된 날부터 로그가 쌓였을 것이라고 가정한 것이 틀렸다. 「사흘치를 본다」는 절차는 실행할 대상이 없었다.

그래서 되재기 스크립트(`scripts/audit-zero-result-hook.mjs`)로 세션 JSONL을 훅과 같은 판정으로 다시 훑었다. 4,997건 중 v1 판정 후보가 120건, 2차 좁힘(v2) 후보가 26건이다. 04:43Z 이후 v2 발화 13건은 대부분 **훅을 시험하던 세션이 스스로 만든 측정 인공물**이고, 실사용에서 반복되는 헛걸림 후보는 `미커밋: 0건`(워크트리 상태 확인) 세 건뿐이었다.

**넓힐지 판단하는 조건을 「사흘」이 아니라 「로그가 며칠 쌓인 뒤」로 고쳤다.** 날짜로 세면 또 없는 기록을 찾는다.

### 다이얼로그와 게시는 이미 끝나 있었다

`11183:13664` 안은 `Settings/Dialog Shell` 구조의 인스턴스(`11870:8277` · 660×540)로 바뀌어 있다. 다른 세션 몫이었고 그 세션이 이미 했다.

게시는 오늘 10:36에 있었다. 이것으로 `heading/web-*` 삭제는 이미 해소돼 있었고(관련 PR #540 열림), 남은 `Sheet Body`는 아래처럼 게시와 무관한 항목이었다.

### `Sheet Body` 고아는 지울 수 있는 항목이 아니었다

계획 절에는 「고아 마스터 4개 … 지워도 되면 지운다」로 적혀 있었는데 **셋이 다 틀렸다.**

- **넷이 아니라 둘이다.** `plan-32` 표의 `9278:185807`·`9278:185871`은 마스터가 아니라 그 안의 TEXT 노드였고, 부모를 거슬러야 실제 마스터 `9278:185810`·`9278:185874`가 나온다. `10288:7476`(재로그인 요구)은 조회하면 `null`이라 이미 없고, `9252:35159`(피드백 사유)는 고아가 아닌 일반 마스터다.
- **그 둘은 오늘 이미 처리됐다.** 주미님 승인으로 `❌ Login/Setting_1.08`의 참조 인스턴스 둘을 `detachInstance()`로 떼어내 참조가 0이 됐고, 렌더 대조에서 화면 두 장은 토스트 문구까지 같았다.
- **삭제라는 선택지가 없다.** 고아 마스터는 `remove()`가 예외도 안 던지고 조용히 무시하며, 참조를 0으로 만들어도 부모를 붙여 줘도 안 지워지고, 페이지를 통째로 지워도 남는다(고아는 애초에 페이지에 속해 있지 않다). 실측은 `plan-32`와 `plan-20`에 있다.

**즉 이 줄은 「주미님 승인 대기」로 두면 안 되는 항목이었다.** 승인을 받아도 닫히지 않기 때문이다. `plan-20`의 `ScoreBar`(`2711:113`)가 같은 이유로 먼저 종료됐고, 이 건도 같은 유형이다. `plan-20`·`plan-32`·`plan-41` 세 문서 모두 관련 체크박스는 이미 닫혀 있어서 레포에서 고칠 것은 없었다 — **열려 있던 자리는 워크로그 한 줄뿐이었다.**

**교훈은 「대기 사유를 적을 때 무엇이 어떻게 되면 풀리는지 같이 적는다」의 변주다.** 이 줄은 풀리는 조건을 「게시」로 적었는데, 실제로 막고 있던 것은 게시가 아니라 **API가 그 동작을 지원하지 않는다는 사실**이었다. 조건을 잘못 적으면 조건이 충족돼도 항목이 안 닫힌다.

---

## 재측에서 나온 PR 둘을 머지했다 — Chromatic이 필수가 아닌 것을 확인했다

| PR | 내용 | 결과 |
| --- | --- | --- |
| [#541](https://github.com/riiid/prism/pull/541) | plan-35 컷 단위 재측 + 계약 세 줄 보충 + changeset | squash 머지 `dab57ef2` |
| [#540](https://github.com/riiid/prism/pull/540) | `heading/web-2·3·4` 삭제 정본 반영 + 타이포 스펙 표 실측 | squash 머지 `1a079a42` |

**「검사가 안 돌았다」와 「검사가 실패했다」를 가르는 데 시간을 썼다.** #541은 `validate`만 통과하고 `check`·`storybook`을 포함한 다섯이 `skipped`로 보였는데, 문서만 바뀐 PR이라 경로 필터가 건너뛴 것이다. 잡 목록을 직접 열어 `conclusion`이 `skipped`이지 `failure`가 아닌 것을 확인했다.

**#540의 `UNSTABLE`은 Chromatic 때문이고 그건 필수 검사가 아니다.** `UI Review`·`UI Tests` 둘이 「Update your plan to resume」로 계속 pending이라 머지 상태가 `UNSTABLE`로 뜬다. `check`·`storybook`·`validate` 셋은 전부 통과했고 머지도 그대로 됐다. **`mergeStateStatus` 한 값만 보고 막힌 것으로 읽으면 안 된다** — 어느 검사가 왜 pending인지까지 봐야 한다.

`.changeset/hungry-pandas-repeat.md`가 patch로 들어갔으니 릴리스 PR [#539](https://github.com/riiid/prism/pull/539)(`chore: 0.44.0`)가 받아 간다. `origin/main`에서 계약 두 파일의 새 줄과 changeset을 직접 확인했다.

**뒷정리.** 워크트리 `.claude/worktrees/plan35-xref`와 로컬 브랜치를 지웠다(미커밋 없음). 원격 브랜치는 머지 때 이미 지워져 `push --delete`가 「remote ref does not exist」로 끝났는데 **이건 실패가 아니라 지울 것이 없었다는 뜻이다.** 다른 세션 워크트리는 손대지 않았다.

---

## 이미지 칩이 파일명을 통째로 버리고 있었다 — 정본을 다시 읽으니 툴팁 자리였다

앞 절에서 첨부 에러 칩 사유 툴팁을 포팅하고 남은 것을 보고했더니 주미님이 「응 진행해 피그마도 해도되」라고 하셨다. 승인 없이 집을 수 있는 것은 **전체 파일명 hover 툴팁 하나**였고, 나머지는 전부 피그마 마스터가 먼저거나 PM 판단이다.

### 무대가 실패 칩이 아니라 정상 칩이었다

계약에는 「전체 파일명 hover 툴팁(`7708:60662` ①, 500ms 유지)은 아직 코드에 없다」로만 적혀 있었다. 정본을 다시 열어 보니 ①의 무대가 `Type=photo`·`state=Default` 칩(`7708:60665`)이다. **이건 실패와 무관한 정상 이미지 칩 기능이고, 코드는 그동안 이미지 칩에서 `name`을 아예 안 쓰고 있었다.** 파일 칩은 이름을 칩 안에 그리는데(`AttachmentFile.tsx`), 이미지 칩은 56px이라 자리가 없어서 그냥 버렸던 것이다. 여러 장을 올리면 어느 칩이 무슨 파일인지 볼 방법이 없었다.

### 지연을 칩이 쥔다

`Tooltip`의 기본 hover는 즉시 열려서 ①을 못 낸다. 그래서 칩이 500ms 타이머를 쥐고 `open`으로 넘긴다. **지연이 있는 이유가 「스쳐 지나갈 때 안 뜨는 것」이라, 500ms가 도는지보다 200ms만 머물고 떠났을 때 안 열리는지가 진짜 판정이다.**

실패 이미지 칩에는 걸지 않았다. 같은 자리를 어제 붙인 ④ 사유 툴팁(`7708:60672`)이 이미 쓰고, 그 순간 필요한 정보는 이름이 아니라 사유다. 파일 칩도 그대로다 — 이름이 이미 보이고 정본에 파일 칩용 컷이 없다.

모바일은 같은 시간의 long-press인데, **손을 떼면 따라오는 `click`을 캡처 단계에서 삼킨다.** 정본 ④가 「정상 칩을 탭하면 미리보기(③)」라고 정하므로, 안 삼키면 파일명을 보려던 동작이 미리보기까지 연다. 닫는 시점은 정본이 long-press 쪽을 안 적어서 hover와 대칭으로 정하고 그 판단을 계약에 적었다.

### 게이트가 다 통과해도 이 변경은 안 잰다

빌드·타입체크·테스트 371개·`pack:check`·스토리북·데모·문서 검증이 전부 통과했지만 **jsdom은 CSS를 안 그려서 hover 규칙이 통째로 죽어도 초록불이다.** 정적 빌드를 띄워 Chromium으로 여덟 경로를 쟀다.

| 경로 | 결과 |
| --- | --- |
| hover 300ms | `display:none` · opacity 0 |
| hover 700ms | `data-open="true"` · opacity 1 |
| 이탈 50ms / 300ms | 페이드 중(0.94) / `display:none` |
| 200ms만 머물고 떠난 뒤 700ms | 안 열림 |
| 실패 칩 hover 800ms | 파일명 닫힘 · 「5MB 초과」만 열림 |
| 파일 칩 hover 800ms | 파일명 없음 |
| 포커스 60ms → Tab | 열림 → 닫힘 |
| 터치 long-press 300 / 700 / 손 뗀 뒤 | 닫힘 / 열림 / 닫힘 |

**한 번 잘못 잰 자리가 있다.** 처음 판정에서 blur가 안 걸린 것처럼 보였는데, `document.body.focus()`가 포커스를 실제로 안 옮긴 것이었다. `Tab` 키로 다시 재니 닫혔다. 재는 방법이 틀려서 나온 「결함」이었다.

### 「약 2.5s」는 피그마 쪽을 고쳤다

어제 사유 툴팁을 포팅할 때 정본 ④ 주석은 「약 2.5s」인데 코드에 3000ms를 썼다. `toast.md`와 plan-22가 오류성 알림의 하한을 3초로 정하고 있어서 코드를 내리면 우리 기준을 우리가 깨는 자리였다. 주미님 write 승인을 받아 **주석을 「3s 뒤에 자동으로 닫힌다」로 고치고 ※로 근거 한 줄을 덧붙였다**(`7801:95783`, 382자 → 489자). 프레임 높이 108 · 텍스트 높이 108 · overflow 0으로 클리핑이 없음을 실측했다. **기획서 원문(노션)은 안 건드렸다.**

### PR (socraAI_product design → riiid/prism)

| PR | 내용 | 결과 |
| --- | --- | --- |
| [#545](https://github.com/riiid/prism/pull/545) | `AttachmentChip` 파일명 hover 툴팁 포팅 + 계약·스토리·changeset + plan-59 갱신 | squash 머지 `a72c29ad` |

`validate`·`check`·`storybook` 전부 통과했다. Chromatic `UI Review`·`UI Tests`는 「Update your plan to resume」로 영구 pending인데 필수 검사가 아니다(어제 #540에서 확인한 것과 같다).

### 남긴 것

plan-59의 열린 항목 수는 **14 그대로**다. 오늘 닫은 둘(파일명 툴팁, 「약 2.5s」)은 애초에 체크박스가 아니라 계약에만 적혀 있던 항목이라, 체크박스 집계는 안 움직인다. 그 사실을 상태 줄에 적어 뒀다 — 안 적으면 다음 사람이 「닫았다는데 왜 숫자가 그대로지」를 다시 조사한다.

### 뒷정리와 인계 판단

워크트리 `.claude/worktrees/filename-tooltip`과 로컬 브랜치를 지웠다. **삭제 전에 「잃는 것이 없다」를 SHA가 아니라 내용으로 확인했다** — 스쿼시 머지라 로컬 커밋 `27d9db78`이 `origin/main`에 그대로는 안 보인다. `git show origin/main:…`으로 `AttachmentChip.tsx`의 `FILENAME_TOOLTIP_DELAY_MS`, 계약의 「파일명 툴팁 (2026-09-07)」 절, changeset 셋이 다 올라간 것을 직접 확인하고 지웠다. 원격 브랜치는 머지 때 이미 사라져 있었다.

**남은 14개는 다른 에이전트에게 넘기지 않는다.** 넘길 만한 일이 아니라서다. 열네 항목이 「피그마로 보낼 것」 넷 + PM 확인 여덟 + 사람이 한 번 보면 닫히는 둘인데, **앞의 열둘은 어느 에이전트가 붙어도 진행이 안 된다** — 피그마 마스터 생성·승인이 먼저거나 주미님 결정이 먼저인 항목들이다. 남은 둘(언어 순서 정본 대조 · 스토리북 게시본 육안 확인)은 한 번 열어 보면 끝나는 크기라 인계 브리핑을 쓰는 비용이 작업보다 크다.

**즉 지금은 「누가 이어받나」가 아니라 「주미님이 무엇을 고르시나」가 병목이다.** 인계 문서를 만들어 두면 그 자체가 열린 항목처럼 보여서 집계만 부풀린다.

### 회고 및 인사이트

#### 「아직 없다」고 적어 둔 줄은 무엇이 없는지까지 안 적는다

계약에 「전체 파일명 hover 툴팁은 아직 코드에 없다」가 있었지만, 그 줄만 읽고는 **그게 실패 칩 이야기인지 정상 칩 이야기인지 알 수 없었다.** 실제로 어제 나는 이걸 사유 툴팁의 곁가지로 읽고 있었다. 정본을 열어 무대 노드(`7708:60665`)를 보고서야 정상 칩 기능인 게 드러났다. 미포팅 메모를 남길 때는 **어느 상태·어느 variant인지를 같이 적어야** 다음 사람이 열지 말지를 판단할 수 있다.

#### 결함처럼 보이는 것이 재는 방법의 결함일 때가 있다

blur가 안 걸리는 줄 알고 구현을 의심했는데, 원인은 `body.focus()`였다. **「안 된다」를 봤을 때 코드부터 고치러 가면 멀쩡한 것을 건드린다** — 측정 경로를 한 번 바꿔서 같은 답이 나오는지부터 봤어야 했고, 이번엔 다행히 그렇게 했다.

#### 코드를 우리 기준에 맞추고 피그마를 그대로 두면 다음 사람이 또 잰다

「약 2.5s vs 3s」는 어제 「write 승인 사안이라 손대지 않았다」로 남겼던 항목이다. 그대로 뒀으면 **다음에 이 컴포넌트를 보는 사람이 「코드가 정본과 다르다」로 다시 잡았을 것이다.** 값을 정본과 다르게 쓸 이유가 있으면 그 이유를 정본 쪽에도 적어야 어긋남이 어긋남으로 안 남는다.

---

## 언어 순서를 재러 갔다가 화면 방식이 바뀐 것을 봤다 — 「적용하기」가 폐지돼 있었다

앞 절에서 남긴 셋 중 마지막 하나를 닫았다. 주미님이 #518 시각 변경 셋을 육안으로 확인하시고 「응 확인했어 이어서 진행해」라고 하셔서 이어 붙였다.

### 항목이 물어본 것보다 어긋난 곳이 하나 더 있었다

항목은 「순서가 맞나」만 물었는데, 정본 컷을 여니 **고르는 방식 자체가 달랐다.**

| | 조립본(전) | v1.0.9 정본(후) |
| --- | --- | --- |
| 나열 순서 | `한국어 · English · 日本語` | `日本語 · 한국어 · English` |
| 고르는 방식 | 행을 누르면 체크가 옮겨가고, 아래 「적용하기」를 또 눌러야 반영 | 행을 누르면 확인 팝업 → [변경]을 눌러야 체크가 옮겨감 |
| 대기 구간 | 없음 | 「변경 중」 — 실행 버튼만 잠기고 취소는 평소 그대로 |

순서는 기획서 §4.11(`한국어·영어·일본어`)과도 다르다. 셋이 다 다른데 **이 자리는 피그마가 정본이다** — JP가 주 타깃이라 2026-08-12에 일본어를 앞으로 뺐고 v1.0.9 컷도 그 순서를 유지한다. 레포 기록(`design-system/components/settings-dialog-shell.md:107`)도 같은 순서를 이미 적고 있었다.

「적용하기」 폐지는 페이지 주석 `11634:7514`에 근거가 있다. 고르는 순간 체크가 옮겨가면 이미 바뀐 것처럼 보이는데 버튼을 한 번 더 눌러야 해서 같은 말을 두 번 시켰고, 설정의 다른 행은 누르면 바로 동작하는데 언어 행만 문법이 달랐다.

근거 노드는 제품 파일 `VHdwVCvG0YG7wucLtOWmOg`의 `✅ Login/Setting_1.09`(`9256:88921`)에 있다.

| | 언어 목록 | 변경 확인 팝업 | 변경 중 |
| --- | --- | --- | --- |
| mo | `10407:249055` | `10407:249065` | `11633:7433` |
| web | `10407:249075` | `11633:7516` | `11646:7540` |

**`plan-29`에 적혀 있던 v1.0.8 노드(`8315:1929` 등)는 지금 전부 `null`이다.** 페이지가 v1.0.9로 다시 잘리면서 컷이 새로 생겼다. 「없다」로 보고하지 않고 페이지 구조를 훑어 대체 컷을 찾았다.

### 타입 검사가 못 잡고 렌더가 잡은 것

`ModalDialog`는 확인 버튼에서 `onConfirm()` 뒤에 `onClose()`를 바로 부른다. 그래서 **`onConfirm` 안에서 올린 state를 `onClose`의 클로저가 못 본다.** 처음엔 대기 여부를 별도 `useState`로 뒀는데 팝업이 즉시 닫혀 「변경 중」이 아예 안 나왔다. 네트워크 모달이 이미 쓰던 방식대로 대기 여부를 오버레이 값에 담고 함수형 갱신으로 읽게 고쳤다.

정적 빌드를 띄워 Chromium으로 실제 조작해 쟀다.

| 잰 것 | 결과 |
| --- | --- |
| 행 순서 | `["日本語","한국어","English"]` |
| `aria-checked` | `["false","true","false"]` |
| 「적용하기」 | 0개 |
| 팝업 문구 | 「언어를 日本語로 변경할까요?」 / 「화면에 보이는 글이 모두 日本語로 바뀌어요.」 |
| 대기 중 버튼 | `취소:enabled` · `변경:disabled` |
| 끝난 뒤 설정 값 | `日本語` |

### PR (socraAI_product design → riiid/prism)

| PR | 내용 | 결과 |
| --- | --- | --- |
| [#544](https://github.com/riiid/prism/pull/544) | 언어 화면을 v1.0.9 정본으로 — 순서·확인 팝업·대기 구간 + plan-59 갱신 | squash 머지 `8f2dbe62` |

첫 머지 시도가 `Pull Request has merge conflicts`로 끝났다. 그사이 `origin/main`이 여섯 커밋 앞서 갔고 `plan-59` 상태 줄 한 곳이 충돌이었다. 백업 태그를 걸고 리베이스해 **양쪽 문장을 순서대로 다 살렸다** — main 쪽이 더한 「전체 파일명 hover 툴팁까지 포팅」과 내 쪽의 「언어 순서 대조도 닫아 13이다」가 같은 줄에 들어왔다. 리베이스 뒤 게이트 일곱과 렌더 측정을 다시 돌렸고, 열린 체크박스를 세어 13개인 것을 확인했다.

`check` · `validate` · `storybook` · `Storybook Publish`가 통과했다. Chromatic `UI Review` · `UI Tests`는 「Update your plan to resume」로 영구 pending인데 필수 검사가 아니다(#540 · #545에서 확인한 것과 같다).

### 남긴 것

프로토타입 스토리와 `SettingsRow` 스토리만 고쳤고 **패키지 소스는 안 건드렸다.** 언어 화면을 컴포넌트로 포팅하는 일은 별도이고, 이번 변경으로 포팅 대상 구조가 확정된 셈이다.

`plan-59`의 열린 항목은 **14 → 13**이다. 남은 13은 「피그마로 보낼 것」 넷 + PM 확인 여덟 + 스토리북 게시본 육안 확인 하나다.

### 회고 및 인사이트

#### 항목이 물어본 것만 재면 어긋남의 일부만 본다

이 항목은 「순서를 다시 읽는다」였다. 순서만 확인하고 고쳤으면 **「적용하기」가 남은 화면이 그대로 굳었을 것이다.** 순서가 틀렸다는 것은 그 화면이 v1.0.8 기준으로 조립됐다는 신호였고, 그러면 같은 화면의 다른 것도 옛 기준일 확률이 높다. **항목이 지목한 자리가 아니라 그 자리를 담은 컷 전체를 한 번 보는 편이 싸다.**

#### 「기획서와 피그마가 다르다」가 늘 결함은 아니다

기획서 §4.11은 `한국어·영어·일본어`이고 정본 컷은 `日本語·한국어·English`다. 이건 어긋남이 아니라 **8월에 내려진 결정이 피그마에만 반영된 것**이다. 다른 쪽을 정본으로 삼기 전에 어느 쪽이 더 최근인지부터 봤어야 하는데, 마침 레포 문서에도 같은 순서가 적혀 있어서 교차 확인이 됐다.

#### 렌더로만 잡히는 결함이 또 나왔다

「변경 중」이 안 뜨는 것은 빌드·타입체크·테스트 367개가 전부 초록불이어도 안 잡힌다. 클로저가 오래된 값을 보는 것은 타입이 맞는 코드이기 때문이다. **화면 상태를 다루는 변경은 게이트를 통과한 것과 별개로 한 번 띄워 봐야 한다.**

## Chromatic이 멈춰 있어서 게시본 두 벌을 픽셀로 맞대 봤다

plan-60의 시각 변경을 머지하려는데 시각 QA를 돌릴 방법이 없었다. Chromatic `UI Tests`·`UI Review`가 「Update your plan to resume testing」으로 영구 pending이라 자동 비교가 안 나온다. 필수 검사는 아니라 그대로 머지할 수도 있었지만, **설정 다이얼로그의 사이드바 폭을 건드린 변경이라 눈으로는 봐야 했다.**

### 기준선을 따로 빌드해서 맞댔다

`origin/main`(`077e7fc8`)을 별도 워크트리에 뽑아 스토리북을 한 벌 더 빌드하고, 두 정적 빌드를 각각 다른 포트로 띄워 같은 스토리를 같은 뷰포트로 찍었다. playwright로 촬영하고 sharp로 raw RGBA를 비교해 차이 바운딩 박스를 냈다. 14컷을 봤다.

| 잰 것 | 결과 |
| --- | --- |
| 1440 폭 스토리들 | 바뀐 픽셀이 전부 `x 400~539` · `y 263~386` 안 |
| 1024(태블릿) `--adaptive` | `x 192~331` |
| `pages-settings--mobile`(393) | 차이 0 |
| 393 `--adaptive` | 차이 0 |

`x 400~539`는 폭이 140이다. **사이드바 열 폭과 정확히 같다.** 본문·헤더·푸터는 한 픽셀도 안 움직였고, 모바일 판은 애초에 사이드바가 없어 완전히 같았다. 소스 diff가 한 줄이라 영향 범위를 예상할 수 있었고, 실제 픽셀도 그 범위 안에만 있었다.

### 걸린 것 둘

**`/tmp`에 측정 스크립트를 두면 전역 모듈을 잡는다.** `sharp`와 `playwright` 둘 다 `MODULE_NOT_FOUND`가 났다. 워크트리의 `.pnpm` 스토어 절대 경로로 `require`를 바꿔서 풀었다.

**`gh pr checks --watch`가 안 끝난다.** Chromatic 두 체크가 영영 pending이라 watch가 600초를 다 쓰고 타임아웃됐다. 한 번만 읽어서 판정하면 되는 일이었다.

### PR (socraAI_product design → riiid/prism)

| PR | 내용 | 결과 |
| --- | --- | --- |
| [#533](https://github.com/riiid/prism/pull/533) | plan-60에 시각 QA 방법과 결과를 남겼다 | squash 머지 |

## plan-59를 세 번째로 재니 「없다」고 적은 둘이 다 있었다

「피그마로 보낼 것」에 남은 넷이 각각 무엇인지 주미님께 실물로 보여 드리려고 컷을 뽑다가, **둘은 애초에 없는 것이 아니었다는 것을 알았다.**

### 넷에서 둘로 줄었다

| 항목 | 09-06까지의 판정 | 09-07 재측 |
| --- | --- | --- |
| 출처 칩 | DS 검색 0건 · 피그마 선행 유지 | 제품 파일에 컷 셋 + DS 마스터 `Markdown Block`(`3490:1485`) + 코드 `MarkdownLink` 포팅까지 다 있다 |
| 세션 재열람 실패 | DS에 컷 없음 → PM 몫으로 이월 | 제품 파일 `9376:22532`에 `Callout Type=error, Layout=action` 「대화를 불러오지 못했어요」 |
| 수식(LaTeX) | 원문 유지인지 조판인지 피그마 정의 먼저 | 기획서 §3.4.9가 「원문 그대로 (인라인·블록 모두)」로 이미 골랐다 |
| 표 끝단 페이드 | 마스터에 없음 · 신설 여부 결정 | `Edge Band`(`4432:2063`)에 `Edge × Surface` 축이 이미 있다 |

출처 칩은 남은 것이 **결정 한 줄**이다 — 출처를 일반 웹링크와 같은 `Markdown Block`으로 계속 둘지, 전용 마스터로 가를지. 세션 재열람 실패와 수식은 승인이 필요 없는 **포팅**으로 내려갔다.

표 끝단 페이드는 범위가 줄었지만 결정이 하나 남았다. 기존 좌우 밴드를 그대로 붙일지, 짙은 끝을 1.0으로 올린 표 전용 램프를 더할지다. **후자면 토큰 신설이 따라온다** — 그라데이션 스톱 셋이 전부 변수에 물려 있어서(`semantic/surface/fade-10·50·90`) 값을 노드에 직접 쓰면 이 컴포넌트만 바인딩이 끊긴다.

읽기 전용 정보 행에는 갈래를 하나 더했다. `Settings/Row`(`1357:1910`)와 `_SettingsTrailing`(`1385:985`) **어느 축에도 chevron을 끄는 BOOLEAN이 없다.** 그래서 정본의 「값 + chevron 없음」은 축이 낸 모양이 아니라 인스턴스 override다. 규칙 52 쪽으로 가기로 하면 읽기 전용 Type 신설이 따라온다.

### 머지가 두 번 막혔다

첫 리베이스에서 네 곳이 충돌했다. 열어 보니 main 쪽 상태 줄이 이미 「다른 세션이 고친 판과 합쳤다」고 적고 있어서, **섞기 전에 내 것만 가려내려고 중단했다.** 대조해 보니 main이 앞서 있었고 같은 항목을 다른 결론으로 이미 닫아 둔 곳이 있었다 — 「생성 중 토글만 비활성」은 코드가 이미 기획서대로 동작한다는 실측으로 09-06에 닫혀 있었다.

그래서 patch를 기계적으로 얹지 않고 **main을 base로 잡은 뒤 내 실측이 main을 실제로 앞서는 곳만 골라 다시 썼다.** 파일을 통째로 옮기지 않았다.

푸시하고 나니 그사이 #544가 머지돼 또 충돌했다. 이번엔 두 곳이었고 둘 다 양쪽을 살리는 자리였다 — 상태 줄은 문장을 잇고, 「남은 확인」은 main이 닫은 항목 뒤에 내 포팅 둘을 끼웠다.

### PR (socraAI_product design → riiid/prism)

| PR | 내용 | 결과 |
| --- | --- | --- |
| [#529](https://github.com/riiid/prism/pull/529) | plan-59 세 번째 재측 — 「피그마로 보낼 것」 넷 → 둘 | squash 머지 `0cd2d77e` |

`validate` 통과. 나머지 검사는 문서 전용 PR이라 skip이다. 충돌 마커 0, 열린 체크박스 13으로 상태 줄이 적은 수와 일치한다.

### 회고 및 인사이트

#### 같은 오판이 한 문서에서 두 번 났다 — 둘 다 한 파일만 봤다

출처 칩도 세션 재열람 실패도 **DS 파일에서 이름으로 찾아 0건이라 「없다」고 적은 것**이었다. 둘 다 제품 파일에 컷이 있었고, 출처 칩은 DS에도 `Citation`이 아니라 `Markdown Block`이라는 다른 이름으로 살아 있었다.

`design-system/rules.md` 41이 「없다고 하기 전 세 곳」(컴포넌트 축 · DS 인벤토리 · **옆 캔버스**)을 이미 적어 뒀는데 매번 세 번째가 빠진다. **컴포넌트는 DS에 먼저 생기는 게 아니라 제품 파일 컷에 먼저 그려지는 경우가 많다.**

그래서 문서의 「어긋난 방식이 셋으로 갈린다」 절에 넷째 갈래로 세우고, 「이 절에 『없다』를 적을 때는 어느 파일 어느 페이지를 봤는지 그 줄에 같이 적는다」를 남겼다. 수치에 요구하는 것(규칙 44-5-2)과 같은 규율이다.

#### 「정본이 없다」로 사람 몫에 올린 것이 실은 있는 것이었다

세션 재열람 실패는 09-06에 「문구도 표면도 정해진 적이 없다」며 PM 확인 항목으로 올라갔다. 기획서에 없다는 뜻으로는 맞지만 **화면에 없다는 뜻으로는 틀렸다.** 정본은 기획서 하나가 아니다.

AGENTS.md가 「확인 못 하면 무엇이 잘못되나를 먼저 적는다」고 요구하는데, 그 줄은 실제로 적혀 있었다. 적었는데도 걸러지지 않은 이유는 **질문 자체가 성립하는지를 안 봤기 때문**이다. 「문구를 정해 주세요」는 문구가 없을 때만 질문이 된다.

#### 하루에 여러 세션이 같은 문서를 고치면 리베이스가 판정 작업이 된다

`plan-59` 하나를 오늘만 세 세션이 고쳤다. 충돌이 나면 기계적으로 「양쪽 다 살리기」를 할 수 없다 — **같은 항목을 서로 다른 결론으로 닫아 놨을 수 있기 때문이다.** 이번에는 「생성 중 토글만 비활성」이 그랬다. 내 쪽은 열어 두고 있었고 main은 닫아 놨는데, 닫은 근거가 더 나았다(코드가 이미 그렇게 동작한다).

**충돌을 풀기 전에 상대 쪽이 그 항목을 어떻게 판정했는지부터 읽는다.** 줄 단위로 합치면 문서가 자기 모순을 갖게 된다.

## plan-59의 「물어봐야 한다」던 항목이 이미 정본에서 정리돼 있었다 — 정본이 v1.0.10으로 올라갔다

plan-59에 남은 15건 중 「코드·문서로 닫히는 것」 다섯만 골라 쟀다. 그중 하나가 **첨부 용량 한도**였는데, 09-04에 「PM 확인 필요」로 올려 둔 항목이었다. 근거는 화면 정의서와 기획서가 서로 다른 값을 적고 있다는 것이었다.

**물을 일이 아니었다.** 노션 정본이 v1.0.9에서 **v1.0.10으로 올라가면서** §3.4.2가 이미 바뀌어 있었다 — 이미지 장당 10MB · 문서 2MB로 정리됐고 페이지 제한은 아예 없어졌다. 「5페이지 초과」 칩도 정본에서 지워졌다. **어긋난 두 문서 중 화면 정의서 쪽이 맞았고 기획서가 뒤늦게 따라온 것**이다.

**그래도 코드는 일부러 안 고쳤다.** 제품 파일 `✅ chat/report_1.09`를 전수로 재니 안내 문구가 아직 v1.0.9 값이다 — 「최대 5장, 장당 5MB」 5곳 · 「최대 1개, 10MB·5페이지」 5곳 · 에러 칩 「5MB 초과」·「5페이지 초과」. 나머지 두 페이지는 0건이다. 코드만 먼저 고치면 피그마보다 앞서 가고, 나중에 피그마가 다른 문구로 정해지면 두 번 고친다. 그래서 **「첨부 한도 문구를 피그마부터 v1.0.10으로」를 새 항목으로 열었다.** 운영 순서가 피그마 > 패키지 > 스토리북이라 이 순서를 뒤집지 않는다.

**교훈은 「PM에게 물을 것」도 낡는다는 것이다.** 「A 때문에 대기」가 A가 사라져도 안 닫히는 것과 같은 축인데, 여기서는 **A가 사라진 게 아니라 A가 이미 답을 냈다.** 정본 버전이 올라갔는지부터 보는 것이 물어보는 것보다 싸다.

**게시본 육안 확인도 같이 닫았다.** 정적 빌드를 띄워 훑다가 `ModalShell`에서 로그인 수단 세 개의 간격이 8로 남아 있는 것을 잡았다 — 정본은 10이고, 간격 확정 때 이 스토리만 빠져 있었다. 열린 항목은 13 → 12가 됐다(닫힘 47 → 49, 새로 연 것 하나).

### 언어 순서는 옆 PR과 겹쳐서 걷어냈다

같이 고쳤던 언어 순서 두 줄(`Prototype.stories.tsx` · `SettingsRow.stories.tsx`)은 **되돌려서 [#544](https://github.com/riiid/prism/pull/544)에 넘겼다.** 그쪽이 순서만이 아니라 「적용하기」 폐지 · 확인 팝업 · 대기 구간까지 같은 파일에서 더 넓게 고치고 있었다. 두 PR이 같은 줄을 각자 고치면 나중 리베이스에서 한쪽이 조용히 되돌아간다. **범위가 겹치면 넓은 쪽에 몰아 준다.** #544는 그 뒤 머지됐다.

### 리베이스를 네 번 했다 — main이 계속 움직였다

`origin/main`이 짧은 사이에 #533·#540·#541·#542·#545·#546·#544·#547·#529로 움직여 PR #543이 반복해서 충돌했다. 충돌 자리는 매번 plan-59의 **상태 줄 한 줄**이었다 — 여러 세션이 같은 문장을 뒤에 이어 붙이는 자리라 그렇다.

**해소는 매번 「양쪽을 다 살린다」로 했다.** main 판을 그대로 두고 내 문장만 뒤에 붙이고, 열린/닫힌 항목 수를 산술로 검산했다. 리베이스 전에는 `backup-p59*` 태그를 걸었다.

**중간에 「내가 main 내용을 되돌렸다」고 잘못 판단한 적이 있다.** diff에서 파일명 툴팁 서술이 `-`로 보여서 내 실수로 읽었는데, `git fetch`를 하고 보니 **#545가 나중에 머지된 것**이었다. 로컬 `origin/main` 참조가 낡으면 diff 방향이 반대로 보인다. **diff로 놀라기 전에 fetch부터 한다.**

## `Google`의 g 꼬리가 잘리고 있었다 — 코드가 얹은 클리핑이었다

주미님이 「Google로 시작하기」 버튼에서 g의 아래 꼬리가 잘려 보인다고 지적했다. 재 보니 **실제 결함이었다.**

**피그마 정본은 잘릴 이유가 없다.** 마스터 `Button/Login`의 라벨은 line-height 100%로 코드와 같은 값인데, 텍스트가 `truncation: DISABLED`이고 프레임도 `clipsContent: false`다. **자를 것이 애초에 없다.**

**어긋난 것은 코드가 따로 얹은 클리핑이다.** 라벨 span에 `leading-none`(=16)과 `truncate`가 같이 걸려 있었다. `truncate`는 말줄임표만 붙이는 게 아니라 `overflow: hidden`을 함께 건다. 16px 글자가 실제로 차지하는 높이는 **18px**이라 줄 상자가 16이면 위아래 1px씩이 잘려 나간다. `g`·`y`·`p`처럼 baseline 아래로 내려가는 글자에서만 눈에 띄어서 그동안 안 보였다.

**`leading-5`(=20)로 줄 상자에만 여유를 줬다.** 버튼이 `items-center`라 상자가 커져도 중심이 같아서 **글자 위치는 그대로다** — 렌더 실측으로 버튼 안 `top 15 · bottom 33`이 변경 전후 동일했다. 폰트 크기도 정렬도 안 건드렸다.

**같은 유형이 더 있는지 전 스토리 508개를 훑었다.** `document.createRange()`로 글리프 상자를 실측해 줄 상자와 비교했다. 걸린 5건은 넘침이 214~8,927px이라 디센더 잘림이 아니라 **스크롤 컨테이너 안의 정상 상황**이었다. `LoginButton` 하나뿐이다.

**여기서 배울 것은 「같은 값인데 결과가 다르다」는 자리다.** 피그마와 코드가 둘 다 line-height 100%로 일치했기 때문에, 계약만 대조하면 이 결함은 영영 안 나온다. **틀린 것은 값이 아니라 코드에만 있는 `overflow: hidden`이었다.** 계약 수치가 맞는지만 보지 말고 렌더를 한 장 찍어 본다.

계약 `login-button.md`의 「라벨: line-height 1」 한 줄도 정정했다 — 피그마 실측과 `leading-5`를 쓰는 이유, 글자 위치가 안 바뀐다는 근거를 같이 적었다. 이대로 두면 다음 사람이 「계약대로 1로 되돌리는」 수정을 한다.

## 답변 대기의 3닷을 시머로 바꿨다. 마스터에는 정지 프레임 한 장뿐이었다

주미님이 게시본을 보고 두 곳을 짚었다. 홈의 약관 고지 자리와, 답변 대기 중에 뜨는 점 세 개다. 점 세 개에는 「내가 디자인 정의한 적 없지」라는 말이 붙었다.

**확인해 보니 사실이었다.** 피그마 `Message Bubble`의 `Status=thinking` variant는 `ThinkingIndicator` 인스턴스를 물고 있었고, 그 마스터에는 **정지 프레임 한 장**만 있었다. 움직임도 타이밍도 색도 정해진 적이 없다. 코드가 정본을 어긴 것이 아니라 **정의가 비어 있던 자리**였다.

### 시안 다섯을 만들어 골랐다

스토리북에 탐색 스토리를 세워 다섯을 나란히 놨다. 확정은 **D안(시머 텍스트)**이고, 「너무 확 퍼지는 거 아니냐」는 지적을 받아 폭은 **중간(문장 폭 55%)**으로 잡았다.

- **새 색을 만들지 않았다.** 브랜드 그라디언트 `color.gradient.brand-fill`이 이미 있어서, 그것을 `background-clip: text`로 글자에 입히고 마스크를 움직여 훑는 방식으로 했다. 램프에 이름이 없는 `#00DC91`·`#0092F8`을 토큰으로 신설하려던 처음 계획은 접었다.
- **폭은 `mask-size` 220%로 환산했다.** 문장 폭의 55%가 실제로 빛나려면 마스크 상자가 그만큼 커야 한다.
- **2400ms는 호흡 계열이라서 골랐다.** plan-46의 모션 대장이 기계적 반복(Spinner·버튼 로딩 점)을 1000ms로, 호흡 계열(Skeleton 펄스)을 2400ms로 나눠 놨다. 대기 표시는 재촉하는 쪽이 아니라 숨 쉬는 쪽이다.
- `motion-reduce`에서는 그라디언트 층을 감춘다. 문구 자체는 그대로 읽힌다. 문구가 화면에 보이므로 sr-only는 두지 않았고, 그라디언트 층에 `aria-hidden`을 걸었다.

### 피그마 마스터도 같은 날 갱신했다

코드만 고치면 다음 사람이 마스터를 보고 되돌린다. read-only 감사로 영향 범위부터 쟀다. **DS와 제품 파일을 통틀어 `Status=thinking` 인스턴스가 0개**라 전파될 화면이 없었다. 제품 파일의 `Message Bubble` 616개는 default 614 · collapsed 2다.

승인을 받고 `1359:899`의 `BubbleBox`에서 `ThinkingIndicator` 인스턴스를 빼고 텍스트 노드를 넣었다. 스타일을 손으로 맞추지 않고 **AI text 변형(`247:79`)의 본문 텍스트를 `clone()`해서** 폰트와 색 변수 바인딩을 통째로 승계했다.

갱신 뒤 코드와 대조한 값이 전부 맞았다.

| 항목 | 코드 실측 | 피그마 |
| --- | --- | --- |
| 글자 크기 | 17px | 17 |
| 줄 높이 | 28.9px (=170%) | 170% |
| 색 | `rgb(83,85,92)` | `neutral/500` `#53555C` |
| 패딩 | 8 · 10 | 8 · 10 |
| 행 크기 | 190×45 | 191×45 |

폭 1px 차이는 글자 렌더 차이다. 주미님이 게시한 뒤 제품 파일에서 세트를 다시 가져와 `BubbleBox` 자식이 `ThinkingLabel`(171×29)로 내려온 것까지 확인했다.

### 약관 고지는 자리와 정렬 둘 다였다

컴포저 바로 아래에 붙어 있던 것을 화면 바닥으로 내리고 가운데 정렬했다. 익명 상태에서만 인라인으로 두고 나머지는 화면 고정이다(`dock={p.auth === "anon" ? "inline" : "screen"}`).

### PR (socraAI_product design → riiid/prism)

| PR | 내용 | 결과 |
| --- | --- | --- |
| [#550](https://github.com/riiid/prism/pull/550) | 대기 표시 시머 전환 + 약관 고지 위치·정렬 + 계약·plan-59 기록 | squash 머지 |

`check`·`validate`·`storybook`·`Storybook Publish`(512 stories) 통과. 리베이스 뒤 `pnpm build` 0 · `pnpm typecheck` 0 · `pnpm test` 370 통과를 다시 확인했다. 워크트리 `/tmp/wt-thinking`과 브랜치는 지웠고, 커밋하지 않은 측정 스크립트 넷은 `/tmp/thinking-measure-scripts/`에 옮겨 뒀다.

### 회고 및 인사이트

#### 「디자인이 구리다」를 결함으로 읽지 않아서 일이 줄었다

주미님 말은 「구현이 틀렸다」가 아니라 「정한 적이 없다」였다. 그래서 고칠 곳을 찾는 대신 **마스터에 무엇이 있는지부터** 봤고, 정지 프레임 한 장뿐인 것을 확인한 다음에야 시안을 만들었다. 코드부터 손댔으면 「피그마와 다르게 만든 것」이 됐을 자리다.

#### 새 토큰을 만들려다 접었다

처음엔 램프에 이름이 없는 두 색을 토큰으로 신설할 계획이었다. 그런데 필요한 것은 색 두 개가 아니라 **그 둘을 잇는 그라디언트**였고, 그건 `gradient.brand-fill`로 이미 있었다. 토큰 이름은 「따로 움직일 것」에만 붙인다는 기준에서도 이 둘은 따로 움직일 일이 없다.

#### Chromatic pending은 이 저장소의 정상 상태다

머지 직전에 `UI Review`·`UI Tests`가 pending으로 남아 판단이 걸렸다. 사유가 「Update your plan to resume」이라 코드 문제가 아니라 **플랜 한도**였고, 이미 머지된 #548도 같은 두 체크가 같은 상태였다. **상태값만 보면 빨간불로 읽히지만 이 둘은 애초에 돌지 않는다.** 다만 그만큼 시각 회귀가 자동으로 안 잡히므로, 게시된 스토리북에서 대기 문구를 직접 재는 것으로 대신했다.

## Quick 카드 글자 계단을 확정했다: 제목 24 / 라벨 15

주미님이 「카드가 커 보인다」며 제목을 22로 내리신 데서 출발했는데, 실측해 보니 원인이 제목이 아니었다.

**제목 아래가 다 커서 제목이 커 보인 것이다.** 선택지 라벨이 `body/lg-m`(17)이라 요약(`markdown/body` 17)과 같은 크기인데 굵기만 Medium이라 요약보다 무거웠고, 제목 22와도 1.29배 차이뿐이라 제목이 목록 머리글처럼 읽혔다. 네 조합(22/17 · 24/17 · 22/15 · 24/15)을 임시 인스턴스로 나란히 찍어 비교했더니, 제목만 올린 컷은 대비가 거의 안 생기고 **라벨을 내린 컷에서 제목이 떠올랐다.**

확정한 계단은 제목 24(`heading/0`, 웹은 `heading/web-0` 26) → 요약 17 → 선택지 라벨 15(`body/md-m`) → 뱃지 14이고 CTA는 15다.

주미님이 「CTA만 유독 15라 작아 보인다」고 짚으신 것도 같은 뿌리였다. 17이 여섯 줄 있다가 15로 떨어지니 CTA만 튄 것이고, 라벨이 15가 되면서 같은 급이 됐다. **CTA 15는 기준으로 삼으면 안 된다.** 이름과 접미어 합이 289px를 넘으면 두 줄로 뛰는 제약 때문에 16에서 내린 값이다.

**카드 높이는 639에서 637로 거의 안 변한다.** 행 높이가 막대 트랙에 묶여 있어서 바뀌는 것은 높이가 아니라 밀도다. 「크다」는 지적을 높이 문제로 읽었으면 엉뚱한 곳을 손댈 뻔했다.

제품 파일 반영은 마스터만 고치고 끝내지 않고 인스턴스에 직접 넣었다. 네 페이지에서 라벨 376개와 제목 31개다.

**문서가 두 세대 낡아 있었다.** `score-bar.md`와 계약이 `heading/web-4`(18)로 적혀 있었는데 실물은 이미 17이었다. 값은 문서 말고 노드에서 읽는다.

## JP 검수 페이지의 CTA가 한국어였던 것은 번역 누락이 아니라 언어 모드 미지정이었다

어제 「CTA 문구가 왜 한국어냐」는 질문을 받고 원인을 쟀다. **변수에는 JP 값이 이미 다 들어 있었다.**

원인은 페이지에 `explicitVariableModes`가 비어 있던 것이다. 지정이 없으면 컬렉션 `defaultModeId`로 풀리는데 그 기본이 KR이다. 제목·요약·라벨은 손으로 일본어를 타이핑한 자리라 모드와 무관하게 일본어였고, **변수에 물린 CTA와 뱃지만 한국어로 남아** 대비 때문에 「번역이 빠진 것」처럼 보였다.

**판정할 때 `node.characters`를 믿으면 안 된다.** 이 값은 모드 해석 전의 저장값을 준다. 언어는 렌더로 보거나 `boundVariables.characters`와 `valuesByMode`로 판정한다.

주미님 승인을 받고 두 페이지(`12682:29060` JP검수 임시 · `12721:90865` JP/다크 검수 임시)에 세 컬렉션(`strings` · `strings-demo` · `strings-screen`)의 JP 모드를 지정했다. 라이트와 다크 렌더 두 장으로 검산했고, 다크 페이지는 `color` Dark가 프레임 수준에 따로 걸려 있어서 그대로 유지됐다.

### 모드를 걸자 CTA가 두 줄로 넘쳤다. 손대지 않기로 했다

Deep CTA 여섯 개 중 다섯이 두 줄이 됐다. 높이 40에서 60으로, 카드가 601에서 621로 늘었다. 원인은 접미어 길이다. JP `のより詳しい回答を受け取る`가 183px으로 KR `의 더 자세한 답변 받기` 132px보다 51px 길다. 데모용 `転職アドバイザー`는 113 + 183 = 296px이라 말줄임 폭 289를 7px 넘긴다.

처음엔 「줄바꿈 자리가 들쭉날쭉해 보인다」고 걱정했는데, 주미님이 「버튼은 리포트당 하나라서 들쭉날쭉이 애매해 보일 일이 있나」라고 되물으셔서 다시 보니 **내 우려가 과했다.** 한 화면에 버튼이 하나뿐이라 비교 대상이 없고, `layoutWrap: WRAP` 구조에서 이름과 접미어가 별도 노드라 줄바꿈 자리가 이미 둘 사이로 고정돼 있다. 두 줄이 되면 오히려 이름이 윗줄에 통째로 드러나 읽기에 낫다.

**KR 페이지 `✅ chat/report_1.09`의 모드 지정이 세 개에서 0개로 바뀐 것을 봤다.** 첫 조회에서는 세 개였는데 재조회하니 비어 있었다. 주미님 실시간 편집으로 판단해 그 페이지는 건드리지 않았다. 의도치 않게 풀린 것이면 되돌린다.

## `deep-cta` 계약이 화살표 아이콘 시절 값에 멈춰 있었다

JP 임계를 재다가 계약 수치와 실측이 25px 어긋나는 것을 발견했다. 계약은 이름 몫 132px인데 실측은 157px이었다.

gap·패딩·아이콘 유무를 다 재 봤더니 전부 0이거나 없었고, **근거 문서 `design-system/components/deep-cta.md`는 이미 158px로 갱신돼 있었다.** 계약만 버튼에 화살표가 있던 시절 값에 남아 있던 것이다.

계약 두 줄을 근거 문서 값으로 맞췄다. 모바일 1줄 132 → **158px**, 2줄 259 → 말줄임 **289px**, 웹 1줄 331 → **357px**이고, 모바일 1줄을 넘는 종수는 12종에서 **1종**(`스토킹·성범죄 피해 전문 상담사` 184px)으로 바뀐다.

**JP 임계 줄을 새로 넣었다.** 접미어가 51px 길어 이름 몫이 그만큼 줄고, 모바일 1줄 임계가 106px이 된다. 말줄임 폭 289px은 언어와 무관하다.

처음에 「JP는 두 줄」이라고 적으려다 주미님이 「JP에서 반드시 2줄은 아니잖아」라고 짚으셔서 고쳤다. 실제로 폴백 `Deep モードでより詳しい回答を受け取る`는 262px으로 한 줄이고(실측 높이 40), KR도 184px짜리 이름은 두 줄이 된다. **언어가 줄 수를 정하는 것이 아니라 이름 길이가 정한다.**

`bash scripts/validate-design-system.sh` 통과를 확인하고 두 파일만 지정해 커밋했다(`94616ab2`, 2 files · +9 / -3). 브랜치는 `docs/contract-usage-v109`이고 **푸시는 하지 않았다.**

### 회고 및 인사이트

#### 「번역이 빠졌다」로 보이는 것이 「모드를 안 걸었다」일 수 있다

화면 절반은 일본어고 절반은 한국어라, 증상만 보면 번역 작업이 덜 끝난 것으로 읽힌다. 실제로는 손으로 타이핑한 자리와 변수에 물린 자리가 갈렸을 뿐이고 변수에는 JP 값이 다 있었다. **어느 자리가 변수를 물고 있는지부터 갈라 보면 번역 진척을 세는 일 자체가 없어진다.**

#### 하루에 같은 종류의 문서 어긋남을 두 번 만났다

`score-bar`는 근거 문서와 계약이 둘 다 낡아 있었고(`heading/web-4` 18인데 실물 17), `deep-cta`는 근거 문서만 갱신되고 계약이 남아 있었다. 같은 수치를 `design-system/`과 `packages/prism/component-contracts/` 두 곳이 각각 적는 구조라서 한쪽만 고치면 조용히 갈린다. **여유가 있을 때 두 폴더의 같은 수치를 훑어 어긋난 곳을 세어 볼 만하다.**

#### 우려를 판단으로 올리기 전에 사용 조건을 봤어야 했다

「줄바꿈 자리가 들쭉날쭉해 보인다」는 걱정은 버튼이 한 화면에 여럿 있을 때만 성립한다. 리포트당 하나라는 것을 먼저 봤으면 안 꺼낼 이야기였다. 넘침 수치를 재기 전에 **그 수치가 무엇을 판정하는지**를 정했어야 한다.
