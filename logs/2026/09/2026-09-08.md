# 2026-09-08

### 미해결 항목

9/7에서 열려 있던 9건을 그대로 옮겼다. 닫힌 9건은 이월하지 않는다. 소절 이름의 「(9/7 밤)」은 그 항목이 만들어진 시점이지 오늘 것이 아니다.

#### 주미님 판단이 필요한 것

- [x] ~~**모션을 어디까지 매끄럽게 다듬을지**~~ — **해소(9/8).** 흡수 → 리포트 경계 하나만 하기로 주미님이 정하셨고 PR #564로 넣었다. 컷 경계 전반은 사용성 재점검 때 다시 본다. 판정은 rAF 프레임 기록으로 했다 — 고치기 전에는 카드 3장이 한 프레임에 0장이 되어 중간 프레임이 0개였고, 고친 뒤에는 21프레임에 걸쳐 opacity가 1.00에서 0.00으로 내려간다(181ms). PR #372 본문의 결정 항목 첫 줄도 함께 닫았다. **reduced-motion 경로는 코드로만 확인했고 렌더로는 안 쟀다**
- [ ] **다크 회색 램프에 스크롤바 자리가 없다** — 알고 고른 값. 다크 회색이 한 단 늘면 그때 다시 잰다 (9/2 → 9/4 이관). **추적 정본은 `design-system/rules.md`의 룰 59 · `component-contracts/chat-history-panel.md` 「목록 스크롤바」 · `src/tokens/styles.css`의 `prism-scroll` 주석 세 곳이다(9/7 정정).** 앞서 `plan-30` §4-12 B행이라고 적었는데 틀렸다 — 그 절은 옛 컴포넌트 세트 인스턴스를 센 자리이고 스크롤바 이야기가 없다. 닫을 때 위 세 곳을 함께 닫는다. **9/7 저녁에 조건이 한 번 발동했는데 안 풀렸다** — `neutral/450`이 늘긴 했지만 다크 450은 `#939599`로 `#86888C`보다 **밝은** 쪽이라 빈 구간(`#303235`~`#86888C`)의 반대편이다. 평소 막대는 `state-disabled-border`라 1.35:1 그대로다. 다음에 램프가 늘 때도 **어느 쪽으로 느는지**를 먼저 본다. **다만 색값은 다시 재 보니 둘 다 바뀌어 있었다** — 9/4 램프 재설계로 막대가 `#303235`에서 `#3B3C3F`로, 패널 표면(`bg-surface-raised`)이 `#191A1D`에서 `#27282B`로 함께 밝아져서 대비만 1.34로 남았다. 위 빈 구간도 이제 `#3B3C3F`~`#939599`다. 정본 세 곳의 표기를 이 값으로 고쳤다
- [ ] **색 램프 남은 45칸을 코드로 포팅할지 — 범위 결정** — 피그마에는 다크 값이 80칸 전부 있는데 코드는 4칸만 가져와 있었다(9/7 실측). **9/8에 화면에서 실제로 쓰이는 라벨·뱃지 계열 30칸을 옮겼다(pink 10 · green 10 · cyan 10, PR #566).** 주미님이 이 범위로 정하셨고, `brand`와 `purple`은 라이트로 굳혀 둔 것이 의도인지 확인이 필요해서 뺐다. 남은 것은 `blue`·`orange`·`purple`·`lime`이며 아직 「거울 단계」로 적혀 있다. **푸는 조건:** 각 계열이 라이트와 같은 값인 것이 의도인지 먼저 가린 다음, 소비처 시각 QA 범위를 재고 착수한다

- [x] ~~**미커밋 92줄 중 무엇을 살릴지**~~ **해소(9/8).** 다시 세니 92가 아니라 98줄이었다(파일별 수치를 손으로 더하다 틀렸고 `awk`로 잡았다). 살릴 몫 셋을 PR [#569](https://github.com/riiid/prism/pull/569)로 올렸고, 나머지 51줄은 main이 이미 같거나 더 정확해서 버렸다. 공유 트리의 미커밋 추적 파일 21개는 되돌려 비웠다. 아래 「셋을 올리고 나머지를 지웠다」 절.
- [ ] **카드가 짧을 때 꼬리 여백을 상한으로 막을지** — Deep 투표 스트림에서 카드가 짧으면 아래 여백이 넓게 남는다(웹 1024에서 570px쯤). 앵커 이동이 목적지에 닿게 하려고 스트림 꼬리에 모자란 만큼 여백을 넣은 결과라, 상한을 두면 짧은 카드에서 앵커가 다시 클램프된다. PR #549에서 넘어왔다
- [ ] **사고 과정 캡슐이 카드 제목을 가리는 것** — 앵커 목적지가 카드 상단이라 sticky 캡슐이 그 위에 겹친다. 앵커를 캡슐 높이만큼 내리면 풀리는데, 그러면 카드가 화면 중앙에서 조금 아래로 앉는다. PR #549에서 넘어왔다

#### 나중에

- [ ] **`Settings/Dialog Shell` 메뉴 행 44 반영 — 게시 자체가 막혀 있다** — 부품 `_SettingsNavItem`을 칩 38 + 클릭 영역 44로 갈랐는데(9/7 밤) 제품 파일 16곳에 안 내려갔다. `✅ Login/Setting_1.09` 인스턴스가 전부 옛 값이다(합격 0/16). **구조 변경이라 직접 넣는 길이 없다** — 인스턴스 안에는 `chip` 프레임을 추가할 수 없다.
  - **9/8에 세 번 눌러 주셨는데 세 번 다 이 파일에 게시가 일어나지 않았다.** 게시본 시각이 `2026-09-04T01:36:56Z`이고 파일 전체 최신 게시도 `2026-09-07T10:37:14Z`다(REST `updated_at`).
  - **게시 창 목록에 이 항목이 아예 안 뜬다**(주미님 확인, 9/8). 그래서 「체크가 빠졌다」도 아니다.
  - **컴포넌트 쪽 원인은 전부 배제했다.** 깨진 인스턴스 0 · 이름 중복 0 · 내부 원격 의존은 대조군과 같음 · 게시 안 되는 부품(`_SettingsTrailing`) 의존은 9/7에 나간 컴포넌트도 동일 · 섹션 게시 속성 없음 · 조상에 `.`/`_` 접두 없음 · variant 1개짜리 세트 셋 중 둘은 정상 게시 · 파일 브랜치 0개. 마스터 값도 살아 있다(`gap=0`, 세 행 44).
  - **시도한 것:** 세트 description 끝에 144자를 덧붙여 게시 대상 변경을 만들었다(9/8, 원문 1,140자는 그대로 두고 append). 그래도 목록에 안 떴다. 그다음 파일 새로고침도 안 통했다.
  - **다음에 할 것:** 세트 이름을 바꿨다 되돌려 목록에 강제로 올린다. 게시를 두 번 눌러야 해서 미뤘다(9/4에 같은 이유로 한 번 접었다). 컴포넌트 키는 유지해야 한다 — 잘라내기·재생성은 인스턴스 16곳이 끊기므로 금지.
  - **급한 건 아니다.** 지금 잘못되는 것은 피그마 안에서 클릭 영역이 38로 남는 것뿐이고, 코드로 옮길 때 44로 만들면 된다. 오늘 안 풀려도 손해가 쌓이지 않는다.
  - **재는 법:** 세트 키 `af384fc5217c9851b1156b26c3e1a74ec8126bbe`로 `importComponentSetByKeyAsync`해 게시본 스냅샷을 보고, 제품 인스턴스는 이름 말고 `mainComponent`의 세트 키로 고른다 — **인스턴스 이름이 `nav-앱`·`nav-계정`으로 override돼 있어 이름으로 찾으면 0개가 나온다.** 합격은 바깥 44 · 칩 38 · 간격 0.
  - **딸린 것:** 세트 description 11~17줄이 문장 중간에 끊겨 있다(「660×540 499×393」 등). 도구 출력이 압축된 흔적이 있어 원문이 정말 깨진 것인지는 확정 못 했다. 압축 없이 받아 확인하고 깨졌으면 복원한다

- [ ] **`Prompt Pill` 세트에 텍스트 프로퍼티를 열지** — 지금은 공개 속성이 `State` 하나뿐이라 칩 문구를 변수로만 바꿀 수 있고, 변수 칸이 비면 그 자리만 옛 데모 문구로 조용히 되돌아간다(9/7에 실제로 났다). 인스턴스 1,042곳이 걸려 있어 별도 plan과 승인이 필요하다

#### 다른 세션·조건부

- [ ] **판단 규칙 중 훅으로 옮길 다음 것** — 44-5-2 훅(#418)이 헛걸림 없는지 며칠 보고 넓힌다. `record-trap` 스킬에는 9/4에 Step 0(「코드로 막을 수 있나」)을 넣었다. **9/7 재측: 「사흘치 발화 기록」이라는 것이 없었다** — 발화 로그는 오늘 05:07Z에 처음 쓰였고 한 줄뿐이다. 세션 4,997건을 되재서 대신 쟀고, 2차 좁힘 뒤 실사용 헛걸림 후보는 `미커밋: 0건` 세 건이었다. 넓힐지는 로그가 며칠 쌓인 뒤에 본다 — 아래 「훅 발화 기록은 사흘치가 아니라 한 줄이었다」 절

#### 인수인계 — plan-59 세션 (9/7 밤)

- [ ] **첨부 한도 문구를 v1.0.10 값으로 — 피그마가 먼저다.** 정본 §3.4.2가 이미지 장당 10MB · 문서 2MB로 올라갔고 페이지 제한은 없어졌는데, 제품 파일 `✅ chat/report_1.09`에 옛 문구가 10곳 남아 있다(안내 5 + 5, 에러 칩 「5MB 초과」·「5페이지 초과」). 코드도 같은 옛 값이다(`ChatInputBar`·`AttachmentChip`·`AttachmentDnD`·`Prototype` 스토리). **풀리는 조건:** 피그마 문구가 v1.0.10으로 갱신되면 코드가 따라간다. 코드부터 고치면 순서가 뒤집혀 두 번 고친다
- [ ] **v1.0.10 전체 델타 재판정 — 아직 승인 안 받았다.** 정본이 v1.0.9에서 v1.0.10으로 올라갔는데 레포 미러 `docs/product-spec/`는 v1.0.9에 멈춰 있고, plan-59의 표도 v1.0.9 기준으로 짜여 있다. 첨부 한도 하나가 바뀐 것을 확인했을 뿐이라 **다른 절도 바뀌었는지는 안 쟀다.** 착수 전에 주미님 승인이 필요하다. 다른 세션이 이미 하고 있는지도 먼저 본다

#### 인수인계 — Quick 카드·JP 언어 모드 세션 (9/7 밤)

- [ ] **`design-system/`과 `packages/prism/component-contracts/`의 같은 수치를 훑어 어긋난 곳 세기** — 아직 승인받지 않았다. 오늘 하루에 두 건이 나왔다(`score-bar`는 양쪽 다 낡음, `deep-cta`는 계약만 낡음). **재는 법:** 계약 문서가 px·개수를 적은 줄을 뽑아 같은 이름의 근거 문서와 대조한다. 어느 쪽이 맞는지는 피그마 노드에서 읽는다
## 게시가 반만 갔다 — 셸만 남았다

어젯밤 작업(빠른 시작 칩 문구 · 게시 대기 14개 대조 · 설정 메뉴 행)은 9/7 로그의 「빠른 시작 칩 문구가 되돌아간 원인은 변수 칸 하나였다」 절에 적었다. 오늘 이어서 한 것만 여기 남긴다.

주미님이 게시하신 뒤 제품 파일에서 두 층으로 쟀다. `QuickStart/Group`은 `CURRENT`가 됐고 게시본 마스터가 `demo/topic-stock`·`demo/topic-nisa`·`demo/topic-saving`을 물고 내려왔다. 반면 `Settings/Dialog Shell`은 여전히 `CHANGED`이고, 키로 다시 임포트한 게시본 스냅샷이 38·38·38에 간격 2였다. `✅ Login/Setting_1.09`의 인스턴스 16곳도 전부 옛 값이다(합격 0/16).

**한 묶음에서 하나는 되고 하나는 안 됐으면 전파 지연이 아니다.** 지연이면 둘 다 늦는다. 게시 창에서 체크가 빠진 것으로 보고 다시 부탁드렸는데, 두 번 더 눌러 주셨는데도 수치가 한 자리도 움직이지 않았다.

### 게시가 실제로 나갔는지는 REST로 재야 알 수 있었다

`getPublishStatusAsync`는 파일 안에서 「내보낼 게 남았다」만 알려 줄 뿐, 게시를 눌렀을 때 그것이 실제로 나갔는지는 말해 주지 않는다. **라이브러리에 올라간 쪽의 시각은 REST로 읽을 수 있다.** `GET /v1/files/:key/components`와 `/component_sets`의 `updated_at`이 그 컴포넌트가 마지막으로 게시된 시각이다(세트는 앞쪽, 단일 컴포넌트는 뒤쪽에 있으니 둘 다 봐야 한다 — `QuickStart/Group`은 세트가 아니라 단일 컴포넌트라 `/component_sets`에만 물으면 「라이브러리에 없음」으로 나온다).

이렇게 재니 정황이 한 번에 갈렸다. `Settings/Dialog Shell`은 `2026-09-04T01:36:56Z`이고, **파일을 통틀어 가장 최근 게시가 `2026-09-07T10:37:14Z`**다. 그 게시에서 `QuickStart/Group`이 나갔고(`10:37:13Z`), 9/8에 세 번 누르신 것은 이 파일에 게시를 한 번도 일으키지 않았다. 「눌렀는데 안 나갔다」가 아니라 **내보낼 항목이 잡히지 않은 채로 눌린 것**이다.

남은 `CHANGED` 9개의 게시 시각을 나란히 놓으면 더 분명하다. `Attachment/Group`은 8/5, `Chart/ProgressBar`는 8/6에 멈춰 있다. **한 달 넘게 같은 항목만 계속 빠지고 있다.** Figma 게시 창은 변경 항목을 기본으로 체크해 두지만 사용자가 끈 선택은 기억한다. 언젠가 이 항목들을 끈 채로 게시했고 그 상태가 그대로 남아 있는 것으로 보인다.

컴포넌트 쪽 원인은 전부 배제했다. 깨진 인스턴스 0개, 이름 중복 0개, 내부에서 무는 원격 컴포넌트도 `lucide:check`뿐이라 게시가 잘 되는 대조군과 다르지 않다. **`Chart/ProgressBar`는 내부 인스턴스가 아예 0개인데도 굳어 있다.** 섹션 단위 제외도 아니다 — `SECTION` 노드에는 게시 관련 속성이 없고, Settings 섹션 4개 중 2개는 `CURRENT`다.

이 건은 평소와 달리 부탁드릴 이유를 한 줄로 적을 수 있었다. 부품 안에 `chip` 프레임을 새로 만든 구조 변경이라 제품 파일에 직접 넣는 길이 없다. 인스턴스 안에는 자식을 추가할 수 없고, 바깥 패딩만 늘리면 회색 칩까지 같이 커져 원래 문제로 되돌아간다. 게시 API가 정말 없는지도 프로토타입까지 열어 확인했다 — `figma.teamLibrary`에는 변수 조회 두 개뿐이고 노드에는 `getPublishStatusAsync` 하나다.

### 게시 창에 아예 안 뜬다 — 오늘은 여기서 접었다

체크가 빠진 것으로 보고 부탁드렸는데, 주미님이 게시 창을 열어 보시니 **`Settings/Dialog Shell`이 목록에 아예 없었다.** 그러면 「눌러도 안 나간다」가 아니라 Figma가 이 컴포넌트를 게시 대상 변경으로 안 보고 있는 것이다. 내 쪽에서는 계속 `CHANGED`로 읽히는데 주미님 목록에는 없으니 두 판정이 갈린 상태다.

컴포넌트 쪽 원인은 하나씩 재서 다 배제했다. 깨진 인스턴스 0개, 이름 중복 0개, 내부에서 무는 원격 컴포넌트도 게시가 잘 되는 대조군과 같다. 게시되지 않는 부품에 기대서 그런가 싶었지만 `_SettingsTrailing`을 무는 `Settings/Row`·`Settings Content / 앱`은 9/7에 정상적으로 나갔다. `SECTION`에는 게시 관련 속성이 없고 조상 이름에 `.`이나 `_` 접두도 없다. variant가 하나뿐인 세트라 그런가 싶었지만 같은 형태인 `SocraAI_logo`·`Scroll FAB`는 둘 다 `CURRENT`다. 파일에 브랜치도 없다. 마스터 값은 멀쩡히 살아 있다(`gap=0`, 세 행 모두 44).

목록에 다시 올리려고 **세트 description 끝에 144자를 덧붙였다.** 설명글도 게시 대상이라 이걸로 잡히기를 기대했는데 그래도 안 떴고, 파일을 새로고침해도 마찬가지였다. 원문 1,140자는 건드리지 않고 뒤에 붙이기만 했으니 지우면 원상복구다. 내용 자체는 어차피 남겨야 할 것이라 그대로 둔다.

**여기서 접었다.** 다음 수는 세트 이름을 바꿨다 되돌려 목록에 강제로 올리는 것인데, 게시를 두 번 눌러야 해서 들이는 품에 비해 얻는 것이 작다. 9/4에도 같은 이유로 접었던 방법이다. **지금 잘못되는 것은 피그마 안에서 클릭 영역이 38로 남는 것뿐이고, 코드로 옮길 때 44로 만들면 된다.** 안 풀린 채로 두어도 손해가 쌓이지 않아서 「나중에」로 내렸다.

### 기록을 정리했다

plan-43에 재발 원인과 게시 대조 결과를 적어 커밋했고(`33a24a0a`), 9/7 워크로그에 절을 넣었다(`af540e6`). 손대지 않고 남긴 셋 중 둘은 이월하지 않고 이유를 적어 닫았다. `Empty State`는 쓰는 곳이 0곳이라 확인해도 막을 것이 없고, 「投資」 라벨 override는 원인이 칩 문구와 달라 JP 문구 검수 트랙 몫이다. 이월한 것은 `Prompt Pill` 텍스트 프로퍼티 하나뿐이다.

### 회고

**「어디까지 했어」를 물으실 때까지 오늘 로그를 안 만들고 있었다.** 어젯밤부터 이어진 작업이라 9/7에만 적었는데, 계획 탭은 그날 로그의 「미해결 항목」 절에서 뽑히므로 오늘 탭이 통째로 안 뜨는 상태였다. 9/5·9/6에 같은 일이 있었고 그때 원인까지 적어 뒀는데도 자정을 넘긴 세션에서 또 났다. **날짜가 바뀌면 작업이 이어지더라도 그날 파일을 먼저 만든다.**

## 배웅 모션은 본문만 붙잡아서는 안 보였다

투표 스트림에서 합의 리포트로 넘어가는 자리가 한 프레임에 툭 끊겼다. 컨테이너 `key`가 갈려 리마운트되기 때문이다. 그래서 컨테이너를 200ms 늦게 갈아 끼우도록 고쳤는데, 다시 재 보니 여전히 카드 3장이 한 프레임에 0장이 됐다.

원인은 렌더가 아니라 데이터에 있었다. 컨테이너는 `shownBody`로 늦췄지만 그 안에 그릴 카드는 `cut.cards`에서 곧바로 왔다. 리포트 컷에는 `cards`가 없으니 카드가 먼저 사라지고 빈 상자만 200ms 페이드한 것이다. 마지막으로 그린 카드 묶음을 `useRef`로 들고 있다가 배웅하는 동안 그대로 다시 그리게 하니 21프레임에 걸쳐 opacity가 1.00에서 0.00으로 내려갔고(181ms), 42ms 뒤에 리포트가 마운트됐다.

**코드만 봤으면 못 찾았을 결함이다.** 첫 수정 뒤에 "부드러워졌다"고 넘어갈 수도 있었는데, 매 프레임 DOM 상태를 문자열로 만들어 변화한 프레임만 남기는 rAF 기록이 "중간 프레임 0개"를 그대로 보여 줬다. 인상이 아니라 프레임 수가 판정이다.

reduced-motion 경로는 코드로만 확인했고 렌더로는 안 쟀다. PR #564 본문에 그대로 적어 뒀다.

## 램프가 2모드가 되면 손으로 뒤집은 시맨틱은 이중 반전이 된다

라벨 뱃지가 다크에서 라이트와 똑같이 보이던 것이 증상이었다. blue 라벨은 배경이 `#DBECFF`에서 `#11406A`로 뒤집히는데 pink 라벨은 양쪽 다 `#FFD8E4`였다. 원인은 램프 쪽이다. 피그마 `color` 컬렉션은 80칸 전부 Light/Dark 2모드인데 코드에는 19칸만 2모드로 들어와 있었고, 다크 색 토큰 393개 중 81개가 라이트와 같은 값이었다.

주미님이 정하신 범위대로 화면에서 실제로 쓰이는 pink·green·cyan 30칸을 피그마에서 읽어 옮겼다. 라이트 30칸은 코드와 한 칸도 어긋나지 않아 순수 추가였다.

옮기자마자 빌드가 막혔다. `callout success text dark`가 2.41로 4.5에 못 미쳤는데, 원인은 사람이 `light: green.600 / dark: green.400`처럼 반대편 칸을 골라 두었던 자리였다. 램프가 스스로 뒤집히는 순간 그 자리는 두 번 뒤집힌다. 시맨틱 두 곳과 컴포넌트 다섯 곳을 한 칸 참조로 접어 풀었다.

green 라벨은 별개로 3.40이 나왔는데 빌드는 조용히 통과했다. `component.tokens.json`의 `contrast` 배열에 없는 짝은 게이트가 아예 보지 않기 때문이다. 원인을 재 보니 다크 `green/100`의 휘도가 0.2269로 형제인 `pink/100`(0.1285)·`cyan/100`(0.1537)보다 눈에 띄게 밝았다. 글자 칸을 `/700`·`/800`·`/900` 어디로 바꿔도 3.24에서 3.53 사이라 배경을 내리는 길밖에 없었고, `label.green.bg` 다크만 `green/50`으로 내려 6.92를 얻었다. 라벨 뱃지 세 쌍을 게이트에도 넣었다.

피그마 램프 자체를 손보는 편이 정공법일 수 있지만 시각 변경이라 쓰지 않았다. `plan-52` §7에 확인 항목으로 남겼다.

## 오늘 연 PR

| 번호 | 내용 |
| --- | --- |
| #563 | 스크롤바 다크 색값을 9/4 램프 재설계 뒤 값으로 문서 세 곳에 다시 적었다 |
| #564 | 투표 스트림에서 리포트로 넘어가는 배웅 모션 |
| #566 | pink·green·cyan 램프 30칸 다크 값 포팅 |
| #567 | 공유 트리에만 있던 문서 커밋 둘을 옮기고 `deep-cta` 화살표 시절 임계값을 정정했다 |
| #569 | 공유 트리에만 있던 드로어 폭 규칙·Quick 카드 글자 계단·`AGENTS.md` 잔비용 절을 옮겼다 |

`check`·`validate`·`storybook`이 전부 통과했다. Chromatic `UI Review`·`UI Tests`만 pending인데 사유가 「Update your plan to resume review」라 요금제 한도이지 코드 실패가 아니다. **주미님 승인을 받아 #563 · #566 · #564 순으로 squash 머지했다**(`508664b7` · `d1c11629` · `f2c532c6`). 앞의 두 런은 다음 커밋이 들어오면서 취소됐으므로 판정은 세 커밋을 모두 담은 마지막 main 런으로 했다. 시각 QA와 reduced-motion 렌더 측정은 아직 안 돌렸다.

### 회고

날짜를 하루 틀리게 적었다. 오늘이 9/8인데 `plan-46`과 스토리 주석, PR #372 본문에 9/7로 적었고 주미님이 "오늘 9월 8일이야"로 짚어 주셨다. 세 곳을 고쳤다. 자정을 넘겨 이어진 세션이라 앞 세션 날짜를 그대로 쓴 것인데, 어제 회고에도 같은 축의 지적("날짜가 바뀌면 그날 파일을 먼저 만든다")이 적혀 있다. **작업 기록에 날짜를 적을 때는 세션 시작 시각이 아니라 지금 날짜를 확인하고 쓴다.**

## Deep 여정 모션 이음매를 머지했다 (00:52)

PR [#549](https://github.com/riiid/prism/pull/549)가 `1049a771`로 squash 머지됐다. 파일 18개에 650줄 추가·105줄 삭제다. PR #372 리뷰의 「모션을 더 매끄럽게 다듬어야 합니다」에서 갈라져 나온 트랙이고, 앵커 이동을 거리에 비례시킨 것, 「다음 고민」 목적지를 다음 카드 상단으로 옮긴 것, 하단 바 아래 여백에서 페이드를 빼지 않기로 계약에 적은 것이 들어 있다.

### 머지 직전에 changeset 누락을 잡았다

이 PR이 `TextDivider.tsx`와 `ReportSurface.tsx`를 바꿔 놓고 changeset을 안 냈는데 CI는 통과했다. 게이트가 `.changeset/`에 파일이 하나라도 있으면 통과하고 **이 PR이 냈는지는 안 보기** 때문이다. 옆 PR 넷(#536·#541·#545·#548)이 쌓아 둔 파일 덕에 그냥 넘어갔다. `patch`로 채워 다시 올렸다.

재는 법은 이렇다. `git diff --name-only origin/main...HEAD -- packages/prism/src`가 안 비었는데 같은 명령의 `.changeset` 쪽이 비어 있으면 써야 한다.

### Chromatic이 멈춰서 게시본 두 벌을 픽셀로 맞댔다

`UI Review`·`UI Tests`가 요금제 한도로 pending이라 전체 시각 비교를 못 돌렸다. 대신 main 게시본과 브랜치 게시본의 공통 스토리 23개를 CDP로 찍어 대조했다. 차이가 난 12개는 전부 `TextDivider`의 `px-4` 제거 때문이고, 라벨은 한 픽셀도 안 밀렸다. 차이 난 행이 선이 있는 행뿐이라는 것이 근거다.

**정지 스냅샷으로는 이 PR의 대부분을 못 본다.** 모션 타이밍·하단 여백·전환 중 흐림은 원리적으로 안 잡힌다. 그래서 스냅샷 비교를 기다리는 대신 머지를 먼저 하고 표적 대조로 갈음했다.

## 공유 트리에만 남아 있던 문서 커밋 둘을 옮겼다

`docs/contract-usage-v109` 워킹트리에 푸시 안 된 커밋이 둘 있었다. 원격은 이미 지워졌고(`[gone]`) main보다 258 앞·28 뒤라 그대로는 못 올린다. 앞선 258 중 어제 것은 이 둘뿐이고 나머지 256은 9/2~9/3에 다른 세션들이 쌓은 것이라, 통째로 밀면 남의 커밋이 딸려 간다.

`origin/main` 위에 새 워크트리를 떼고 둘만 cherry-pick해 PR [#567](https://github.com/riiid/prism/pull/567)로 올렸다. 충돌 없이 붙었고 `git patch-id --stable`이 두 쌍 다 일치한다. 합계도 원본과 같은 43줄 추가·4줄 삭제다.

계약 문서(`component-contracts/deep-cta.md`)를 바꾸므로 changeset을 `patch`로 붙였다. 위에서 잡은 것과 같은 구멍이라 안 내도 CI는 통과한다.

### 미커밋 21개 중 살릴 값이 얼마인지 쟀다

같은 트리에 미커밋 변경 21개가 떠 있었다. **대부분은 「내가 바꾼 것」이 아니라 트리가 28커밋 뒤처져서 생긴 표시였다.** 미커밋 편집이 추가한 줄을 뽑아 `origin/main`의 같은 파일과 한 줄씩 대조하니, 추가 494줄 중 main에 없는 것은 92줄이었다.

| 묶음 | 줄 | 판정 |
| --- | --- | --- |
| 드로어 폭 「화면 폭 − 56」 (스토리 둘 · DS 문서 둘 · plan-10 절) | 21 | 살릴 값. main은 아직 「드로어 337 고정」이다 |
| Quick 카드 글자 계단 제목 24 / 라벨 15 (DS 둘 · 계약 둘) | 14 | 살릴 값. main 계약에 15px이 없다 |
| `AGENTS.md` 「도구 호출 잔비용」 절 | 6 | 살릴 값. main에 아예 없어서 다른 세션이 못 본다 |
| `Thinking/Toggle`·`Progress`를 componentKey로 인용 | 13 | 미확인 |
| 나머지(`rules.md` 일부 · `.gitignore` · `plan-37` · `ScrollReturn`) | 38 | 일부 미확인 |

**버려도 잃을 것이 없는 것도 갈렸다.** `WebShellFlow.stories.tsx`·`WebSidebar.tsx`·`thinking-capsule.md`·`token-contract.md` 넷은 미커밋 편집을 했는데도 결과가 main과 똑같다. 이미 머지된 내용을 이 트리에서 다시 만든 것이다.

`rules.md`의 「58. 오버레이는 트리거와 배경이 정체다」는 **main에 이미 60번으로 들어가 있다.** 날짜도 같은 2026-09-02다. 내 트리는 규칙이 58개인데 main은 66개라, 그대로 올리면 58번이 둘이 된다. `AGENTS.md`의 규칙 58 색인 줄도 같은 이유로 낡았다. plan-10 아카이브 분리(−4,445줄)는 9/7에 이미 「올릴 게 아니다」로 판정이 끝난 건이다.

### 셋을 올리고 나머지를 지웠다 (주미님 "응 해줘 다.")

살릴 몫 셋을 `origin/main` 위 새 워크트리에서 세 커밋으로 갈라 PR [#569](https://github.com/riiid/prism/pull/569)로 올렸다. 13파일 · 64줄 추가 · 15줄 삭제이고 `check`·`validate`·`storybook`이 전부 통과했다. 계약 둘이 바뀌어 changeset을 `patch`로 붙였다. 머지 커밋은 `6b86db49`다.

**계약 충돌 넷은 축을 갈라 합쳤다.** `report-quick`·`score-bar`는 크기와 색이 서로 다른 날짜의 확정이라, 한쪽을 통째로 고르면 다른 축이 되돌아간다. 크기는 9/7 확정값(제목 24 · 라벨 15)을, 색은 main의 9/4 plan-52 A안(`text-primary`)을 각각 살렸다. 충돌 마커가 남았는지는 워크트리 추적 파일 전체를 `git grep`으로 훑어 0건을 확인했다(해소 전에 같은 패턴을 돌렸을 때는 네 파일이 걸렸으니 패턴 오기는 아니다).

나머지 51줄은 지웠다. 되돌리기 전에 공유 트리 전체 diff 5,673줄을 `/tmp/uncommitted-backup-0908.patch`로 백업했고, 추적 파일 21개의 미커밋 편집을 되돌려 트리를 비웠다. **오늘 손댄 파일이 하나도 없어서 옆 세션과 겹칠 자리가 아니었다** — 가장 최근 수정이 9/7 19:14다. untracked 다섯은 남의 작업일 수 있어 건드리지 않았다.

**스테이지가 남아 있던 탓에 커밋이 한 번 잘못 갈렸다.** 파일을 지정해 `git add`했는데도 앞 단계에서 이미 스테이지돼 있던 것들이 함께 들어가, 첫 커밋이 11개 파일을 통째로 삼켰다. AGENTS.md가 경고하는 「메시지와 실제 내용이 어긋난다」가 이렇게 난다. **`git add` 전에 인덱스가 비어 있는지 본다.**

**되돌리려고 `reset --mixed origin/main`을 썼다가 base가 갈렸다.** 그사이 옆 세션이 #563·#566·#564를 머지해 `origin/main`이 앞서 있었고, 새 main 기준으로 리셋하니 main이 가져온 변경까지 전부 역방향 수정으로 잡혔다. 파일 26개가 갑자기 `M`으로 떠서 알았다. **되돌릴 자리는 브랜치 이름이 아니라 reflog에서 읽은 base 커밋이다.** 커밋 셋은 reflog에 그대로 있어서 잃은 것은 없다.

### 회고

**「M 표시가 21개」를 「내가 고친 게 21개」로 읽으면 안 된다.** 뒤처진 브랜치에서는 main이 앞서간 것도 전부 수정으로 보인다. 파일별로 재니 실제 내 몫은 92줄이고, 그중 넷은 아예 0줄이었다. 세어 보기 전에 지우거나 통째로 올렸으면 어느 쪽으로든 틀렸다.

**같은 규칙이 번호만 다르게 두 벌 살아 있었다.** 58번이 이 트리에서는 오버레이 규칙이고 main에서는 마스터 값 규칙이다. 규칙을 새로 매길 때 `origin/main`을 안 보고 로컬 파일 끝 번호에 이어 붙이면 이렇게 된다. 규칙 번호는 로컬이 아니라 `origin/main`에서 잰다.
